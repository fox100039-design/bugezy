# BugEzy Job — 2026-06-22

> 接續 job-0620.md（PM-48~53）
> 上架前功能補齊

---

### 🟡 PM-54：MCP 每次呼叫附 token 估算 + 省錢對比

**目標**：每個 MCP tool 回應的最後，自動附上本次查詢的 token 消耗估算，以及跟 Claude in Chrome 的省錢對比。讓客戶每次用都看到「BugEzy 省了 95%」。

---

#### §1 Token 估算函式

在 server/src/index.ts 加一個通用的 token 估算函式：

```ts
interface TokenEstimate {
  bugezyTokens: number;
  chromeTokens: number;
  savedPercent: number;
  bugezyUSD: string;
  chromeUSD: string;
}

function estimateTokens(responseText: string, toolName: string): TokenEstimate {
  // 估算方式：response 字串長度 / 3.5 ≈ token 數
  const bugezyTokens = Math.ceil(responseText.length / 3.5);
  
  // Claude in Chrome 對比基準（同場景的估算倍率）
  const chromeMultiplier: Record<string, number> = {
    'list_reports': 5,           // Chrome 要讀整頁 DOM 找報告
    'get_report_overview': 10,   // Chrome 要讀整頁
    'get_console_logs': 20,      // Chrome 讀全量 console
    'get_network_errors': 20,    // Chrome 讀全量 network
    'get_voice_transcript': 50,  // Chrome 沒有語音功能，要人工描述
    'get_page_info': 15,
    'get_rrweb_summary': 10,
    'get_rrweb_events': 2,       // 都是大量資料
    'get_live_errors': 30,       // Chrome 要開 DevTools 掃全頁
    'get_terminal_logs': 40,     // Chrome 完全做不到
  };
  
  const multiplier = chromeMultiplier[toolName] || 10;
  const chromeTokens = bugezyTokens * multiplier;
  const savedPercent = Math.round((1 - bugezyTokens / chromeTokens) * 100);
  
  // 價格：Claude Sonnet ~$3/MTok input, ~$15/MTok output
  // 簡化用 $8/MTok 平均
  const pricePerToken = 8 / 1_000_000;
  const bugezyUSD = (bugezyTokens * pricePerToken).toFixed(4);
  const chromeUSD = (chromeTokens * pricePerToken).toFixed(4);
  
  return { bugezyTokens, chromeTokens, savedPercent, bugezyUSD, chromeUSD };
}

function formatTokenFooter(est: TokenEstimate): string {
  return `\n\n---\n📊 Token 估算：~${est.bugezyTokens.toLocaleString()} tokens ≈ $${est.bugezyUSD}\n💡 同場景 Claude in Chrome：~${est.chromeTokens.toLocaleString()} tokens ≈ $${est.chromeUSD}\n✅ BugEzy 為你省了 ${est.savedPercent}%`;
}
```

---

#### §2 每個 MCP Tool 加上 token footer

在每個 tool 的 return 之前，計算 token 並附在 response 後面：

```ts
// 範例：get_console_logs
server.tool('get_console_logs', '...', { report_id: z.string() }, async ({ report_id }) => {
  // ...existing 邏輯...
  const responseText = JSON.stringify(consoleLogs, null, 2);
  const est = estimateTokens(responseText, 'get_console_logs');
  
  return {
    content: [{
      type: 'text',
      text: responseText + formatTokenFooter(est),
    }],
  };
});
```

**對所有 10 個 tool 都做同樣的處理。**

---

#### §3 不改的東西
- extension/：不動
- web/：不動
- cli/：不動

#### Build + Deploy
```
cd server && npx wrangler deploy
```

#### 驗收

1. 在 Claude Chat 問：「讀我最新的 BugEzy 報告的 console logs」
2. AI 回應最後應看到：
```
---
📊 Token 估算：~500 tokens ≈ $0.0040
💡 同場景 Claude in Chrome：~10,000 tokens ≈ $0.0800
✅ BugEzy 為你省了 95%
```

---

### 🔵 DONE-54：MCP 每次呼叫附 token 估算 + 省錢對比 ✅ 已部署 + 線上驗證

**改動**：只改 `server/src/index.ts`（extension/web/cli 不動，與規格一致）。

- **§1** 新增 `estimateTokens(responseText, toolName)`：`bugezyTokens = ceil(len/3.5)`；依 `chromeMultiplier`（各 tool 對比 Claude in Chrome 的倍率，list_reports=5…get_terminal_logs=40）算 `chromeTokens`、`savedPercent`、$（$8/MTok 平均）。加了 `chromeTokens>0` 防 0 除（空資料不會 NaN）。`formatTokenFooter(est)` 產出規格的三行 footer。
- **§2** 新增 `txtWithTokens(data, toolName)`（同 `txt()` 但附 footer）；**全部 10 個 tool 的「資料回應」**改用它（list_reports / get_report_overview / get_console_logs / get_network_errors / get_voice_transcript / get_page_info / get_rrweb_summary / get_rrweb_events / get_live_errors / get_terminal_logs）。錯誤/stale/找不到 等訊息維持 plain `txt()`（不附 footer——那不是省錢場景）。

**tsc / deploy**：`npx tsc --noEmit` ✅ + `npx wrangler deploy` ✅（Version `2ded6696`）。

**線上實測（真實 MCP 連線 `get_terminal_logs`）**：回應尾端正確附上
```
---
📊 Token 估算：~74 tokens ≈ $0.0006
💡 同場景 Claude in Chrome：~2,960 tokens ≈ $0.0237
✅ BugEzy 為你省了 98%
```
（×40 倍率 → 98%，金額與 token 數皆吻合公式）。

**待 FOX 驗收**：Claude Chat 任一 BugEzy 查詢（如 console logs）回應末尾應見 token 估算 + 省錢對比。

---

### 🟡 PM-55：報告頁顯示各區塊 token 估算

**目標**：edit-report 編輯頁和 web 報告頁底部，顯示這份報告各區塊的 token 估算，讓客戶上傳前就知道 AI 讀這份報告要花多少 token。

---

#### §1 edit-report.html 改動

在上傳按鈕上方加 token 估算面板：

```html
<div id="tokenEstimate" class="section token-panel">
  <h2>📊 Token 估算</h2>
  <div id="tokenBreakdown"></div>
</div>
```

CSS：
```css
.token-panel {
  background: var(--panel, #1a1a2e);
  border: 1px solid rgba(124, 58, 237, 0.3);
  border-radius: 12px;
  padding: 16px;
  margin-bottom: 16px;
}
.token-row {
  display: flex;
  justify-content: space-between;
  padding: 4px 0;
  font-size: 13px;
  color: #ccc;
}
.token-row.total {
  border-top: 1px solid #2a2a3e;
  margin-top: 8px;
  padding-top: 8px;
  font-weight: 700;
  color: #fff;
}
.token-row .label { color: #a78bfa; }
.token-save {
  margin-top: 12px;
  padding: 10px;
  background: rgba(16, 185, 129, 0.1);
  border: 1px solid rgba(16, 185, 129, 0.3);
  border-radius: 8px;
  font-size: 13px;
  color: #10b981;
  text-align: center;
}
```

---

#### §2 edit-report.ts 改動

payload 載入後計算 token：

```ts
function renderTokenEstimate(payload: RecordingPayload) {
  const container = document.getElementById('tokenBreakdown');
  if (!container) return;

  // 各區塊的 token 估算
  const voiceText = payload.voiceTranscript.map(s => s.text).join('');
  const consoleText = JSON.stringify(payload.consoleLogs);
  const networkText = JSON.stringify(payload.networkErrors);
  const descText = payload.description || '';
  const markersText = JSON.stringify(payload.markers || []);

  const estimates = [
    { label: '語音記錄', text: voiceText, icon: '🎤' },
    { label: 'Console logs', text: consoleText, icon: '🖥' },
    { label: 'Network errors', text: networkText, icon: '🌐' },
    { label: '補充說明', text: descText, icon: '📝' },
    { label: '時間軸標記', text: markersText, icon: '📌' },
  ];

  let totalTokens = 0;
  let html = '';

  for (const est of estimates) {
    const tokens = Math.ceil(est.text.length / 3.5);
    if (tokens > 0) {
      totalTokens += tokens;
      html += `<div class="token-row">
        <span class="label">${est.icon} ${est.label}</span>
        <span>~${tokens.toLocaleString()} tokens</span>
      </div>`;
    }
  }

  // rrweb 摘要（get_rrweb_summary 只回筆數，很小）
  const rrwebSummaryTokens = 30;
  totalTokens += rrwebSummaryTokens;
  html += `<div class="token-row">
    <span class="label">📹 DOM 摘要</span>
    <span>~${rrwebSummaryTokens} tokens</span>
  </div>`;

  // 總計
  const bugezyUSD = (totalTokens * 8 / 1_000_000).toFixed(4);
  const chromeTokens = totalTokens * 15;
  const chromeUSD = (chromeTokens * 8 / 1_000_000).toFixed(4);
  const savedPercent = Math.round((1 - totalTokens / chromeTokens) * 100);

  html += `<div class="token-row total">
    <span>AI 讀取總計</span>
    <span>~${totalTokens.toLocaleString()} tokens ≈ $${bugezyUSD}</span>
  </div>`;

  html += `<div class="token-save">
    💡 同場景 Claude in Chrome：~${chromeTokens.toLocaleString()} tokens ≈ $${chromeUSD}<br>
    ✅ BugEzy 為你省了 ${savedPercent}%
  </div>`;

  container.innerHTML = html;
}
```

在 payload 載入完成後呼叫：
```ts
renderTokenEstimate(payload);
```

---

#### §3 不改的東西
- server / inject / content / background / cli：不動
- web 報告頁之後再做（先做 edit-report）

#### Build
```
cd extension && npm run build
```

#### 驗收

1. 錄製一段（用測試專頁觸發幾個 error + 講語音）→ 停止 → 編輯頁
2. 上傳按鈕上方應顯示：
```
📊 Token 估算
  🎤 語音記錄        ~150 tokens
  🖥 Console logs    ~300 tokens
  🌐 Network errors  ~200 tokens
  📝 補充說明         ~50 tokens
  📹 DOM 摘要        ~30 tokens
  ─────────────────────
  AI 讀取總計  ~730 tokens ≈ $0.0058

  💡 同場景 Claude in Chrome：~10,950 tokens ≈ $0.0876
  ✅ BugEzy 為你省了 93%
```

---

### 🔵 DONE-55：報告頁顯示各區塊 token 估算 ✅

**改動**：`extension/src/edit-report.html` + `edit-report.ts`（server/inject/content/background/cli/web 不動，與規格一致）。

**§1 edit-report.html**：補充說明下方、上傳按鈕上方插入 `#tokenEstimate`（`.token-panel`：「📊 Token 估算」+ `#tokenBreakdown`）＋對應 CSS（`.token-row`/`.token-row.total`/`.token-save`，沿用 `--panel/--line/--success` 變數）。

**§2 edit-report.ts**：新增 `renderTokenEstimate(payload)`，於 `init()` 載入 payload + initMiniPlayer 後呼叫。各區塊以 `ceil(len/3.5)` 估 token（語音/console/network/補充說明/時間軸標記，>0 才列）＋固定 DOM 摘要 30 tokens；總計 + 對比 Claude in Chrome（×15、$8/MTok）+ 省 % + 防 0 除。

**據實微調（避型別錯）**：規格用 `payload.description`，但 `RecordingPayload` 無 `description` 欄（描述是上傳時才併入）→ 改讀 `descInput.value`（更正確且 tsc 過）；時間軸標記用模組 `markers` 陣列（編輯頁實際標的）而非 `payload.markers`。

**build**：`npm run build` ✅ ＋ `tsc --noEmit` ✅。

**待 FOX 瀏覽器驗收**：錄製/回溯 → 編輯頁上傳鈕上方出現各區塊 token 明細 + 總計 + 「BugEzy 為你省了 X%」。（web 報告頁的同款顯示規格註明之後再做。）

---

### 🟡 PM-56：月度使用量統計（Supabase 記錄 MCP 呼叫）

**目標**：追蹤每次 MCP 呼叫的 token 消耗，儲存到 Supabase，提供月度統計 API。未來可在報告頁或 popup 顯示月度用量。

---

#### §1 Supabase Schema

FOX 手動跑：

```sql
CREATE TABLE IF NOT EXISTS mcp_usage (
  id SERIAL PRIMARY KEY,
  tool_name TEXT NOT NULL,
  tokens_estimated INT NOT NULL,
  chrome_tokens_estimated INT NOT NULL,
  report_id TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 索引加速月度查詢
CREATE INDEX IF NOT EXISTS idx_mcp_usage_created ON mcp_usage(created_at);
```

---

#### §2 Server 改動 — 記錄每次 MCP 呼叫

在 `estimateTokens()` 呼叫後，非同步寫入 Supabase：

```ts
async function logMcpUsage(env: Env, toolName: string, est: TokenEstimate, reportId?: string) {
  try {
    await fetch(`${env.SUPABASE_URL}/rest/v1/mcp_usage`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'apikey': env.SUPABASE_ANON_KEY,
        'Authorization': `Bearer ${env.SUPABASE_ANON_KEY}`,
        'Prefer': 'return=minimal',
      },
      body: JSON.stringify({
        tool_name: toolName,
        tokens_estimated: est.bugezyTokens,
        chrome_tokens_estimated: est.chromeTokens,
        report_id: reportId || null,
      }),
    });
  } catch {
    // 記錄失敗不影響 MCP 回應
  }
}
```

在每個 MCP tool 的 return 前呼叫（非同步，不 await 避免拖慢回應）：

```ts
// 不 await，背景執行
logMcpUsage(env, 'get_console_logs', est, report_id);
```

---

#### §3 月度統計 API

**GET /api/usage/monthly**：回傳當月的使用量統計

```ts
if (url.pathname === '/api/usage/monthly' && request.method === 'GET') {
  // 取當月第一天
  const now = new Date();
  const monthStart = new Date(now.getFullYear(), now.getMonth(), 1).toISOString();
  
  const res = await fetch(
    `${env.SUPABASE_URL}/rest/v1/mcp_usage?select=tool_name,tokens_estimated,chrome_tokens_estimated&created_at=gte.${monthStart}`,
    {
      headers: {
        'apikey': env.SUPABASE_ANON_KEY,
        'Authorization': `Bearer ${env.SUPABASE_ANON_KEY}`,
      },
    }
  );
  const rows = await res.json();

  // 彙總
  let totalCalls = 0;
  let totalTokens = 0;
  let totalChromeTokens = 0;
  const byTool: Record<string, { calls: number; tokens: number }> = {};

  for (const row of rows as any[]) {
    totalCalls++;
    totalTokens += row.tokens_estimated;
    totalChromeTokens += row.chrome_tokens_estimated;
    const t = row.tool_name;
    if (!byTool[t]) byTool[t] = { calls: 0, tokens: 0 };
    byTool[t].calls++;
    byTool[t].tokens += row.tokens_estimated;
  }

  const savedTokens = totalChromeTokens - totalTokens;
  const savedUSD = (savedTokens * 8 / 1_000_000).toFixed(4);
  const totalUSD = (totalTokens * 8 / 1_000_000).toFixed(4);

  return new Response(JSON.stringify({
    month: `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}`,
    totalCalls,
    totalTokens,
    totalChromeTokens,
    totalUSD,
    savedTokens,
    savedUSD,
    savedPercent: totalChromeTokens > 0 ? Math.round((1 - totalTokens / totalChromeTokens) * 100) : 0,
    byTool,
  }), {
    headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' },
  });
}
```

---

#### §4 MCP 新增 Tool（可選）

讓 AI 也能查月度用量：

```ts
server.tool('get_usage_stats', '取得當月的 MCP 使用量統計（呼叫次數、token 消耗、省了多少）', {}, async () => {
  const res = await fetch(`${API_BASE}/api/usage/monthly`);
  const data = await res.json();
  const text = `📊 ${data.month} 月度使用報告
MCP 呼叫次數：${data.totalCalls} 次
BugEzy Token 消耗：~${data.totalTokens.toLocaleString()} tokens ≈ $${data.totalUSD}
同場景 Claude in Chrome：~${data.totalChromeTokens.toLocaleString()} tokens
省下的 Token：~${data.savedTokens.toLocaleString()} tokens ≈ $${data.savedUSD}
節省比例：${data.savedPercent}%`;

  const est = estimateTokens(text, 'get_usage_stats');
  logMcpUsage(env, 'get_usage_stats', est);
  return { content: [{ type: 'text', text: text + formatTokenFooter(est) }] };
});
```

---

#### §5 不改的東西
- extension / web / cli：不動

#### Build + Deploy
```
cd server && npx wrangler deploy
```

#### 驗收

1. FOX 先跑 SQL 建 mcp_usage 表
2. 部署後用 MCP 呼叫幾次（get_report_overview、get_console_logs 等）
3. 查 API：`curl https://bugezy-api.bugezy-api.workers.dev/api/usage/monthly`
4. 應看到：
```json
{
  "month": "2026-06",
  "totalCalls": 3,
  "totalTokens": 850,
  "totalChromeTokens": 12750,
  "totalUSD": "0.0068",
  "savedTokens": 11900,
  "savedUSD": "0.0952",
  "savedPercent": 93,
  "byTool": { ... }
}
```

---

### 🔵 DONE-56：月度使用量統計（Supabase 記錄 MCP 呼叫）✅ 已部署 + 線上驗證

**改動**：`server/src/index.ts` + `server/schema.sql`（extension/web/cli 不動，與規格一致）。

- **§2 `logMcpUsage(env, toolName, est, reportId?)`**：POST 到 Supabase `mcp_usage`（`Prefer: return=minimal`），`try/catch` 吞錯。為了讓 `env` 進得來，把 PM-54 的 `txtWithTokens` 從 module-level **移進 `createMcpServer`** 成閉包（捕獲 env），裡面 `void logMcpUsage(...)` 背景記錄（不 await、不拖慢回應）。**全 10 個資料 tool 自動記錄**；7 個 report-scoped tool 另帶 `args.report_id`。
- **§3 `GET /api/usage/monthly`** + 共用 `getMonthlyUsage(env)`：查當月 `mcp_usage` 彙總 `totalCalls/totalTokens/totalChromeTokens/totalUSD/savedTokens/savedUSD/savedPercent/byTool`。
- **§4 MCP Tool 11 `get_usage_stats`**：讀 `getMonthlyUsage`（直接查 Supabase，不自我 fetch——Workers 端無 `API_BASE` 常數且省一跳）→ 中文月報 + footer + 自我記錄。
- **§1 schema.sql**：補上 `mcp_usage` 建表 + 索引（FOX 手動跑）。

**tsc / deploy**：`tsc --noEmit` ✅ + `wrangler deploy` ✅（Version `9eb1f3a4`）。

**🟡 需 FOX 跑 SQL（但這次不阻擋）**：`mcp_usage` 表尚未建（線上實測：呼叫 MCP `get_terminal_logs` 仍正常回資料 + footer；`GET /api/usage/monthly` 回 `totalCalls:0` 的正確 shape）。**與 PM-28 不同——表不存在不會壞**：`logMcpUsage` 有 try/catch + 不 await，`getMonthlyUsage` 用 `Array.isArray(rows)?rows:[]` 容錯 Supabase 錯誤回應 → 一律回 0。FOX 在 Supabase 跑 `schema.sql` 的 `CREATE TABLE mcp_usage ...` 後，統計即開始累積。

**待 FOX 驗收**：跑 SQL → 用 MCP 呼叫幾次（會記錄）→ `curl /api/usage/monthly` 看 totalCalls/省%；或用 MCP `get_usage_stats` 看月報。

---

### 🟡 PM-56b：修 MCP 使用量記錄沒寫入（Workers 提前終止）

**根因**：`void logMcpUsage(env, ...)` 是 fire-and-forget，Cloudflare Workers 回應送出後立刻終止，背景 fetch 來不及完成。

**修法**：`txtWithTokens` 改成 async，await logMcpUsage。多等幾十毫秒不影響體驗。

#### server/src/index.ts 改動：

```ts
// 把 txtWithTokens 改成 async
const txtWithTokens = async (data: unknown, toolName: string, reportId?: string) => {
  const text = typeof data === 'string' ? data : JSON.stringify(data, null, 2);
  const est = estimateTokens(text, toolName);
  await logMcpUsage(env, toolName, est, reportId); // 改成 await
  return { content: [{ type: 'text' as const, text: text + formatTokenFooter(est) }] };
};
```

所有呼叫 `txtWithTokens` 的 tool 都已經是 async（`return await txtWithTokens(...)`），所以不需要改其他地方。

#### Deploy
```
cd server && npx wrangler deploy
```

---

### 🔵 DONE-56b：修 MCP 使用量沒寫入（Workers 提前終止）✅ 已部署 + 線上驗證

**改動**：只改 `server/src/index.ts`（2 處）。

**根因**：`void logMcpUsage(...)` fire-and-forget——Cloudflare Workers 回應送出後 isolate 立刻終止，背景 Supabase POST 來不及完成 → 記錄沒寫入（PM-56 實測 `totalCalls` 一直 0，原以為只是表沒建，實則 await 才是關鍵）。

**修法**：
- `txtWithTokens` 改 `async` + `await logMcpUsage(...)`（10 個資料 tool 共用此閉包，全部受惠；tool 回呼本就是 async，`return txtWithTokens(...)` 的 Promise 自動展平，呼叫端不需改）。
- 連帶把 `get_usage_stats` 自身的 `void logMcpUsage` 也改 `await`（同根因，否則它的呼叫不會被記錄）。

**tsc / deploy**：`tsc --noEmit` ✅ + `wrangler deploy` ✅（Version `fc329e12`）。

**線上實測（真實 MCP 連線）**：呼叫 `get_terminal_logs`（69 tokens）後 `GET /api/usage/monthly` 回：
```json
{"month":"2026-06","totalCalls":1,"totalTokens":69,"totalChromeTokens":2760,
 "totalUSD":"0.0006","savedTokens":2691,"savedUSD":"0.0215","savedPercent":98,
 "byTool":{"get_terminal_logs":{"calls":1,"tokens":69}}}
```
**記錄成功寫入**（69 tokens 與 footer 吻合、×40=2760、省 98%）——修正前同樣呼叫 `totalCalls` 仍為 0。（`mcp_usage` 表已存在，FOX 已跑 SQL。）

**FOX 可驗收**：MCP `get_usage_stats` 看月報，或多呼叫幾次 tool 看 `byTool` 累積。

---

### 🟡 PM-57：MCP get_screenshots 回傳圖片 + 勾選控制

**目標**：新增 MCP tool `get_screenshots`，回傳報告的截圖圖片（base64）。客戶可透過參數控制是否包含圖片（省 token）。

---

#### §1 現有截圖儲存架構

報告的截圖存在 R2：
- `reports` 表有 `screenshots_r2_key` 欄位
- R2 裡存的是 JSON array，每個元素是 `{ dataUrl: "data:image/png;base64,..." }`

---

#### §2 MCP Tool：get_screenshots

```ts
server.tool(
  'get_screenshots',
  '取得報告的截圖圖片（視覺 Bug 用）。⚠ 圖片會消耗較多 token（每張 ~3,000-8,000），建議只在需要看畫面時使用。',
  {
    report_id: z.string(),
    include_images: z.boolean().optional().describe('是否回傳圖片內容（預設 false，只回 metadata 省 token）'),
  },
  async ({ report_id, include_images }) => {
    // 從 Supabase 讀 R2 key
    const { data } = await supabase()
      .from('reports')
      .select('screenshots_r2_key, screenshot_count')
      .eq('report_id', report_id)
      .single();

    if (!data || !data.screenshots_r2_key) {
      return txtWithTokens(
        { message: '此報告沒有截圖', screenshot_count: 0 },
        'get_screenshots',
        report_id
      );
    }

    // 從 R2 讀截圖資料
    const obj = await env.R2.get(data.screenshots_r2_key);
    if (!obj) {
      return txtWithTokens(
        { message: '截圖資料已過期或不存在', screenshot_count: data.screenshot_count },
        'get_screenshots',
        report_id
      );
    }

    const screenshots = JSON.parse(await obj.text()) as Array<{ dataUrl: string; annotation?: string }>;

    if (!include_images) {
      // 只回 metadata（省 token）
      const metadata = {
        screenshot_count: screenshots.length,
        message: `此報告有 ${screenshots.length} 張截圖。如需 AI 分析視覺問題，請加 include_images: true（每張約 3,000-8,000 tokens）。`,
      };
      return txtWithTokens(metadata, 'get_screenshots', report_id);
    }

    // 回傳圖片（include_images = true）
    const content: Array<{ type: 'text' | 'image'; text?: string; data?: string; mimeType?: string }> = [];

    for (let i = 0; i < screenshots.length; i++) {
      const ss = screenshots[i];
      // dataUrl 格式：data:image/png;base64,xxx
      const base64 = ss.dataUrl.replace(/^data:image\/\w+;base64,/, '');
      const mimeMatch = ss.dataUrl.match(/^data:(image\/\w+);base64,/);
      const mimeType = mimeMatch ? mimeMatch[1] : 'image/png';

      content.push({
        type: 'text',
        text: `📸 截圖 ${i + 1}/${screenshots.length}${ss.annotation ? `\n📝 標注：${ss.annotation}` : ''}`,
      });
      content.push({
        type: 'image',
        data: base64,
        mimeType,
      });
    }

    // Token 估算（圖片的 token 用固定估算值）
    const imageTokens = screenshots.length * 5000; // 每張圖約 5000 tokens
    const textPart = content.filter(c => c.type === 'text').map(c => c.text).join('\n');
    const textTokens = Math.ceil(textPart.length / 3.5);
    const totalTokens = textTokens + imageTokens;
    const chromeTokens = totalTokens * 8; // Chrome 要看整頁 DOM 更貴

    const footer = `\n\n---\n📊 Token 估算：~${totalTokens.toLocaleString()} tokens ≈ $${((totalTokens * 8) / 1_000_000).toFixed(4)}（含 ${screenshots.length} 張圖片）\n💡 同場景 Claude in Chrome：~${chromeTokens.toLocaleString()} tokens ≈ $${((chromeTokens * 8) / 1_000_000).toFixed(4)}\n✅ BugEzy 為你省了 ${Math.round((1 - totalTokens / chromeTokens) * 100)}%`;

    content.push({ type: 'text', text: footer });

    // 記錄使用量
    await logMcpUsage(env, 'get_screenshots', {
      bugezyTokens: totalTokens,
      chromeTokens,
      savedPercent: Math.round((1 - totalTokens / chromeTokens) * 100),
      bugezyUSD: ((totalTokens * 8) / 1_000_000).toFixed(4),
      chromeUSD: ((chromeTokens * 8) / 1_000_000).toFixed(4),
    }, report_id);

    return { content };
  }
);
```

---

#### §3 AI 的使用行為

AI 會自然地這樣用：

```
1. AI 呼叫 get_report_overview
   → 看到 screenshot_count: 2
   → 看到 description:「按鈕顏色不對」

2. AI 判斷是視覺問題
   → 呼叫 get_screenshots(report_id, include_images: true)
   → 收到 2 張截圖
   → 分析：「按鈕應該是藍色但目前是灰色，CSS 的 --btn-color 需要修改」
```

如果不是視覺問題（例如 console error），AI 不會呼叫 get_screenshots，省下圖片的 token。

---

#### §4 不改的東西
- extension / web / cli：不動
- 其他 MCP tools：不動

#### Build + Deploy
```
cd server && npx wrangler deploy
```

#### 驗收

1. 用 BugEzy 截圖標注一張 → 上傳
2. MCP 呼叫 `get_screenshots(report_id)` → 應回傳 metadata（截圖數量 + 提示）
3. MCP 呼叫 `get_screenshots(report_id, include_images: true)` → 應回傳圖片

---

### 🔵 DONE-57：MCP get_screenshots 回傳圖片 + 勾選控制 ✅ 已部署 + 線上驗證

**改動**：只改 `server/src/index.ts`，於 `createMcpServer` 新增 **Tool 12 `get_screenshots`**（其他 tool/extension/web/cli 不動）。

**參數**：`report_id: z.string()` + `include_images: z.boolean().optional()`（預設 false）。
**流程**：
- Supabase 讀 `screenshots_r2_key, screenshot_count` → 無 key → 回「此報告沒有截圖」（txtWithTokens）。
- R2 讀截圖 JSON（`{dataUrl, annotation?}[]`）；R2 缺檔 → 回「截圖資料已過期或不存在」。
- **`include_images` 未開（預設）**：只回 metadata（截圖數 + 「要看畫面請加 include_images:true，每張約 3k-8k tokens」），走 `txtWithTokens` 附 footer + 記錄使用量。
- **`include_images:true`**：每張組「📸 截圖 i/N（+標注）」text + `{type:'image', data:base64, mimeType}` image block；自算 token（每張 ~5000 + 文字）、對比 Chrome（×8）、附 footer、`await logMcpUsage`（PM-56b：await 才寫得進）。

**型別**：MCP SDK 接受混合 `text`/`image` content union（tsc 通過）；`mimeType` 由 dataUrl 正則取（fallback image/png）。

**tsc / deploy**：`tsc --noEmit` ✅ + `wrangler deploy` ✅（Version `25c30aab`）。

**線上實測（raw `/mcp` JSON-RPC）**：
- `tools/list` → 12 個 tool，**含 `get_screenshots`**。
- `tools/call get_screenshots`（screenshot_count=0 的真實報告，metadata 模式）→ 回 `{"message":"此報告沒有截圖","screenshot_count":0}` + footer「省了 90%」（預設 ×10）。
- 圖片模式（`include_images:true`）因現有 20 筆報告皆 `screenshot_count:0` 無法實打，但 tsc 已驗 image content 型別、邏輯與既有 pattern 一致。

**待 FOX 驗收**：用 BugEzy 截圖標注上傳一筆（screenshot_count>0）→ Claude Chat `get_screenshots(report_id)` 看 metadata、加 `include_images:true` 看圖片分析。⚠ claude.ai 的 BugEzy MCP 連線可能需**重新連線**才會出現新 tool（連線端會快取 tool list）。

---

### 🟡 PM-58：報告頁升級 Jam 風格 DevTools 分頁

**目標**：web 報告頁（`/report/:id`）的 Console/Network/Voice 區塊改成 **Tab 分頁切換**，類似 Jam 的 DevTools 面板。加上 Info tab 顯示頁面資訊 + 描述 + 標記。

---

#### §1 新的 Layout 設計

```
┌─────────────────────────────────────────────────┐
│  🐛 BugEzy Bug 報告                              │
├─────────────────────────────────────────────────┤
│  頁面標題 + URL + 瀏覽器 + 時間                    │
├─────────────────────────────────────────────────┤
│  [rrweb 播放器]                                   │
│  ▶ ━━━━━━━━━━━━━━━━━━━━━━━ 0:30 / 1:20          │
├─────────────────────────────────────────────────┤
│  [ Info ] [ Console(4) ] [ Network(2) ] [ Voice(8) ] [ 📸(1) ]  │
│ ─────────────────────────────────────────────── │
│  │ ❌ TypeError: Cannot read property...        │ │
│  │ ⚠ Deprecated API usage...                   │ │
│  │ ❌ ReferenceError: foo is not defined        │ │
│  │ ❌ Cannot read properties of null...         │ │
│  │                                              │ │
├─────────────────────────────────────────────────┤
│  📊 Token 估算：~730 tokens ≈ $0.0058           │
│  ✅ BugEzy 為你省了 93%                           │
└─────────────────────────────────────────────────┘
```

---

#### §2 ReportPage.tsx 改動

把原本的 `<div className="panels">` 改成 Tab 元件：

```tsx
type TabKey = 'info' | 'console' | 'network' | 'voice' | 'screenshots';

const [activeTab, setActiveTab] = useState<TabKey>('info');

// Tab 定義
const tabs: { key: TabKey; label: string; count?: number; show: boolean }[] = [
  { key: 'info', label: 'Info', show: true },
  { key: 'console', label: 'Console', count: report.consoleLogs.length, show: true },
  { key: 'network', label: 'Network', count: report.networkErrors.length, show: true },
  { key: 'voice', label: 'Voice', count: report.voiceTranscript.length, show: report.voiceTranscript.length > 0 },
  { key: 'screenshots', label: '📸', count: report.screenshots?.length || 0, show: (report.screenshots?.length || 0) > 0 },
];

// 自動選到有資料的 tab
useEffect(() => {
  if (report.consoleLogs.length > 0) setActiveTab('console');
  else if (report.networkErrors.length > 0) setActiveTab('network');
  else setActiveTab('info');
}, [report]);
```

JSX：
```tsx
{/* Tab Bar */}
<div className="tab-bar">
  {tabs.filter(t => t.show).map(t => (
    <button
      key={t.key}
      className={`tab-btn ${activeTab === t.key ? 'active' : ''} ${t.count && t.count > 0 && t.key !== 'info' ? 'has-data' : ''}`}
      onClick={() => setActiveTab(t.key)}
    >
      {t.label}
      {t.count !== undefined && t.count > 0 && (
        <span className={`tab-badge ${t.key === 'console' ? 'badge-error' : ''}`}>{t.count}</span>
      )}
    </button>
  ))}
</div>

{/* Tab Content */}
<div className="tab-content">
  {activeTab === 'info' && (
    <div className="tab-panel">
      {report.description && (
        <div className="info-section">
          <h3>💬 描述</h3>
          <p>{report.description}</p>
        </div>
      )}
      {report.markers && report.markers.length > 0 && (
        <div className="info-section">
          <h3>📌 時間軸標記</h3>
          {report.markers.map((m: any, i: number) => (
            <div key={i} className="marker-item">
              <span className="marker-time">{Math.floor(m.time_sec / 60)}:{String(m.time_sec % 60).padStart(2, '0')}</span>
              <span>{m.note || '（無描述）'}</span>
            </div>
          ))}
        </div>
      )}
      <div className="info-section">
        <h3>📊 摘要</h3>
        <div className="info-grid">
          <div>DOM 事件：{report.rrwebEvents.length}</div>
          <div>Console：{report.consoleLogs.length}</div>
          <div>Network：{report.networkErrors.length}</div>
          <div>語音：{report.voiceTranscript.length} 段</div>
          <div>截圖：{report.screenshots?.length || 0}</div>
        </div>
      </div>
    </div>
  )}
  {activeTab === 'console' && <ConsolePanel logs={report.consoleLogs} />}
  {activeTab === 'network' && <NetworkPanel errors={report.networkErrors} />}
  {activeTab === 'voice' && <VoicePanel transcript={report.voiceTranscript} />}
  {activeTab === 'screenshots' && report.screenshots && (
    <ScreenshotPanel screenshots={report.screenshots} />
  )}
</div>
```

---

#### §3 CSS（index.css 加）

```css
/* Tab Bar */
.tab-bar {
  display: flex;
  gap: 0;
  border-bottom: 2px solid #2a2a3e;
  margin-top: 16px;
  overflow-x: auto;
}

.tab-btn {
  padding: 10px 20px;
  background: none;
  border: none;
  border-bottom: 2px solid transparent;
  margin-bottom: -2px;
  color: #888;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  white-space: nowrap;
  display: flex;
  align-items: center;
  gap: 6px;
  transition: all 0.15s;
}

.tab-btn:hover {
  color: #ccc;
  background: rgba(124, 58, 237, 0.05);
}

.tab-btn.active {
  color: #a78bfa;
  border-bottom-color: #7c3aed;
}

.tab-btn.has-data {
  color: #ccc;
}

.tab-badge {
  font-size: 11px;
  padding: 1px 7px;
  border-radius: 10px;
  background: #2a2a3e;
  color: #aaa;
}

.tab-badge.badge-error {
  background: rgba(239, 68, 68, 0.2);
  color: #ef4444;
}

.tab-content {
  min-height: 200px;
  padding: 16px 0;
}

.tab-panel {}

.info-section {
  margin-bottom: 20px;
}

.info-section h3 {
  font-size: 14px;
  color: #a78bfa;
  margin-bottom: 8px;
}

.info-section p {
  color: #ccc;
  line-height: 1.7;
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  gap: 8px;
  font-size: 13px;
  color: #aaa;
}

.marker-item {
  display: flex;
  gap: 8px;
  padding: 6px 0;
  border-bottom: 1px solid #1a1a2e;
  font-size: 13px;
  color: #ccc;
}

.marker-time {
  background: #7c3aed;
  color: #fff;
  padding: 2px 8px;
  border-radius: 4px;
  font-family: monospace;
  font-size: 12px;
  white-space: nowrap;
}
```

---

#### §4 移除舊的 panels 佈局

原本的：
```tsx
<div className="panels">
  <ConsolePanel logs={report.consoleLogs} />
  <NetworkPanel errors={report.networkErrors} />
  <VoicePanel transcript={report.voiceTranscript} />
</div>
```

**刪除**，改成上面的 tab-bar + tab-content。

同時把 description-block 和 screenshots 的獨立區塊移除（已整合到 Info 和 Screenshots tab 裡）。

---

#### §5 不改的東西
- extension / server / cli：不動
- 各 Panel 元件的內部邏輯不動（ConsolePanel、NetworkPanel、VoicePanel、ScreenshotPanel）

#### Build
```
cd web && npm run build
cd ../server && npx wrangler deploy
```

#### 驗收

1. 開一份已上傳的報告連結
2. 看到 Tab Bar：`[ Info ] [ Console(4) ] [ Network(4) ] [ Voice(1) ]`
3. 點 Console tab → 顯示 console logs
4. 點 Network tab → 顯示 network errors
5. 點 Info tab → 顯示描述 + 摘要
6. 有資料的 tab 自動被選中

---

### 🔵 DONE-58：報告頁升級 Jam 風格 DevTools 分頁 ✅

**改動**：`web/src/pages/ReportPage.tsx` + `web/src/index.css`（extension/server/cli 不動；各 Panel 元件內部不動）。

**§2 ReportPage.tsx**：
- 加 `activeTab` 狀態（`TabKey = info|console|network|voice|screenshots`）；`useEffect([report])` 載入後自動選有資料的 tab（console > network > info）。
- 移除舊的獨立 `description-block`、`screenshots` 區塊、`<div className="panels">` 三欄；改成 **tab-bar + tab-content**（rrweb 播放器保留在 tab 上方）。
- Tab Bar：`info/console/network/voice(>0才顯示)/📸(>0才顯示)`，有 count 顯示 badge（console 用 `badge-error` 紅）。
- Info tab：描述 + 時間軸標記（`fmtSec` m:ss）+ 摘要 grid（DOM/Console/Network/語音/截圖）；其餘 tab 各掛 `ConsolePanel/NetworkPanel/VoicePanel/ScreenshotPanel`。
- 型別乾淨：tabs 陣列 `as { key: TabKey; ... }[]`、markers `.map((m,i)=>...)`（m: TimeMarker，不用 any）。

**§3 index.css**：加 `.tab-bar/.tab-btn(.active/.has-data)/.tab-badge(.badge-error)/.tab-content/.info-section/.info-grid/.marker-item/.marker-time`。

**build / deploy**：`web npm run build`（tsc + vite）✅；`server wrangler deploy` ✅（Version `b2fa2adb`）。

**⚠ 重要（請 FOX 知悉）— Worker 不服務 SPA**：實測 `GET /report/:id` → Worker 回 **404**（wrangler.toml 無 `[assets]`/`[site]`）。報告頁是靠 **`web` 的 Vite dev（`npm run dev` :5173，proxy `/api` 到線上 Worker）** 或另外的 host 來看；`cd ../server && npx wrangler deploy` **不會** bundle `web/dist`（這次 server 程式碼也沒變，等於 no-op 重部署）。所以本 PM 的 UI 改動目前只在 `web` 本地預覽看得到，share_url（`<origin>/report/...`）在 Worker origin 上仍 404。**若要讓報告頁線上可開**，需另開 PM：給 Worker 加 assets binding 服務 `web/dist`，或把 `web` 部署到 Cloudflare Pages（屬 §5「server 不動」之外的基建決策，未自作主張處理）。

**待 FOX 驗收**：`cd web && npm run preview`（或 dev）開報告連結 → 看到 Tab Bar、點 console/network/voice/info 切換、有資料 tab 自動選中、Info 顯示描述+標記+摘要。

---

### 🟡 PM-59：Server 直接 serve 報告頁 HTML（含 DevTools 分頁）

**問題**：`/report/:id` 回傳 `{"error":"not found"}`。web/ React 應用從沒部署到 Worker。

**修法**：跟 `/test` 一樣，Server 在 `/report/:id` 路由直接回傳完整 HTML 頁面。頁面載入後用 fetch 讀 `/api/reports/:id` 的資料，用 vanilla JS 渲染。

---

#### §1 Server 路由

```ts
// GET /report/:id → 回傳報告頁 HTML
if (url.pathname.startsWith('/report/') && request.method === 'GET') {
  const reportId = url.pathname.split('/report/')[1];
  if (reportId && reportId.length > 10) {
    return new Response(REPORT_PAGE_HTML, {
      headers: { 'Content-Type': 'text/html; charset=utf-8' },
    });
  }
}
```

---

#### §2 報告頁 HTML（REPORT_PAGE_HTML）

完整的 Jam 風格報告頁，包含：

**Header**：標題 + URL + 瀏覽器 + 時間
**Tab Bar**：Info | Console(n) | Network(n) | Voice(n) | 📸(n)
**Tab Content**：各面板內容
**Token 估算**：底部顯示

HTML 內容大綱（具體 CSS 用 BugEzy 品牌色 #7c3aed + 深色主題）：

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>BugEzy — Bug 報告</title>
  <style>
    /* 深色主題 + BugEzy 品牌色 */
    * { margin:0; padding:0; box-sizing:border-box; }
    body { background:#0f0f1a; color:#e0e0e0; font-family:system-ui,"Microsoft JhengHei",sans-serif; }
    
    .topbar { display:flex; align-items:center; gap:12px; padding:12px 24px; background:#1a1a2e; border-bottom:1px solid #2a2a3e; }
    .topbar-brand { font-size:18px; font-weight:700; color:#a78bfa; }
    .topbar-title { color:#888; font-size:14px; }
    
    .report { max-width:1100px; margin:0 auto; padding:24px; }
    
    .header { margin-bottom:20px; }
    .header h1 { font-size:20px; color:#fff; margin-bottom:8px; }
    .meta { font-size:13px; color:#888; line-height:1.8; }
    .meta a { color:#a78bfa; text-decoration:none; }
    
    /* Tab Bar */
    .tab-bar { display:flex; gap:0; border-bottom:2px solid #2a2a3e; margin:20px 0 0; overflow-x:auto; }
    .tab-btn { padding:10px 20px; background:none; border:none; border-bottom:2px solid transparent; margin-bottom:-2px; color:#888; font-size:14px; font-weight:500; cursor:pointer; white-space:nowrap; display:flex; align-items:center; gap:6px; transition:all 0.15s; }
    .tab-btn:hover { color:#ccc; background:rgba(124,58,237,0.05); }
    .tab-btn.active { color:#a78bfa; border-bottom-color:#7c3aed; }
    .tab-badge { font-size:11px; padding:1px 7px; border-radius:10px; background:#2a2a3e; color:#aaa; }
    .tab-badge.error { background:rgba(239,68,68,0.2); color:#ef4444; }
    
    .tab-content { min-height:200px; padding:16px 0; }
    .tab-panel { display:none; }
    .tab-panel.active { display:block; }
    
    /* Info Panel */
    .info-section { margin-bottom:20px; }
    .info-section h3 { font-size:14px; color:#a78bfa; margin-bottom:8px; }
    .info-section p { color:#ccc; line-height:1.7; white-space:pre-wrap; }
    .info-grid { display:grid; grid-template-columns:repeat(auto-fill,minmax(150px,1fr)); gap:8px; font-size:13px; color:#aaa; }
    .marker-item { display:flex; gap:8px; padding:6px 0; border-bottom:1px solid #1a1a2e; font-size:13px; color:#ccc; }
    .marker-time { background:#7c3aed; color:#fff; padding:2px 8px; border-radius:4px; font-family:monospace; font-size:12px; }
    
    /* Console Panel */
    .log-item { padding:8px 12px; border-bottom:1px solid #1a1a2e; font-family:monospace; font-size:13px; display:flex; gap:8px; }
    .log-item.error { color:#ef4444; }
    .log-item.warn { color:#f59e0b; }
    .log-icon { flex-shrink:0; }
    .log-msg { word-break:break-all; }
    .log-time { color:#555; font-size:11px; margin-left:auto; flex-shrink:0; }
    
    /* Network Panel */
    .net-item { padding:8px 12px; border-bottom:1px solid #1a1a2e; font-family:monospace; font-size:13px; display:flex; gap:12px; align-items:center; }
    .net-status { font-weight:700; min-width:36px; }
    .net-status.s4xx { color:#f59e0b; }
    .net-status.s5xx { color:#ef4444; }
    .net-method { color:#3b82f6; min-width:40px; }
    .net-url { color:#ccc; word-break:break-all; flex:1; }
    .net-duration { color:#555; font-size:11px; }
    
    /* Voice Panel */
    .voice-item { padding:8px 12px; border-bottom:1px solid #1a1a2e; font-size:14px; color:#ccc; line-height:1.6; }
    .voice-time { color:#555; font-size:11px; margin-right:8px; }
    
    /* Screenshots */
    .ss-grid { display:grid; grid-template-columns:repeat(auto-fill,minmax(300px,1fr)); gap:12px; }
    .ss-img { width:100%; border-radius:8px; border:1px solid #2a2a3e; cursor:pointer; }
    .ss-img:hover { border-color:#7c3aed; }
    
    /* Token Panel */
    .token-panel { margin-top:24px; padding:16px; background:#1a1a2e; border:1px solid rgba(124,58,237,0.3); border-radius:12px; }
    .token-row { display:flex; justify-content:space-between; padding:3px 0; font-size:13px; color:#aaa; }
    .token-row.total { border-top:1px solid #2a2a3e; margin-top:6px; padding-top:6px; font-weight:700; color:#fff; }
    .token-save { margin-top:10px; padding:10px; background:rgba(16,185,129,0.1); border:1px solid rgba(16,185,129,0.3); border-radius:8px; font-size:13px; color:#10b981; text-align:center; }
    
    .loading { text-align:center; padding:60px; color:#888; }
    .error-msg { text-align:center; padding:60px; color:#ef4444; }
    .empty { text-align:center; padding:24px; color:#555; font-size:13px; }
  </style>
</head>
<body>
  <div class="topbar">
    <span class="topbar-brand">🐛 BugEzy</span>
    <span class="topbar-title">Bug 報告</span>
  </div>
  <div class="report" id="app">
    <div class="loading" id="loading">載入中…</div>
  </div>

  <script>
    const reportId = location.pathname.split('/report/')[1];
    const API = location.origin;

    fetch(API + '/api/reports/' + reportId)
      .then(r => { if (!r.ok) throw new Error('not found'); return r.json(); })
      .then(render)
      .catch(() => {
        document.getElementById('app').innerHTML = '<div class="error-msg">找不到報告</div>';
      });

    function fmtTime(ts) {
      if (!ts) return '';
      const d = new Date(ts);
      const p = n => String(n).padStart(2,'0');
      return p(d.getHours()) + ':' + p(d.getMinutes()) + ':' + p(d.getSeconds());
    }

    function fmtDate(iso) {
      if (!iso) return '';
      const d = new Date(iso);
      const p = n => String(n).padStart(2,'0');
      return d.getFullYear()+'-'+p(d.getMonth()+1)+'-'+p(d.getDate())+' '+p(d.getHours())+':'+p(d.getMinutes());
    }

    function esc(s) { return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }

    function render(r) {
      const consoleCount = r.console_logs?.length || 0;
      const networkCount = r.network_errors?.length || 0;
      const voiceCount = r.voice_transcript?.length || 0;
      const ssCount = r.screenshots?.length || 0;
      const markers = r.markers || [];

      // Header
      let html = '<div class="header">';
      html += '<h1>' + esc(r.title || '（無標題）') + '</h1>';
      html += '<div class="meta">';
      html += '<div>URL：<a href="'+esc(r.url)+'" target="_blank">'+esc(r.url)+'</a></div>';
      html += '<div>'+esc(r.browser||'')+(r.screen_size ? ' ｜ '+r.screen_size : '')+'</div>';
      html += '<div>'+fmtDate(r.created_at)+'</div>';
      html += '</div></div>';

      // Tab Bar
      const tabs = [
        { key:'info', label:'Info', count:null },
        { key:'console', label:'Console', count:consoleCount, isError:true },
        { key:'network', label:'Network', count:networkCount },
        { key:'voice', label:'Voice', count:voiceCount },
      ];
      if (ssCount > 0) tabs.push({ key:'screenshots', label:'📸', count:ssCount });

      // 自動選有資料的 tab
      let defaultTab = 'info';
      if (consoleCount > 0) defaultTab = 'console';
      else if (networkCount > 0) defaultTab = 'network';

      html += '<div class="tab-bar">';
      tabs.forEach(t => {
        const badge = t.count !== null && t.count > 0
          ? '<span class="tab-badge'+(t.isError?' error':'')+'">'+t.count+'</span>'
          : '';
        html += '<button class="tab-btn'+(t.key===defaultTab?' active':'')+'" data-tab="'+t.key+'">'+t.label+badge+'</button>';
      });
      html += '</div>';

      html += '<div class="tab-content">';

      // Info Tab
      html += '<div class="tab-panel'+(defaultTab==='info'?' active':'')+'" id="tab-info">';
      if (r.description) {
        html += '<div class="info-section"><h3>💬 描述</h3><p>'+esc(r.description)+'</p></div>';
      }
      if (markers.length > 0) {
        html += '<div class="info-section"><h3>📌 時間軸標記</h3>';
        markers.forEach(m => {
          const min = Math.floor(m.time_sec/60);
          const sec = String(m.time_sec%60).padStart(2,'0');
          html += '<div class="marker-item"><span class="marker-time">'+min+':'+sec+'</span><span>'+esc(m.note||'（無描述）')+'</span></div>';
        });
        html += '</div>';
      }
      html += '<div class="info-section"><h3>📊 摘要</h3><div class="info-grid">';
      html += '<div>DOM 事件：'+(r.rrweb_count||0)+'</div>';
      html += '<div>Console：'+consoleCount+'</div>';
      html += '<div>Network：'+networkCount+'</div>';
      html += '<div>語音：'+voiceCount+' 段</div>';
      html += '<div>截圖：'+ssCount+'</div>';
      html += '</div></div></div>';

      // Console Tab
      html += '<div class="tab-panel'+(defaultTab==='console'?' active':'')+'" id="tab-console">';
      if (consoleCount === 0) {
        html += '<div class="empty">沒有 Console 錯誤 ✓</div>';
      } else {
        (r.console_logs||[]).forEach(log => {
          const cls = log.level === 'error' ? 'error' : 'warn';
          const icon = log.level === 'error' ? '❌' : '⚠';
          html += '<div class="log-item '+cls+'"><span class="log-icon">'+icon+'</span><span class="log-msg">'+esc(log.message)+'</span><span class="log-time">'+fmtTime(log.timestamp)+'</span></div>';
        });
      }
      html += '</div>';

      // Network Tab
      html += '<div class="tab-panel'+(defaultTab==='network'?' active':'')+'" id="tab-network">';
      if (networkCount === 0) {
        html += '<div class="empty">沒有 Network 錯誤 ✓</div>';
      } else {
        (r.network_errors||[]).forEach(err => {
          const cls = err.status >= 500 ? 's5xx' : 's4xx';
          html += '<div class="net-item"><span class="net-status '+cls+'">'+err.status+'</span><span class="net-method">'+esc(err.method)+'</span><span class="net-url">'+esc(err.url)+'</span><span class="net-duration">'+(err.duration||0)+'ms</span></div>';
        });
      }
      html += '</div>';

      // Voice Tab
      html += '<div class="tab-panel'+(defaultTab==='voice'?' active':'')+'" id="tab-voice">';
      if (voiceCount === 0) {
        html += '<div class="empty">沒有語音記錄</div>';
      } else {
        (r.voice_transcript||[]).forEach(v => {
          html += '<div class="voice-item"><span class="voice-time">'+fmtTime(v.timestamp)+'</span>'+esc(v.text)+'</div>';
        });
      }
      html += '</div>';

      // Screenshots Tab
      if (ssCount > 0) {
        html += '<div class="tab-panel" id="tab-screenshots"><div class="ss-grid">';
        (r.screenshots||[]).forEach(ss => {
          const src = typeof ss === 'string' ? ss : ss.dataUrl || ss.url || '';
          if (src) html += '<img class="ss-img" src="'+src+'" onclick="window.open(this.src)">';
        });
        html += '</div></div>';
      }

      html += '</div>'; // tab-content

      // Token 估算
      const voiceText = (r.voice_transcript||[]).map(v=>v.text).join('');
      const consoleText = JSON.stringify(r.console_logs||[]);
      const networkText = JSON.stringify(r.network_errors||[]);
      const descText = r.description || '';
      const items = [
        { label:'🎤 語音記錄', len:voiceText.length },
        { label:'🖥 Console', len:consoleText.length },
        { label:'🌐 Network', len:networkText.length },
        { label:'📝 描述', len:descText.length },
        { label:'📹 DOM 摘要', len:105 },
      ];
      let totalT = 0;
      let tokenHtml = '';
      items.forEach(it => {
        const t = Math.ceil(it.len / 3.5);
        if (t > 0) { totalT += t; tokenHtml += '<div class="token-row"><span>'+it.label+'</span><span>~'+t.toLocaleString()+' tokens</span></div>'; }
      });
      const chromeT = totalT * 15;
      const pct = chromeT > 0 ? Math.round((1-totalT/chromeT)*100) : 0;
      tokenHtml += '<div class="token-row total"><span>AI 讀取總計</span><span>~'+totalT.toLocaleString()+' tokens ≈ $'+((totalT*8/1e6).toFixed(4))+'</span></div>';
      html += '<div class="token-panel"><div style="font-weight:600;margin-bottom:8px;color:#a78bfa;">📊 Token 估算</div>' + tokenHtml;
      html += '<div class="token-save">💡 同場景 Claude in Chrome：~'+chromeT.toLocaleString()+' tokens ≈ $'+((chromeT*8/1e6).toFixed(4))+'<br>✅ BugEzy 為你省了 '+pct+'%</div></div>';

      document.getElementById('app').innerHTML = html;

      // Tab 切換
      document.querySelectorAll('.tab-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
          document.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'));
          btn.classList.add('active');
          document.getElementById('tab-' + btn.dataset.tab)?.classList.add('active');
        });
      });
    }
  </script>
</body>
</html>
```

---

#### §3 Server 路由加在 API 路由之前

確保 `/report/:id` 路由在 `/api/reports/:id` 之前匹配。

---

#### §4 不改的東西
- extension / web / cli：不動（web/ React 版暫時不用）

#### Deploy
```
cd server && npx wrangler deploy
```

---

### 🔵 DONE-59：Server 直接 serve 報告頁 HTML ✅ 已部署 + 線上驗證（解 DONE-58 的 share_url 404）

**改動**：只改 `server/src/index.ts`（extension/web/cli 不動）。新增 `REPORT_PAGE_HTML` 常數（自包含深色主題 + Tab 分頁 + Token 估算 + vanilla JS）+ `/report/:id` 路由。

**§1 路由**（放在 `/test`、`/api/*` 之前）：`GET /report/<id>`（`reportId.length > 10`）→ 回 `html(REPORT_PAGE_HTML)`；短/無效 id 落空 → 走後續 404。
**§2 HTML**：載入後 `fetch(location.origin + '/api/reports/' + id)` → vanilla JS 渲染 Header / Tab Bar（Info|Console(n)|Network(n)|Voice(n)|📸(n)）/ 各面板 / Token 估算；`esc()` 防注入；點 tab 切 `.active`；自動選有資料 tab（console>network>info）。

**🚨 我據實修正的 spec bug（已實測證實）**：規格 HTML 讀 **snake_case**（`r.console_logs`/`r.network_errors`/`r.voice_transcript`/`r.rrweb_count`），但 `GET /api/reports/:id` 實際回 **camelCase**（curl 確認 keys：`consoleLogs/networkErrors/voiceTranscript/rrwebEvents`，無 snake_case）。照規格部署會**整頁計數 0、無資料**。故把 HTML 內欄位全改 camelCase（DOM 數用 `rrwebEvents?.length`），並順手讓 `esc()` 容 null。

**tsc / deploy**：`tsc --noEmit` ✅ + `wrangler deploy` ✅（Version `d0923c17`）。

**線上實測（curl）**：
- `GET /report/<id>` → **200 / text/html**，含 `tab-bar`、標題、修正後的 `r.consoleLogs?.length` / `r.rrwebEvents?.length`（會渲染真資料）。
- `GET /report/x`（短 id）→ 404（正確落空）。
- `GET /api/reports/<id>` → 200 / application/json（路由順序未影響 API）。

**至此 DONE-58 標註的 share_url 404 解決**：報告頁現在直接由 Worker 服務，`<origin>/report/<id>` 可開（web/ React 版暫不使用，待之後決定是否保留）。

**待 FOX 驗收**：瀏覽器開任一報告連結（如 `/report/0bf69766-...`）→ 看到 Header + Tab 切換 + 有資料 tab 自動選中 + console/network/voice/截圖/標記/Token 正常顯示。

---

#### 驗收

開 `https://bugezy-api.bugezy-api.workers.dev/report/edbe6494-db97-4a1b-b149-0799755ef6c0`

應看到：深色主題 + Tab 分頁（Info / Console(4) / Network(4) / Voice(1)）+ Token 估算

---

### 🟡 PM-收工-0622：Git commit + 文件同步

---

1. **更新 `docs/BugEzy_project_status.md`**：
   - MCP Tools 10→12（get_screenshots + get_usage_stats）
   - 新增 Token 透明度三層（PM-54~56）
   - 報告頁升級（PM-58~59，Server 直接 serve HTML + DevTools 分頁）
   - §6 加 PM-54~59

2. **更新 CHANGELOG.md**：加 Day 6 (6/22)

3. **更新 `docs/SKILL.md`**：反映 12 MCP Tools + Token 透明度 + 報告頁

4. **git add + commit**：
   ```
   git add -A
   git commit -m "feat: BugEzy Day 6 — token transparency, get_screenshots, report page DevTools tabs (PM-54~59)"
   ```

---

### 🔵 DONE-收工-0622 ✅

**文件同步**：
- `docs/BugEzy_project_status.md`：§2 七代表 ④ 改 **12 Tool**（+get_screenshots/get_usage_stats，回應附 token 省錢對比）；新增 **§6d「第 6 代 Day 6（PM-54~59）」**（Token 透明度三層 + get_screenshots + 報告頁 + Server/MCP 增量）。
- `CHANGELOG.md`：新增 `## 2026-06-22` 區塊，逐條 PM-54~59 + 收工。
- `docs/SKILL.md`：MCP 10→12 Tool；技術棧加 Token 省錢透明度、報告頁（Server serve `/report/:id`）兩條。

**commit**：`feat: BugEzy Day 6 — token transparency, get_screenshots, report page DevTools tabs (PM-54~59)`；暫存區已確認無 node_modules/dist（陌生垃圾檔 `cli/{throw` 仍未提交，續留磁碟待 FOX 刪）。

**本日 7 個 PM（PM-54/55/56/56b/57/58/59）全部 build/tsc 通過**；server 多次部署，MCP **10→12 tool**、新增 `/api/usage/monthly` 與 `/report/:id`（Worker 直接服務報告頁，解 share_url 404）。線上以 curl + 真實 MCP 連線驗證了 token footer、月度記錄、get_screenshots、報告頁；瀏覽器互動（edit-report token 面板、報告頁 Tab）待 FOX 實機。未 push（依慣例）。

---
