# job-0905.md — BugEzy Day 52（2026-09-05）

## 今日任務
- 部署大黃蜂官網上線
- 其他待定

---

### 🟡 PM-438：部署 server — 大黃蜂官網上線

**執行**：
1. `cd server && npm run build`（或 `wrangler deploy`）
2. 確認 bugezy.dev 首頁已顯示大黃蜂黃底風格
3. 確認分享報告頁、付費牆、找不到報告頁也已更新

**驗收**：bugezy.dev 線上版本為大黃蜂風格，端到端 0 failed

---

🔵 **DONE-438**

**已上線**。`wrangler deploy` ×2，最終版本 `b4937e68-0afc-45b6-bfc0-6d03f215157d`。

| 驗收 | 結果 |
|---|---|
| bugezy.dev 線上版本為大黃蜂風格 | ✅ 12 頁逐頁抓下來驗過（見下） |
| 端到端 0 failed | ✅ **1002 passed, 0 failed**（14 套）；`_verify403` 280 → **283** |
| `tsc --noEmit` | ✅ exit 0 |

**改動**：`server/src/index.ts`（修 1 個字元）、`bridge/_verify403.mjs`（+3 條護欄）、新增 `server/_verify-live.mjs`（線上驗收腳本）

---

## 🔴 第一次部署把首頁弄壞了，第二次才修好

上線之後抓 `bugezy.dev` 回來看，首頁 **Content-Length 只有 8013、沒有 `<body>`**，HTML 在這裡斷掉：

```
/* §2 hero（決策點 1）。⚠ 蜂巢紋 data URI 內不可有未編碼的
```

原因是我在 CSS 註解裡寫了 `` `;` `` —— **反引號提前結束了整個 template literal**。後面那半頁（三步驟、展示、賣點、語言帶、定價、CTA、footer）全部變成 dead code，從來沒有被回傳過。

**為什麼三道關卡全部沒擋下來**：

| 關卡 | 為什麼沒擋到 |
|---|---|
| `tsc --noEmit` | 反引號關掉字串之後，接著的 `;` 剛好結束 `return`，後面成了合法的 dead code —— 語法完全正確 |
| `wrangler deploy --dry-run` | 打包得起來就過，不會去執行頁面函式 |
| 280 條斷言 | 全部讀「原始碼字串」`srvRaw.includes(...)`，字串裡有沒有東西跟「這一頁真的長怎樣」是兩回事 |

**這是我這兩天第二次踩同一個雷** —— PM-435 的 FAQ 註解也寫過 `` `.faq-q.open + .faq-a` ``，那次 `tsc` 剛好報錯（`TS1109`）所以當場修掉；這次它沒報錯，就一路上了線。

### 補了兩層防線

1. **靜態護欄**（`_verify403` ⑰，3 條）：
   - 每個整頁樣板要在**自己這一段**裡收到 `` </html>` `` 終止符
   - 整頁樣板內的 CSS/HTML 註解不准有**未跳脫**的反引號
   - 開頭與終止符數量對得上（20 = 16 個多行 + 4 個字串串接）
2. **線上驗收腳本** `server/_verify-live.mjs`：部署後抓每一頁，檢查標記在、舊色碼不在、**收在 `</html>`**。靜態斷言永遠證明不了最後這件事，只有真的抓一次頁面作得了數。

護欄本身也反向測過 **5/5 全紅**（三種反引號寫法 + 弄丟終止符 + 把今天那個字元放回去）。過程中又抓到自己兩條假的：

- 「收得到終止符」是往後 `indexOf`，樣板壞掉時會撿到**下一頁**的終止符 → 永遠綠。改成限制在「下一個開頭之前」。
- 「`closes >= opens`」也永遠綠（整份檔案有 20 個 `` </html>` ``，多行 opener 只有 16）。改成兩邊都要等於 20。

另外原本想寫個會處理 `${...}` 巢狀的掃描器，結果被 `` ${post.description.replace(/"/g, '&quot;')} `` 裡的 **regex literal** 帶到檔尾（掃出 241 KB 的「樣板」）。手寫 JS 剖析器划不來，改成「開頭 → 終止符」的區間 + 只查註解，簡單而且正中失效模式。

---

## 線上逐頁驗收（`node server/_verify-live.mjs`）

```
  OK  /                   200  (23538b)      OK  /changelog          200  (14621b)
  OK  /features           200  (19513b)      OK  /blog               200  (13727b)
  OK  /guide              200  (26752b)      OK  /testimonials       200  (14544b)
  OK  /faq                200  (17165b)      OK  /feedback           200  (15131b)
  OK  /privacy            200  (16866b)      OK  /day-pass-success   200  (2127b)
  OK  /skill              200  (48381b)
 GAP  /reports            200  reportsPage() 的列表內容還沒改版  📋🗑❌🌐🎙📸🎬
  OK  /report/:id 殼       200  六角=true 收在</html>=true
  OK  /report-page.js?v=433  付費牆鎖頭=true 找不到報告=true 分享卡搬家=true
  OK  /hornet-real.png    200  image/png 71121b
```

每頁都查：大黃蜂標記在、15 個舊色碼不在、emoji 為 0（CSS 註解不算）、**收在 `</html>`**。付費牆與找不到報告是 client 端畫的，所以驗的是外部 JS 裡的 `lock-shackle` / `nf-cell` / `shareSlot.appendChild`。

---

## ⚠ 一個要你決定的缺口：`/reports`

`/reports` 的**外框**（`reportsShell`）PM-432 換過了，但**列表內容**是 `reportsPage()` 畫的，至今沒有任何一張卡碰過。線上還是：

- h1「📋 我的報告」、「🗑️ 刪除選取」
- 五顆狀態徽章 `❌3 🌐2 🎙️ 📸 🎬`
- 未登入提示寫「請從擴充的『📋 我的報告』按鈕開啟」—— **這句現在是錯的**，PM-413~421 之後那顆按鈕上已經沒有 emoji 了

徽章要換成幾何圖示還是文字標籤（`錯誤 3` / `網路 2` …）牽涉到可讀性取捨，我沒有自己決定。**建議開一張卡**；線上驗收腳本目前把它列為 `GAP`（會印出來但不算失敗），不會讓你以為它已經好了。

---

## 其他

- **bundle gzip 770 KiB**，Worker 啟動 61 ms。
- **舊版本仍可回滾**：部署前的線上版是舊紫色系；要退回用 `wrangler rollback`。
- **擴充功能端沒有動**，Day 49 起累積的畫面仍未實機驗收。

---

### 🟡 PM-439：建立送審分支 — 移除 bridge，保留大黃蜂 + 票券

**背景**：main 分支包含 bridge（51 支 MCP 工具、~10000 行、嵌入 background.ts 700 行）。送審版不需要 bridge，移除以降低複雜度。大黃蜂視覺和票券功能全部保留。

**執行**：

#### 1) 建立分支
```
git checkout -b release/v1.2.0
```

#### 2) background.ts — 移除 bridge 程式碼
移除以下區塊（約 L786~L1494）：
- `BRIDGE_QUERY_MEMORY_STATS` case handler
- `BRIDGE_URL` 常數、`bridgeSocket` 變數、所有 bridge 相關型別
- `BridgeCommandMsg` interface
- `runBridgeCommand()` 函式
- 所有 `BRIDGE_*` 的 content script 呼叫（`BRIDGE_CLICK`、`BRIDGE_PIN_ELEMENT`、`BRIDGE_PIN_ANALYZE`、`BRIDGE_MAP_ZONES`、`BRIDGE_ZONE_*`、`BRIDGE_SHOW_PANEL`、`BRIDGE_HIDE_PANEL`、`BRIDGE_PATROL_PINS`、`BRIDGE_REMOVE_PIN`、`BRIDGE_CLEAR_PINS`、`BRIDGE_GET_PIN_RESULTS`、`BRIDGE_GET_PAGE_HEALTH`、`BRIDGE_GET_WEB_VITALS`、`BRIDGE_ANALYZE_ELEMENT`、`BRIDGE_GET_BROWSER_ERRORS`、`BRIDGE_TYPE_TEXT`、`BRIDGE_READ_PAGE`）
- `BridgeQueryWaiter` interface、`bridgeQueries` map、`bridgeQuerySeq`
- `queryBridge()` 函式
- `scheduleBridgeReconnect()` 函式
- `connectBridge()` 函式
- `ensureBridge()` 及其所有 listener（`onStartup`、`onInstalled`、`tabs.onActivated`、`tabs.onUpdated`）
- 底部的 `connectBridge()` 初始呼叫

#### 3) content.ts — 移除 bridge message handler
搜尋所有 `BRIDGE_` 開頭的 message type handler 並移除：
- `BRIDGE_CLICK`、`BRIDGE_PIN_ELEMENT`、`BRIDGE_PIN_ANALYZE`
- `BRIDGE_MAP_ZONES`、`BRIDGE_ZONE_HEALTH`、`BRIDGE_SHOW_ZONE_OVERLAY`、`BRIDGE_HIDE_ZONE_OVERLAY`、`BRIDGE_ZONE_CHANGES`、`BRIDGE_STOP_WATCH_ZONES`、`BRIDGE_ZONE_ERRORS`、`BRIDGE_WATCH_ZONES`
- `BRIDGE_SHOW_PANEL`、`BRIDGE_HIDE_PANEL`
- `BRIDGE_PATROL_PINS`、`BRIDGE_REMOVE_PIN`、`BRIDGE_CLEAR_PINS`、`BRIDGE_GET_PIN_RESULTS`
- `BRIDGE_GET_PAGE_HEALTH`、`BRIDGE_GET_WEB_VITALS`、`BRIDGE_ANALYZE_ELEMENT`
- `BRIDGE_GET_BROWSER_ERRORS`、`BRIDGE_TYPE_TEXT`、`BRIDGE_READ_PAGE`

#### 4) inject.ts — 移除 bridge message handler
搜尋所有 `BRIDGE_` 開頭的 handler 並移除（如果有的話）

#### 5) popup.ts — 清除 bridge 相關 UI
- 移除 bridge 連線狀態顯示（如果有）
- 移除 `BRIDGE_QUERY_MEMORY_STATS` 的呼叫

#### 6) manifest.json — 確認不影響
- bridge 不需要額外權限，移除不影響 manifest
- 確認 `host_permissions` 裡沒有 `ws://127.0.0.1:*` 之類的（如果有就刪）

#### 7) 不動的部分
- `bridge/` 目錄不刪（它不在 extension 打包範圍內）
- `mcp-server/` 不動（8 支 cloud 工具保留）
- 所有大黃蜂視覺保留
- 所有票券功能保留
- `server/` 不動

#### 8) Build + 測試
- `npm run build` 確認編譯通過
- 端到端測試（bridge 相關的標記為 skip 或預期減少）
- extension zip 大小應該會變小

**驗收**：
- `release/v1.2.0` 分支建立
- background.ts 無任何 `bridge`、`WebSocket`、`ws://127.0.0.1` 引用
- content.ts / inject.ts 無 `BRIDGE_` handler
- Build 成功，extension 功能正常（錄製、截圖、語音、上傳、票券）
- 大黃蜂視覺完整保留

---

🔵 **DONE-439**

分支 `release/v1.2.0` 已建立並 commit `2201eb3`（**只在分支上，master 沒動**）。

| 驗收 | 結果 |
|---|---|
| `release/v1.2.0` 分支建立 | ✅ |
| background.ts 無 `bridge` / `WebSocket` / `ws://127.0.0.1` | ✅ 全檔 0 次命中 |
| content.ts / inject.ts 無 `BRIDGE_` handler | ✅ 0 |
| Build 成功 | ✅ `tsc` exit 0、`npm run build` 通過 |
| 大黃蜂視覺完整保留 | ✅ `_verify403` 273 passed / 0 failed |
| 端到端 | ✅ 分支上 **635 passed, 0 failed, 8 skipped**；另 4 套整套 SKIP |

**刪掉的量**：`background.ts` 1494 → 822 行（**−672**）、`content.ts` 3954 → 3258 行（**−696**）、`types.ts` −17 個訊息型別、`i18n.ts` −15 條 key。同樣 esbuild 參數（minify）下：**background 27.5 KB → 14.4 KB（−48%）、content 185.7 KB → 164.9 KB（−11%）**。

---

## 🔴 卡片有一條會弄壞 popup，我改了做法

卡片 §3 要求「搜尋所有 `BRIDGE_` 開頭的 handler 並移除」，但 **`BRIDGE_` 是歷史誤名** —— 23 個裡有 **8 個是 popup 自己的協定**，跟 bridge 一點關係都沒有：

| 訊息 | 誰在用 |
|---|---|
| `BRIDGE_PIN_ANALYZE` / `BRIDGE_REMOVE_PIN` / `BRIDGE_CLEAR_PINS` / `BRIDGE_PATROL_PINS` | popup 的**釘選**功能 |
| `BRIDGE_MAP_ZONES` / `BRIDGE_ZONE_HEALTH` / `BRIDGE_GET_PAGE_HEALTH` / `BRIDGE_GET_BROWSER_ERRORS` | popup 的**偵察模式**一鍵全掃 |

照字面刪光，這兩個功能（PM-419／420 才剛做完大黃蜂改版的那兩塊）當場失效，而同一張卡的驗收又寫「extension 功能正常」「大黃蜂視覺完整保留」—— 自相矛盾。

**做法：bridge 專用的刪掉、popup 用的改名成 `PIN_*` / `SCOUT_*`。** 這樣「無 `BRIDGE_` handler」與「功能正常」兩條驗收**同時**成立，而不是二選一。

## 連帶被帶走的三塊 UI（要你確認）

`tsconfig` 開了 `noUnusedLocals`，所以只要沒人呼叫就編不過 —— 我讓 tsc 一輪一輪點名，總共刪掉 **33 個宣告**。其中三塊是**真的看得到的 UI**，因為它們的唯一入口就是 bridge 工具：

1. **頁內除錯面板**（PM-339，`show_debug_panel`）—— PM-429 才剛換過大黃蜂視覺。除了 bridge 沒有任何地方打得開它。
2. **區域覆蓋層**（PM-344，`show_zone_overlay`）—— popup 的偵察模式只取數據，不畫覆蓋層。
3. **區域監看**（PM-345，`watch_zones` 計時器）與 **`read_page` 全套**（PM-309，約 170 行）。

要保留的話得在 popup 開新入口（那是另一張卡）。**現在是整組刪掉。**

另外 **popup 的「記憶矩陣」整塊移除**（卡片 §5 有要求）：它是 bridge 唯讀查詢的前端，bridge 沒了只會永遠顯示「Bridge 未連線」。UI、事件綁定、15 條 i18n key 一起清乾淨。偵察模式從三塊變兩塊（圖釘／AI 監測）。

## 回歸測試：4 套整套 SKIP，不是讓它們變紅燈

四套是純測 bridge 的（`_verify309` read_page、`_verify383` 釘選 bridge 側、`_verify392` pin_analyze、`_verify_phase1` navigate/click），在分支上加了自動判斷：

```js
if (!background.ts.includes('BRIDGE_URL')) { console.log('SKIP …'); process.exit(0); }
```

`_verify403` 則是**個別條目** SKIP（8 條記憶矩陣／bridge 通道），偵察模式的區塊數改成參數（有 bridge = 3、沒有 = 2），一鍵全掃的斷言同時接受 `BRIDGE_*` 與 `SCOUT_*` 兩種命名。**這些守衛在 master 上完全不會觸發**（那邊有 `BRIDGE_URL`），可以安全合併回 master 讓兩邊測試檔保持同步。

讓它們長期紅著只會訓練大家忽略紅燈，所以選 SKIP 而不是放著不管。

## 沒動的

`bridge/` 目錄、`mcp-server/`、`server/`、票券、大黃蜂視覺、`manifest.json`（本來就沒有 `ws://` 或 `host_permissions`，權限維持 5 項）。

## ⚠ 兩件要你決定

1. **版本號沒動**（仍是 1.1.5）。分支叫 `release/v1.2.0`，但卡片 §6 只說「確認不影響 manifest」，沒說要改版號 —— 要 bump 跟我說。
2. **`bridgeXxx()` 這些函式名留著沒改**（`bridgeMapPageZones` / `bridgeGetZoneHealth` / `bridgePinElement` …）。它們現在是 popup 偵察模式與釘選的實作，名字是歷史包袱。改名要動幾十處呼叫點、風險大於好處，所以只在原始碼加了一段註解說明來歷。要清就開一張卡。
3. **這份 DONE 只 commit 在分支上**，master 的 `job-0905.md` 不會有。要同步到 master 我再處理。

### 🟡 PM-440：版本號 bump 到 v1.2.0

**執行**（在 `release/v1.2.0` 分支上）：

1. `extension/manifest.json` — `"version": "1.1.5"` → `"1.2.0"`
2. `package.json` — `"version"` → `"1.2.0"`
3. `server/src/index.ts` — footer 裡的版本號更新
4. `CHANGELOG.md` — 頂部加 v1.2.0 區塊：
   ```
   ## v1.2.0（2026-09-05）— 大黃蜂視覺系統 + 精簡架構
   - 全站大黃蜂視覺改版（Popup / 截圖標注 / 報告編輯 / 付款 / 麥克風授權 / 頁內注入 UI / 分享報告頁 / 官網 11 頁）
   - 移除 bridge 模組，降低複雜度
   - Extension 打包體積 background −48%、content −11%
   ```
5. commit + push 到 `release/v1.2.0` 分支

**驗收**：manifest / package.json / footer 都顯示 1.2.0，build 成功

---

🔵 **DONE-440**

| 驗收 | 結果 |
|---|---|
| `extension/manifest.json` | ✅ `1.1.5` → **`1.2.0`**（`dist/manifest.json` 也是 1.2.0） |
| `package.json` | ✅ `0.1.0` → **`1.2.0`**（根目錄那份，見下面的說明） |
| 官網 footer | ✅ `© 2026 BugEzy · v1.2.0` |
| `CHANGELOG.md` | ✅ 頂部加 v1.2.0 區塊 |
| build 成功 | ✅ extension／server `tsc` 皆 exit 0、`npm run build` 通過 |
| 端到端 | ✅ **635 passed, 0 failed, 8 skipped**（另 4 套整套 SKIP） |

---

## `package.json` 從來不是版本來源

六個 `package.json` 全部是 **`0.1.0`**（只有 `cli/` 是 `1.1.0`）—— 從 v1.0.0 一路到 v1.1.5 都沒有人動過它們，真正的版本來源一直是 `manifest.json`。

照卡片把**根目錄**那份改成 `1.2.0` 了，但這等於**新開一個慣例**，而其餘五個（`extension/` `server/` `bridge/` `mcp-server/` 各 0.1.0、`cli/` 1.1.0）仍然對不上。要嘛全部跟著版本走，要嘛全部維持 0.1.0 —— **請你決定**，我沒有自己擴大範圍去改另外五個。

## 🔴 `/api/version` 刻意沒有改，理由要看一下

`server/src/index.ts` 裡還有四處 v1.1.5：`/api/version`、官網 `/changelog` 的版本條目、SKILL 手冊的「最新版本」與「v1.1.5 新功能」。**全部保持 1.1.5**，因為：

1. **`/api/version` 是 popup 更新提示的來源。** 在 Chrome Web Store 真的上架 1.2.0 之前把它改成 1.2.0，等於對所有現役 1.1.5 使用者謊報「有新版可裝」，而他們去商店根本裝不到。
2. **server 是從 master 部署的**，這個分支改了也不會生效；真要生效的正確順序是「商店審核通過 → 才 deploy 帶 `/api/version = 1.2.0` 的 server」。

同理，官網 `/changelog` 與 SKILL 手冊也還沒加 v1.2.0 段落 —— 那是「已發佈」才該寫的東西。

## ⚠ 順手發現一個既有的判斷 bug（建議開卡）

`popup.ts` 的更新提示是這樣判的：

```js
if (data.latest && data.latest !== currentVersion) { /* 顯示「有新版」 */ }
```

**是「不相等」，不是「比較新」。** 所以現在這個分支的狀態下，本機載入 1.2.0 的 build 時，`/api/version` 回 `1.1.5`，popup 會跳出「有新版 **1.1.5** 可用」—— 版本號往回走也照跳。這在 1.1.5 時代看不出來（兩邊一直相等），但只要商店版與 server 版有任何落差就會誤報，兩個方向都會。

應該改成 semver 比較「確實更新才提示」。這不在這張卡的範圍，**建議開一張卡**。

## 沒做的

- **沒有打包 zip**（卡片沒要求；要送審再說）。
- **master 沒動** —— 版本號、CHANGELOG 的 v1.2.0 區塊都只在 `release/v1.2.0` 上。

### 🟡 PM-441：打包 v1.2.0 extension zip 送審

**執行**（在 `release/v1.2.0` 分支上）：

1. `npm run build` 確認最終 build 乾淨
2. 打包 extension zip（排除 bridge、node_modules、測試檔、設計稿）
3. 確認 zip 內容：manifest.json version=1.2.0、所有大黃蜂資源、bee.webm、無 bridge 程式碼
4. zip 放到 `dist-zip/bugezy-v1.2.0.zip`
5. 報告 zip 大小

**驗收**：`dist-zip/bugezy-v1.2.0.zip` 產出，manifest 版本 1.2.0，無 bridge 相關檔案

---

🔵 **DONE-441**

`dist-zip/bugezy-v1.2.0.zip` 已產出：**3,314,965 bytes（3237.3 KB）／25 entries**。

| 驗收 | 結果 |
|---|---|
| zip 產出於 `dist-zip/` | ✅ |
| manifest 版本 1.2.0 | ✅ 從 **zip 內**讀出來確認，不是看 dist |
| 無 bridge 相關檔案 | ✅ `background.js` / `content.js` / `popup.js` / `inject.js` 四支都掃過 `BRIDGE_`、`ws://127.0.0.1`、`bridgeSocket`、`connectBridge`，全 0 |
| 大黃蜂資源齊全 | ✅ 六角圖示 16/32/48/128 四尺寸、`bee-video.js`、`assets/bee.webm` |
| build 乾淨 | ✅ `npm run build`（正式版，非 `build:dev`）；`host_permissions` 為空、權限維持 5 項 |
| 無 sourcemap | ✅ 零 `.map`（PM-283 之後正式 build 本來就不產） |

## 排除了三類東西

| 排除 | 為什麼 |
|---|---|
| `icons/proposals/`（6 檔） | 六個 icon 設計提案，上架等於公開設計原稿（PM-251／281 就是這樣處理的） |
| `icons/icon-source.svg` | 同上，是原始向量檔 |
| `assets/hornet-real.png`（71 KB） | **擴充功能根本沒用到它** —— PM-424 把登入頁的靜態備援拿掉之後，原始碼裡只剩一段解釋「為什麼移除」的註解。它是官網 hero 用的（PM-434 已改由 server 的 `/hornet-real.png` 提供） |

`build.mjs` 是整個 `src/assets/` 遞迴複製，所以 `hornet-real.png` 還是會進 `dist/`。這次在打包階段排除；**要根治的話得改 `build.mjs` 的資產清單**，那是另一張卡。

## 🔴 包大小：84.5% 是那支影片

| | 大小 |
|---|---|
| v1.1.5（當時） | 407.8 KB / 22 entries |
| **v1.2.0（完整）** | **3237.3 KB** / 25 entries |
| v1.2.0 不含 `bee.webm` | **503.3 KB** |

**`bee.webm` 一支就佔 2734 KB（84.5%）。** 也就是說：程式碼本身從 407.8 KB 只長到 503.3 KB（+95 KB，多的是大黃蜂視覺的 CSS 與四個尺寸的新圖示），**其餘全部是登入頁那支蜂的影片**。

Chrome Web Store 沒有硬性大小上限（限制在單檔 > 100 MB 才會擋），所以能上架；但 3.2 MB 的擴充功能安裝體感差很多，而且它只為了登入頁一個 92px 的動畫。**建議壓過再送審** —— 同一支 webm 壓到 300~500 KB 是很容易的（降解析度到 200px 見方、降 fps、提高 CRF），整包會回到 800 KB 以內。這件事你之前就記過待辦了，現在有具體數字。

## 沒做的

- **zip 沒有進版控**：`.gitignore` 第 16~17 行本來就有 `dist-zip/` 與 `bugezy-v*.zip`，這是刻意的（舊的 1.1.0~1.1.3 是在那條規則之前才被 commit 的）。
- **沒有上傳到 Chrome Web Store** —— 那要你的帳號操作。上架前請再確認一次 PM-440 提的：`/api/version` 要等商店真的通過之後才改成 1.2.0。
