# BugEzy Job — 2026-06-24

> 接續 job-0622.md（PM-54~59）
> 上架前功能補齊（續）

---

### 🟡 PM-60：免費版 AI 語音校正按鈕（Workers AI LLM）

**目標**：編輯頁的語音記錄區加一個「🔧 AI 校正」按鈕，用 Workers AI Llama 3.1 修正語音辨識的錯字、贅字、術語。跟現有的「🤖 AI 精簡」並列。

---

#### §1 差異：校正 vs 精簡

| | 🔧 AI 校正（新） | 🤖 AI 精簡（現有） |
|---|---|---|
| 目的 | 修錯字，保留原意 | 移除廢話，濃縮重點 |
| 輸出長度 | 跟原文差不多 | 比原文短很多 |
| 使用時機 | 語音有錯字時 | 語音太囉嗦時 |
| 可疊加 | 先校正 → 再精簡 | ✅ |

---

#### §2 Server 改動（POST /api/correct）

新增 API 端點，跟 `/api/summarize` 類似但用不同 System Prompt：

```ts
if (url.pathname === '/api/correct' && request.method === 'POST') {
  const { text } = await request.json() as { text: string };
  if (!text?.trim()) {
    return json({ error: '沒有文字可校正' }, 400);
  }

  const result = await env.AI.run('@cf/meta/llama-3.1-8b-instruct', {
    messages: [
      {
        role: 'system',
        content: `你是語音轉文字的校對專家。請修正以下語音辨識的原始文字：

規則：
1. 修正同音錯字（例：噴五白 → 噴 500、台破 → TypeError）
2. 移除口語贅字（呃、那個、就是說、然後然後、對對對）
3. 技術術語保持英文原文（console error、TypeError、404、undefined、null、fetch、API）
4. 數字和 HTTP 狀態碼用阿拉伯數字（五百 → 500、四零四 → 404）
5. 保留原始語意和描述順序，不改寫不摘要不增加內容
6. 適當加入標點符號，讓句子更好閱讀
7. 如果原文已經很正確，就原樣回傳

只回傳校正後的文字，不加任何說明或前綴。`,
      },
      {
        role: 'user',
        content: text,
      },
    ],
  });

  const corrected = (result as any)?.response?.trim() || text;
  return json({ corrected });
}
```

---

#### §3 edit-report.html 改動

在「🤖 AI 精簡」按鈕旁邊加「🔧 AI 校正」：

```html
<div class="voice-actions">
  <button id="correctBtn" class="btn btn-accent">🔧 AI 校正</button>
  <button id="summarizeBtn" class="btn btn-accent">🤖 AI 精簡</button>
</div>
```

CSS：
```css
.voice-actions {
  display: flex;
  gap: 8px;
  margin-top: 8px;
  justify-content: flex-end;
}
```

---

#### §4 edit-report.ts 改動

```ts
const correctBtn = $opt('correctBtn');
correctBtn?.addEventListener('click', async () => {
  const text = voiceText.value.trim();
  if (!text) return;
  
  correctBtn.disabled = true;
  correctBtn.textContent = '🔧 校正中...';
  
  try {
    const res = await fetch(`${API_BASE}/api/correct`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ text }),
    });
    const data = await res.json();
    if (data.corrected) {
      voiceText.value = data.corrected;
      correctBtn.textContent = '✅ 已校正';
    } else {
      correctBtn.textContent = '❌ 校正失敗';
    }
  } catch {
    correctBtn.textContent = '❌ 校正失敗';
  }
  
  // 3 秒後恢復按鈕（可以再按一次微調）
  setTimeout(() => {
    correctBtn.disabled = false;
    correctBtn.textContent = '🔧 AI 校正';
  }, 3000);
});
```

⚠ 注意：AI 校正不像 AI 精簡那樣只能按一次。校正可以按多次（每次微調），所以**不鎖死按鈕**，3 秒後恢復。

---

#### §5 不改的東西
- inject.ts / content.ts / background.ts / cli：不動
- annotate 的語音不加校正（標注頁的語音很短，不需要）
- AI 精簡的邏輯不動

#### Build + Deploy
```
cd extension && npm run build
cd ../server && npx wrangler deploy
```

#### 驗收（用測試專頁）

1. 錄製 → 故意講一些口語廢話和錯字 → 停止 → 編輯頁
2. 語音記錄區應有兩個按鈕：「🔧 AI 校正」+「🤖 AI 精簡」
3. 按「🔧 AI 校正」→ 文字變乾淨（修錯字 + 去贅字 + 術語還原）
4. 可以再按一次微調
5. 也可以接著按「🤖 AI 精簡」進一步濃縮

---

### 🔵 DONE-60：免費版 AI 語音校正按鈕 ✅ 已部署（⚠ 模型品質需 FOX 決策）

**改動**：`server/src/index.ts` + `edit-report.html` + `edit-report.ts`（inject/content/background/cli/annotate 不動，與規格一致）。

**§2 server**：新增 `POST /api/correct`（`correctText()`，校對 system prompt，回 `{ corrected }`；空字串 → 400）。
**§3 html**：`transcript-header` 內把 `summarizeBtn` 包進 `.ai-actions`（flex gap），前面加 `🔧 AI 校正` `#correctBtn`（同 `ai-btn` 樣式）。
**§4 ts**：`correctBtn` handler——`/api/correct`、成功寫回 `voiceText.value`、「✅ 已校正」、**3 秒後恢復不鎖死**（可多次微調）；失敗顯示「❌ 校正失敗」。

**據實修正**：規格用 `@cf/meta/llama-3.1-8b-instruct`，但該模型 **2026-05-30 已 deprecated**（PM-25 踩過）→ 改用 summarize 同款 `@cf/meta/llama-3.3-70b-instruct-fp8-fast`。

**build / deploy**：extension `npm run build` ✅ + `tsc` ✅；server `tsc` ✅ + `wrangler deploy` ✅（Version `cdac796b`）。

**🚨 模型品質問題（請 FOX 決策）**：端點/驗證/按鈕全部接好，但**免費 Workers AI 模型校正繁中語音不可靠**——線上實測：
- 「…台破的錯誤…四零四」→ 回「無法辨識的文字，請提供正確的輸入。」（**拒絕校正**）
- 乾淨句子 → 被截斷/亂改成「出現了 console error，顯示 { TypeError 的錯誤，」
- 英文偏多的句子才校得不錯。
我已把規格 prompt **強化兩次**（加防拒絕指令 + 一次性範例 + 改寫 user turn）仍無解；`/api/summarize` 用同模型卻正常（因為它是「轉成重點」而非「原樣校正」，模型擅長前者）。這是**模型能力限制非程式 bug**。建議：① FOX 先用真實錄音實測（較長有上下文也許不同）；② 若仍差，另開 PM 換 zh-TW 較強的模型（如 `@cf/qwen/...`）——換模型屬產品決策且怕影響共用模型，我未自作主張處理。

**待 FOX 驗收 / 決策**：實機按「🔧 AI 校正」看真實錄音效果；決定是否換模型。功能結構與部署已完成。

---
