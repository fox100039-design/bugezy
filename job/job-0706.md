# job-0706.md — BugEzy Day 21

> 接續 job-0705.md（PM-153~169）

---

### 🟡 PM-170：免費版每月用量重置 + 回溯檢查補齊 + 用完升級引導

**三個缺口一次修**：
1. **用量不重置**：recording/rewind/mcp_count 一生累加，用完永久鎖住
2. **回溯沒用量檢查**：按「回溯 30s」不檢查 rewind_count
3. **用完沒引導**：403 只顯示錯誤訊息，沒有引導到日票/月費

---

#### §1 Supabase：users 表加 usage_reset_at

FOX 手動跑：
```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS usage_reset_at TIMESTAMPTZ DEFAULT NOW();
```

---

#### §2 Server bumpUsage：每月自動重置

bumpUsage 開頭檢查 usage_reset_at，超過 30 天就歸零：

```ts
const resetAt = new Date(u.usage_reset_at || 0);
const now = new Date();
const daysSinceReset = (now.getTime() - resetAt.getTime()) / (1000 * 60 * 60 * 24);

if (daysSinceReset >= 30) {
  await supa(env).from('users').update({
    recording_count: 0, rewind_count: 0, mcp_count: 0,
    usage_reset_at: now.toISOString(),
  }).eq('user_id', userId);
  u.recording_count = 0;
  u.rewind_count = 0;
  u.mcp_count = 0;
}
```

bumpUsage 的 select 加 `usage_reset_at`。

---

#### §3 Server getUserPlan：回傳用量資訊

確認 getUserPlan 回傳包含 usage：
```ts
usage: {
  recording: { used: u.recording_count || 0, max: FREE_LIMITS.recording },
  rewind: { used: u.rewind_count || 0, max: FREE_LIMITS.rewind },
  mcp: { used: u.mcp_count || 0, max: FREE_LIMITS.mcp },
},
usage_reset_at: u.usage_reset_at,
```

---

#### §4 Extension background.ts：回溯加用量檢查

新增 checkRewindUsage()，仿照 checkRecordingUsage()：
```ts
async function checkRewindUsage(): Promise<string | null> {
  const headers = await getAuthHeaders();
  if (!headers.Authorization) return null;
  try {
    const res = await fetch(`${API_BASE}/api/user/usage`, {
      method: 'POST', headers,
      body: JSON.stringify({ type: 'rewind' }),
    });
    if (res.status === 403) {
      const err = await res.json();
      if (err.error === 'limit_reached') return err.message || '回溯額度已用完';
    }
    return null;
  } catch { return null; }
}
```

在 startRewind() 最前面呼叫，達上限不進入回溯。

---

#### §5 Extension popup.ts：免費版顯示剩餘次數

popup 三大模式卡片下方顯示剩餘次數：
- 免費版：「剩 7 次」/ 「剩 3 次」/ 「✨ 無限」（截圖）
- 付費版：「✨ 無限次」
- 剩 2 次以下 → 紅色警告

---

#### §6 Extension popup.ts：用完後彈升級引導

403 limit_reached 時彈 overlay：

```html
<div id="upgrade-prompt" class="upgrade-prompt">
  <div class="upgrade-prompt-card">
    <p class="upgrade-icon">📋</p>
    <p class="upgrade-title">本月額度已用完</p>
    <p class="upgrade-desc">錄製 10/10 次已使用</p>
    <button class="day-pass-btn">⚡ 日票 NT$20（24hr 無限）</button>
    <button class="monthly-btn">✨ 月費 NT$80/月（最划算）</button>
    <p class="upgrade-hint">💡 免費額度每月自動重置</p>
  </div>
</div>
```

按日票/月費跳到對應付款頁。可關閉。

---

#### §7 i18n 支援

```ts
'usage-exhausted':   { zh: '本月額度已用完', en: 'Monthly quota exhausted' },
'usage-desc-record': { zh: '錄製 {used}/{max} 次已使用', en: 'Recording {used}/{max} used' },
'usage-desc-rewind': { zh: '回溯 {used}/{max} 次已使用', en: 'Rewind {used}/{max} used' },
'usage-desc-mcp':    { zh: 'AI 讀取 {used}/{max} 次已使用', en: 'AI reads {used}/{max} used' },
'usage-reset-hint':  { zh: '💡 免費額度每月自動重置', en: '💡 Free quota resets monthly' },
'remaining':         { zh: '剩 {n} 次', en: '{n} left' },
'unlimited':         { zh: '✨ 無限次', en: '✨ Unlimited' },
```

---

#### §8 不改的東西

- ECPay / 認證 / MCP tool：不動
- 截圖無限：不動
- 付費版：不動（unlimited）

#### 前置：FOX 跑 SQL（§1）

#### Build + Deploy

```
cd server && npx wrangler deploy
cd extension && npm run build
```

#### 驗收

1. 免費用戶 → popup 顯示「剩 N 次」
2. 錄製用完 → 彈升級引導（日票/月費）
3. 回溯用完 → 同上
4. 剩 2 次以下 → 紅色
5. 付費版 → 「✨ 無限次」
6. usage_reset_at 改 31 天前 → 操作 → count 歸零
7. 中英文正確

---

### ✅ DONE-170 — Claude Code 回報（2026-07-05）

**免費版每月用量重置 + 回溯用量檢查補齊 + 用完升級引導 overlay**：

1. **§1 usage_reset_at 欄位**：**已存在**（PM-63 已加入 `schema.sql`：`ALTER TABLE users ADD COLUMN IF NOT EXISTS usage_reset_at TIMESTAMPTZ DEFAULT NOW()`，且 `getUserPlan` 早已讀取它）——**FOX 無需再跑 SQL**。
2. **§2 bumpUsage 每月自動重置**：`bumpUsage` select 加 `usage_reset_at`，免費用戶（付費上面已 early-return）進來後檢查「距上次重置 ≥30 天」→ 把 `recording_count`/`rewind_count`/`mcp_count` 歸零 + `usage_reset_at=now`。杜絕「用完永久鎖住」。
3. **§3 getUserPlan 回傳用量**：`limits`（既有欄位，popup 已消費：recording/rewind/mcp 的 used/max）即 spec 的 `usage`；另加回 `usage_reset_at`。
4. **§4 回溯用量檢查**：`background.ts` 把 `checkRecordingUsage` 抽成泛型 `checkUsage(type)`，新增 `checkRewindUsage`（type:'rewind'）；`REWIND_30S` handler 最前面呼叫，達上限回 `{ ok:false, limitReached }` 不進入回溯。
5. **§5 popup 三卡片剩餘次數**：`renderFreeUsage()` 於免費版設 錄製/回溯「剩 N 次」（`setActionDesc` + **剩 ≤2 次 `.low` 紅色**）、截圖「✨ 無限」；付費/日票/取消 → 三卡片皆「✨ 無限次」。
6. **§6 用完升級引導 overlay**：新增 `#upgradeOverlay` modal（📋 + 本月額度已用完 + 「錄製/回溯 N/N 次已使用」+ ⚡日票 + ✨月費 + 💡每月自動重置 + 關閉）；錄製/回溯 403 `limitReached` → `showUpgradeOverlay(type)`（used/max 取自快取 `freeLimits`）；日票/月費鈕跳對應付款頁；點背景/✕ 關閉。
7. **§7 i18n**：加 `usage-exhausted`/`usage-desc-record`/`usage-desc-rewind`/`usage-desc-mcp`/`usage-reset-hint`/`day-pass-btn-full`/`monthly-btn-full`（中英），`remaining`/`unlimited`/`used-up` 沿用既有。

**設計判斷**：`bumpUsage` 用 spec 指定的 **30 天滾動窗**，`getUserPlan` 沿用既有**月曆邊界**重置——兩者皆更新 `usage_reset_at` 且不會重複重置（先觸發者歸零、後者看到近期 reset 就不動）；差異僅「先開 popup（月界）vs 先錄製（30 天）」的觸發時機，無鎖死/無雙重扣，屬可接受（如需完全一致可後續統一為 30 天）。

**驗收**：server `wrangler deploy` ✅（Version `a4c29ca8`）；server + extension `tsc` ✅；extension `npm run build` ✅。
- ✅ 端點無回歸：`GET /api/user/plan`、`POST /api/user/usage` 未帶 auth → 401。
- ✅ 源碼確認：server 含 `daysSinceReset >= 30` 歸零邏輯；dist（popup.js/background.js）含 `checkRewindUsage`/`showUpgradeOverlay`/`renderFreeUsage`/`classList.toggle`/`usage-desc-record`/`usage-reset-hint`；dist/popup.html 含 `upgradeOverlay`/`overlayDayPassBtn`/`usage-exhausted`/`upgrade-overlay`。
- ⚠ 需**實機驗收**（瀏覽器載入擴充 + 付費用戶 session）：剩 N 次顯示/紅色、用完彈 overlay、日票/月費跳轉、`usage_reset_at` 改 31 天前 → 操作 → count 歸零、中英切換。server 用量流程需有效 session 才能驅動，無現成憑證故以 tsc + 端點 401 + 源碼佐證。

**FOX 待辦**：① extension 需**重上架**（本卡 popup/background/i18n + 沿用 Day 19/20 整套）；② §1 SQL 已存在無需跑。

**DONE-170**

---
