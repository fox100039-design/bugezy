# BugEzy Job — 2026-06-30

> 接續 job-0629.md（PM-74~79）
> 首頁受眾擴展 + 域名綁定

---

### 🟡 PM-80：首頁更新受眾定位（涵蓋所有 Web 開發者）

**問題**：目前首頁給人感覺只針對 HTML 前端開發者。實際上 BugEzy 支援所有有 Web 前端的開發框架。

**改動**：更新 HOMEPAGE_HTML 的內容，擴大受眾定位。

---

#### §1 主標語改動

舊：
```
開發者 Bug 報告工具，AI 幫你修
```

新：
```
Web 開發者的 AI Bug 報告工具
前端後端一起抓，10 分鐘修好 Bug
```

---

#### §2 新增「支援的開發框架」區塊

在六種模式下方、定價上方，新增一個區塊：

```html
<section>
  <h2>支援所有 Web 開發框架</h2>
  <p>只要你的產品跑在瀏覽器上，BugEzy 就能用</p>
  
  <div class="framework-grid">
    <!-- 前端 -->
    <div class="fw-category">
      <h3>🖥 前端框架（Chrome 擴充錄製）</h3>
      <div class="fw-tags">
        <span>React</span>
        <span>Vue</span>
        <span>Angular</span>
        <span>Next.js</span>
        <span>Nuxt</span>
        <span>Svelte</span>
        <span>HTML/CSS/JS</span>
      </div>
    </div>
    
    <!-- 後端 -->
    <div class="fw-category">
      <h3>⚙ 後端框架（終端機 CLI 攔截）</h3>
      <div class="fw-tags">
        <span>Django</span>
        <span>Flask</span>
        <span>FastAPI</span>
        <span>Laravel</span>
        <span>Rails</span>
        <span>Spring Boot</span>
        <span>Express</span>
        <span>Node.js</span>
      </div>
    </div>
  </div>
  
  <p class="fw-note">前端用 Chrome 擴充錄製 DOM + Console + Network<br>後端用 <code>npx bugezy-watch -- python manage.py runserver</code> 攔截 stderr</p>
</section>
```

CSS（fw-tags 用標籤樣式，深色背景 + 紫色邊框）：
```css
.framework-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; margin: 24px 0; }
.fw-category { background: #1a1a2e; border-radius: 12px; padding: 20px; }
.fw-category h3 { font-size: 16px; margin-bottom: 12px; color: #a78bfa; }
.fw-tags { display: flex; flex-wrap: wrap; gap: 8px; }
.fw-tags span { background: #2a2a3e; border: 1px solid #7c3aed40; border-radius: 20px; padding: 4px 14px; font-size: 13px; color: #ccc; }
.fw-note { text-align: center; color: #888; font-size: 13px; margin-top: 16px; }
.fw-note code { background: #2a2a3e; padding: 2px 6px; border-radius: 4px; color: #10b981; }

@media (max-width: 600px) {
  .framework-grid { grid-template-columns: 1fr; }
}
```

---

#### §3 MCP 區塊更新

加上支援的 AI 工具列表：

```html
<div class="ai-tools">
  <h3>支援所有 MCP 工具</h3>
  <div class="fw-tags">
    <span>Claude Desktop</span>
    <span>Claude Code</span>
    <span>Cursor</span>
    <span>VS Code</span>
    <span>Zed</span>
    <span>Windsurf</span>
    <span>Codex</span>
    <span>Replit</span>
  </div>
  <p>一行 URL 連接，零安裝</p>
</div>
```

---

#### 不改的東西
- extension / cli：不動
- 定價區塊：不動
- 其他頁面（guide/faq/privacy）：不動

#### Deploy
```
cd server && npx wrangler deploy
```

---

### 🔵 DONE-80：首頁更新受眾定位（涵蓋所有 Web 開發者）✅ 已部署 + 線上驗證

**改動**：只改 `server/src/index.ts` 的 `HOMEPAGE_HTML`（extension/cli/定價/其他頁面不動）。

**§1 主標語**：`開發者 Bug 報告工具，AI 幫你修` → **`Web 開發者的 AI Bug 報告工具`** +（換行）**`前端後端一起抓，10 分鐘修好 Bug`**。

**§2 新增「支援所有 Web 開發框架」區塊**（`<section id="frameworks">`，插在六種模式 `#modes` 之後、定價 `#pricing` 之前）：
- 兩欄 `.framework-grid`：**前端框架（Chrome 擴充錄製）** React/Vue/Angular/Next.js/Nuxt/Svelte/HTML·CSS·JS；**後端框架（終端機 CLI 攔截）** Django/Flask/FastAPI/Laravel/Rails/Spring Boot/Express/Node.js。
- `.fw-note`：前端 Chrome 擴充錄 DOM+Console+Network、後端 `npx bugezy-watch -- python manage.py runserver` 攔 stderr。

**§3 MCP 工具列表**（`.ai-tools`，併入同區塊內）：支援所有 MCP 工具 — Claude Desktop/Claude Code/Cursor/VS Code/Zed/Windsurf/Codex/Replit + 「一行 URL 連接，零安裝」。

**CSS**：`.framework-grid`/`.fw-category`/`.fw-tags`（深色膠囊標籤 + 紫框）/`.fw-note`/`.ai-tools` + **RWD `@media (max-width:600px)` 框架格改單欄**。

**tsc / deploy**：`tsc --noEmit` ✅ + `wrangler deploy` ✅（Version `c3fd3617`，cron `0 3 * * *` 仍在）。

**線上實測（urllib UTF-8）**：`GET /` → 8564 bytes（原 6021），新標語/兩類框架/全部標籤/`npx bugezy-watch -- python manage.py runserver`/MCP 8 工具/RWD media query 全數命中；**區塊順序 modes < frameworks < pricing** ✅；**定價仍含 NT$80（未動）** ✅；`/guide`、`/faq`、`/privacy` 仍 200。
> 註：部署後約 10 秒 edge 傳播延遲，稍候即為新版。

**待 FOX 驗收**：開 `https://bugezy-api.bugezy-api.workers.dev/` 看新標語 + 六模式下方的「支援所有 Web 開發框架」區塊（桌機兩欄、手機單欄）+ MCP 工具列。

---

### 🟡 PM-81：bugezy.dev 域名切換稽核（唯讀調查）

**背景**：bugezy.dev 已購買並綁定 Cloudflare Workers，DNS+SSL 已生效（2026-06-30 驗證通過）。Chrome Web Store + 綠界 ECPay 雙審核中，**嚴禁修改任何程式碼**。本任務純調查，產出遷移清單。

**目標**：掃描 codebase 所有 URL 引用，產出 `docs/domain-migration-checklist.md`。

---

#### §1 全域搜尋舊域名

```bash
cd C:\dev\bugezy
rg -n "bugezy-api.bugezy-api.workers.dev" --type-add "src:*.{ts,tsx,js,json,html,md}" -t src
```

記錄每個命中：檔案路徑 + 行號 + 用途（API call / MCP endpoint / 文件引用 / 設定檔）。

---

#### §2 查 Google OAuth 設定

1. 讀 `manifest.json` 的 `oauth2` 區塊（client_id / scopes）
2. 搜尋 `chrome.identity` 呼叫，確認 redirect URI 是 Chrome 內建還是自訂
3. 結論：切域名後 OAuth 需不需要改？

---

#### §3 查 API Base URL 設定

1. 搜尋 `API_URL` / `BASE_URL` / `apiUrl` / `baseUrl` / `endpoint` 等關鍵字
2. 確認寫死在程式碼裡 or 統一 config 檔 or 環境變數（.env / wrangler.toml）
3. 列出所有需要改的位置

---

#### §4 查 MCP 端點

1. 搜尋 `/mcp` 路由定義（Worker 端）
2. 確認給使用者的 MCP URL 寫在哪（guide 頁 / README / 文件）
3. 評估：是否讓 MCP 也走 `bugezy.dev/mcp`

---

#### §5 查 ECPay 回調 URL

1. 搜尋 `ReturnURL` / `NotifyURL` / `PaymentInfoURL` / `PeriodReturnURL`
2. 記錄目前回調指向哪個域名
3. 備註：換正式 key 時是否需同步改回調域名

---

#### §6 查首頁/文件 URL 引用

搜尋 `workers.dev` 出現在 HTML / MD 中的位置，列出面對使用者的項目。

---

#### 產出

`docs/domain-migration-checklist.md`，含以下表格：

1. **擴充功能程式碼**：檔案 / 行號 / 現值 / 改為 / 備註
2. **OAuth 設定**：現況 + 是否需改 + 改法
3. **MCP 端點**：現況 + 建議
4. **ECPay 回調**：現況 + 換 key 時一起改的項目
5. **文件/首頁引用**：檔案 / 行號 / 說明
6. **遷移順序建議**：排出執行先後

#### 鐵律

- ⛔ 不改任何 `.ts` / `.tsx` / `.js` / `.json` / `.html`
- ⛔ 不 `npm run build`、不 `wrangler deploy`
- ✅ 只讀、只搜、只產 `docs/domain-migration-checklist.md`

---

### 🟡 PM-82：報告頁截圖可視化勾選 + MCP 連動

**背景**：PM-57 已完成 MCP `get_screenshots` 的 `include_images` 分層控制，但目前只有 AI 自己決定是否帶 `include_images: true`。使用者無法主動告訴 AI「我這張截圖有視覺問題，請看圖」。

**目標**：在報告頁 `/report/:id` 新增一個勾選開關，讓使用者自己決定是否允許 AI 讀取截圖圖片。勾了 → MCP `get_screenshots` 自動回傳圖片，不用 AI 猜。

---

#### §1 Supabase 新增欄位

`reports` 表加一個欄位：

```sql
ALTER TABLE reports ADD COLUMN allow_screenshot_images BOOLEAN DEFAULT false;
```

`false` = AI 只拿 metadata（省 token）
`true` = AI 自動拿到圖片（視覺 Bug 用）

---

#### §2 報告頁 UI（server-rendered HTML）

在報告頁的 Screenshots 分頁區塊內（或分頁上方），加一個勾選開關：

```html
<div class="screenshot-toggle">
  <label>
    <input type="checkbox" id="allow-images-toggle"
           ${report.allow_screenshot_images ? 'checked' : ''} />
    <span class="toggle-label">📸 允許 AI 讀取截圖圖片</span>
  </label>
  <p class="toggle-hint" id="toggle-hint">
    ${report.allow_screenshot_images
      ? '✅ 已開啟 — AI 可直接看到截圖，適合視覺類 Bug（顏色、排版、CSS）'
      : '🔒 未開啟 — AI 只讀文字描述，省 Token。遇到視覺 Bug 時再開啟'}
  </p>
  <p class="toggle-token" id="toggle-token">
    ${report.allow_screenshot_images
      ? `⚠️ 每張截圖約 3,000~8,000 tokens（${report.screenshot_count || 0} 張 ≈ ${(report.screenshot_count || 0) * 5000} tokens）`
      : '💰 目前 AI 讀取此報告約 200~1,500 tokens'}
  </p>
</div>
```

---

#### §3 前端 JS：勾選即時儲存

報告頁底部加 inline script（不用 React，server-rendered 頁面用原生 JS）：

```js
const toggle = document.getElementById('allow-images-toggle');
const hint = document.getElementById('toggle-hint');
const token = document.getElementById('toggle-token');

toggle?.addEventListener('change', async () => {
  const allow = toggle.checked;
  // 即時更新提示文字
  hint.textContent = allow
    ? '✅ 已開啟 — AI 可直接看到截圖，適合視覺類 Bug（顏色、排版、CSS）'
    : '🔒 未開啟 — AI 只讀文字描述，省 Token。遇到視覺 Bug 時再開啟';
  token.textContent = allow
    ? '⚠️ 每張截圖約 3,000~8,000 tokens（SCREENSHOT_COUNT 張）'
    : '💰 目前 AI 讀取此報告約 200~1,500 tokens';
  // 存到 Supabase
  try {
    await fetch('/api/reports/' + REPORT_ID + '/settings', {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ allow_screenshot_images: allow }),
    });
  } catch (err) {
    console.error('儲存失敗', err);
  }
});
```

---

#### §4 新增 API endpoint

```
PATCH /api/reports/:id/settings
Body: { "allow_screenshot_images": true/false }
```

- 驗證 report_id 存在
- 更新 Supabase `reports.allow_screenshot_images`
- 回傳 `{ ok: true }`
- **不需登入驗證**（報告連結本身就是 share link，有連結就能改設定；跟目前報告頁可直接看的邏輯一致）

---

#### §5 MCP `get_screenshots` 改動

目前邏輯：依 `include_images` 參數決定是否回傳圖片。

新邏輯：**兩層判斷**

```ts
// 1. 讀報告設定
const { data } = await supabase()
  .from('reports')
  .select('screenshots_r2_key, screenshot_count, allow_screenshot_images')
  .eq('report_id', report_id)
  .single();

// 2. 決定是否回傳圖片
//    使用者勾了 OR AI 明確要求 → 回傳圖片
const shouldIncludeImages = data.allow_screenshot_images || include_images;
```

這樣的好處：
- 使用者勾了 → AI 不用帶參數就自動看到圖
- 使用者沒勾 → AI 仍然可以用 `include_images: true` 主動要求看圖
- 兩邊都有控制權，不衝突

---

#### §6 CSS 樣式

```css
.screenshot-toggle {
  padding: 16px;
  margin: 12px 0;
  background: #161b22;
  border: 1px solid #30363d;
  border-radius: 10px;
}
.screenshot-toggle label {
  display: flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
  font-size: 15px;
  color: #f0f6fc;
}
.screenshot-toggle input[type="checkbox"] {
  width: 18px;
  height: 18px;
  accent-color: #7c3aed;
}
.toggle-hint {
  margin-top: 8px;
  font-size: 13px;
  color: #8b949e;
}
.toggle-token {
  margin-top: 4px;
  font-size: 12px;
  color: #d29922;
  font-family: monospace;
}
```

---

#### §7 不改的東西

- extension / cli：不動
- 其他 MCP tools：不動（get_screenshots 以外的 tool 不受影響）
- 首頁 / guide / faq / privacy：不動
- DOM 相關（rrweb）：AI 自行調度，不加勾選

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. 用 BugEzy 截圖標注上傳一筆報告
2. 開報告頁 → 應看到「📸 允許 AI 讀取截圖圖片」勾選框（預設未勾）
3. 勾選 → 提示文字即時切換 + token 警告出現
4. 用 MCP `get_screenshots(report_id)` **不帶** `include_images` → 應自動回傳圖片（因為使用者已勾選）
5. 取消勾選 → MCP 再叫一次 → 只回 metadata
6. 手機版排版正常

---

### 🔵 DONE-82：報告頁截圖可視化勾選 + MCP 連動 ✅ 已部署 + 線上驗證（⚠ FOX 需先跑 SQL ALTER）

**改動**：`server/src/index.ts` + `server/schema.sql`（extension/cli/首頁/guide/faq/privacy/其他 MCP tool 皆不動，符合 §7）。

**§1 Schema**（`schema.sql`，**FOX 手動跑**）：`ALTER TABLE reports ADD COLUMN IF NOT EXISTS allow_screenshot_images BOOLEAN DEFAULT false`。

**§2+§3 報告頁 UI**（`REPORT_PAGE_HTML`，client-rendered）：Screenshots 分頁頂端加 `.screenshot-toggle` 勾選框「📸 允許 AI 讀取截圖圖片」+ 即時提示（`#toggle-hint` 已開/未開）+ token 警告（`#toggle-token`，開→「每張 3,000~8,000 tokens（N 張 ≈ N×5000）」、關→「約 200~1,500 tokens」）。預設依 `r.allowScreenshotImages` 顯示勾選狀態。`getReport` 回應加 `allowScreenshotImages`（用 `select('*')`，欄位未建時 `undefined→false`，**不破壞**）。

**§3 JS**：勾選 `change` → 即時更新提示文字 + `PATCH /api/reports/:id/settings`（原生 fetch，`reportId` 取自 path）。

**§4 新增 `PATCH /api/reports/:id/settings`**（`updateReportSettings`）：驗 body `allow_screenshot_images` 為 boolean（否則 400）→ 更新 Supabase → `{ ok:true }`。**不需登入**（有 share link 即可改，與報告頁可直接看一致）。

**§5 MCP `get_screenshots` 兩層判斷**：select 加 `allow_screenshot_images`，**`shouldIncludeImages = allow_screenshot_images===true || include_images===true`**——使用者勾了 AI 不帶參數就自動看圖、沒勾 AI 仍可主動 `include_images:true`。⚠ **防呆**：欄位未建時主查詢會錯 → 退回不含新欄位的 fallback 查詢（不誤判「沒截圖」、不崩）。

**§6 CSS**：`.screenshot-toggle`/`.toggle-hint`/`.toggle-token` 照規格。

**tsc / deploy**：`tsc --noEmit` ✅ + `wrangler deploy` ✅（Version `eb870142`，cron `0 3 * * *` 仍在）。

**線上實測**：
- `GET /report/:id` → 200，HTML 含 `screenshot-toggle`/`allow-images-toggle`/`允許 AI 讀取截圖圖片`/`toggle-hint`/`toggle-token`/`/settings`/`allow_screenshot_images` 全中 ✅。
- `GET /api/reports/:id` → 回 `allowScreenshotImages:false`（欄位未建仍 graceful）✅。
- `PATCH …/settings`（空 body）→ **400**；（`{allow_screenshot_images:true}`）→ **500「column ... does not exist」**（證明端點邏輯/欄位名正確、**待 FOX 跑 ALTER**）✅。
- MCP `get_screenshots`（欄位未建）→ 防呆 fallback 正常回「此報告沒有截圖」，**未崩** ✅。

**🟡 提醒 FOX（重要）**：① **必須先在 Supabase 跑 §1 的 ALTER**（`reports.allow_screenshot_images`），否則勾選 PATCH 會 500（報告頁端僅 `console.error`、不崩；MCP 端有 fallback 不崩，但勾選不會生效）。② 完整驗收需「先用截圖標注上傳一筆**有截圖**的報告」→ 開報告頁勾選 → MCP `get_screenshots(report_id)` **不帶** include_images 應自動回圖。③ 此 PM 為 server-only，**不影響** Chrome Web Store / 綠界審核（未動擴充與付費流程）。

---

### 🟡 PM-83：popup 新增「允許 AI 看截圖」勾選（上傳時帶入）

**背景**：PM-82 在報告頁加了勾選框，但使用者要事後去報告頁才能開。更自然的操作是在 popup 就決定，跟「即時監控」「鍵盤模式」同一排。

**目標**：popup 新增一個 toggle「📸 高畫質 AI 分析（高 Token）」，放在「鍵盤模式」下方。勾選狀態存 `chrome.storage.local`，截圖上傳時自動帶入報告的 `allow_screenshot_images`。同時把報告頁（PM-82）的勾選文字也同步改成一致的「📸 高畫質 AI 分析（高 Token）」。

⚠ **注意**：此 PM 改動擴充功能（popup.ts），Chrome Web Store 目前審核中。**先改、先 build，但不重新提交**。等當前版本過審後，跟域名遷移一起打包新版上架。

---

#### §1 popup.html 新增 toggle

在「鍵盤模式」那行下方加一行，樣式一致：

```html
<div class="mode-toggle">
  <span>📸 高畫質 AI 分析（高 Token）</span>
  <input type="checkbox" id="allow-screenshots-toggle" />
</div>
```

跟即時監控、鍵盤模式的 toggle 排版完全一致（icon + 文字 + 右邊 checkbox）。

---

#### §2 popup.ts 邏輯

```ts
// ── 讀取 ──
const allowScreenshotsToggle = $<HTMLInputElement>('#allow-screenshots-toggle');
const ALLOW_SS_KEY = 'bugezy:allow-screenshot-images';

// 初始化：讀 storage
const ssStore = await chrome.storage.local.get(ALLOW_SS_KEY);
allowScreenshotsToggle.checked = ssStore[ALLOW_SS_KEY] ?? false;

// ── 切換 ──
allowScreenshotsToggle.addEventListener('change', async () => {
  await chrome.storage.local.set({ [ALLOW_SS_KEY]: allowScreenshotsToggle.checked });
});
```

---

#### §3 截圖上傳時帶入設定

找到截圖上傳的地方（annotate.ts 或 background.ts 裡上傳報告的 API call），在 POST body 加：

```ts
// 讀 storage
const ssStore = await chrome.storage.local.get('bugezy:allow-screenshot-images');
const allowScreenshotImages = ssStore['bugezy:allow-screenshot-images'] ?? false;

// POST /api/reports body 加
body: JSON.stringify({
  ...payload,
  allow_screenshot_images: allowScreenshotImages,
})
```

---

#### §4 Server 端接收

在 `createReport`（server/src/index.ts）處理 POST body 時，讀 `allow_screenshot_images` 並存入 Supabase：

```ts
const allowScreenshotImages = body.allow_screenshot_images ?? false;

// insert 時加入
.insert({
  ...reportData,
  allow_screenshot_images: allowScreenshotImages,
})
```

---

#### §5 報告頁文字同步（server/src/index.ts）

PM-82 的報告頁勾選文字也同步改：
- 勾選框文字：`📸 允許 AI 讀取截圖圖片` → `📸 高畫質 AI 分析（高 Token）`
- 開啟提示：`已開啟 — AI 可直接看到截圖，適合視覺類 Bug（顏色、排版、CSS）` → `已開啟 — AI 可看到截圖畫面，視覺 Bug 更精準（顏色、排版、CSS）`
- 關閉提示：`未開啟 — AI 只讀文字描述，省 Token。遇到視覺 Bug 時再開啟` → `未開啟 — AI 只讀文字，省 Token。遇到視覺 Bug 再開啟`

#### §6 不改的東西

- 報告頁的勾選框：保留（PM-82，事後可改）
- MCP `get_screenshots` 邏輯：不動（PM-82 已有兩層判斷）
- server 首頁/guide/faq/privacy：不動
- CLI：不動

#### Build

```
cd extension && npm run build
```

⚠ **不重新提交 Chrome Web Store**，等當前審核通過後再一起打包。

#### 驗收

1. reload 擴充 → 打開 popup → 應看到「📸 高畫質 AI 分析（高 Token）」toggle 在鍵盤模式下方
2. 勾選 → 關閉 popup → 重新打開 → 勾選狀態應保持
3. 勾選狀態下截圖上傳 → 開報告頁 → 「允許 AI 讀取截圖圖片」應自動勾選
4. 不勾選狀態下截圖上傳 → 報告頁應未勾選

---

### 🔵 DONE-83：popup 新增「高畫質 AI 分析」勾選（上傳時帶入）✅ build + deploy（⚠ extension 未重上架，依規格）

**改動**：`extension/src/{types.ts, popup.html, popup.ts, annotate.ts}` + `server/src/index.ts`（cli 不動；MCP get_screenshots 邏輯/首頁/guide/faq/privacy 不動，符合 §6）。

**§1 popup.html**：「🔇 鍵盤模式」下方加第三個 `.toggle-row`：`📸 高畫質 AI 分析（高 Token）` + `#allowScreenshots` checkbox（與監控/鍵盤同排版）。

**§2 popup.ts**：`types.ts` 加 `ALLOW_SCREENSHOT_KEY = 'bugezy:allow-screenshot-images'`；popup 讀 storage 初始化勾選狀態 + `change` 寫回（與 monitorMode/keyboardMode 同模式，沿用 `$()` 不帶 `#` 慣例）。

**§3 annotate.ts**（截圖獨立上傳處，PM-18）：上傳前讀 `ALLOW_SCREENSHOT_KEY`，POST `/api/reports` payload 加 `allow_screenshot_images`。

**§4 server createReport**：讀 `payload.allow_screenshot_images`（預設 false）→ insert 帶入。⚠ **非破壞性**（PM-61 同款）：欄位若尚未建（PM-82 ALTER 未跑）會讓 insert 失敗 → **偵測 `allow_screenshot_images` 錯誤後退回不含該欄位重試**，確保截圖/報告上傳**永不因此中斷**。

**§5 報告頁文字同步**（PM-82 `REPORT_PAGE_HTML`，含初始 render + change handler 兩處）：checkbox 標籤 → `📸 高畫質 AI 分析（高 Token）`；開啟提示 → `✅ 已開啟 — AI 可看到截圖畫面，視覺 Bug 更精準（…）`；關閉提示 → `🔒 未開啟 — AI 只讀文字，省 Token。遇到視覺 Bug 再開啟`。

**Build / Deploy**：extension `npm run build` ✅ + `tsc` ✅；server `tsc` ✅ + `wrangler deploy` ✅（Version `d44ff466`，cron `0 3 * * *` 仍在）。**依規格 extension 只 build、未重上架 Web Store**。

**線上驗證**：
- `dist/popup.html` 含 `allowScreenshots` + `📸 高畫質 AI 分析（高 Token）` ✅。
- 報告頁 3 處新文字全中、checkbox 標籤已換 ✅（殘留「允許 AI 讀取截圖圖片」僅 2 處程式註解 + MCP 訊息，非使用者可見的勾選文字）。
- **createReport 非破壞性**：POST `/api/reports` 帶 `allow_screenshot_images:true`（欄位未建）→ **200 + report_id**（退回重試成功，上傳未中斷）✅。

**🟡 提醒 FOX**：① extension 已改+build，但**未重新提交 Chrome Web Store**（依規格，等當前審核過 + 域名遷移一起打包上架）。② 仍需先跑 **PM-82 的 `reports.allow_screenshot_images` ALTER**——未跑前：勾選上傳的報告其 flag 不會真的存進去（createReport 會退回不含該欄位、報告照常建立但 flag 失效），跑 ALTER 後才生效。③ 完整驗收需瀏覽器實機（popup 勾選持久化 → 勾選下截圖上傳 → 報告頁自動勾選）。④ MCP `get_screenshots` 的提示訊息仍寫舊標籤「允許 AI 讀取截圖圖片」（§6 規定不動 get_screenshots，故保留；如要一致可另開小 PM）。⑤ 驗證時建立了一筆測試報告 `864a863d…`（無截圖），可忽略。

---

### 🟡 PM-84：MCP get_screenshots 提示文字同步「高畫質 AI 分析」

**背景**：PM-83 已把 popup + 報告頁統一改成「📸 高畫質 AI 分析（高 Token）」，但 MCP `get_screenshots` tool 的 description 和 metadata 回傳訊息還是舊文字。

**目標**：同步所有面向使用者/AI 的文字，保持一致。

---

#### 改動範圍（只改 server/src/index.ts 裡 get_screenshots 相關字串）

1. **tool description**（`server.tool()` 第二個參數）：
   - 舊：`取得報告的截圖圖片（視覺 Bug 用）。⚠ 圖片會消耗較多 token...`
   - 新：`高畫質 AI 分析：取得報告截圖圖片（視覺 Bug 用）。⚠ 圖片消耗較高 Token（每張 ~3,000-8,000），建議只在需要看畫面時使用。`

2. **include_images 參數 describe**：
   - 舊：`是否回傳圖片內容（預設 false，只回 metadata 省 token）`
   - 新：`開啟高畫質 AI 分析（預設 false，只回 metadata 省 Token）`

3. **metadata 模式回傳訊息**（`include_images` 未開時）：
   - 舊：`此報告有 N 張截圖。如需 AI 分析視覺問題，請加 include_images: true...`
   - 新：`此報告有 N 張截圖。如需高畫質 AI 分析（視覺 Bug），請加 include_images: true（每張約 3,000-8,000 Token）。`

4. **搜尋 `允許 AI 讀取截圖圖片`**：如有殘留，改成 `高畫質 AI 分析（高 Token）`。

#### 不改的東西
- extension / cli：不動
- 其他 MCP tools：不動
- get_screenshots 的邏輯/兩層判斷：不動，只改字串

#### Deploy
```
cd server && npx wrangler deploy
```

#### 驗收
MCP 呼叫 `get_screenshots(report_id)` 不帶 include_images → 回傳訊息應包含「高畫質 AI 分析」字樣，不含「允許 AI 讀取截圖圖片」。

---

### 🟡 PM-85：Server 端 Groq Whisper 語音轉文字端點（麥克風架構升級 1/3）

**背景**：目前語音用 Web Speech API 在 inject.ts MAIN world 執行，每個網站都要授權麥克風，UX 差。改為 offscreen document 錄原始音訊 + server 端 Groq Whisper 轉文字，一次授權永久通用。本卡只做 server 端，extension 在 PM-86/87。

**目標**：新增 `POST /api/transcribe` 端點，接收音訊檔 → 呼叫 Groq Whisper API → 回傳中文逐字稿。

---

#### §1 wrangler.toml 加 GROQ_API_KEY binding

```toml
# wrangler.toml 不需要加明文 — 已用 wrangler secret put 存好
# 只需確認 server/src/index.ts 的 Env 型別宣告有 GROQ_API_KEY
```

在 Env interface 加：
```ts
GROQ_API_KEY: string;
```

---

#### §2 新增 POST /api/transcribe

```ts
// 路由
if (method === 'POST' && path === '/api/transcribe') {
  return handleTranscribe(request, env);
}
```

---

#### §3 handleTranscribe 實作

```ts
async function handleTranscribe(request: Request, env: Env): Promise<Response> {
  // 1. 讀取音訊（multipart form-data 或 raw binary）
  const contentType = request.headers.get('content-type') || '';
  let audioBlob: Blob;

  if (contentType.includes('multipart/form-data')) {
    const formData = await request.formData();
    const file = formData.get('audio');
    if (!file || !(file instanceof File)) {
      return json({ error: '缺少 audio 欄位' }, 400);
    }
    audioBlob = file;
  } else {
    // raw binary（extension 直接送 blob）
    audioBlob = await request.blob();
  }

  // 2. 檢查大小（上限 25MB，Groq 免費版限制）
  if (audioBlob.size > 25 * 1024 * 1024) {
    return json({ error: '音訊超過 25MB 上限' }, 400);
  }
  if (audioBlob.size < 100) {
    return json({ error: '音訊太短' }, 400);
  }

  // 3. 呼叫 Groq Whisper API
  const groqForm = new FormData();
  groqForm.append('file', audioBlob, 'audio.webm');
  groqForm.append('model', 'whisper-large-v3-turbo');
  groqForm.append('language', 'zh');          // 預設中文
  groqForm.append('response_format', 'verbose_json');

  try {
    const groqRes = await fetch('https://api.groq.com/openai/v1/audio/transcriptions', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${env.GROQ_API_KEY}`,
      },
      body: groqForm,
    });

    if (!groqRes.ok) {
      const errText = await groqRes.text();
      console.error('Groq API error:', groqRes.status, errText);
      return json({ error: 'Groq 轉錄失敗', detail: errText }, 502);
    }

    const result = await groqRes.json() as {
      text: string;
      segments?: Array<{ start: number; end: number; text: string }>;
      language?: string;
      duration?: number;
    };

    return json({
      ok: true,
      text: result.text,
      segments: result.segments || [],
      language: result.language || 'zh',
      duration: result.duration || 0,
    });

  } catch (err) {
    console.error('Groq fetch error:', err);
    return json({ error: '語音轉錄服務暫時不可用' }, 503);
  }
}
```

---

#### §4 CORS

`/api/transcribe` 要允許擴充功能跨域呼叫。確認現有 CORS 設定已覆蓋 `/api/*`（應該已有）。

---

#### §5 不改的東西

- extension：不動（PM-86 才改）
- MCP tools：不動
- 其他 API 端點：不動
- 首頁/guide/faq/privacy：不動

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

用 curl 直接測（把 YOUR_AUDIO.webm 換成實際音訊檔）：

```bash
curl -X POST https://bugezy.dev/api/transcribe \
  -F "audio=@YOUR_AUDIO.webm" \
  -H "Content-Type: multipart/form-data"
```

應回傳：
```json
{
  "ok": true,
  "text": "這個按鈕按下去沒反應",
  "segments": [...],
  "language": "zh",
  "duration": 5.2
}
```

如果沒有音訊檔可以測，也可以用 PowerShell 測 error handling：
```powershell
Invoke-RestMethod -Uri "https://bugezy.dev/api/transcribe" -Method POST -Body "too-short" -ContentType "application/octet-stream"
```
應回傳 `{ "error": "音訊太短" }`。

---

### 🔵 DONE-85：Server 端 Groq Whisper 語音轉文字端點 ✅ 已部署 + 線上驗證

**改動**：只改 `server/src/index.ts`（extension/cli/MCP/其他端點/首頁等不動，符合 §5）。`wrangler.toml` 不需動（`GROQ_API_KEY` 走 `wrangler secret`，非明文）。

**§1 Env**：加 `GROQ_API_KEY: string`（用 `wrangler secret put` 設定）。

**§2 路由**：try 區塊內 `POST /api/transcribe` → `handleTranscribe`（掛在 `/api/correct` 之後）。

**§3 handleTranscribe**：
- 讀音訊：`multipart/form-data` 取 `audio` 欄位（無 → 400「缺少 audio 欄位」），否則 `request.blob()` 收 raw binary。
- 大小檢查：>25MB → 400「音訊超過 25MB 上限」；<100B → 400「音訊太短」。
- 呼叫 **Groq Whisper**（`api.groq.com/openai/v1/audio/transcriptions`，model `whisper-large-v3-turbo`、`language=zh`、`response_format=verbose_json`、`Authorization: Bearer GROQ_API_KEY`）。
- 回 `{ ok, text, segments, language, duration }`；Groq 非 200 → 502「Groq 轉錄失敗」+ detail；fetch throw → 503。

**§4 CORS**：既有 `CORS`（`Access-Control-Allow-Methods: GET, POST, OPTIONS`、`Allow-Headers: Content-Type`、`Origin: *`）+ 頂層 OPTIONS handler 已覆蓋 `/api/*`，擴充可跨域呼叫，無需改。

**tsc / deploy**：`tsc --noEmit` ✅ + `wrangler deploy` ✅（Version `ec1da982`，cron `0 3 * * *` 仍在）。

**線上實測（urllib + 瀏覽器 UA）**：
- raw binary `<100B` → **400「音訊太短」** ✅。
- multipart 無 `audio` 欄位 → **400「缺少 audio 欄位」** ✅。
- raw / multipart 帶 500B 假音訊 → **502「Groq 轉錄失敗」** + Groq detail「could not process file - is it a valid media file?」 ✅——**證明路由抵達 Groq、且 `GROQ_API_KEY` 已設定有效**（若 key 缺失會是 401 認證錯，而非媒體驗證錯）；真實音訊將回 200 + 逐字稿。
> 註：部署後極短時間內單次測到 404（edge 傳播延遲），稍候重測即穩定。

**🟡 提醒 FOX**：① `GROQ_API_KEY` 確認**已設定且有效**（Groq 已通過認證進到媒體驗證）。② 這是麥克風架構升級 **1/3**（純 server）；extension 端（offscreen 錄音 + 改呼叫 `/api/transcribe`）在 **PM-86/87**。③ 規格驗收用 `https://bugezy.dev/api/transcribe`——`bugezy.dev` 綁同 Worker，路由相同可通；本次以 `…workers.dev` 實測。④ 真實驗收建議用一段中文 `.webm` 音訊跑完整 `{ok:true,text:"…"}`。

---

### 🔵 DONE-84：MCP get_screenshots 提示文字同步「高畫質 AI 分析」✅ 已部署 + 線上驗證

**改動**：只改 `server/src/index.ts` 的 `get_screenshots` 相關**字串**（邏輯/兩層判斷不動；extension/cli/其他 tool 不動，符合 §不改）。

1. **tool description** → `高畫質 AI 分析：取得報告截圖圖片（視覺 Bug 用）。⚠ 圖片消耗較高 Token（每張 ~3,000-8,000），建議只在需要看畫面時使用。Report screenshots.`
2. **`include_images` describe** → `開啟高畫質 AI 分析（預設 false，只回 metadata 省 Token）`
3. **metadata 模式訊息** → `此報告有 N 張截圖。如需高畫質 AI 分析（視覺 Bug），請加 include_images: true（每張約 3,000-8,000 Token）。`（移除原本「或由使用者在報告頁勾選『允許 AI 讀取截圖圖片』」尾句）
4. **§4 殘留清理**：剩餘 2 處 `允許 AI 讀取截圖圖片` 僅為程式註解（CSS/JS comment），順手改為 `高畫質 AI 分析（高 Token）`——已無任何使用者/AI 可見的舊字串。

**tsc / deploy**：`tsc --noEmit` ✅ + `wrangler deploy` ✅（Version `86c22eee`，cron `0 3 * * *` 仍在）。

**線上實測（MCP）**：
- `tools/list` → `get_screenshots.description` = 「高畫質 AI 分析：…」、`include_images.describe` = 「開啟高畫質 AI 分析（…省 Token）」 ✅。
- 建立一筆含截圖測試報告 → `get_screenshots(report_id)` **不帶** include_images → 訊息「此報告有 1 張截圖。如需高畫質 AI 分析（視覺 Bug），請加 include_images: true（每張約 3,000-8,000 Token）。」**含「高畫質 AI 分析」=true、含舊「允許 AI 讀取截圖圖片」=false** ✅。

**🟡 提醒 FOX**：① 至此 PM-82/83/84 的「高畫質 AI 分析（高 Token）」文案在 popup / 報告頁 / MCP 三處全一致。② 驗證時建了一筆含 1×1 截圖的測試報告 `4c8ecdc5…`，可忽略。

---

**遵守鐵律**：全程**只搜/只讀**，**未改任何 `.ts/.tsx/.js/.json/.html`、未 build、未 deploy**。唯一產出 `docs/domain-migration-checklist.md`（新 .md 檔）。

**調查方法**：`Grep` 全 codebase 搜 `bugezy-api.bugezy-api.workers.dev`、`workers.dev`、`API_BASE/API_URL/BASE_URL/BUGEZY_API_URL`、`chrome.identity/redirect_uri`、`ReturnURL/OrderResultURL/PeriodReturnURL/url.origin`，並讀 `manifest.json` oauth2。

**核心結論**（詳見清單 §1~§7）：
- ✅ **Worker 後端與域名無關**：`server/src/index.ts` 全程用 `url.origin` 組 share_url / ECPay 回調，不寫死自家域名 → 後端**邏輯零改動**，`bugezy.dev` route 綁同 Worker 即雙域名並行。
- 🎯 **真正要改的「程式碼」只有 1 處**：`extension/src/types.ts:121` 的 `API_BASE` 常數（全擴充 API/結帳/分享連結基準的唯一源頭）→ 改它即全擴充跟著走新域名（但**要重 build + 重打包 zip + 重上 Web Store**）。
- ✅ **OAuth 不用改**：`chrome.identity.getAuthToken`（Chrome 內建擴充 OAuth，綁擴充 ID + client_id，**無 web redirect URI**）。
- ✅ **ECPay 回調不用改 code**：`ReturnURL/OrderResultURL/PeriodReturnURL` 皆由 `${url.origin}` 動態產生，**隨 `API_BASE` 改動自動連動**。
- 📄 其餘為文件/顯示字串：guide/faq 的 MCP 網址（server HTML 673/680/686/770）、`docs/chrome-web-store.md` 上架文案、`cli/src/index.ts` 預設、`web/vite.config.ts` dev proxy。

**清單含 6 張表 + 遷移順序**：①擴充程式碼 ②OAuth ③API Base 彙整 ④MCP 端點 ⑤ECPay 回調 ⑥文件/首頁引用 ⑦遷移順序（含風險）。

**🟡 提醒 FOX**：① **現在別動**——Chrome Web Store + 綠界雙審核中，擴充改 `API_BASE` 必須重上架（會再審核），等結果出來再依 §7 順序執行。② 最省力關鍵：後端 `url.origin` 設計讓 ECPay 回調 / 分享連結零改動自動跟隨域名；改 `API_BASE` 一處即連動。③ 舊 `workers.dev` route 建議**長期保留**（別過早移除），避免已設定 MCP 的老用戶斷線。

---
