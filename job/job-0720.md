# job-0720.md — BugEzy Day 34

> 接續 job-0719.md（PM-255）

---

### 🟡 PM-256：建立 /blog 架構（路由 + 列表頁 + 文章模板 + SEO meta）

**背景**：bugezy.dev 目前沒有 blog 功能，需要新增 /blog 路由來放 SEO 文章，提升 Google 搜尋能見度和 AI 訓練素材。

**需求**：

1. **`/blog` 列表頁**：
   - 顯示所有文章的標題、摘要、發佈日期
   - 每篇文章連結到 `/blog/{slug}`
   - 頁面風格與現有 bugezy.dev 一致（深色系、BugEzy 品牌色）
   - 頂部有返回首頁連結

2. **`/blog/{slug}` 單篇文章頁**：
   - 標題、發佈日期、正文（支援段落、小標題、粗體）
   - SEO meta tags：`<title>`、`<meta name="description">`、`<meta property="og:title">`、`<meta property="og:description">`、`<meta property="og:url">`
   - JSON-LD Article schema（讓 Google 知道這是一篇文章）
   - 文章底部有 CTA：「BugEzy — 免費開始 · bugezy.dev」
   - 上一篇 / 下一篇導航
   - 返回 /blog 列表連結

3. **文章資料結構**：
   - 在 server/src/index.ts 裡用陣列或物件存文章資料（標題、slug、date、description、content）
   - 方便之後新增文章（加一筆資料就好）

4. **先建空架構**，文章內容由下一張卡（PM-257）提供

5. **放入 5 篇文章的 slug 預留位**：
   - `vibe-coding-debug-trap`
   - `ai-coding-debug-cost`
   - `stop-screenshot-debugging`
   - `beginner-debug-no-console`
   - `why-ai-needs-more-info`

6. **sitemap.xml 更新**：把 /blog 和五篇文章 URL 加入 sitemap

7. **`wrangler deploy`**

**改動檔案**：`server/src/index.ts`

**驗收條件**：
1. `/blog` 顯示文章列表（5 篇標題 + 摘要佔位）
2. `/blog/{slug}` 顯示單篇文章頁面（內容可以是佔位文字）
3. SEO meta tags + JSON-LD 正確
4. sitemap.xml 包含新 URL
5. 頁面風格與現有 bugezy.dev 一致
6. deploy 成功
7. `wrangler deploy` 無錯誤

完成後在下方寫 `🔵 DONE-256`。

---

### 🔵 DONE-256：/blog 架構（列表 + 文章模板 + SEO）已完成

**改動檔案**：只改 `server/src/index.ts`（已 `tsc --noEmit` + `wrangler deploy`，hash `34d014c9`）。

**實作**：
1. **文章資料結構**：`interface BlogPost { slug; date; title; description; blocks }`（blocks 為 `{h2}`/`{p}` 陣列，段落支援 `**粗體**`）+ `BLOG_POSTS[]` 集中管理，新增一篇只加一筆。**5 篇 slug 預留位皆已放入**，內容為 `BLOG_PLACEHOLDER` 佔位（實際內文待 PM-257）：`vibe-coding-debug-trap` / `ai-coding-debug-cost` / `stop-screenshot-debugging` / `beginner-debug-no-console` / `why-ai-needs-more-info`（各附合理的中文佔位標題 + 摘要）。
2. **`/blog` 列表頁** `blogListPage(lang)`：深色主題（共用 `BLOG_CSS`，與 /changelog 一致）、依日期新→舊排序、每篇標題連結 `/blog/{slug}` + 日期 + 摘要、返回首頁連結、中英雙語 chrome（`makeT`）。
3. **`/blog/{slug}` 單篇頁** `blogPostPage(post, lang)`：標題 / 日期 / 正文（`renderBlogPara` 段落粗體、`escHtml` 防破版）、完整 **SEO meta**（`<title>`/description/canonical/`ogMeta` 給 og:title/description/url）、**JSON-LD Article schema**（headline/description/datePublished/author/publisher/mainEntityOfPage）、底部 CTA「BugEzy — 免費開始 · bugezy.dev」連首頁、**上一篇/下一篇導航**（陣列前後、首末篇自動省略）、返回 /blog 連結。
4. **路由**：`GET /blog` → 列表；`GET /blog/{slug}` → 找 `BLOG_POSTS`，找到渲染單篇、找不到回 404。皆 `no-store`（依語言變動）。
5. **sitemap.xml**：新增 `/blog`（weekly 0.6）+ 5 篇文章 URL（`...BLOG_POSTS.map`，monthly 0.5）。

**驗證**（curl 實測，皆通過；`/blog/{slug}` 首發有 edge 傳播延遲，retry 後穩定）：
- `/blog` → `<html lang="zh-Hant">` + 5 篇文章連結；EN footer「Back to home」。
- `/blog/stop-screenshot-debugging` → 正確 title/canonical/description/og:title+url+description/**JSON-LD `"@type":"Article"`**/CTA「免費開始 · bugezy.dev」/返回部落格。
- prev/next：中間篇兩者皆有、首篇無 prev 有 next（邊界正確）。
- `/blog/does-not-exist` → 404。
- `/sitemap.xml` 含 `/blog` + 5 篇文章 URL。

**驗收**：
1. ✅ `/blog` 顯示 5 篇文章列表（標題 + 摘要佔位）
2. ✅ `/blog/{slug}` 顯示單篇（佔位內文）
3. ✅ SEO meta（title/desc/og）+ JSON-LD Article 正確
4. ✅ sitemap.xml 含新 URL（/blog + 5 篇）
5. ✅ 頁面風格與現有 bugezy.dev 一致（深色 + 品牌色）
6. ✅ deploy 成功（`34d014c9`）
7. ✅ `wrangler deploy` 無錯誤

**FOX / 下一張卡**：PM-257 提供 5 篇實際內文——只需把各 post 的 `blocks` 由 `BLOG_PLACEHOLDER` 換成實際 `{h2}/{p}` 段落即可（結構已備妥）。

---

### 🟡 PM-257：五篇 SEO 部落格文章內容填入 + wrangler deploy

**背景**：PM-256 已建好 /blog 架構，5 篇文章目前是佔位文字。本卡填入實際內容。

**改動**：把 `BLOG_POSTS` 陣列中 5 篇文章的 `blocks` 從 `BLOG_PLACEHOLDER` 替換為以下實際內容，同時更新 title、description、date。

**改動檔案**：`server/src/index.ts`（只改 BLOG_POSTS 的內容）

---

#### 文章 1：`vibe-coding-debug-trap`

- **title**：Vibe Coding 的隱藏成本：為什麼你的 Bug 越修越多？
- **description**：用 AI 寫程式很快，但 Bug 出現時截圖貼了好幾次，AI 越修越錯？問題不在你，在於你給 AI 的資訊不夠完整。
- **date**：2026-07-20

**blocks**：

h2: 用 AI 寫程式，30 分鐘搞定一個網站

p: 你用 ChatGPT 或 Cursor 描述需求，AI 幫你生出整套程式碼。購物車、後台管理、會員系統——以前要花幾週的東西，現在一個下午就有雛形。這就是 Vibe Coding 的魅力，讓不會寫程式的人也能打造自己的產品。

h2: 然後 Bug 出現了

p: 畫面跑版、按鈕按了沒反應、資料存不進去。你看著螢幕上的紅字，完全不知道它在說什麼。沒關係，你把畫面截圖，貼給 AI，跟它說「這裡壞了，幫我修」。

p: AI 回你一段新的程式碼，你貼上去，重新整理頁面——另一個地方又壞了。

h2: 截圖 Debug 的惡性循環

p: 你再截一張圖，再解釋一次，AI 再給你一段 code。但這次它修的方向跟上次矛盾，你的程式開始出現連鎖錯誤。一個 Bug 變三個，三個變十個，整個專案越修越歪。

p: 這不是你的錯。**問題在於截圖只拍到表面**。AI 看到的是一張「畫面壞了」的圖片，但它真正需要的是——Console 裡的錯誤訊息、Network 請求的狀態碼、DOM 的實際結構。這些你在截圖裡都沒給它。

h2: AI 不是不會修，是你給的線索不夠

p: 想像你去看醫生，只說「我不舒服」，但不讓醫生量體溫、聽心跳、驗血。醫生只能猜，猜錯了開錯藥，你的狀況就越來越糟。Debug 也是一樣的道理。

p: 當你把**完整的錯誤資訊**（Console log、Network 失敗的 API、報錯的程式碼行數）一次丟給 AI，它的修復準確率會大幅提升。不是一張截圖，而是一份完整的診斷報告。

h2: 用對工具，一次給齊

p: BugEzy 做的就是這件事：你按一下錄製，用嘴巴講「這個按鈕按了沒反應」，它會自動在背景蒐集 Console 錯誤、Network 請求、DOM 變化，打包成一份完整的 Bug 報告。你不需要看懂那些技術細節，AI 看得懂就夠了。

p: 下次 Bug 出現時，不要再截圖貼來貼去了。一次給齊，讓 AI 一次修對。

---

#### 文章 2：`ai-coding-debug-cost`

- **title**：AI 寫程式很快，Debug 卻花了 3 倍時間？問題出在這裡
- **description**：非工程背景用 AI 開發網站和應用，寫完很快但 Debug 花了大把時間和 AI 額度。這篇告訴你問題出在哪，怎麼解決。
- **date**：2026-07-20

**blocks**：

h2: 寫 Code 一小時，Debug 三小時

p: 你用 AI 助手建了一個網路商店，前端畫面、商品列表、結帳流程——AI 幫你產生的程式碼又快又整齊。但上線測試的時候，結帳按鈕沒反應、商品圖片顯示不出來、手機版整個跑版。

p: 你開始 Debug。截圖、打字說明、貼給 AI、等回覆、複製新 code、貼上、測試、又壞了。重複這個循環十幾次，三小時過去了，問題還在。

h2: 你的 AI 額度就是這樣燒掉的

p: 每次你貼截圖加一大段說明文字給 AI，就是在消耗對話額度。免費版的 ChatGPT、Claude、Gemini 都有使用上限，**截圖張數越多、說明文字越長，額度燒得越快**。

p: 有些人一個下午就把額度用完了，然後要等好幾個小時才能回來繼續。等的時候你什麼都做不了，Bug 還在那邊等你。這個等待的時間成本，Vibe Coder 很少算進去。

h2: 為什麼截圖 Debug 效率這麼低？

p: 因為截圖只拍到「症狀」，沒有拍到「病因」。你貼了一張「按鈕按了沒反應」的截圖，但 AI 看不到 Console 裡噴了什麼錯誤、Network 是不是 API 回傳了 500、JavaScript 是不是根本沒載入成功。

p: 所以 AI 只能用猜的。它猜的修法如果剛好對了，你運氣好。猜錯了，就是越修越遠，額度也越燒越多。

h2: 一次給齊，省時間也省額度

p: 如果你能**把 Console 錯誤、Network 狀態、你的操作步驟一次打包丟給 AI**，它就不用猜了。一次看完所有線索，一次修對的機率大幅提升。

p: BugEzy 就是設計來做這件事的。你按錄製，用嘴巴描述問題，它在背景自動蒐集所有技術資訊。一份報告取代十張截圖，AI 一看就懂，修一次就對。額度只花一輪，不用來回十幾次。

p: 把省下來的額度拿去開發新功能，不是更好嗎？

---

#### 文章 3：`stop-screenshot-debugging`

- **title**：別再用截圖 Debug 了——你正在浪費你的 AI 額度
- **description**：每次截圖加說明就燒一輪 AI 對話額度，免費版幾次就沒了。用對工具一次給齊，省額度省時間。
- **date**：2026-07-20

**blocks**：

h2: 截圖 Debug 的真實成本

p: 你大概算過蓋一個網站要花多少時間，但你有算過 Debug 燒掉多少 AI 額度嗎？

p: 假設一個 Bug 你平均要截 3 張圖、打 200 字說明、來回對話 5 次才修好。每次對話消耗的 token 大約是純文字的 3-5 倍（因為圖片很吃額度）。**一個 Bug 就可能吃掉你每日免費額度的三分之一。**

p: 一天遇到三個 Bug，額度就燒完了。然後你只能等，等好幾個小時額度恢復，再回來繼續修。

h2: 不是 AI 不夠聰明，是你餵的資料不夠

p: 你有沒有這種經驗——貼了截圖，AI 回你「請問可以提供 Console 的錯誤訊息嗎？」或是「你可以開 DevTools 的 Network 面板看一下嗎？」

p: AI 在跟你要的，就是那些截圖裡看不到的東西。**Console 錯誤、Network 請求的 response、你操作的步驟順序**——這些才是 Debug 的關鍵線索。

h2: 自動蒐集 vs 手動截圖

p: 手動的做法是：打開 DevTools → 找到 Console 面板 → 截圖 → 切到 Network 面板 → 截圖 → 把所有圖片加上文字說明 → 貼給 AI。光這個過程就要花 5-10 分鐘，而且你可能漏掉關鍵資訊。

p: 自動的做法是：按一個按鈕，所有資訊在背景自動蒐集好。你只需要用嘴巴講「這個按鈕按了沒反應」，其他的交給工具。

h2: 把額度花在刀口上

p: Debug 不應該是你花最多時間和額度的地方。用對工具，把完整資訊一次給 AI，讓它一次修對。省下來的額度和時間，拿去做更有價值的事。

p: BugEzy 自動蒐集 Console、Network、DOM 狀態，打包成一份報告。一次對話就能解決的事，不需要來回十幾次。免費開始，到 bugezy.dev 試試看。

---

#### 文章 4：`beginner-debug-no-console`

- **title**：程式新手除錯第一步：你不需要看懂 Console 錯誤
- **description**：看不懂 Console 的紅字沒關係，AI 看得懂。關鍵是怎麼把完整的錯誤資訊餵給 AI，讓它幫你修。
- **date**：2026-07-20

**blocks**：

h2: Console 那堆紅字到底是什麼？

p: 你打開瀏覽器的開發者工具，看到 Console 面板一片紅——TypeError、ReferenceError、404、CORS⋯⋯這些字看起來像外星文，而且每一行後面都跟著一堆你完全看不懂的檔案路徑和行號。

p: 如果你是非工程背景的 Vibe Coder，這個畫面大概會讓你想直接關掉。但先別走，因為你其實**不需要看懂這些東西**。

h2: 你不需要當翻譯，AI 會讀

p: 那些紅字是寫給機器和工程師看的，但現在 AI 也看得懂。ChatGPT、Claude、Gemini 都能解讀 Console 錯誤訊息，告訴你「這行 code 有問題、應該改成這樣」。

p: 問題是：**你要怎麼把這些紅字完整地丟給 AI？** 截圖只能拍到畫面上看得見的部分。如果錯誤訊息很長、需要往下滾動，一張截圖根本截不完。而且圖片裡的文字 AI 不一定能精確讀取，尤其是程式碼和堆疊追蹤。

h2: 複製貼上？你可能漏掉關鍵資訊

p: 有人會手動複製 Console 的文字貼給 AI，這比截圖好一些。但問題是，Console 只是線索的一部分。很多 Bug 的根源在 Network（API 回傳錯誤）、DOM（元素沒有被正確渲染）、或是操作順序（你按了 A 才壞，按 B 就不會）。

p: 如果你只給 Console 錯誤，AI 可能修得了表面症狀，但根本原因還在。過幾分鐘，另一個 Bug 就冒出來了。

h2: 讓工具幫你蒐集，你只要描述問題

p: 最理想的 Debug 流程是這樣的：你描述問題（「結帳按鈕按了沒反應」），工具在背景自動蒐集 Console 錯誤、Network 請求、DOM 狀態、你的操作步驟，然後打包成一份 AI 看得懂的完整報告。

p: 你不需要看懂任何一行紅字。你只需要知道問題在哪裡，然後把報告丟給 AI。它會告訴你怎麼修。

p: BugEzy 就是做這件事的工具。按一下錄製，用嘴巴講就好，技術細節它會自動處理。不懂程式也能高效 Debug。到 bugezy.dev 免費試用。

---

#### 文章 5：`why-ai-needs-more-info`

- **title**：為什麼 AI 助手老是說「請提供更多資訊」？
- **description**：你貼了截圖問 AI 怎麼修 Bug，它卻一直要你提供更多資訊？因為截圖只有表面，AI 需要完整的錯誤脈絡才能幫你。
- **date**：2026-07-20

**blocks**：

h2: 你問 AI，AI 反問你

p: 你遇到 Bug 了，趕快截圖貼給 ChatGPT：「這個頁面壞了，怎麼修？」AI 的回覆卻是：「請問你可以提供 Console 的錯誤訊息嗎？」「Network 面板有沒有失敗的請求？」「你是在什麼操作步驟之後出現這個問題的？」

p: 你心想，我就是不知道這些東西才來問你啊。

h2: AI 不是在刁難你

p: 其實 AI 跟醫生一樣，需要足夠的診斷資訊才能下判斷。你貼一張「畫面壞了」的截圖，就像走進診間說「我不舒服」——醫生看得出你不舒服，但不知道是頭痛、肚子痛、還是骨折。

p: **一張截圖能提供的資訊量大概只有完整 Debug 報告的 10%。** 另外 90% 藏在你看不到的地方：Console 的錯誤堆疊、Network 面板裡失敗的 API 請求、DOM 裡沒有被正確渲染的元素。

h2: 為什麼完整資訊這麼重要？

p: 舉個真實例子。你的購物車「加入」按鈕按了沒反應。截圖看起來就是一個按鈕在那邊，沒有任何異狀。但如果你打開 Console，會看到一行紅字：「TypeError: Cannot read property 'push' of undefined」。打開 Network，會看到一個 POST 請求回傳了 401 Unauthorized。

p: 有了這兩條線索，AI 馬上知道：第一，購物車陣列沒有被正確初始化；第二，使用者的登入 token 過期了。**兩分鐘就能修好的問題，沒有這些線索的話 AI 可能要猜三四次才猜到。**

h2: 不想手動蒐集？讓工具代勞

p: 你不需要學會操作 DevTools，也不需要知道 Console 和 Network 在哪裡。有工具可以在你回報 Bug 的同時，自動蒐集這些 AI 需要的資訊。

p: BugEzy 就是這樣的工具。你按錄製，用嘴巴講「加入購物車按鈕沒反應」，它會自動抓取 Console 錯誤、Network 失敗請求、DOM 狀態、你的操作軌跡，打包成一份完整報告。你只要把報告丟給 AI，它一看就懂，不會再問你「請提供更多資訊」了。

p: 省掉來回問答的時間和額度，直接解決問題。到 bugezy.dev 免費開始。

---

**部署後驗證每篇**：
1. 正文內容正確、段落小標題正確
2. SEO title + description 正確
3. JSON-LD Article schema 的 headline + description 正確
4. 文章底部 CTA 正常顯示

完成後在下方寫 `🔵 DONE-257`。

---

### 🔵 DONE-257：五篇 SEO 部落格文章內容填入 已完成

**改動檔案**：只改 `server/src/index.ts` 的 `BLOG_POSTS`（已 `tsc --noEmit` + `wrangler deploy`，hash `56c614d4`）。

**實作**：
- 5 篇文章的 `title` / `description` / `date`（皆 2026-07-20）/ `blocks` 全部由 `BLOG_PLACEHOLDER` 換成 PM-257 規格的實際內容（各 4 個 h2 小標題 + 完整段落，段落 `**粗體**` 由 `renderBlogPara` 渲染）。
- 移除已無用的 `BLOG_PLACEHOLDER` 常數。
- 因 5 篇同日期，列表排序改 `b.date.localeCompare(a.date)`（同日期由 V8 stable sort 保留陣列順序 → 列表即文章 1→5 閱讀順序）。

**5 篇文章**：
1. `vibe-coding-debug-trap` — Vibe Coding 的隱藏成本：為什麼你的 Bug 越修越多？
2. `ai-coding-debug-cost` — AI 寫程式很快，Debug 卻花了 3 倍時間？問題出在這裡
3. `stop-screenshot-debugging` — 別再用截圖 Debug 了——你正在浪費你的 AI 額度
4. `beginner-debug-no-console` — 程式新手除錯第一步：你不需要看懂 Console 錯誤
5. `why-ai-needs-more-info` — 為什麼 AI 助手老是說「請提供更多資訊」？

**驗證**（curl 實測 5 篇，皆通過；Python urllib 被 Cloudflare 擋 UA 故改 curl）：
- 正文小標題（h2）+ 段落內容正確、`**粗體**`→`<strong>` 正常渲染。
- `<title>` = 文章標題 · BugEzy；`<meta description>` = 各篇 description。
- JSON-LD `"@type":"Article"` 的 `headline` = 文章標題、`description` 有值。
- 底部 CTA「免費開始 · bugezy.dev」正常顯示。
- 上一篇/下一篇導航連結正確（vibe → 下一篇 ai-coding-debug-cost）。
- `/blog` 列表順序為文章 1→5 閱讀順序。

**逐篇驗收**（4 項每篇皆過）：
1. ✅ 正文內容正確、段落小標題正確
2. ✅ SEO title + description 正確
3. ✅ JSON-LD Article headline + description 正確
4. ✅ 文章底部 CTA 正常顯示

**FOX**：/blog 五篇 SEO 文章已上線（bugezy.dev/blog）。

---

### 🟡 PM-258：官網加 Blog 入口 + 全站簡體字掃描改繁體 + wrangler deploy

**背景**：
1. 官網首頁和導航列沒有 /blog 的入口連結，使用者找不到部落格
2. FOX 發現官網有簡體字和繁體字混用的情況，所有中文內容應統一為繁體字

**修復內容**：

#### 修 1：加 Blog 入口

在以下位置加上 /blog 連結：
- **首頁導航列**（如果有 nav）：加「部落格」連結到 /blog
- **首頁 footer**：加「部落格」連結
- **其他頁面**（/features、/changelog 等）：如果有共用導航或 footer，一併加入

#### 修 2：全站簡體字掃描改繁體

掃描 `server/src/index.ts` 中所有中文字串（HTML 頁面內容、meta description、JSON-LD、SEO 文字等），找出簡體字並改為對應繁體字。

常見需要檢查的簡體→繁體：
- 开→開、关→關、设→設、页→頁、发→發、错→錯
- 项→項、运→運、时→時、问→問、应→應、创→創
- 视→視、选→選、进→進、连→連、产→產、备→備
- 还有其他所有出現的簡體字

**方法**：可以用正則或逐字比對找出 Big5/繁體字集以外的中文字元，或直接人工掃讀所有中文字串。

**改動檔案**：`server/src/index.ts`

**驗收條件**：
1. 首頁有「部落格」或「Blog」連結，點擊到 /blog
2. 其他頁面（features、changelog）也有連結
3. 全站中文內容統一繁體，無簡體字殘留
4. `wrangler deploy` 成功

完成後在下方寫 `🔵 DONE-258`。

---

### 🔵 DONE-258：官網加 Blog 入口 + 全站簡體字掃描改繁體 已完成

**改動檔案**：只改 `server/src/index.ts`（已 `tsc --noEmit` + `wrangler deploy`，hash `b5f1d00f`）。

#### 修 1：加 Blog 入口
- **首頁 footer**（pipe 分隔連結列）：於 `更新日誌` 後加 `📝 部落格`（`t('📝 部落格', '📝 Blog')`）連 `/blog`。
- **各頁 footer**（`.links` 換行式，features/changelog/guide/faq/install/feedback/skill 等）：`replace_all` 在 6 縮排的 `更新日誌` 連結後插入 `/blog` 連結（命中 5 個）+ 1 個 4 縮排 footer（L1492）單獨補上。共 **7 個 footer** 皆有部落格入口。
- 首頁本身為 landing page 無頂部 nav bar（只有固定 lang-switch），故以 footer 連結為入口。

#### 修 2：全站簡體字掃描改繁體
- **偵測法**：用 opencc（PM-232 已用）判定「簡體專用字」= `t2s(C)===C 且 s2t(C)!==C`（本身已是簡體形、且存在對應繁體），比 T2S_CHARS 值集精準（後者含大量「簡繁共用字」如 回/注/制/面 會誤報）。掃描時**排除** T2S_CHARS / JA_MAP / KO_MAP / VI_MAP 區段（那裡非繁體 zh 內容）。
- **實際簡體殘留（4 處，皆為 PM-254 SKILL_MD 我加入的內容）已修**：
  - `简体中文（zh-CN）` → `簡體中文（zh-CN）`（支援語言列）
  - `右上「音声記録」面板` → `右上「語音記錄」面板`（誤植日文，改繁體面板名）
  - `popup 選简体中文時` → `popup 選簡體中文時`
  - `简体中文輸出簡體` → `簡體中文輸出簡體`
- **刻意保留（非簡體錯誤，經逐一判定）**：`台`（台灣/後台，標準繁體，opencc 偏好古字 臺 屬誤報）、`群`（社群）、`吃`（吃額度）；日文 prompt 字串（音声/数字/読点…）；**line 5709 的 zh-CN Whisper prompt `以下是简体中文的语音转录内容`（PM-232 刻意用簡體引導 Whisper 輸出簡體）**。
- 重掃確認：4 處已清（1587/1592/1594/1600 不再命中）；`简体` 全站僅剩 5709 的 Whisper prompt（正確）。

**驗證**（curl 實測，皆通過；首發偶有 edge 傳播延遲，retry 後穩定）：
- 首頁 / /features / /changelog footer 皆含 `<a href="/blog">📝 部落格</a>`（EN 為 `📝 Blog`）。
- /skill：`簡體中文（zh-CN）`（繁）出現、`简体中文（zh-CN）`（簡）0 筆、`語音記錄」面板` 1 筆。
- sitemap 仍含 /blog + 5 篇（回歸無損）。

**驗收**：
1. ✅ 首頁有「📝 部落格」連結，點擊到 /blog
2. ✅ features / changelog（及其餘 4 個頁面 footer）也有連結
3. ✅ 全站繁體 zh 內容無簡體殘留（4 處已修；其餘為簡繁共用字/日文/刻意簡體 Whisper prompt）
4. ✅ `wrangler deploy` 成功（`b5f1d00f`）

---

### 🟡 PM-260：隱藏聯絡我們的電話和服務時間（只留 Email）

**背景**：v1.1.4 已過審，不再需要展示電話和服務時間。只保留 Email 聯繫方式。

**修法**：掃描 `server/src/index.ts` 全站所有頁面的「聯絡我們」區塊，移除：
- 📱 電話 / Phone 那一行
- 服務時間 / Hours 那一行

只保留 📧 Email：fox100039@gmail.com

**改動檔案**：`server/src/index.ts` + `wrangler deploy`

**驗收條件**：
1. 首頁（中/英/日/韓/越）聯絡區塊只剩 Email
2. 其他有聯絡資訊的頁面也同步移除
3. `wrangler deploy` 成功

完成後在下方寫 `🔵 DONE-260`。

---

### 🔵 DONE-260：隱藏聯絡我們的電話和服務時間（只留 Email）已完成

**改動檔案**：只改 `server/src/index.ts`（已 `tsc --noEmit` + `wrangler deploy`，hash `a822c63b`）。

**修法**：
- 全站掃描「聯絡我們」聯絡資訊 —— **實際渲染電話/服務時間的只有首頁** footer 的 `.contact-info` 區塊；其餘頁面 footer 本就只顯示 Email，無需改。
- 首頁 `.contact-info`：移除 `📱 電話：0983-101-085` 與 `服務時間：週一至週五 09:00-18:00` 兩行，只保留 `📧 Email：fox100039@gmail.com`。此區塊以 `t()/makeT` 渲染，一處移除即涵蓋中/英/日/韓/越所有語言。
- **清理孤兒翻譯**：移除 JA_MAP/KO_MAP/VI_MAP 中已無人使用的 `電話` / `服務時間…` 對應 key（先 grep 確認 `t('電話')`/`t('服務時間')` 全站 0 引用才刪，避免死碼）。
- 全站掃描確認 `電話 / 服務時間 / tel:+886 / Hours: Mon / 📱` 殘留 **0 筆**。

**驗證**（curl 實測，deploy 後 edge 傳播數次 retry 後穩定）：
- 首頁五語（zh/en/ja/ko/vi）`.contact-info` 皆 `mailto:fox100039@gmail.com` 存在、`📱/電話/tel/營業時間/服務時間` 皆 0。

**驗收**：
1. ✅ 首頁（中/英/日/韓/越）聯絡區塊只剩 Email
2. ✅ 其他頁面本就只顯示 Email（無電話/服務時間可移除）
3. ✅ `wrangler deploy` 成功（`a822c63b`）

---

### 🟡 PM-261：子頁面多語化——/features、/install、/faq 加入 ja/ko/vi + wrangler deploy

**背景**：目前只有首頁有日/韓/越語版本，子頁面全是中英雙語。需要把最重要的三個轉換頁面加入 ja/ko/vi，讓國際使用者能用母語了解產品。

**優先順序**（按轉換價值排）：
1. **/features** — 產品功能頁，SEO 價值最高
2. **/install** — 安裝指南，直接帶動安裝
3. **/faq** — 常見問題，消除疑慮幫助轉換

（/guide 內容太長、/changelog 偏技術、/privacy 偏法律——這三個暫不翻）

**修法**：

跟首頁一樣的做法——在 `JA_MAP` / `KO_MAP` / `VI_MAP` 中新增這三個頁面用到的所有 `t()` 中文 key 的翻譯。頁面 render 函數已經用 `t(zhText, enText)` 或 `makeT`，只要補翻譯就會自動生效。

同時這三個頁面也要加上**語言切換列**（跟首頁一樣的 繁/简/EN/日/한/VI 切換），讓使用者可以切語言。

#### /features 頁面翻譯重點：
- 頁面標題、副標題
- 六大錄製模式的名稱和說明
- MCP 13 工具清單的說明文字
- 語音引擎區塊
- Python/Node CLI 區塊
- 價格區塊
- 所有 section header

#### /install 頁面翻譯重點：
- 安裝步驟說明
- 系統需求
- Chrome Web Store 連結區塊的文字

#### /faq 頁面翻譯重點：
- 所有問題和答案
- 分類標題

**語言切換**：三個頁面都加上右上角語言切換列（`?lang=ja/ko/vi`），跟首頁同樣的六語切換。

**sitemap.xml**：新增 `/features?lang=ja`、`/features?lang=ko`、`/features?lang=vi`（同理 install、faq）共 9 個新 URL。

**改動檔案**：`server/src/index.ts`

**驗收條件**：
1. `/features?lang=ja` 顯示日文功能頁
2. `/features?lang=ko` 顯示韓文功能頁
3. `/features?lang=vi` 顯示越南文功能頁
4. `/install?lang=ja/ko/vi` 三語正常
5. `/faq?lang=ja/ko/vi` 三語正常
6. 三頁都有語言切換列
7. sitemap 包含 9 個新 URL
8. `wrangler deploy` 成功

完成後在下方寫 `🔵 DONE-261`。

---

### 🔵 DONE-261：子頁面多語化（/features、/install、/faq 加 ja/ko/vi）已完成

**改動檔案**：只改 `server/src/index.ts`（已 `tsc --noEmit` + `wrangler deploy`，hash `9c49814b`）。

**實作**：
1. **/features 本已多語化**：PM-233~235 早已把 featuresPage 全部字串納入 JA/KO/VI_MAP，`?lang=ja/ko/vi` 原本就是日/韓/越文（本卡驗證確認）。
2. **/install + /faq 補譯**：抽出 installPage（59 條）+ faqPage（37 條）+ 共用 footer `📝 部落格` 的所有 `t()` 繁體 key，**手譯 ja/ko/vi 各 95 條**（含長段 FAQ 問答、安裝步驟、MCP 說明；保留內嵌 `<b>`/`<code>` 標籤與 URL），用產生器一次插入 `JA_MAP`/`KO_MAP`/`VI_MAP`（各 +95）。頁面用 `makeT`，補表即自動生效；faqPage 的 FAQPage JSON-LD 由可見 Q&A 生成，故 SEO 結構化資料也同步多語。
3. **六語切換列**：新增 helper `langSwitchBar(lang)`（繁/简/EN/日/한/VI，inline style 自帶當前語言高亮），取代 /features、/install、/faq 三頁原本的「中↔EN」單一切換（其餘頁面維持不變）。
4. **sitemap.xml**：新增 9 個 URL（`/features`、`/install`、`/faq` × `?lang=ja/ko/vi`）。

**驗證**（curl 實測，deploy 後 edge 傳播 retry 後穩定）：
- /features：ja/ko/vi 皆母語（`chế độ ghi hình` 等）。
- /install：ja「拡張機能をインストール」、ko「설치」、vi「Cài tiện ích」。
- /faq：ja「音声認識」、ko「음성 인식」、vi「nhận dạng giọng nói」。
- 三頁 lang-switch 皆 6 語連結，當前語言 `.on` 高亮（install?lang=ja 的「日」為 `#fff`）。
- sitemap 含 9 個新 URL（features/install/faq × ja/ko/vi）。

**驗收**：
1. ✅ `/features?lang=ja` 日文功能頁
2. ✅ `/features?lang=ko` 韓文功能頁
3. ✅ `/features?lang=vi` 越南文功能頁
4. ✅ `/install?lang=ja/ko/vi` 三語正常
5. ✅ `/faq?lang=ja/ko/vi` 三語正常
6. ✅ 三頁都有語言切換列（六語 + 當前高亮）
7. ✅ sitemap 含 9 個新 URL
8. ✅ `wrangler deploy` 成功（`9c49814b`）

---

### 🟡 PM-262：Day 34 收工（CHANGELOG + ARCHITECTURE + git push）

**步驟：**

1. **更新 CHANGELOG.md**，新增區塊（日期 2026-07-20），涵蓋今日改動：
   - PM-256：/blog 架構建立（路由 + 列表頁 + 文章模板 + SEO meta + JSON-LD）
   - PM-257：五篇 SEO 部落格文章上線（Vibe Coding / Debug 成本 / 截圖浪費 / 新手除錯 / AI 要更多資訊）
   - PM-258：官網加 Blog 入口 + 全站簡體字掃描改繁體
   - PM-259：七語標籤全亮 + 首頁六語切換列 + sitemap 加 ja/ko/vi
   - PM-260：隱藏電話和服務時間（只留 Email）
   - PM-261：/features、/install、/faq 子頁面多語化（ja/ko/vi 翻譯 + 六語切換列 + sitemap 9 URL）

2. **更新 ARCHITECTURE.md §6 開發進度表**，新增 2026-07-20 行，簡述 Day 34 內容（PM-256~262，SEO 部落格 + 官網七語全面化）

3. **git add + commit + push**：
   - commit message: `Day 34: SEO部落格5篇+官網七語全面化+子頁面多語化 (PM-256~262)`

完成後在下方寫 `🔵 DONE-262`。

---（已完成，見下方 DONE-259）：官網七語全面更新（features 語言標籤 + 多語 SEO 頁面）

**背景**：七語語音已全部完成，但官網 /features 頁面的語言標籤 JP/KR/VI 還是灰色的，且官網只有中文和英文版本。需要：
1. /features 語言標籤全部改為啟用（七語全亮）
2. 全站掃描，所有寫「支援三種語言」的地方改為「支援七種語言」
3. 新增日文、韓文、越南語的基本 SEO 著陸頁（讓這些語言的開發者搜尋時能找到 BugEzy）

**修復內容**：

#### 修 1：/features 語言標籤全亮

把 JP 日本語、KR 韓語、越南語 的標籤從灰色改為亮色（與 TW/HK/GB 相同樣式），表示已支援。

#### 修 2：全站「三種語言」→「七種語言」

掃描 server/src/index.ts，所有中英文提到「3 languages」「三種語言」「更多語言即將開放」「more coming soon」的地方，改為「7 languages」「七種語言」，移除「即將開放」的說法。

#### 修 3：新增三個語言著陸頁

新增 `?lang=ja`、`?lang=ko`、`?lang=vi` 的首頁版本，讓日/韓/越南使用者用母語搜尋時能找到 BugEzy。內容不需要翻譯整站，只需首頁的核心內容：

**日文版（?lang=ja）**：
- 標題：BugEzy — 開発者向けバグレポートツール、AIがバグを修正
- 副標題：バグをうまく説明できない？録画ボタンを押して、話すだけ。BugEzyが画面・操作・エラー情報を自動収集、AIが修正します。
- CTA：🔧 Chrome拡張機能を無料インストール
- 三步驟、為什麼選 BugEzy 等核心區塊翻成日文

**韓文版（?lang=ko）**：
- 標題：BugEzy — 개발자를 위한 버그 리포팅 도구, AI가 버그를 수정
- 副標題：버그를 설명하기 어렵나요? 녹화 버튼을 누르고 말하기만 하세요. BugEzy가 화면, 동작, 오류를 자동 수집하여 AI가 수정합니다.
- CTA：🔧 Chrome 확장 프로그램 무료 설치
- 三步驟、為什麼選 BugEzy 等核心區塊翻成韓文

**越南語版（?lang=vi）**：
- 標題：BugEzy — Công cụ báo cáo lỗi cho lập trình viên, AI sửa lỗi giúp bạn
- 副標題：Không thể giải thích lỗi rõ ràng? Chỉ cần nhấn ghi và nói. BugEzy tự động thu thập màn hình, thao tác, thông tin lỗi để AI sửa giúp bạn.
- CTA：🔧 Cài đặt miễn phí tiện ích Chrome
- 三步驟、為什麼選 BugEzy 等核心區塊翻成越南語

**語言切換**：右上角語言按鈕增加 JA / KO / VI 選項（或在現有 中文/EN 旁加入）

**sitemap.xml**：加入 `?lang=ja`、`?lang=ko`、`?lang=vi` 的 URL

**改動檔案**：`server/src/index.ts`

**驗收條件**：
1. /features 七語標籤全部亮色
2. 全站無「三種語言」「more coming soon」殘留
3. `bugezy.dev/?lang=ja` 顯示日文首頁
4. `bugezy.dev/?lang=ko` 顯示韓文首頁
5. `bugezy.dev/?lang=vi` 顯示越南文首頁
6. sitemap 包含新 URL
7. `wrangler deploy` 成功

完成後在下方寫 `🔵 DONE-259`。

---

### 🔵 DONE-259：官網七語全面更新（features 標籤 + 多語 SEO 頁面）已完成

**改動檔案**：只改 `server/src/index.ts`（已 `tsc --noEmit` + `wrangler deploy`，hash `787cf4b4`）。

#### 修 1：/features + 首頁語言標籤全亮
- **/features**：`badge2 soon`（🇯🇵/🇰🇷/🇻🇳）→ `badge2 active`；並補上原本缺的 `🇨🇳 簡體中文` → **7 個全亮**（繁中/簡中/粵/英/日/韓/越）。
- **首頁 §5**：`badge soon` → `badge active` 同上，補簡中 → 7 個全亮。

#### 修 2：「三種語言」→「七種語言」
- 首頁 §5 sec-sub：`t('支援三種語言語音輸入，更多語言即將開放', 'Voice input in 3 languages, more coming soon')` → `t('支援七種語言語音輸入', 'Voice input in 7 languages')`（移除「即將開放 / more coming soon」）。
- **同步更新 JA_MAP/KO_MAP/VI_MAP 的對應 key**（makeT 以繁體 zh 為 key 查表）：舊 key `支援三種語言…` → 新 key `支援七種語言語音輸入`，值改 7 語版（日「7 言語の音声入力に対応」/韓「7개 언어 음성 입력 지원」/越「Hỗ trợ nhập giọng nói bằng 7 ngôn ngữ」）；並各補 `簡體中文` 徽章 label 的日/韓/越譯（簡体字中国語 / 중국어 간체 / Tiếng Trung giản thể）。
- 全站掃描確認 `三種語言 / 更多語言即將開放 / 3 languages / more coming soon` 殘留 **0 筆**。

#### 修 3：日/韓/越首頁著陸頁 + 語言切換 + sitemap
- **著陸頁本已可用**：首頁 homePage 早在 PM-233~235 就把 Hero 標題/副標/CTA/三步驟/為什麼選 BugEzy 全數納入 `JA_MAP`/`KO_MAP`/`VI_MAP`，故 `?lang=ja/ko/vi` **原本就完整渲染日/韓/越首頁**（本卡驗證確認），無需另寫著陸頁。
- **語言切換升級**：首頁右上 lang-switch 由「中↔EN 單一切換」改為 **6 語連結列**（繁/简/EN/日/한/VI，`?lang=` 直連、當前語言 `.on` 高亮）；CSS 改 flex + 子 anchor 樣式。
- **sitemap.xml**：新增 `/?lang=ja`、`/?lang=ko`、`/?lang=vi`（weekly 0.8）。

**驗證**（curl 實測，deploy 後 edge 傳播數次 retry 後穩定）：
- /features 徽章 5/5 回應皆 7 active / 0 soon；含 🇨🇳/🇯🇵/🇰🇷/🇻🇳 active。
- 首頁 sec-sub「支援七種語言語音輸入」（無即將開放）；ja→「7 言語の音声入力に対応」、ko→「7개 언어…」、vi→「…bằng 7 ngôn ngữ」、zh-CN→「七种语言语音输入」（toSimplified）。
- lang-switch 6 語連結、當前語言 `.on`（如 ja 頁「日」高亮）。
- `?lang=ko`→中文簡中徽章「중국어 간체」、`?lang=vi`→越文、`?lang=zh-CN`→「🇨🇳 简体中文」。
- sitemap 含 `/?lang=ja`、`/?lang=ko`、`/?lang=vi`。

**驗收**：
1. ✅ /features 七語標籤全部亮色
2. ✅ 全站無「三種語言」「more coming soon」殘留
3. ✅ `?lang=ja` 顯示日文首頁
4. ✅ `?lang=ko` 顯示韓文首頁
5. ✅ `?lang=vi` 顯示越南文首頁
6. ✅ sitemap 含 ja/ko/vi URL
7. ✅ `wrangler deploy` 成功（`787cf4b4`）

---

