# job-0703.md — BugEzy Day 18

> 接續 job-0702.md（PM-94 + PM-108~127）

---

### 🟡 PM-128：Fable 5 安全稽核第一輪（全面掃描 + 產出缺失清單）

**背景**：BugEzy 已有正式金流（ECPay NT$80 月費 + NT$20 日票），安全是第一優先。仿照 LottoShare 的 Fable 5 八輪審計，對 BugEzy 做全面安全掃描。

**目標**：掃描整個 codebase，產出 `docs/security-audit-round1.md`，按嚴重度分類列出所有缺失。

---

#### 稽核範圍（六大面向）

**① 金流安全**
- ECPay callback 驗證（CheckMacValue 是否每次都驗？偽造 callback 能不能騙過？）
- 日票時間計算（能不能竄改 day_pass_expires_at？到期判斷有沒有 race condition？）
- 月費定期定額（取消訂閱流程安全嗎？能不能重複訂閱？）
- 金額篡改（POST body 的 TotalAmount 能不能改？server 有沒有固定金額？）
- CustomField1（user_id）能不能被竄改冒充其他使用者？

**② 認證與授權**
- Google OAuth flow 有沒有漏洞？
- Session / token 管理（過期、刷新、撤銷）
- API endpoint 哪些需要登入、哪些公開？有沒有該保護但沒保護的？
- user_id 從哪裡取得？能不能偽造？

**③ API 安全**
- 每個 API endpoint 的 input validation（有沒有 SQL injection、XSS？）
- Rate limiting（有沒有？能不能被 DDoS？）
- CORS 設定（太寬鬆？）
- Error handling（有沒有洩漏內部資訊？stack trace 外洩？）

**④ 資料安全**
- Supabase RLS 狀態確認（PM-93 已鎖，但要確認沒有新 table 漏掉）
- service_role key 有沒有外洩風險（程式碼裡有沒有硬寫？git history？）
- 使用者報告資料（別人能不能讀到？report_id 可猜測嗎？）
- R2 儲存的截圖/rrweb（存取控制？）

**⑤ MCP 安全**
- MCP endpoint 認證（任何人都能呼叫？）
- 隱私過濾（user_email 有沒有外洩？）
- Token 消耗（有沒有防止濫用？）

**⑥ Extension 安全**
- Content script 注入風險（inject.ts 在 MAIN world）
- chrome.storage.local 存了什麼敏感資料？
- manifest.json permissions 是否最小化？
- offscreen document 安全性

---

#### 產出格式

`docs/security-audit-round1.md`：

```markdown
# BugEzy 安全稽核 — 第一輪
> 日期：2026-07-03
> 稽核者：Claude Code
> 範圍：server/src/index.ts + extension/src/*.ts + wrangler.toml + manifest.json

## 🔴 P0 — 嚴重（必須立即修）
| # | 問題 | 位置 | 風險 | 建議修法 |
|---|---|---|---|---|

## 🟠 P1 — 高（應盡快修）
| # | 問題 | 位置 | 風險 | 建議修法 |
|---|---|---|---|---|

## 🟡 P2 — 中（排進計畫修）
| # | 問題 | 位置 | 風險 | 建議修法 |
|---|---|---|---|---|

## 🟢 P3 — 低（知道就好）
| # | 問題 | 位置 | 風險 | 建議修法 |
|---|---|---|---|---|

## ✅ 已做好的安全措施
（列出已到位的防護，作為正面確認）
```

---

#### 鐵律

- ⛔ **不改任何程式碼**（純稽核，只產報告）
- ✅ 只讀、只搜、只分析、只寫報告

---

### 🔴 Fable 5 第一輪稽核結果（由 Chat PM 完成）

已在對話中完成掃描，產出 P0~P3 分級缺失清單。以下 PM 逐一修復。

---

### 🟡 PM-128：P0 修復 — Server 端 session token 認證（取代假 base64）

**問題**：`getUserIdFromHeader` 只做 base64 decode，無簽章驗證。任何人可偽造 Authorization header 冒充其他使用者。金流相關端點（取消訂閱、買日票）都受影響。

**修法**：改為 session token 驗證——登入時 server 產生隨機 token 存 DB，後續 API 呼叫用 token 查 DB 取得 user_id。

---

#### §1 Supabase 新增 sessions 表

FOX 需在 SQL Editor 手動跑：

```sql
CREATE TABLE IF NOT EXISTS sessions (
  session_token TEXT PRIMARY KEY,
  user_id TEXT NOT NULL REFERENCES users(user_id),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  expires_at TIMESTAMPTZ DEFAULT (NOW() + INTERVAL '90 days')
);

-- RLS 鎖死（只有 service_role 能存取）
ALTER TABLE sessions ENABLE ROW LEVEL SECURITY;
```

---

#### §2 Server：新增 POST /api/auth/session

擴充登入 Google OAuth 成功後，呼叫此端點換取 session token：

```ts
async function createSession(request: Request, env: Env): Promise<Response> {
  const body = await request.json() as { user_id: string; email: string; name?: string };
  if (!body.user_id || !body.email) {
    return json({ error: 'missing user_id or email' }, 400);
  }

  // 驗證 user 存在（或自動建立）
  const { data: user } = await supa(env)
    .from('users')
    .select('user_id')
    .eq('user_id', body.user_id)
    .maybeSingle();

  if (!user) {
    // 首次登入：建立 user
    await supa(env).from('users').insert({
      user_id: body.user_id,
      email: body.email,
      name: body.name || '',
      plan: 'free',
    });
  }

  // 產生 session token（crypto 隨機，不可猜測）
  const sessionToken = crypto.randomUUID() + '-' + crypto.randomUUID();

  // 存入 sessions 表
  await supa(env).from('sessions').insert({
    session_token: sessionToken,
    user_id: body.user_id,
    expires_at: new Date(Date.now() + 90 * 24 * 60 * 60 * 1000).toISOString(),
  });

  return json({ session_token: sessionToken });
}
```

---

#### §3 Server：改寫 getUserIdFromHeader → verifySession

```ts
async function verifySession(request: Request, env: Env): Promise<string | null> {
  const auth = request.headers.get('Authorization');
  if (!auth) return null;
  const token = auth.replace('Bearer ', '').trim();
  if (!token || token.length < 10) return null;

  // 查 DB 驗證
  const { data } = await supa(env)
    .from('sessions')
    .select('user_id, expires_at')
    .eq('session_token', token)
    .maybeSingle();

  if (!data) return null;

  // 檢查是否過期
  if (new Date(data.expires_at) <= new Date()) {
    // 過期：刪除 session
    await supa(env).from('sessions').delete().eq('session_token', token);
    return null;
  }

  return data.user_id;
}
```

---

#### §4 全站替換 getUserIdFromHeader → verifySession

搜尋所有 `getUserIdFromHeader(request)` 呼叫，替換為 `await verifySession(request, env)`。

注意：`verifySession` 是 async（要查 DB），所有呼叫處都要加 `await`。

影響的端點：
- `POST /api/day-pass/create` — 買日票
- `POST /api/user/cancel` — 取消訂閱
- `GET /api/user/plan` — 查 plan
- `POST /api/user/usage` — 更新用量
- `POST /api/reports` — 上傳報告（fallback user_id）

---

#### §5 月費結帳 user_id 改為需 session

現有 `/checkout?user_id=xxx` 直接從 URL 讀 user_id（P0❷）。改為：

```ts
async function ecpayCheckout(request: Request, env: Env): Promise<Response> {
  const userId = await verifySession(request, env);
  if (!userId) return json({ error: '請先登入' }, 401);
  // ... 後續不變
}
```

擴充端呼叫時改為 POST 帶 session token，不走 GET URL。

---

#### §6 路由註冊

```ts
if (request.method === 'POST' && path === '/api/auth/session') {
  return await createSession(request, env);
}
```

---

#### §7 保留舊的 getUserIdFromHeader 做過渡

短期內舊版擴充還在用舊的 base64 header。可以做 fallback：

```ts
async function getAuthUserId(request: Request, env: Env): Promise<string | null> {
  // 優先用新的 session token
  const sessionUserId = await verifySession(request, env);
  if (sessionUserId) return sessionUserId;
  
  // 過渡期：舊版 base64（等全部使用者更新後移除）
  return getUserIdFromHeader(request);
}
```

等 Chrome Web Store 新版全面推送後，再刪除舊的 fallback。

---

#### §8 不改的東西

- extension：不動（PM-129 才改）
- ECPay callback 流程：不動（用 CustomField1 帶 user_id，不走 session）
- MCP：不動
- 首頁 / 報告頁：不動

#### 前置：FOX 跑 SQL（§1）

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. `POST /api/auth/session` 帶 `{ user_id, email }` → 回傳 `{ session_token }`
2. 用 session_token 呼叫 `GET /api/user/plan` → 正常回 plan
3. 用假的 token → 401
4. 用舊的 base64 header → 過渡期仍可用（fallback）

---

### ✅ DONE-128 — Claude Code 回報（2026-07-03）

**完成內容**（只改 `server/src/index.ts` + `server/schema.sql`，extension 未動，符合 §8）：

1. **§2 新增 `POST /api/auth/session`**（`createSession`）— 帶 `{ user_id, email, name? }` → 驗 user 存在（不存在自動建）→ 產生雙 UUID 隨機 token 存 `sessions` 表（90 天到期）→ 回 `{ session_token }`。路由註冊於 `/api/auth/google` 下方。
2. **§3 新增 `verifySession`（async）**— 查 `sessions` 表驗 token；過期自動刪除並回 null；token 長度 < 10 直接拒（擋舊短 base64 誤判）。
3. **§7 新增 `getAuthUserId`（async fallback）**— 優先 `verifySession`，查無再退回舊 `getUserIdFromHeader`（base64）。**全站呼叫處統一改用它**，過渡期新舊擴充並存。
4. **§4 全站替換 + 加 `await`**（5 處）：`createReport`(fallback user_id)、`getUserPlan`、`bumpUsage`、`handleDayPassCreate`、`ecpayCancel`。原 `getUserIdFromHeader` 現只剩 `getAuthUserId` 內部 fallback 引用。
5. **§5 月費 checkout 改 POST 帶 session**：新增 `POST /checkout`（`getAuthUserId` 驗證，401 擋未登入）；`ecpayCheckout` 簽名由 `(url, env)` 改 `(userId, origin, env)`。**保留 `GET /checkout?user_id=` 做過渡 fallback**（舊版擴充仍可用，待新版全面推送後移除）。
6. **schema.sql** 加 `sessions` 表定義 + `ENABLE ROW LEVEL SECURITY`（與 §1 一致，方便 FOX 對照；FOX 仍需手動跑）。

**驗收（線上 curl，`bugezy-api.bugezy-api.workers.dev`）**：
- `POST /api/auth/session` 缺 body → **400** ✅
- `GET /api/user/plan` 無 auth → **401** ✅
- 假 token → **401** ✅（sessions 查無 + base64 解不出）
- `POST /checkout` 無 auth → **401** ✅
- `GET /checkout` 無 user_id → **400** ✅（過渡路徑存活）
- 舊 base64 header（不存在用戶）→ **404 user not found** ✅（fallback 有解出 user_id 打到 DB）

**部署**：`npx wrangler deploy` 成功，Version `6d9705f9-fafc-4ee3-ab30-b6eff368f1dc`。
- tsc `--noEmit` 通過。
- 即使 FOX 尚未跑 SQL，`verifySession` 查不到表也 graceful 回 null → 自動走 base64 fallback，不會 500（已線上驗證）。

**⚠ FOX 待辦**：
1. Supabase SQL Editor 跑 §1 的 `CREATE TABLE sessions` + `ENABLE ROW LEVEL SECURITY`（未跑前 `/api/auth/session` 建 token 會 500，但既有 base64 fallback 不受影響、全站正常）。
2. PM-129（下一卡）：extension 端登入後改呼叫 `/api/auth/session` 取 token、月費升級改 `POST /checkout`，之後才能移除舊 base64 fallback。

**DONE-128**

---

### 🟡 PM-129：P0 修復 — Extension 端改用 session token 認證

**背景**：PM-128 server 端已完成 session token 驗證。Extension 端還在用舊的 base64 header，需要改為登入後取得 session token，所有 API 呼叫改用 token。

---

#### §1 background.ts / popup.ts：登入後取得 session token

Google OAuth 登入成功後（取得 user_id + email），呼叫 server 取 session token：

```ts
// Google OAuth 成功後
const sessionRes = await fetch(`${API_BASE}/api/auth/session`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ user_id: user.user_id, email: user.email, name: user.name }),
});
const sessionData = await sessionRes.json();

if (sessionData.session_token) {
  // 存到 storage
  await chrome.storage.local.set({ 'bugezy:session-token': sessionData.session_token });
}
```

---

#### §2 統一的 API 呼叫 header

建一個 helper 函式，所有 API 呼叫統一用：

```ts
async function getAuthHeaders(): Promise<Record<string, string>> {
  const store = await chrome.storage.local.get('bugezy:session-token');
  const token = store['bugezy:session-token'];
  return {
    'Content-Type': 'application/json',
    ...(token ? { 'Authorization': `Bearer ${token}` } : {}),
  };
}
```

---

#### §3 全域搜尋舊的 Authorization header

搜尋 extension 所有 `Authorization` / `Bearer` / `x-bugezy-user` / `btoa` 相關的 API 呼叫，全部改用 `getAuthHeaders()`。

可能在：
- `background.ts`（上傳報告、查 plan、用量、取消訂閱）
- `popup.ts`（查 plan、升級付費、日票）
- `annotate.ts`（上傳截圖報告）
- `day-pass-checkout.ts`（如果有）

---

#### §4 月費升級改 POST

現有 popup 的升級按鈕開 `GET /checkout?user_id=xxx`。改為：

```ts
monthlyBtn.addEventListener('click', async () => {
  const headers = await getAuthHeaders();
  // 用 POST 開新分頁（需要用 form submit 或 fetch + open）
  // 最簡單：fetch POST 拿到 HTML → 開 blob URL
  const res = await fetch(`${API_BASE}/checkout`, {
    method: 'POST',
    headers,
  });
  const html = await res.text();
  const blob = new Blob([html], { type: 'text/html' });
  chrome.tabs.create({ url: URL.createObjectURL(blob) });
});
```

或者更簡單：server 的 POST /checkout 回傳 redirect URL，extension 直接開。Claude Code 自行判斷最佳方案。

---

#### §5 日票購買同步

日票的 `handleDayPassCreate` 已經用 `getAuthUserId`（PM-128），extension 端只需確保送 session token header 就行。

---

#### §6 登出時清除 session token

```ts
logoutBtn.addEventListener('click', async () => {
  await chrome.storage.local.remove('bugezy:session-token');
  // ...現有的登出邏輯
});
```

---

#### §7 不改的東西

- server：不動（PM-128 已完成）
- MCP：不動
- 首頁 / 報告頁：不動
- ECPay callback：不動（用 CustomField1）

#### Build

```
cd extension && npm run build
```

#### 驗收

1. reload → 登出 → 重新 Google 登入 → DevTools console 確認 session token 已存到 storage
2. 查 plan → 應正常回 plan（用 session token 驗證）
3. 買日票 → 正常跳綠界（session token 認證）
4. 月費升級 → 正常跳綠界（POST 認證，不是 GET URL）
5. Supabase sessions 表 → 應有一筆 session 記錄
6. 假的 session token → 401

---

### ✅ DONE-129 — Claude Code 回報（2026-07-03）

**完成內容**（只改 extension，server/MCP/首頁不動，符合 §7）：

1. **§1 登入後換取 DB session token**（`popup.ts`）— 新增 `ensureSessionToken(session, force)`：POST `/api/auth/session` 帶 `{user_id,email,name}` → 取回 token 存 `SESSION_TOKEN_KEY`（`bugezy:session-token`）。Google 登入成功 `force=true` 強制換新；popup 啟動若「已登入但無 token」（升級後首開）自動補換取（`force=false` 有 token 則跳過，避免每次開 popup 都建新 session 列）。
2. **§2 `getAuthHeaders()` helper**（新 `extension/src/auth.ts`）— 統一回 `{Content-Type, Authorization?}`；**優先讀新 DB token，過渡期退回舊 `SESSION_KEY.session_token`（base64）**，配合 server PM-128 fallback，升級用戶不用重登也能用。
3. **§3 全站替換舊 header**（6 處，全走 `getAuthHeaders()`）：`popup.loadPlan`／`popup.cancelSub`／`background.checkRecordingUsage`／`background.uploadReport`／`background.UPLOAD_MONITOR_REPORT`／`annotate` 截圖上傳／`day-pass-checkout`。搜尋確認 `Bearer ${...}` 僅剩 `auth.ts` 一處。
4. **§4 月費升級改 POST**（`popup.openCheckout`）— 原 `chrome.tabs.create('/checkout?user_id=xxx')`（GET 暴露 user_id）→ 改開新跳板頁 `checkout.html`（新 `checkout.ts`：讀 session → POST `/checkout` 帶 token → 取回綠界表單 HTML 手動 submit）。**沿用日票 `day-pass-checkout` 成熟做法**（不用 popup blob URL，避免 popup 關閉時 blob 被撤銷失效）。build.mjs 加 `checkout` entry + `checkout.html` 複製。
5. **§5 日票**：`day-pass-checkout.ts` 改用 `getAuthHeaders()`（送 session token），server 端 PM-128 已就緒。
6. **§6 登出清 token**：`logoutBtn` 一併 `remove([SESSION_KEY, SESSION_TOKEN_KEY])`。

**建置**：`tsc --noEmit` 通過（修掉 `loadPlan` 移除 header 後 `session` 參數未用 → 一併移除參數 + 2 處呼叫）；`npm run build` 成功，`dist/` 含 `checkout.html/js`（+ 既有 17 檔）。popup.js 27.6kb。

**未做（依指示）**：未 `wrangler deploy`（server 端 PM-128 已部署）；extension 未重打包上架 zip（等後續一起）。

**⚠ 交接**：
- 需 FOX 先跑 PM-128 §1 的 `CREATE TABLE sessions`，否則 `/api/auth/session` 建 token 會 500 → `ensureSessionToken` 靜默失敗 → `getAuthHeaders` 退回舊 base64（仍可用，不崩）。跑了 SQL 後新登入才會有真 token。
- 驗收 1~6 需瀏覽器實機（reload 擴充 → 登入 → 查 storage `bugezy:session-token` → 月費/日票跳綠界 → sessions 表出現記錄）。

**DONE-129**

---

### 🟡 PM-130：P1 修復 — CORS 收緊 + FAQ 刪競品 + 錯誤訊息脫敏

**修三個問題**：
- P1❸ CORS `*` → 只允許自家域名 + chrome-extension
- P2❿ FAQ 刪 Jam 比較，改自身優勢
- P2❽ 錯誤訊息脫敏（不洩漏 Supabase 內部錯誤）

---

#### §1 CORS 收緊

`server/src/index.ts` 第 78~83 行：

現有：
```ts
const CORS = { 'Access-Control-Allow-Origin': '*' };
```

改為動態判斷：
```ts
function getCorsHeaders(request: Request): Record<string, string> {
  const origin = request.headers.get('Origin') || '';
  const allowed = [
    'https://bugezy.dev',
    'https://bugezy-api.bugezy-api.workers.dev',
    'chrome-extension://mpkakmmfllghcdaeicdlnpogneeanhmb',
  ];
  
  // chrome-extension 不帶固定 origin，允許所有 chrome-extension://
  const isAllowed = allowed.includes(origin) || origin.startsWith('chrome-extension://');
  
  return {
    'Access-Control-Allow-Origin': isAllowed ? origin : 'https://bugezy.dev',
    'Access-Control-Allow-Methods': 'GET, POST, PATCH, OPTIONS',
    'Access-Control-Allow-Headers': 'Content-Type, Authorization',
    'Access-Control-Max-Age': '86400',
  };
}
```

所有 `json()` / `html()` 回傳都改用 `getCorsHeaders(request)`。OPTIONS preflight 也要正確回應。

---

#### §2 FAQ 刪競品比較

`server/src/index.ts:871-872`：

刪除：
```html
<div class="faq-q">跟 Jam 有什麼不同？</div>
<div class="faq-a">...</div>
```

改成強調自身優勢：
```html
<div class="faq-q">BugEzy 最大的優勢是什麼？</div>
<div class="faq-a"><p>專為亞洲開發者設計：中文/粵語/日韓語音支援、NT$80 超平價月費、MCP 整合讓 AI 直接讀報告。獨家功能：即時監控、30 秒回溯、Whisper 精準語音、終端機 CLI、Token 透明度。</p></div>
```

---

#### §3 錯誤訊息脫敏

搜尋所有 `error.message` 直接回傳客戶端的地方，改為通用訊息：

```ts
// 舊：
return json({ error: `supabase insert failed: ${error.message}` }, 500);

// 新：
console.error('supabase insert failed:', error.message);
return json({ error: '伺服器內部錯誤，請稍後再試' }, 500);
```

所有 500 錯誤都改成通用訊息，原始 error 只記 console.error（Workers log）。

---

#### §4 不改的東西

- extension：不動
- ECPay callback / MCP / Supabase：不動
- 認證機制：不動（PM-128/129 已完成）

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. 從 `bugezy.dev` fetch API → CORS 正常（允許）
2. 從其他網站 fetch API → CORS 被擋
3. FAQ 頁不再出現「Jam」字樣
4. 故意觸發 500 → 回「伺服器內部錯誤」不洩漏 Supabase 訊息

---

### ✅ DONE-130 — Claude Code 回報（2026-07-03）

**完成內容**（只改 `server/src/index.ts`，extension/MCP/認證不動，符合 §4）：

1. **§1 CORS 收緊（P1❸）**——刪 `const CORS = { ACAO: '*' }`，改 `getCorsHeaders(request)`：只放行 `bugezy.dev` + `bugezy-api...workers.dev` + 任意 `chrome-extension://`（擴充 ID 可能變），其餘回退 `https://bugezy.dev`（等同拒絕跨源讀取）；補 `Allow-Methods` 加 `PATCH`、`Allow-Headers` 加 `Authorization`、`Max-Age` + `Vary: Origin`。**實作**：`json()` 只留 Content-Type，CORS 改在 `fetch()` **統一出口**注入（把主路由包成 IIFE → 對回應 `headers.set` 覆蓋）；OPTIONS preflight 用動態 cors；**MCP 端點移到 IIFE 前、不套自訂 CORS**（交 handler 自理，避免破壞 Claude.ai Connector）。
2. **§2 FAQ 刪競品（P2❿）**——`跟 Jam 有什麼不同？` → `BugEzy 最大的優勢是什麼？`，內容改自身優勢（中文/粵語/日韓語音、NT$80、MCP、即時監控/回溯/Whisper/CLI/Token 透明度），全站無 Jam 字樣。
3. **§3 錯誤脫敏（P2❽）**——14 處 500 回應（含最外層 catch）原本回 `error.message`/`String(err)` → 一律改 `console.error(原始錯誤)` + 回通用 `GENERIC_500='伺服器內部錯誤，請稍後再試'`。涵蓋：createReport insert、updateReportSettings、listReports、summarize、correct、googleAuth（查/建使用者 + catch）、getUserPlan、bumpUsage、ecpayCancel。400/401/404 維持原訊息（非敏感）；MCP 的 `txt(查詢失敗:...)` 依 §4 不動。

**驗收（線上 curl，`bugezy-api...workers.dev`）**：
- CORS `Origin: bugezy.dev` → 回 `ACAO: bugezy.dev` + `Vary: Origin` ✅
- `Origin: chrome-extension://…` → 回顯同 origin ✅
- `Origin: evil.com` → 回退 `bugezy.dev`（不放行）✅（非快取端點 `/api/user/plan` 驗證）
- OPTIONS preflight → `Allow-Methods: GET, POST, PATCH, OPTIONS` + `Allow-Headers: Content-Type, Authorization` ✅
- `/faq` → Jam 出現 0 次、新標題在 ✅
- 500 脫敏：`tsc` + grep 確認全 500 路徑已無 `error.message`（外部難觸發真實 DB 500，改以程式碼稽核確認）✅

**部署**：`npx wrangler deploy` 成功，Version `c81e0d89-ab09-4a47-b523-db5803c545f7`。tsc `--noEmit` 通過。
- ⚠ 部署後首測 `/api/version`、`/faq` 曾短暫回舊值（`ACAO:*`／含 Jam）——是 **Cloudflare 邊緣快取的部署前舊回應**；加 cache-buster / 打非快取端點即為新結果，舊快取會自然過期，非程式問題。

**DONE-130**

---

### 🟡 PM-131：P1 修復 — Rate Limiting + Payload 大小限制

**問題**：
- P1❹ 所有 API 零限流，可被 DDoS 或濫用（灌報告、消耗 Groq 額度）
- P2❼ 報告上傳無 body size 限制，可灌爆 R2

---

#### §1 簡易 Rate Limiter（基於 IP，Cloudflare Workers 記憶體）

Cloudflare Workers 無持久化 state（除非用 Durable Objects/KV），但可用**請求級別的簡易防護**：

方案：用 Cloudflare 的 `request.headers.get('CF-Connecting-IP')` 取 IP，搭配一個 Map 做滑動窗口限流。但 Workers 每次請求是獨立的，Map 不跨請求。

**實際可行方案**：用 Supabase 做簡易限流表，或直接用 Cloudflare 的 Rate Limiting Rules（dashboard 設定）。

最簡單有效的做法——在程式碼裡加**單次請求防護**：

```ts
// 1. POST body 大小限制
const MAX_REPORT_SIZE = 5 * 1024 * 1024; // 5MB
const MAX_TRANSCRIBE_SIZE = 25 * 1024 * 1024; // 25MB（Groq 限制）

// 在 createReport 開頭加：
const contentLength = parseInt(request.headers.get('Content-Length') || '0');
if (contentLength > MAX_REPORT_SIZE) {
  return json({ error: '報告大小超過 5MB 上限' }, 413);
}

// 在 handleTranscribe 開頭加（如果沒有的話）：
if (contentLength > MAX_TRANSCRIBE_SIZE) {
  return json({ error: '音訊超過 25MB 上限' }, 413);
}
```

---

#### §2 Cloudflare Dashboard Rate Limiting（推薦）

最有效的限流是在 Cloudflare Dashboard 設定 Rate Limiting Rules：

```
規則 1：/api/reports POST — 每 IP 每分鐘 10 次
規則 2：/api/transcribe POST — 每 IP 每分鐘 20 次
規則 3：/api/day-pass/create POST — 每 IP 每分鐘 3 次
規則 4：/api/auth/session POST — 每 IP 每分鐘 5 次
規則 5：/checkout POST — 每 IP 每分鐘 3 次
```

這個 FOX 在 Cloudflare Dashboard 手動設，不需要改程式碼。但 PM 先在程式碼裡加 body size 限制。

---

#### §3 程式碼層級的防護

在 server 主路由的最前面加一個全域 body size 檢查：

```ts
// 所有 POST 請求的 body 上限 10MB（transcribe 除外）
if (request.method === 'POST' && path !== '/api/transcribe') {
  const cl = parseInt(request.headers.get('Content-Length') || '0');
  if (cl > 10 * 1024 * 1024) {
    return json({ error: '請求過大' }, 413);
  }
}
```

---

#### §4 /api/transcribe 已有的防護確認

PM-85 已加 25MB 限制和最小 100 bytes 檢查，確認還在就好。

---

#### §5 不改的東西

- extension：不動
- ECPay callback：不動
- MCP：不動
- Cloudflare Dashboard 設定：FOX 手動做（非程式碼）

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. 上傳 > 5MB 報告 → 413 被擋
2. POST body > 10MB（非 transcribe）→ 413 被擋
3. 正常報告上傳 → 正常通過

#### FOX 手動：Cloudflare Dashboard Rate Limiting

deploy 後，FOX 去 Cloudflare Dashboard → Security → WAF → Rate Limiting Rules，按 §2 設定。

---

### ✅ DONE-131 — Claude Code 回報（2026-07-03）

**完成內容**（只改 `server/src/index.ts`，extension/MCP/callback 不動，符合 §5）：

1. **§3 全域 POST body 10MB 上限**——在 `fetch()` 統一路由（IIFE）最前面加：`request.method === 'POST' && path !== '/api/transcribe'` 時讀 `Content-Length`，> `MAX_POST_SIZE`(10MB) 直接回 `413 請求過大`。依 Content-Length 先擋，省下讀 body 成本。transcribe 明確排除（音訊另計 25MB）。
2. **§1 createReport 5MB 上限**——`createReport` 開頭讀 `Content-Length`，> `MAX_REPORT_SIZE`(5MB) 回 `413 報告大小超過 5MB 上限`（防灌爆 R2）。位置在 `request.json()` 之前，超標不浪費解析。
3. **§4 transcribe 25MB 確認**——`handleTranscribe` 既有 `audioBlob.size > 25*1024*1024 → 400` + `< 100 bytes → 400` 兩道檢查**仍在**，未動。
4. 新增常數 `MAX_POST_SIZE` / `MAX_REPORT_SIZE`（放 `GENERIC_500` 旁）。

**驗收（線上 curl，`bugezy-api...workers.dev`）**：
- 報告帶 6MB body → **413**「報告大小超過 5MB 上限」✅
- 非 transcribe POST（`/api/user/usage`）11MB → **413**「請求過大」✅
- 正常小報告（`{"hello":"world"}`）→ **400** 缺 pageInfo（代表通過 size 檢查進入邏輯）✅
- transcribe 送 11MB → **不是** 413（豁免 10MB），進入 Groq 邏輯（拒非音訊 502）→ 確認 25MB 限制仍在、11MB 放行 ✅

**部署**：`npx wrangler deploy` 成功，Version `05039693-b898-40d4-b39d-cb1eee4bdabc`。tsc `--noEmit` 通過。

**⚠ FOX 手動待辦**（§2，非程式碼）：Cloudflare Dashboard → Security → WAF → Rate Limiting Rules 設 5 條 IP 限流（reports 10/min、transcribe 20/min、day-pass 3/min、auth/session 5/min、checkout 3/min）。程式層 body size 已擋，限流仍建議在邊緣做。

**備註**：Content-Length 是防呆第一道（惡意 client 可偽造/不帶）；真正硬上限由 Cloudflare 平台（Workers request body 預設上限）+ Dashboard Rate Limiting 把關，程式層 413 主要擋掉正常/半惡意的超大請求，兩者互補。

**DONE-131**

---

---

### 🔍 Fable 5 獨立安全稽核（第二輪）

> 日期：2026-07-03
> 稽核模型：Fable 5
> 範圍：server（index.ts 3579 行）+ extension（src/*.ts）+ config（wrangler.toml / schema.sql / rls-lockdown.sql / manifest.json）全掃描
> 方法：全新視角重讀，不預設第一輪修復正確。重點放在「有實際金流／資料風險」的可利用漏洞。

#### 驗證第一輪修復（Opus 修的 6 項是否真的修好）
| # | 問題 | 修復狀態 | 我的評估 |
|---|---|---|---|
| 1 | getUserIdFromHeader 假認證 → session token + verifySession（PM-128） | ❌ **未真正修好** | verifySession 本身正確，但（a）`getAuthUserId` 仍保留 `getUserIdFromHeader` base64 fallback（:172-176），偽造 token 依舊可用；（b）`googleAuth` 登入後仍簽發 base64 token（:2639）；（c）致命點：簽發端 `createSession`（:179-210）憑客戶端傳的 user_id 直接發正式 token、零身分證明。驗證端安全、簽發端不設防 → 淨安全收益幾乎為零。見 P0-2 / P0-3。 |
| 2 | /checkout?user_id= URL 洩漏 → POST + session（PM-128） | ⚠ 大致修好 | 新 POST /checkout 需 getAuthUserId（:2205-2208）✅。但舊 GET /checkout?user_id= 仍在線且無認證（:2211-2215），且整體把 user_id 當非機密到處傳，與 #1 同根。建議移除過渡 GET。 |
| 3 | CORS * 全開 → 自家域名 + chrome-extension（PM-130） | ✅ 已收緊（但非安全邊界） | 依 Origin 白名單反射（:86-97）已符規格。惟：此 API 用 Bearer token 非 cookie，且多數敏感端點根本無認證，CORS 擋不住 curl／server-side 攻擊；`chrome-extension://` 前綴放行「任意」擴充而非只放 BugEzy ID。CORS 收緊屬防禦縱深，對真正的攻擊面幾乎無保護。 |
| 4 | 錯誤訊息洩漏 Supabase 內部資訊 → 脫敏（PM-130） | ⚠ 部分修好 | 未處理例外 → GENERIC_500 ✅。殘留洩漏：`/api/transcribe` 回傳 `detail: errText`（Groq 原始錯誤，:2560）；MCP 多處 `查詢失敗: ${error.message}`（Supabase 錯誤，:3269/3281…）經「無認證」MCP 對外。見 P2-4。 |
| 5 | FAQ 含競品名稱 Jam → 已刪除（PM-130） | ✅ 已刪 | `search_text server/src "Jam"` 查無任何結果，部署頁面已無競品名，符合行銷鐵律。 |
| 6 | POST body 無大小限制 → 5MB/10MB 上限（PM-131） | ✅ 合理 | Content-Length 預檢 + 報告 5MB + 全域 10MB + transcribe 25MB（:2102-2107, :2278-2282）。caveat：Content-Length 可被惡意 client 偽造/省略，串流時未逐段強制，真正硬上限靠 Cloudflare 平台上限 + 待設 WAF 限流（DONE-131 已自述）。方向正確。 |

#### 🔴 P0 — 新發現的嚴重問題
| # | 問題 | 位置（檔案:行號） | 風險描述 | 建議修法 |
|---|---|---|---|---|
| P0-1 | **GET /api/reports 無認證、無 user 過濾 → 全站報告任意讀取** | index.ts:2238-2240（listReports :2421-2443）；配合 GET /api/reports/:id :2246-2249（getReport :2356-2397） | 任何**匿名者**打 `GET /api/reports?limit=50` 取得全站最新 50 筆報告的 report_id，再逐一 `GET /api/reports/:id` 讀完整 console_logs / network_errors（含 responseBody）/ voice_transcript / rrweb / screenshots。直接違反隱私政策「僅在主動分享連結時可見」。因 RLS 採 deny-all + service_role，Worker 是唯一守門員，此處**完全沒守**。這是本次最嚴重、最易利用、傷害最廣的漏洞。 | listReports 必須先 `getAuthUserId` 再 `.eq('user_id', me)`；getReport 對非分享情境驗證呼叫者為 owner。分享連結若要維持免登入，須確保 report_id 絕不可被列舉（即移除 listReports 的公開/無過濾行為）。 |
| P0-2 | **createSession 憑客戶端 user_id 直接簽發正式 session token（無身分證明）** | index.ts:179-210（POST /api/auth/session :2191-2193） | 只要知道任一 victim 的 user_id（UUID），POST `{user_id: "<victim>", email:"x"}` 即可換到 victim 的**合法** DB token → 完整帳號接管，並繞過 Google 登入。這正是 PM-128「安全 token」的致命破口：token 驗證安全、但 token 簽發不設防。app 把 user_id 當非機密（base64 token 可解、request body 到處帶），使 UUID 事實上可取得。 | `/api/auth/session` **不可**接受客戶端 user_id；改由 server 從**已驗證的 Google ID token** 推導 user_id（見 P1-4），或直接把 googleAuth 合併成「驗 Google → 發不可偽造 DB token」，全程不信任客戶端 user_id。 |
| P0-3 | **舊 base64 token fallback 仍啟用 + googleAuth 仍簽發 base64 token** | getUserIdFromHeader index.ts:135-144；getAuthUserId :172-176；googleAuth 簽發 :2639 | `Authorization: Bearer <btoa("<victim_uuid>:任意時戳")>` **永久有效、可偽造、無到期、無簽章**。與 P0-2 同根。任何一處洩漏 user_id（base64 token 本身、共用電腦、log）即被永久冒用。 | 立即移除 getUserIdFromHeader fallback；googleAuth 直接回 DB session token（或前端一律改走 P0-2 修正後的安全簽發路徑），並在移除前確認新版擴充已全面推送。 |

#### 🟠 P1 — 新發現的高風險
| # | 問題 | 位置 | 風險描述 | 建議修法 |
|---|---|---|---|---|
| P1-1 | **/mcp 端點完全無認證；list_reports 僅靠 email 當能力憑證** | index.ts:2095-2098（handler 無 auth 包裹）；list_reports :3241-3283 | email **非機密**。攻擊者用 victim email → `list_reports` 取得所有 report_id → 逐一 `get_console_logs / get_network_errors / get_voice_transcript / get_rrweb_events / get_screenshots` → 全量竊取。報告內含 network responseBody、console 訊息（可能有 token/API key/PII）、語音逐字稿、DOM 快照。 | MCP 端點加 per-user 金鑰/OAuth；list_reports 與各 get_* 綁定「已認證呼叫者」並驗證 report ownership，而非以 email 當通行證。 |
| P1-2 | **即時監控／終端機日誌為全域單一 R2 key → 跨租戶洩漏，且端點與 MCP 皆無認證** | LIVE_ERRORS_KEY='live-errors/latest.json'（index.ts:1764）；POST/GET /api/live-errors :2145-2162（extension 推送無 auth，background.ts:324-334）；/api/terminal-logs :2164-2173；MCP get_live_errors :3415-3426、get_terminal_logs :3429-3440 | 使用者 A 的即時 console/network、以及 A 的**終端機 stderr（可能含 stack trace、env 變數、DB 連線字串、密鑰）**寫入**全站共用**的單一槽，任何人（匿名 GET 或匿名 MCP）可讀，且使用者間互相覆蓋。終端機日誌敏感度最高。 | 改為 per-user key（如 `live-errors/{user_id}.json`、`terminal-logs/{user_id}.json`）；POST/GET 端點加認證；MCP 對應工具綁呼叫者身分。 |
| P1-3 | **未認證且會產生費用的 AI 端點：/api/transcribe（Groq）、/api/summarize、/api/correct（Workers AI）** | handleTranscribe index.ts:2521（路由 :2184）；summarizeText :2446（:2178）；correctText :2476（:2181） | 三者皆**無認證、無速率限制**，直接消耗 FOX 的 Groq 額度與 Cloudflare Workers AI 用量 → 荷包型 DoS／成本濫用。transcribe 每次可送 25MB 音訊，成本放大明顯。 | 三個端點加 `getAuthUserId` + 速率限制，並把用量計入付費/免費額度。 |
| P1-4 | **Google 登入用 access token 當認證、未驗 aud / 未用 ID token** | googleAuth index.ts:2583-2599（打 /oauth2/v2/userinfo） | 任一其他 App 取得、含 email/profile scope 的 Google access token 可被**重放**到此端點，冒充該 Google 使用者登入（access token 無 audience 綁定）。搭配 P1-5 的 client_secret 外洩風險更高。 | 改為驗證 Google **ID token**，檢查 `aud === BugEzy client_id` 及 `iss`、`exp`；不要用 access token 當身分證明。 |
| P1-5 | **OAuth client secret 明文存在於 repo（+ debug/ 含真實報告 payload）** | debug/client_secret_610395663887-…apps.googleusercontent.com.json（304B）；debug/*.json（多個 5MB+ 真實 payload） | GitHub remote 設定為待辦事項；一旦推上（即使私有）即憑證外洩。debug/*.json 為真實 bug 報告，可能含真實使用者資料與網路內容。 | 立即從 repo 移除該檔並**輪替** OAuth client secret；`debug/` 加入 .gitignore；推 GitHub 前用 git filter-repo / BFG 清 git history。 |

#### 🟡 P2 — 新發現的中風險
| # | 問題 | 位置 | 風險描述 | 建議修法 |
|---|---|---|---|---|
| P2-1 | **ECPay callback 無冪等/重放防護、無金額比對、無「訂單存在且未處理」檢查** | ecpayCallback index.ts:2830-2866；handleDayPassCallback :2947-2977；ecpayPeriodCallback :2998-3031 | 無 orders/payments 表、不去重 MerchantTradeNo。綠界在未收到 1\|OK 前會**重送**通知；任何重複遞送的合法通知都會再次授予權益——**日票每收一次 callback +24h、月費每收一次 +1 個月**，單筆付款可被放大成多期。TotalAmount 雖被 CheckMacValue 涵蓋，但 handler 從不比對是否等於 80/20，也只信 CustomField1 帶回的 user_id。 | 建 payments 表記錄 MerchantTradeNo + 狀態；callback 先查重（已處理直接回 1\|OK、不再變更 DB）；比對 TotalAmount 與預期金額；以 MerchantTradeNo 綁定 user 而非僅信 CustomField1。 |
| P2-2 | **免費額度為客戶端驅動、可繞過** | bumpUsage index.ts:2729（路由 :2197，由擴充自行呼叫 background.ts:149）；createReport 不檢查用量 :2277；MCP 完全不計 mcp_count | 使用者不呼叫 /api/user/usage、或直接打 /api/reports、或直接用 MCP，即可無限使用 → 收入流失。MCP 因無認證更無法計量。 | 在真正產生資源的端點（createReport、各 MCP 工具）於**伺服器端**強制檢查並遞增用量，且綁定已認證 user。 |
| P2-3 | **登出不撤銷伺服器端 session；無撤銷端點** | popup logout extension/src/popup.ts:752-756（只清本地 + 清 Google 快取）；server 無 logout 路由 | session token 90 天內仍有效；token 被竊後登出無法止血。 | 加 `POST /api/auth/logout` 刪除 sessions 該列；logout 時呼叫之。可另加「撤銷所有裝置」。 |
| P2-4 | **殘留錯誤資訊外洩（transcribe detail、MCP error.message）** | index.ts:2560（Groq detail）；MCP :3269/:3281 等 `查詢失敗: ${error.message}` | 對外洩漏 Groq / Supabase 原始錯誤字串；MCP 無認證使其對任何人可見。 | 對外一律回 GENERIC 訊息，細節只入 console.error。 |
| P2-5 | **PATCH /api/reports/:id/settings 無認證（翻轉截圖曝光）** | updateReportSettings index.ts:2399-2418（路由 :2242-2245；已知 #2） | 任何知道 report_id 者可將 `allow_screenshot_images` 設為 true，使該報告截圖對任意 MCP 呼叫者可見（原預設 false 是保護）。搭配 P0-1/P1-1 使 report_id 可被列舉/取得 → 實際可被用來擴大截圖曝光。 | PATCH 綁 owner 認證（需 session 且為報告 owner）。 |

#### 🟢 P3 — 新發現的低風險
| # | 問題 | 位置 | 風險描述 | 建議修法 |
|---|---|---|---|---|
| P3-1 | **postMessage 橋接僅用常數 BUGEZY_SOURCE 當守門** | content.ts:79-82；inject.ts:194-199、:1042-1045 | BUGEZY_SOURCE 是 bundle 內公開常數，惡意頁面可偽造 to-content/to-inject 訊息：注入假 console/network/rrweb/voice 到使用者自己的錄製、偽造 MONITOR_UPLOADED 讓 badge 指向攻擊者 URL（輕度釣魚）、或以 REQUEST_VOICE_HISTORY 誘出記憶體語音 buffer。皆限於使用者自身 session、無法竊 token 或跨源資料。 | 改用 chrome.runtime port，或加隨機 nonce 握手；至少 FLUSH_* 僅在錄製中才接受。 |
| P3-2 | **formatEcpayDate 用 UTC（非 UTC+8）** | index.ts:269-272（已知 #3） | MerchantTradeDate 慢 8 小時。目前綠界接受（7/2 已成功付款），屬對帳/顯示正確性問題，**非安全漏洞**。取消訂閱 TimeStamp 用 Unix epoch，不受影響。 | 建議修正為台灣時間，以免綠界日後收緊驗證或對帳混亂。 |
| P3-3 | **manifest 廣域注入（<all_urls> MAIN world）+ 版本 0.1.0 未更新** | extension/manifest.json:4, :28-41 | MAIN world 全站注入是產品必要（要側錄任意頁），但屬最大攻擊面（擴充若被入侵即握有所有頁面 JS 上下文）。版本 0.1.0 vs /api/version 1.1.0 為待辦，非安全。 | 評估能否縮小注入範圍/改按需注入；上架前把 manifest 版本同步為 1.1.0。 |

#### 未修復的已知問題評估
| # | 問題 | 我的嚴重度評估 | 理由 | 建議 |
|---|---|---|---|---|
| 1 | MCP 端點 /mcp 無認證 | 🔴 **P1（升級）** | 不只「無認證」——搭配 list_reports 以 email 為通行證，可用 email 全量竊取他人報告（含 network body/語音/DOM）。見 P1-1。 | MCP 加 per-user token/OAuth；list_reports 與 get_* 綁身分 + 驗 ownership。 |
| 2 | PATCH /settings 無認證（by design, share link） | 🟡 P2 | 「有 share link 即可改」在 report_id 不可列舉時尚可接受；但 P0-1 已破壞不可列舉性，故實際可被用來擴大截圖曝光。見 P2-5。 | 綁 owner 認證。 |
| 3 | ECPay formatEcpayDate 用 UTC 非台灣時間 | 🟢 P3 | 非安全問題；綠界目前接受。屬對帳/正確性。見 P3-2。 | 修正時區，優先度低於上述所有金流/認證項。 |

#### ✅ 安全措施正面確認
- **Supabase RLS 全表 deny-all + service_role 繞過**（schema.sql、rls-lockdown.sql）設計正確、冪等腳本完整。惟因此 **Worker 成為唯一授權層**，上述 app 層漏洞皆**無第二道防線**（這也是我把授權缺口評為 P0/P1 的原因）。
- **CheckMacValue 演算法正確**：SHA256 + ecpayUrlEncode（含 .NET 特殊字元還原）+ **常數時間比較 timingSafeEqualStr**（防 timing attack）。ECPay 四把金鑰全走 `wrangler secret`，原始碼與 wrangler.toml 皆**無明文**（PM-94 確認）。
- **SimulatePaid=1 處理正確**：回 1|OK 但不升級（三個 callback 一致）。
- **report_id 用 crypto.randomUUID()**（不可預測）；所有 DB 查詢走 supabase-js `.eq()` **參數化**，無 SQL injection；`.ilike` 的 `%` 包在值上、由 client 端跳脫。
- **擴充無有效 XSS sink**：動態 innerHTML 僅來自受信任來源（manifest 版本 :764、伺服器產生的綠界表單 checkout.ts:39 / day-pass-checkout.ts:39、靜態標籤+數字 edit-report.ts:113）；inject.ts 刻意用 textContent/DOM 節點（相容 Trusted Types，如 GitHub）。session token 僅存在特權情境（popup/background），**從不進 MAIN world/頁面**（content.ts、inject.ts 皆未引用 SESSION_TOKEN_KEY）。
- **session 驗證端設計良好**：雙 UUID token（不可猜測）、90 天到期、verifySession 到期自動刪除。（問題在**簽發端**，非驗證端。）
- **金額寫死在 checkout 端**（TotalAmount 80/20 由 server 設定，客戶端不可改）。
- **body 大小上限**（PM-131）+ 待設 Cloudflare WAF 限流，方向正確。

#### 總結
**整體安全評分：4 / 10**
金流簽章（CheckMacValue）與 RLS 基礎穩固、擴充端無 XSS/token 外洩、密鑰管理乾淨——這些是加分項。但**認證信任鏈**與**資料存取授權**存在多個「匿名或低成本即可利用」的嚴重漏洞，且因 RLS 採 service_role 繞過，app 層每個缺口都沒有退路。距離「可安心承載正式金流與真實使用者資料」仍有明顯差距。

**最優先要修的 Top 3：**
1. **P0-1 — GET /api/reports 全站報告外洩**：匿名可利用、影響全體使用者、傷害最大且最容易被踩。listReports 立即加 `getAuthUserId` + `.eq('user_id', me)`，並收斂 getReport/report_id 的可列舉性。
2. **P0-2 + P0-3 — 認證信任鏈（createSession 無授權簽發 + base64 fallback + googleAuth 發 base64）**：帳號接管根因。一次性重構 auth：改驗 Google **ID token（檢查 aud）** → server 端簽發不可偽造 token → **移除所有對客戶端 user_id 的信任與 base64 fallback**。
3. **P1-3 + P1-2 — 未認證 AI 端點（transcribe/summarize/correct）+ 全域終端機/即時日誌**：直接金流成本濫用 + 高敏感 stderr 跨租戶外洩。三個 AI 端點加認證 + 限流；即時/終端機日誌改 per-user key + 端點與 MCP 綁身分。

（另註：CLI `cli/` 與舊 `mcp-server/` 目錄非本次部署 Worker 的主要攻擊面，未深入；CLI 對 /api/terminal-logs 的推送已納入 P1-2 一併評估。Cloudflare WAF 限流為 DONE-131 列的 FOX 手動待辦，若尚未於 Dashboard 設定，P1-3/P2 的濫用風險目前僅由程式層 body-size 部分緩解。）

---

## 🔧 Fable 5 稽核修復（依優先序，Opus PM 出卡）

### 🟡 PM-132：P0-1 修復 — GET /api/reports 加認證 + user 過濾（全站報告外洩）

**問題（Fable 5 P0-1）**：`GET /api/reports` 無認證、無 user 過濾。任何匿名者打 `GET /api/reports?limit=50` 取得全站報告 report_id，再逐一 `GET /api/reports/:id` 讀完整內容（console/network body/語音/截圖）。直接違反隱私政策。

**這是最急的洞**——匿名可利用、影響全體使用者。

---

#### §1 listReports 加認證 + user 過濾

`server/src/index.ts` 的 `listReports`（約 :2421）：

```ts
async function listReports(request: Request, env: Env): Promise<Response> {
  // 必須登入
  const userId = await getAuthUserId(request, env);
  if (!userId) return json({ error: '請先登入' }, 401);

  const url = new URL(request.url);
  const limit = Math.min(parseInt(url.searchParams.get('limit') || '20'), 50);

  // 只回傳自己的報告
  const { data, error } = await supa(env)
    .from('reports')
    .select('report_id, url, title, created_at, console_count, network_count, voice_count, screenshot_count')
    .eq('user_id', userId)   // ← 關鍵：只查自己的
    .order('created_at', { ascending: false })
    .limit(limit);

  if (error) {
    console.error('listReports failed:', error.message);
    return json({ error: GENERIC_500 }, 500);
  }
  return json(data ?? []);
}
```

---

#### §2 getReport 加 owner 驗證

`GET /api/reports/:id`（getReport 約 :2356）目前任何人有 report_id 就能讀。因為報告頁 `/report/:id` 是設計成「有連結就能看」（分享用），這裡要區分兩種存取：

**做法**：getReport 保持可用（分享連結需要），但**report_id 不可被列舉**已由 §1 解決（listReports 只回自己的）。所以：

- `GET /api/reports/:id` → 維持免登入（分享連結功能）
- 但因為 report_id 是 `crypto.randomUUID()` 不可猜測 + listReports 不再洩漏他人 ID，實際上等於「只有拿到分享連結的人能看」

這符合隱私政策「僅在主動分享連結時可見」。不需要改 getReport 本身。

---

#### §3 確認 MCP list_reports 也要修（但這張卡先不動，PM 另開）

MCP 的 `list_reports`（:3241）用 email 當通行證，也有同樣問題（Fable 5 P1-1）。這個留到 PM-134 一起處理 MCP 認證。本卡只修 REST 的 `GET /api/reports`。

---

#### §4 extension 端確認

extension 呼叫 `GET /api/reports` 的地方（如果有）要確認有帶 session token header（PM-129 的 getAuthHeaders 已處理）。搜尋 extension 是否有呼叫 list reports，確認帶 auth。

---

#### §5 不改的東西

- getReport（單筆讀取）：不動（分享連結需要）
- MCP list_reports：不動（PM-134 處理）
- createReport / 其他端點：不動
- 認證機制本身：不動（PM-133 才重構）

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. **匿名** `GET /api/reports` → 401（不再回傳任何報告）
2. 帶有效 session token → 只回傳自己的報告
3. 帶 A 的 token → 看不到 B 的報告
4. `GET /api/reports/:id`（有正確 report_id）→ 仍可讀（分享連結功能保留）
5. extension 的報告列表功能正常（如果有用到）

---

### ✅ DONE-132 — Claude Code 回報（2026-07-03）

**完成內容**（只改 `server/src/index.ts`，extension/MCP/getReport 不動，符合 §5）：

1. **§1 listReports 加認證 + user 過濾**——簽名由 `(url, env)` 改 `(request, env)`（call site 同步改傳 `request`）；開頭 `getAuthUserId(request, env)`，未登入回 **401「請先登入」**；查詢加 `.eq('user_id', userId)` **只回自己的報告**。保留原欄位選擇、`limit`(≤50)、`url` keyword 過濾（皆已 scope 到本人）與 `{ reports: [...] }` 回應形狀。
2. **§2 getReport 不動**——單筆 `GET /api/reports/:id` 維持免登入（分享連結需要）。report_id 是 `crypto.randomUUID()` 不可猜 + listReports 不再洩漏他人 ID → 等效「只有拿到分享連結的人能看」。
3. **§3/§4 MCP `list_reports` 不動**（PM-134 處理）；extension 無呼叫 GET list（全部 `/api/reports` 都是 POST 上傳），§4 確認通過。

**額外（驗收中發現的真洞，一併補）**：實測 `GET /api/reports` **被 Cloudflare 邊緣快取**（部署後匿名打無 cache-buster 竟回舊全站清單）。對「依 user 過濾的私有端點」，以 URL 為鍵的快取會把 A 的資料跨服給 B——等於繞過本次認證。故新增 `jsonNoStore()`（`Cache-Control: no-store`），listReports 三個 return（401/500/200）全走它，杜絕邊緣/共享快取。加 cache-buster 後行為即正確。

**驗收（線上 curl，`bugezy-api...workers.dev`，皆帶 cache-buster 避開舊快取）**：
- 匿名 → **401「請先登入」** + `Cache-Control: no-store` ✅
- 假 token → **401** ✅
- 舊 base64（不存在用戶）→ **200 `{"reports":[]}`**（只回自己、空）+ no-store ✅
- `GET /api/reports/:id` 隨機 id → **404 report not found**（非 401，分享連結功能保留）✅

**部署**：`npx wrangler deploy` 成功，Version `8fd5cca0-7027-4521-ac0c-5a70c915b647`。tsc `--noEmit` 通過。

**備註**：部署後舊快取清單會隨 no-store + 自然過期消失；真實登入用戶（有效 session token）打此端點只會看到自己的報告。PM-134 再補 MCP `list_reports` 認證。

**DONE-132**

---

### 🟡 PM-133：P0-2+P0-3+P1-4 修復 — 認證信任鏈重構（帳號接管根因）

**問題（Fable 5 三合一）**：
- **P0-2**：`createSession` 憑客戶端傳的 user_id 直接發正式 token → 知道任一 UUID 即可帳號接管
- **P0-3**：舊 base64 fallback（getUserIdFromHeader）仍啟用，`Bearer base64("uuid:ts")` 永久可偽造
- **P1-4**：Google 登入用 access token 沒驗 aud，其他 App 的 token 可重放冒充

**根本修法**：改為「server 端驗證 Google token 的 audience → 從 Google 回應推導 user_id → 發 DB token」，全程不信任客戶端 user_id，並移除 base64 fallback。

**⚠ 前提**：擴充尚未上架（送審的是舊版）、FOX 幾乎是唯一使用者 → **可乾淨切換，不留 fallback**。

---

#### §1 Server：新增 GOOGLE_CLIENT_ID 設定

Google client_id 是**公開資訊**（非機密），可寫 wrangler.toml [vars] 或 secret。先讀 `extension/manifest.json` 的 `oauth2.client_id` 取得確切值（`610395663887-...apps.googleusercontent.com`），設為：

```toml
# wrangler.toml [vars]
GOOGLE_CLIENT_ID = "610395663887-....apps.googleusercontent.com"
```

Env interface 加 `GOOGLE_CLIENT_ID: string;`

---

#### §2 Server：新增 Google token 驗證函式

```ts
/** 驗證 Google access token：確認 audience 是 BugEzy、未過期，回傳 { sub, email } 或 null。 */
async function verifyGoogleToken(accessToken: string, env: Env): Promise<{ sub: string; email: string } | null> {
  try {
    // 1. tokeninfo 驗證 audience（防其他 App 的 token 重放）
    const tokenInfoRes = await fetch(
      `https://www.googleapis.com/oauth2/v3/tokeninfo?access_token=${encodeURIComponent(accessToken)}`
    );
    if (!tokenInfoRes.ok) return null;
    const tokenInfo = await tokenInfoRes.json() as { aud?: string; azp?: string; expires_in?: string };

    // audience 必須是 BugEzy 的 client_id（aud 或 azp 其一符合）
    const aud = tokenInfo.aud || tokenInfo.azp || '';
    if (aud !== env.GOOGLE_CLIENT_ID) {
      console.error('Google token audience mismatch');
      return null;
    }

    // 2. 取使用者資訊（sub = Google 唯一 ID，作為 user_id）
    const userInfoRes = await fetch('https://www.googleapis.com/oauth2/v2/userinfo', {
      headers: { Authorization: `Bearer ${accessToken}` },
    });
    if (!userInfoRes.ok) return null;
    const userInfo = await userInfoRes.json() as { id?: string; email?: string };
    if (!userInfo.id || !userInfo.email) return null;

    return { sub: userInfo.id, email: userInfo.email };
  } catch (e) {
    console.error('verifyGoogleToken failed:', e);
    return null;
  }
}
```

---

#### §3 Server：重寫 createSession（不信任客戶端 user_id）

```ts
async function createSession(request: Request, env: Env): Promise<Response> {
  const body = (await request.json().catch(() => null)) as
    | { google_token?: string; name?: string }
    | null;
  if (!body?.google_token) {
    return json({ error: 'missing google_token' }, 400);
  }

  // 關鍵：user_id 由 server 從已驗證的 Google token 推導，絕不信任客戶端傳的 user_id
  const verified = await verifyGoogleToken(body.google_token, env);
  if (!verified) {
    return json({ error: 'Google token 驗證失敗' }, 401);
  }

  const userId = verified.sub;
  const email = verified.email;

  // user 不存在則建立
  const { data: user } = await supa(env)
    .from('users').select('user_id').eq('user_id', userId).maybeSingle();
  if (!user) {
    await supa(env).from('users')
      .insert({ user_id: userId, email, name: body.name || '', plan: 'free' });
  }

  const sessionToken = `${crypto.randomUUID()}-${crypto.randomUUID()}`;
  await supa(env).from('sessions').insert({
    session_token: sessionToken,
    user_id: userId,
    expires_at: new Date(Date.now() + 90 * 24 * 60 * 60 * 1000).toISOString(),
  });

  return json({ session_token: sessionToken, user_id: userId, email });
}
```

---

#### §4 Server：移除 base64 fallback

- **刪除** `getUserIdFromHeader`（:143-152）
- **改寫** `getAuthUserId`（:180-184）→ 只用 verifySession，不再 fallback：

```ts
async function getAuthUserId(request: Request, env: Env): Promise<string | null> {
  return await verifySession(request, env);
}
```

- 搜尋 `googleAuth`（:2583 附近）——如果它還在簽發 base64 token，一併移除或改為呼叫新流程。如果 `/api/auth/google` 端點還存在且發 base64，刪掉或改導向 createSession。

---

#### §5 Server：移除過渡 GET /checkout

`GET /checkout?user_id=`（P0-2 相關）刪除，只留 `POST /checkout`（需 session）。

---

#### §6 Extension：登入改送 Google token

`popup.ts` / `background.ts` 的登入流程：

現在應該是 `chrome.identity.getAuthToken` 拿 access token → 打 userinfo 拿 user_id → 送給 createSession。

改為：拿到 access token 後**直接送給 server**，讓 server 驗證 + 推導 user_id：

```ts
// 取得 Google access token
const { token } = await chrome.identity.getAuthToken({ interactive: true });

// 送給 server 換 session（server 會驗證 token audience + 推導 user_id）
const res = await fetch(`${API_BASE}/api/auth/session`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ google_token: token, name: userName }),
});
const data = await res.json();
if (data.session_token) {
  await chrome.storage.local.set({
    'bugezy:session-token': data.session_token,
    'bugezy:user': { user_id: data.user_id, email: data.email, name: userName },
  });
}
```

extension 不再自己決定 user_id，由 server 回傳。

---

#### §7 Extension：移除舊 base64 token 產生

搜尋 extension 所有產生 base64 token 的地方（`btoa`），移除。auth.ts 的 getAuthHeaders 只用新的 session token（PM-129 已大致做到，確認沒有 base64 fallback）。

---

#### §8 不改的東西

- verifySession 本身：不動（設計正確）
- sessions 表：不動
- ECPay callback（用 CustomField1）：不動（PM-135 另修冪等）
- MCP：不動（PM-134 處理）

#### 前置：FOX 確認 manifest client_id

Claude Code 讀 `extension/manifest.json` 的 oauth2.client_id，填入 wrangler.toml。

#### Deploy + Build

```
cd server && npx wrangler deploy
cd extension && npm run build
```

#### 驗收

1. reload 擴充 → 登出 → 重新 Google 登入 → 成功取得 session token（server 端驗證 Google token）
2. Supabase sessions 表出現新記錄，user_id = Google sub
3. `POST /api/auth/session` 帶假 google_token → 401
4. `POST /api/auth/session` 帶「其他 App 的」Google token（不同 aud）→ 401（audience 不符）
5. 舊 base64 header（`Bearer base64("uuid:ts")`）→ **不再有效**（fallback 已移除）→ 相關 API 回 401
6. 登入後查 plan、買日票、月費升級全部正常
7. `GET /checkout?user_id=` → 404（過渡端點已移除）

---

### ✅ DONE-133 — Claude Code 回報（2026-07-03）

**完成內容**（server + extension；認證信任鏈整體重構）：

**Server（`wrangler.toml` + `src/index.ts`）**
1. **§1 GOOGLE_CLIENT_ID**——讀 `extension/manifest.json` 的 `oauth2.client_id` = `610395663887-piqtrs4bvbj738iimbk3bre2ij78rvdl.apps.googleusercontent.com`，寫入 wrangler.toml `[vars]`（公開資訊非機密）+ `Env.GOOGLE_CLIENT_ID`。
2. **§2 `verifyGoogleToken`**——`oauth2/v3/tokeninfo` 驗 `aud||azp === GOOGLE_CLIENT_ID`（防其他 App token 重放，P1-4）→ `oauth2/v2/userinfo` 取 `id`(=sub)+`email`，回 `{sub,email}` 或 null。
3. **§3 重寫 `createSession`**——改收 `{google_token, name}`，**不再收 user_id**；`verifyGoogleToken` 推導 `userId=sub`（絕不信任客戶端，P0-2）；user 不存在則建；發雙 UUID token；回 `{session_token, user_id, email}`。
4. **§4 移除 base64 fallback**——刪 `getUserIdFromHeader`；`getAuthUserId` 只 `return verifySession(...)`（P0-3）；刪整個 `googleAuth` 函式 + `POST /api/auth/google` 路由（它發 base64 token + 以 email 查 user，是帳號接管根因）。
5. **§5 移除過渡 `GET /checkout?user_id=`**——只留 `POST /checkout`（需 session）。

**Extension（`popup.ts` + `auth.ts`）**
6. **§6 登入改送 google_token**——`googleLogin(interactive)` 回 access token；新 `fetchGoogleProfile`（client 端取 name/picture 純顯示、非信任邊界）；`exchangeSession` POST `/api/auth/session` 帶 `{google_token, name}` → 回 `{user_id,email,session_token}`；`doLogin` 串起來存 `SESSION_KEY`+`SESSION_TOKEN_KEY`。登入鈕改呼叫 `doLogin(true)`。**刪 `authenticate`（打 /api/auth/google）+ `ensureSessionToken`**。
   - 新增 `refreshSessionSilently()`：既有登入（可能是舊 base64 token）於 popup 開啟時用 `getAuthToken({interactive:false})` 靜默換成有效 DB token（已授權者無感），失敗則等手動重登。
7. **§7 移除 base64**——`auth.ts getAuthHeaders` 只讀 `SESSION_TOKEN_KEY`（刪 SESSION_KEY.session_token fallback）；extension 全域無 `btoa`/`/api/auth/google`/`authenticate` 殘留。

**驗收（線上 curl）**：
- 假 google_token → **401「Google token 驗證失敗」** ✅
- 缺 google_token / 只傳 user_id+email → **400「missing google_token」**（不再接受 user_id，P0-2 修好）✅
- 舊 base64 header → **401 unauthorized**（fallback 已移除，P0-3）✅
- `GET /checkout?user_id=` → **404**（過渡端點移除）✅
- 舊 `POST /api/auth/google` → **404**（已移除）✅
- `POST /checkout` 無 auth → **401** ✅

**建置/部署**：server `wrangler deploy` 成功 Version `3d14c901`（vars 出現 GOOGLE_CLIENT_ID）；extension `npm run build` 成功（popup.js 28.2kb）；兩端 tsc 通過。

**⚠ 待實機驗收（curl 無法測，需真實 Google OAuth）**：驗收 1/2/6——reload 擴充 → 重新登入 → 確認 sessions 表新記錄 `user_id = Google sub` + 查 plan/買日票/月費升級正常。**關鍵**：`chrome.identity` 的 token `aud/azp` 須等於 manifest client_id（=已設的 GOOGLE_CLIENT_ID），login 才會過；此為信任鏈能否運作的唯一未測點。

**備註**：
- **user_id 語意改變**——舊 `googleAuth` 用 Supabase 自動 UUID 當 user_id，新流程用 Google sub。同一 Google 帳號登入後 user_id 不同，**舊報告（綁舊 UUID）會與新 user_id 脫鉤**。因擴充未上架 + FOX 近乎唯一使用者，屬預期的乾淨切換（job §前提）。
- 部署傳播期（deploy 後數秒）曾短暫命中舊 worker（3c 一度回 200 建了一筆 `user_id="any-uuid"` 測試 user+session）——傳播完成後即恢復正確；該筆為無害測試殘留（無報告），FOX 可忽略或日後清。

**DONE-133**

---

### 🟡 PM-134：popup 月費會員狀態顯示 + 取消訂閱入口（UX 缺口）

**問題**：月費（paid）用戶打開 popup 看不到「自己是月費會員」，也找不到取消訂閱的入口。目前 popup 只處理免費（顯示升級按鈕）和日票（顯示倒數）兩種狀態，漏了 paid / cancelled。

**目標**：popup 依 plan 狀態明確顯示會員身份 + 對應操作。

---

#### §1 四種 plan 狀態的 popup 顯示

`/api/user/plan`（getUserPlan）已回傳 plan + day_pass_expires_at + plan_expires_at。popup 依狀態渲染：

| plan 狀態 | 顯示 |
|---|---|
| `free` | 「⚡ 日票 NT$20」+「✨ 月費 NT$80」升級按鈕（現狀） |
| `day_pass`（未過期） | 「⚡ 日票 剩餘 23h」倒數（現狀） |
| `paid` | **「✨ 付費版會員」+「取消訂閱」按鈕**（新增） |
| `cancelled` | **「✨ 付費版（已取消）· 到期日 YYYY-MM-DD」**（不顯示取消按鈕，因已取消）（新增） |

---

#### §2 確認 getUserPlan 回傳欄位

確認 `getUserPlan`（server）有回傳 `plan_expires_at`（cancelled 狀態要顯示到期日）。如果沒有，補上：

```ts
// getUserPlan 回傳
return jsonNoStore({
  plan: u.plan,
  day_pass_expires_at: u.day_pass_expires_at ?? null,
  plan_expires_at: u.plan_expires_at ?? null,  // ← cancelled 顯示到期日用
  usage: { ... },
});
```

**順便修快取問題**：getUserPlan 也改用 `jsonNoStore`（跟 PM-132 listReports 一樣，避免私有 plan 狀態被邊緣快取）。這正是 FOX 剛剛「看不到狀態」可能的原因之一。

---

#### §3 popup.ts：paid 狀態 UI

```ts
function showPaidStatus() {
  // 隱藏升級按鈕區
  if (upgradeSection) upgradeSection.style.display = 'none';
  if (dayPassStatus) dayPassStatus.style.display = 'none';

  // 顯示會員狀態列
  paidStatus.style.display = 'flex';
  paidStatus.innerHTML = `
    <span class="paid-badge">✨ 付費版會員</span>
    <button id="cancel-sub-btn" class="cancel-sub-btn">取消訂閱</button>
  `;

  document.getElementById('cancel-sub-btn')?.addEventListener('click', handleCancelSub);
}
```

---

#### §4 popup.ts：cancelled 狀態 UI

```ts
function showCancelledStatus(expiresAt: string) {
  if (upgradeSection) upgradeSection.style.display = 'none';
  paidStatus.style.display = 'flex';
  const dateStr = new Date(expiresAt).toLocaleDateString('zh-TW');
  paidStatus.innerHTML = `
    <span class="paid-badge cancelled">✨ 付費版（已取消）</span>
    <span class="expire-note">${dateStr} 到期</span>
  `;
}
```

到期後綠界 period-callback 會自動降回 free（PM-72b 已實作），到期後 popup 就會回到免費狀態。

---

#### §5 popup.ts：取消訂閱處理（含二次確認）

```ts
async function handleCancelSub() {
  if (!confirm('確定要取消月費訂閱嗎？\n取消後到期日前仍可使用付費功能，到期後自動降回免費版。')) {
    return;
  }
  const headers = await getAuthHeaders();
  const res = await fetch(`${API_BASE}/api/user/cancel`, { method: 'POST', headers });
  const data = await res.json();
  if (res.ok) {
    alert('已取消訂閱。到期日前仍可使用付費功能。');
    location.reload(); // 刷新 popup 狀態
  } else {
    alert(data.error || '取消失敗，請稍後再試');
  }
}
```

`/api/user/cancel`（ecpayCancel）PM-73 已實作，會呼叫綠界停止定期定額 + 標記 cancelled。

---

#### §6 CSS

```css
.paid-status {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  margin: 8px 0;
  padding: 10px 12px;
  background: linear-gradient(135deg, rgba(124,58,237,0.15), rgba(59,130,246,0.1));
  border: 1px solid #7c3aed;
  border-radius: 10px;
}
.paid-badge { color: #c4b5fd; font-size: 14px; font-weight: 700; }
.paid-badge.cancelled { color: #9aa3b2; }
.cancel-sub-btn {
  background: transparent;
  color: #888;
  border: 1px solid #444;
  border-radius: 8px;
  padding: 6px 12px;
  font-size: 12px;
  cursor: pointer;
}
.cancel-sub-btn:hover { border-color: #f85149; color: #f85149; }
.expire-note { color: #9aa3b2; font-size: 12px; }
```

---

#### §7 popup.html：新增 DOM

```html
<div id="paid-status" class="paid-status" style="display:none;"></div>
```

放在升級按鈕區 / 日票倒數區同一位置。

---

#### §8 不改的東西

- server ecpayCancel（/api/user/cancel）：不動（PM-73 已實作，只確認可用）
- 免費 / 日票的顯示邏輯：不動
- 認證：不動

#### Build + Deploy

```
cd server && npx wrangler deploy   # 若有改 getUserPlan（§2 加 no-store + plan_expires_at）
cd extension && npm run build
```

#### 驗收

1. reload → 你（paid）→ popup 顯示「✨ 付費版會員」+「取消訂閱」按鈕
2. 按取消訂閱 → 二次確認 → 確認後呼叫 /api/user/cancel → 變 cancelled 狀態
3. cancelled 狀態 → 顯示「✨ 付費版（已取消）· X 到期」無取消按鈕
4. （測完記得把自己改回 paid，或重訂）
5. getUserPlan 回應帶 `Cache-Control: no-store`

---

### ✅ DONE-134 — Claude Code 回報（2026-07-03）

**先確認現況**：popup 的 paid / cancelled 狀態 UI **早在 PM-73/75/111 已實作完成**（非本卡新增）：
- `paidBadge`（付費版徽章 + `cancelSubBtn` 取消訂閱 link）、`cancelledBadge`（「已取消訂閱，可用到 <expiresDate>」+ `resubBtn` 重新訂閱），`loadPlan` 依 `plan.plan` 四態互斥渲染。
- `cancelSubBtn` handler 已含**二次確認 `confirm()` → POST `/api/user/cancel` → `loadPlan()` 刷新**（③ 已就緒）。

→ 故 ②③ 已存在。paid 用戶「看不到狀態」的**真因是 server `getUserPlan` 用 `json()` 被 Cloudflare 邊緣快取**（回舊/他人狀態）。本卡核心即修此。

**完成內容**：

**Server（`src/index.ts` getUserPlan）**
1. **§1/§2 全 return 改 `jsonNoStore`**（401/404/500/success 皆是）——私有 plan 狀態不再被邊緣快取（跟 PM-132 listReports 同手法）。
2. **回傳補 `plan_expires_at`**——與既有 `expires_at` 同值並存（`expires_at` 保留相容舊 popup，`plan_expires_at` 滿足本卡需求 + cancelled 顯示到期日）。

**Extension（`popup.html` + `popup.ts`，小幅打磨）**
3. paid 徽章文字「✨ 付費版」→「✨ 付費版會員」（對齊 ② 語意）。
4. `PlanInfo` 加 `plan_expires_at`；cancelled 到期日改讀 `plan.plan_expires_at ?? plan.expires_at`（兩欄位皆相容）。

**驗收**：
- `getUserPlan` 回應帶 **`Cache-Control: no-store`**（匿名 401 已驗；success 走同 `jsonNoStore` 故一致）✅
- 匿名 → 401、舊 base64 → 401（PM-133 後 fallback 已移除，符合預期）✅
- paid/cancelled 視覺 + 取消訂閱二次確認 → 程式碼就緒（`paidBadge`/`cancelledBadge`/`cancelSubBtn`），需實機驗收 1~4。

**建置/部署**：server `wrangler deploy` Version `7bf26d6f`；extension `npm run build` ✅；兩端 tsc 通過。

**⚠ 提醒**：PM-133 後 user_id 改用 Google sub，**FOX 舊 paid 狀態綁在舊 UUID row**——新登入的 Google-sub user 預設 free。若要實機看到 paid 狀態，需把新 user_id 的 `plan` 設為 paid（或用綠界重新走一次月費）。此為 PM-133 乾淨切換的已知後果，非本卡程式問題。

**DONE-134**

---
