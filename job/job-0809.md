# job-0809.md — BugEzy Day 37

> 接續 job-0808.md（PM-271）

---

### 🟡 PM-272：官網用戶心得頁 + 首頁入口

**目標**：新增 `/testimonials` 獨立頁面展示用戶心得（文字 + YouTube 影片嵌入），首頁加入口連結。資料手動維護在程式碼中。

---

#### 1. 新增 testimonialsPage 函式（`server/src/index.ts`）

仿照 `homePage` / `faqPage` 的結構，新增 `function testimonialsPage(lang: PageLang): string`。

**心得資料結構**（維護在程式碼中）：

```typescript
const TESTIMONIALS = [
  {
    name: 'BugEzy 團隊',
    text: { zh: '歡迎分享你的 debug 體驗！', en: 'Share your debug experience!' },
    videoUrl: null,  // 純文字
  },
  // FOX 日後新增更多
];
```

**YouTube 嵌入方式**：

```typescript
// 從 YouTube URL 提取 video ID
function youtubeEmbed(url: string): string {
  const id = url.match(/(?:v=|youtu\.be\/)([a-zA-Z0-9_-]{11})/)?.[1];
  if (!id) return '';
  return `<div style="position:relative;padding-bottom:56.25%;height:0;overflow:hidden;border-radius:12px;margin-bottom:16px;">
    <iframe src="https://www.youtube-nocookie.com/embed/${id}" 
      style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;" 
      allow="accelerometer;autoplay;clipboard-write;encrypted-media;gyroscope;picture-in-picture" 
      allowfullscreen></iframe>
  </div>`;
}
```

用 `youtube-nocookie.com` 保護隱私。

**有影片的心得**：先嵌入播放器 → 下方文字引言 + 署名
**純文字心得**：引號卡片 + 署名

#### 2. 路由註冊

```typescript
if (request.method === 'GET' && path === '/testimonials') {
  const res = html(testimonialsPage(getLang(request)));
  res.headers.set('Cache-Control', 'no-store');
  return res;
}
```

#### 3. 首頁入口

在 `homePage` 底部（footer 前）加入：

```html
<section style="text-align:center;padding:40px 20px;">
  <p style="font-size:18px;color:#c9c9d6;">
    💬 <a href="/testimonials">看看其他開發者怎麼說 →</a>
  </p>
</section>
```

#### 4. 頁尾的行動呼籲

心得頁底部加 CTA：

```html
<section class="cta-feedback">
  <h3>📢 分享你的使用體驗</h3>
  <p>拍一段 debug 影片分享 → 免費 30 天</p>
  <p>寫 50 字使用心得 → 免費 10 天</p>
  <p>寄到 <a href="mailto:fox100039@gmail.com">fox100039@gmail.com</a>，附上你的 BugEzy 帳號 email</p>
</section>
```

#### 5. SEO + 多語系

- canonical + hreflang 標籤（照其他頁面做法）
- `<title>` + `<meta description>`
- 所有文字用 `t()` 處理（zh/en/zh-CN）

---

**驗收條件**：
1. `/testimonials` 能開啟，深色主題與官網一致
2. 至少有 1 則預設心得（佔位用）
3. YouTube URL → 正確嵌入播放器（16:9、圓角）
4. 純文字心得用引號卡片顯示
5. 底部有 CTA 區塊（寄信 + 免費天數說明）
6. 首頁有「💬 看看其他開發者怎麼說 →」入口
7. 中/英切換正常
8. `tsc --noEmit` 通過
9. `wrangler deploy` 成功

完成後在下方寫 `🔵 DONE-272`。

---

### 🔵 DONE-272：`/testimonials` 上線，**並修掉卡片程式碼會讓影片完全無法顯示的 CSP 問題**

**改動檔案**：`server/src/index.ts`（主要）、`extension/src/t2s.ts`（補 1 個繁簡對照字，見下）。`tsc --noEmit` 零錯誤，deploy `fa5ca8c5`。

---

#### 🔴 卡片的 iframe 會被 CSP 直接擋掉（畫面只剩空白框）

本站全站注入 CSP，其中有 `default-src 'self'` 但**沒有 `frame-src`**。CSP 規則裡 `frame-src` 未指定時會**回退到 `default-src`**，也就是 `'self'` —— 於是 `https://www.youtube-nocookie.com/embed/...` 的 iframe 會被瀏覽器擋下，影片區只會留一個黑色空框，且錯誤只出現在 console，很容易誤以為是 YouTube 的問題。

**修法**：`CSP_VALUE` 加 `frame-src https://www.youtube-nocookie.com;`。
- 只放行 `youtube-nocookie`（不放 `youtube.com`），維持卡片的隱私考量。
- **不影響 `frame-ancestors 'none'`** —— 那條管的是「誰可以嵌入我們」（防點擊劫持，PM-219），這條管的是「我們可以嵌入誰」，兩者互不相干。

已線上驗證回應標頭確實帶 `frame-src https://www.youtube-nocookie.com;`。

---

#### 🔴 自查抓到：zh-CN 版的新文案沒有轉成簡體

PM-232 的 `T2S_CHARS` 是**依當時站上文案產生的 414 字子集**，不是完整繁簡表。我新增的文案帶進了表裡沒有的字——實測 zh-CN 版顯示「**歡**迎分享…」（其餘如 體驗→体验、寫→写 都正常），只有這個字漏轉。

用 opencc 掃過本卡新增的**全部**繁中文案（頁面 + 首頁入口 + 心得資料），確認只缺 1 字，已補 `"歡":"欢"`。
**server 與 extension 兩份 T2S_CHARS 原本內容完全相同**（各 414 字，已用程式比對確認），所以兩邊一起補到 415 字並再次驗證仍相同——避免留下「兩份表不一致」這種日後極難察覺的坑。

> 這也代表：**日後在官網加中文文案，都要順手用 opencc 檢查有無漏字**，否則簡體版會夾雜繁體字。

---

#### 實作內容

- **`testimonialsPage(lang)`**（仿 `blogListPage` 結構、共用 `BLOG_CSS` + 新增 `TESTIMONIALS_CSS`）：`TESTIMONIALS` 陣列（`name` / `text{zh,en}` / `videoUrl`）+ 引號卡片（`.t-quote::before` 用 CSS `\201C` 大引號）+ 署名。
- **`youtubeEmbed(url)`**：16:9 響應式（`padding-bottom:56.25%`）+ 圓角 12px + `youtube-nocookie`。**比卡片多支援 `/shorts/` 與 `/embed/` 兩種連結形式**——FOX 很可能直接貼手機分享出來的 shorts 連結，只認 `v=`/`youtu.be/` 會靜默失效。另加 `loading="lazy"`（影片多了不拖慢首屏）與 `title`（無障礙）；取不到 11 碼 ID 時回空字串，該則心得只顯示文字而不會壞版。
- **路由** `GET /testimonials` + `Cache-Control: no-store`（與其他多語頁一致，防邊緣快取把某語言的版本誤送給其他語言）。
- **首頁入口**（footer 前）+ **CTA 區塊**（影片 30 天 / 心得 10 天 / 寄信）。
- **SEO**：canonical + hreflang（`LANGS_3` = zh / zh-CN / en，對應卡片 §5 指定的三語）+ title/description + og。
- **sitemap 補 `/testimonials`**（卡片未提）：既然做了 hreflang/canonical，卻不進 sitemap 會與站上其他頁的做法不一致，PM-264 已把 sitemap 建立為所有頁面的單一事實來源。

---

#### 驗證（皆為線上實測）

| # | 驗收 | 結果 |
|---|---|---|
| 1 | `/testimonials` 可開啟、深色主題一致 | ✅ HTTP 200、`no-store`、共用 `BLOG_CSS` |
| 2 | 至少 1 則預設心得 | ✅ 1 則（`t-item`/`t-quote`/`t-name` 各 1） |
| 3 | YouTube URL → 正確嵌入（16:9、圓角） | ✅ **暫時加一則含影片的心得實測後移除**：產出正確 iframe（`youtube-nocookie/embed/<id>`）、`.t-video` = `padding-bottom:56.25%` + `border-radius:12px`；embed URL 實打回 200；純文字那則確認**沒有**多出 iframe。另用 8 組 URL 測 `youtubeEmbed` 取 ID（watch / youtu.be / 帶 `?si=` / shorts / embed / 多參數 / 非 YouTube / 長度不足）**8/8 通過** |
| 4 | 純文字用引號卡片 | ✅ |
| 5 | 底部 CTA（寄信 + 天數） | ✅ 30 天 / 10 天 / `mailto:fox100039@gmail.com` |
| 6 | 首頁有「💬 看看其他開發者怎麼說 →」 | ✅ 中英皆有 |
| 7 | 中/英切換正常 | ✅ zh→`zh-Hant`「用戶心得」／en→`en`「Testimonials」／zh-CN→`zh-Hans`「用户心得」且引言**全簡體**（修完後） |
| 8 | `tsc --noEmit` | ✅ server + extension 皆過 |
| 9 | `wrangler deploy` | ✅ `fa5ca8c5`（最終線上已確認暫時測試資料已移除） |

---

### 🔴 FOX 待辦
1. **新增心得**：編輯 `server/src/index.ts` 的 `TESTIMONIALS` 陣列（上方註解已附含影片的範例格式），`npx wrangler deploy` 即生效。
2. **extension 重上架時包含這次的 t2s 補字**（`dist/` 已 build；本來就在待重上架清單中，不需為此單獨上架）。


---

### 🟡 PM-273：票券錢包 UI 折疊（收合/展開）

**問題**：票券區塊佔太多空間，popup 太長。

**修法**（只動 `extension/src/popup.html` + `popup.ts`）：

把票券區塊（使用中 + 庫存票券）包在一個可折疊的容器裡：

```html
<div id="ticketSection">
  <div id="ticketToggle" style="cursor:pointer;display:flex;align-items:center;gap:6px;">
    <span id="ticketArrow">▶</span>
    <span>🎫 票券錢包</span>
    <span id="ticketBadge" style="...">3</span>  <!-- 庫存數量 badge -->
  </div>
  <div id="ticketBody" style="display:none;">
    <!-- 現有的 activeTicket + savedTickets 內容搬到這裡 -->
  </div>
</div>
```

**行為**：
- 預設**收合**（`display:none`），只顯示「▶ 🎫 票券錢包 (3)」一行
- 有使用中的票券時，收合狀態下也簡短顯示「🟢 BUZZ100 剩 30 天」
- 點擊 → 展開（▶ 變 ▼），顯示完整票券資訊
- 再點 → 收合
- 展開/收合狀態存 `localStorage`，下次開 popup 記住

**收合時的樣子**：
```
▶ 🎫 票券錢包  🟢 BUZZ100 剩30天  庫存3
```

**展開時的樣子**：
```
▼ 🎫 票券錢包
  🟢 免費體驗中（BUZZ100）
     到期日：2026/09/07  剩 30 天
  
  💾 庫存票券（3）
  🎫 FIRSTMONTH — 1 個月   [啟用]
  🎫 NTFYTEST — 1 個月     [啟用]
  🎫 DISCORDTEST — 1 個月  [啟用]
```

**輸入活動代碼欄位不折疊**——它要隨時可見，用戶才知道去哪裡輸入代碼。

---

**驗收條件**：
1. 預設收合，只顯示一行摘要
2. 點擊展開/收合正確
3. ▶ / ▼ 箭頭跟著切換
4. 展開/收合狀態存 localStorage
5. 收合時仍能看到使用中票券的簡短資訊
6. 輸入代碼欄位不受折疊影響
7. `tsc --noEmit` 通過

完成後在下方寫 `🔵 DONE-273`。

---

### 🔵 DONE-273：票券錢包可折疊，預設收合

**改動檔案**：`extension/src/popup.html` + `popup.ts` + `i18n.ts`（新增 2 個 key × 5 語）。`tsc --noEmit` 零錯誤、`npm run build` 成功（`dist/` 已更新，待重上架）。

---

#### 結構（`promoInput` / `redeemResult` 刻意留在折疊區外）

```
#ticketWallet
├── #ticketToggle   ▶ 🎫 票券錢包  🟢 BUZZ100 剩 30 天   庫存 3   ← 可點擊
├── #promoInput     [輸入活動代碼] [兌換]                        ← 永遠可見
├── #redeemResult   兌換結果 + 立即啟用/儲存備用                   ← 永遠可見
└── #ticketBody     使用中票券 + 到期提醒 + 庫存清單               ← 折疊
```

已用程式比對 DOM 巢狀確認 `promoInput`/`redeemResult` 是 `ticketBody` 的**兄弟節點**而非子節點（驗收 #6）。

**行為**：預設收合（`localStorage` 無值 → `▶`）；點擊 `▶`↔`▼` 並存 `localStorage['bugezy:ticket-open']`；收合時標題列顯示「🟢 代碼 剩 N 天」摘要 + 「庫存 N」badge，**展開後這兩項自動收起**（下方已有完整的「使用中票券」與「庫存票券（N）」，避免同一資訊出現兩次）。

---

#### 三個卡片沒寫、但不處理會出問題的地方

1. **完全沒有票券時整條折疊列隱藏**。若照卡片一律顯示，沒兌換過任何代碼的用戶會看到「▶ 🎫 票券錢包」，點開來卻是空的。現在這種情況只留「輸入活動代碼」欄位。
2. **把折疊狀態的宣告搬到 `renderTicketWallet` 之前**。`renderTicketWallet()` 會呼叫 `updateTicketFold()`，而後者讀 `let ticketOpen`——原本宣告在後方，雖然實際兩個呼叫點都在 callback 裡（模組跑完才觸發）不會炸，但這是靠時序僥倖。搬到前面直接消除順序依賴（PM-267 踩過同類的 TDZ 問題）。
3. **摘要文字用 `flex:1` + `text-overflow: ellipsis`**。代碼名稱可能很長，不設限會把右側的庫存 badge 擠出可視範圍。

#### 用 `localStorage` 而非 PM-122 accordion 的 `chrome.storage.local`

卡片指定 `localStorage`，這裡也確實比較適合：`chrome.storage.local` 是**非同步**的，popup 開啟瞬間會先套預設值、拿到值後再改，展開狀態會**閃一下**；`localStorage` 同步讀取沒這問題。（PM-122 的進階設定 accordion 仍是 `chrome.storage.local`，本卡不動它。）

#### i18n
新增 `promo_wallet_title`（🎫 票券錢包）與 `promo_stock`（庫存 {n}），皆補齊 zh/en/ja/ko/vi 五語；zh-CN 由 `toSimplified` 自動處理。標題列用 `data-i18n`，摘要/badge 在 `renderTicketWallet` 內依當前語言重繪（語言切換時 `applyTranslations` 會呼叫它）。

---

#### 驗證

- ✅ `tsc --noEmit` 通過、`npm run build` 成功
- ✅ **折疊狀態機測試 8/8 通過** —— 沒有 DOM 模擬套件（也不值得為此加依賴），改用最小 DOM stub **直接執行從 `popup.ts` 抽出的真實函式原文**（不是另抄一份，抽取失敗會直接報錯），涵蓋：預設收合、點擊展開、再點收合、`localStorage` 記住展開、記住收合、收合時摘要仍在、無使用中票券只顯示 badge、完全無票券整列隱藏
- ✅ `popup.ts` 取用的**所有**元素 id 都存在於 `popup.html`（含本次新增 5 個）
- ✅ `.hidden { display: none !important }` 確認會蓋過 `.ticket-toggle { display: flex }`（兩者同為單一 class 選擇器，若無 `!important` 會因後定義而失效）

---

**驗收狀態**：
1. ✅ 預設收合，只顯示一行摘要
2. ✅ 點擊展開/收合正確
3. ✅ ▶ / ▼ 箭頭跟著切換
4. ✅ 狀態存 `localStorage`
5. ✅ 收合時仍看得到使用中票券資訊
6. ✅ 輸入代碼欄位不受折疊影響（DOM 結構已驗證）
7. ✅ `tsc --noEmit` 通過

> 1~6 為邏輯層已驗證；**實際視覺呈現待 FOX 在 Chrome 重新載入擴充後點一下確認**（與先前 extension 卡片一致）。


---

### 🟡 PM-274：票券啟用二次確認

**問題**：誤觸「啟用」就直接開始倒數，不可逆。

**修法**（只動 `extension/src/popup.ts` + `popup.html`）：

點「啟用」後彈出確認提示，而非直接呼叫 API：

```
用戶點 [啟用]
  → 顯示確認區塊：
    ┌──────────────────────────────────┐
    │ ⚠ 確認啟用？                      │
    │ FIRSTMONTH（1 個月）               │
    │ 啟用後立即開始倒數，無法取消        │
    │                                    │
    │  [✅ 確認啟用]    [取消]            │
    └──────────────────────────────────┘
  → 點「確認啟用」→ 呼叫 API → 開始倒數
  → 點「取消」→ 回到原畫面
```

**兌換成功後的「🚀 立即啟用」也要加確認**——同樣的流程。

**實作方式**：
- 不用 `confirm()` 原生彈窗（太醜）
- 在 popup.html 加一個隱藏的確認區塊 `#ticketConfirm`
- 點啟用時顯示確認區塊 + 帶入票券資訊
- 確認或取消後隱藏

**i18n**：
```typescript
'promo_confirm_title': { zh: '⚠ 確認啟用？', en: '⚠ Confirm activation?' },
'promo_confirm_desc': { zh: '啟用後立即開始倒數，無法取消', en: 'Timer starts immediately and cannot be undone' },
'promo_confirm_btn': { zh: '✅ 確認啟用', en: '✅ Confirm' },
'promo_cancel_btn': { zh: '取消', en: 'Cancel' },
```

---

**驗收條件**：
1. 庫存票券點「啟用」→ 顯示確認提示（含票券名稱 + 天數）
2. 點「確認啟用」→ 呼叫 API → 啟用成功
3. 點「取消」→ 回到原畫面，不呼叫 API
4. 兌換成功後的「🚀 立即啟用」也有確認
5. i18n 中英正確
6. `tsc --noEmit` 通過

完成後在下方寫 `🔵 DONE-274`。

---

### 🔵 DONE-274：啟用二次確認，兩條啟用路徑都收斂到同一個確認關卡

**改動檔案**：`extension/src/popup.html` + `popup.ts` + `i18n.ts`（4 個 key × 5 語）。`tsc --noEmit` 零錯誤、`npm run build` 成功（`dist/` 已更新，待重上架）。

---

#### 關鍵：`activateTicket()` 現在**只剩一個呼叫點**

原本有兩條路徑會直接打啟用 API（庫存票的「啟用」鈕、兌換後的「🚀 立即啟用」）。改法不是各自加確認，而是**兩條都改成呼叫 `askActivate()`**，真正打 API 的 `activateTicket()` 只由「✅ 確認啟用」觸發：

```
庫存票 [啟用] ─┐
                ├─→ askActivate()（只顯示確認框，不打 API）
[🚀 立即啟用] ─┘         │
                          └─ [✅ 確認啟用] ─→ activateTicket() ─→ POST /api/promo/activate
                          └─ [取消] ─────────→ 關閉，什麼都不做
```

已用 grep 確認 `activateTicket(` 全檔只剩**確認鈕**這一個呼叫點——日後若有人新增第三條啟用入口，也不可能繞過確認。

---

#### 卡片沒寫、但會造成實際問題的四點

1. **確認框放在折疊區外面**。庫存票的「啟用」鈕在 PM-273 的折疊區**內**，兌換後的「立即啟用」在折疊區**外**；確認框若放進折疊區，從「立即啟用」觸發時會看不到。放外面兩條路徑都看得見。
2. **`scrollIntoView({ block: 'nearest' })`**。確認框在觸發鈕上方，若 popup 已捲動，按下去可能整個在畫面外——使用者會以為按鈕壞了而狂點。
3. **確認後先關閉再啟用**（`closeActivateConfirm()` 在 `activateTicket()` 之前）。否則連點兩下「確認啟用」會送出兩個請求。已測：第二次點擊不再送出；「取消後誤按確認」也不會啟用。
4. **重新兌換時清掉殘留的確認框**。否則兌換 B 代碼時，畫面還停在 A 票券的確認框，按下去會啟用錯的票。

#### `pendingTicketId` → `pendingTicket`
確認框要顯示「代碼（天數）」，但兌換流程原本只留了 `ticket_id`（`code` 來自輸入框、兌換成功後就被清空）。改存 `{ id, code, days }`，在清空輸入框前就抓下來。

#### i18n
`promo_confirm_title` / `promo_confirm_desc` / `promo_confirm_btn` / `promo_cancel_btn`，皆補齊 zh/en/ja/ko/vi 五語（dict 型別要求）。
**依 PM-272 的教訓，先用 opencc 檢查新增繁中文案在 T2S 表中有無漏字 → 無漏字**，zh-CN 可正確轉換。

---

#### 驗證

- ✅ `tsc --noEmit` 通過、`npm run build` 成功
- ✅ **確認流程測試 7/7 通過** —— 同 PM-273 做法，用最小 DOM stub **直接執行從 `popup.ts` 抽出的真實程式碼**（抽取失敗會直接報錯），並把 `activateTicket` 換成記錄呼叫的 stub，因此能真正斷言「有沒有打 API」：
  1. 點啟用 → 顯示確認框、帶入「FIRSTMONTH（1 個月）」、**API 呼叫次數 0**
  2. 確認 → API 呼叫 1 次且 `ticket_id` 正確、確認框關閉
  3. 連點第二次 → **不重複送出**
  4. 取消 → **完全沒打 API**、確認框關閉
  5. 取消後誤按確認 → 仍不會啟用
  6/7. 兌換後「立即啟用」路徑走同一關卡，60 天資訊正確、確認後正常啟用
- ✅ `popup.ts` 取用的所有元素 id 都存在於 `popup.html`（含新增 4 個）
- ✅ 4 個 i18n key 五語齊全，且 HTML 皆有對應 `data-i18n`（語言切換會自動套用）

---

**驗收狀態**：
1. ✅ 庫存票「啟用」→ 顯示確認（含票券名稱 + 天數）
2. ✅ 「確認啟用」→ 呼叫 API
3. ✅ 「取消」→ 不呼叫 API
4. ✅ 兌換後「🚀 立即啟用」也有確認
5. ✅ i18n 中英正確（另補日/韓/越）
6. ✅ `tsc --noEmit` 通過

> 邏輯層已驗證；**實際視覺與點擊手感待 FOX 在 Chrome 重新載入擴充後確認**（與先前 extension 卡片一致）。


---

### 🟡 PM-275：付費會員隱藏票券啟用按鈕

**問題**：付費會員啟用票券會白燒免費天數（ECPay 照扣）。

**修法**（只動 `extension/src/popup.ts`）：

在 `renderTicketWallet` 裡判斷 `plan.isPaid`（ECPay 訂閱，不含票券）：

**付費會員看到的庫存票券**：
```
💾 庫存票券（3）— 備用額度
🎫 FIRSTMONTH — 1 個月   ✨ 已是會員
🎫 NTFYTEST — 1 個月     ✨ 已是會員
🎫 DISCORDTEST — 1 個月  ✨ 已是會員
```
- 不顯示「啟用」按鈕
- 改顯示「✨ 已是會員」標籤
- 兌換成功後的「🚀 立即啟用」也隱藏，只保留「💾 儲存備用」

**免費/票券用戶看到的**：（跟現在一樣，有啟用按鈕）
```
🎫 FIRSTMONTH — 1 個月   [啟用]
```

**判斷方式**：
```typescript
// ECPay 訂閱中（不含票券）
const isEcpayPaid = plan.plan === 'paid' || plan.plan === 'cancelled';
// cancelled 表示已取消但還在有效期內，仍算付費中

// 如果 ECPay 付費中 → 隱藏啟用按鈕
// 如果 ECPay 到期了（plan 回到 free/null）→ 顯示啟用按鈕
```

**i18n**：
```typescript
'promo_already_member': { zh: '✨ 已是會員', en: '✨ Member' },
'promo_backup_note': { zh: '備用額度', en: 'Backup credits' },
```

---

**驗收條件**：
1. 付費會員的庫存票券不顯示「啟用」按鈕
2. 付費會員的庫存票券顯示「✨ 已是會員」
3. 付費會員兌換成功後只有「💾 儲存備用」，沒有「🚀 立即啟用」
4. 免費用戶的庫存票券仍有「啟用」按鈕（不受影響）
5. 票券用戶（非 ECPay）的庫存票券仍有「啟用」按鈕
6. `tsc --noEmit` 通過

完成後在下方寫 `🔵 DONE-275`。

---

### 🔵 DONE-275：ECPay 訂閱中隱藏啟用入口，票券用戶不受影響

**改動檔案**：`extension/src/popup.ts`（主要）、`popup.html`（1 條標籤樣式）、`i18n.ts`（2 個 key × 5 語）。`tsc --noEmit` 零錯誤、`npm run build` 成功（`dist/` 已更新，待重上架）。

---

#### ⚠ 這裡有個很容易踩、而且會直接違反驗收 #5 的陷阱

popup 裡已經有一個現成的 `isPaidMember`（PM-189 用來解 JSON 鎖），直覺會想拿來用——**但不能用**。

server 的 `getUserPlan` 是這樣算的：
```ts
const isPaid = isActiveUser(u) || activeTicketRows.length > 0;  // ← 票券也算付費
```
也就是說 `plan.isPaid` **把「持有有效票券」一併算成付費**。若拿它當判斷，**票券用戶的啟用鈕會被一起藏掉**，他們就無法在目前票券到期前先排隊啟用下一張——正是驗收 #5 要擋的情況。

所以照卡片的寫法另開一個旗標，只認 ECPay：
```ts
let isEcpayPaid = false;                                   // 模組級，供 renderTicketWallet / showRedeemMsg 共用
isEcpayPaid = plan.plan === 'paid' || plan.plan === 'cancelled';
```
`cancelled` 算付費中是正確的——已查證 `getUserPlan` 會把**過期的** cancelled 自動降回 `free`，所以看得到 `cancelled` 就代表仍在有效期內。

---

#### 實作內容

| 位置 | ECPay 訂閱中 | 免費 / 票券用戶 |
|---|---|---|
| 庫存清單標題 | `💾 庫存票券（3）— 備用額度` | `💾 庫存票券（3）` |
| 每一列 | `🎫 CODE — 1 個月　✨ 已是會員` | `🎫 CODE — 1 個月　[啟用]` |
| 兌換成功後 | 只有 `💾 儲存備用` | `🚀 立即啟用` + `💾 儲存備用` |

付費會員那條路徑**根本不建立按鈕**（不是建了再 disable），所以沒有任何可點的啟用入口；`showRedeemMsg` 則以 `activateNowBtn.classList.toggle('hidden', isEcpayPaid)` 隱藏立即啟用。

**旗標在 `renderTicketWallet()` 之前設定** —— `loadPlan` 裡順序寫成 `ticketState → isEcpayPaid → renderTicketWallet()`，否則第一次載入會用到上一輪的舊值。

#### 日票（NT$20/24hr）不在本卡範圍
日票用戶的 `plan` 仍是 `free`，因此**仍看得到啟用鈕**。這與卡片指定的判斷式一致；且日票只有 24 小時，就算重疊也僅浪費不到 1 天，反而是藏起來更令人困惑。若你要一併擋，再開一張卡。

#### i18n
`promo_already_member`（✨ 已是會員）、`promo_backup_note`（備用額度），皆補齊 zh/en/ja/ko/vi 五語。依 PM-272 的教訓先用 opencc 驗 T2S 漏字 → **無漏字**。

---

#### 驗證

- ✅ `tsc --noEmit` 通過、`npm run build` 成功
- ✅ **付費閘門測試 9/9 通過** —— 同前兩卡做法，用最小 DOM stub **直接執行從 `popup.ts` 抽出的真實 `renderTicketWallet` 與 `showRedeemMsg`**（抽到的程式碼若不含 PM-275 邏輯會直接報錯），檢查渲染出來的實際 DOM 節點型別：
  1. 付費會員：兩列的第二個節點都是 `span`（**不是 button**）
  2. 付費會員：`class="saved-ticket-member"`、文字為 `promo_already_member`
  3. 付費會員：標題帶「備用額度」
  4. 免費用戶：兩列都是 `button`、文字 `promo_activate`；標題不帶「備用額度」
  5. **票券用戶（有 ACTIVE 票券但非 ECPay）：仍是 `button`** ← 這條就是誤用 `plan.isPaid` 時會失敗的案例
  6. 付費會員兌換後：`activateNowBtn` 被加上 `hidden`，動作區本身仍顯示（儲存備用可用）
  7. 免費用戶兌換後：`activateNowBtn` 未隱藏
- ✅ `popup.ts` 取用的所有元素 id 都存在於 `popup.html`
- ✅ 2 個 i18n key 五語齊全、`.saved-ticket-member` 樣式已加

---

**驗收狀態**：
1. ✅ 付費會員庫存票券不顯示「啟用」按鈕（根本不建立該節點）
2. ✅ 付費會員顯示「✨ 已是會員」
3. ✅ 付費會員兌換後只有「💾 儲存備用」
4. ✅ 免費用戶仍有「啟用」按鈕
5. ✅ 票券用戶（非 ECPay）仍有「啟用」按鈕
6. ✅ `tsc --noEmit` 通過

> 邏輯層已驗證；**實機請用付費帳號與免費帳號各開一次 popup 對照**（需 ECPay 付費帳號才看得到差異）。


---

### 🟡 PM-276：安裝碼功能（Install Code）— 用戶身份驗證用

**目標**：用戶登入後自動產生一組 6 碼安裝碼，綁定 user_id。用於 FB 推廣活動時驗證身份，不可兌換任何東西。

---

#### 1. DB 欄位（Supabase）

在 `users` 表新增一個欄位：

```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS install_code VARCHAR(8) UNIQUE;
```

不另開表，直接加在 users 上——一個用戶一組碼，永不改變。

#### 2. 產碼邏輯（`server/src/index.ts`）

在 `POST /api/auth/session`（登入/建帳）時：

```typescript
// 查用戶有沒有 install_code
if (!user.install_code) {
  // 產一組 BZ-XXXX（4~6 碼英數，大寫，好唸好打）
  const code = 'BZ-' + randomAlphaNum(4);  // 例如 BZ-7K3M
  // 寫入，UNIQUE 約束防碰撞
  await supa(env).from('users')
    .update({ install_code: code })
    .eq('user_id', userId);
}
```

**碰撞處理**：UNIQUE 約束擋住 → 重試一次換新碼。4 碼 = 36^4 = 168 萬組合，現階段不會碰撞。

**固定不變**：同 user_id 登入永遠拿到同一組碼，不管裝幾次。

#### 3. API — 查詢安裝碼歸屬（FOX 用）

```
GET /api/admin/verify-install?code=BZ-7K3M
Header: x-member-id 或 admin 認證

回傳：{
  found: true,
  email: 'fox_fan@gmail.com',
  install_code: 'BZ-7K3M',
  created_at: '2026-08-09T...',
  plan: 'free'
}

找不到：{ found: false }
```

只有 admin 能查（或用 service_role）。

#### 4. API — 用戶查自己的安裝碼

```
GET /api/user/install-code
Header: Authorization

回傳：{ install_code: 'BZ-7K3M' }
```

供 popup 顯示用。

#### 5. popup UI

在票券錢包區塊上方（或折疊標題旁），顯示安裝碼：

```
📋 我的安裝碼：BZ-7K3M  [複製]
```

- 點「複製」→ 複製到剪貼簿 → 按鈕變「✅ 已複製」1.5 秒後恢復
- 收合時也要看得到（不在折疊區內）

#### 6. Discord 通知擴充

新用戶註冊的 Discord 通知加上安裝碼：

```
🆕 新用戶
fox_fan@gmail.com 剛註冊了 BugEzy
安裝碼：BZ-7K3M
```

這樣你在 Discord 搜尋安裝碼也能找到對應用戶。

#### 7. i18n

```typescript
'install_code_label': { zh: '📋 我的安裝碼', en: '📋 My Install Code' },
'install_code_copy': { zh: '複製', en: 'Copy' },
'install_code_copied': { zh: '✅ 已複製', en: '✅ Copied' },
```

---

**驗收條件**：
1. 登入後 users 表有 install_code（格式 BZ-XXXX）
2. 重新登入 → 同一組碼
3. popup 顯示安裝碼 + 複製按鈕
4. 複製功能正常
5. `GET /api/admin/verify-install?code=BZ-XXXX` 回傳正確用戶資訊
6. `GET /api/user/install-code` 回傳自己的碼
7. Discord 新用戶通知包含安裝碼
8. `tsc --noEmit` 通過
9. `wrangler deploy` 成功

**Supabase SQL（FOX 需手動執行）**：
```sql
ALTER TABLE users ADD COLUMN IF NOT EXISTS install_code VARCHAR(8) UNIQUE;
```

完成後在下方寫 `🔵 DONE-276`。

---

### 🔵 DONE-276：安裝碼功能完成，**DB 欄位待 FOX 執行 ALTER**

**改動檔案**：`server/src/index.ts`、`server/install-code.sql`（新增）、`server/schema.sql`、`extension/src/popup.html/.ts`、`i18n.ts`。server deploy `8eead94e`；extension `dist/` 已 build 待重上架。

---

#### 🔴 自查抓到並修掉：差點讓每次登入都推一則假的「🆕 新用戶」

我第一版把 `install_code` 併進 `createSession` 既有的用戶查詢（`.select('user_id, install_code')`）以省一次 SELECT。**但 FOX 還沒跑 ALTER，欄位不存在時 PostgREST 會讓整筆查詢失敗** → `data` 為 `null` → 既有用戶被判成新用戶 → 重複 INSERT（被 PK 擋掉但錯誤被忽略）→ **每次登入都推一則假的「🆕 新用戶」到 Discord**。

改成：`createSession` 的查詢維持原樣只取 `user_id`，安裝碼交給 `ensureInstallCode()` 自己查，且**任何失敗都只回 `null`**（記 log），登入流程完全不受影響。多一次 SELECT 換「欄位還沒建也絕不影響登入」，這筆很划算。

---

#### 🔴 admin 端點的安全設計（卡片只寫「只有 admin 能查」）

`GET /api/admin/verify-install` 會**吐出 email**，等於「知道安裝碼就能拿到信箱」。安裝碼只有約 92 萬組合，是可以暴力枚舉的。專案目前**沒有任何既有的 admin 認證機制**（已全庫搜尋確認），所以我建立了一套最小的：

- 新增 secret `ADMIN_TOKEN`，以 `x-admin-token` 或 `Authorization: Bearer` 傳入。
- **未設 `ADMIN_TOKEN` → 整支端點視同不存在（404）**，而非預設開放。這點很重要：若寫成「沒設就放行」，這次 deploy 當下就會是一個公開的 email 查詢接口。
- token 不符也回 **404**（不是 401/403），避免這支端點的存在本身被探測出來。

已線上實測：未設 secret 時，帶 token 或不帶都回 404。

---

#### 實作內容

**產碼** `randomInstallCode()`：`BZ-` + 4 碼。
- 用 `crypto.getRandomValues` 而非 `Math.random`（這組碼等同用戶識別碼）。
- **字母表排除 0/O/1/I/L**（剩 31 字元）——這組碼會被口頭唸、手打、在 FB 私訊裡貼來貼去，少一次「這是 0 還是 O」的往返，比多幾萬組合更有價值。

**`ensureInstallCode(userId, env)`**：有就回、沒有才產。寫入用 `.update(...).eq(user_id).is('install_code', null)` —— **條件式更新**讓併發請求只有第一個寫得進去，之後回讀確認實際值，所以同一用戶不論同時開幾個 popup 都只會有一組碼。碰撞（UNIQUE `23505`）最多換 5 組重試。

**兩支 API**：`GET /api/user/install-code`（需登入，沒有就當場產）、`GET /api/admin/verify-install?code=`（回 `found/email/install_code/created_at/plan`）。

**舊用戶補發**：卡片只在建帳時產碼，但 session token 有效期 90 天——本功能上線前已登入的用戶要等到 token 過期才拿得到碼。因此 `createSession` 與 `GET /api/user/install-code` **兩處都會補發**，既有用戶一開 popup 就有碼。

**Discord**：`🆕 新用戶` 通知加第二行 `安裝碼：BZ-XXXX`。

**popup**：安裝碼列放在票券錢包**之外**（已用程式驗證位置在 `#ticketWallet` 與 `#ticketBody` 之前），因此**不受 PM-273 折疊影響**，登入後隨時可見。等寬字型顯示（逐字對照好認），複製後按鈕變綠並顯示「✅ 已複製」，1.5 秒還原。查不到碼時整列隱藏，不干擾其他區塊。

**i18n**：3 個 key × 5 語；依 PM-272 教訓先用 opencc 驗 T2S 漏字 → **無漏字**。

---

#### 驗證

| # | 驗收 | 結果 |
|---|---|---|
| 1 | 登入後 users 有 `install_code`（BZ-XXXX） | ⏳ **待 FOX 跑 ALTER**（程式已就緒） |
| 2 | 重新登入 → 同一組碼 | ⏳ 同上（`ensureInstallCode` 只在欄位為空時寫入） |
| 3 | popup 顯示安裝碼 + 複製鈕 | ⏳ 待欄位建立後實機確認（UI 已完成、位置已驗證） |
| 4 | 複製功能正常 | ⏳ 同上（沿用 popup 既有的 `navigator.clipboard.writeText`） |
| 5 | `/api/admin/verify-install` 回正確用戶 | ⏳ 待 ALTER + `ADMIN_TOKEN`；**已驗證未設 secret 時回 404** |
| 6 | `/api/user/install-code` 回自己的碼 | ✅ 端點已上線：未登入 **401**、假 token **401**（回碼本身待欄位建立） |
| 7 | Discord 新用戶通知含安裝碼 | ⏳ 待 ALTER + 新用戶註冊 |
| 8 | `tsc --noEmit` | ✅ server + extension 皆過 |
| 9 | `wrangler deploy` | ✅ `8eead94e`；`/api/version` 200、`/api/promo/wallet` 401、`/testimonials` 200 **無回歸** |

**額外驗證**：
- **產碼函式抽出真實原始碼跑 20,000 次**：格式不符 **0** 筆、31 個字元全數出現、**完全不含 0/O/1/I/L**、相異碼 19,792 組
- `install-code.sql` 與 `schema.sql` 皆通過 **Postgres 官方 parser**（`pglast`）解析（同 PM-265 做法）
- popup 所有 `$()` 取用的 id 都存在；3 個 i18n key 五語齊全

---

### 🔴 FOX 待辦（3 步）

**1. 建欄位**：Supabase Dashboard → SQL Editor → 貼上 `server/install-code.sql` **整份**執行
（含 `ALTER` + UNIQUE 約束 + 三條驗收查詢；可重複執行）

**2. 設 admin token**（要用反查功能才需要）：
```bash
cd server
npx wrangler secret put ADMIN_TOKEN   # 自己取一組夠長的隨機字串
npx wrangler deploy
```
查詢方式：
```bash
curl -H "x-admin-token: <你的 token>" \
  "https://bugezy.dev/api/admin/verify-install?code=BZ-7K3M"
```

**3. 驗收**：重新登入 extension → popup 應出現「📋 我的安裝碼 BZ-XXXX」；再登入一次確認碼不變。
