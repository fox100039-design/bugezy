# job-0820.md — BugEzy Day 47（2026-08-20）

## 今日任務
- MCP30 票券系統（MCP 對接驗證 + 自動發放）

---

### 🟡 PM-398：建立 MCP30 票券 + 兌換時驗證 MCP 使用紀錄

**目標**：建立 MCP30 票券代碼，兌換時自動檢查用戶是否有 MCP 呼叫紀錄，有才發放 30 天體驗券。

---

**步驟 1：建立 MCP30 票券**

Supabase SQL：
```sql
INSERT INTO promo_codes (code, description, duration_days, code_type, max_uses, current_uses, is_active, code_expires_at)
VALUES ('MCP30', 'MCP 對接成功專屬 30 天體驗券', 30, 'promo', NULL, 0, true, NULL);
```

**步驟 2：Workers 兌換邏輯加 MCP 驗證**

位置：`server/src/index.ts`，找到 promo_codes 兌換流程

在兌換 MCP30 時加一道檢查：
```
if (code === 'MCP30') {
  // 查 mcp_usage 表：這個 user 有沒有任何 MCP 呼叫紀錄
  const { data: usage } = await supabase
    .from('mcp_usage')
    .select('id')
    .eq('user_id', userId)
    .limit(1)

  if (!usage || usage.length === 0) {
    return error: '請先完成 MCP 對接，再輸入此代碼。教學請見 bugezy.dev/guide'
  }
  // 有紀錄 → 正常發放
}
```

**關鍵細節**：
- MCP30 跟 BUG10 走同一套兌換流程，只多一道 mcp_usage 檢查
- 同一個用戶 MCP30 只能用一次（既有的重複兌換檢查應該已有）
- BUG10 不受影響（沒有 MCP 檢查）
- mcp_usage 表已有資料（用戶的 AI 呼叫 MCP 工具時就會寫入）

**驗收條件**：
1. 有 MCP 使用紀錄的用戶輸入 MCP30 → 成功拿到 30 天券
2. 沒有 MCP 紀錄的用戶輸入 MCP30 → 被拒 + 友善提示
3. 同一用戶重複輸入 MCP30 → 被拒（已兌換過）
4. BUG10 兌換流程不受影響
5. mcp_usage 表結構確認（確保有 user_id 欄位可查）

完成後在下方寫 `🔵 DONE-398`。

---

🔵 **DONE-398**

**🔴 先講一件會決定這張卡能不能動的事：`mcp_usage` 原本沒有 `user_id` 欄位。**

卡片的兌換邏輯是 `.from('mcp_usage').eq('user_id', userId)`，但 `logMcpUsage()` 寫入時只有 `tool_name` / `tokens_estimated` / `chrome_tokens_estimated` / `report_id` —— **完全沒有使用者維度**。這件事 PM-380 的授權稽核已經標紅過（當時列為「`get_usage_stats` 回的是全站數字」的同一個根因）。

所以照卡片直接實作的話，MCP30 會對**每一個人**都回「查不到 MCP 紀錄」，包括你自己。卡片的驗收條件 5「mcp_usage 表結構確認（確保有 user_id 欄位可查）」問的正是這件事，答案是：**沒有，要先補**。

---

**做了三件事**

**① `logMcpUsage` 開始記 `user_id`**

身分來自 MCP URL 的 `?token=`（`/mcp` 路由每個請求會複製一份帶 token 的 env），用既有的 `verifySessionByToken` 解出 user_id。沒帶 token 的呼叫照舊寫入，不會因此漏記。

🔴 **而且一定要有退路**：`user_id` 欄位要等你跑完 ALTER 才存在，而 PostgREST 對未知欄位是**整筆拒絕**，`logMcpUsage` 的錯誤又是被吞掉的 —— 先部署程式碼再跑 SQL 的話，**全站的 MCP 用量記錄會靜靜地停止寫入，而且沒有任何人會發現**。所以做成「先帶 user_id 試一次，失敗就退回原本的欄位組合」（同 PM-82 對 `allow_screenshot_images` 的處理方式）。

**② 兌換時檢查 MCP 紀錄**

用 `MCP_VERIFIED_CODES = new Set(['MCP30'])` 而不是寫死字串比對，之後要再發同類代碼只要加一行。

**🔴 位置很重要：擺在「重複兌換檢查」之後、「搶名額」之前。** 反過來的話，資格不符的人會先把限量代碼的名額搶走再被拒絕 —— MCP30 雖然不限量，但這段程式碼是所有代碼共用的，順序錯了會傷到 BUZZ100 那類限量碼。

**🔴 查不出來時 fail closed。** `hasMcpUsage` 回 `boolean | null`：`null` 代表「查詢本身失敗」（例如欄位還沒建），這時**不發券**。反過來寫的話，資料庫一出問題就變成人人可領。但訊息要跟「你還沒接 MCP」**分開**（前者回 503「目前無法驗證，請稍後再試」，後者回 403 + 教學連結 + `need_mcp: true` 旗標）—— 混在一起的話，使用者會一直重試自己根本沒做錯的事。

**③ SQL 腳本 `server/mcp30-ticket.sql`**（你要在 Supabase 執行）

包含卡片的 INSERT（加 `ON CONFLICT DO NOTHING`，重跑不會壞）、**卡片沒寫但缺了就不會動的 ALTER**、以及 `user_id` 的索引（兌換是 `WHERE user_id` 的查詢）。附三段驗證 SQL，跑完貼結果就知道成不成功。

---

**⚠ 一個資料面的事實，請先知道再測**

`user_id` 是現在才加的欄位，**先前所有的 MCP 呼叫都沒有留下歸屬**。所以包含你自己在內，**都必須在部署後「再呼叫一次任何 MCP 工具」**，才會產生第一筆可用來驗證資格的紀錄。這不是 bug，是那份資料本來就不存在——沒有辦法回填。

對真實使用者反而是對的：他們的流程本來就是「接上 MCP → 用一次 → 兌換」。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 有 MCP 紀錄 → 成功拿券 | ⚠ 程式碼完成；**實測需要真實 session token + 你先跑 SQL**（見 DONE-399） |
| 2 | 沒有 MCP 紀錄 → 被拒 + 友善提示 | ✅ 邏輯已驗；訊息附 `bugezy.dev/guide` 與 `need_mcp` 旗標 |
| 3 | 重複兌換 → 被拒 | ✅ 沿用既有的 `UNIQUE(user_id, code)` 檢查，未改動 |
| 4 | BUG10 不受影響 | ✅ 不在 `MCP_VERIFIED_CODES` 裡；線上實測兩者行為一致 |
| 5 | mcp_usage 結構確認 | 🔴 **原本沒有 user_id**，已補 ALTER + 程式碼 + 退路 |

**測試**：新增 `server/_verify398.mjs` **22/22**（server 端原本沒有驗收腳本慣例——邏輯都在 Workers + Supabase 裡，mock 整個 PostgREST 成本遠高於價值。所以這支驗**原始碼層面的不變式**（順序、fail-closed、退路）＋**線上端點實際行為**）。

---

### 🟡 PM-399：PM-398 部署 + 端到端驗證

**前提**：PM-398 程式碼完成

**執行**：
1. deploy Workers：`cd server && npx wrangler deploy`
2. 用你自己的帳號測試：
   - 你有 MCP 使用紀錄 → 輸入 MCP30 → 應成功
   - 確認票券錢包顯示 MCP30 30 天
3. 確認 BUG10 不受影響

**驗收條件**：
1. Workers deploy 成功
2. MCP30 兌換成功（你的帳號有 MCP 紀錄）
3. BUG10 仍可正常兌換
4. 票券錢包同時顯示 BUG10 + MCP30

完成後在下方寫 `🔵 DONE-399`。

🔵 **DONE-399**

**Workers 已部署**：Version `2c4c2713-74fc-4e45-a60a-b102b66108c1`。

**線上實測（不需要使用者 token 就驗得到的部分）**
```
未登入兌換 MCP30      → 401 unauthorized
假 token 兌換 MCP30   → 401 unauthorized
未登入兌換 BUG10      → 401 unauthorized（行為一致，未受影響）
```
另確認 **未登入時的回應不會洩漏「MCP30 這個代碼存不存在」**——認證閘門在最前面，探測不到代碼清單。

**全套回歸 708 / 0**
- bridge 12 套 **686**（`_verify309` 151／`_verify392` 78／`_verify355` 81／`_verify383` 71／`_verify_phase1` 57／`_verify367` 47／`_verify375` 47／`_verify389` 43／`_verify327` 42／`_verify350` 27／`_verify362` 23／`_verify366` 19）
- server 新增 `_verify398` **22**

---

**🔴 驗收條件 2～4 我沒有辦法自己完成，需要你動手**

三件事都卡在同一個地方：**兌換要真實的 session token，而我沒有你的**；而且 MCP30 這個代碼在你跑 SQL 之前根本不存在。

**請照這個順序做：**

**① 在 Supabase SQL Editor 跑 `server/mcp30-ticket.sql`**（兩段都要，缺一不可）
- 建立 MCP30 代碼
- `ALTER TABLE mcp_usage ADD COLUMN IF NOT EXISTS user_id text` ← **卡片沒寫，但少了它 MCP30 會對所有人都失敗**
- 腳本最後有三段驗證查詢，跑完貼結果就知道成不成功

**② 隨便呼叫一次 MCP 工具**（例如在 Claude Code 裡用 `list_reports`）
- 這一步**不能跳過**：`user_id` 是現在才加的欄位，你先前的所有 MCP 呼叫都沒有留下歸屬，資料庫裡沒有任何一筆能證明你接過 MCP
- ⚠ 目前 bridge 的方案閘門是開的，若你還沒設 `BUGEZY_SESSION_TOKEN`，**bridge 的工具會被擋**。用**雲端 MCP** 的工具（`list_reports` 等，走的是 `bugezy.dev/mcp?token=`）就沒有這個問題，而且那正是會寫進 `mcp_usage` 的那一條路徑
- 驗證：`SELECT count(*) FROM mcp_usage WHERE user_id IS NOT NULL;` 應該 ≥ 1

**③ 在擴充功能的優惠碼欄位輸入 `MCP30`**
- 預期：成功，票券錢包出現「MCP30 30 天」
- 順便確認 BUG10 仍可正常兌換，且錢包能同時顯示兩張

**如果第 ③ 步回「請先完成 MCP 對接」** → 表示第 ② 步的紀錄沒寫進去，先跑驗證查詢看 `user_id` 是不是還是 NULL。
**如果回「目前無法驗證 MCP 使用紀錄」** → 表示第 ① 步的 ALTER 沒跑成功（這是刻意跟上一句分開的訊息，就是為了讓你一眼分辨是哪一段出問題）。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | Workers deploy 成功 | ✅ `2c4c2713` |
| 2 | MCP30 兌換成功 | ⚠ **需要你跑 SQL + 呼叫一次 MCP 後自行驗證**（見上） |
| 3 | BUG10 仍可正常兌換 | ✅ 程式碼路徑未改動；線上確認行為一致。實際兌換同樣需要你的帳號 |
| 4 | 錢包同時顯示 BUG10 + MCP30 | ⚠ 同 2，票券錢包的顯示邏輯（PM-265~271）本次未改動 |

**沒有動到的東西**：extension、bridge 都沒改，不需要重新載入或重連 MCP。

---

### 🟡 PM-400：MCP30 驗證 — 產生 mcp_usage 紀錄 + 確認 user_id 寫入

**目標**：呼叫一次雲端 MCP 工具，確認 mcp_usage 表有寫入帶 user_id 的紀錄。

**執行**：
1. 用雲端 MCP（`bugezy.dev/mcp?token=...`）呼叫 `list_reports` 或 `get_report_overview`
2. 呼叫完後，用 Supabase 查詢確認：
   ```sql
   SELECT count(*) AS total, count(user_id) AS with_user_id FROM mcp_usage;
   ```
3. `with_user_id` 應該 ≥ 1

**驗收條件**：
1. 雲端 MCP 工具呼叫成功（回傳有資料或空陣列皆可）
2. mcp_usage 表有至少一筆 user_id 非 NULL 的紀錄
3. 回報 total 和 with_user_id 的數字

完成後在下方寫 `🔵 DONE-400`。


---

🔵 **DONE-400**

**🔴 執行結果：驗收條件 2 無法達成，而且原因很具體 —— 你目前的 MCP 連線網址沒有帶 `?token=`。**

---

**實際做的事與觀察到的**

| 步驟 | 結果 |
|---|---|
| ① 呼叫 `get_usage_stats`（基準） | MCP 呼叫次數 **109** |
| ② 呼叫 `list_reports(user_email: fox100039@gmail.com)` | ❌ 回「**請在 MCP URL 加上 ?token=xxx**，或提供 session_token 參數」 |
| ③ 再呼叫 `get_usage_stats` | MCP 呼叫次數 **110**（+1） |

另外試了另一個 bugezy MCP 連線（`mcp__bugezy__*`）→ `fetch failed`，那個設定已經連不上了，目前能用的雲端 MCP 只有一個。

---

**這三個觀察合起來說明了什麼**

**① `logMcpUsage` 本身正常，PM-398 的退路沒有把記錄寫壞。** 呼叫次數從 109 → 110，代表 insert 有成功落地。（這點很重要：如果 ALTER 還沒跑而我又沒做退路，這裡會停在 109 而且**完全沒有錯誤訊息**。）

**② `list_reports` 那一次沒有計入**（109→110 只加了 `get_usage_stats` 自己那一筆）。因為沒有 token 的分支是 `return txt(...)` 而不是 `txtWithTokens(...)` —— 那條路徑本來就不記錄用量。這是既有設計，不是這次改壞的。

**③ 🔴 關鍵：`user_id` 一定是 NULL，所以 `with_user_id` 不可能 ≥ 1。**

`logMcpUsage` 解使用者的唯一來源是 `env.__mcp_session_token`，而它只在 `/mcp` 路由從**網址的 `?token=`** 取得：

```ts
const urlToken = url.searchParams.get('token') || '';
const mcpEnv: Env = { ...env, __mcp_session_token: urlToken };
```

`list_reports` 明確回報「請在 MCP URL 加上 ?token=」，這句話只有在 `env.__mcp_session_token` 是空的時候才會出現 —— 也就是**你的 MCP 連線網址就是 `https://bugezy.dev/mcp`，後面沒有帶 token**。

所以：**不管呼叫幾次雲端 MCP，寫進 `mcp_usage` 的每一筆都是 `user_id = NULL`，MCP30 永遠拿不到。**

---

**怎麼修（一步）**

把 claude.ai 那個 bugezy 連結器的網址從
```
https://bugezy.dev/mcp
```
改成
```
https://bugezy.dev/mcp?token=<你的 session token>
```
token 從 **BugEzy 擴充功能 → 進階設定** 複製。改完之後：
1. 隨便呼叫一次雲端 MCP 工具（`get_usage_stats` 最省事，不需要任何參數）
2. 跑 `SELECT count(*) AS total, count(user_id) AS with_user_id FROM mcp_usage;`
3. `with_user_id` 應該 ≥ 1，這時 MCP30 才兌換得到

⚠ 前提仍然是 **`server/mcp30-ticket.sql` 的 ALTER 已經跑過**（PM-398／399）。若還沒跑，`user_id` 欄位不存在，我的退路會讓那一筆退回不帶 user_id 的寫法 —— 記錄照樣進得去（所以次數還是會加），但 `with_user_id` 一樣是 0。**這兩種情況的外在表現一模一樣，所以請先確認 ALTER 跑過再看數字。**

---

**⚠ 順帶發現一個相關的缺口（建議另開一張卡）**

`list_reports` 等工具的 token 有兩條來源：URL 的 `?token=` **或**參數 `session_token`。但 **`logMcpUsage` 只看得到前者** —— 它拿不到工具的參數。

也就是說：一個照文件用「參數帶 session_token」的使用者，工具可以正常運作，但**他的用量永遠記不到 user_id，因此永遠拿不到 MCP30**。兩條路徑在這件事上不等價，而且從使用者角度完全看不出差別。

要補的話是把已解出的 token 回填進 `env.__mcp_session_token`（那本來就是每請求一份的副本，寫它是安全的），約三處。**這超出 PM-400 的範圍，等你決定。**

---

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 雲端 MCP 呼叫成功 | ✅ `get_usage_stats` 兩次都成功；`list_reports` 因缺 token 被擋（**這正是問題所在**） |
| 2 | 至少一筆 user_id 非 NULL | ❌ **做不到** —— MCP 網址沒帶 `?token=`，見上 |
| 3 | 回報 total 與 with_user_id | ⚠ `total` 可從 `get_usage_stats` 推得（**110 次**／當月）；`with_user_id` 需要你跑 SQL，但依上述分析**目前必定是 0** |

**沒有改任何程式碼**，不需要 deploy、不需要重新載入擴充功能。

---

### 🟡 PM-401：修復 logMcpUsage — 工具參數的 session_token 也要記 user_id

**問題**：Claude Connector 的 MCP URL 不帶 `?token=`，用戶的 token 是透過工具參數 `session_token` 傳入。但 `logMcpUsage` 只從 `env.__mcp_session_token`（URL token）取 user_id，看不到工具參數的 token。導致 mcp_usage 的 user_id 永遠是 NULL，MCP30 永遠拿不到。

**DONE-400 已指出修法**：把工具內解出的 token 回填進 `env.__mcp_session_token`。

---

**位置**：`server/src/index.ts`

**修法**：在每個 MCP 工具的處理邏輯中，當從工具參數取得 `session_token` 並成功驗證後，回填到 env：

```typescript
// 工具解出 token 後（不管是從 URL 還是參數）
const resolvedToken = env.__mcp_session_token || params.session_token;
if (resolvedToken) {
  env.__mcp_session_token = resolvedToken;  // 回填，讓 logMcpUsage 看得到
}
```

或者更乾淨的做法：在 `logMcpUsage` 函式加一個 optional 參數：

```typescript
async function logMcpUsage(env, toolName, tokens, reportId, userId?: string) {
  const resolvedUserId = userId || await resolveUserIdFromToken(env.__mcp_session_token);
  // ... insert with resolvedUserId
}
```

然後在工具呼叫 `logMcpUsage` 時，把已經解出的 userId 傳進去。

**找出所有需要改的地方**：grep 所有 `logMcpUsage` 呼叫點，確保每一處都能帶上 userId。

**驗收條件**：
1. 透過 Claude Connector（URL 不帶 token）呼叫 MCP 工具 → mcp_usage.user_id 非 NULL
2. 透過 URL 帶 token 的方式呼叫 → 行為不變（向下相容）
3. 不帶任何 token 的呼叫 → user_id = NULL（合理，不報錯）
4. deploy 成功
5. 跑 `SELECT count(user_id) AS with_user_id FROM mcp_usage;` 確認有增加

完成後在下方寫 `🔵 DONE-401`。


---

🔵 **DONE-401**

**做了什麼**：`logMcpUsage` 與 `txtWithTokens` 加一個 optional 的 `knownUserId`，四支會驗證身分的工具在**驗證成功之後**把已解出的 userId 傳進去。已 deploy（`7acacf35`）。

---

**🔴 我選了卡片的第二個做法（傳 userId），刻意不做第一個（回填 env）**

卡片提的第一個做法是 `env.__mcp_session_token = resolvedToken`。但 `/mcp` 路由那裡有一段 PM-190 留下的註解，講的正是不要這樣做：

> 用副本（非改共用 env）**避免同 isolate 併發 request 互相覆寫 token（跨 tool await 期間的競態）**

回填等於把當初特意避開的樣式又加回來。傳一個明確的值沒有共享可變狀態，也好讀。驗收裡加了一條斷言鎖住這件事：**原始碼中不得出現對 `env.__mcp_session_token` 的賦值**。

**另一個安全上的講究**：只有在**已經比對過 token 屬於該使用者**之後才帶 userId。四支工具（`list_reports` / `get_live_errors` / `get_terminal_logs` / `get_usage_quota`）都是驗證成功才走到 `txtWithTokens`。`list_reports` 在「查無此 email」時回的空陣列**刻意不帶 userId**——那時根本還沒驗證身分。這條也用測試鎖住了。

---

**線上實測（不需要真 token 就驗得到的）**

| 動作 | 結果 |
|---|---|
| `get_usage_stats`（基準） | 110 次 |
| `list_reports` 帶**故意寫錯的** session_token | 「session_token 驗證失敗」 |
| `get_usage_stats` | **111 次**（只加了我自己這一筆） |

也就是說**驗證失敗的呼叫根本不會產生用量紀錄**（那條分支走 `txt()` 而非 `txtWithTokens()`），自然也不可能被錯誤歸戶。

---

**⚠ 一個卡片前提沒說完、但會影響你怎麼用的事實**

卡片寫「用戶的 token 是透過工具參數 `session_token` 傳入」。但 PM-400 實測時，我**沒有**帶參數呼叫 `list_reports`，它回的是「請在 MCP URL 加上 ?token=xxx，**或提供 session_token 參數**」——代表 connector **兩條都沒有自動帶**。

所以這次修好之後的真實情況是：

| 情境 | user_id 會不會記到 |
|---|---|
| AI 呼叫四支工具之一並**明確帶上** `session_token` | ✅ 會（**這張卡修好的就是這條**） |
| MCP 網址帶 `?token=` | ✅ 會，而且**所有工具**都適用 |
| 呼叫 `get_usage_stats`（它根本沒有 session_token 參數） | ❌ 不會 |

**所以最省事、覆蓋最廣的仍然是把 `?token=` 加進 MCP 網址**（DONE-400 的建議不變）。這張卡的價值是把「用參數帶 token」這條路也補起來，讓兩條路徑在用量歸戶上等價——原本它們不等價，而且從使用者角度完全看不出差別。

---

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 透過 Connector（URL 不帶 token）呼叫 → user_id 非 NULL | ⚠ **程式碼路徑已完成並驗證，但實測需要你的真實 session token**（我沒有，也不該有）。前提是**呼叫時要帶 `session_token` 參數**，見上表 |
| 2 | URL 帶 token → 行為不變 | ✅ 舊路徑原封不動（`env.__mcp_session_token` 仍是 fallback），有測試鎖住 |
| 3 | 完全不帶 token → user_id = NULL 且不報錯 | ✅ 有測試鎖住；線上也驗證了「驗證失敗的呼叫連紀錄都不會產生」 |
| 4 | deploy 成功 | ✅ `7acacf35-8bbb-4ba9-ac73-00e2df9b0c04` |
| 5 | `SELECT count(user_id) …` 有增加 | ⚠ **需要你跑 SQL**。而且前提是 `mcp30-ticket.sql` 的 ALTER 已經跑過，否則欄位不存在、退路會讓那筆退回不帶 user_id 的寫法 → 數字仍是 0 |

**測試**：`server/_verify398.mjs` 22 → **33 項全過**（新增 11 條 PM-401 的不變式）。

**沒有動 extension／bridge**，不需要重新載入或重連 MCP。
