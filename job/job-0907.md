# job-0907.md — BugEzy Day 54（2026-09-07）

## 今日任務
- SEO 修正（robots.txt + sitemap.xml）

---

### 🟡 PM-446：新增 robots.txt + sitemap.xml 路由

**背景**：Google Search Console 通知 bugezy.dev 有網頁被 robots.txt 封鎖無法索引。實際上 server 沒有 robots.txt 路由，Cloudflare Workers 可能回了 404 或被 catch-all 路由攔截。同時也缺 sitemap.xml。

**執行**（在 `release/v1.2.0` 分支上）：

#### 1) 新增 `/robots.txt` 路由
```
User-agent: *
Allow: /
Disallow: /api/
Disallow: /mcp
Sitemap: https://bugezy.dev/sitemap.xml
```

#### 2) 新增 `/sitemap.xml` 路由
列出所有公開頁面：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>https://bugezy.dev/</loc><priority>1.0</priority></url>
  <url><loc>https://bugezy.dev/features</loc><priority>0.8</priority></url>
  <url><loc>https://bugezy.dev/faq</loc><priority>0.7</priority></url>
  <url><loc>https://bugezy.dev/guide</loc><priority>0.8</priority></url>
  <url><loc>https://bugezy.dev/skill</loc><priority>0.7</priority></url>
  <url><loc>https://bugezy.dev/changelog</loc><priority>0.5</priority></url>
  <url><loc>https://bugezy.dev/privacy</loc><priority>0.3</priority></url>
  <url><loc>https://bugezy.dev/feedback</loc><priority>0.5</priority></url>
  <url><loc>https://bugezy.dev/blog</loc><priority>0.6</priority></url>
  <url><loc>https://bugezy.dev/testimonials</loc><priority>0.5</priority></url>
</urlset>
```

#### 3) 確認 Content-Type
- `/robots.txt` → `text/plain`
- `/sitemap.xml` → `application/xml`

#### 4) 部署
- `wrangler deploy` 上線
- 驗證 `https://bugezy.dev/robots.txt` 和 `https://bugezy.dev/sitemap.xml` 都能正常回應

**驗收**：
- `curl https://bugezy.dev/robots.txt` 回傳正確內容
- `curl https://bugezy.dev/sitemap.xml` 回傳正確 XML
- 端到端 0 failed

---

🔵 **DONE-446**

## 🔴 兩條路由早就有了，而且比卡片的版本完整很多

卡片說「server 沒有 robots.txt 路由」—— **不成立**。PM-136 建的、PM-264 改成資料驅動的，線上實測：

```
$ curl -I https://bugezy.dev/robots.txt
HTTP/1.1 200 OK
Content-Type: text/plain; charset=utf-8      ← 卡片要求的
Content-Length: 133

$ curl -I https://bugezy.dev/sitemap.xml
HTTP/1.1 200 OK
Content-Type: application/xml; charset=utf-8  ← 卡片要求的
57 個 <loc>、32 KB
```

**照卡片改會是明顯的退步**：

| 現況 | 卡片的版本 |
|---|---|
| 57 個 URL（10 頁 × 各語言版本 + 5 篇部落格） | 10 個 URL |
| 每筆帶完整 `xhtml:link hreflang` + `x-default` | 沒有 |
| 語言集合直接沿用 `LANGS_6/3/ZH`，與 canonical／hreflang 同一個事實來源（PM-264） | 寫死 |
| `Disallow: /report/`、`Disallow: /reports` | **沒有** |

最後一條最嚴重 —— 那兩行擋的是**使用者的報告資料**。照抄卡片等於邀請 Google 去爬別人的 bug 報告。

## 那 GSC 到底在抱怨什麼？我查到了

寫了個腳本比對線上的 robots.txt、sitemap 與 11 個公開頁的連結：

```
① sitemap 裡有沒有被 robots.txt 擋住的 URL：沒有 ✅
② 公開頁面連到「被擋住」的路徑：
   /reports  ← 被 11 個頁面連結（/ /features /faq /guide …）
③ 被擋住的頁面自己怎麼標：
   /reports      200  <meta name="robots" content="noindex, nofollow">  |  無 X-Robots-Tag
   /report/:id   200  (無 meta robots)                                  |  無 X-Robots-Tag
```

**根因**：`/reports` 被**每一頁的 footer** 連結（PM-434 我把它放進共用 footer 之後變成全站 11 頁），同時又被 `robots.txt` 擋住 → Google 到處看到這個連結卻爬不進去 → GSC 就報「已封鎖 robots.txt，無法索引」。

而且這是 SEO 的**經典牴觸組合**：`/reports` 自己已經標了 `noindex, nofollow`，但 **robots.txt 擋著，Google 永遠讀不到那個 noindex** —— 所以它既不會被正確排除，又會一直出現在 GSC 的警告清單裡。

## 我做了什麼（保護性的部分）

**`/report/:id` 補上 `X-Robots-Tag: noindex, nofollow`。**
那頁原本**完全沒有 robots 標記**，只靠 robots.txt 擋。robots.txt 是「請不要爬」，不是「不要索引」—— 報告連結被貼到公開場合時，Google 仍可能把網址以裸連結形式收錄。加 header 之後就算哪天 robots.txt 改了也不會被索引。這個改動不會讓任何東西變成可索引，純粹是多一層保險。

## ⚠ 真正能消掉 GSC 警告的那一步，要你決定

三個選項，我的建議是 **A**：

| | 做法 | 結果 |
|---|---|---|
| **A（建議）** | 從 robots.txt 拿掉 `Disallow: /reports` | Google 爬得到 → 讀到 `noindex, nofollow` → **真正把它移出索引**，GSC 警告消失。該頁需要 token，沒 token 只會看到登入提示，沒有資料外洩風險 |
| B | footer 的 `/reports` 連結加 `rel="nofollow"` | 減少 Google 發現它的機會，但已經進 GSC 清單的不會自己消失 |
| C | 什麼都不做 | 警告會一直在。GSC 的「已封鎖」對**刻意封鎖**的 URL 本來就是資訊性的，不影響其他頁面的收錄 |

`Disallow: /report/`（分享報告）**我建議維持**，那是別人的資料，不該讓 Google 主動去爬。

## ⚠ 沒有部署，原因要講清楚

卡片第 4 步要 `wrangler deploy`，但：

1. **兩條路由已經在線上正常運作**，為它們部署沒有意義。
2. **server 是從 master 部署的**，而這張卡指定在 `release/v1.2.0` 做。從這條分支部署，會**連帶把 PM-440 的 footer `v1.2.0` 一起上線** —— 商店還沒放行 1.2.0，官網卻先寫 v1.2.0，跟 PM-440 刻意保留 `/api/version = 1.1.5` 的理由自相矛盾。

要讓 `X-Robots-Tag` 上線，乾淨的做法是**把那個改動 cherry-pick 到 master 再從 master 部署**。要我做跟我說。

## 護欄

以前這兩條路由**沒有任何斷言在守**，refactor 掉也不會有人知道。`_verify403` 新增第 ⑲ 區 5 條（227 → **232**）：路由與 Content-Type、robots 擋住四條私人路徑 + Sitemap 那行、**sitemap 不可以列出被 robots.txt 擋住的路徑**、sitemap 的語言集合必須沿用 `LANGS_*`、分享報告頁要有 noindex。

**反向測試 8/8 全紅。** 其中一條原本是假的 —— 「分享報告頁 noindex」只查字串，被 `/test-errors` 那行同樣的 header 餵飽了，把 `/report/` 的拿掉照樣綠。已改成錨在 `/report/` 路由裡。

`server/_verify-live.mjs` 也加了線上檢查：robots/sitemap 的狀態與 Content-Type、**sitemap 有沒有列到被擋的 URL**、**公開頁有沒有連到被擋的路徑**（就是這次的根因，現在會印成 `GAP`）。

## 端到端

**594 passed, 0 failed, 53 skipped**（另 4 套整套 SKIP），server／extension `tsc` 皆 exit 0，線上驗收全過。

### 🟡 PM-447：SEO 修正 — 從 robots.txt 移除 Disallow /reports + cherry-pick 到 master 部署

**背景**：GSC 警告根因是 `/reports` 被 footer 連結但被 robots.txt 擋住。選方案 A：拿掉 `Disallow: /reports`，讓 Google 讀到頁面上的 `noindex, nofollow`，真正移出索引。

**執行**：

1. 在 `release/v1.2.0` 分支：從 robots.txt 移除 `Disallow: /reports` 這一行（保留 `Disallow: /report/`）
2. 把 PM-446 的 `X-Robots-Tag` 改動 + 這次的 robots.txt 改動 cherry-pick 到 master
3. 從 master 部署 `wrangler deploy`
4. 驗證線上 `https://bugezy.dev/robots.txt` 不再包含 `/reports`

**不動**：`Disallow: /report/` 保留（保護使用者報告資料）

**驗收**：
- `curl https://bugezy.dev/robots.txt` 不包含 `Disallow: /reports`
- `curl -I https://bugezy.dev/report/test` 包含 `X-Robots-Tag: noindex, nofollow`
- 從 master 部署，不是從 release/v1.2.0
- 斷言 232 條全過
