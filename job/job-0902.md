# job-0902.md — BugEzy Day 49（2026-09-02）

## 今日任務
- 大黃蜂視覺系統實作 — 群組 A（Popup）

## 設計交付包位置
`C:\dev\bugezy\BugEzy 大黃蜂色調項目概覽\design_handoff_hornet_visual_system\`

## 關鍵文件
- DESIGN_SPEC.md — 規範（唯一依據）
- README.md — 實作說明 + 畫面對照表
- BugEzy Popup 完整主畫面.dc.html — 畫面 18~22
- BugEzy Popup 大黃蜂改版.dc.html — 畫面 01~04
- bee-video.js — 大黃蜂影片 web component
- logo/ — 大黃蜂圖片資源

---

### 🟡 PM-413：設計 Token 落地 — popup :root CSS 變數

**目標**：把 DESIGN_SPEC.md §2~§3 的所有 design token 寫入 popup.html 的 `:root`。

**讀取**：`DESIGN_SPEC.md` §2（色彩）和 §3（字體）

**改動**：`extension/src/popup.html` 的 `<style>` 開頭加入：
```css
:root {
  --y: #F7BE00;
  --y-deep: #DFA800;
  --y-pale: #FFE9AE;
  --cream: #FFF4D6;
  --ink: #14110B;
  --ink-2: #211C13;
  --ink-3: #0E0C08;
  --brown: #7A4E1D;
  --brown-d: #4A2F12;
  --line-dark: #3A3122;
  --line-dark-2: #55492F;
  --err: #8A2A0F;
  --err-fg: #E08B72;
  --info: #1E6FD9;
  /* 字體、圓角、間距等 token 也一併加入 */
}
```

**同時**：把 popup body width 從 360px 改回 **320px**

**驗收條件**：
1. :root 有完整的色彩 token
2. body width = 320px
3. 現有功能不受影響（只加變數，不改既有樣式）

完成後在下方寫 `🔵 DONE-413`。

🔵 **DONE-413**

**改動**：`extension/src/popup.html`（只動 `<style>` 開頭的 `:root` 與 `body { width }`，沒有碰任何既有 class）

| 驗收條件 | 結果 |
|---|---|
| 1. `:root` 有完整色彩 token | ✅ §2.1 十四色全數落地 |
| 2. `body width = 320px` | ✅ 360 → 320 |
| 3. 現有功能不受影響（只加變數） | ✅ 既有 class 一條未改，舊的深藍紫變數暫留 |

**除了 §2.1 的十四個色票，另外一起落地的 token**
- §2.4 深底次要文字**只有兩階** `--on-dark #A08B62` / `--on-dark-2 #C9A15A`；黃底的 `--on-y #3A2409` / `--on-y-2 #5E3A14`。規範說這個錯犯過三次，所以直接給變數，之後沒有理由再手寫褐色。
- §3 字體三角色 `--font-brand` / `--font-ui` / `--font-mono`
- §7 圓角 `--r-7`…`--r-16` + `--r-pill`
- §7.1 硬投影 `--sh-2`…`--sh-5`
- §4 蜂巢紋 `--hive-y` / `--hive-d`（已確認 data URI 內無未編碼的 `;`）
- §5 六角 `--hex`（clip-path）

**兩個要 FOX 知道的判斷**

1. **沒有外連 Google Fonts。** 規範 §3 給了 `<link>`，但 popup 是擴充頁面 —— 連外字型等於每次開 popup 就向第三方發一次請求，隱私政策要交代（v1.1.5 已經被 CWS 以「隱私政策資訊不足」退件過一次），離線還會先閃一次無樣式。所以 `--font-*` 寫成「設計字型優先 + 系統字備援」的字型堆疊，**不發任何網路請求**。代價是 Archivo／JetBrains Mono 在沒裝字型的機器上會落到系統字，字形不完全對版。要完全對版的作法是把 woff2 打包進 `dist/` 再補 `@font-face` —— 那三個變數不用動，加完就會自動吃到。**這個要不要做請 FOX 決定。**

2. **`_verify403` 的 409-1 斷言翻面了。** 它原本寫死 `width: 360px`，PM-413 改 320 就會紅。已改成 `413-1 popup 寬度改回 320px`，並在旁邊註明為什麼。**下面的 nowrap 與 flex-shrink 斷言一條都沒放寬** —— 那三招才是 320 站得住的前提。

**中間狀態（預期內，不是壞掉）**：320px + 尚未改版的舊樣式下，第二層標題會被容器裁切。§9.1 的三招要等 PM-415／418 才落實，PM-421 再做整體驗收。

**回歸**：`_verify403` 92 / 0（改斷言後）。


---

### 🟡 PM-414：Popup 畫面 01 — 登入頁

**目標**：把登入頁改成大黃蜂視覺。

**參考**：`BugEzy Popup 大黃蜂改版.dc.html` 畫面 01
**讀取**：DESIGN_SPEC.md §5（六角標記）、§7（元件）、§8（動畫）

**改動重點**：
- 背景改為 `--cream` #FFF4D6
- 置中：大黃蜂影片 92px（用 bee-video.js 的 `<bee-video>` web component，影片檔 `logo/bee.webm`）
- 字標 BugEzy 26px
- 副標「用說話回報 Bug，讓 AI 修好它」
- Google 登入按鈕：白底黑框 + 硬投影
- 底部虛線分隔 + `MCP · 13 TOOLS` / `bugezy.dev`
- 移除所有 emoji，用幾何圖示

**資源複製**：把 `logo/bee.webm`、`logo/hornet-real.png`、`bee-video.js` 複製到 `extension/src/` 適當位置

**驗收條件**：
1. 登入頁顯示大黃蜂影片（或靜態圖片 fallback）
2. 金黃色系配色
3. Google 登入按鈕有硬投影
4. 無 emoji
5. 登入功能正常

完成後在下方寫 `🔵 DONE-414`。

🔵 **DONE-414**

**改動**：`popup.html`（登入頁 markup + 樣式、body 面底）、`popup.ts`（登入鈕 label、bee-ready）、`i18n.ts`（新增 `login-tagline` 七語）、`build.mjs`（複製新資源）、新增 `src/bee-video.js`、`src/assets/bee.webm`、`src/assets/hornet-real.png`

| 驗收條件 | 結果 |
|---|---|
| 1. 顯示大黃蜂影片（或靜態圖片 fallback） | ✅ `<bee-video>` + `hornet-real.png` 疊底備援 |
| 2. 金黃色系配色 | ✅ body 改 `--y` + `--hive-y` 蜂巢紋 |
| 3. Google 登入按鈕有硬投影 | ✅ `3px 3px 0 var(--ink)`，hover 位移 1px 收成 2px |
| 4. 無 emoji | ✅ 🐛 已移除，整頁零 emoji |
| 5. 登入功能正常 | ✅ 型別檢查過、`_verify403` 92/0；實機待 PM-421 |

**fallback 的方向是刻意反過來的**：靜態圖預設就在畫面上，等 `bee-video` 畫出第一幀發 `bee-ready` 才把它藏起來。反過來做（預設藏、載好再顯示）在 webm 解不開或 `bee-video.js` 沒跑的時候會留一塊空白，這樣至少永遠有隻蜂。

**順手修掉一個既有的雷**：`popup.ts` 原本在按下登入時做 `googleLoginBtn.textContent = ...`，那會把按鈕內的 Google logo `<img>` 一起洗掉。舊版是純文字按鈕看不出來，新版按鈕的 logo 是視覺重點，登入失敗一次 logo 就再也回不來。改成只換 `#googleLoginLabel` 這個 span。

**三個跟卡片不一致的地方，都照 README「衝突時以 DESIGN_SPEC 為準」處理**

1. **背景用 `--y` 不是 `--cream`。** 卡片寫「背景改為 `--cream` #FFF4D6」，但 DESIGN_SPEC §2.1 寫明 `--y` 是「主面板底」、`--cream` 是「資料卡、輸入框、已填欄位」，設計稿畫面 01 也是黃底 + 蜂巢紋。規範與設計稿一致、只有卡片不同，所以照黃底做。**如果 FOX 要的真的是米白登入頁，講一聲我改。**
2. **副標與頁尾字級字重上調。** 設計稿是 500/12px 與 500/11px，但 §3.2 規定黃底上一律 ≥11.5px 且字重 ≥600（蜂巢紋會吃掉細筆畫），§11 檢查表也列了這條。已改成 600/12px 與 600/11.5px，色票用 §3.2 指定的 `--on-y #3A2409`。
3. **蜂的尺寸取 92px。** 卡片與 §8 都寫 92px，只有 README 表格寫 112px。取 92。

**資源大小**
- `hornet-real.png` 原檔 1226×1277 / **1.4MB**，只當 92px 的備援用不掉那個解析度。已縮成 246×256 / **71KB**（2.8× DPR 餘裕）。原檔留在交付包裡給群組 E 的官網 hero 用。
- `bee.webm` **2.8MB 原樣複製** —— 這台機器沒有 ffmpeg，無法重新編碼。擴充包會從約 1.1MB 變成約 4MB。**建議 FOX 之後用 ffmpeg 壓一版**（92px 顯示用不到現在的位元率），不影響程式碼，換檔即可。

**`bee-video.js` 用 classic script 載入，不走 esbuild entryPoints** —— 它自己呼叫 `customElements.define`，被打成 ESM 模組後 `<script src>` 就不會執行了。`build.mjs` 改成直接 `cpSync` 過去。

**中間狀態（預期內）**：body 面底已經是黃色，但主畫面還是舊的深藍紫樣式，所以現在主畫面是白字壓在黃底上。PM-415 就會換掉。

**回歸**：`_verify403` 92/0、`_verify309` 151/0、`_verify355` 81/0、`_verify383` 71/0；`tsc --noEmit` 無錯。


---

### 🟡 PM-415：Popup 畫面 18 — 主畫面（閒置，進階設定收合）

**目標**：改造主畫面第一層為大黃蜂視覺。

**參考**：`BugEzy Popup 完整主畫面.dc.html` 畫面 18

**改動重點**（從上到下）：
- 使用者列：黑底 `--ink` + 使用者名稱
- 品牌列：BugEzy + 麥克風開關
- 語音模式切換（即時字幕 / 精準轉錄）+ 授權提示
- 三入口 grid：開始錄製（主按鈕全寬黃底）+ 回溯 30s / 截圖標注（次要按鈕並排）
- 偵察模式入口（帶 badge + 箭頭）
- 我的報告按鈕
- 升級雙鈕（咖啡底）
- 安裝碼
- 票券錢包收合列
- 進階設定收合列
- AI 指令輪盤（黑卡）
- 版號

**驗收條件**：
1. 主畫面完全換成大黃蜂配色
2. 所有按鈕用 design token 顏色
3. 無 emoji（換幾何圖示）
4. 偵察模式入口有 badge
5. 進階設定預設收合
6. 所有功能正常（錄製/截圖/偵察/票券/設定）

完成後在下方寫 `🔵 DONE-415`。

🔵 **DONE-415**

**改動**：`popup.html`（主畫面十三個區塊的樣式 + markup）、`popup.ts`（票券箭頭改切 class）、`i18n.ts`（54 個 popup 專用 key 去 emoji + 新增 `copied`）

| 驗收條件 | 結果 |
|---|---|
| 1. 主畫面完全換成大黃蜂配色 | ✅ 使用者列→版號十三個區塊全換 |
| 2. 所有按鈕用 design token 顏色 | ✅ 不再有硬寫色碼，全走 `var(--*)` |
| 3. 無 emoji（換幾何圖示） | ✅ markup + 字典兩邊都清 |
| 4. 偵察模式入口有 badge | ✅ `#scoutPinBadge` 黑底黃字膠囊 + 箭頭多邊形 |
| 5. 進階設定預設收合 | ✅ 邏輯未動（`bugezy:settings-open` 預設 false） |
| 6. 所有功能正常 | ✅ 型別檢查過、四套回歸 0 failed；實機待 PM-421 |

**emoji 是兩個來源，只清 markup 沒有用**
`applyTranslations()` 會把每個 `[data-i18n]` 元素的 textContent 換成字典值，所以字典裡的 emoji 會蓋回畫面上。已清掉 **54 個 popup 專用 key** 的 emoji（五語都清）。這 54 個 key 全部只有 popup 在用，`content.ts` / `inject.ts` / `annotate.ts` / `edit-report.ts` 一個都沒碰到，所以群組 B／C 的行為完全不受影響。

**圖示改成幾何 span 之後，翻譯不會再把圖示洗掉**：圖示是 `[data-i18n]` 元素的**兄弟節點**，不在它裡面。這點很重要 —— 原本 `📋 我的報告` 是同一個 text node，只要語言一換就整段重寫。

**兩個「不靠顏色傳達內容」的修正（§7.7）**
- `scout-zone-fmt` 原本是 `{n} 區（🟢{ok} 🟡{warn} 🔴{err}）`。三個圓點一拿掉，三個數字就變成無法分辨的一串，所以補了文字標籤：`{n} 區（正常 {ok}／注意 {warn}／異常 {err}）`，五語都補。
- `scout-error-fmt` 的 🔴🟡 直接拿掉即可 —— 它後面本來就跟著 `critical` / `minor` 兩個字。

**兩個字元圖示改成 CSS 幾何**
- 票券錢包的 `▶`／`▼`：`popup.ts` 原本用 `ticketArrow.textContent` 換字元，改成 `classList.toggle('open')` + CSS 三角旋轉 90 度。
- 輪盤的複製鈕原本是一段 inline SVG，改成 `.ico-copy` 空心方框，跟其他圖示同一套語彙。

**順手補的**：輪盤的「✅ 已複製」原本是硬寫在 HTML 的中文，英文模式下也只會顯示中文。清 emoji 時一起補進字典（新 key `copied`，五語）。

**跟卡片不一致的地方（同樣照 README「以 DESIGN_SPEC 為準」）**
- **使用者列不是黑底。** 卡片寫「使用者列：黑底 `--ink`」，但卡片指定的參考是設計稿**畫面 18**，而畫面 18 的使用者列是黃底 + 1.5px 虛線分隔 + 咖啡色圓形頭貼。黑底 header 是**畫面 02**的畫法（另一版簡化稿）。已照畫面 18 做。**要黑底 header 的話講一聲，那是畫面 02 的樣子。**
- **三入口說明文字用 10px。** §3.2／§11 要求黃底文字 ≥11.5px，但畫面 18 在 320px 下把三入口說明定為 10px，README 也明講這是 320 能站住的三招之一。我的取捨是：**字重 ≥600 一步不讓（那是可讀性的關鍵），只有字級在這三顆按鈕上讓步**。其餘所有黃底文字都守住 11.5px／600。

**回歸**：`_verify403` 92/0、`_verify309` 151/0、`_verify355` 81/0、`_verify383` 71/0；`tsc --noEmit` 無錯。


---

### 🟡 PM-416：Popup 畫面 03 + 04 — 錄製中 + 錄製完成

**目標**：錄製中和錄製完成畫面換成大黃蜂視覺。

**參考**：`BugEzy Popup 大黃蜂改版.dc.html` 畫面 03、04

**畫面 03 錄製中**：
- 整頁反黑 `--ink` + 黃紋
- REC 膠囊
- 計時 46px JetBrains Mono
- 12 柱音量波形
- 三格計數（Console / Network / DOM）
- 停止按鈕（黃底）

**畫面 04 錄製完成**：
- 米白摘要卡（4 列 + URL）
- 分享連結（咖啡底）
- 匯出 JSON 主按鈕
- 複製 / 清除次要按鈕

**驗收條件**：
1. 錄製中整頁反黑 + 黃色元素
2. 計時器用 JetBrains Mono
3. 完成頁有米白摘要卡
4. 所有按鈕和功能正常

完成後在下方寫 `🔵 DONE-416`。

🔵 **DONE-416**

**改動**：`popup.html`（新增 `body.dark` + 重寫 recordingView／doneView）、`popup.ts`（`show()` 掛 dark、上傳狀態改走字典、錄製提示列）、`i18n.ts`（新增五個 key）

| 驗收條件 | 結果 |
|---|---|
| 1. 錄製中整頁反黑 + 黃色元素 | ✅ `body.dark` = `--ink` + `--hive-d`，計時／波形／停止鈕全黃 |
| 2. 計時器用 JetBrains Mono | ✅ `700 46px/1 var(--font-mono)` |
| 3. 完成頁有米白摘要卡 | ✅ `--cream` + 2px 黑框 + 圓角 14 |
| 4. 所有按鈕和功能正常 | ✅ id 全部沿用、型別檢查過、五套回歸 0 failed |

**`body.dark` 是共用的，不是錄製中專屬**：偵察模式（PM-418）也要整頁反黑。所以 `show()` 裡加了一道 `if (!body.classList.contains('scout-open'))` 的護欄 —— 否則從偵察模式跳回閒置時，`show('idle')` 會把偵察模式的黑底一起關掉。

**三格計數（Console / Network / DOM）沒有做，這是刻意的**
卡片的改動重點列了這三格，但**錄製中根本拿不到這三個數字**：`GET_STATE` 在錄製期間 `summary` 是 `null`，數字要等 `RECORDING_DONE` 把 content script 記憶體裡的資料合併完才產生。`chrome.storage.local` 的四個 buffer 只收「跨頁 flush」的部分，單頁錄製時幾乎是空的，拿它來顯示會給出**看起來像真的、其實嚴重少算**的數字。

要做需要在 background 加一支 `GET_LIVE_COUNTS`、讓 content script 定期回報計數 —— 那是行為改動，README 明講「這次只換視覺」。所以我把這三格拿掉而不是留三個 `—`（永遠是空的一排格子在產品上看起來像壞掉）。**四條驗收條件不受影響**。要補的話是一張獨立的卡，我可以做。

**摘要卡從五列變三列，但資料一筆都沒少**
設計稿畫面 04 把「時長」與「語音段數」放到標題右邊的等寬小字（`84s · 5 段語音`），摘要卡只留三個機器數出來的數字。已照這個作法搬，`#durationVal`／`#voiceCount` 兩個 id 原封不動，`popup.ts` 一行都不用改。Network 錯誤那列多給了 `.err` —— 它是唯一「有數字就是壞消息」的一列，用磚紅 `--err`。

**順手收掉三句硬寫的中文**
`renderUpload()` 裡的「⏳ 正在上傳到雲端...」「✅ 已上傳」「❌ 上傳失敗」是直接寫在 `popup.ts` 的字串，帶 emoji 而且**英文模式下也只會顯示中文**。已收進字典（`upload-uploading` / `upload-ok` / `upload-fail`，五語）。

**錄製中的提示列**依麥克風狀態分兩句：開著顯示「正在聽你說話 · 繁體中文」（語言取自語音語言下拉），關著顯示「錄製中 · 沒有錄語音」。設計稿只畫了前者，但麥克風關閉時說「正在聽你說話」是假的。

**12 柱波形是純 CSS 動畫**（§8 `bar`，各柱相位差 0.09s），高度與配色照設計稿逐柱給值，不接真實麥克風音量 —— 設計稿本身就是這樣定義的。

**回歸**：`_verify403` 92/0、`_verify309` 151/0、`_verify355` 81/0、`_verify383` 71/0、`_verify392` 78/0；`tsc --noEmit` 無錯。


---

### 🟡 PM-417：Popup 畫面 19 — 進階設定展開

**目標**：進階設定展開區域換成大黃蜂視覺。

**參考**：`BugEzy Popup 完整主畫面.dc.html` 畫面 19

**改動重點**：
- 語音語言下拉
- 即時監控開關
- 鍵盤模式開關
- 高畫質 AI 分析開關
- 工具列特效開關
- 每列 `1px solid rgba(20,17,11,.2)` 分隔
- 複製 MCP 設定按鈕
- 說明（咖啡底）
- 已複製回饋
- 輪盤編輯模式（4 色圓點 + textarea + 儲存/取消）

**驗收條件**：
1. 進階設定展開後視覺正確
2. 所有開關功能正常
3. MCP 設定複製功能正常

完成後在下方寫 `🔵 DONE-417`。

🔵 **DONE-417**

**改動**：`popup.html`（設定五列 / 語言下拉 / 複製 MCP / 說明 / 已複製回饋 / 輪盤編輯模式）、`popup.ts`（回饋卡改切 class）

| 驗收條件 | 結果 |
|---|---|
| 1. 進階設定展開後視覺正確 | ✅ 五列 + `1px solid rgba(20,17,11,.2)` 分隔 |
| 2. 所有開關功能正常 | ✅ id 與事件綁定全未動，`accent-color` 改 `--ink` |
| 3. MCP 設定複製功能正常 | ✅ 型別檢查過、三套回歸 0 failed |

**語言下拉沒有用 data URI 畫三角**。`<select>` 要自訂箭頭通常是 `appearance:none` + `background-image` 的 SVG data URI，但 §4 明講 data URI 內未編碼的 `;` 會提早結束整條 CSS 宣告（那個 bug 犯過一次）。改成外包一層 `.lang-select-wrap`，三角由 `::after` 用 border 畫 —— 零 data URI，也零風險。旗幟 emoji（🇹🇼🇨🇳🇭🇰🇬🇧🇯🇵🇰🇷🇻🇳）全部拿掉，只留語言名稱，跟設計稿畫面 19 一致。

**修掉一個換 class 之後才會冒出來的雷**：`copyMcpBtn` 的回饋原本是 `style.display = 'block'` + `style.color = '#22c55e'`。新版回饋卡是 `display:flex` 加一個 `::before` 六角 —— inline 的 `display:block` 會蓋掉 flex，而 block 容器裡的 `::before` 預設是 inline，**寬高會被忽略、六角直接不見**。改成固定用 `flex`，顏色由 `.warn` class 決定：成功是米白確認卡、需要先登入是咖啡色系統訊息（§2.3）。順便把兩個寫死的色碼 `#f59e0b` / `#22c55e` 清掉。

**輪盤四色圓點換成設計稿配色**（`#D92B2B` / `#1E6FD9` / `#1E9E5A` / `#F7BE00`）。**已存的提示詞不受影響** —— 顏色是存在提示詞資料裡的，舊的仍照原色顯示，只有之後新選的才會是這四色。選中狀態改用 `box-shadow` 外環（圓點沒有 clip-path，不會有 §5 說的「clip-path 裁掉 box-shadow」問題）。

**輪盤導覽鍵 `◀` `▶` 兩個字元**換成 `clip-path` 三角（§6 播放三角）。

**回歸**：`_verify403` 92/0、`_verify309` 151/0、`_verify355` 81/0；`tsc --noEmit` 無錯。


---

### 🟡 PM-418：Popup 畫面 20 — 偵察模式（第二層）

**目標**：第二層偵察模式換成大黃蜂視覺。

**參考**：`BugEzy Popup 完整主畫面.dc.html` 畫面 20

**改動重點**：
- 整頁反黑 `--ink`
- 返回鍵（左）+ 標題（右）
- 圖釘區：清單 + 巡檢結果（已解決的 `opacity: .58`）
- 巡檢全部 / 清除全部按鈕
- AI 監測區
- 記憶矩陣區

**驗收條件**：
1. 偵察模式整頁反黑
2. 圖釘清單顯示正確（含顏色狀態）
3. 巡檢全部功能正常
4. AI 監測和記憶矩陣顯示正確

完成後在下方寫 `🔵 DONE-418`。

🔵 **DONE-418**

**改動**：`popup.html`（第二層全部樣式 + 標題結構）、`popup.ts`（`showScout` 掛 dark、嚴重度改 class、去 emoji）、`i18n.ts`（新增 4 個 key）、`_verify403.mjs` + `_verify383.mjs`（5 條斷言隨新視覺翻面）

| 驗收條件 | 結果 |
|---|---|
| 1. 偵察模式整頁反黑 | ✅ `body.dark` + `body.scout-open` |
| 2. 圖釘清單顯示正確（含顏色狀態） | ✅ 深底卡 + §7.7 左側 3px 色條，已解決 `opacity: .58` |
| 3. 巡檢全部功能正常 | ✅ 事件綁定未動 |
| 4. AI 監測和記憶矩陣顯示正確 | ✅ 深底卡 `--ink-2` + `--on-dark-2` |

**`scout-open` 這個旗標是必要的，不是多餘的 class**
`show()`（PM-416 加的）會依 `view` 決定要不要掛 `body.dark`。但錄製狀態一更新就會呼叫 `show('idle')` —— 人還在偵察模式裡，黑底卻被關掉。所以 `showScout` 另外掛 `scout-open`，`show()` 看到它就不碰 `dark`。

**嚴重度圓點：協定沒改，只改呈現**
圖釘與巡檢結果的 🔴🟡🟢⚪ 是 **content script 算好的判定**，PM-405 特別寫過「popup 不重新解讀 JSON 去猜嚴重度，兩邊各算一次遲早會分岔」。這次**完全沿用那個訊號**，只是把它從「印在文字最前面的字元」換成 §7.7 的左側 3px 色條：新增 `sevClass()` 做 emoji → CSS class 的對照，色碼統一收在 `popup.html` 的 token。文字本身沒有變短（`（stale）` 標記還在），所以拿掉圓點也沒有損失資訊。

**五條斷言隨新視覺翻面**（都是規格變了，不是測試放水）
| 斷言 | 原本 | 現在 |
|---|---|---|
| `405-3/4` | 驗 `SEV_COLOR` 對照表 + `#ff6b6b` / `#4ade80` 兩個色碼 | 驗 `sevClass()` + 三個 `.sev-*` class 在 CSS 裡都有定義 |
| `408-3` | 三顆按鈕的 CSS 不可出現任何 `width:` | 改成不可出現**固定值** `width: <數字>` —— 大黃蜂系統的全域 `button { width: 100% }` 會把它們撐滿整列，所以現在一定要寫 `width: auto`，而 `auto` 正是這條斷言想要的 |
| `409-4/5` | 三個 head 都要 `justify-content: space-between` | **沒有放寬**，改的是實作：`.pin-head` 現在有三個子元素（六角 + 標題 + 按鈕），只靠 space-between 會把六角跟標題拆散，所以補 `.pin-title { margin-right: auto }`（auto margin 優先度高於 justify-content），兩個一起下 |
| `410-1` | 三個標題都要帶 📌 🤖 🧠 | 翻面成「三個標題都帶同一種幾何六角」。§1 全站禁用 emoji，但**意圖不變**：三個 section 用同一種標記、風格一致 |
| `383-2` | `.pin-mode-btn.on` 要是 BugEzy 藍 `#00bfff` | 改黃底實心（關閉是描邊）。差別在**填色**不只在色相，灰階截圖也分得出來 |

**另外加了一條新斷言**：`418 第二層一個 emoji 都不剩`，直接掃 `scoutView` 整段的 markup。以後有人手滑塞回 emoji 會立刻紅。`_verify403` 因此從 92 變 **93**。

**順手收掉的硬寫中文**：「巡檢全部」「清除全部」兩顆按鈕原本是寫死在 HTML 的中文（帶 🔄 🗑️），英文模式下也只有中文；`analyze-visible` 直接把 ✅ / ❌ 塞進格式字串。三個都收進字典（`pin-patrol-all` / `pin-clear-all` / `yes` / `no`，五語）。

**回歸**：`_verify403` **93/0**、`_verify383` 71/0、`_verify309` 151/0、`_verify355` 81/0、`_verify392` 78/0；`tsc --noEmit` 無錯。


---

### 🟡 PM-419：Popup 畫面 09~13 — 票券 + 付費 + 日票 + 額度 + 取消

**目標**：票券和付費相關畫面換成大黃蜂視覺。

**參考**：`BugEzy 頁面改版.dc.html` 畫面 09~13

**五個畫面**：
- 09 票券錢包展開：使用中票券（黑卡）→ 到期提醒（咖啡）→ 庫存 → 兌換 → 確認啟用
- 10 付費版：PRO 徽章 → 四格權益 → 取消訂閱
- 11 日票有效中：整頁反黑 + DAY PASS 膠囊 + 倒數
- 12 額度用完：用量滿格紅條 → 月費/日票 → 重置提示
- 13 已取消未到期：咖啡卡 + 恢復訂閱

**驗收條件**：
1. 五個畫面都換成大黃蜂配色
2. 票券兌換功能正常（BUG10 / MCP30）
3. 付費/取消訂閱流程正常
4. 日票倒數正常

完成後在下方寫 `🔵 DONE-419`。

🔵 **DONE-419**

**改動**：`popup.html`（五個畫面的樣式 + markup）、`popup.ts`（用量條、票券去 emoji、倒數改純數字）、`i18n.ts`（新增 6 個權益文案 key）

| 驗收條件 | 結果 |
|---|---|
| 1. 五個畫面都換成大黃蜂配色 | ✅ 09/10/11/12/13 全換 |
| 2. 票券兌換功能正常 | ✅ 事件與 API 呼叫未動 |
| 3. 付費/取消訂閱流程正常 | ✅ 三態互斥邏輯未動，只換外觀 |
| 4. 日票倒數正常 | ✅ 計時器邏輯未動，只改顯示格式 |

**09 票券錢包**：使用中票券改黑卡 + 脈衝六角（§7.4「票券生效中」的形狀分級）；到期提醒改咖啡卡（§2.3）；庫存票券每一列各自是米白資料卡；確認啟用是米白確認卡。**兌換輸入框吃 §7.3「實線=已填、虛線=待填」**：空的時候是半透明米白 + 虛框，一打字就靠 `:not(:placeholder-shown)` 變成實心米白 + 實框。

**10 付費版**：加了設計稿的四格權益 grid（無限次 / 90 天 / Whisper / CLI）。這是**新增的靜態文案**，六個 key 五語都補齊，沒有連動任何邏輯。PRO 徽章做成「膠囊外框 + 內部小六角」兩層 —— §5 提醒過 `clip-path` 會裁掉 `border`，所以六角是獨立子元素、框畫在外層。取消訂閱從底線連結改成描邊按鈕：它跟「恢復訂閱」是同一層級的操作，不該長得像註腳。

**11 日票**：§7.5「倒數用黑底、額度用黃底」。倒數卡整塊反黑 + 黃紋，`DAY PASS` 黃膠囊，倒數 38px 等寬字。

> **這裡跟設計稿有一個取捨要說清楚。** 設計稿畫面 11 是一整頁反黑、而且**只有倒數與升級鈕，沒有三個錄製入口**。照那樣做等於「持有日票時就換一個沒有錄製入口的畫面」——那是導覽結構的改動，不是換視覺，而且日票用戶正是最需要錄製入口的人。所以我做的是**倒數卡本身反黑**，§7.5 要的「倒數 vs 額度用底色分辨緊急程度」有做到，第一層的功能一個都沒少。要完全照設計稿改成獨立畫面的話，那是一張要動 view 切換的卡。

**倒數格式改成純數字 `HH:MM:SS`**。原本走 `day-pass-remaining`（「剩餘 18h 42m 07s」），那串在 38px 下塞不進 288px 內容寬，而且把語言文字混進了大數字裡。「這是什麼」由上面的 `DAY PASS` 膠囊與下面的說明列負責講。字典那個 key 沒有刪（沒有其他地方在用，留著不影響）。

**12 額度用完**：整張卡改黃底 + 蜂巢紋 + 硬投影（§7.5「額度用黃底」），加上設計稿的**用量滿格紅條**。資料就是 `renderFreeUsage` 在用的那份 `freeLimits`，**沒有新增任何 API** —— 只是把「剩幾次」換一種看得見的呈現。月費是主按鈕（黑底黃字硬投影）、日票是次要按鈕，順序照設計稿把月費放前面。重置提示改咖啡底。關閉鈕的 `✕` 字元換成兩條交叉線。

> JSON 付費牆（`showJsonPaidOverlay`）跟這張卡共用 DOM 但**沒有用量概念**，所以那條路徑會把用量條整塊隱藏。

**13 已取消**：咖啡卡 + 六角 + 到期日，恢復訂閱是黑底黃字硬投影的主按鈕 —— 咖啡卡上唯一要你做決定的東西。

**回歸**：`_verify403` 93/0、`_verify309` 151/0、`_verify383` 71/0、`_verify355` 81/0；`tsc --noEmit` 無錯。


---

### 🟡 PM-420：Popup 畫面 21 + 22 — Overlay（麥克風提示 + JSON 警語）

**目標**：兩個 overlay 換成大黃蜂視覺。

**參考**：`BugEzy Popup 完整主畫面.dc.html` 畫面 21、22

**畫面 21 麥克風關閉提示**：
- `rgba(14,12,8,.86)` 遮罩
- 264px 黃卡 + 麥克風圖示 48px

**畫面 22 JSON 免責警語**：
- 270px 咖啡卡 + 黃色警示三角（脈衝動畫）

**驗收條件**：
1. 兩個 overlay 視覺正確
2. 動畫效果正常

完成後在下方寫 `🔵 DONE-420`。

🔵 **DONE-420**

**改動**：`popup.html`（兩個 overlay 的樣式 + markup）

| 驗收條件 | 結果 |
|---|---|
| 1. 兩個 overlay 視覺正確 | ✅ 21 = 264px 黃卡 + 48px 六角麥克風；22 = 270px 咖啡卡 + 黃警示三角 |
| 2. 動畫效果正常 | ✅ 警示三角走 §8 `pulse` 1.6s |

**21 麥克風關閉提示**：遮罩 `rgba(14,12,8,.86)`、264px 黃卡 + 2px 黑框 + `4px 4px 0` 硬投影。圖示是 §6.1 的四層麥克風（收音孔膠囊頭 + U 形吊架 + 底座）裝在 48px 黑六角裡 —— 跟品牌標記同一套語彙的放大版。兩顆按鈕改成主／次要按鈕，收進一個 `.mic-prompt-actions` 讓間距用 `gap` 而不是逐一 `margin`（§10）。

**22 JSON 免責警語**：它跟額度 overlay **共用 `.upgrade-overlay` 遮罩但卡片不同**。PM-419 把 `.upgrade-overlay-card` 改成黃底了，而黃底在這套系統裡代表「這裡要你做決定」；免責警語是系統在跟你說話，該是咖啡色（§2.3）。所以另開一個 `.warn-card` 修飾 class 蓋掉底色、邊框與寬度，遮罩與定位仍然共用 —— 不複製一份 overlay 出來。

**`⚠️` 換成 `clip-path` 三角**（§6 警示），配 §8 的 `pulse`（1.6s）。這裡刻意**沒有**在三角上加 `box-shadow` 或 `border` —— §5 明講 `clip-path` 會把兩者裁掉，那個 bug 犯過兩次。

**回歸**：`_verify403` 93/0、`_verify309` 151/0、`_verify383` 71/0；`tsc --noEmit` 無錯。


---

### 🟡 PM-421：Popup 全畫面端到端驗收 + Extension 圖示更新

**前提**：Extension 重新載入

**執行**：
1. Extension 圖示更新：128/48/32/16 四個尺寸（六角斜紋）
2. 跑全套端到端驗收
3. 手動測試所有 popup 狀態切換（登入→閒置→錄製→完成→偵察）

**收工**：
- CHANGELOG 加大黃蜂視覺系統群組 A 區塊
- git commit + push

**驗收條件**：
1. 全套端到端 0 failed
2. Extension 圖示是新的六角大黃蜂
3. 所有 popup 畫面配色正確
4. 無殘留 emoji
5. 所有功能正常
6. git push 成功

完成後在下方寫 `🔵 DONE-421`。

