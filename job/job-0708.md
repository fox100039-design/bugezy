# job-0708.md — BugEzy Day 23

> 接續 job-0707.md（PM-187~200）

---

### 🟡 PM-201：BugEzy SKILL.md — AI 客服手冊（使用說明 + Q&A + 故障排除）

**背景**：使用者遇到問題時，不需要找客服——直接問自己的 AI，AI 讀 SKILL.md 就能回答一切。等於讓 AI 當 24 小時客服。

**放置位置**：
- 官網 `bugezy.dev/skill` 提供檢視 + 一鍵複製
- 專案根目錄 `SKILL.md`（使用者也可以手動放）
- Claude Code 用戶可放到 `/mnt/skills/user/bugezy/SKILL.md`

---

#### §1 建立 SKILL.md 檔案

在 `C:\dev\bugezy\SKILL.md` 建立以下內容：

```markdown
# BugEzy — AI 除錯工具使用指南

> 這份文件是給 AI 讀的。當使用者問你關於 BugEzy 的任何問題，請根據以下內容回答。

## 什麼是 BugEzy

BugEzy 是一個 Chrome 擴充工具 + MCP server，讓開發者用語音 + 一鍵錄製 Bug，AI 透過 MCP 直接讀取報告並修復。

- 官網：https://bugezy.dev
- Chrome Web Store：https://chromewebstore.google.com/detail/bugezy/hfnkjlbbpehkflgfbjenfmnmjkdjadcj
- 安裝教學：https://bugezy.dev/install
- 功能說明：https://bugezy.dev/features
- FAQ：https://bugezy.dev/faq
- 隱私政策：https://bugezy.dev/privacy
- 問題回報：https://bugezy.dev/feedback

## 如何讀取使用者的 Bug 報告

### 方法 1：使用者說「讀我最新報告」
1. 呼叫 MCP tool `list_reports`（需要 user_email + session_token）
2. 取得最新的 report_id
3. 呼叫 `get_timeline`（report_id）→ 取得完整時間軸 + AI 導航摘要
4. 根據摘要的根因分析，定位程式碼 → 提出修復方案

### 方法 2：使用者貼報告連結
使用者貼 `https://bugezy.dev/report/xxx-xxx` 給你時：
1. 從 URL 取得 report_id
2. 呼叫 `get_timeline`（report_id）→ 同上

### MCP 工具清單（13 個）
| Tool | 用途 |
|---|---|
| `list_reports` | 列出使用者的報告（需 session_token） |
| `get_report_overview` | 報告概覽 + AI Bug 導航摘要 |
| `get_timeline` | ⭐ 最推薦 — 完整時間軸（Console + Network + 語音 + 環境，一次看完） |
| `get_console_logs` | Console error/warn 記錄 |
| `get_network_errors` | Network 4xx/5xx 失敗 |
| `get_screenshots` | 截圖（高 Token，謹慎使用） |
| `get_rrweb_events` | DOM 錄影事件（高 Token，謹慎使用） |
| `get_rrweb_summary` | DOM 摘要（輕量） |
| `get_voice_transcript` | 語音轉錄文字 |
| `get_metadata` | 報告 metadata |
| `get_live_errors` | 即時監控錯誤（需 session_token） |
| `get_terminal_logs` | Terminal CLI 錯誤（需 session_token，付費功能） |
| `get_usage_stats` | Token 用量統計 |

### 建議的讀取順序
1. 先呼叫 `get_timeline` — 一次拿到 AI 導航摘要 + 完整時間軸（最省 Token）
2. 如果需要更多細節，再呼叫 `get_console_logs` 或 `get_network_errors`
3. 截圖和 DOM 錄影 Token 消耗高，最後再用

## 如何教使用者錄製 Bug

當使用者說「程式壞了」但沒有報告時，教他：

1. 點 Chrome 右上角 BugEzy 圖示（紫色 B）
2. 按「🎬 錄製」按鈕
3. 操作壞掉的步驟（BugEzy 會自動捕捉 Console 錯誤、Network 失敗、DOM 變化）
4. 邊操作邊用語音描述問題（可選）
5. 按「⏹ 停止」
6. 在編輯頁補充說明（可選）→ 按「上傳」
7. 跟你說「讀我最新報告」

### 六種錄製模式
| 模式 | 適合場景 |
|---|---|
| 🎬 錄製 | 操作流程中的 Bug |
| ⏪ 回溯 30s | Bug 已經發生了，回溯最近 30 秒 |
| 📸 截圖標注 | 畫面問題（UI 破版、樣式錯誤） |
| 🟢 即時監控 | 背景持續監控，有錯誤時通知 |
| 🐍 Terminal CLI | Python/Node.js 後端錯誤 |
| 🤖 MCP AI 讀取 | AI 直接透過 MCP 讀取報告 |

## 故障排除

### AI 無法讀取報告（MCP 連線問題）
**症狀**：呼叫 `list_reports` 回傳「請提供 session_token」

**解法**：
1. 告訴使用者：「請打開 BugEzy popup → 進階設定 → 📋 複製 MCP 設定 → 貼給我」
2. 使用者會貼一段 JSON，裡面有帶 token 的 MCP URL
3. 用這個 URL 重新設定 MCP 連線

### 麥克風無法開啟
**症狀**：錄製時音量條不跳動

**解法**：
1. 確認瀏覽器有授權麥克風（網址列左邊的 🔒 圖示 → 麥克風 → 允許）
2. 如果使用精準轉錄模式，需要選「**允許這個網站使用**」而不是「允許這次使用」
3. 如果還是不行，切換到「即時字幕」模式（popup 進階設定）

### 語音沒有文字
**症狀**：錄製時音量條有跳動，但停止後沒有文字

**解法**：
1. 確認網路連線正常（精準轉錄需要上傳音訊到 server）
2. 確認是付費用戶（精準轉錄 Whisper 是付費功能）
3. 免費用戶可用「即時字幕」模式（Web Speech API，不需上傳）

### 免費額度用完
**症狀**：按錄製彈出「本月額度已用完」

**解法**：
- 免費版每月限制：錄製 10 次 / 回溯 5 次 / AI 讀取 20 次 / 截圖無限
- 額度每月自動重置
- 升級方式：日票 NT$20（24 小時無限）或月費 NT$80/月
- 在 popup 按「⚡ 日票」或「✨ 月費」升級
- 目前只支援台灣付款（信用卡/ATM/超商），國際付款即將開放

### 截圖有敏感資料
**症狀**：截圖可能拍到密碼、API Key

**說明**：
- BugEzy 會自動偵測頁面上的密碼欄位，截圖後自動馬賽克
- 使用者也可以用 🔒 馬賽克筆刷手動塗掉敏感區域
- localStorage/sessionStorage 的敏感值（token、password、API key）會在使用者端自動遮罩，server 永遠不碰原值

### Terminal CLI 使用
**適用**：Python / Node.js / Go 後端錯誤

```bash
BUGEZY_TOKEN=<token> npx bugezy-watch -- python manage.py runserver
BUGEZY_TOKEN=<token> npx bugezy-watch -- node server.js
```

- Token 從 popup 進階設定的「📋 複製 MCP 設定」取得
- 終端機 CLI 是付費功能
- AI 用 `get_terminal_logs` 讀取，會拿到結構化的 Python traceback + 環境快照 + 白話錯誤解釋

## BugEzy 能捕捉什麼

### 前端（Chrome 擴充自動捕捉）
- JS 執行錯誤（TypeError / ReferenceError / SyntaxError）
- Promise 靜默失敗（未捕捉的 async/await 錯誤）
- Console 警告（CORS / Mixed Content / Deprecated API）
- Network 失敗（API 4xx/5xx / timeout / CORS blocked）
- 資源載入失敗（圖片/CSS/JS/字型 404）
- Web Vitals 效能（LCP / CLS / FID 超標警告）
- 網路環境快照（WiFi/4G/離線/延遲/頻寬）
- 儲存空間快照（localStorage / sessionStorage / Cookie，敏感值自動遮罩）
- DOM 變化（rrweb 全紀錄）
- 語音描述（Whisper 精準轉錄 / Web Speech 即時字幕）
- 截圖標注（全頁/區域/自由形狀 + 馬賽克筆刷）

### 後端（Terminal CLI）
- Python traceback / exception（結構化解析：type/message/file/line）
- Node.js uncaughtException / unhandledRejection
- 任何語言的 stderr / crash log
- 環境快照（Python 版本 + pip list / Node 版本 + npm list）
- 敏感資料自動遮罩（DB URI / API Key / JWT / 密碼）

### 支援框架
前端：React · Vue · Angular · Next.js · Nuxt · Svelte · 任何 Web 應用
後端：Django · Flask · FastAPI · Express · Nest.js · 任何語言

## 定價
| 方案 | 價格 | 內容 |
|---|---|---|
| 免費版 | NT$0 | 錄製 10 次/月 · 回溯 5 次/月 · AI 讀取 20 次/月 · 截圖無限 · 報告保留 7 天 |
| 日票 | NT$20 | 24 小時無限 · Whisper 精準轉錄 · 報告保留 90 天 |
| 月費 | NT$80/月 | 全部無限 · Whisper · Terminal CLI · 報告保留 90 天 |

目前只支援台灣付款。國際付款即將開放。
```

---

#### §2 Server：新增 /skill 頁面

新增 `GET /skill` 端點，顯示 SKILL.md 內容 + 一鍵複製按鈕。

頁面設計：
- 標題：「🤖 BugEzy AI 客服手冊」
- 說明：「把這份文件放到你的專案裡，AI 就會教你怎麼用 BugEzy」
- 顯示 SKILL.md 全文（server 端渲染 markdown → HTML）
- 一鍵複製按鈕（複製原始 markdown 文字）
- 下載按鈕（下載 SKILL.md 檔案）
- 中英文（getLang）

路由：
```ts
if (method === 'GET' && path === '/skill') {
  return skillPage(getLang(request), request);
}
```

---

#### §3 全站 footer 加連結

所有頁面 footer 加「🤖 AI 客服手冊」連結到 /skill。

---

#### §4 sitemap.xml 加 /skill

---

#### §5 不改的東西

- extension / MCP / ECPay：不動
- 現有頁面內容：不動（只加 footer 連結）

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. `bugezy.dev/skill` → 顯示完整 AI 客服手冊
2. 一鍵複製 → 貼到記事本有完整 markdown
3. 全站 footer 有「🤖 AI 客服手冊」
4. sitemap.xml 有 /skill
5. 中英文切換正常

---

## DONE-201 — Claude Code 回報（2026-07-08）

**BugEzy SKILL.md — AI 客服手冊** — 根目錄 SKILL.md + server `/skill` 檢視頁 + `/skill/download` 下載 + 全站 footer + sitemap。

### §1 SKILL.md（`C:\dev\bugezy\SKILL.md`）
- 依規格建立完整內容：什麼是 BugEzy + 官網連結、如何讀報告（方法 1/2）、MCP 13 工具清單表、建議讀取順序、如何教錄製、六種模式表、故障排除（MCP 連線/麥克風/語音無文字/額度/敏感資料/Terminal CLI）、能捕捉什麼（前端/後端/框架）、定價表。
- **Worker 無檔案系統**，故 server 端內嵌同一份為 `SKILL_MD` 常數（`/skill` 渲染 + `/skill/download` 皆用它，單一來源與根目錄檔一致）。

### §2 `GET /skill`（`skillPage`，`server/index.ts`）
- 標題「🤖 BugEzy AI 客服手冊」+ 說明「把這份文件放到你的專案裡，AI 就會教你怎麼用 BugEzy…等於讓 AI 當 24 小時客服」。
- **極簡 Markdown→HTML 渲染器** `renderMarkdown()`（`skillEsc`/`skillInline`）：涵蓋標題/表格/有序無序清單/引言/```fenced code```/`**粗體**`/`` `行內碼` ``/連結/`---` 分隔線——SKILL.md 全文渲染成排版 HTML（含 13 工具表、六模式表、定價表）。
- **一鍵複製**：`data-copy-text="${encodeURIComponent(SKILL_MD)}"` + inline script（`navigator.clipboard` → 失敗 fallback 視窗內 1px textarea `execCommand`，沿用 PM-192 教訓）→ 按鈕變「✅ 已複製！」2s 恢復。`/skill` 走 `html()`（CSP `script-src 'self' 'unsafe-inline'` 允許 inline script）。
- **下載按鈕** → `/skill/download`。
- **Claude Desktop 安裝步驟教學**（install-box）：Settings → Skills → Add；Claude Code 放 `/mnt/skills/user/bugezy/SKILL.md`；Cursor/VS Code/其他放專案根目錄或直接貼全文。
- 中英雙語（`getLang` + `?lang=` 切換 + 語言切換鈕）。

### §2b `GET /skill/download`
- 回 `SKILL_MD`，`Content-Type: text/markdown; charset=utf-8` + **`Content-Disposition: attachment; filename="SKILL.md"`**（瀏覽器直接下載）。

### §3 全站 footer
- 7 頁 footer（首頁 inline `|` 格式 + guide/faq/features/changelog/privacy/install 標準格式）皆加「🤖 AI 客服手冊 / 🤖 AI Manual」→ `/skill`（線上實測每頁 `href="/skill"` count=1）。skillPage 自身 footer 亦含。

### §4 sitemap.xml
- 加 `['/skill', 'monthly', '0.5']`（線上 sitemap 含 `/skill`）。

### §5 不動
- extension / MCP / ECPay / 現有頁面內容 → 皆未改（只加 footer 連結 + 新頁/端點/sitemap 一列）。

### 踩雷 & 修正
- **SKILL_MD 內含大量 ``` 反引號**（13 工具名行內碼 + bash fence）：template literal 內須 escape。首版誤寫成「3 反斜線+反引號」→ 渲染出 `` \`list_reports\` ``（多餘反斜線）；用 Python 腳本一次修 50 處為「1 反斜線+反引號」→ 正確渲染為 `` `list_reports` ``。線上 `/skill/download` 實測反引號乾淨無殘留反斜線。

### 驗收（線上實測 `bugezy.dev`，`wrangler deploy a4879590`）
1. `/skill` → 完整手冊渲染（`<td><code>get_timeline</code></td>` 等表格 + 行內碼正確）✅
2. `/skill/download` → GET 回 `text/markdown` + `attachment; filename="SKILL.md"`、7092 bytes、反引號乾淨 ✅
3. 一鍵複製鈕 `data-copy-text` present ✅
4. 全站 7 頁 footer `href="/skill"` 皆 count=1 ✅
5. sitemap.xml 含 `/skill` ✅
6. 中英：`?lang=en` → 「AI Support Manual / Copy all / Download SKILL.md」皆 present ✅

### Deploy
- `tsc --noEmit` clean → `wrangler deploy` Version `a4879590`（cron `0 3 * * *` 保留）。純 server + 根目錄 SKILL.md，extension 不動。

---

### 🟡 PM-202：首頁強化 — 加入 AI Skill 專區 + 特點重整

**背景**：BugEzy 現在有四大殺手級特色——六種模式 + MCP 13 工具 + 語音 + AI Skill。但首頁沒有提到 Skill，AI 或使用者看首頁時不知道 BugEzy 還有專屬的 AI 客服手冊可裝。首頁要讓人一眼看到就驚艷。

---

#### §1 首頁新增「🤖 AI Skill」區塊

在 MCP 區塊之後，新增獨立區塊：

**中文版**：
```
🤖 專屬 AI Skill — 讓 AI 當你的 24 小時客服

安裝 BugEzy 的 AI Skill 後，你的 AI 就會：
• 教你怎麼錄製 Bug
• 自動讀取報告並分析根因
• 遇到問題時引導你排除故障
• 告訴你每個功能怎麼用

不需要讀文件、不需要看教學影片——問你的 AI 就好。

[下載 SKILL.md →]（連到 /skill/download）
[了解更多 →]（連到 /skill）
```

**英文版**：
```
🤖 AI Skill — Let AI Be Your 24/7 Support

Install BugEzy's AI Skill, and your AI will:
• Teach you how to record bugs
• Auto-read reports and analyze root causes
• Guide you through troubleshooting
• Explain every feature

No docs, no tutorials — just ask your AI.

[Download SKILL.md →]  [Learn more →]
```

---

#### §2 首頁 Hero 副標題強化

更新為四大特色並列：

中文：`六種錄製模式 × 13 個 MCP AI 工具 × 語音辨識 × AI Skill — 捕捉 95% 以上的 Web Bug`
英文：`6 Recording Modes × 13 MCP AI Tools × Voice Recognition × AI Skill — Catches 95%+ of Web Bugs`

---

#### §3 捕捉能力列表尾部加 Skill 提示

在「BugEzy 能捕捉什麼？」列表最後加：

中文：`📘 以上所有功能的使用說明、故障排除、Q&A，都在 AI Skill 手冊裡。安裝後你的 AI 就會教你怎麼用。`
英文：`📘 Usage guides, troubleshooting & Q&A for all features above are in the AI Skill manual. Install it and your AI will teach you.`

加「下載 SKILL.md」連結。

---

#### §4 不改的東西

- /skill 頁面：不動（PM-201 已完成）
- extension / MCP / ECPay：不動

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. 首頁有「🤖 專屬 AI Skill」獨立區塊
2. Hero 副標題有四大特色
3. 捕捉能力尾部有 Skill 提示
4. 下載連到 /skill/download、了解更多連到 /skill
5. 中英文正確

---

## DONE-202 — Claude Code 回報（2026-07-08）

**首頁強化 — 加入 AI Skill 專區 + 特點重整** — 純 server（`homePage`），只加內容不動既有結構。

### §1 首頁「🤖 專屬 AI Skill」獨立區塊
- 位置：**MCP 區塊（frameworks／支援所有 MCP 工具）之後、「讓 AI 幫你安裝」之前**，新增 `<section id="skill">` + `.skill-box`（紫漸層卡片）。
- 內容：標題「🤖 專屬 AI Skill — 讓 AI 當你的 24 小時客服」+ 四項清單（教你錄製 / 自動讀報告分析根因 / 引導排除故障 / 說明每個功能）+ 「不需要讀文件…問你的 AI 就好」+ 兩顆 CTA：**「下載 SKILL.md →」→ `/skill/download`**、**「了解更多 →」→ `/skill`**。
- 新增 CSS `.skill-box`/`.skill-list`/`.skill-btn`（+secondary）。

### §2 Hero 副標題四大特色並列
- tagline 第二行改為：中「六種錄製模式 × 13 個 MCP AI 工具 × 語音辨識 × AI Skill — 捕捉 95% 以上的 Web Bug」／英「6 Recording Modes × 13 MCP AI Tools × Voice Recognition × AI Skill — Catches 95%+ of Web Bugs」（保留品牌首行）。

### §3 捕捉能力列表尾部 Skill 提示
- 「🔍 BugEzy 能捕捉什麼？」區塊 capture-grid 之後加 `.capture-skill-note`：「📘 以上所有功能的使用說明、故障排除、Q&A，都在 AI Skill 手冊裡。安裝後你的 AI 就會教你怎麼用。」+ **「下載 SKILL.md →」連結 → `/skill/download`**。

### §4 不動（已確認）
- `/skill` 頁面（PM-201）、extension / MCP / ECPay → 皆未改。

### 驗收（線上實測 `bugezy.dev`，中英，`?lang=zh` / `?lang=en`）
1. §1：`id="skill"`/`class="skill-box"`，中「專屬 AI Skill — 讓 AI 當你的 24 小時客服」+ 四項清單、英「Let AI Be Your 24/7 Support」+「Teach you how to record bugs」皆 present ✅
2. §2：中「六種錄製模式 × 13 個 MCP AI 工具 × 語音辨識 × AI Skill」/ 英「6 Recording Modes × 13 MCP AI Tools × Voice Recognition × AI Skill」✅
3. §3：`capture-skill-note`，中「以上所有功能的使用說明、故障排除」/ 英「are in the AI Skill manual. Install it and your AI will teach you」✅
4. 首頁 2 處 `href="/skill/download"`（skill-box 按鈕 + capture 提示）+ 「了解更多」`/skill` ✅
5. 中英雙語皆正確（`t()`；zh 以 Python UTF-8 實測，非 Git-Bash grep）✅

### 設計/驗證註記
- 首頁回應 `Cache-Control: no-store`，但部署後純網址曾短暫命中 Cloudflare 邊緣舊快取（PM-201 版）→ 加 `?cb=` cache-buster 即見新內容，屬傳播延遲非程式問題。
- 中文驗證用 `?lang=zh`（`getLang` 無 Accept-Language 預設 en），並以 UTF-8 Python 腳本比對（Git-Bash grep/print 對中文有 cp950 亂碼，不可靠）。

### Deploy
- `tsc --noEmit` clean → `wrangler deploy` Version `b76c3bf1`（cron `0 3 * * *` 保留）。

---

## DONE-203 — Claude Code 回報（2026-07-08）

**manifest 更新 name + description + version 1.1.2 + /api/version 同步** — extension manifest + server 版號。

### 改動
- **`extension/manifest.json`**：
  - `name` `BugEzy` → **`BugEzy — AI Bug Reporter | 語音除錯工具`**（33 字，CWS 上限 45，OK）。
  - `description` → **`Voice-powered bug reporter with MCP AI integration — record DOM, Console, Network + voice in one click. AI reads and fixes your bugs. 語音 Bug 回報，AI 一鍵分析修復。6 modes, 13 MCP tools, AI Skill.`**
  - `version` `1.1.1` → **`1.1.2`**。
- **`server/src/index.ts` `GET /api/version`**：`latest` `1.1.1` → **`1.1.2`**。

### ⚠ description 132 字上限 — 已解決（DONE-203b）
- 初版 description 186 字元超過 Chrome Web Store 132 上限；**FOX 指定精簡版已套用**：`Voice-powered bug reporter with MCP AI — record bugs, AI analyzes and fixes. 6 modes, 13 tools. 語音 Bug 回報，AI 一鍵修復.`（**114 字元，符合 ≤132**）。`npm run build` ✅（dist manifest 確認），version 維持 1.1.2。

### 驗收
1. dist `manifest.json`：name/version(1.1.2)/description 皆已更新 ✅
2. server：workers.dev 直連 `/api/version` → `{"latest":"1.1.2"}`（權威來源）✅；`bugezy.dev/api/version` 邊緣快取過期後亦回 `1.1.2` ✅
3. `tsc --noEmit` clean。

### Build / Deploy
- extension：`npm run build` ✅（dist manifest 確認）。**待 FOX 重上架**（先處理 description 132 字上限）。
- server：`wrangler deploy` Version `9fe349df`（cron `0 3 * * *` 保留）。

---

### 🟡 PM-204：截圖流程統一 — 標注完導到編輯報告頁（跟錄製一致）

**問題**：錄製停止後有完整的編輯報告頁（回放 + 語音 + 描述 + Console + Network + Token + AI 校正 + 確認後上傳），但截圖標注完直接上傳，使用者沒有機會檢查、補充、修改。

**目標**：截圖標注完成後，不直接上傳，而是導到 edit-report 頁讓使用者確認後再上傳。跟錄製的流程一致。

---

#### §1 annotate.ts：標注完成後不直接上傳

現在 annotate.ts 的「📤 上傳報告」按鈕直接呼叫 API 上傳。改為：

1. 把截圖 dataUrl + 語音文字 + console/network + 描述 存到 chrome.storage.local（臨時）
2. 跳到 edit-report.html

```ts
// 舊：直接上傳
submitBtn.addEventListener('click', async () => {
  await uploadReport(payload);
});

// 新：存資料 → 跳到 edit-report
submitBtn.addEventListener('click', async () => {
  // 組裝報告資料（跟錄製停止後一樣的格式）
  const reportData = {
    type: 'screenshot',
    screenshots: [{ dataUrl: canvasDataUrl, description: descInput.value }],
    consoleLogs: collectedConsoleLogs,
    networkErrors: collectedNetworkErrors,
    voiceTranscript: voiceText ? [{ text: voiceText, timestamp: Date.now() }] : [],
    url: pageUrl,
    title: pageTitle,
    networkSnapshot: networkSnapshot,
    storageSnapshot: storageSnapshot,
    // 沒有 rrweb（截圖不錄 DOM）
    rrwebEvents: [],
  };
  
  await chrome.storage.local.set({ 'bugezy:pending-report': reportData });
  chrome.tabs.create({ url: chrome.runtime.getURL('edit-report.html') });
});
```

---

#### §2 edit-report.html / edit-report.ts：支援截圖報告

edit-report 目前只處理錄製報告（有 rrweb 播放器）。加判斷：

```ts
const pendingReport = await chrome.storage.local.get('bugezy:pending-report');
const data = pendingReport['bugezy:pending-report'];

if (data.type === 'screenshot') {
  // 截圖報告：顯示截圖預覽（取代 rrweb 播放器）
  const img = document.createElement('img');
  img.src = data.screenshots[0].dataUrl;
  img.style.cssText = 'width:100%; border-radius:8px;';
  playerArea.replaceWith(img);
} else {
  // 錄製報告：顯示 rrweb 播放器（現有邏輯不動）
}
```

其他區塊（語音記錄 / 補充說明 / Token 估算 / AI 校正 / AI 精簡 / 上傳按鈕）完全複用，不用改。

---

#### §3 annotate.ts 的「上傳報告」按鈕文字改成「下一步」

```ts
// 舊
submitBtn.textContent = '📤 上傳報告';
// 新
submitBtn.textContent = '📤 下一步';
```

i18n：
```ts
'annotate-next': { zh: '📤 下一步', en: '📤 Next' },
```

---

#### §4 不改的東西

- edit-report 的上傳邏輯：不動（它已有上傳功能）
- 錄製流程：不動
- server / MCP：不動

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 截圖 → 標注 → 按「下一步」→ 跳到編輯報告頁
2. 編輯報告頁顯示截圖預覽（取代 rrweb 播放器）
3. 語音記錄 / 補充說明 / Token / AI 校正 / 上傳 都正常
4. 上傳後顯示報告連結 + 複製按鈕
5. 錄製流程不受影響（仍有 rrweb 播放器）

---

## DONE-204 — Claude Code 回報（2026-07-08）

**截圖流程統一 — 標注完導到編輯報告頁（跟錄製一致）** — 純 extension。

### §1+§3 annotate.ts：標注完不直接上傳 → 存暫存 + 導到編輯頁
- `saveBtn`（原「✅ 完成儲存」直接 `POST /api/reports`）改為：組好與現行**完全相同**的截圖 payload（`screenshots`/`consoleLogs`/`networkErrors`/`networkSnapshot`/`pageInfo`/`allow_screenshot_images`/`user_id`/`rrwebEvents:[]`/`voiceTranscript:[]`/`description`）→ **存進 `STORAGE_KEY`（`bugezy:lastPayload`，與錄製報告同一入口）** + 清 `STATE_KEY`（避免殘留錄製摘要）+ 清截圖暫存底圖 → `chrome.tabs.create(getURL('edit-report.html'))` → 關閉標注頁。移除 annotate 內的 `fetch /api/reports` 與 `SCREENSHOT_UPLOADED`（改由編輯頁上傳）。
- 按鈕文字改「📤 下一步」（`data-i18n="annotate-next"`，i18n 中英 `📤 下一步 / 📤 Next`）。
- Whisper 語音轉錄（`/api/transcribe`）不動；annotate 語音本就寫入 `descInput`（描述），隨 payload.description 帶到編輯頁。

### §2 edit-report.ts：判斷報告類型 → 截圖顯示預覽
- `init()` 加 `isScreenshot = !rrwebEvents.length && screenshots.length>0`（無需新增 server 可見欄位，靠既有結構判別）。
- **截圖**：`showScreenshotPreview()` 把 `#miniPlayer` 換成 `<img>` 截圖預覽、標題改「📸 截圖預覽」、隱藏播放/標記控制列與 markerList；摘要列改「截圖 N / Console / Network / 頁面」（去掉 DOM 事件/語音片段/時長）；**不呼叫 `initMiniPlayer`**。
- **錄製**：走原 `initMiniPlayer`（rrweb 播放器）——完全不動。
- 另加：`init` 把 `payload.description` 載入補充說明欄（避免上傳時 background 用空值覆寫 annotate 帶入的描述；錄製 payload.description 為空，無影響）。
- 其餘區塊（語音記錄 / 補充說明 / Token 估算 / AI 校正 / AI 精簡 / 上傳按鈕 + PM-199 複製鈕）**完全複用**，未改。

### §4 不動（已確認）
- edit-report 上傳邏輯（`uploadBtn`→`UPLOAD_REPORT`→background `uploadReport` 讀 `STORAGE_KEY` POST）不動 → 截圖報告沿用同一條上傳路徑。
- 錄製流程、server / MCP：不動。
- `types.ts` `RecordingPayload` 加 3 個**選填**欄位（`screenshots?`/`description?`/`allow_screenshot_images?`）供 TS 存取，向後相容。

### 設計註記（與規格差異，已說明）
- 規格 §1/§2 用新 key `bugezy:pending-report`；實作改存 **既有 `STORAGE_KEY`**——因規格 §4 要求「edit-report 上傳邏輯不動」，而該邏輯（background `uploadReport`）本就讀 `STORAGE_KEY`。用同一 key 才能同時滿足「不改上傳邏輯」+「編輯頁載入」，且與錄製報告共用單一入口，最一致。
- 報告類型判別用「無 rrweb + 有 screenshots」啟發式，**不新增 server 可見欄位** → 上傳 payload 與原截圖直傳完全相同，server 零風險。

### 驗收（build wiring + 邏輯）
1. 截圖→標注→「📤 下一步」→ 存 `STORAGE_KEY` + 開 `edit-report.html`（dist `annotate.js` 含 `getURL("edit-report.html")`、`/api/reports` 上傳 **0 筆**、`SCREENSHOT_UPLOADED` **0 筆**、`/api/transcribe` 保留）✅
2. 編輯頁截圖預覽取代播放器（dist `edit-report.js` 含 `isScreenshot`/`showScreenshotPreview`）✅
3. 語音/補充說明/Token/AI 校正/AI 精簡/上傳 完全複用 ✅
4. 上傳後報告連結 + 複製鈕（PM-199 既有，複用）✅
5. 錄製流程不受影響（`isScreenshot=false` 走原 `initMiniPlayer`）✅
6. i18n 中英 `annotate-next`（dist annotate.js/html 皆含）✅

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收（截圖→下一步→編輯頁預覽→上傳）。

---

### 🟡 PM-205：截圖標注頁加綠色音量跳動 bar

**目標**：annotate 頁 Whisper 錄音提示（「🔴 Whisper 錄音中，講完按 ⏹ 轉錄…」）旁加 5 條綠色音量視覺化 bar（與 popup/inject 錄製中音量條一致），AnalyserNode + requestAnimationFrame 驅動。i18n 不需要（純視覺）。

---

## DONE-205 — Claude Code 回報（2026-07-08）

**截圖標注頁 Whisper 錄音音量條** — 純 extension（annotate）。

### 改動
- **`annotate.html`**：`#liveCaptions`（Whisper 錄音提示浮層）改 flex 置中，內加 `<span id="volBars" class="hidden">` + 5 個 `.vol-bar`（置於 `#captionText` 前，即提示文字旁）。CSS：`#volBars`（`display:flex;align-items:flex-end;gap:2px;height:20px`）+ `.vol-bar`（`width:4px;height:4px;background:#ef4444;border-radius:2px;transition:height .12s`）——**與 popup/inject 5 條音量條同規格**。
- **`annotate.ts`**：
  - `startVolumeMeter(stream)`：對 `whisperStream` 建 `AudioContext` +（`suspended` 時 `resume()`，PM-192 同理）+ `AnalyserNode`（`fftSize:256`）→ `requestAnimationFrame` 迴圈：`getByteFrequencyData` 算 `level=min(avg/128,1)` → `updateVolBars(level)`（**與 inject 同公式**：第 i 條 `threshold=(i+1)/5`，過門檻 `height=4+16*level+jitter` 上限 20px，`level>0.3` 綠 `#3fb950` 否則紅 `#ef4444`）。顯示 `#volBars`。
  - `stopVolumeMeter()`：`cancelAnimationFrame` + 關 `AudioContext` + 隱藏並重置 5 條為矮紅。
  - 接線：`startWhisper()` 錄音啟動後 `void startVolumeMeter(whisperStream)`；`stopWhisper()` 開頭 `stopVolumeMeter()`（按 ⏹ 當下即停）。
  - 只在 **Whisper 模式**顯示（Web Speech 免費路徑不觸發，符合「Whisper 錄音提示旁」）。

### 位置說明
- bar 放在 `#liveCaptions` 浮層內、緊鄰「🔴 Whisper 錄音中…」提示文字左側（即該提示旁邊）——符合規格「在 Whisper 錄音提示旁邊」。

### 不動
- Whisper 轉錄邏輯 / Web Speech / 截圖標注 / 上傳流程（PM-204）/ server / MCP → 皆未改。

### 驗收（build wiring + 邏輯）
1. Whisper 錄音時提示旁出現 5 條音量條，講話綠色跳動、安靜矮紅 ✅（dist `annotate.js` 含 `startVolumeMeter`/`stopVolumeMeter`/`createAnalyser`/`getByteFrequencyData`/`requestAnimationFrame`；`annotate.html` 含 `id="volBars"` + `vol-bar`）
2. 按 ⏹ 停止 → 音量條隱藏並重置 ✅
3. 免費 Web Speech 模式不顯示音量條 ✅
4. i18n 不需要（純視覺）✅

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收。

---

### 🟡 PM-206：截圖標注頁語音存入 voiceTranscript

**問題**：annotate.ts 存檔 payload 的 `voiceTranscript` 永遠是空陣列 → 截圖報告的語音只進了 description，MCP get_timeline / 報告頁語音面板看不到語音事件。

**修法**：把 Whisper/Web Speech 錄到的語音文字也存入 `voiceTranscript`（格式同錄製報告 `[{ text, timestamp }]`）。並確認 PM-205 音量條仍正常。

---

## DONE-206 — Claude Code 回報（2026-07-08）

**截圖標注頁語音存入 voiceTranscript** — 純 extension（annotate）。

### 改動（`annotate.ts`）
- 新增 module 變數 `let voiceAccumulated = ''`——累積本次標注 session 實際錄到的語音文字。
- **Web Speech**：`onresult` final 分支 `descInput.value += text` 之後同步 `voiceAccumulated += text`。
- **Whisper**：轉錄成功 `descInput.value += … data.text` 之後同步 `voiceAccumulated += (voiceAccumulated ? ' ' : '') + data.text`。
- **存檔 payload**：`voiceTranscript` 由永遠 `[]` 改為——有語音才存 `[{ text: voiceAccumulated.trim(), timestamp: Date.now(), isFinal: true, source: useWhisper ? 'whisper' : 'web-speech' }]`（**格式同錄製報告 `VoiceSegment`**：補 `isFinal`/`source` 讓與 background RECORDING_DONE 合併的 whisper 段落一致），無語音則維持 `[]`。

### 設計說明
- 用「語音累積器」而非直接鏡射 `descInput.value`：`voiceTranscript` 精確反映**實際口說內容**，不把使用者手打的文字誤標為語音；語音本就同時 append 進 `descInput`（描述），故與 description 同步（描述含語音+手打，voiceTranscript 只含語音）。
- 結果：截圖報告的語音事件出現在報告頁語音面板 + `get_timeline` 時間軸（與錄製報告一致）。編輯頁（PM-204）會把 voiceTranscript 載入「語音記錄」欄、description 載入「補充說明」欄。

### PM-205 音量條複查（未受影響）
- dist `annotate.js` 仍含 `startVolumeMeter`/`stopVolumeMeter`/`createAnalyser`/`getByteFrequencyData`/`requestAnimationFrame`；`annotate.html` 仍含 `#volBars` + `.vol-bar` → 音量條邏輯完整（AnalyserNode + rAF 驅動 5 條，講話綠跳動）。本卡只加語音累積，未動 PM-205 任何程式。實機跳動待 FOX 重上架驗收（與所有 extension 改動同）。

### 不動
- 音量條（PM-205）、Whisper/Web Speech 轉錄邏輯、截圖上傳流程（PM-204）、server / MCP → 皆未改。

### 驗收（build wiring + 邏輯）
1. 截圖標注錄語音（Whisper 或 Web Speech）→ 存檔 payload `voiceTranscript` 含 `[{text,timestamp,isFinal,source}]`（dist 含 `voiceAccumulated`/`isFinal`/`web-speech`/`whisper`）✅
2. 無語音（純打字/無錄音）→ `voiceTranscript` 為 `[]` ✅
3. MCP get_timeline / 報告頁語音面板可見語音事件（voiceTranscript 已填）✅（邏輯）
4. PM-205 音量條 wiring 完整、未受影響 ✅

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收。

---

### 🟡 PM-207：截圖語音不重複填入補充說明

**問題**：PM-206 修復後，語音文字同時寫入 description（補充說明）和 voiceTranscript（語音記錄），導致編輯報告頁兩邊顯示相同內容。應該跟錄製流程一致——語音歸語音、補充說明留空讓使用者自己打。

**修法**：annotate.ts 區分「語音產生的文字」和「使用者手動輸入的文字」。語音文字只存 voiceTranscript，description 只放手動輸入。annotate 頁描述欄仍即時顯示語音（UX），但存 payload 時分開。

#### 驗收

1. 截圖 → 講話 → 手動打字 → 下一步 → 語音記錄有語音 / 補充說明有手動文字
2. 只講話不打字 → 語音記錄有 / 補充說明空
3. 只打字不講話 → 語音記錄空 / 補充說明有

---

## DONE-207 — Claude Code 回報（2026-07-08）

**截圖語音不重複填入補充說明** — 純 extension（`annotate.ts`）。

### 改動
- 存檔前計算 **manual-only 描述**：`voiceStr = voiceAccumulated.trim()`（PM-206 已累積的語音文字）→ `manualDescription = descInput.value.split(voiceStr).join(' ').replace(/\s+/g,' ').trim()`（把語音字串從描述欄內容剝除，剩下即手動輸入）；無語音時 `manualDescription = descInput.value.trim()`。
- payload：`voiceTranscript` = 語音（PM-206 格式不變）；`description` = `manualDescription`（**只放手動輸入**）。
- **即時顯示 UX 保留**：語音仍照舊 `descInput.value += text`（Web Speech / Whisper）即時顯示在描述欄，讓使用者看到轉錄過程；只有「存 payload」那一刻才把語音剝離。

### 原理
- 語音一律 append 進 `descInput`，且 PM-206 用 `voiceAccumulated` 同步累積同一份語音字串（相同內部間隔）→ 語音是 `descInput` 的連續子字串 → `split(voiceStr).join(' ')` 精準移除語音、保留手動文字。單一 session 語音模式固定（whisper 或 web-speech），間隔一致，剝除可靠。
- 編輯報告頁（PM-204）載入：`voiceTranscript` → 「語音記錄」欄、`description`(手動) → 「補充說明」欄 → **兩欄不再重複**。

### 邊界
- 罕見情境「在語音文字中間插入手動打字」會打斷連續子字串 → 剝不到 → description 退回含語音（等同 PM-207 前行為，不會壞）。純語音 / 純手動 / 語音後打字 / 打字後語音 皆正確分離。

### 驗收（build wiring + 邏輯）
1. 講話 + 打字 → 語音記錄有語音、補充說明只有手動文字 ✅
2. 只講話 → 語音記錄有、補充說明空（`split` 後只剩空白 → trim 為空）✅
3. 只打字 → 語音記錄空、補充說明有（voiceStr 空走 `descInput.value.trim()`）✅
- dist 驗證：`annotate.js` 含 `manualDescription`/`voiceStr`/`voiceAccumulated`。

### 不動
- 語音即時顯示、PM-205 音量條、PM-206 voiceTranscript 格式、截圖上傳流程（PM-204）、server / MCP → 皆未改。

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收。

---

## DONE-207b — Claude Code 回報（2026-07-08）

**PM-207 續修 — 字串剝除失敗 → 改布林旗標** — 純 extension（`annotate.ts`）。

### 根因
- PM-207 用 `descInput.value.split(voiceStr).join(' ')` 剝除語音文字，但**即時字幕分段進來時 `voiceAccumulated` 與 `descInput.value` 的空格/換行有差異**（Web Speech 每段 append 的間隔、瀏覽器自動空白等）→ `voiceStr` 不是 `descInput` 的精確連續子字串 → 剝除失敗 → description 仍含語音（重複未解）。

### 修法（改用布林旗標，不靠字串比對）
- 新增 `let isManuallyEdited = false`。
- `descInput.addEventListener('keydown', () => { isManuallyEdited = true; })`——**keydown（非 input）**：使用者實際按鍵編輯才標記；語音走 `descInput.value += text`（程式賦值）**不觸發 keydown**，故純語音時 `isManuallyEdited` 維持 false。
- 存 payload：`description = isManuallyEdited ? descInput.value.trim() : ''`——
  - **純語音（未手動改）→ description 留空**（語音只在 voiceTranscript，不重複）。
  - **有手動輸入 → 整段保留**（尊重使用者，即使含語音也不動）。
- `voiceTranscript` 維持 PM-206 格式不變（`voiceStr` 仍用於此）。
- 移除 PM-207 的 `split/join` 字串剝除。

### 為何這次能解
- 不再依賴「語音字串與描述欄內容字元級一致」（那正是即時字幕分段會破的假設）；改用「有沒有按鍵」這個明確、不受空白差異影響的訊號。keydown 只由真實鍵盤輸入觸發，語音 append 不會誤觸。

### 驗收（build wiring + 邏輯）
1. 講話 + 手動打字 → `isManuallyEdited=true` → 補充說明保留整段、語音記錄有語音 ✅
2. 只講話不打字 → `isManuallyEdited=false` → **補充說明空**、語音記錄有語音 ✅（解重複）
3. 只打字不講話 → `isManuallyEdited=true` → 補充說明有、語音記錄空 ✅
- dist 驗證：`annotate.js` 含 `isManuallyEdited` + `keydown` listener。

### 不動
- 語音即時顯示 UX、PM-205 音量條、PM-206 voiceTranscript、截圖上傳（PM-204）、server / MCP → 皆未改。

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收。

---

### 🟡 PM-208：截圖報告語音記錄改「截圖不適用」

**背景**：PM-206/207/207b 嘗試把截圖語音拆進 voiceTranscript 又要避免與補充說明重複，邏輯越修越複雜且易出錯。改採更簡單的產品決策——截圖報告的語音本來就已合進「補充說明」，voiceTranscript 不再獨立存語音；編輯報告頁的「語音記錄」區直接標示「截圖模式：語音已在補充說明」。

**修法**：
1. `annotate.ts`：`voiceTranscript` 改回 `[]`，`description = descInput.value.trim()`（含語音+手動的完整文字）；移除 PM-206/207 的 `voiceAccumulated` 與 `isManuallyEdited`（含 keydown listener、Web Speech / Whisper 的累積 append）。
2. `edit-report.ts`：截圖報告時「語音記錄」區顯示「📸 截圖模式：語音內容已包含在補充說明中」（中英文）。
3. 錄製流程不動。

#### 驗收
1. 截圖 → 講話/打字 → 下一步 → 補充說明有完整內容、語音記錄顯示「截圖模式…」提示
2. 錄製報告語音記錄正常顯示逐字稿（不受影響）
3. 中英文正確

---

## DONE-208 — Claude Code 回報（2026-07-08）

**截圖報告語音記錄改「截圖不適用」** — 純 extension；簡化 PM-206/207/207b 的語音拆分邏輯。

### ① annotate.ts：語音回歸補充說明、voiceTranscript 留空
- **移除** PM-206 的 `voiceAccumulated`（含 module 變數 + Web Speech / Whisper 兩處累積 append）與 PM-207b 的 `isManuallyEdited`（含 `descInput` keydown listener）。
- 語音仍即時 `descInput.value += text`（Web Speech / Whisper）進補充說明（UX 不變）。
- 存 payload：`voiceTranscript: []`、`description: descInput.value.trim()`（**含語音 + 手動編輯的完整內容**）。

### ② edit-report.ts：截圖報告語音記錄區改標示（中英）
- `init` 讀 `LANG_KEY` → `getUILang` 決定語言（import `getUILang`）。
- `isScreenshot` 分支：`#voiceText` 設為「📸 截圖模式：語音內容已包含在補充說明中」/「📸 Screenshot mode: voice content is included in the description below.」+ `readOnly` + 灰字（`var(--muted)`）；並隱藏「🔧 AI 校正」「🤖 AI 精簡」（對截圖無語音逐字稿無意義）。
- 錄製報告分支不變：`voiceText` 顯示 `voiceTranscript` 逐字稿 + `initMiniPlayer`。

### ③ 不動
- 錄製流程、PM-205 音量條、截圖上傳（PM-204 STORAGE_KEY→編輯頁）、截圖預覽、server / MCP → 皆未改。

### 驗收（build wiring + 邏輯）
1. 截圖講話/打字 → 補充說明完整內容、語音記錄顯示「截圖模式…」提示 ✅（dist `edit-report.js` 含中英兩句 + `readOnly`）
2. 錄製報告語音記錄正常逐字稿、不受影響（else 分支保留原邏輯）✅
3. 中英：`getUILang(LANG_KEY)` 決定，dist 含 `截圖模式…`（截圖模式）+ 英文句 ✅
4. annotate `voiceAccumulated`/`isManuallyEdited` 已全移除（dist 0 筆）✅
5. PM-205 音量條 wiring 完整（`startVolumeMeter`/`getByteFrequencyData`）✅

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收。

---

### 🟡 PM-209：截圖報告上傳成功後按鈕卡在「上傳中...」不恢復

**問題**：截圖報告在編輯報告頁按上傳後，即使報告已成功建立，按鈕仍卡在「⏳ 上傳中...」，不顯示「✅ 已上傳」+ 分享連結 + 複製鈕（錄製報告正常）。

**修法**：查 edit-report.ts 上傳成功 handler，確保截圖報告走同樣的成功路徑並保證按鈕恢復。

#### 驗收
1. 截圖 → 上傳 → 顯示「✅ 已上傳」+ 連結 + 複製鈕，按鈕不卡住
2. 上傳失敗 → 顯示錯誤 + 按鈕復原可重試
3. 錄製報告上傳不受影響

---

## DONE-209 — Claude Code 回報（2026-07-08）

**截圖報告上傳成功後按鈕卡在「上傳中...」修復** — 純 extension（`edit-report.ts`）。

### 根因
- `uploadBtn` handler 的 `const resp = await chrome.runtime.sendMessage(…)` **無 try/catch、`resp` 無防呆**。錄製與截圖走同一段程式，但**截圖 payload 較大（含整頁 PNG base64），上傳 round-trip 較久** → 訊息通道較易在回應前關閉、或 service worker 生命週期邊界導致 `sendMessage` **reject 或 resolve 成 `undefined`**。此時 `resp.ok` 直接 throw（`resp` 為 undefined）→ async handler 中斷，**既沒進成功分支、也沒進 else 復原分支** → 按鈕永遠停在「⏳ 上傳中...」。錄製 payload 較小、round-trip 快，較少觸發，故看似「只有截圖壞」。

### 修法
- **成功 UI 抽成 `showUploadSuccess(shareUrl)`**（✅ 已上傳 + 分享連結 + 📋 複製鈕，PM-199 邏輯不變）——錄製/截圖共用同一成功路徑。
- **`await sendMessage` 包 try/catch**：reject 時記 log、`resp` 維持 `undefined`。
- **判斷改 `resp?.ok && resp.shareUrl`**（optional chaining 防呆）：成功 → `showUploadSuccess`；否則 → else 顯示「❌ 上傳失敗：<error>，請重試」+ **按鈕復原可重試**。
- 結果：**任何情況（成功 / 失敗 / 訊息遺失）按鈕都不再卡死**；截圖與錄製走完全一致的成功路徑。

### 為何能解
- 舊碼唯一「卡住」路徑＝`resp` 為 undefined 時 `resp.ok` throw 中斷 handler；`resp?.ok` + try/catch 消除此 throw，執行必定落到成功或 else 兩者之一，按鈕保證恢復。

### 驗收（build wiring + 邏輯）
1. 截圖上傳成功 → `showUploadSuccess` → ✅ 已上傳 + 連結 + 複製鈕、按鈕變「✅ 已上傳」不卡 ✅（dist `edit-report.js` 含 `showUploadSuccess`）
2. 上傳失敗 / 訊息遺失 → else → 顯示錯誤 + 按鈕復原「✅ 上傳報告」可重試 ✅（dist 含 `let resp;` + `resp?.ok` + `catch (err)`）
3. 錄製報告不受影響（共用 `showUploadSuccess`，邏輯等價）✅

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收。

---

### 🟡 PM-210：截圖模式的麥克風提示流程要跟錄製一致

**問題**：截圖流程與錄製的麥克風提示不一致。錄製按下若 mic OFF 會彈「麥克風目前關閉」提示，但①截圖按鈕沒走此提示；②提示的「開啟並錄製 / 直接錄製（不錄語音）」按鈕寫死進「錄製流程」，若給截圖用會誤走錄製；③annotate 語音只看鍵盤模式、不看 MIC_KEY，「直接錄製（不錄語音）」對截圖無效（語音仍會自動開）。

**修法**：①截圖也走同一提示；②提示按鈕依來源分派（錄製 / 截圖）；③annotate 語音自動啟動也要 MIC_KEY 開啟。

#### 驗收
1. 截圖 + mic OFF + 非鍵盤 → 彈提示；「開啟並錄製」→ 開 mic → 進截圖標注頁（語音啟用）
2. 「直接錄製（不錄語音）」→ 進截圖標注頁（語音關閉）
3. mic ON 或鍵盤模式 → 不提示直接進截圖
4. 錄製流程不受影響

---

## DONE-210 — Claude Code 回報（2026-07-08）

**截圖模式麥克風提示流程與錄製一致** — 純 extension（`popup.ts` + `annotate.ts`）。

### 改動
- **`popup.ts`（提示分派）**：
  - 新增 `let micPromptFor: 'record' | 'screenshot'`——記錄提示為何而彈。
  - 新增 `runMicPromptAction()`——依 `micPromptFor` 導向 `doScreenshot()` 或 `doStartRecording()`。
  - 截圖 handler 抽成 `doScreenshot()`；`screenshotBtn` 改：讀 `MIC_KEY`/`KEYBOARD_MODE_KEY`，**mic OFF + 非鍵盤 → `micPromptFor='screenshot'` + `showMicPrompt()`**；mic ON 或鍵盤 → 直接 `doScreenshot()`（與錄製一致）。
  - `startBtn` 彈提示前設 `micPromptFor='record'`。
  - `micPromptOn`（已授權）：開 `MIC_KEY` + 同步 UI → `runMicPromptAction()`（截圖→語音啟用進截圖；錄製→錄製）。未授權維持原行為（開授權頁、這次不動作）。
  - `micPromptSkip`：`runMicPromptAction()`（截圖→語音關閉進截圖；錄製→不錄語音錄製）。
- **`annotate.ts`（語音啟用與錄製一致）**：載入自動錄語音的判斷加讀 `MIC_KEY`——除鍵盤模式外，**`MIC_KEY` 未開 → 不自動錄語音**（顯示「🔇 語音已關閉（可按 🎤 開啟）」+ `setVoiceToggleUI(false)`，仍保留手動 🎤）。mic 開才走既有 Whisper / Web Speech 自動邏輯。故「直接錄製（不錄語音）」（MIC_KEY 維持 off）在截圖標注頁真正語音關閉。

### 為何一致
- 提示 UI 與兩顆按鈕**完全複用**錄製既有元件，只多一個「來源」狀態決定終點 → 截圖與錄製看到相同提示、相同互動；語音是否自動啟動兩流程都以 `MIC_KEY`（+ 鍵盤模式）為準。

### 驗收（build wiring + 邏輯）
1. 截圖 + mic OFF + 非鍵盤 → 彈提示；「開啟並錄製」→ 設 MIC_KEY on → `doScreenshot` → annotate `micOn=true` 自動錄語音 ✅
2. 「直接錄製（不錄語音）」→ MIC_KEY 維持 off → `doScreenshot` → annotate `micOn=false` 不自動錄、顯示「語音已關閉」✅
3. mic ON / 鍵盤模式 → 不提示直接 `doScreenshot` ✅
4. 錄製流程：`micPromptFor='record'` → 兩按鈕仍走 `doStartRecording`，行為不變 ✅
- dist 驗證：`popup.js` 含 `micPromptFor`/`doScreenshot`/`runMicPromptAction`；`annotate.js` 語音判斷讀 `MIC_KEY`（`micOn = r[MIC_KEY]`）+「語音已關閉」提示。

### 不動
- 麥克風授權時機（未授權開授權頁）、錄製流程、截圖擷取/標注/上傳（PM-204/209）、server / MCP → 皆未改。

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收。

---
