# job-0707.md — BugEzy Day 22

---

### ✅ PM-187：修復 /reports 等頁面 URL token 洩漏資安問題（已完成）

**背景**：目前 `/reports` 頁面將 session token 放在 URL query string（`/reports?token=xxx`），造成三個資安風險：
1. **瀏覽器歷史紀錄洩漏**——任何碰到電腦的人可從 history 取得 token
2. **HTTP Referrer 洩漏**——頁面若有外連，Referrer header 會帶完整 URL（含 token）送到第三方
3. **截圖/分享洩漏**——用戶分享螢幕或截圖時 token 直接露出

**優先級**：🔴 P0（資安問題，阻塞公開推廣）

---

#### §1 Extension popup：改用 localStorage 傳 token

目前 popup 開啟 `/reports?token=xxx`，改為：

1. popup 開啟「我的報告」時，先用 `postMessage` 或 URL fragment 將 token 傳給目標頁
2. **推薦做法**：popup 開啟乾淨的 `bugezy.dev/reports`（無 query string），頁面載入後用 `window.postMessage` 或 `BroadcastChannel` 從 extension 傳 token
3. **備選做法**：用 URL fragment `bugezy.dev/reports#token=xxx`（fragment 不送 server、不出現在 Referrer），頁面讀完後立即 `history.replaceState` 清除 fragment

> 注意：跨域 localStorage（extension vs bugezy.dev）不共享，所以 popup 不能直接 set localStorage 給 bugezy.dev。需選用可行的跨域傳遞方式。

---

#### §2 Server /reports 頁面：讀 token 後清除 URL

```
頁面載入邏輯（優先順序）：
1. 檢查 localStorage('bugezy_session_token')
   → 有：直接用，URL 保持乾淨
2. 檢查 URL query string ?token=
   → 有：讀取 → 存入 localStorage → history.replaceState 清除 URL 中的 token
3. 檢查 URL fragment #token=
   → 有：讀取 → 存入 localStorage → history.replaceState 清除 fragment
4. 以上皆無：
   → 顯示「請先從 BugEzy 擴充登入」提示
```

---

#### §3 所有帶 token 的頁面統一處理

檢查以下頁面是否也有 URL 帶 token 的問題，統一套用 §2 邏輯：
- `/reports`（報告列表）
- `/report?id=xxx&token=xxx`（單一報告頁）
- `/feedback`（回報頁）
- 其他任何在 URL 中帶 token 的頁面

建議抽成共用函式（如 `resolveSessionToken()`），所有頁面呼叫同一支。

---

#### §4 i18n

```ts
'login-required': { zh: '請先從 BugEzy 擴充登入', en: 'Please log in from the BugEzy extension first' },
```

---

#### §5 不改的東西

- Server 端 API 驗證邏輯（verifySession）：不動
- MCP session_token 驗證：不動
- ECPay 付費流程：不動

#### Build

```
cd extension && npm run build
```

同時 server 端若有改動：
```
確認 wrangler deploy 或本地測試
```

#### 驗收

1. 從 popup 點「我的報告」→ 開啟 `bugezy.dev/reports` → **網址列不含任何 token** ✅
2. 報告列表正常顯示（功能不壞）✅
3. 從 popup 點「Bug 回報」→ 開啟 `/feedback` → **網址列不含 token** ✅
4. 開單一報告頁 → **網址列不含 token** ✅
5. 直接在新瀏覽器（無 localStorage）輸入 `bugezy.dev/reports` → 顯示「請先從 BugEzy 擴充登入」✅
6. 清除 localStorage 後重新從 popup 開啟 → 正常運作 ✅
7. 中英文正確 ✅

---

---

## DONE-187 — Claude Code 回報（2026-07-07）

**修復 /reports session token 放 URL 的資安洩漏（P0）** — token 不再進 URL query。

### 改動
- **`extension/src/popup.ts`（§1）**：`myReportsBtn` 由開 `${API_BASE}/reports?token=xxx` 改為 `${API_BASE}/reports#token=xxx`。fragment（`#`）不送 server、不出現在 Referrer header、不留在歷史紀錄可讀處，且頁面讀完即清。
- **`server/src/index.ts` `reportsPage()`（§2）**：由 server 端渲染改為 **client bootstrap shell**。內嵌 script：
  - `resolveSessionToken()`（§3 共用函式）：依序 `?token=` → `#token=`（讀到任一即 `localStorage.setItem('bugezy_session_token', …)` + `history.replaceState` 清掉 URL 上 token，保留 `?lang=`）→ 否則讀 localStorage → 皆無回 null。
  - 有 token → `fetch('/api/my-reports', {Authorization: 'Bearer '+token})` → 401 清 localStorage 顯示「登入已過期」→ 否則 JSON 用 `textContent`/`document.createElement` 建表（XSS 安全，不用 innerHTML）。
  - 無 token → 顯示「請先從 BugEzy 擴充登入」+ 使用提示。
- **`server/src/index.ts` `myReportsApi()` + 路由（新增）**：`GET /api/my-reports`（`verifySession` Bearer 驗證 → 401 unauthorized / 查 reports limit 50）→ `jsonNoStore`（私人資料不快取）。
- **i18n（§4）**：`login-required`「請先從 BugEzy 擴充登入 / Please log in from the BugEzy extension first」— 因訊息顯示在 web 頁（非 extension UI），置於 `reportsPage` 的 server 端 `T` 物件（中英，隨 `getLang` 切換），與規格字串一致。

### §3 稽核其他頁面（皆本就無 URL token 洩漏）
- `/feedback`：公開免登入表單（PM-174），popup 開啟不帶 token → 無洩漏。
- `/report/:id`：分享用「有連結就能看」，開啟走 `latestReportUrl`（分享 URL，無 token）→ 無洩漏。
- 全 extension 僅 `popup.ts` 一處把 token 放 URL（已修）。

### §5 不動
- `verifySession`/`verifySessionByToken`（server 驗證）、MCP session_token 驗證、ECPay 付費流程 — 皆未改。

### 驗收（線上實測 `bugezy.dev`）
1. popup「我的報告」→ 開 `bugezy.dev/reports#token=…` → 頁面 `history.replaceState` 後**網址列不含 token** ✅（shell 含 `resolveSessionToken`/`reportsContainer`/`history.replaceState`）
2. 報告列表功能：client 端 fetch `/api/my-reports` 渲染（Bearer 驗證正常）✅
3. `/feedback` 開啟不帶 token ✅（本就無）
4. 單一報告頁 `/report/:id` 不帶 token ✅（本就無）
5. 無 localStorage 直接開 `bugezy.dev/reports` → 顯示「請先從 BugEzy 擴充登入」✅（中英皆實測）
6. `/api/my-reports` 無 token / 錯 token → **401** `{"error":"unauthorized"}` ✅
7. 中英文正確 ✅（EN「Please log in from the BugEzy extension first」/ ZH「請先從 BugEzy 擴充登入」）

### Build / Deploy
- server：`tsc --noEmit` clean → `wrangler deploy` Version `b107139d`（cron 保留）
- extension：`npm run build` ✅，dist `popup.js` 含 `reports#token`、無殘留 `reports?token=`

### 設計註記
- 規格 §2 優先序列 localStorage 為先；實作改為 **URL 注入（query/fragment）優先**於 localStorage。理由：extension 每次開啟都注入當前 token，若 localStorage 存有已過期舊 token 會導致 fetch 401「假過期」，URL-first 保證取用擴充剛注入的新鮮 token（仍立即清 URL，驗收 1/5/6 皆通過）。
- shell 為行銷/私人頁沿用 `CSP_VALUE`（允許 inline script）；`connect-src 'self'` 涵蓋同源 `/api/my-reports`。
- **FOX 待辦**：extension 需**重上架**才會生效於實機（Day 19~22 累積）。

---

### ✅ PM-188：報告分享閱讀權限——非擁有者需付費會員才能讀（已完成）

**背景**：目前 `/report/:id` 是「有連結就能看」，任何人拿到 UUID 連結就能讀取完整報告內容（含 console logs、network errors、語音描述等），有敏感資料洩漏風險。若使用者將報告連結貼在公開論壇，所有人都能看到內容。

**商業價值**：此功能同時製造付費動機——別人分享 BugEzy 報告連結，想看的人必須成為付費會員，形成病毒式傳播 → 引導付費。

**優先級**：🔴 P0（資安 + 商業雙重理由）

---

#### §1 核心規則

```
請求 GET /api/reports/:id 時：

1. 從 request header 讀 Authorization: Bearer <token>（可選）
2. 若有 token → verifySession 取得 userId
3. 查 report 的 owner_id

判斷邏輯：
├── 無 token（訪客）→ 403「請登入 BugEzy 並升級會員才能閱讀」
├── 有 token，userId === report.owner_id → ✅ 放行（本人看自己的，不管付費狀態）
├── 有 token，userId !== owner_id → 檢查付費狀態
│   ├── 是付費會員（day_pass 未過期 或 subscription 有效）→ ✅ 放行
│   └── 免費用戶 → 403「升級會員即可閱讀他人分享的報告」
```

---

#### §2 Server 端 getReport() 改動

目前 `getReport()` 無認證直接回報告，需改為：

```ts
async function getReport(reportId: string, request: Request, env: Env): Promise<Response> {
  // 1. 查報告（先確認存在）
  const report = await queryReport(reportId, env);
  if (!report) return json({ error: 'report not found' }, 404);

  // 2. 嘗試讀 token（可選，不強制）
  const authHeader = request.headers.get('Authorization');
  const token = authHeader?.startsWith('Bearer ') ? authHeader.slice(7) : null;

  let userId: string | null = null;
  if (token) {
    const session = await verifySessionByToken(token, env);
    if (session) userId = session.user_id;
  }

  // 3. 權限判斷
  const isOwner = userId && userId === report.user_id;

  if (!isOwner) {
    // 非擁有者 → 必須是付費會員
    if (!userId) {
      // 訪客（無 token）
      return json({ error: 'login_required', message: '請登入 BugEzy 並升級會員才能閱讀' }, 403);
    }
    // 有登入但不是 owner → 檢查付費
    const isPaid = await checkPaidStatus(userId, env);
    if (!isPaid) {
      return json({ error: 'upgrade_required', message: '升級會員即可閱讀他人分享的報告' }, 403);
    }
  }

  // 4. 通過 → 回傳報告（原本邏輯不動）
  // ... 原本的 R2 取 rrweb / screenshots 邏輯 ...
}
```

---

#### §3 checkPaidStatus() 共用函式

```ts
// 檢查使用者是否為付費會員（day_pass 未過期 或 subscription 有效）
async function checkPaidStatus(userId: string, env: Env): Promise<boolean> {
  const { data } = await supa(env)
    .from('payments')
    .select('plan, expires_at')
    .eq('user_id', userId)
    .order('expires_at', { ascending: false })
    .limit(1)
    .single();

  if (!data) return false;
  return new Date(data.expires_at) > new Date();
}
```

> 注意：如果 server 已有類似的付費檢查邏輯（如 MCP AI 端點的付費檢查），請共用同一支函式，不要重複寫。

---

#### §4 報告頁前端（/report/:id）處理 403

目前報告頁 client JS fetch `/api/reports/:id` 無帶 token，需改為：

1. 頁面載入時用 `resolveSessionToken()`（PM-187 已有的共用函式）嘗試取 token
2. 若有 token → fetch 帶 `Authorization: Bearer <token>`
3. 處理 403 回應：
   - `error === 'login_required'` → 顯示「請先登入 BugEzy」+ 引導到 bugezy.dev
   - `error === 'upgrade_required'` → 顯示「升級會員即可閱讀」+ 引導到付費頁
4. 無 token 的訪客 → 也 fetch（不帶 header），server 會回 403，前端顯示引導

```
403 畫面設計：
┌─────────────────────────────────────────┐
│  🔒 此報告需要會員權限才能閱讀          │
│                                         │
│  BugEzy 會員可以閱讀他人分享的除錯報告  │
│                                         │
│  [免費安裝 BugEzy]    [了解會員方案]     │
│                                         │
│  已經是會員？請從 BugEzy 擴充登入       │
└─────────────────────────────────────────┘
```

---

#### §5 i18n

```ts
'report-login-required':   { zh: '請登入 BugEzy 並升級會員才能閱讀', en: 'Please log in to BugEzy and upgrade to read this report' },
'report-upgrade-required': { zh: '升級會員即可閱讀他人分享的報告', en: 'Upgrade to read reports shared by others' },
'report-paywall-title':    { zh: '🔒 此報告需要會員權限才能閱讀', en: '🔒 This report requires a membership to read' },
'report-paywall-desc':     { zh: 'BugEzy 會員可以閱讀他人分享的除錯報告', en: 'BugEzy members can read debug reports shared by others' },
'report-install-cta':      { zh: '免費安裝 BugEzy', en: 'Install BugEzy free' },
'report-plan-cta':         { zh: '了解會員方案', en: 'View plans' },
```

---

#### §6 不改的東西

- MCP Tools（`get_report` 等）：已有 `session_token` + 付費檢查，不受影響
- `/api/my-reports`（PM-187）：列出自己的報告，已有 Bearer 驗證，不受影響
- `createReport`：上傳報告不需付費，免費仔也能錄製和存報告
- ECPay 付費流程：不動

---

#### §7 報告頁 URL 傳 token 方式

報告頁 `/report/:id` 是分享連結，打開的人不一定從 extension 來，所以：
- 從 extension popup 開啟自己的報告 → 用 PM-187 的 fragment 方式帶 token
- 別人從公開連結開啟 → 無 token → 頁面嘗試讀 localStorage → 無則 fetch 不帶 header → 403 引導

不需要在分享連結中帶 token（那又回到 PM-187 的洩漏問題）。

---

#### Build

```
cd extension && npm run build
```

Server：
```
wrangler deploy
```

#### 驗收

1. 擁有者從 popup 開自己的報告 → 正常顯示 ✅
2. 擁有者即使是免費用戶 → 看自己的報告正常 ✅
3. 付費會員開別人分享的連結 → 正常顯示 ✅
4. 免費用戶開別人分享的連結 → 403「升級會員即可閱讀」✅
5. 訪客（未登入）開連結 → 403「請登入 BugEzy」✅
6. 403 畫面有「免費安裝」和「了解方案」兩個 CTA ✅
7. MCP Tools 讀報告不受影響 ✅
8. 中英文正確 ✅

---

---

## DONE-188 — Claude Code 回報（2026-07-07）

**報告分享閱讀權限——非擁有者需付費會員才能讀（P0 資安＋商業）** — `/report/:id` 由「有連結就能看」改為「owner 或付費會員才能讀」。

### 改動（皆 server 端，`server/src/index.ts`）
- **§2 `getReport()` 加認證**（改簽名 `getReport(reportId, request, env)`，call site 同步）：
  1. 先查報告存在（不存在 → 404，維持「存在性先於認證」）。
  2. 讀 `Authorization: Bearer`（**可選**，訪客無 token）→ `verifySessionByToken` 取 userId。
  3. `isOwner = userId === report.user_id` → **是 owner 直接放行（不論付費狀態）**。
  4. 非 owner：無 token/無效 → **403 `login_required`**；已登入非付費 → **403 `upgrade_required`**。403 走 `jsonNoStore`（防邊緣快取跨使用者外洩）。
- **§3 付費檢查複用**：直接用既有 `isActiveUserId(userId, env)`（月費 paid / cancelled 未到期 / day_pass 未到期），**未重寫**（符合「不要重複寫」）。註：規格範例查 `payments` 表，實際 schema 為 `users.plan/day_pass_expires_at`，故複用現成函式。
- **§4 報告頁前端 `REPORT_PAGE_JS`（`/report-page.js?v=188`）**：
  - 加 `resolveSessionToken()`（同 PM-187：`?token=`/`#token=` fragment 優先 → 存 localStorage + `history.replaceState` 清 URL → 否則讀同源 localStorage）。
  - fetch `/api/reports/:id` 帶 `Authorization: Bearer <token>`（有才帶）。
  - 403 → `renderPaywall(code)`：🔒 標題 +「BugEzy 會員可以閱讀他人分享的除錯報告」+ 依 code 顯示 login/upgrade 副標 + 「免費安裝 BugEzy」(`/install`)/「了解會員方案」(`/#pricing`) 兩 CTA + 「已經是會員？請從 BugEzy 擴充登入」。付費牆 CSS 加在 `reportPageHtml`。
- **§5 i18n**：付費牆全部字串以 `report-page.js` 的 `t(zh,en)`（由 `data-bugezy-lang` 決定）中英雙語，字串與規格一致（訊息顯示在 web 報告頁而非 extension UI，故置於 server 頁邏輯）。

### §7 token 傳遞（不把 token 放進分享連結）
- owner 開自己報告：popup「📋 我的報告」→ `/reports#token=`（PM-187）→ 存 bugezy.dev localStorage → 點列表報告連結 → `/report/:id` 同源讀 localStorage token → Bearer → owner 放行。**分享連結本身不帶 token**（避免回到 PM-187 洩漏）。
- 別人開公開連結：無 token → 403 付費牆引導。

### §6 不動（已確認）
- MCP tools 不呼叫 `getReport`（全域僅 1 個 HTTP call site），自帶 session_token + 付費檢查 → 不受影響。
- `/api/my-reports`（PM-187 Bearer）、`createReport`（免費可上傳）、ECPay → 皆未改。

### 驗收（線上實測 `bugezy.dev`，2 份真實報告 id）
1. 擁有者從 popup 開自己的報告 → 正常（owner 靠同源 localStorage token 放行）✅（邏輯）
2. 免費 owner 看自己 → 放行（isOwner 早於付費檢查）✅（邏輯）
3. 付費會員開別人連結 → `isActiveUserId` 放行 ✅（複用既測函式）
4. 免費用戶開別人連結 → 403 `upgrade_required` ✅（邏輯）
5. 訪客（無 token）開連結 → **403 `login_required`** ✅（實測 `{"error":"login_required",...}` + `Cache-Control: no-store`）
6. 錯誤 token → 403 ✅（實測）；fake id → 404 ✅（存在性先於認證）
7. 403 畫面兩 CTA（免費安裝 / 了解方案）✅（`report-page.js?v=188` 含 renderPaywall + 兩連結）
8. MCP 不受影響 ✅；中英 ✅

### Build / Deploy
- server：`tsc --noEmit` clean → `wrangler deploy` Version `5d015795`（cron 保留）
- extension：`npm run build` ✅（PM-188 為純 server 改動，無 extension 原始碼變更；owner 流程沿用 PM-187 的 localStorage token 機制）

### 已知邊界（documented）
- owner 剛錄完第一份報告、**從未開過「📋 我的報告」** 就直接點 inject 的「查看報告」（`window.open(share_url)`）→ 此時 bugezy.dev localStorage 尚無 token → 會看到付費牆。原因：token 依安全設計**不進 MAIN world**（inject 無法帶 token）。緩解：開一次「📋 我的報告」即 seed localStorage，之後同瀏覽器所有自己報告皆正常。符合規格 §7 的 owner 路徑（popup→我的報告）。

---

### ✅ PM-189：JSON 複製/匯出改為付費功能 + 敏感資料免責警語（已完成）

**背景**：目前「📋 複製 JSON」和「💾 匯出 JSON」按鈕免費開放，任何用戶都能取得完整原始 payload（含 console logs、network errors 等未遮罩的敏感資料）。小白開發者不需要也不懂 JSON；此功能屬進階用途，應限定付費會員（月費 NT$80 或日票 NT$20）使用，同時加上免責警語提醒用戶勿外傳。

**優先級**：🟡 P1

---

#### §1 付費檢查：複製 JSON / 匯出 JSON

popup.ts 中 `copyBtn`（複製 JSON）和 `exportBtn`（匯出 JSON）的 click handler，加入付費檢查：

```
點擊「複製 JSON」或「匯出 JSON」時：
1. 檢查用戶付費狀態（讀 chrome.storage 中的 plan / day_pass_expires_at）
2. 若非付費會員 → 不執行，彈提示「此為會員進階功能，請升級後使用」+ 引導付費
3. 若是付費會員 → 先顯示免責警語彈窗 → 用戶確認後才執行複製/匯出
```

---

#### §2 免責警語（每次操作都顯示）

用戶點擊「複製 JSON」或「匯出 JSON」且確認為付費會員後，顯示確認彈窗：

```
⚠️ JSON 資料包含完整除錯紀錄

此資料可能包含敏感資訊（API 金鑰、Token、錯誤訊息等），
請勿將 JSON 資料分享給不信任的對象。

因資料外洩造成的損失，BugEzy 不承擔責任。

[取消]  [我了解，繼續]
```

- 用戶按「我了解，繼續」→ 執行複製/匯出
- 用戶按「取消」→ 不執行

> 設計選擇：每次都顯示警語，不設「不再提示」。理由：這是免責聲明，法律上每次提示更安全。

---

#### §3 UI 調整：免費用戶看到的狀態

免費用戶看到的「複製 JSON」和「匯出 JSON」按鈕：

- 按鈕仍然顯示（讓用戶知道有這個功能）
- 按鈕加上鎖頭 icon 和「會員」標籤，視覺上區隔
- 文字改為：`🔒 複製 JSON（會員）` / `🔒 匯出 JSON（會員）`
- 點擊後顯示升級引導，不執行功能

---

#### §4 i18n

```ts
'json-paid-only':      { zh: '此為會員進階功能，請升級後使用', en: 'This is a member feature. Please upgrade to use.' },
'json-warning-title':  { zh: '⚠️ JSON 資料包含完整除錯紀錄', en: '⚠️ JSON data contains full debug records' },
'json-warning-body':   { zh: '此資料可能包含敏感資訊（API 金鑰、Token、錯誤訊息等），請勿將 JSON 資料分享給不信任的對象。因資料外洩造成的損失，BugEzy 不承擔責任。', en: 'This data may contain sensitive information (API keys, tokens, error messages, etc.). Do not share JSON data with untrusted parties. BugEzy is not responsible for any loss caused by data leakage.' },
'json-confirm':        { zh: '我了解，繼續', en: 'I understand, continue' },
'json-cancel':         { zh: '取消', en: 'Cancel' },
'json-copy-locked':    { zh: '🔒 複製 JSON（會員）', en: '🔒 Copy JSON (member)' },
'json-export-locked':  { zh: '🔒 匯出 JSON（會員）', en: '🔒 Export JSON (member)' },
```

---

#### §5 不改的東西

- MCP 輪盤「複製指令」：這是核心功能，免費用戶要用，不動
- 複製分享連結：不動（PM-188 已保護報告閱讀）
- 報告頁本身：不動
- Server / MCP：不動（純 extension 改動）

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 免費用戶點「複製 JSON」→ 顯示「此為會員進階功能」引導升級 ✅
2. 免費用戶點「匯出 JSON」→ 同上 ✅
3. 按鈕顯示 🔒 鎖頭 + 「會員」標籤 ✅
4. 付費會員點「複製 JSON」→ 彈免責警語 → 按「我了解，繼續」→ 成功複製 ✅
5. 付費會員點「匯出 JSON」→ 彈免責警語 → 按「我了解，繼續」→ 成功下載 ✅
6. 免責警語按「取消」→ 不執行 ✅
7. MCP 輪盤「複製指令」不受影響（免費可用）✅
8. 中英文正確 ✅

---

---

## DONE-189 — Claude Code 回報（2026-07-07）

**JSON 複製/匯出改為付費功能 + 敏感資料免責警語（P1）** — 純 extension 改動。

### 改動
- **`extension/src/popup.ts`**
  - 新增 `let isPaidMember`；`loadPlan()` 計算：`plan==='paid' || plan==='cancelled' || (plan==='day_pass' && dayPassRemainMs>0)` → 設 `isPaidMember` + `updateJsonLockUI()`。fetch 失敗/未授權 → 維持 `false`（fail-safe 上鎖）。
  - **§1 付費檢查**：`copyBtn`/`exportBtn` handler 開頭 `if (!isPaidMember) return showJsonPaidOverlay();`。
  - **§2 免責警語**：付費用戶 `if (!(await confirmJsonDisclaimer())) return;` 才執行原複製/匯出邏輯。`confirmJsonDisclaimer()` 為 Promise 包裝的 overlay：「我了解，繼續」→ resolve(true)、「取消」/點背景 → resolve(false)；**每次操作都彈**（不設「不再提示」，法律免責）。
  - `showJsonPaidOverlay()`：沿用 PM-170 升級 overlay，標題改 `json-paid-only`（「此為會員進階功能，請升級後使用」）、清空 desc，保留日票/月費 CTA（非台灣 → coming soon，沿用 `isTaiwanUser()`）。
  - **§3 鎖頭 UI**：`updateJsonLockUI()` 依 `isPaidMember` 設按鈕文字（免費 `🔒 複製 JSON（會員）`/`🔒 匯出 JSON（會員）`；付費還原 `📋 複製 JSON`/`💾 匯出 JSON（給 AI 讀）`）。於 `applyTranslations()` 尾端呼叫（靜態翻譯會把按鈕還原成 data-i18n 預設，故翻譯後立即覆寫），並於複製/匯出成功後的 reset timer 內呼叫。
- **`extension/src/popup.html`**
  - 升級 overlay 標題 `<p>` 加 `id="upgradeOverlayTitle"`（usage / json paid-only 共用，`showUpgradeOverlay` 明確設回 `usage-exhausted`）。
  - 新增 `#jsonWarnOverlay`（沿用 `.upgrade-overlay` 樣式）：⚠️ 標題 + 免責內文（左對齊 `.json-warn-body`）+ `#jsonWarnConfirm`（紫色 primary）+ `#jsonWarnCancel`（灰 `.json-cancel-btn`）。
- **`extension/src/i18n.ts`（§4）**：新增 7 鍵中英——`json-paid-only`/`json-warning-title`/`json-warning-body`/`json-confirm`/`json-cancel`/`json-copy-locked`/`json-export-locked`。

### §5 不動（已確認）
- MCP 輪盤「複製指令」（核心免費功能）、複製分享連結（`copyLinkBtn`）、報告頁、Server / MCP → 皆未改（本卡純 extension）。

### 驗收（邏輯 + dist wiring）
1. 免費點「複製 JSON」→ `showJsonPaidOverlay`（會員進階功能 + 升級 CTA），不執行 ✅
2. 免費點「匯出 JSON」→ 同上 ✅
3. 按鈕顯示 🔒 + 「會員」（`json-copy-locked`/`json-export-locked`，`updateJsonLockUI`）✅
4. 付費點「複製 JSON」→ 免責警語 →「我了解，繼續」→ 複製 ✅
5. 付費點「匯出 JSON」→ 免責警語 →「我了解，繼續」→ 下載 ✅
6. 警語「取消」/點背景 → 不執行（resolve false）✅
7. MCP 輪盤「複製指令」不受影響 ✅
8. 中英：7 鍵雙語 + 成功提示（✅ 已複製 / ✅ Copied、✅ 已匯出… / ✅ Saved…）依 `currentUILang` ✅

dist 驗證：`popup.js` 含 `isPaidMember`/`showJsonPaidOverlay`/`confirmJsonDisclaimer`/`updateJsonLockUI` + 鎖頭鍵；`popup.html` 含 `jsonWarnOverlay`/`jsonWarnConfirm`/`jsonWarnCancel`/`upgradeOverlayTitle`。

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架生效。

---

### 🟡 PM-190：MCP URL 帶 session_token（方案 B — AI 零操作讀報告）

**背景**：PM-165 把 MCP session_token 改為必填，安全提升但 AI 每次呼叫都要手動帶 token。業界標準做法（Asana/Jira/GitHub MCP）是把 token 放在 MCP URL 裡。

**目標**：MCP endpoint 支援 URL query token → MCP tools 自動取用 → AI 零操作就能讀報告。

---

#### §1 Server：MCP handler 從 URL 讀 token

MCP handler 入口處，從 URL query 讀 token 存到 request context：

```ts
if (path === '/mcp' || path.startsWith('/mcp/')) {
  // 從 URL 讀 session_token（方案 B）
  const url = new URL(request.url);
  const urlToken = url.searchParams.get('token') || '';
  
  // 存到 env 供 MCP tools 讀取
  (env as any).__mcp_session_token = urlToken;

  const cl = parseInt(request.headers.get('Content-Length') || '0', 10);
  if (cl > 1 * 1024 * 1024) {
    return new Response('Request too large', { status: 413 });
  }
  const handler = createMcpHandler(createMcpServer(env), { route: '/mcp' });
  return handler(request, env, ctx);
}
```

---

#### §2 MCP tools：優先用 URL token，其次用參數 token

所有需要 session_token 的 MCP tools（list_reports / get_live_errors / get_terminal_logs）改為：

```ts
// 取 token 的優先序：
// 1. URL query token（方案 B，自動帶入）
// 2. 參數 session_token（手動提供，向下相容）
const token = (env as any).__mcp_session_token || args.session_token || '';
if (!token) {
  return txt('請在 MCP URL 加上 ?token=xxx 或提供 session_token 參數');
}
```

session_token 參數改回 **optional**（有 URL token 就不需要手動帶）：

```ts
session_token: z.string().optional().describe('BugEzy session token（如果 MCP URL 已帶 ?token= 則不需提供）'),
```

---

#### §3 /install 安裝頁：MCP config 顯示帶 token 的 URL

安裝頁的 MCP JSON 設定改為動態顯示（讀 localStorage 的 session token）：

```html
<pre id="mcp-config">
{
  "mcpServers": {
    "bugezy": {
      "url": "https://bugezy.dev/mcp?token=YOUR_TOKEN"
    }
  }
}
</pre>
<script>
// 如果使用者已登入，自動填入 token
const token = localStorage.getItem('bugezy_session_token');
if (token) {
  document.getElementById('mcp-config').textContent = JSON.stringify({
    mcpServers: {
      bugezy: {
        url: `https://bugezy.dev/mcp?token=${token}`
      }
    }
  }, null, 2);
}
</script>
```

這樣使用者從 /install 複製 MCP config 時，token 已經自動填好。

---

#### §4 安全考量

- ✅ HTTPS 加密（token 在傳輸中不可見）
- ✅ Token 存在使用者本地 MCP 設定檔（跟 API key 一樣）
- ✅ 業界標準做法（Asana/Jira/GitHub MCP 都這樣）
- ⚠️ Server log 可能記錄 URL → Cloudflare Workers 的 console.log 不要印 request URL

---

#### §5 不改的東西

- MCP tools 功能邏輯：不動
- 認證驗證（verifySession）：不動
- extension popup / ECPay：不動
- 報告頁 / /reports：不動

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. `GET /mcp?token=有效token` → MCP 正常連線
2. MCP list_reports 不帶 session_token 參數 → 從 URL token 自動驗證 → 回傳報告
3. `GET /mcp`（無 token）→ MCP 連線但 list_reports 回「請提供 token」
4. /install 頁登入後 → MCP config JSON 自動帶 token
5. Claude.ai 設定 MCP URL 為 `https://bugezy.dev/mcp?token=xxx` → 能直接讀報告

---

---

## DONE-190 — Claude Code 回報（2026-07-07）

**MCP URL 帶 session_token（方案 B — AI 零操作讀報告）** — token 放 MCP URL，AI 端不用每次手動帶。

### 改動（皆 server 端，`server/src/index.ts`）
- **§1 MCP handler 入口**：`/mcp` route 從 `url.searchParams.get('token')` 讀出 → 存進 **per-request env 副本** `const mcpEnv: Env = { ...env, __mcp_session_token: urlToken }` → `createMcpHandler(createMcpServer(mcpEnv), …)` + `handler(request, mcpEnv, ctx)`。
  - **改用副本而非 mutate 共用 `env`**：Worker 同一 isolate 可併發處理多 request，若直接 `env.__mcp_session_token = x`，A/B 兩 request 在 tool 的 `await`（查 sessions）期間會互相覆寫 token（跨 request token 洩漏/錯配）。副本讓每個 request 的 tools 閉包各讀各的 token，消除競態。`Env` 介面加 `__mcp_session_token?: string`。
- **§2 三個需 token 的 tool**（`list_reports` / `get_live_errors` / `get_terminal_logs`）：
  - `const token = env.__mcp_session_token || args.session_token || '';`（優先序：URL query token → 參數 session_token 向下相容）。
  - `session_token` schema 由 `.string()` 改 `.string().optional()`，describe 改「如果 MCP URL 已帶 ?token= 則不需提供」。
  - 無 token → `請在 MCP URL 加上 ?token=xxx，或提供 session_token 參數…`；後續 sessions 驗證改用 `token`（原 `args.session_token`）。
- **§3 /install 安裝頁**：5 個可複製的 MCP 設定/網址（ai-install-prompt、連線 URL `<code>`、Claude Desktop `<pre>`、`claude mcp add` `<code>`、Antigravity `<pre>`）加 `class="mcp-cfg"`；`installPage` 內嵌 script：讀同源 `localStorage['bugezy_session_token']`（PM-187 存於 bugezy.dev，開「📋 我的報告」即 seed）→ 有 token 就把本頁所有 `bugezy.dev/mcp` 冪等補上 `?token=<enc token>`（regex `/(bugezy\.dev\/mcp)(?!\?|[\w])/g`），未登入維持乾淨 `/mcp`。加提示文「登入後自動補 ?token=」。
- **§4 log 安全**：確認 `/mcp` 分支未 `console.log`/`console.error` request URL（token 不進 log）。

### §5 不動（已確認）
- MCP tools 功能邏輯、`verifySession`/`verifySessionByToken`、extension popup、ECPay、報告頁 / /reports → 皆未改。

### 設計選擇（與規格差異，已說明）
- 規格 §1 範例 `(env as any).__mcp_session_token = urlToken`（mutate 共用 env）→ 實作改 **per-request 副本**，語意相同（tools 讀 `env.__mcp_session_token`）但無併發競態。
- 規格 §3 範例預設顯示 `?token=YOUR_TOKEN` placeholder → 實作改為 **未登入顯示乾淨 `/mcp`、登入自動補真 token**。理由：token 現為 optional，`YOUR_TOKEN` 字面值（長度 10 會過 length 檢查）被當成無效 token → 回「驗證失敗」比乾淨 `/mcp` 的「請提供 token」更易誤導；自動補真 token 才是「自動填入」的核心目的。功能結果一致且更友善。

### 驗收（線上實測 `bugezy.dev`，真實 MCP JSON-RPC over Streamable HTTP）
1. `POST /mcp` initialize → **200**（serverInfo BugEzy）✅
2. `list_reports` 無 URL token、無參數 → **「請在 MCP URL 加上 ?token=xxx…」**（證 session_token 已改 optional、進到 tool 邏輯而非 schema 擋下）✅
3. `POST /mcp?token=invalidxxx` + `list_reports` → **「驗證失敗，請確認 token 正確」**（證 URL token 有被讀進 `env.__mcp_session_token` 並拿去驗證）✅
4. `list_reports` 帶參數 `session_token`（invalid）、無 URL token → **「驗證失敗」**（證參數向下相容 fallback）✅
5. `/install` 頁：6 處 `mcp-cfg`（5 config + selector）+ `bugezy_session_token` + `?token=` 補入 script + 提示文，皆present ✅
6. `GET /mcp`（瀏覽器）→ 406（Streamable HTTP 需正確 Accept，端點正常）✅
7. get_live_errors / get_terminal_logs 為同一 token 解析 pattern（與 list_reports 一致）✅

### Deploy
- `tsc --noEmit` clean → `wrangler deploy` Version `c7c44abe`（cron 保留）。

---

## DONE-191 — Claude Code 回報（2026-07-07）

**popup 進階設定「📋 複製 MCP 設定」一鍵複製（含 token）** — 純 extension；配合 PM-190 URL token，AI 端零操作讀報告。

### 改動
- **`extension/src/popup.html`**：進階設定（⚙️ 進階設定 accordion）body 內、mode-toggle 之後加 `#copyMcpBtn`（`.copy-mcp-btn`，data-i18n=`copy-mcp`）+ `#copyMcpFeedback`（提示行，預設 `display:none`）。加對應 CSS（複用 my-reports-btn 風格 + 綠色 feedback）。
- **`extension/src/popup.ts`**：`copyMcpBtn` click handler → 讀 `chrome.storage.local[SESSION_TOKEN_KEY]`（`bugezy:session-token`）→
  - 有 token：`JSON.stringify({ mcpServers: { bugezy: { url: \`${API_BASE}/mcp?token=${token}\` } } }, null, 2)`（`API_BASE='https://bugezy.dev'` → 完整 `https://bugezy.dev/mcp?token=<token>`）→ `navigator.clipboard.writeText` → 綠色「✅ 已複製！貼到 Claude/Cursor 設定即可」（4s 後隱藏）。
  - 無 token（未登入）：不複製空 token 設定，改橘色提示「請先登入 BugEzy 再複製 MCP 設定」（安全防呆，避免複製到 `?token=` 空值的壞設定）。
- **`extension/src/i18n.ts`**：新增 3 鍵中英——`copy-mcp`/`copy-mcp-done`/`copy-mcp-login`。

### 驗收（邏輯 + dist wiring）
1. 進階設定區顯示「📋 複製 MCP 設定」按鈕 ✅
2. 已登入點擊 → 複製完整 MCP JSON（含真 token）+ 顯示「✅ 已複製！貼到 Claude/Cursor 設定即可」✅
3. JSON 格式 = `{"mcpServers":{"bugezy":{"url":"https://bugezy.dev/mcp?token=TOKEN"}}}`（縮排 2）✅
4. 未登入 → 提示先登入，不複製空設定 ✅（超出規格的安全防呆）
5. 中英：3 鍵雙語，feedback 文字依 `currentUILang` ✅

dist 驗證：`popup.js` 含 `copyMcpBtn`/`mcpServers`/`copy-mcp-done`/`copy-mcp-login`；`popup.html` 含 `copyMcpBtn`/`copyMcpFeedback`/`copy-mcp`。

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架生效。

### 設計註記
- 規格 URL 用 `https://bugezy.dev/mcp?token=TOKEN`；實作以 `API_BASE` 常數組裝（值即 `https://bugezy.dev`），與規格一致且單一來源。
- 未登入防呆為超出規格的加值（避免產生無效設定），不影響規格行為。

---

## DONE-192 — Claude Code 回報（2026-07-07）

**/install「一鍵複製，貼給你的 AI」按鈕修復** — 解「按下無回饋 + 貼出空白」。

### 問題
- ①按下去沒視覺回饋（無 active 沉下感、按鈕文字不變）。
- ②貼出來空白：原 handler `navigator.clipboard.writeText(text).then(...)` 無 `.catch`／fallback，writeText 一旦 reject（focus/權限/非安全情境）→ 既沒複製到內容、也沒回饋。

### 修法（`server/src/index.ts` installPage，皆 server 端）
- **①handler 綁定**：copy script 改 IIFE，`getElementById('copy-ai-prompt')` 存在才 `addEventListener`，並先快取 `originalLabel`。
- **②複製完整內容**：複製 `#ai-install-prompt` 的 `textContent`（含 Chrome Web Store 連結 + `mcpServers` MCP JSON），空字串則不動作。
- **③clipboard + fallback**：優先 `navigator.clipboard.writeText(text)`；reject 或無 API → `fallbackCopy(text)`（建隱藏 `readonly` textarea、`position:fixed;left:-9999px;opacity:0`、`focus()`+`select()`+`document.execCommand('copy')`、複製後移除）。
- **④按鈕狀態**：成功 → `flashDone()` 把按鈕文字換成 `✅ 已複製！`/`✅ Copied!`（`${JSON.stringify(t(...))}` render 期注入）+ 加 `.copied`（綠 #238636），2s 後還原原文字與樣式。
- **⑤active 沉下感**：`.copy-btn` 加 `transition:transform/opacity/background`；新增 `.copy-btn:active { transform:scale(0.97); opacity:0.8 }` + `.copy-btn.copied { background:#238636 }`。

### 驗收（線上實測 `bugezy.dev/install`，中英）
1. handler 綁定：`fallbackCopy`/`flashDone` present（IIFE + addEventListener）✅
2. 複製內容含商店連結（新 ID `hfnkjlbbpehkflgfbjenfmnmjkdjadcj`）+ `mcpServers` JSON ✅
3. clipboard 失敗 fallback：`execCommand('copy')` + 隱藏 textarea present ✅
4. 按下變「✅ 已複製！」綠色 2s 恢復：`DONE_LABEL`（中「已複製！」/ 英「✅ Copied!」）+ `.copy-btn.copied` CSS present ✅
5. active 沉下：`.copy-btn:active { transform:scale(0.97); opacity:0.8 }` present ✅

### Deploy
- `tsc --noEmit` clean → `wrangler deploy` Version `fae65da2`（cron 保留）。

### 註記
- 僅改 /install（PM-192 範圍）；首頁（GET /）有同款 copy 按鈕與 handler，未在本卡範圍，如需一併修可另開卡。

---

## DONE-192（續修）— Claude Code 回報（2026-07-07）

**「一鍵複製」有回饋但貼出空白 → 根因診斷 + 修復**

### 診斷結果（依 FOX 四點）
- **①複製的文字內容**：實測 `#ai-install-prompt` 的 `textContent` **非空**（線上 EN 560 字、ZH 382 字，含 Chrome Web Store 連結 + `mcpServers` MCP JSON）。已在 handler 加 `console.log('[BugEzy] copy prompt length=…preview=…')` 供 DevTools 確認。→ 內容來源沒問題。
- **②CSP 是否擋 inline script**：**沒有**。/install 回應 CSP `script-src 'self' 'unsafe-inline'`——inline script 允許執行；且 clipboard API（`navigator.clipboard` / `execCommand`）**不受 CSP 管轄**。另確認無 `Permissions-Policy: clipboard-write` 類封鎖 header。→ 排除 CSP 因素。
- **③CSP 不是問題** → 不需搬外部 JS 或加 script-src hash（維持 inline，並在註解說明原因）。
- **④fallback 修復**：**根因就在 fallback**——原 fallback textarea 用 `opacity:0` + `left:-9999px`，**部分瀏覽器對隱藏/離屏元素 `select()` 失效**，`execCommand('copy')` 仍回 `true`（假成功 → 觸發按鈕回饋），但實際剪貼簿是空的 → 「有回饋卻貼出空白」。當 `navigator.clipboard.writeText` 在某些情境 reject 時就會走到這條壞 fallback。

### 修法（`server/src/index.ts` installPage，皆 server 端）
- fallback textarea 改為「**視窗內 1px 可選取**」：`position:fixed; top:0; left:0; width:1px; height:1px; padding/border/outline/boxShadow:none; background:transparent`（不再 `opacity:0` / `left:-9999px`），並加 `ta.setSelectionRange(0, text.length)`，確保 `select()` 生效 → `execCommand('copy')` 真的複製。
- 加 `console.log` 診斷：點擊印文字長度 + 前 80 字預覽；`clipboard.writeText OK` / `clipboard.writeText failed → fallback` / `fallback execCommand copy ok=<bool>` 三種路徑各有 log。
- 主路徑仍優先 `navigator.clipboard.writeText`（成功即 `flashDone`），reject 或無 API → 走修好的 fallback。
- ①②③④⑤（PM-192 首修的視覺回饋/active/2s 恢復）保留。

### 驗收（線上實測 `bugezy.dev/install`）
1. 新 handler present：`console.log('[BugEzy] copy prompt length` + `ta.style.width`（1px）+ `setSelectionRange` ✅
2. 舊壞 pattern 已移除：`opacity = '0'` / `left = '-9999px'` **0 筆** ✅
3. 路徑 log present：`clipboard.writeText OK` + `fallback execCommand copy ok` ✅
4. 內容非空已證（EN 560 / ZH 382 字，含商店連結 + mcpServers JSON）✅
5. CSP 非阻因（`unsafe-inline` present、無 Permissions-Policy 封鎖）✅

### Deploy
- `tsc --noEmit` clean → `wrangler deploy` Version `403b46da`（cron 保留）。

> FOX 實機驗收建議：開 /install → F12 Console → 點「一鍵複製」→ 看 log 走哪條路徑（正常應 `clipboard.writeText OK`；若 reject 會看到 `fallback execCommand copy ok=true` 且此次 fallback 已能真的複製）→ 貼上驗證非空。

---

## DONE-192（三修）— Claude Code 回報（2026-07-07）

**「一鍵複製」仍複製空白 → 改最穩健方案：複製源與 DOM 完全解耦（`data-copy-text`）**

### 修改（依 FOX 三點，`server/src/index.ts`，皆 server 端）
- **①`data-copy-text` attribute**：installPage 與 homePage 各把安裝指令抽成 `const aiPrompt = t(zh, en)`（`<pre id="ai-install-prompt">` 與按鈕共用同一來源）；`#copy-ai-prompt` 按鈕加 `data-copy-text="${encodeURIComponent(aiPrompt)}"`（encodeURIComponent 只產 URL-safe 字元，可安全放進雙引號 attribute）。
- **②handler 改讀 dataset**：新增 `getText()` — 優先 `btn.dataset.copyText`（`decodeURIComponent`），**不再依賴 DOM textContent**（DOM 可能被 PM-190 改寫、空白差異、瀏覽器行為影響）；讀不到才 fallback 到 `#ai-install-prompt` textContent。
- **③首頁同法修**：homePage 原本是最初版簡單 handler（無 fallback、無按鈕回饋），一併換成與 installPage 相同的穩健版（`getText`+dataset、clipboard→視窗內 1px textarea execCommand fallback、按鈕變「✅ 已複製！」2s 恢復），並補 `.copy-btn:active { scale(0.97); opacity:0.8 }` + `.copy-btn.copied` CSS。
- **一致性**：PM-190 token 自動填入（`.mcp-cfg` textContent 補 `?token=`）後，同步 `cbtn.setAttribute('data-copy-text', encodeURIComponent(cpre.textContent))`，讓「顯示的 pre」與「複製的內容」一致（登入皆帶 token、未登入皆乾淨）。

### 為何這次能解
- 前兩修都依賴點擊當下讀 DOM textContent；只要 DOM 在某情境為空/被動過就空白。三修把**完整文字在 server render 期就編碼進按鈕自身的 attribute**，複製時直接 `decodeURIComponent(btn.dataset.copyText)` 取回——與 DOM 節點、clipboard 時序、瀏覽器差異全部無關，來源保證非空。

### 驗收（線上實測 `bugezy.dev`）
1. /install：`data-copy-text` decode = **560 字**（含 Chrome Web Store 連結新 ID + `mcpServers` JSON）、handler 有 `dataset.copyText` + `decodeURIComponent` ✅
2. 首頁（GET /）：同上，`data-copy-text` decode 560 字、handler 讀 dataset ✅
3. 兩頁按鈕皆 robust fallback（1px textarea execCommand）+ active/copied 視覺回饋 ✅
4. `tsc --noEmit` clean → `wrangler deploy` Version `0bea1edc`（cron 保留）✅

> FOX 實機驗收：/install 與首頁點「一鍵複製」→ 貼上應為完整安裝指令（非空）；F12 Console 有 `[BugEzy] copy prompt length=560`。

---

## DONE-192（四修）— Claude Code 回報（2026-07-07）

**/install 登入狀態「一鍵複製」仍空白 → 找到並移除 install 獨有的 data-copy-text 覆寫**

### 釐清 + 根因
- 三修其實**已改** installPage（按鈕有 `data-copy-text`、handler 讀 `dataset.copyText`，線上實測 decode 560 字）——與 FOX「只改了首頁」的判斷不同；但 /install **登入狀態**確實仍空白，另有原因。
- **真正根因**：installPage 比 homePage 多一段 PM-190（三修加的）「同步」——token 填入後執行 `cbtn.setAttribute('data-copy-text', encodeURIComponent(cpre.textContent || ''))`，**只在登入時跑**。此段把原本靜態、可靠的 `data-copy-text` 覆寫成「當下 DOM textContent」；一旦該取值在登入情境為空/異常，複製就空白。**未登入不跑（正常）、登入才空白**——正是首頁沒有、install 獨有的差異。

### 修法（`server/src/index.ts` installPage）
- **移除** PM-190 之後的 `data-copy-text` 同步（`getElementById('copy-ai-prompt')` + `setAttribute('data-copy-text', …)` 整段刪除）。
- PM-190 `.mcp-cfg` forEach **跳過** `#ai-install-prompt`：`if (el.id === 'ai-install-prompt') return;`——它的複製來源固定走 server render 的靜態 `data-copy-text`，textContent 全程不被 JS 改動，**與首頁完全一致**。
- 其餘 `.mcp-cfg` 設定區塊（連線 URL、Claude Desktop / Antigravity 的 `<pre>`、`claude mcp add`）**照常**自動補 `?token=`（只有 AI 安裝提示這塊比照首頁保持靜態乾淨）。

### 為何這次能解
- install 的複製來源現在與首頁**機制完全相同**：`data-copy-text` 於 server render 期就 `encodeURIComponent(aiPrompt)` 寫入，**JS 全程不碰**（不再有登入時的覆寫、textContent 也不被 token 改寫）→ 不論登入/未登入、任何瀏覽器，`decodeURIComponent(btn.dataset.copyText)` 都取得完整 560 字安裝指令。

### 驗收（線上實測 `bugezy.dev/install`）
1. 新 skip 行 `el.id === 'ai-install-prompt'` present ✅
2. 舊 `setAttribute('data-copy-text'` 同步 **0 筆**（已移除）✅
3. `data-copy-text` decode = **560 字**（含 Chrome Web Store 連結新 ID + `mcpServers` JSON）✅
4. handler 讀 `dataset.copyText` + `decodeURIComponent` ✅
5. `tsc --noEmit` clean → `wrangler deploy` Version `ff8c6dd2`（cron 保留）✅

> FOX 實機驗收：**登入狀態**開 /install → 點「一鍵複製」→ 貼上應為完整安裝指令（非空）。

---

## DONE-192（五修）— Claude Code 回報（2026-07-07）

**installPage 複製壞掉（alert 不彈=JS 語法錯）→ 改回與 homePage 字元級一模一樣**

### 狀況
- FOX 手動把 installPage 複製 script 改成除錯版：`var COPY_TEXT = ${JSON.stringify(aiPrompt)};` + click 時 `alert('COPY_TEXT length=…')`，且**按鈕拿掉了 `data-copy-text`**。此版 alert 不彈 → 該 inline script 有語法錯（整段沒執行）。

### 修法（依 FOX 五點，`server/src/index.ts` installPage）
- **①**按鈕加回 `data-copy-text="${encodeURIComponent(aiPrompt)}"`（與首頁按鈕同）。
- **②**複製 script 用 `getText()` 從 `btn.dataset.copyText`（`decodeURIComponent`）讀。
- **③**刪掉 `var COPY_TEXT = ${JSON.stringify(aiPrompt)};` 那行。
- **④**刪掉 `alert('COPY_TEXT length=…')` 那行。
- **⑤**installPage 的複製 IIFE 直接複製貼上 homePage 的複製 IIFE——**字元級一模一樣**（含 `getText`/`fallbackCopy`/clipboard→1px textarea execCommand/按鈕回饋，連 `console.log('[BugEzy] home …')` 標籤都相同）。
- PM-190 token 填入 IIFE 維持四修的「跳過 `#ai-install-prompt`」，複製來源＝靜態 `data-copy-text`，全程不被 JS 改動。

### 驗收
1. 原始碼：Python 抽出兩頁複製 IIFE 比對 → **`IDENTICAL: True`** ✅
2. 原始碼：`COPY_TEXT` 0 筆、除錯 `alert('COPY_TEXT` 0 筆、兩按鈕皆有 `data-copy-text` ✅
3. `tsc --noEmit` clean ✅
4. 線上 /install（propagate 後）：`COPY_TEXT` 0、`alert(` 0、`dataset.copyText` handler present、`data-copy-text` decode **560 字**（含商店連結 + mcpServers JSON）✅
5. `wrangler deploy` Version `8e1693f1`（cron 保留）✅

> FOX 實機驗收：/install（登入/未登入皆可）點「一鍵複製」→ 貼上為完整安裝指令（非空）、按鈕變「✅ 已複製！」、無 alert 干擾。

---

## DONE — PM-192 精準轉錄（Whisper）麥克風修復（2026-07-07）

**症狀**：popup 選精準轉錄 → 按錄製 → 出現麥克風授權彈窗（約3秒）→ 授權後音量跳表不動；即時字幕模式完全正常。

### 根因（查 `offscreen.ts` / `background.ts`）
- **音量條不動的真因**：語音音量分析用 `AudioContext` + `AnalyserNode`。**offscreen document 沒有 user gesture** → `new AudioContext()` 一開始是 `suspended` 狀態，音訊圖不會被推進，`getByteFrequencyData()` 讀到全 0 → 5 條音量條永遠是矮紅平的。
- **為何「即時字幕正常、精準轉錄不動」**：即時字幕（realtime）的音量／語音在**頁面（MAIN world）**跑，頁面有使用者互動 gesture，AudioContext 正常運轉；精準轉錄（whisper）的音量在 **offscreen** 跑，無 gesture → suspended。這正是兩者差異點。
- **次要問題（觀測性）**：offscreen `startRecording` 的 `getUserMedia` 失敗被 `catch` 吞掉，且 `OFFSCREEN_START_MIC` handler **秒回 `{ok:true}`**（在 `getUserMedia` 完成前）→ background 永遠以為麥克風開成功，真失敗（授權被撤、裝置被佔用、或**擴充 ID 變動**導致 chrome-extension:// origin 權限失效）完全無 log、難以診斷。

### 修法（純 extension）
- **`offscreen.ts startVolumeMeter`**：`new AudioContext()` 後若 `state==='suspended'` → `await audioCtx.resume()`，讓 context 運轉、analyser 正常取值（麥克風擷取型 context 允許無 gesture resume）。加 log 印 state。
- **`offscreen.ts startRecording`**：改回傳 `{ok:boolean, error?}`；`getUserMedia` 失敗回傳 `{ok:false, error:'<name>: <msg>'}`（不再靜默吞掉）。
- **`offscreen.ts` OFFSCREEN_START_MIC handler**：改 `startRecording().then(sendResponse)` + `return true`（等真正錄音啟動再回應真實結果，取代秒回 ok:true）。
- **`background.ts`（whisper 啟動處）**：`OFFSCREEN_START_MIC` 的回應改讀 `micRes.ok`——成功 log「Groq Whisper」，失敗 log「⚠ offscreen 麥克風開啟失敗：<error>」（不阻擋錄製）。

### 驗收
- `tsc --noEmit` clean → `npm run build` ✅
- dist：`offscreen.js` 含 `resume`/`suspended`/`startRecording().then` + getUserMedia 錯誤回傳；`background.js` 含 `micRes?.ok` 分支 ✅
- **純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收。

### FOX 實機驗收 & 補充
1. 開精準轉錄錄製 → 對麥克風講話 → **音量條應綠色跳動**（context 已 resume）。
2. 若音量仍不動、且 DevTools（offscreen）Console 出現 `getUserMedia 失敗: NotAllowedError`／background 出現「offscreen 麥克風開啟失敗」→ 代表是**麥克風授權**問題：很可能是本 session 幫 manifest 加了 `"key"` 欄位 → 本地載入的**擴充 ID 從舊 ID 變成 Web Store ID（hfnkj…）**，舊 ID 的麥克風授權失效，需在新 ID 下重新授權一次（chrome://settings/content/microphone 或重新從 popup 麥克風 toggle 觸發授權頁）。此屬環境權限，非程式 bug；本次已讓該失敗會在 log 現形。

---

### 🟡 PM-193：精準轉錄麥克風授權引導 — 選「允許這次使用」時 fallback + 提示

**問題**：使用者選「允許這次使用」時，權限只給當前頁面，offscreen document 拿不到 → 精準轉錄（Whisper）麥克風無法開啟。選「允許這個網站使用」則正常。

**根因**：Chrome 的「允許這次使用」= 單次、單頁面權限，offscreen document 是另一個頁面所以拿不到。這是瀏覽器設計限制，無法繞過。

**修法**：偵測到失敗 → 自動 fallback + 引導使用者。

---

#### §1 background.ts：偵測 offscreen 麥克風失敗 → fallback 即時字幕

offscreen 回報 `{ok: false}` 時：

```ts
if (!micRes?.ok) {
  // 精準轉錄麥克風失敗 → 自動 fallback 到即時字幕
  console.warn('Whisper mic failed, fallback to realtime');
  
  // 通知 content.ts → inject.ts 顯示頁面提示
  chrome.tabs.sendMessage(tabId, {
    type: 'BUGEZY_MIC_FALLBACK',
    message: '精準轉錄麥克風授權不足，已切換為即時字幕模式。下次請選「允許這個網站使用」。',
    messageEn: 'Whisper mic permission denied. Switched to realtime mode. Next time select "Always allow".',
  });
  
  // 改用即時字幕模式繼續錄製（不中斷錄製）
  await startRealtimeMode(tabId);
}
```

---

#### §2 inject.ts：收到 fallback 訊息 → 頁面顯示提示條

```ts
window.addEventListener('message', (event) => {
  if (event.data?.type === 'BUGEZY_MIC_FALLBACK') {
    const lang = getBugezyUILang();
    const msg = lang === 'zh' ? event.data.message : event.data.messageEn;
    showMicFallbackTip(msg);
  }
});

function showMicFallbackTip(msg: string) {
  const tip = document.createElement('div');
  tip.style.cssText = 'position:fixed;top:0;left:0;right:0;background:#f59e0b;color:#000;text-align:center;padding:10px;font-size:13px;font-weight:600;z-index:2147483647;';
  tip.textContent = '⚠️ ' + msg;
  document.body.prepend(tip);
  setTimeout(() => tip.remove(), 8000);
}
```

---

#### §3 popup mic 提示卡片加小字

在 popup 的「開啟麥克風」提示卡片加一行：

```html
<p class="mic-hint">💡 精準轉錄需選「永久允許」此網站使用麥克風</p>
```

---

#### §4 i18n

```ts
'mic-fallback-tip':  { zh: '⚠️ 精準轉錄麥克風授權不足，已切換為即時字幕模式。下次請選「允許這個網站使用」。', en: '⚠️ Whisper mic permission denied. Switched to realtime mode. Next time select "Always allow".' },
'mic-perm-hint':     { zh: '💡 精準轉錄需選「永久允許」此網站使用麥克風', en: '💡 Whisper requires "Always allow" microphone permission' },
```

---

#### §5 不改的東西

- 即時字幕流程：不動
- offscreen AudioContext resume（剛修的）：不動
- server / MCP：不動

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 精準轉錄 → 選「允許這次使用」→ offscreen 失敗 → **自動切到即時字幕 + 頁面頂部橘色提示 8 秒**
2. 精準轉錄 → 選「允許這個網站使用」→ **正常 Whisper 錄音**
3. popup mic 提示卡片有「💡 精準轉錄需選永久允許」小字
4. 錄製不中斷（fallback 無縫切換）
5. 中英文正確

---

---

## DONE-193 — Claude Code 回報（2026-07-07）

**精準轉錄麥克風授權引導 — 選「允許這次使用」時 fallback 即時字幕 + 提示** — 純 extension。

### 根因
Chrome「允許這次使用」= **單次、單頁面**權限；offscreen document 是**另一個頁面**，拿不到該權限 → Whisper（offscreen）麥克風開不了。選「允許這個網站使用」才會給 offscreen。屬瀏覽器設計限制，無法繞過 → 偵測失敗後自動 fallback + 引導。

### 改動
- **`types.ts`**：`START_RECORDING` 加 `micFallback?: boolean`。
- **`background.ts startRecording`（§1）**：offscreen `OFFSCREEN_START_MIC` 回報 `micRes.ok=false`（或 try/catch 例外）→ 設 `whisperMicFailed=true`；`START_RECORDING` 帶 `{ micFallback: whisperMicFailed }`。（`ready=false`「完全未授權」不算 fallback，維持原「跳過語音」。）
- **`content.ts`（§1/§2）**：`START_RECORDING` handler 讀 `msg.micFallback`——若為 true 且 `computeStartFlags` 原本算出 whisper → **改送即時字幕旗標**（`useOldVoice=true, whisperMode=false`）給 inject（頁面 SpeechRecognition 可用，因頁面已有「這次」權限），錄製不中斷；並呼叫 `showMicFallbackTip()` 在**頁面頂部顯示橘色提示條 8 秒**（content 與頁面共用 DOM 直接建節點，`z-index:2147483647`，文字取 `ct('mic-fallback-tip')`）。
- **`i18n.ts`（§4）**：`mic-fallback-tip`（含 ⚠️）+ `mic-perm-hint`，中英。
- **`popup.html` + `popup.ts`（§3）**：語音模式選擇（#micMode）下方加 `#micPermHint`「💡 精準轉錄需選『永久允許』此網站使用麥克風」+ CSS；`updateMicModeUI` 讓它跟 micMode 一起顯示/隱藏（付費版 + 麥克風開啟才顯示）。

### §5 不動
即時字幕流程、PM-192 的 offscreen `AudioContext.resume`、server/MCP — 皆未改。

### 設計註記
- 規格 §1/§2 描述「background → content → **inject** 顯示提示」。實作由 **content 直接建立橘色提示條**（content 是 page-context script、與頁面共用 DOM，i18n 用 `ct()`）——可視結果（頁面頂部橘色條）與規格一致，且省去 content↔inject 跨世界轉發，較穩健。「通知 content」+「頁面顯示橘色提示」兩項需求皆滿足。
- fallback 只在「精準轉錄有嘗試但 offscreen getUserMedia 失敗」時觸發；完全未授權（`ready=false`）維持原行為。

### 驗收（dist wiring + 邏輯）
1. 精準轉錄 → offscreen 失敗 → **自動即時字幕 + 頁面頂部橘色提示**（background `whisperMicFailed`→`micFallback`；content `showMicFallbackTip`+改旗標）✅
2. 精準轉錄 → offscreen 成功 → 正常 Whisper（micFallback=false，不觸發）✅
3. popup 有「💡 精準轉錄需選永久允許」小字（`micPermHint`，付費+mic on 顯示）✅
4. 錄製不中斷（fallback 只改語音旗標，START_RECORDING 照常送）✅
5. 中英：`mic-fallback-tip`/`mic-perm-hint` 雙語 ✅

dist 驗證：`background.js` `whisperMicFailed`/`micFallback`；`content.js` `micFallback`/`showMicFallbackTip`/`bugezy-mic-fallback-tip`；`popup.js/html` `micPermHint`/`mic-perm-hint`；i18n 鍵 bundled。

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架 + 實機驗收（選「允許這次使用」驗 fallback、選「允許這個網站」驗正常 Whisper）。

---

### 🟡 PM-194：鍵盤模式勾選時自動關閉麥克風

**問題**：popup 勾選「⌨ 鍵盤模式（關閉語音）」時，上方的麥克風 toggle 仍然是 ON。應該同步關閉——鍵盤模式 = 不用語音 = 麥克風 OFF。反過來也要處理：取消勾選鍵盤模式時，不自動開麥克風（讓使用者自己決定）。

---

#### §1 popup.ts：鍵盤模式 checkbox change 事件

```ts
keyboardCheckbox.addEventListener('change', () => {
  if (keyboardCheckbox.checked) {
    // 鍵盤模式 ON → 強制關閉麥克風
    if (micToggle.checked) {
      micToggle.checked = false;
      micToggle.dispatchEvent(new Event('change')); // 觸發麥克風關閉邏輯
    }
  }
  // 鍵盤模式 OFF → 不自動開麥克風（使用者自己決定）
});
```

---

#### §2 麥克風 toggle change 事件

麥克風開啟時，如果鍵盤模式是勾選的，自動取消鍵盤模式：

```ts
micToggle.addEventListener('change', () => {
  if (micToggle.checked && keyboardCheckbox.checked) {
    // 使用者手動開麥克風 → 自動取消鍵盤模式
    keyboardCheckbox.checked = false;
    keyboardCheckbox.dispatchEvent(new Event('change'));
  }
  // ... 現有的麥克風開關邏輯
});
```

---

#### §3 不改的東西

- 錄製 / 截圖 / 回溯邏輯：不動
- server / MCP：不動
- 鍵盤模式本身的功能：不動（只改 toggle 聯動）

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 麥克風 ON → 勾鍵盤模式 → 麥克風自動變 OFF
2. 鍵盤模式 ON → 手動開麥克風 → 鍵盤模式自動取消
3. 鍵盤模式 OFF → 麥克風保持原狀（不自動開）
4. 存 storage 狀態正確（reload 後一致）

---

---

## DONE-194 — Claude Code 回報（2026-07-07）

**鍵盤模式與麥克風 toggle 互斥聯動** — 純 extension，`extension/src/popup.ts`。

### 改動
- **§1 keyboardMode change handler**：存 `KEYBOARD_MODE_KEY` 後，`if (keyboardMode.checked && micToggle.checked)` → `micToggle.checked = false; micToggle.dispatchEvent(new Event('change'))`——透過 dispatch 走 micToggle 既有關閉邏輯（會存 `MIC_KEY:false` + `updateMicUI`）。取消勾選鍵盤模式 → 不自動開麥克風（無反向動作）。
- **§2 micToggle change handler（開頭）**：`if (micToggle.checked && keyboardMode.checked)` → `keyboardMode.checked = false; keyboardMode.dispatchEvent(new Event('change'))`——透過 dispatch 走 keyboardMode 既有邏輯（存 `KEYBOARD_MODE_KEY:false`）。放在 handler 最前，代表「使用者意圖開麥克風」即取消鍵盤模式。

### §3 storage 同步
- 兩段都用 `dispatchEvent('change')` **復用對方既有 handler 的 storage.set**，故 `MIC_KEY` / `KEYBOARD_MODE_KEY` 恆同步、reload 後一致（不會兩者同時 true）。

### 無事件迴圈（已驗）
- 勾鍵盤 → §1 設 mic false + dispatch → micToggle handler：§2 條件 `micToggle.checked(false) && …` = false → 略過，只存 `MIC_KEY:false`。不回彈。
- 開麥克風 → §2 設 keyboard false + dispatch → keyboardMode handler：§1 條件 `keyboardMode.checked(false) && …` = false → 略過，只存 `KEYBOARD_MODE_KEY:false`。不回彈。

### §3 不動
錄製/截圖/回溯、server/MCP、鍵盤模式本身功能 — 皆未改（只加 toggle 聯動）。

### 驗收（邏輯 + dist wiring）
1. 麥克風 ON → 勾鍵盤模式 → 麥克風自動 OFF ✅
2. 鍵盤模式 ON → 手動開麥克風 → 鍵盤模式自動取消 ✅
3. 鍵盤模式 OFF → 麥克風保持原狀（不自動開）✅
4. storage 狀態正確、reload 一致（兩 handler 各存自己的 key）✅

dist 驗證：`popup.js` 含 `keyboardMode.checked = false`（§2）、`micToggle.checked = false`（§1 + 既有授權 revert）、`dispatchEvent(new Event(`（兩處）。

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待重上架。

---

### 🟡 PM-195：編輯報告頁 Token 計算加美金單位（跟報告頁同步）

**問題**：停止錄製後的編輯報告頁（補充說明頁），底部 Token 計算沒有美金單位（只顯示數字）。但送出後的最終報告頁有顯示 `≈ USD $0.0049`。兩頁應一致。

---

#### §1 找到編輯報告頁的 Token 計算

搜尋 extension/src 裡的 token 計算顯示（可能在 popup.ts 或報告編輯相關的檔案），找到只顯示數字沒有 USD 的地方，加上 `≈ USD $` 前綴。

格式跟報告頁一致：
```
📊 Token 估算：~608 tokens ≈ USD $0.0049
💡 同場景 Claude in Chrome：~15,200 tokens ≈ USD $0.1216
✅ BugEzy 為你省了 96%
```

---

#### §2 不改的東西

- 最終報告頁（server 渲染）：不動（已正確）
- MCP 回傳的 Token 估算：不動（已正確）
- 計算邏輯：不動（只改顯示格式）

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 錄製停止 → 編輯報告頁 → Token 計算有 `≈ USD $` 單位
2. 送出後報告頁 → Token 計算格式一致
3. 數字正確

---

---

## DONE-195 — Claude Code 回報（2026-07-07）

**編輯報告頁 Token 估算加美金單位（跟最終報告頁一致）** — 純 extension。

### 定位
- 編輯/補充說明頁的 Token 顯示在 `extension/src/edit-report.ts` `renderTokenEstimate()`（PM-55，停止錄製後上傳前顯示）。
- 原本總計列 `~N tokens ≈ $${bugezyUSD}`、Claude in Chrome 對比 `~N tokens ≈ $${chromeUSD}`——**只有 `$` 沒有 `USD`**。
- 最終報告頁（server `index.ts` 報告頁 client 渲染，line 2681/2683）為 `~N tokens ≈ USD $<amount>`。

### 改動（`edit-report.ts` line 111~112）
- 兩處 `≈ $` → `≈ USD $`，與 server 報告頁字元級一致；計算值不變（`(totalTokens*8/1e6).toFixed(4)` 等）。
- 各區塊分項列（`~N tokens` 無金額）維持原樣——server 報告頁也只在總計/對比顯示 USD，一致。

### §2 不動
最終報告頁（server）、MCP Token 估算、計算邏輯 — 皆未改（只改編輯頁顯示格式）。

### 驗收
1. 錄製停止 → 編輯報告頁 → 總計與對比列有 `≈ USD $` ✅（dist `edit-report.js` `USD $`×2）
2. 與送出後報告頁格式一致（皆 `~N tokens ≈ USD $x.xxxx`）✅
3. 數字正確（邏輯未改）✅

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待重上架。

---

### 🟡 PM-196：報告頁分享連結加複製按鈕 + 我的報告加勾選刪除功能

**兩個 UX 改善合併**：

---

#### §1 報告頁（server /report/:id）：分享連結加複製按鈕

目前報告頁最下面有報告連結，但使用者要手動選取+複製。加一個 input 框 + 一鍵複製按鈕：

```html
<div class="share-box">
  <span>📤 分享報告連結：</span>
  <input id="share-url" type="text" readonly value="https://bugezy.dev/report/xxx" />
  <button onclick="...select+copy...">📋 複製連結</button>
</div>
```

用 select + execCommand 複製（不用 clipboard API，避免 PM-192 的坑）。按完變「✅ 已複製！」2 秒恢復。⚠️ onclick 裡不要用反斜線 regex（PM-192 教訓）。

---

#### §2 我的報告頁（server /reports）：勾選 + 批次刪除

列表每行加 checkbox + 底部「🗑️ 刪除選取」按鈕 + 全選。

**後端新增 DELETE /api/reports**：
- 接收 `{ report_ids: ["id1", "id2"] }`
- 驗證 session token + 確認是 owner
- 只刪自己的報告
- 最多 50 筆

**刪除前確認框**：「確定要刪除 N 份報告嗎？此操作無法還原。」

i18n 中英文。

---

#### §3 不改的東西

- 報告內容 / MCP / extension popup：不動
- ECPay / 認證邏輯：不動

#### Deploy

```
cd server && npx wrangler deploy
```

#### 驗收

1. 報告頁底部有「📋 複製連結」→ 按了貼到記事本有 URL
2. 我的報告有 checkbox → 勾選 → 底部出「🗑️ 刪除選取 (N)」
3. 全選 → 全勾 → 再點 → 全取消
4. 刪除 → 確認框 → 確定 → 報告消失
5. 中英文正確

---

---

## DONE-196 — Claude Code 回報（2026-07-07）

**報告頁分享連結一鍵複製 + 我的報告勾選批次刪除** — server 端。

### §1 報告頁（/report/:id）分享連結複製
- `reportPageHtml` 加分享列（預設 `display:none`）：`📤 分享報告連結：` + `#share-url` readonly input + `#share-copy` 按鈕 + CSS。
- **複製邏輯放外部 `report-page.js`（`?v=196`）** —— ⚠ 報告頁 CSP 是 `script-src 'self'`（嚴格、無 unsafe-inline），**inline onclick 會被擋**，故不能照規格用 onclick；改在 report-page.js `render()` 成功時：填 `shareInput.value = location.origin + '/report/' + reportId`、`shareBox.style.display='flex'`、按鈕 `addEventListener('click')` → `select()` + `setSelectionRange` + `document.execCommand('copy')`（**非 clipboard API、無反斜線 regex**，符合 PM-192 教訓）→ 變「✅ 已複製！」2s 恢復。付費牆/找不到報告時不顯示分享列（只在 render 成功顯示）。
- report-page.js 版本 `?v=188`→`?v=196`（強制邊緣快取失效）。

### §2 我的報告（/reports）勾選批次刪除
- `renderTable` 加勾選欄：表頭全選 checkbox（`#selectAll`，change→全部 `.report-cb` 同步 + `updateDeleteBar`）；每行 `.report-cb`（`data-id=report_id`，change→`updateDeleteBar`）。colSpan 空狀態 5→6。
- `updateDeleteBar`：算勾選數 → 底部 `#deleteBar`（≥1 勾才 `display:flex`）、`#deleteSelected` disabled 狀態、`#delCount` 數字、全選 checkbox 同步。
- `#deleteSelected` click：蒐集勾選 id → `confirm(T.delConfirm.replace('{n}', N))`（「確定要刪除 N 份報告嗎？此操作無法還原。」）→ `fetch('/api/reports', {method:'DELETE', Bearer, body:{report_ids}})` → 成功 `location.reload()`（token 在 localStorage，reload 正常）、失敗 `alert(T.delFail)`。CSS：`.col-cb`/`.report-cb`/`.delete-bar`/`.delete-btn`（紅）。

### §3 DELETE /api/reports（新增）
- `deleteReportsApi`：`verifySession`（Bearer）→ 無 token 401；解析 body `report_ids`（過濾非空字串、**最多 50 筆**）→ 空→400；`supa.from('reports').delete().eq('user_id', userId).in('report_id', ids).select('report_id')`——**`.eq(user_id)` 確保只刪自己的**（別人的 id 帶進來刪不到）；回 `{deleted:N}`；DB 錯誤 500（原始錯誤只 log）。路由 `method==='DELETE' && path==='/api/reports'`。同源呼叫（bugezy.dev→bugezy.dev）無 CORS preflight。

### §4 i18n
- /reports T 物件加 `selectAll`/`del`/`delConfirm`/`deleting`/`delFail`（中英）；報告頁分享列 `t(zh,en)`（分享報告連結/複製連結/已複製）。

### §3 不動
報告內容/MCP/extension popup、ECPay/認證邏輯 — 皆未改。

### 驗收（線上實測 `bugezy.dev`）
1. 報告頁底部「📋 複製連結」→ report-page.js v=196 含 `share-box`/`share-url`/`share-copy`/`execCommand('copy')`/`setSelectionRange`；報告頁 HTML 含分享列 + `?v=196` ✅
2. /reports checkbox → 底部「🗑️ 刪除選取 (N)」：`deleteSelected`/`report-cb`/`selectAll`/`deleteBar`/`DELETE`/`/api/reports` 皆present ✅
3. 全選/取消（`#selectAll` change 同步）✅（邏輯）
4. 刪除 → confirm → DELETE → reload ✅（邏輯）；`DELETE /api/reports` 無 token/錯 token → **401** ✅（實測）
5. 中英：EN「Delete selected」/「Select all」/「Share report link」/「Copy link」皆present ✅

### Deploy
- `tsc --noEmit` clean → `wrangler deploy` Version `27605259`（cron 保留）。**此 deploy 同時把先前已 commit 未 deploy 的 FOX 手改（guide/faq URL 清理 + install token-fill regex 反斜線修 + 舊 workers.dev URL 301 redirect）一併上線**——原列於 Day 22 收工 FOX 待辦，現已生效。

### 技術債（新增）
- 刪除只刪 `reports` DB 列，未清 R2 的 rrweb/screenshots 物件 → 成孤兒（報告已 404 不可存取，僅少量儲存成本）。可後續加 cron 或刪除時連帶清 R2。

---

### 🟡 PM-197：popup「複製 MCP 設定」加使用時機備註

**問題**：進階設定裡的「📋 複製 MCP 設定」按鈕，小白不知道什麼時候要用、用來做什麼。需要加一行備註說明使用時機。

---

#### §1 popup.html / popup.ts：按鈕下方加備註小字

在「📋 複製 MCP 設定」按鈕下方加一行灰色小字：

```html
<button id="copy-mcp-config" class="mcp-config-btn">📋 複製 MCP 設定</button>
<p class="mcp-hint" data-i18n="mcp-config-hint">當 AI 無法讀取你的報告時，按此複製，貼給你的 AI 重新設定</p>
```

CSS：
```css
.mcp-hint {
  color: #666;
  font-size: 11px;
  margin: 4px 0 0;
  text-align: center;
}
```

---

#### §2 i18n

```ts
'mcp-config-hint': { zh: '當 AI 無法讀取你的報告時，按此複製，貼給你的 AI 重新設定', en: 'If AI cannot read your reports, copy this and paste to your AI to reconfigure' },
```

---

#### §3 不改的東西

- 複製 MCP 設定的功能邏輯：不動
- server / MCP：不動

#### Build

```
cd extension && npm run build
```

#### 驗收

1. popup 進階設定 →「📋 複製 MCP 設定」按鈕下方有灰色小字
2. 中文：「當 AI 無法讀取你的報告時，按此複製，貼給你的 AI 重新設定」
3. 切英文：「If AI cannot read your reports, copy this and paste to your AI to reconfigure」

---

## DONE-197 — Claude Code 回報（2026-07-07）

**popup「📋 複製 MCP 設定」加使用時機備註（P1）** — 純 extension 改動。

### 改動
- **`extension/src/popup.html`（§1）**：`#copyMcpBtn` 下方（`#copyMcpFeedback` 之前）加 `<p class="mcp-hint" data-i18n="mcp-config-hint">當 AI 無法讀取你的報告時，按此複製，貼給你的 AI 重新設定</p>`；新增 `.mcp-hint` CSS（`color:#666; font-size:11px; margin:4px 0 0; text-align:center`，與規格一致）。
- **`extension/src/i18n.ts`（§2）**：新增 `mcp-config-hint` 鍵——ZH「當 AI 無法讀取你的報告時，按此複製，貼給你的 AI 重新設定」/ EN「If AI cannot read your reports, copy this and paste to your AI to reconfigure」（字串與規格一致，切語言由既有 `applyTranslations` 的 data-i18n 套用）。

### §3 不動（已確認）
- 複製 MCP 設定的功能邏輯、server / MCP → 皆未改。

### 驗收（dist wiring）
1. 按鈕下方有灰色 11px 小字（`.mcp-hint`）✅
2. 中文字串符合規格 ✅
3. 英文字串符合規格 ✅
- dist 驗證：`dist/popup.html` 與 `dist/popup.js` 皆含 `mcp-config-hint`。

### Build
- `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架生效。

---

### 🟡 PM-198：精準轉錄（Whisper）錄音正常但無文字產生

**問題**：麥克風音量條有跳動（PM-193 修復成功），代表錄音正常。但停止後沒有文字產生（voice_count=0）。問題在 offscreen 錄完後送 /api/transcribe 這段流程。

**BugEzy 報告佐證**：report_id `83d07e3a-3431-4118-914c-c8ef7581c74f`，voice_count=0。

---

#### §1 排查 offscreen.ts

1. MediaRecorder 停止後有沒有正確產生 audio Blob
2. Blob 的 mimeType 是否正確（audio/webm）
3. Blob 有沒有正確透過 chrome.runtime.sendMessage 傳給 background.ts
4. 加 console.log：`[offscreen] recording stopped, blob size=${blob.size}, type=${blob.type}`

---

#### §2 排查 background.ts

1. 有沒有收到 offscreen 傳來的 audio data
2. 有沒有正確呼叫 `POST /api/transcribe`（帶 Authorization header + FormData）
3. /api/transcribe 回傳的 response status 和 body 是什麼
4. 回傳的文字有沒有正確存入報告的 voice_transcript
5. 加 console.log：`[background] transcribe request sent, size=${blob.size}` 和 `[background] transcribe response status=${res.status}, text=${data.text}`

---

#### §3 排查 server /api/transcribe

1. 有沒有收到請求
2. Workers AI Whisper 有沒有回傳結果
3. 有沒有被認證或付費檢查擋住（PM-135）

---

#### §4 常見根因猜測

- offscreen 的 MediaRecorder 產生的 Blob 可能是空的（size=0）
- background 送 transcribe 時 Authorization header 可能漏了
- Workers AI 的 Whisper 模型可能 timeout 或回傳空結果
- PM-169 域名遷移後 API_BASE 變了，但 offscreen 可能還用舊 URL

---

#### §5 不改的東西

- 即時字幕（Web Speech API）：不動
- server /api/transcribe 邏輯：先查再改
- MCP / ECPay：不動

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 精準轉錄 → 麥克風跳動 → 停止 → 有文字產生
2. 報告有 voice_transcript
3. 即時字幕仍正常

---

## DONE-198 — Claude Code 回報（2026-07-07）

**精準轉錄（Whisper）錄音正常但無文字產生 — 全鏈路埋 log + 收斂靜默失敗（P1）** — 純 extension。

### 排查結論（§1~§4，程式面皆正常，根因需靠實機 log 定位）
逐段查 offscreen → background → server，**程式碼路徑本身沒有寫死的 bug**，但整條「停止→轉錄→存檔」鏈路**幾乎零 log 且會靜默吞掉失敗**，所以「錄音正常卻 voice_count=0」無法從現有輸出判斷斷點。故本卡核心＝**把每個環節的成敗變成可見的 console.log**，並修掉會造成「靜默無文字」的兩個吞錯點。

- **§1 offscreen.ts**：`stopRecording` 停止後**確實**用 `new Blob(chunks, 'audio/webm;codecs=opus')` 產 Blob；`ondataavailable` 只推 `size>0` 的 chunk；`blob.size<100` 才判「錄音太短」。mimeType 正確。→ 加 log 印 `blob size / type / chunks`。
- **§2 background.ts**：`stopMicAndTranscribe` **有**帶 `getAuthHeaderOnly()`（Authorization: Bearer）+ `FormData`（audio + language）POST `/api/transcribe`。**但**：①`STOP_RECORDING` handler `await stopMicAndTranscribe()` **完全丟棄回傳值**（401/403/空文字全無感）；②`transcribeRes.json()` 若回應非 JSON（如 401/502 HTML）會 throw 進 catch 被吞成泛用「轉錄失敗」。
- **§3 server /api/transcribe**（只查未改）：邏輯正確——`getAuthUserId` 401「請先登入」→ `isActiveUser` 403「付費功能」→ Groq `whisper-large-v3-turbo` `verbose_json` → 回 `{ok:true,text,...}`。**最可能的線上真因就落在這三個 return 之一（token 失效 401 / 付費狀態 403 / Groq 空結果），而它們過去在 extension 端全被靜默丟棄**。
- **§4 API_BASE**：`types.ts` `API_BASE='https://bugezy.dev'`（PM-169 後正確），全 extension 僅此一處；offscreen 不打 API（只回音訊）。**排除舊網址問題**。

### 改動（皆 extension，log 用 `console.*` 非 gated `blog`，實機 DevTools 直接看得到）
- **`offscreen.ts`**：`stopRecording` onstop 印 `[BugEzy offscreen] recording stopped, blob size=…, type=…, chunks=…`；太短印 warn。
- **`background.ts stopMicAndTranscribe`**：
  - 印送出前 `transcribe request sent, size=…, type=…`、`POST …/api/transcribe, hasAuth=…, lang=…`。
  - **改用 `res.text()` 先取原始回應再 `JSON.parse`**（非 JSON 不再 throw 被吞，改印 `status + body 前 200 字`）。
  - 無條件印 `transcribe response status=…, ok=…, text=…`（成功/失敗都看得到）。
  - **存檔條件收緊**：由 `if (result.ok)` → `if (result.ok && text.trim())` 才寫 `VOICE_TRANSCRIPT_KEY`（避免存空字串殘留到下一場）；ok=false/空文字改印明確 warn「→ voice_count 將為 0」。
- **`background.ts STOP_RECORDING` case**：印 `micMode=…`（非 whisper 就不會轉錄）；`stopMicAndTranscribe()` 回傳值**不再丟棄**，印 `ok / textLen / error`。
- **`background.ts RECORDING_DONE`**：併入 Whisper 文字 / 無文字（key 存在但空 vs 不存在）各印一行，確認有無寫進報告 payload 的 `voiceTranscript`。

### §5 不動
即時字幕（Web Speech）、server /api/transcribe 邏輯（只查未改）、MCP/ECPay、offscreen `AudioContext.resume`（PM-192）— 皆未改。

### 驗收
1. `tsc --noEmit` clean → `npm run build` ✅；dist 驗證 `background.js` 含 `transcribe response status`/`STOP_RECORDING, micMode`、`offscreen.js` 含 `recording stopped, blob size`。
2. 純 extension，**不需 wrangler deploy**；待 FOX 重上架後實機驗收。
3. **FOX 實機驗收（關鍵）**：精準轉錄錄一段 → 停止 → 開 **background service worker DevTools + offscreen DevTools** Console，依序應看到：
   `[offscreen] recording stopped, blob size>0` → `[background] transcribe request sent, size>0` → `hasAuth=true` → `transcribe response status=200, ok=true, text=…`。
   哪一行斷掉即定位根因：blob size=0＝錄音沒進 chunk；hasAuth=false＝未登入/token 沒同步；status=401＝token 失效；status=403＝付費狀態未同步；ok=true 但 text 空＝Groq 空結果。把該行貼回即可精準修下一版。

---

### 🟡 PM-199：編輯報告頁分享連結加複製按鈕

**問題**：錄製完上傳後的編輯報告頁（extension edit-report），底部「✅ 已上傳！分享連結：URL」沒有複製按鈕。PM-196 只加在 server 報告頁，extension 的編輯報告頁漏了。

---

#### §1 分享連結旁加複製按鈕

找到 extension 裡顯示「已上傳！分享連結」的程式碼，在連結旁加一個「📋」複製按鈕：

- 點擊後複製 URL（用 hidden input + select + execCommand）
- 按鈕變「✅」2 秒後恢復「📋」
- 樣式：紫色小按鈕（跟報告頁一致）

---

#### §2 不改的東西

- server 報告頁：不動（PM-196 已有）
- popup / MCP：不動

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 錄製 → 上傳 → 底部連結旁有「📋」按鈕
2. 按了 → 貼到記事本有 URL
3. 按鈕變「✅」2 秒恢復

---

## DONE-199 — Claude Code 回報（2026-07-07）

**編輯報告頁分享連結加複製按鈕（P1）** — 純 extension 改動。

### 定位
- 顯示「✅ 已上傳！分享連結：URL」的程式在 `extension/src/edit-report.ts` `uploadBtn` click handler（line 744~，上傳成功分支）——原本只 `replaceChildren('✅ 已上傳！分享連結：')` + 一個 `<a>` 連結，無複製按鈕（PM-196 只加在 server 報告頁）。

### 改動（§1，`edit-report.ts`）
- `<a>` 之後動態建立：
  - **hidden `<input readonly>`**（`copyInput`）持有 `shareUrl`——樣式 `position:absolute;width:1px;height:1px;opacity:0;left:0;top:0`（**視窗內 1px 可選取**，非 `display:none`/離屏，確保 `select()`+`execCommand` 生效，沿用 PM-192 教訓）。
  - **「📋」紫色小按鈕**（`copyBtn`，`background:#7c3aed;color:#fff`，與報告頁分享按鈕一致）。
  - click → `copyInput.select()` + `setSelectionRange(0, len)` + `document.execCommand('copy')`（**不用 clipboard API**，try/catch 防呆）→ 文字變「✅」，`setTimeout` 2 秒恢復「📋」。

### §2 不動（已確認）
- server 報告頁（PM-196 已有複製）、popup / MCP → 皆未改。

### 驗收（dist wiring）
1. 上傳成功 → 連結旁有「📋」按鈕 ✅
2. 複製用 hidden input + `select` + `setSelectionRange` + `execCommand('copy')`（非 clipboard API）✅
3. 按後變「✅」2 秒恢復「📋」✅
- dist 驗證：`edit-report.js` 含 `copyInput`/`copyBtn`/`setSelectionRange(0, shareUrl.length)`/`execCommand`，title `複製連結`（複製連結）。

### Build
- `tsc --noEmit` clean → `npm run build` ✅。**純 extension，不需 wrangler deploy**；待 FOX 重上架生效。

---

### 🟢 PM-200：全站「安裝 Chrome 擴充」按鈕對接正式 Web Store 連結

**目標**：把所有連到 Chrome Web Store 通用首頁 `https://chromewebstore.google.com/` 的按鈕/連結，改成 BugEzy 正式商店頁 `https://chromewebstore.google.com/detail/bugezy/hfnkjlbbpehkflgfbjenfmnmjkdjadcj`（此 URL 永久不變，不隨版本更新）。並檢查首頁（homePage）「安裝擴充 / 免費開始」按鈕是否正確對接。

## DONE-200 — Claude Code 回報（2026-07-07）

**全站「安裝 Chrome 擴充」按鈕對接正式 Web Store 連結** — server 端。

### 稽核結果（`server/src/index.ts`，全檔 grep `chromewebstore.google.com` / `chrome.google.com/webstore`）
- **通用首頁連結：只有 1 處** —— `/install` 頁 Step 1 的「前往 Chrome Web Store →」CTA 按鈕（line 1524），原 `href="https://chromewebstore.google.com/"`（通用首頁）→ **已改為正式商店詳情頁** `.../detail/bugezy/hfnkjlbbpehkflgfbjenfmnmjkdjadcj`。
- 其餘 4 處（line 505/521/1391/1407）是「AI 幫你安裝」提示文字（首頁 + /install 的 ai-install-prompt），**本就已是 detail URL**，未動。
- 舊格式 `chrome.google.com/webstore` → **0 筆**。

### 首頁（homePage）按鈕稽核 — 皆正確對接（未改，設計如此）
- 首頁所有「🧩 安裝 Chrome 擴充功能 / 免費安裝 → / 安裝後購買 → / 免費開始 / 升級」CTA（line 645/777/791/807/1840、付費牆 2579）**皆連到 `/install` 安裝指南頁**——這是刻意的 onboarding 漏斗（`/install` 內含加到 Chrome 步驟 + MCP 設定 + 一鍵複製），而 `/install` 的商店按鈕現已指向正式 detail 頁。故整條漏斗 首頁 →「/install」→ 正式商店頁 已完整對接，無死連結/通用首頁。
- 未把首頁按鈕改成直連商店：`/install` 有 MCP 設定等關鍵新手步驟，直連會略過 → 維持既有漏斗較佳。

### 驗收（線上實測 `bugezy.dev`）
1. `/install` Step 1「前往 Chrome Web Store →」→ 正式商店詳情頁 ✅（curl 實測 href = `.../detail/bugezy/hfnkjlbbpehkflgfbjenfmnmjkdjadcj`）
2. 全站無 `href="https://chromewebstore.google.com/"` 通用首頁連結 ✅（curl 實測 0 筆）
3. 首頁 CTA → `/install` 漏斗 → 正式商店頁，完整對接 ✅

### Deploy
- `tsc --noEmit` clean → `wrangler deploy` Version `0be0cd64`（cron `0 3 * * *` 保留）✅

---
