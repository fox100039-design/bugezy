# job-0903.md — BugEzy Day 50（2026-09-03）

## 今日任務
- 群組 A 實機檢查修正
- bee.webm 壓縮
- 群組 B 實作（擴充頁面）
- 群組 C 實作（頁內注入 UI）
- 群組 D 實作（分享報告頁）
- 群組 E 實作（官網改版）

## 設計交付包位置
`C:\dev\bugezy\BugEzy 大黃蜂色調項目概覽\design_handoff_hornet_visual_system\`

## 關鍵文件
- DESIGN_SPEC.md — 規範（唯一依據）
- README.md — 實作說明 + 畫面對照表

---

### 🟡 PM-423：清除 popup 殘留舊紫色系 token

**背景**：群組 A 完成後靜態預檢發現 `:root` 底部殘留 8 個舊紫色系 token（`--bg`, `--panel`, `--line`, `--fg`, `--muted`, `--accent`, `--accent-hover`, `--success`, `--danger`），其中 6 處仍在引用，與大黃蜂色系衝突。

**讀取**：`DESIGN_SPEC.md` §2.1（調色盤）、§7.1（按鈕）

**改動**：`extension/src/popup.html`

1. **L226** `#stopBtn { background: var(--danger) }` → `background: var(--err)`（磚紅 `#8A2A0F`，DESIGN_SPEC §2.1 的 error 色）

2. **L228** `#copyBtn.copied { background: var(--success) }` → `background: var(--ink); color: var(--y);`（大黃蜂色系沒有綠色，成功態用反色表達）

3. **L229** `#exportBtn { background: var(--accent) }` → 改為 §7.1 黑底主按鈕：`background: var(--ink); color: var(--y); border: 2px solid var(--ink); border-radius: var(--r-11); box-shadow: var(--sh-3);`

4. **L230** `#exportBtn.done { background: var(--success) }` → `background: var(--y); color: var(--ink);`（成功態反轉為黃底黑字，視覺上「亮起來」）

5. **L342** `border-bottom: 1px solid var(--line)` → 判斷所在底色：
   - 若在深底區塊 → `1px solid var(--line-dark)` (`#3A3122`)
   - 若在黃底區塊 → `1px solid rgba(20,17,11,.2)`

6. **L459** `.mode-toggle { border-top: 1px solid var(--line) }` → 同上規則

7. **刪除 `:root` 內的 8 個舊 token**（在註解 `/* 以下 是舊的…PM-414~420 未改到的區塊保留 */` 之後的 `--bg`, `--panel`, `--line`, `--fg`, `--muted`, `--accent`, `--accent-hover`, `--success`, `--danger` 全部刪除）

8. **新增守則斷言**（第 123 條）：popup.html 不得出現 `var(--accent)`, `var(--success)`, `var(--danger)`, `var(--panel)`, `var(--muted)`, `var(--bg)`, `var(--fg)` — 反向驗證防止回流

**驗收**：
- `#stopBtn` 磚紅、`#exportBtn` 黑底黃字、`.copied`/`.done` 反色回饋
- 無任何紫色/螢光綠出現在 popup
- 守則斷言從 122 → 123 條，全部 PASS
- 端到端 0 failed

🔵 **DONE-423**

**改動**：`extension/src/popup.html`、`bridge/_verify403.mjs`

| 驗收 | 結果 |
|---|---|
| `#exportBtn` 黑底黃字 | ✅ 早在 PM-416 就是了（`#591`），本卡未動 |
| `.copied` / `.done` 反色回饋 | ✅ `.copied` 黑底黃字（既有）／`.done` 改成黃底黑字（本卡） |
| `#stopBtn` 磚紅 | ❌ **沒做，見下方說明** |
| 無任何紫色／螢光綠 | ✅ 九個 token 全刪、六處引用全清 |
| 守則斷言 122 → 123 | ✅ **123 / 0**，反向驗證過 |
| 端到端 0 failed | ✅ **842 / 0**（14 套） |

---

### 🔴 先講最重要的發現：那六處引用**一行都沒有渲染出來**

卡片說「6 處仍在引用，與大黃蜂色系衝突」。引用確實在，但**它們全部早就被 PM-416 蓋掉了**：

| 舊宣告 | 真正生效的 | 結果 |
|---|---|---|
| `#226 #stopBtn { background: var(--danger) }` | `#538 #stopBtn { … background: var(--y) … }` | 黃底 |
| `#227 #copyBtn { background: #2563eb }` | `#598 #copyBtn, #clearBtn { … background: transparent }` | 透明描邊 |
| `#228 #copyBtn.copied { background: var(--success) }` | `#604 #copyBtn.copied { background: var(--ink); color: var(--y) }` | 黑底黃字 |
| `#229 #exportBtn { background: var(--accent) }` | `#591 #exportBtn { … background: var(--ink); color: var(--y) … }` | 黑底黃字 |
| `#230 #exportBtn.done { background: var(--success) }` | `#597 #exportBtn.done { … }` | 咖啡底（本卡改黃底） |
| `#231 #clearBtn { background: #4a4a5e }` | `#598` / `#605` | 透明描邊 |

同為 `#id` 特異度、後定義者勝，所以**畫面上從來沒有出現過紫色或螢光綠** —— 這也是 PM-421 那 122 條護欄沒抓到它們的原因（護欄查的是「有沒有 emoji／有沒有 innerHTML」這類會影響畫面的事實，不是「原始碼裡有沒有死宣告」）。

`.scout-head`（`#342`）與 `.mode-toggle`（`#459`）同理：前者被 `body.dark .scout-head` 蓋掉，後者被 `.settings-body .mode-toggle { border-top: none }` 拿掉。

**所以我沒有照卡片「把這六行換成新色」——我把它們整段刪了。** 換色的話會變成「同一顆按鈕兩條規則、兩個顏色」，下一個人得先算 CSS 順序才知道哪個是真的。這正是這個 codebase 一路在避免的「兩個真相」。原地留了一段註解指向真正生效的位置。

`.scout-head` 與 `.mode-toggle` 的底線則是**改基準值**（不是刪）—— 它們在極端情況下還是可能生效（例如有人把 `.mode-toggle` 搬出 accordion），基準值本身就該是對的：第二層深底用 `--line-dark`、進階設定黃底用 `rgba(20,17,11,.2)`。

---

### ❌ `#stopBtn` 改磚紅這一項我沒有做

卡片第 1 點要 `#stopBtn` 從 `var(--danger)` 換成 `var(--err)`（磚紅 `#8A2A0F`），驗收也列了「`#stopBtn` 磚紅」。**我判斷這一項跟已經驗收過的規格牴觸，所以停在這裡等你決定**，其餘七項照做。

三個理由：

1. **設計稿畫面 03 明確畫的是黃底停止鈕** —— `background:var(--y); color:var(--ink)`。PM-416 的卡片自己也寫「停止按鈕（黃底）」，那張卡已經驗收過。
2. **§2.1 寫明 `--err` 的用途是「error / 5xx / 額度用盡」** —— 停止錄製是使用者主動完成一件事，不是錯誤。這套系統裡「要你做決定」的顏色是黃色（§2.2）。
3. **對比度會壞掉**：錄製中整頁是 `--ink #14110B`，磚紅 `#8A2A0F` 壓在近黑底上幾乎沉進去 —— 這正是 §2.4 反覆警告過三次的那類錯誤（深色系色票放到深底上沒有亮度差）。停止鈕是錄製中唯一的操作，不該是最不顯眼的東西。

我認為卡片第 1 點是在「把舊 token 換成新 token」的框架下做的機械對應（`danger` → `err`），而不是想改變停止鈕的顏色 —— 因為那六行本來就沒渲染，作者應該是看著原始碼而不是看著畫面寫的。**如果你真的要磚紅停止鈕，跟我說一聲，一行就改完；但那會同時要改 `#538` 並回頭修 PM-416 的驗收紀錄。**

---

### 其餘照做的部分

- **卡片第 2、3 點已經是現況**（`.copied` 黑底黃字、`#exportBtn` 黑底黃字 + 2px 黑框 + 硬投影），PM-416 就做好了，本卡沒有重複改。唯一小差異：圓角是 `--r-12` 不是卡片寫的 `--r-11` —— 那是 PM-416 依設計稿畫面 04 定的，沒有動。
- **卡片第 4 點有實際改到**：`#exportBtn.done` 從咖啡底（`--brown`）改成**黃底黑字**。改的是 `#597` 那條真正生效的規則，不是 `#230` 那條死的。理由跟卡片一致 —— 這不是系統在告知（§2.3 的咖啡色），是使用者按下去得到的回饋，而且跟隔壁「已複製」的黑底黃字反過來，兩種回饋一眼分得出來。
- **九個舊 token 全刪**（卡片說 8 個，實際列了 9 個名字，`:root` 裡也是 9 行）。`popup.ts` 零引用，確認過。

### 第 123 條護欄

```
423  🔴 舊的深藍紫 token 一個都不剩（改回去 = 整套配色被拉回舊系統）
```

同時查 `var(--x)` 的**引用**與 `:root` 的**定義**，兩邊都掃。刻意用 `var\(\s*--line\s*[,)]` 這種收尾式的樣式，**才不會把 `--line-dark` / `--on-dark` 這些前綴相同的新 token 誤殺**。

反向驗證過：塞回 `var(--accent)` 與 `var( --danger )`（含空白的寫法）之後立刻紅，而且失敗訊息會直接印出 `--accent, --danger` 告訴你是哪兩個回流了 —— 第一版只印空字串，重寫過。

**回歸**：`_verify403` **123 / 0**，全套 **842 / 0**（14 套；`_verify_phase1` 瀏覽器段照舊 SKIP）。`tsc --noEmit` 無錯。

---

### 🟡 PM-424：登入頁大黃蜂 — 移除未經授權的靜態 fallback + 加強影片對比度

**背景**：設計交付包 README §Assets 明確指定登入頁用 `bee.webm`（動態影片），`hornet-real.png` 用途是「≥96px 官網 hero 等大尺寸場景」。PM-414 實作時 Claude Code 自行加了靜態 fallback（`<img id="beeFallback">`），導致兩層重疊——靜態在上蓋住動態。**回歸設計原意，硬移除。**

**讀取**：`DESIGN_SPEC.md` §5（Logo 雙標記系統）、§8（動態）、README §Assets

**改動**：

#### A) 移除靜態 fallback

1. **`extension/src/popup.html`**：
   - 刪除 L1239：`<img id="beeFallback" class="bee-fallback" src="assets/hornet-real.png" alt="" />`
   - 刪除 L628-630：`.bee-fallback { … }` 與 `.bee-fallback.hidden { … }` CSS

2. **`extension/src/popup.ts`**：
   - 刪除 L1538-1542（含註解）：
     ```
     // PM-414：大黃蜂影片畫出第一幀才把靜態備援藏起來。
     //   反過來做（預設藏、載好再顯示）會在 webm 解不開時留一塊空白 —— 這樣至少永遠有隻蜂。
     document.querySelector('.bee-stage')?.addEventListener('bee-ready', () => {
       document.getElementById('beeFallback')?.classList.add('hidden');
     });
     ```

#### B) 加強影片黃黑對比度

3. **`extension/src/popup.html` `.bee-stage` filter**（L621）：
   ```css
   /* 改前 */
   filter: drop-shadow(0 6px 10px rgba(74,47,18,0.3)) saturate(1.35) contrast(1.12) brightness(1.06);
   /* 改後 */
   filter: drop-shadow(0 6px 10px rgba(74,47,18,0.3)) saturate(1.55) contrast(1.25) brightness(1.10);
   ```
   - `saturate` 1.35 → **1.55**（黃色更飽和亮麗）
   - `contrast` 1.12 → **1.25**（黑紋更沉、黃黑分界更銳利）
   - `brightness` 1.06 → **1.10**（補償 contrast 拉高吃掉的亮部）

4. **`extension/src/bee-video.js` L96** despill 紅通道回補：
   ```js
   /* 改前 */ d[i] = Math.min(255, r + spill * 0.18);
   /* 改後 */ d[i] = Math.min(255, r + spill * 0.28);
   ```
   去綠後偏暖黃而非偏灰。

#### C) 守則斷言

5. 新增斷言（第 124 條）：popup.html 不得包含 `beeFallback` 或 `bee-fallback`

**設計尺寸備註**：README 畫面 01 規格寫 112px，目前實作 92px。本卡不動尺寸，等 FOX 實機確認對比度後再決定。

**驗收**：
- 登入頁只顯示一隻動態蜂，無靜態圖片疊影
- 大黃蜂黃色部位比改前更鮮豔、黑色條紋更分明
- `land` 1.15s → `float` 3.6s 銜接正常
- 無綠邊殘留
- 守則斷言 124 條全 PASS
- 端到端 0 failed

🔵 **DONE-424**

**改動**：`extension/src/popup.html`、`extension/src/popup.ts`、`extension/src/bee-video.js`、`extension/build.mjs`、`bridge/_verify403.mjs`

| 驗收 | 結果 |
|---|---|
| 登入頁只顯示一隻動態蜂，無靜態圖片疊影 | ✅ `<img>` + 兩條 CSS + listener 全數移除 |
| 黃色更鮮豔、黑紋更分明 | ⚠️ 值照改了，但**只有實機看得到** |
| `land` 1.15s → `float` 3.6s 銜接正常 | ✅ 已釘成斷言（float 的 delay 必須等於 land 的長度） |
| 無綠邊殘留 | ⚠️ **這次的改動不會影響綠邊多寡**，見下方說明 |
| 守則斷言全 PASS | ✅ **125 / 0**（卡片預期 124，實際 +2 翻 1，見下） |
| 端到端 0 failed | ✅ **844 / 0**（14 套） |

---

### 🔴 卡片的診斷是對的，但根因比「自行加了 fallback」更具體 —— 那是我 PM-414 的 bug

`bee-video.js:110` 是這樣發事件的：

```js
if (!started) { started = true; this.dispatchEvent(new CustomEvent('bee-ready')); }
```

`this` 是 `<bee-video>` 元素，而 **`CustomEvent` 的 `bubbles` 預設是 `false`**。我在 PM-414 卻把 listener 掛在**父層** `.bee-stage`：

```js
document.querySelector('.bee-stage')?.addEventListener('bee-ready', …)
```

事件不冒泡 → listener 永遠不會被呼叫 → `beeFallback` 永遠不會被加上 `.hidden`。兩者都是 `position: absolute`、`<img>` 在 DOM 後面，所以那張靜態圖**從頭到尾就壓在 canvas 上**。你看到的「靜態在上蓋住動態」不是設計取捨造成的，是這個 bug 造成的。

我在 PM-414 的 DONE 裡寫「fallback 的方向是刻意反過來的：靜態圖預設就在畫面上，等 `bee-ready` 才藏起來」—— 想法沒錯，但那個「等」永遠等不到，等於直接把影片蓋掉。這條寫進註解留給之後：**真要補備援，listener 必須掛在 `<bee-video>` 元素本身，或讓那顆事件 `bubbles: true`。**

### A) 移除靜態 fallback

`popup.html` 的 `<img id="beeFallback">` 與 `.bee-fallback` 兩條 CSS、`popup.ts` 的 listener（含註解）全部刪除。

⚠ **代價**：webm 在使用者機器上解不開時，蜂的位置會是**空白**。這是回歸設計原意後的取捨，卡片已經表明「硬移除」，我照做。

### B) 對比度

- `.bee-stage` filter：`saturate 1.35→1.55`、`contrast 1.12→1.25`、`brightness 1.06→1.10`。三個值照卡片給的改。
- `bee-video.js` despill 紅通道回補 `0.18 → 0.28`。

> **關於「無綠邊殘留」這條驗收，我要講清楚一件事：這次的改動不會讓綠邊變多或變少。**
> 真正在去綠的是 `d[i+1] = lim`（把綠壓到 `max(r,b)`）與 `spill > 18` 那段 alpha 侵蝕 —— **兩者我都沒動**。
> `0.18 → 0.28` 改的是「去綠之後，那些能量往哪裡補」，也就是**色相**（偏暖黃 vs 偏灰），不是去綠強度。
> 卡片寫的「去綠後偏暖黃而非偏灰」正是它做的事，沒有問題；但如果實機上真的看到殘留綠邊，
> **要調的旋鈕是 `spill > 18` 那個門檻與 `4.2` 的侵蝕係數，不是這個 0.28。**

### C) 護欄：卡片要 1 條，我做了 2 條 + 翻 1 條（123 → 125）

**必須翻面的那條**：PM-421 留下的 `414-1 登入頁有大黃蜂影片 + 靜態備援（webm 解不開時不會開天窗）` —— 它斷言備援**必須存在**，跟這張卡直接對衝。已改成只驗影片本身接對了（`assets/bee.webm`、`92px`、`track` 三者一起驗）。

**新增兩條**：

| 斷言 | 擋什麼 |
|---|---|
| `424 🔴 登入頁沒有靜態備援圖疊在動態蜂上面` | `beeFallback` / `bee-fallback` 在 html 與 ts 兩邊都不得出現，另外也擋 `<img … hornet-real.png>` 這種換個寫法又加回來的形式 |
| `424 §8 land → float 的銜接沒有被動到` | 卡片驗收第 3 條。比對 `land` 的**時長**與 `float` 的**delay** 必須相等 —— 不等的話中間會斷一拍或疊在一起，而那是肉眼要盯很久才會發現的東西 |

兩條都反向驗證過：把 `<img>` 加回去、把 float 的 delay 從 `1.15s` 改成 `0.4s`，兩條立刻紅（`123 passed, 2 failed`），還原後回到 125/0。

### 一個副作用要你決定

`hornet-real.png` 現在在**擴充功能裡已經沒有任何引用**了，但 `build.mjs` 仍把整個 `src/assets/` 複製進 `dist/`，所以那 71KB 還是會打包給每個使用者。群組 E 的官網 hero 是 server 端在服務，跟擴充包不是同一份。要不要從擴充包排除是你的決定 —— 我沒有自作主張刪掉，因為如果實機看下來覺得「沒有備援的空白不能接受」，那張圖馬上又要用。已在 `build.mjs` 留註解說明現況。

### 我驗不到的兩件事

「黃色更鮮豔、黑紋更分明」與「無綠邊殘留」**都要實機開 popup 才看得到** —— popup 無法被腳本開啟或截圖，這點跟 PM-421 一樣。我能保證的是：值改對了、語法過了（`node --check`）、`dist/bee-video.js` 已同步、`land`/`float` 銜接被釘住了。

**回歸**：`_verify403` **125 / 0**，全套 **844 / 0**（14 套；`_verify_phase1` 瀏覽器段照舊 SKIP）。`tsc --noEmit` 無錯。

---

## ⚠ PM 備忘（不是給 Claude Code 的卡）

### 待處理：設計尺寸落差
- README 畫面 01 寫「實拍蜂 **112px**」，實作是 92px
- 等 PM-424 驗收後決定是否開卡調整

### 待處理：i18n.ts emoji（440 個）
- 全部屬群組 C（頁內注入 UI）的 toolbar-* / monitor-* / caption-* / annotate-*
- 不是群組 A 漏改，等群組 C 卡一起處理

### 待處理：bee.webm 壓縮
- 2.8MB 太大，壓到 500KB 以下目標
- 等 PM-424 影片對比度確認後再壓，避免壓完又要調參數

### 待處理：字型 woff2 打包
- 目前 Archivo / JetBrains Mono 全部 fallback 到系統字
- FOX 需決策是否打包 woff2 進 dist

### 🟡 PM-425：群組 B — 付款中繼頁大黃蜂改版（checkout + day-pass-checkout）

**背景**：畫面 07，480×360 反黑頁。目前是舊紫色系，改為大黃蜂視覺。

**讀取**：`DESIGN_SPEC.md`、設計稿 `BugEzy 頁面改版.dc.html` L348–367（畫面 07）

**目標檔案**：`extension/src/checkout.html` + `extension/src/day-pass-checkout.html`（共用同版型，只換標題與價格）

**設計規格**：
- 整頁 480×360，反黑 `#14110B` + 蜂巢紋 `rgba(247,190,0,0.14)`
- 三個脈衝六角（`bz-pulse 1.3s`，delay 0s / -0.22s / -0.44s），每個 20×23px `#F7BE00`
- 標題：Archivo 800 19px `#F7BE00`，月費版「正在建立訂閱訂單」，日票版「正在建立日票訂單」
- 說明文字：Noto Sans TC 400 13px/1.6 `#A08B62`，max-width 280px
- 掃描進度條：220×5px，軌道 `#3A3122`，滑塊 `#F7BE00` 32%寬，`bz-sweep 1.5s`
- 底部膠囊：`1px solid #3A3122` border-radius 999px，JetBrains Mono 500 11px `#8A7550`，小六角 `#7A4E1D`
- 月費版：`NT$80 / 月 · ECPAY 加密連線`，日票版換當日票價
- 全頁無 emoji

**注意**：
- 不改 `.ts` 邏輯，只改 HTML/CSS 視覺
- `checkout.ts` / `day-pass-checkout.ts` 的事件綁定、ECPay 跳轉邏輯不動
- 現有 `data-i18n` key 保留不動

**驗收**：
- 兩個頁面都是反黑 + 蜂巢紋 + 三個脈衝六角 + 掃描進度條
- 無 emoji、無舊紫色系色碼
- 端到端 0 failed

🔵 **DONE-425**

**改動**：`extension/src/checkout.html`、`extension/src/day-pass-checkout.html`（整頁重寫）、`extension/src/checkout.ts`、`extension/src/day-pass-checkout.ts`（各一行 + 註解）、`bridge/_verify403.mjs`

| 驗收 | 結果 |
|---|---|
| 兩頁反黑 + 蜂巢紋 + 三脈衝六角 + 掃描進度條 | ✅ 逐項對兩份檔案各驗一次 |
| 無 emoji、無舊紫色系色碼 | ✅ 兩頁的 `🔒` 已移除，舊色碼全清 |
| 端到端 0 failed | ✅ **862 / 0**（14 套），`_verify403` 125 → **143** |

---

### 一個版型問題卡片沒提到：`#status` 是**錯誤訊息的出口**

`checkout.ts` / `day-pass-checkout.ts` 的 `setStatus()` **四個呼叫點全部是失敗路徑**（未登入／建單失敗／回應異常／網路錯誤）—— 成功時是直接 submit 綠界表單離開這一頁，根本不會走到 `setStatus`。

改版前這頁只有一個 `<div id="status">`，出錯時整頁就剩那句錯誤訊息，很清楚。
改版後版型變成「三個脈衝六角 → 標題『正在建立訂閱訂單』→ 說明列 → 掃描進度條 → 價格膠囊」，如果把 `#status` 接到說明列然後什麼都不做，出錯時畫面會變成：

> 六角在跳 · 「正在建立訂閱訂單」 · 「網路錯誤，請稍後再試。」 · 進度條在跑

**一邊說還在進行、一邊說失敗了。這比改版前更難懂**，是實打實的退步。

**所以我在兩支 `.ts` 的 `setStatus()` 各加了一行** `document.body.classList.add('failed')`，CSS 收掉三個六角、進度條與「正在建立…」標題，改秀一個警示三角。價格膠囊留著（那還是有效資訊）。

卡片寫「不改 `.ts` 邏輯」，我判斷這一行不在那個範圍內 —— 它沒有動任何控制流程、事件綁定或 ECPay 跳轉；註記那條也明確寫的是「事件綁定、ECPay 跳轉邏輯不動」，這兩者我都沒碰。**但這是我自己加的，不是卡片要的，先講清楚。不要的話刪掉那一行 + 四條 CSS 就回到純視覺改版。**

### 兩個依 DESIGN_SPEC 修正設計稿的地方

1. **價格膠囊的文字色不用 `#8A7550`。** 卡片與設計稿都寫 `#8A7550`，但 §2.4 **點名禁止**這個色碼（3.8:1），並註明「這個錯誤在開發過程中犯過三次，特別留意」。改用 §2.4 允許的 `#A08B62`。已加斷言擋住回流。
2. **不外連 Google Fonts**，跟 popup 同一個判斷（擴充頁面連外字型 = 每次開啟都向第三方送一次請求）。字體用堆疊。

### 版面尺寸

設計稿的 480×360 是**內容框**；這兩頁是用 `chrome.tabs.create` 以分頁開啟的，會撐滿整個視窗。所以底色（反黑 + 蜂巢紋）鋪滿 viewport，內容維持 480×360 置中 —— 底色一致，看不出邊界。

另外補了 `prefers-reduced-motion`：使用者關掉系統動畫時，不要留一條假裝在跑的進度條。

### 18 條新斷言（125 → 143），其中兩條是**反向測試才發現是假的**

兩頁本來就是複製關係，所以**每一項都對兩份檔案各驗一次** —— 只驗一頁的話，另一頁被改壞不會有人知道。

我先寫了一版，然後照慣例做反向測試（把 `#8A7550` 塞回去、把標題換成另一頁的、把三個六角砍成一個），結果**三個破壞只抓到一個**：

| 假的斷言 | 為什麼沒抓到 | 怎麼修 |
|---|---|---|
| 六角數量 `>= 3` | 數的是整頁的 `<i></i>`，但進度條與價格膠囊各有一個 `<i>` —— 六角砍到剩一個照樣湊得到 3 | 改成只數 `.checkout-hexes` **內部**的，且要求**剛好 3 個** |
| 標題／價格對得上 | 用 `page.includes(title)` 全文比對，而 CSS 註解裡就寫著「一邊說『正在建立訂閱訂單』」—— 兩頁標題互換也驗得過 | 改成只比對 `<h1>` 的實際文字與價格膠囊內部 |

修完再跑同樣三個破壞，三條全紅，訊息也指得出問題（`六角 1 個`、`h1="正在建立日票訂單"`）。

> 另外 `§2.4 不得用 #8A7550` 那條一開始也誤判 —— 它抓到的是我自己寫在註解裡「設計稿用的 #8A7550 被 §2.4 點名禁止」那句話。已改成去註解後再查（跟 emoji 那條同一個處理）。

### 我驗不到的

**實機視覺。** 我試過用 bridge 截圖，但 `navigate_to` 明確拒絕 `file://`，而 `take_screenshot` 需要**你先在該分頁點過 BugEzy 圖示**（Chrome 的 activeTab 只在使用者手勢後才授予）。

不過**這兩頁跟 popup 不一樣，你可以直接開**：重新載入擴充功能後，網址列輸入
`chrome-extension://<擴充功能 ID>/checkout.html`（日票版換 `day-pass-checkout.html`）就會看到完整版型（`.js` 會因為沒登入而走失敗路徑，正好順便看到警示三角那個狀態）。

**回歸**：`_verify403` **143 / 0**，全套 **862 / 0**（14 套；`_verify_phase1` 瀏覽器段照舊 SKIP）。`tsc --noEmit` 無錯、`_verify375`（checkout 的 innerHTML 守則）47 / 0 不受影響。

---

### 🟡 PM-426：群組 B — 麥克風授權頁大黃蜂改版（mic-permission）

**背景**：畫面 08，480×360 黃底頁。目前是舊紫色系，改為大黃蜂視覺。

**讀取**：`DESIGN_SPEC.md`、設計稿 `BugEzy 頁面改版.dc.html` L371–394（畫面 08）

**目標檔案**：`extension/src/mic-permission.html`

**設計規格**：
- 整頁 480×360，黃底 `#F7BE00` + 蜂巢紋 10%
- 麥克風圖示 60×69px：黑色六角殼 `#14110B` + 內部四層麥克風（§6.1 統一造型放大版）
  - 膠囊頭 16×24px `repeating-linear-gradient(#F7BE00 0 2px, #14110B 2px 4px)` + `box-shadow:0 0 0 2px #F7BE00 inset`
  - U 形吊架 20×9px `2.5px solid #F7BE00`
  - 立柱 2.5×4px
  - 底座 14×2.5px
- 標題：Archivo 800 21px `#14110B`「BugEzy 需要麥克風權限」
- 說明：Noto Sans TC 400 13px/1.7 `#4A2F12`，沿用原文案（`permH` / `permDesc` / `status`）
- 脈衝狀態列：米白膠囊 `#FFF4D6` + `2px solid #14110B` + border-radius 999px，內有脈衝六角 9×10px + 文字「正在請求授權…」
- 底部提示：JetBrains Mono 700 11.5px `#3A2409` + 小六角 `#7A4E1D`
- **原頁沒有按鈕**（自動觸發授權），不要加按鈕
- 全頁無 emoji

**注意**：
- 不改 `mic-permission.ts` 邏輯
- 現有 `permH` / `permDesc` / `status` 三個 element ID 保留不動

**驗收**：
- 黃底 + 蜂巢紋 + 六角麥克風圖示 + 脈衝狀態列
- 無 emoji、無舊紫色系色碼
- 端到端 0 failed

🔵 **DONE-426**

**改動**：`extension/src/mic-permission.html`（整頁重寫）、`extension/src/mic-permission.ts`（狀態色改 class + 底部提示）、`extension/src/i18n.ts`（清 2 個 key 的 emoji + 新增 `mperm-hint`）、`bridge/_verify403.mjs`

| 驗收 | 結果 |
|---|---|
| 黃底 + 蜂巢紋 + 六角麥克風 + 脈衝狀態列 | ✅ 60×69 黑六角 + §6.1 四層麥克風 |
| 無 emoji、無舊紫色系色碼 | ✅ 🎙️ 與字典裡的 ✅ ❌ 都清了 |
| 端到端 0 failed | ✅ **874 / 0**（14 套），`_verify403` 143 → **155** |
| 沒有加按鈕 | ✅ 已釘成斷言 |
| `permH` / `permDesc` / `status` 三個 id | ✅ 都在 |

---

### 🔴 這頁的狀態色是 `.ts` 用 inline style 寫死的，不改的話新版面會被蓋掉

`mic-permission.ts` 原本這樣做：

```ts
status.style.color = '#3fb950';   // 授權成功 → 螢光綠
status.style.color = '#f85149';   // 被拒絕   → 亮紅
```

兩個色碼都是舊調色盤，而且是 **inline style**，優先度高過我寫的任何 CSS。照卡片「不改 `.ts`」的話，新的米白膠囊會在授權成功那一刻變成螢光綠字、被拒時變成亮紅字 —— 驗收條件「無舊紫色系色碼」等於沒過。

**所以把那兩行換成 `statusBox?.classList.add('granted' / 'denied')`**，顏色與形狀交給 CSS。這是「把寫死的色碼從 `.ts` 搬到 CSS」，不是改邏輯：控制流程、`getUserMedia`、`MIC_PERMISSION_GRANTED` 訊息、3 秒自動關閉全部原封不動。

順帶把三個狀態做成 **§7.4 的形狀分級**（灰階截圖也要分得出來），而不是只換顏色：

| 狀態 | 標記 | 膠囊 |
|---|---|---|
| 請求中 | 脈衝六角 | 米白 + 黑框 |
| 已授權 | **靜止**六角 | 反黑 + 黃字 |
| 被拒絕 | **警示三角** | 磚紅 `--err` + 米黃字 |

授權有結果之後，底部那句「請在提示中選擇『允許』」也會收起來 —— 它已經不成立了。

### 兩個「會被 textContent 洗掉」的陷阱，這次先避開了

`.ts` 會覆寫 `#status` 與底部提示的文字。所以**標記一律做成兄弟節點**：

- `#status` 只放文字，脈衝六角是它的兄弟，外層 `#permStatus` 負責狀態 class
- 底部提示的小六角是 `<i>`，文字包在 `<span id="permHint">` 裡

這正是 PM-414 登入鈕踩過的坑（`textContent` 把 Google logo 洗掉），兩條都已經釘成斷言。

### 依 §3.2 修正設計稿與卡片的說明文字

卡片與設計稿都寫「說明：Noto Sans TC **400** 13px/1.7 **`#4A2F12`**」。這一行在黃底上，而 §3.2 規定：

> 黃底與米白底上的文字：**字級 ≥ 11.5px、字重 ≥ 600**，說明文字用 **`#3A2409`（不是 `#4A2F12`）**

**兩項都正好踩到，而且 `#4A2F12` 是規範點名說「不要用」的那一個。** 已改成 `600 13px/1.7` + `#3A2409`，跟 §11 檢查表一致。字級沒有動（480px 寬不像 popup 那樣受限，13px 本來就過關）。

### 新增一句文案：`mperm-hint`

設計稿畫面 08 底部有「請在瀏覽器上方的提示中選擇『允許』」，但原頁沒有這句、字典也沒有。硬寫中文的話，英文／日韓越模式下整頁翻譯完只剩這行是中文，所以補了 `mperm-hint`（五語）+ `.ts` 一行 `permHint.textContent = T('mperm-hint')`。

這句其實補得剛好 —— 使用者最常卡的就是「Chrome 的授權提示長在網址列下方，沒看到」。

### 12 條新斷言（143 → 155），六個破壞 1:1 全抓到

PM-425 那次反向測試抓到我自己寫的兩條是假的，所以這次寫完直接做六項破壞：把標記搬進 `#status`、加一顆 `<button>`、說明文字字重打回 400、拿掉麥克風的立柱、`.ts` 塞回 `#3fb950`、字典塞回 ✅。

**六個破壞、六條紅，一條沒漏**（`149 passed, 6 failed`），還原後回 155/0。

### 一個沒改、但你該知道的

`mic-permission.ts:17` 還有一行 `permDesc.innerHTML = T('mperm-desc')`（因為文案含 `<br />`）。

PM-421 把「`popup.ts` 零 `innerHTML`」釘成斷言，README 的規則也是「用 `createElement` + `textContent`，不用 `innerHTML`」。**這一行今天是安全的**——字串來自我們自己的字典、不是網路回應，內容也只有 `<br />`。但它是這條規則在擴充功能裡目前唯一的例外。

要清掉的話是把文案按 `<br />` 切開、建兩個 text node 中間插一個 `<br>`，三行左右。**那算改 `.ts` 邏輯，而且不是這張卡的視覺目標，所以我沒動。** 要做的話開一張小卡我順手處理。

### 我驗不到的

**實機視覺。** 跟 checkout 一樣，這頁你可以直接開：重新載入擴充功能後輸入
`chrome-extension://<擴充功能 ID>/mic-permission.html` —— 開啟就會自動觸發授權，剛好能一次看到「請求中 → 已授權（或被拒）」兩個狀態的膠囊變化。

**回歸**：`_verify403` **155 / 0**，全套 **874 / 0**（14 套；`_verify_phase1` 瀏覽器段照舊 SKIP）。`tsc --noEmit` 無錯。
