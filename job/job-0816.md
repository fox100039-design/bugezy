# job-0816.md — BugEzy Day 43

> 接續 job-0815.md（PM-301）

---

### 🟡 PM-302：規格書新增 §14 — The Octa-Memory Matrix（八層記憶矩陣）

**目標**：把八層記憶矩陣的完整規格寫進 HTML + md 規格書。

**檔案**：`docs/bugezy_v2_v3_product_spec.html` + `docs/BugEzy_v2_v3_規格書.md`

---

**§14 The Octa-Memory Matrix（八層記憶矩陣）**

**開頭說明**：
```
當這八層全部組建完成，BugEzy 的大腦結構會呈現完美的立體防線。
AI 不再是每次從零開始的臨時工，而是一個有記憶、會成長、
懂業務、知分寸的 AI 開發夥伴。
```

**§14.1 L1 Debug 經驗庫 🔴（動態）**
- 核心作用：10 秒秒殺重複 Bug，省 95% 推理 Token
- 記憶內容：每次 debug 成功後自動記錄：症狀 → 根因 → 修法 → 關鍵檔案
- 實戰範例：「TypeError: Cannot read 'map' of undefined → API 回傳 null 沒有預設值 → 加 ?? [] 防護」
- 下次遇到類似 error → 自動比對歷史 → 直接給修法
- 越用越聰明

**§14.2 L2 專案知識庫 🟡（靜態）**
- 核心作用：讓 AI 擁有整幅地圖，不用每次重複讀取大文件
- 記憶內容：架構文件、引擎清單、API 路由表、已知的坑
- 實戰範例：「這個欄位用 timestamptz 不是 timestamp」→ AI 不會再犯

**§14.3 L3 客服知識庫 🔵（支援）**
- 核心作用：產品內建客服，自動排查環境與安裝問題
- 記憶內容：安裝教學（Extension / MCP / Bridge）、FAQ、操作指南、錯誤排解
- 實戰範例：用戶問「MCP 連不上」→ AI 查 L3 →「最常見原因是 token 過期，請到設定頁重新複製」
- 整合 SKILL.md：靜態手冊 + 動態經驗 = 完整客服

**§14.4 L4 商業邏輯庫 🟢（業務）**
- 核心作用：確保 AI 修 Code 時，數據與業務規則完全正確
- 記憶內容：核心業務規則
- 實戰範例（LottoShare）：
  - 「冷熱門號碼的權重計算，任何時候分母都不能為 0，且勝率加總必須剛好等於 1 (100%)」
  - ENG-27 回傳的 JSON 格式正確（不噴錯），但數據總和 = 1.2 (120%)
  - 沒有 L4 → AI 說「沒有 bug」
  - 有 L4 → AI 說「違反商業規則，權重分配錯誤」→ 主動修正
- 這是 AI 最常犯的錯：程式碼寫對了，但商業邏輯改錯了

**§14.5 L5 外部依賴庫 🔵（環境）**
- 核心作用：分辨是自己的 Bug 還是第三方 API 掛掉
- 記憶內容：外部依賴的脾氣與坑
- 實戰範例：
  - 「第三方開獎數據 API 每天晚上 8:30~8:35 例行維護，回傳 503 屬正常」
  - ENG-12 在 8:32 噴網路錯誤
  - 沒有 L5 → AI：「你的程式壞了！」
  - 有 L5 → AI：「這是 API 維護，不是你的問題。已幫你加上 Retry Circuit Breaker，5 分鐘後自動重試」

**§14.6 L6 資安合規庫 🔴（防禦）**
- 核心作用：防止 AI 寫出有安全性漏洞或密碼外洩的程式碼
- 記憶內容：專案的資安鐵律
- 實戰範例：
  - 「嚴禁將 API_KEY、JWT_SECRET、DB 密碼寫死在代碼中，必須用 process.env」
  - 「使用者的密碼、身分證字號在日誌中必須完全遮蔽」
  - AI 修完 bug 自動「自我審查（Audit）」
  - 發現自己不小心寫死了一個變數 → 立刻自己改掉
  - 確保產出的 PR 100% 符合資安規範

**§14.7 L7 效能帳本庫 🟠（監控）**
- 核心作用：確保修好 Bug 的同時，網站速度不會變慢
- 記憶內容：各引擎正常運作時的效能基準（Baseline）
- 實戰範例：
  - 「正常情況下 ENG-15 大數據機率回歸引擎，單次運算 < 200ms，記憶體 < 128MB」
  - AI 修完 bug → 重啟引擎 → 自動測試比對帳本
  - 發現修完後運算卡了 2000ms（效能衰退 10 倍）
  - → 主動撤回修改 → 嘗試更省記憶體的寫法

**§14.8 L8 團隊協作庫 🟣（流程）**
- 核心作用：自動對接 Git 與團隊分工，PR 寫得比人類更優雅
- 記憶內容：模組負責人（Owner）+ Git 修改歷史
- 實戰範例：
  - 「Core-Algorithm 模組負責人是 Max，任何修改必須 @Max Review」
  - AI 修好 code → 自動發 PR → 完美的 commit message
  - 自動標記 @Max → 附上說明：「本次修改影響 ENG-12，已根據 Max 上週五在 Slack 提到的架構進行優化」

**§14.9 MCP 工具設計**

```
記憶寫入：
  bugezy:memory_save(layer, entry)     → 存入指定層
  bugezy:memory_learn(debug_session)   → debug 完成後自動萃取經驗存入 L1

記憶讀取：
  bugezy:memory_search(query, layers?) → 跨層搜尋相關記憶
  bugezy:memory_get(layer, topic)      → 精準提取指定層的特定主題

自動守衛：
  bugezy:memory_audit(code_diff)       → L6 資安審查
  bugezy:memory_perf_check(metrics)    → L7 效能比對
  bugezy:memory_biz_validate(output)   → L4 商業邏輯驗證
```

**§14.10 自動學習循環**

```
debug 成功
  → L1 自動存入經驗
  → L7 記錄修前修後效能
  → L6 審查修改是否合規
  → L4 驗證商業邏輯是否正確
  → 全通過 → 經驗確認
  → 下次遇到同類 bug → 直接提取 → 越來越快
```

**§14.11 開發順序**

```
Phase A（先做，最有即戰力）：
  L1 Debug 經驗庫 + L3 客服知識庫

Phase B（專案深度整合）：
  L2 專案知識庫 + L4 商業邏輯庫

Phase C（防禦層）：
  L5 外部依賴庫 + L6 資安合規庫

Phase D（進階）：
  L7 效能帳本庫 + L8 團隊協作庫
```

---

**格式要求**：
- 每一層用不同顏色的圓點標記（照截圖的配色）
- 每層有：為什麼需要 → 記憶內容 → 實戰威力
- 實戰範例用引號卡片或程式碼區塊
- §14.9 工具表格化
- §14.10 流程圖用 ASCII art
- 目錄導航加 §14 錨點
- md 版同步更新

**驗收條件**：
1. HTML §14 有 §14.1~§14.11 完整內容
2. 八層都有為什麼需要 + 記憶內容 + 實戰範例
3. MCP 工具表格完整
4. 開發順序 Phase A~D 明確
5. 目錄有 §14 錨點
6. md 版同步
7. 深色主題 + 顏色圓點 + 排版美觀

完成後在下方寫 `🔵 DONE-302`。

---

### 🔵 DONE-302：§14 八層記憶矩陣已寫入兩份規格書；**§14.9 的工具名與 §5 已定案的命名慣例衝突，已修正**

**改動檔案**：`docs/bugezy_v2_v3_product_spec.html`（v0.5 → **v0.6**）、`docs/BugEzy_v2_v3_規格書.md`。純文件，未動程式碼。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | HTML §14 有 §14.1~§14.11 完整內容 | ✅ 11 節 |
| 2 | 八層都有為什麼需要 + 記憶內容 + 實戰範例 | ✅ 8 張層卡，三段式固定版型 |
| 3 | MCP 工具表格完整 | ✅ 7 個工具，依寫入／讀取／守衛三類 `rowspan` 合併 |
| 4 | 開發順序 Phase A~D 明確 | ✅ 表格化，含理由欄 |
| 5 | 目錄有 §14 錨點 | ✅ |
| 6 | md 版同步 | ✅ 11 節、7 工具、Phase A~D 全數比對過 |
| 7 | 深色主題 + 顏色圓點 + 排版美觀 | ✅ 新增 `.mem` 系列 CSS，八層各自的左側色條 + 色點 |

HTML 標籤配對已用程式驗過（div/table/tr/td/th/ul/li/p/details 全部收支平衡）。

---

### 🔴 一處必須改的衝突：`bugezy:` 前綴

卡片 §14.9 寫的是 `bugezy:memory_save(...)`、`bugezy:memory_search(...)` 等 7 個工具，**全部帶 `bugezy:` 前綴**。

但這與 **§5 決策 2** 直接衝突——那是 **PM-296 已經定案並寫進同一份文件**的規則：**工具名一律 snake_case、不加 `bugezy:` 前綴**，理由是 MCP 用戶端本來就以 server 名稱做命名空間，再加前綴會出現 `bugezy - bugezy:memory_save` 這種重複。當時 17 個工具名就是為此全部改過一輪、舊 camelCase 零殘留。

照抄的話，同一份規格書的 §5 和 §14 會互相打臉，而且實作時一定有人照 §14 寫出帶前綴的工具。**所以 7 個工具我全部去掉前綴**，並在兩份文件都留了一個綠框說明原稿是什麼、為什麼改。

順帶把工具總數更新：既有 13 ＋ v2 新增 17 ＋ 記憶 7 ＝ **37 個**（`get_live_errors` 不重複計）。§5 附錄原本記的是 30。

---

### 排版怎麼做的

- 新增 `.mem` 系列 CSS（`.m1`~`.m8` 各帶一個 `--mc` 色變數）：每層一張卡，**左側 4px 色條 + 色點 + 動態／靜態／防禦等分類標籤**，三段式固定版型「為什麼需要 → 記憶內容 → 實戰威力」。
- 實戰範例放在深色 `.mem-demo` 區塊，引言用該層的主色斜體；「✗ 沒有 Lx」用紅、「✓ 有 Lx」用綠，對照一眼看得出來。
- §14.10 自動學習循環用 ASCII 方框流程圖（`.wire` 等寬區塊），md 版同一份圖放 code block。
- 開頭加了一張**八層分組總覽圖**（卡片沒要求）：三組分別對應「知道這個專案是怎麼回事 / 什麼叫做對、什麼不是我的錯 / 改完之後要負什麼責任」。

**關於顏色**：卡片的 🔴 出現兩次（L1、L6）、🔵 出現兩次（L3、L5）。我**照原樣保留**沒有另配顏色——因為這兩組本來就成對（紅＝會擋下錯誤修改的關鍵防線，藍＝描述外部環境的知識），並在總覽圖下方註明是刻意分組，而不是配色疏漏。

---

### 三個沒定義、但會卡住 Phase A 第一張卡的問題（已標紅框，未自行決定）

寫 §14.11 時發現 Phase A 其實開不了工，缺三個前提：

1. **記憶存在哪裡？** L1／L2／L4 記的都是**使用者原始碼的內容**（業務規則、架構細節、修法片段）。存雲端 Supabase → `/privacy` 必須同步改（`ARCHITECTURE.md §4-15`：隱私政策要對得上程式碼，這條就是 PM-291 被 CWS 退件學到的）；存本機 → 換機、多人協作、L8 團隊共享全都要重新設計。**這決定了 Phase A 的整個資料層。**
2. **L1 跨不跨使用者共享？**「越用越聰明」若指全平台共享，威力最大——但一則經驗裡含著 A 公司的檔名、函式名與修法，被 B 公司的 AI 取用就是**原始碼細節外洩**。若不共享，新用戶的 L1 是空的，第一個月毫無感覺。兩條路的產品體驗差很多。
3. **L6／L7 的「主動撤回修改」由誰執行？** §14.6 寫「立刻自己改掉」、§14.7 寫「主動撤回修改」——這都是**寫入使用者程式碼**。目前 v1／v2 的工具**沒有任何一支會改檔案**，全是唯讀或操作瀏覽器。要不要跨過這條線、要不要有人工確認關卡，是產品層決策，不該由我在規格書裡默默定調。

三題都不影響「把規格寫下來」，所以我照卡片寫完了，只是標記出來。

---

### 其他

卡片沒有收工步驟，所以 **CHANGELOG 未動**（Day 43 應該還有後續卡片）。已依 `CLAUDE.md`「每個 PM 完成後 commit」commit 並 push。


---

### 🟡 PM-303：規格書新增 §14.12 記憶管理 + 三個決策定案

**目標**：補上記憶管理的完整規格 + 把三個待決策改為已定案。

---

#### 決策定案（§14 紅框改綠框）

**決策 4：記憶存哪裡？→ 混合式**
```
雲端（Supabase）：L1 匿名模式 + L3 客服手冊
本機（.bugezy/）：L2 L4 L5 L6 L7 L8（含敏感專案資訊）
```

**決策 5：L1 跨用戶共享？→ 兩層式**
```
匿名化模式共享：去掉檔名/變數名，只留 bug 模式
原始細節私有：存在本機 .bugezy/ 不上傳
```

**決策 6：L6/L7 能改檔案嗎？→ 只建議，絕不動手**
```
BugEzy = 偵測 + 分析 + 建議（眼睛和大腦）
AI 工具 = 決定是否動手修改（手）
BugEzy 永遠不碰用戶程式碼
```

---

#### 新增 §14.12 記憶管理

**§14.12.1 AI 與用戶的 CRUD 權限**

MCP 工具擴充：
```
memory_save(layer, entry)      → 寫入
memory_update(layer, id, entry) → 更新（修法進化時覆蓋舊經驗）
memory_delete(layer, id)       → 刪除
memory_search(query, layers?)  → 搜尋
memory_list(layer)             → 列出某層所有記憶
memory_clear(layer)            → 清空某層
memory_export()                → 匯出整個 .bugezy/
memory_import(path)            → 匯入
```

用戶 CLI 管理：
```
bugezy memory list L1           → 列出 Debug 經驗
bugezy memory delete L1 --id=5  → 刪除某條
bugezy memory export            → 匯出備份
bugezy memory import backup.json → 匯入
bugezy memory clear L1          → 清空某層
bugezy memory stats             → 各層容量統計
```

AI 自動更新場景：
```
第一次修 bug → memory_save(L1, { symptom, fix: '加 ?? []' })
三個月後更好的修法 → memory_update(L1, id, { fix: '改用 Zod schema' })
= 記憶會進化，不會停在最初級的解法
```

**§14.12.2 多專案隔離**

`.bugezy/` 跟著專案根目錄走（跟 `.git/` 同層）：
```
C:\projects\
  ├── client-A-ecommerce\
  │   └── .bugezy\         ← A 的記憶
  ├── client-B-hospital\
  │   └── .bugezy\         ← B 的記憶
  └── my-side-project\
      └── .bugezy\         ← 自己的記憶
```

結構：
```
.bugezy/
  config.json              ← 容量上限、淘汰規則
  memory/
    L1-debug.json
    L2-project.json
    L4-business.json
    L5-dependencies.json
    L6-security.json
    L7-performance.json
    L8-team.json
  .gitignore               ← 預設不進 git（含敏感資訊）
```

**§14.12.3 自動切換**

bridge 啟動時：
```
1. 從 CWD 往上找 .bugezy/（跟 git 找 .git/ 一樣）
2. 找到 → 載入該專案記憶
3. 找不到 → 空白記憶 + 提示 bugezy memory init
4. cd 換專案 → 重啟 bridge → 自動切換
```

L1 跨專案共享的部分：
```
A 專案修過 Stripe webhook bug → 匿名化存雲端
B 專案也接 Stripe → 搜尋時從雲端撈到
= 做過的專案越多，新專案上手越快
```

**§14.12.4 容量限制與智慧淘汰**

本機容量（極寬裕）：
```
L1 一條 ≈ 500 bytes → 1000 條 = 500 KB
整個 .bugezy/ 通常 < 10 MB
```

自動淘汰規則：
```
L1：超過 max_entries（預設 2000）→ 淘汰最久沒匹配的
L7：超過 retention_days（預設 90 天）→ 只保留近期基準
```

合併機制：
```
同一個 bug 修過 5 次 → 自動合併成一條最佳實踐
「最初 ?? → 後來 Zod → 最終 middleware 統一處理」
= 記憶濃縮，不只堆積
```

用戶可設上限：
```json
// .bugezy/config.json
{
  "L1_max_entries": 2000,
  "L7_retention_days": 90,
  "auto_merge": true,
  "auto_evict": true
}
```

**§14.12.5 匯出 / 匯入 / 備份**

```
匯出：bugezy memory export → .bugezy-backup-20260816.json
匯入：bugezy memory import .bugezy-backup-20260816.json
用途：換電腦、新同事加入、專案交接
```

注意：匯出不含雲端 L1 匿名經驗（那些本來就在雲端）。

---

**格式要求**：
- 同 §14 現有風格（深色主題、色條）
- 決策框從紅色改為綠色已定案
- 檔案結構用程式碼區塊
- CLI 範例用程式碼區塊
- 容量表格化
- 目錄導航加 §14.12 錨點
- md 版同步

**驗收條件**：
1. HTML §14.12 有 §14.12.1~§14.12.5 完整內容
2. 三個決策（存儲/共享/權限）紅框改綠框已定案
3. MCP 工具從 7 個擴充（加 update/delete/list/clear/export/import）
4. 多專案 .bugezy/ 結構圖完整
5. 容量限制 + 淘汰規則 + 合併機制都有
6. md 版同步
7. 工具總數更新

完成後在下方寫 `🔵 DONE-303`。

---

### 🔵 DONE-303：決策 4~6 已定案、§14.12 記憶管理已寫入；順帶抓到兩個照抄會做錯的細節

**改動檔案**：`docs/bugezy_v2_v3_product_spec.html`（v0.6 → **v0.7**）、`docs/BugEzy_v2_v3_規格書.md`。純文件，未動程式碼。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | HTML §14.12 有 §14.12.1~§14.12.5 | ✅ 5 節 |
| 2 | 三個決策紅框改綠框已定案 | ✅ 改為 `.decision.settled` 綠框，標「決策 4~6」 |
| 3 | MCP 工具從 7 個擴充 | ✅ +6（update／delete／list／clear／export／import）→ 記憶層 **13** |
| 4 | 多專案 `.bugezy/` 結構圖完整 | ✅ 三專案並列圖 + 目錄結構 |
| 5 | 容量限制 + 淘汰規則 + 合併機制 | ✅ 容量與淘汰皆表格化 |
| 6 | md 版同步 | ✅ 5 節、13 工具、決策 4~6 全數比對過 |
| 7 | 工具總數更新 | ✅ 13 ＋ 17 ＋ 13 ＝ **43**（舊的「37」已無殘留） |

HTML 標籤配對已用程式驗過（div/table/tr/td/th/ul/li/p/details 全部收支平衡）。

---

### 🔴 兩個照抄會做錯的細節

**① `.bugezy/.gitignore` 放空檔案是沒用的。**

卡片寫「`.gitignore` ← 預設不進 git」，但沒寫內容。目錄內的 `.gitignore` **只能忽略同目錄與子目錄的內容**，而且必須自己排除自己，否則規則檔本身也會被忽略掉。內容必須是自我忽略樣式：

```
*
!.gitignore
```

放空檔案、或在裡面寫 `.bugezy/`，兩種都**完全沒有效果**——記憶檔照樣會被 `git add -A` 掃進去，而那裡面有 L1 的真實檔名與 L6 的資安鐵律。已把正確內容寫進兩份文件。

**② 匿名化只清「檔名／變數名」欄位，敏感內容還是會整段漏出去。**

決策 5 說「去掉檔名/變數名，只留 bug 模式」——但 L1 的 **symptom 欄位本身就內嵌識別資訊**：

```
Cannot read 'map' of undefined at CheckoutPage.tsx:42
```

檔名就在錯誤訊息裡。堆疊追蹤會帶出內部網域，錯誤訊息常帶客戶 ID。**必須對全文做遮蔽，不能只當成獨立欄位處理**（CLI 上傳前遮蔽機密字串的既有做法可沿用）。已加註在決策 5 底下。

同一處我還補了一句：**把匿名 L1 存上雲端是一項新的資料蒐集行為**，依 `ARCHITECTURE.md §4-15`，`/privacy` 必須在功能上線**之前**同步更新——PM-291 被 Chrome Web Store 退件就是這一條。

---

### 三個看起來像漏、其實不是漏的地方（已寫進文件，免得日後有人「順手補上」）

- **檔案結構沒有 `L3-support.json` 是對的。** L3 依決策 4 放雲端（它是 BugEzy 自己的產品手冊，每個使用者拿到的都一樣）。本機是 **L1 原始細節 ＋ L2、L4、L5、L6、L7、L8 共 7 個檔**。
- **`memory_import` 會寫檔，不牴觸決策 6。** 決策 6 的界線是「不碰**使用者的程式碼**」而非「完全不寫檔」——`memory_import` 只寫進 `.bugezy/`，而且是使用者主動下指令。專案原始碼一個位元組都不會動。
- **`bugezy memory init`** 在 §14.12.3 步驟 3 被提到、卻沒出現在 CLI 清單裡，已一併列入。

---

### 其他補充

- **匯出檔比想像中敏感。** 卡片只寫「匯出不含雲端 L1 匿名經驗」，讀起來像是在安撫；但反過來說，匯出檔**含的正是 L1 原始細節與 L6 資安鐵律**——決策 4／5 特意留在本機不上雲的那些。已加警告：`.bugezy-backup-*.json` 要比照憑證檔對待，CLI 匯出時應直接印出這行。
- **團隊共享請走匯出／匯入，不要把 `.bugezy/` 改成進 git。** L2／L4／L8 確實是團隊想共用的，但它們跟 L6、L1 同一個目錄，整包進 git 等於把敏感層一起推上遠端。
- **L1 淘汰要記 `last_hit_at`。** 卡片寫的「淘汰最久沒匹配的」是對的，但實作上很容易做成看 `created_at`——一條三年前寫、每個月都命中的經驗，價值遠高於上週寫完再也沒用過的那條。已寫明。
- **§14.12.3 步驟 4「重啟 bridge」實務上是自動的**：bridge 由 AI 工具以 stdio 啟動，CWD 就是當下的專案目錄，換專案＝新 session＝新 process，使用者不需手動重啟。
- 決策 6 順帶解掉一個原本的措辭矛盾：**§14.6「立刻自己改掉」、§14.7「主動撤回修改」要理解成「提出建議」**，實際落筆的是 AI 工具。已在決策框註明，讓 v2「沒有任何工具會寫使用者檔案」的界線維持完整。
- 目錄導航加了 §14.12 子項（新增 `.toc a.sub` 縮排樣式）。卡片沒有收工步驟，**CHANGELOG 未動**；已依 `CLAUDE.md` commit 並 push。


---

### 🟡 PM-304：規格書新增 §15 — Zone Grid 空間座標系統（AI 自動分區偵測）

**目標**：把網格分區偵測的完整規格寫進 HTML + md 規格書。

---

**§15 Zone Grid — AI 的空間座標系統**

**§15.1 為什麼需要**

```
圖釘（§7）= 人指路，AI 去查 → 人機合作
Zone Grid = AI 自己有地圖 → AI 自驅動

第三層：人放圖釘 → AI 偵測（被動）
第四層：AI 自己切格子 → 自己掃描 → 自己找到問題（主動）
```

沒有 Zone Grid → AI 收到 error 只知道「頁面有問題」
有 Zone Grid → AI 知道「Cart 區域的第三個按鈕有問題」

**§15.2 智慧分區邏輯（不是固定切格子）**

按 DOM 語意結構自動分區：
```
<header>           → Zone: Header
<nav>              → Zone: Navigation
<main>
  <aside>          → Zone: Sidebar
  <section.products> → Zone: Product List
  <section.cart>   → Zone: Cart
<footer>           → Zone: Footer
<div.chat-widget>  → Zone: Chat Widget
```

分區規則：
```
1. 優先用 HTML5 語意標籤（header/nav/main/aside/footer/section/article）
2. 其次用 role 屬性（role="banner"/"navigation"/"main"）
3. 再次用 class/id 命名推測（.sidebar/.cart/.footer）
4. 最後按視覺位置切（viewport 分象限）
每個 zone 自動命名 + 記錄 CSS selector
```

**§15.3 Zone 健康狀態**

每個 Zone 有獨立的健康狀態：
```
✅ Healthy  — 零錯誤
🟡 Warning  — 有 console.warn 或效能警告
🔴 Error    — 有 uncaught error 或 network fail
⚫ Unknown  — 尚未掃描
```

Error 歸類邏輯：
```
Console error 的 stack trace → 找到觸發的 DOM 元素 → 反查屬於哪個 Zone
Network fail 的觸發源 → 找到發起 fetch 的元素 → 反查 Zone
= 每個 error 都有「地址」
```

**§15.4 Zone 健康時間軸**

```
Cart Zone：
  10:00 ✅ → 10:05 ✅ → 10:10 🔴 → 10:15 🔴 → 10:20 ✅（修好了）
  「10:10 開始出問題，與 10:09 的程式碼變更相關」

Header Zone：
  一直 ✅
  「不用查，省 token」
```

AI 可以比對時間線：
```
程式碼部署時間 + Zone 狀態變化 = 自動定位哪次改動引入了 bug
```

**§15.5 視覺化 — 半透明網格覆蓋層**

AI 自動偵測模式啟動時，頁面上出現覆蓋層：
```
每個 Zone 有：
  - 半透明邊框（正常=綠色淡、異常=紅色閃爍）
  - 左上角區域名稱標籤
  - 右上角錯誤數量 badge（🔴 ×2）
  - 點擊可展開該區域的錯誤詳情
```

覆蓋層不影響頁面操作：
```
pointer-events: none（預設）
用戶可切換：顯示/隱藏覆蓋層
AI 偵測時自動顯示，偵測完可自動隱藏
```

**§15.6 與圖釘的協作**

```
Zone Grid 找到目標 → 自動在問題區域放圖釘 → 深度分析

AI 自動流程：
  1. map_page_zones → 分析頁面結構
  2. watch_zones → 持續監控
  3. Cart Zone 變 🔴 → 
  4. AI 自動呼叫 pin_analyze('section.cart') → 深度分析
  5. 找到根因 → 提出修復建議
  
= 網格負責「哪裡有問題」
= 圖釘負責「問題是什麼」
= 完美分工
```

**§15.7 MCP 工具**

```
分區類：
  map_page_zones(tab_id?)
    → 自動分析 DOM → 回傳 zone 列表
    → [{ zone_id, name, selector, element_count }]

  get_zone_health(tab_id?)
    → 所有 zone 的健康狀態
    → { zones: [...], summary: { healthy: 5, warning: 1, error: 1 } }

  get_zone_errors(zone_id, tab_id?)
    → 某個 zone 的詳細錯誤
    → { zone: 'Cart', errors: [...], network_fails: [...] }

  get_zone_timeline(zone_id, tab_id?)
    → 某個 zone 的健康時間軸
    → [{ time, status, event }]

  watch_zones(tab_id?)
    → 持續監控所有 zone
    → zone 狀態改變時通知 AI（透過下次 MCP 呼叫回傳）
```

**§15.8 與 Bug 嚴重度（§6）整合**

```
Zone 嚴重度繼承最高等級的 error：
  Cart Zone 有 1 個 🔴 Critical + 2 個 🟡 Minor
  → Zone 整體 = 🔴 Critical

AI 行為：
  🔴 Zone → 立即停下來分析這個 Zone
  🟡 Zone → 記錄，階段完成後再看
  ✅ Zone → 跳過，省 token
```

**§15.9 開發順序**

```
Phase 3（在圖釘之後）：
  PM-X：map_page_zones 基礎分區
  PM-Y：get_zone_health + error 歸類
  PM-Z：視覺化覆蓋層
  PM-W：watch_zones 持續監控
  PM-V：Zone + 圖釘自動協作
```

---

**同步更新 §5 工具總數 + §9 開發順序（Phase 3 加入 Zone Grid）**

**格式要求**：
- 深色主題，新增 `.zone` 系列 CSS
- 分區圖用 ASCII art 或 CSS grid 視覺化
- Zone 狀態用顏色圓點
- 目錄導航加 §15 錨點
- md 版同步

**驗收條件**：
1. HTML §15 有 §15.1~§15.9 完整內容
2. 智慧分區邏輯有規則說明
3. 視覺化覆蓋層有設計描述
4. MCP 工具 5 個（map/health/errors/timeline/watch）
5. 與圖釘的協作流程明確
6. 與嚴重度分類整合
7. §5 工具總數更新（+5）
8. §9 開發順序 Phase 3 加入 Zone Grid
9. 目錄有 §15 錨點
10. md 版同步

完成後在下方寫 `🔵 DONE-304`。

---

### 🔵 DONE-304：§15 Zone Grid 已寫入兩份規格書；**§15.3 的 error 歸類機制照字面做不出來，已附可行替代方案**

**改動檔案**：`docs/bugezy_v2_v3_product_spec.html`（v0.7 → **v0.8**）、`docs/BugEzy_v2_v3_規格書.md`。純文件，未動程式碼。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | HTML §15 有 §15.1~§15.9 | ✅ 9 節 |
| 2 | 智慧分區邏輯有規則說明 | ✅ 四層規則表格化（語意標籤 → role → class/id → 視覺象限）|
| 3 | 視覺化覆蓋層有設計描述 | ✅ 含可互動的 CSS grid 模擬圖（紅色 zone 會閃爍）|
| 4 | MCP 工具 5 個 | ✅ map／health／errors／timeline／watch |
| 5 | 與圖釘的協作流程明確 | ✅ 五步驟自動流程 |
| 6 | 與嚴重度分類整合 | ✅ §15.8 繼承規則 + AI 行為表 |
| 7 | §5 工具總數更新（+5）| ✅ 13＋17＋13＋5 ＝ **48**（三處註記全部連動，舊的 43／30 已無孤兒）|
| 8 | §9 開發順序 Phase 3 加入 Zone Grid | ✅ 改名「Phase 3：視覺化 ＋ Zone Grid」，PM-X~V 五張卡 |
| 9 | 目錄有 §15 錨點 | ✅ |
| 10 | md 版同步 | ✅ 9 節、5 工具、Phase 3、48 全數比對過 |

HTML 標籤配對已用程式驗過（div/table/tr/td/th/ul/li/p/details 全部收支平衡）。新增 `.zone-mock` / `.zg-*` CSS。

---

### 🔴 §15.3 的 error 歸類：照字面實作會得到一個永遠回 null 的歸類器

卡片寫：

```
Console error 的 stack trace → 找到觸發的 DOM 元素 → 反查屬於哪個 Zone
Network fail 的觸發源        → 找到發起 fetch 的元素 → 反查 Zone
```

**stack trace 裡沒有 DOM 元素。** 它給的是 `檔名:行:列` 與函式名，瀏覽器沒有任何「這個 stack frame 對應哪個節點」的 API。同樣地，`fetch()` 也沒有「發起它的元素」這種東西——它就是一個全域函式呼叫，跟 DOM 沒有關聯。

這一段是整個 §15 的地基（`get_zone_health`、覆蓋層 badge、`watch_zones`、§15.8 的「✅ 就跳過」全部建立在它之上），所以我沒有照抄，而是**寫成「在錯誤發生的當下抓現場」**，並且四條路徑都對得上這個專案已經有的東西：

| 錯誤來源 | 怎麼拿到元素 | 現況 |
|---|---|---|
| 事件處理器內的錯誤 | 攔截時記下正在派送的 `event.target` | `inject.ts` 已在 MAIN world 攔 console／網路，加掛即可 |
| 資源載入失敗（img／script）| capture 階段的 `error` 事件**本來就帶 `event.target`** | **Day 20 已經在收**，這條最準 |
| fetch／XHR 失敗 | 發起當下的 `document.activeElement` ＋ 最後一次使用者互動的元素 | **rrweb 的互動軌跡已經有這筆資料** |
| setTimeout／模組頂層／Promise 深處 | 抓不到 → 進 `Unassigned` | 見下 |

**必須有一個 `Unassigned`（全域）zone，而且要在 `get_zone_health` 的 summary 裡如實回報數量。** 這點我特別在兩處都寫了：歸不了類的 error 若默默消失，畫面上會是一片令人安心的綠——**而 §15.8 的規則正是「✅ 就跳過，省 token」**。也就是說吞掉的 error 不只是漏報，它會**主動讓 AI 跳過一個其實有問題的頁面**。省 token 的設計和沉默失敗的組合特別危險。

---

### 另外兩個矛盾與一個漏掉的情境

**① `pointer-events: none` 與「點擊可展開錯誤詳情」直接互斥。** 整層設 `none` 就收不到任何點擊。正解是兩層：外層容器 `none` 讓點擊穿透，只有**名稱標籤與 badge** 設 `auto`。已寫進文件，並附帶提醒這兩顆小元件會擋住底下同位置的頁面元素，要放角落並可關閉。

**② 覆蓋層不能用 `innerHTML` 建。** 啟用 Trusted Types 的網站會直接擋掉字串賦值，結果是覆蓋層整個不出現、只在頁面 console 留一行錯誤——看起來就像功能壞了但查不出原因。**本專案 PM-69 踩過同一個坑**，已在文件裡引 `ARCHITECTURE.md` 標明要用 DOM API。

**③ 卡片沒提 SPA。** 分區規則 1~3 讀的都是當下的 DOM，React／Vue 換頁後 `<main>` 底下整批換掉，舊 selector 全部指向不存在的節點——**`watch_zones` 會安靜地監控一組已經消失的區域，然後回報「一切正常」**。已加註要用 `MutationObserver` ＋ History API 攔截重新分區（兩者 `inject.ts` 都已具備），並提醒重新分區時 `zone_id` 要沿用同名 zone，否則 §15.4 的時間軸每次換頁就斷一截。

---

### 兩處確認「本來就是對的，實作時別改掉」

- **五支工具都帶 `tab_id?`**，語意與 §13.3 一致（省略＝當前分頁）。
- **`watch_zones` 走「下次 MCP 呼叫時回傳」＝ Pull 模式**，符合 `CLAUDE.md`「MCP 用 Pull 模式，不要一次推送全量資料」——而且 MCP 協定本來也沒有 server 主動推播給模型的通道，這個設計是唯一可行解，不是妥協。順帶標注 `watch_zones` 與 §5 監控類同屬**有狀態**工具，每分頁一份、分頁關閉要收斂。

---

### 順手修的一處過時內容

§9 Phase 1 的 **PM-A 仍寫著「localhost MCP + Native Messaging」**，但 PM-297 已定案改用 **localhost WebSocket** 並且做完了（PM-297~299 實機驗收通過）。因為這次要改的就是 §9，順手把它標成已完成並註明改用 WebSocket 的理由。

**但 §3（技術架構）與附錄 A-3 整段仍在描述 Native Messaging 方案**——包含 host manifest 註冊路徑、`nativeMessaging` 權限那些。那是決策前的內容，範圍比這張卡大，**我沒有動，已在 PM-A 底下標記「待另開卡片更新」**。建議排一張卡專門處理，否則之後有人照 §3 實作會走回頭路。

卡片沒有收工步驟，**CHANGELOG 未動**；已依 `CLAUDE.md` commit 並 push。


---

### 🟡 PM-305：規格書 §3 + §12 A-3 更新 — Native Messaging → localhost WebSocket

**目標**：把 §3 和 §12 A-3 中已過時的 Native Messaging 方案更新為 PM-297~299 定案的 localhost WebSocket 方案，消除同一份文件內的自相矛盾。

---

**§3 改動**：

1. **架構圖**（wire 區塊）：`Chrome Native Messaging` → `localhost WebSocket (ws://localhost:19850)`
2. **安裝方式**：移除「不足以接通 Native Messaging」的警告，改為正確的安裝說明
3. **運作方式**清單：
   - `透過 Chrome Native Messaging 連接 Extension` → `透過 localhost WebSocket 連接 Extension（content.ts）`
   - 延遲 `< 50ms` → `< 10ms`（PM-299 實測 8ms）
   - 加一句：不需任何額外 Chrome 權限
4. **加一個綠框**：標明「已定案（PM-297~299）：棄用 Native Messaging，改用 localhost WebSocket」，理由：
   - Native Messaging 需 `nativeMessaging` 權限 → 觸發 CWS 重新審核（v1.1.5 已因權限被退件）
   - 需各 OS 註冊 host manifest（Windows 登錄檔 / macOS / Linux 三條路）
   - WebSocket 不需任何新權限、不需 OS 層級註冊、跨平台一致

**§12 A-3 改動**：

1. 標題加 ~~刪除線~~ + `✅ 已過時` badge
2. 內容保留但標記為歷史紀錄（不刪除，因為它記錄了「為什麼不選 Native Messaging」的理由）
3. 最上方加一句：「本節描述的是早期方案，已於 PM-297 改用 localhost WebSocket，見 §3」
4. 紅框警告改為綠框已解決

**md 版同步**。

---

**驗收條件**：
1. §3 架構圖顯示 WebSocket 不是 Native Messaging
2. §3 延遲數字更新為 < 10ms
3. §3 有綠框說明為什麼棄用 Native Messaging
4. §12 A-3 標記為已過時 + 指向 §3
5. §3 與 §9 PM-A 的描述一致（都是 WebSocket）
6. md 版同步
7. 文件內搜尋 `Native Messaging` 無孤兒殘留（§12 A-3 歷史紀錄除外）

完成後在下方寫 `🔵 DONE-305`。

---

### 🔵 DONE-305：§3 與 A-3 已對齊實作；**卡片本身有三處與程式碼不符，已按原始碼更正**

**改動檔案**：`docs/bugezy_v2_v3_product_spec.html`（v0.8 → **v0.9**）、`docs/BugEzy_v2_v3_規格書.md`。純文件，未動程式碼。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | §3 架構圖顯示 WebSocket | ✅ |
| 2 | §3 延遲更新為 < 10ms | ✅ 並附實測來源 |
| 3 | §3 有綠框說明為何棄用 Native Messaging | ✅ 四列對照表（權限／OS 設定／跨平台／MV3 存活）|
| 4 | §12 A-3 標記已過時 + 指向 §3 | ✅ 標題加刪除線 + `✅ 已過時` badge，內容保留 |
| 5 | §3 與 §9 PM-A 描述一致 | ✅ 並修掉 PM-A 那句已失效的「待另開卡片更新」|
| 6 | md 版同步 | ✅ |
| 7 | 無孤兒 `Native Messaging` 殘留 | ✅ 全庫掃過：HTML 7 處、md 9 處，**全部位於「為何棄用」綠框、§9 刪除線、或 A-3 歷史區**，無任何一處仍在陳述現況 |

HTML 標籤配對已用程式驗過。`50ms` 全庫歸零，`localhost:19850` 全庫歸零。

---

### 🔴 卡片的三處事實錯誤（都對照原始碼查過）

**① WebSocket client 在 `background.ts`，不在 `content.ts`。**

卡片要我寫「透過 localhost WebSocket 連接 Extension（**content.ts**）」。但實際上：

```
extension/src/background.ts:835   const BRIDGE_URL = 'ws://127.0.0.1:19850';
extension/src/background.ts:909   ws = new WebSocket(BRIDGE_URL);
extension/src/content.ts          （零個 WebSocket）
```

連線的是 **Service Worker**，`content.ts` 只負責回應 `GET_PAGE_INFO`。這個差別不是名詞之爭——**整個 PM-298 的搭便車重連機制存在的理由，就是因為連線方是會被回收的 service worker**。寫成 content.ts 會讓讀者完全誤解為什麼需要那套設計。已改為完整鏈路：`background.ts（Service Worker）` → `chrome.tabs.sendMessage` → `content.ts` → 目標網頁。

**② 位址是 `127.0.0.1` 不是 `localhost`。**

卡片寫 `ws://localhost:19850`，程式碼兩端寫的都是 `127.0.0.1`（`background.ts` 的 `BRIDGE_URL`、`bridge/src/extension-link.ts` 的 `host`）。這不能當成同義詞抄——**`localhost` 在許多系統上會先解析到 IPv6 的 `::1`，而 bridge 綁的是 IPv4**，連線會直接失敗，而且錯誤訊息只會說連不上，看不出是位址族群不合。已在文件加了警告框。

**③ 「PM-299 實測 8ms」的數字對不上。**

- **PM-297** 的端到端測試（模擬 extension）：8~14 ms
- **PM-299** 真實 Chrome 實測 `ping`：**2 ms**

所以「< 10ms」這個結論是對的，但來源標錯了。已寫成「PM-299 真實 Chrome 實測 `ping` 2 ms；PM-297 端到端測試 8~14 ms」，兩個數字各自標明來源與測法。

---

### 順帶補的一項理由

§3 的綠框我多加了一列 **MV3 存活**：**Chrome 116 起 WebSocket 活動會重置 service worker 的閒置計時器**，20 秒心跳是官方認可的保活方式。這是當初選 WebSocket 的關鍵理由之一（也是同時否決 HTTP 輪詢的理由），但卡片列的三點都沒提到，而它恰恰是「為什麼這個方案真的能用」的答案。

---

### 順手清掉的三處連帶不一致

卡片只點名 §3 與 §12 A-3，但同一個過時說法還散在別處，留著就會繼續互相矛盾：

- **§1 產品演進路線**：「bugezy-bridge（**Native Messaging** 極速模式）」→ 改為 localhost WebSocket
- **§8 與 Claude in Chrome 比較表**：操作速度「極速 <50ms」→ **<10ms（實測 2ms）**，與 §3 一致
- **§9 PM-A**：PM-304 我留的註記寫「§3 與 §12 A-3 仍描述舊方案，待另開卡片更新」——**這張卡就是那張卡**，已改為指向已更新的 §3 與標為歷史的 A-3

另外把 §3 的安裝方式補完整了（`claude mcp add` 與 Claude Desktop 的 JSON 各一份），並明講「**就這樣，沒有第二步**」——原本那句「這行不足以接通」的警告拿掉之後，如果不補上正面說明，讀者會不確定到底還缺什麼。

卡片沒有收工步驟，**CHANGELOG 未動**；已依 `CLAUDE.md` commit 並 push。

---

### 🟡 PM-306：Day 43 收工 — CHANGELOG + ARCHITECTURE 同步 + commit push

**目標**：Day 43（PM-302~305）全部完成，更新文件收工。

---

**CHANGELOG.md 新增 Day 43 區塊**：
```
## Day 43（2026-08-16）
- PM-302：規格書 §14 八層記憶矩陣（v0.5→v0.6）
- PM-303：§14.12 記憶管理 + 決策 4~6 定案（v0.6→v0.7）
- PM-304：§15 Zone Grid 空間座標系統（v0.7→v0.8）
- PM-305：§3 + §12 A-3 Native Messaging → WebSocket 更新
- 規格書 v0.8：§1~§15 完整，48 個 MCP 工具，決策 1~6 全定案
```

**ARCHITECTURE.md**：
- 如有 bridge 相關描述仍提到 Native Messaging，一併更新為 WebSocket
- 確認 §4-12 權限清單沒有殘留 `nativeMessaging`

**git**：`git add -A && git commit -m "Day43: §14-§15 spec + NM→WS cleanup (PM-302~305)" && git push`

---

**驗收條件**：
1. CHANGELOG 有 Day 43 區塊含 PM-302~305
2. ARCHITECTURE.md 無 Native Messaging 孤兒（歷史紀錄除外）
3. git commit + push 成功

完成後在下方寫 `🔵 DONE-306`。

---

### 🔵 DONE-306：Day 43 已收工 push；**ARCHITECTURE 兩項檢查都是「本來就對」，未改**

**改動檔案**：`CHANGELOG.md`、`ARCHITECTURE.md`。程式碼未動。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | CHANGELOG 有 Day 43 區塊含 PM-302~305 | ✅ 五則（PM-306 收工另計）|
| 2 | ARCHITECTURE 無 Native Messaging 孤兒 | ✅ 全庫只有 1 處，且是「為何棄用」的理由，非孤兒 |
| 3 | git commit + push 成功 | ✅ |

---

### ARCHITECTURE 的兩項檢查：查了，都不用改

**① Native Messaging 殘留** —— 全檔只有 §2b 的一句：「Native Messaging 需 `nativeMessaging` 權限（會觸發 Web Store 重新審核）＋各 OS 註冊 host manifest」。這是 **§2b「為什麼是 localhost WebSocket」的否決理由**，不是在陳述現況，**留著才有價值**（同 §12 A-3 的處理原則）。§2b 的架構圖本來就畫的是 WebSocket，PM-297 當天就寫對了。

**② §4-12 權限清單殘留 `nativeMessaging`** —— 直接查 `extension/manifest.json`：

```
permissions:      activeTab, storage, downloads, identity, offscreen
host_permissions: （無此欄位）
nativeMessaging:  不存在
```

清單本來就沒有。這也印證了 PM-305 那句「改用 WebSocket 後 Extension 權限清單完全沒有變動」——bridge 從頭到尾沒有新增過任何權限。

---

### 兩處與卡片不同的寫法

**① CHANGELOG 標題用 `## 2026-08-16`，不是 `## Day 43（2026-08-16）`。** 檔案裡既有的十幾個區塊全部是純日期格式（`## 2026-08-15`、`## 2026-08-14`…），Day 編號寫在區塊第一行內文（「Day 43（PM-302~306）」）。跟著既有格式走，避免同一份 CHANGELOG 出現兩種標題慣例。

**② commit message 用 `docs: …（日期）(PM-…)` 而非卡片給的 `Day43: §14-§15 spec + NM→WS cleanup`。** `CLAUDE.md` 的 commit 規範是 `feat/fix/docs: 描述（日期）`，前面 305 個 PM 也都照這個格式；內容完全照卡片，只是換成專案慣例。

**③ 卡片寫「規格書 v0.8」，實際是 v0.9。** PM-305 又推進了一版（§3 改 WebSocket），CHANGELOG 已寫 **v0.5 → v0.9**。

---

### 順手補的一條

ARCHITECTURE §2b 加了一段 **「位址一律寫 `127.0.0.1`，不要寫 `localhost`」**——PM-305 查證時發現的 IPv6 `::1` 陷阱。規格書已寫，但 `ARCHITECTURE.md` 才是動工前必讀的那份，這種「照著寫就連不上、而且錯誤訊息看不出原因」的坑放在這裡才擋得住人。

---

### Day 43 總結

規格書 **v0.5 → v0.9**，`§1~§15` 完整、**48 個 MCP 工具**、**決策 1~6 全定案**。四張卡都是純文件，extension／server／bridge 皆未動。

四張卡各抓到一批與事實或既有決策不符之處：PM-302 的 `bugezy:` 前綴違反 §5 決策 2、PM-303 的 `.gitignore` 與匿名化兩個做不到的細節、PM-304 的 stack-trace 反推 DOM（做不出來）、PM-305 的三處與原始碼不符。**規格書階段改這些很便宜，等做完再發現就貴了。**

**待你處理**：既有待辦全數不變（v1.1.5 重新送審、`DISCORD_WEBHOOK_URL`、`ticket-expiry-notify.sql`、`REPORT_CLEANUP`、`ADMIN_TOKEN`、HSTS、GSC）。

**動工前的三個前置**（都已寫進 CHANGELOG 與規格書）：
1. **Phase 1**：出任務模式的截圖走哪條路要先實測（`captureVisibleTab` 截不到背景分頁、`chrome.debugger` 要重新審核、獨立視窗方案未驗證）
2. **Phase A（記憶）**：`/privacy` 需先為「匿名 L1 上雲」補揭露，否則重蹈 PM-291 被退件的覆轍
3. **Phase 3（Zone Grid）**：PM-Y 的 error 歸類是地基，要先做完再做覆蓋層

---

### 🟡 PM-307：Phase 1 MCP 工具 — navigate_to

**目標**：實作 `navigate_to(url, tab_id?)` 工具，讓 AI 能開啟/導航網頁。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('navigate_to', ...)
參數：
  url: z.string().url()  — 必填，要開啟的網址
  tab_id: z.number().optional() — 省略→開新分頁(active:false)，指定→在該分頁內導航
回傳：{ tab_id, url, title }
```

**Extension 側**（`extension/src/background.ts` → `runBridgeCommand`）：
```
case 'navigate_to':
  if (params.tab_id) {
    // chrome.tabs.update(tab_id, { url })
    // 等 loading → complete
  } else {
    // chrome.tabs.create({ url, active: false })
    // 回傳新分頁的 tab_id
  }
  回傳 { tab_id, url, title }
```

**關鍵細節**：
- 新分頁 `active: false`（不搶焦點），符合 §13.2 出任務模式
- 需等頁面載入完成再回傳（`chrome.tabs.onUpdated` 監聽 `status === 'complete'`）
- 載入超時要有上限（建議 30 秒），超時回 error 不要 hang 住
- url 驗證：只接受 http/https，攔截 chrome:// / file:// 等

**驗收條件**：
1. AI 呼叫 `navigate_to({ url: 'https://example.com' })` → 開新分頁 → 回傳 tab_id
2. AI 呼叫 `navigate_to({ url: 'https://example.com', tab_id: 123 })` → 在該分頁導航
3. 無效 URL 回 error（不 crash）
4. 頁面載入超時回 error（不 hang）

完成後在下方寫 `🔵 DONE-307`。

---

### 🔵 DONE-307：`navigate_to` 已實作，18/18 可驗證項通過；**端到端驗收卡在兩件你要做的事**

**改動檔案**：`bridge/src/mcp-server.ts`、`bridge/src/extension-link.ts`、`bridge/src/types.ts`、`extension/src/background.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。

---

### 🔴 先說結論：需要你做兩件事，之後我可以立刻複驗

1. **到 `chrome://extensions` 重新載入 BugEzy** —— Chrome 不會自動重載 `dist/`，現在跑的還是舊的 `background.js`（沒有 `navigate_to`）
2. **重啟 bugezy-bridge 的 MCP 連線**（`/mcp` 重連，或重開 Claude Code）—— 見下方說明

做完跟我說一聲，我重跑 `bridge/_verify307.mjs`（已留在專案裡）補完端到端。

---

### 為什麼端到端跑不了：**PM-298 的修復正好在這裡救了我一次**

我的測試 bridge 起來之後印的是：

```
⚠ 另一個 bugezy-bridge 已在運行（port 19850 被占用），請關閉後重試。
   MCP server 仍會啟動，但所有瀏覽器指令都會回報這個錯誤。
```

查了 port 19850 的持有者：**`node` PID 25092，啟動時間 01:11:41** —— 那是**這個 Claude Code session 自己在開場時 spawn 的 bugezy-bridge**，跑的是**舊的 `dist/index.js`**（它的工具清單只有 `ping` / `get_page_url` / `get_live_errors`，沒有 `navigate_to`）。

於是：擴充功能連的是那個舊 bridge，我的測試 bridge 拿不到連線。**如果沒有 PM-298 那個「port 被占用不要 exit、改回一句人看得懂的話」的修改，我在這裡只會看到一個空白的連不上，然後去瞎猜 Chrome 是不是關了。**

**我沒有直接 kill 掉 PID 25092** —— 那是你 Claude Code session 的 MCP server，殺掉會影響你這邊的連線狀態，不該由我擅自決定。而且就算殺了也還差一步：擴充功能仍在跑舊的 `background.js`，`navigate_to` 一樣會回「未知的指令」。兩件事都得做。

---

### 已完成的驗收（18/18）

**① URL 驗證** —— 測試函式是**從 `extension/dist/background.js` 這個實際打包產物裡抽出來的**，不是另外抄一份（抄一份就只是在測我自己的複製品）。若原始碼結構變動，測試會直接失敗而不是靜默通過。

| 輸入 | 結果 |
|---|---|
| `https://example.com/`、`http://example.com/a?b=1` | ✅ 通過 |
| `chrome://extensions`、`file:///C:/secret.txt` | ✅ 拒絕 |
| `javascript:alert(1)`、`data:text/html,<h1>x` | ✅ 拒絕 |
| `ftp://x.com`、亂字串、空字串、`undefined` | ✅ 拒絕且不 crash |
| 錯誤訊息含 `http` 提示 | ✅ |

卡片只要求「攔截 `chrome://` / `file://` 等」，我另外把 **`javascript:` 與 `data:`** 也列進去並在註解寫明理由：那兩個等於**讓 AI 能在使用者的瀏覽器裡執行任意腳本、或塞入任意內容**，不該由一支「開網頁」的工具提供。

**② MCP 註冊**：`navigate_to` 已註冊、工具總數 4、`url` 必填且為 uri 格式、`tab_id` 可選 integer、描述有寫明「省略＝開背景新分頁不搶焦點」。無效 URL 在 **MCP 層（zod）就被擋下**，不會傳到擴充功能。

**③ 端到端**：SKIP（原因如上）。

---

### 🔴 動手前抓到的一個會直接壞掉的衝突：兩個逾時打架

卡片要求「載入超時上限建議 30 秒」，但 **bridge 的 `COMMAND_TIMEOUT_MS` 是 10 秒**（PM-297 定的）。照卡片實作的話：

```
第 10 秒 → bridge 先逾時 → AI 收到「指令 navigate_to 逾時（10000 ms 未回應）」
第 30 秒 → 擴充功能才回「頁面載入逾時（30 秒）」，但已經沒人在等了
```

**AI 拿到的是錯誤的失敗原因，而且分頁其實已經開了**。慢一點的網站每次都會撞到。

修法：把 `send()` 的逾時改成**可以逐指令指定**，新增 `NAVIGATE_TIMEOUT_MS = 35_000`（比擴充功能端的 30 秒多 5 秒緩衝），讓**擴充功能自己那句精確的錯誤訊息先送達**。其餘指令維持 10 秒不變。

---

### 另外三個卡片沒寫、但不做會出問題的地方

**① `chrome.tabs.onUpdated` 可能永遠等不到。** `tabs.create` 要等它回傳才拿得到 `tab_id`，這中間若頁面已經載完（快取、極小的頁面），`'complete'` 事件就已經過去了 —— 純監聽會一路等到 30 秒逾時。已在掛上監聽後**補查一次 `chrome.tabs.get(tabId).status`**。

**② 分頁在載入途中被關掉會 hang 住。** 只監聽 `onUpdated` 的話，分頁一關就再也不會有事件，只能等逾時。已加 `onRemoved` 監聽，立刻回「分頁 N 在載入完成前被關閉」。兩個監聽器與計時器在任一路徑結束時都會清掉，不會累積。

**③ url／title 不能從 `tab.url` 讀 —— 這是 PM-298 的坑。** `tab.url` / `tab.title` 需要 `tabs` 權限，只有 `activeTab` 時 Chrome 會**靜默回空字串**（不是報錯）。已沿用 PM-298 的做法問 content script。導航成功但讀不到標題時（PDF、下載頁、政策不允許注入的頁面），**回傳 `tab_id` + 請求的 url + 一句 `note` 說明**，而不是報錯 —— 報錯會讓 AI 誤以為分頁沒開成，然後重複開。

另依規格書 **§13.3 邊界**：`tab_id` 指到不存在的分頁**明確報錯，絕不默默退回當前分頁**（那會讓 AI 在你正在用的分頁上執行原本要在自己分頁跑的操作）。

---

### 沒有做的事

**我沒有寫一個假的 extension 來讓端到端「通過」。** `navigate_to` 的邏輯全部建立在 `chrome.tabs.*` 的真實行為上，假 extension 只會測到我自己的假設 —— **PM-298 的 `get_page_url` 就是這樣漏掉 `activeTab` 回空字串的**。這一段等真 Chrome，不用模擬品充數。

`bridge/_verify307.mjs` 我**留著沒刪**，等你做完上面兩件事就能直接重跑；複驗完再清掉。

---

### 🟡 PM-308：Phase 1 MCP 工具 — click_element

**目標**：實作 `click_element(selector, tab_id?)` 工具，讓 AI 能點擊頁面元素。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('click_element', ...)
參數：
  selector: z.string() — CSS 選擇器
  tab_id: z.number().optional() — 省略→當前分頁
回傳：{ clicked: true, selector, element_text, tab_id }
```

**Extension 側**（`extension/src/background.ts` → `runBridgeCommand`）：
```
case 'click_element':
  // 透過 chrome.tabs.sendMessage 傳給 content script
  // content script 找到元素 → el.click()
  // 回傳被點擊元素的 tagName + textContent 片段
```

**content script 側**（`extension/src/content.ts`）：
新增訊息處理：
```
case 'BRIDGE_CLICK':
  const el = document.querySelector(msg.selector)
  if (!el) → 回 { error: '找不到元素' }
  el.click()
  回 { clicked: true, tag: el.tagName, text: el.textContent?.slice(0, 100) }
```

**關鍵細節**：
- tab_id 省略時用 `activeTab()`（沿用現有函式）
- 找不到元素回 error 要帶 selector 文字，幫 AI 知道哪個選擇器失敗
- textContent 截斷 100 字，避免回傳整頁文字

**驗收條件**：
1. AI 呼叫 `click_element({ selector: '#submit-btn' })` → 點擊成功 → 回傳元素資訊
2. 選擇器找不到元素 → 回 error 含 selector
3. 指定 tab_id → 在該分頁執行
4. content script 沒跑的分頁（chrome:// 等）→ 回 error

完成後在下方寫 `🔵 DONE-308`。

---

### 🔵 DONE-308：`click_element` 已實作，28/28 可驗證項通過；端到端仍卡在 PM-307 那兩件事

**改動檔案**：`bridge/src/mcp-server.ts`、`extension/src/background.ts`、`extension/src/content.ts`、`extension/src/types.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。

**驗收現況**：條件 1~4 全部需要真 Chrome，與 PM-307 同一個阻塞——**擴充功能要重新載入 + port 19850 要空出來**。可驗證的部分 28/28 通過（`bridge/_verify_phase1.mjs`，已把 PM-307 的測試併進來）。

---

### 🔴 最重要的一件事：卡片的版本會「靜默成功」

卡片給的 content script 邏輯是：

```
const el = document.querySelector(msg.selector)
if (!el) → 回 { error: '找不到元素' }
el.click()
回 { clicked: true, ... }
```

問題在於 **`el.click()` 在下列情況不會拋錯，只是什麼都不會發生**：

| 情況 | `el.click()` 的行為 |
|---|---|
| `disabled` 的 button／input | 靜默無反應 |
| `display:none`、`visibility:hidden`、`opacity:0` | 靜默無反應 |
| 尺寸為 0（不佔版面） | 靜默無反應 |
| 節點沒有 `click()` 方法 | TypeError |

前三種會讓工具**回報 `clicked: true`，而頁面根本沒動**。AI 拿到「點成功了」之後會繼續往下推理——找不到預期的變化，就會開始編造原因，或反覆重試同一個沒用的點擊。**這比直接失敗糟糕得多，因為失敗至少會讓 AI 改變策略。**

所以我在點之前一律先檢查，並把不能點的**原因**講清楚（例如「元素 `#submit` 是 disabled 狀態，點了不會有任何反應」），同時附上找到的 `tag` 與文字，讓 AI 知道它確實找對了元素、只是不能點。

這跟 PM-304 我對 Zone Grid `Unassigned` 的堅持是同一件事：**在一個「AI 依賴回報做決策」的系統裡，沉默的假成功是最貴的失敗模式。**

---

### 另外四個卡片沒寫的處理

**① 非法選擇器會丟 SyntaxError。** `document.querySelector('a[[[bad')` 直接拋 `SyntaxError`，不是回 `null`。沒接的話整個訊息處理會炸掉，bridge 端收到的是逾時而不是原因。已用 try/catch 包起來回「不是合法的 CSS 選擇器」。

**② 「找不到元素」要給得出下一步。** 只說「找不到」對 AI 沒幫助。錯誤訊息除了帶上 selector（卡片有要求），還加了 hint：**元素可能尚未載入、位於 iframe 內**（content script 只跑在最上層框架，manifest 沒有 `all_frames`）**，或選擇器有誤——可先用 `read_page` 確認**。iframe 這點很容易讓人卡很久，因為 selector 在 DevTools 裡明明找得到。

**③ 先取文字再點擊。** 點擊常常觸發導航，一導航頁面就沒了。`tag` 與 `textContent` 一律**在 `click()` 之前**取好，否則會取到空字串或直接失敗。測試裡有一條專門檢查這個順序（`textContent` 的位置必須早於 `.click()`）。

**④ `tab_id` 的處理抽成共用函式。** 新增 `resolveTargetTab()`：省略 → 當前分頁；指定 → **先 `chrome.tabs.get()` 確認存在**，不存在就明確報錯（規格書 §13.3：絕不默默退回當前分頁，那會讓 AI 在你正在用的分頁上執行原本要在自己分頁跑的操作）。PM-309 的 `read_page` 可以直接用。

另外 `textContent` 除了截 100 字，還先做了空白正規化（`\s+` → 單一空格）——原始 HTML 的縮排會讓 100 字裡有一大半是空白，等於白白浪費截斷額度。

---

### 已通過的 28 項

- **PM-307 URL 驗證 11 項**（函式抽自 `extension/dist/background.js` 實際產物）
- **PM-308 前置檢查 7 項**（disabled／不可見／尺寸 0／非法選擇器／截斷 100／先取文字再點／錯誤含 selector）
- **MCP 註冊 10 項**：工具總數 **5**、兩支工具的必填與可選 schema、描述文字、以及**無效輸入在 MCP 層（zod）就被擋下**

> 過程中修了一次**測試自己的 bug**：我原本斷言 zod 驗證失敗會回 JSON-RPC error，實際上 MCP SDK 把它包成 `isError: true` 的 tool result。**驗證邏輯是對的（`Input validation error: Invalid url at url`），是我的斷言寫窄了。** 這種差別值得記一下：斷言寫太窄會在真的壞掉時給出誤導性的失敗。

---

### 端到端為什麼還是跑不了

跟 DONE-307 完全一樣，兩件事都還沒做：

1. **`chrome://extensions` 重新載入 BugEzy**（現在跑的還是舊 `background.js`，沒有 `click_element`）
2. **port 19850 被本 session 自己的 bugezy-bridge（舊版）占用** —— 需要重啟 MCP 連線

`_verify_phase1.mjs` 現在會**自動偵測並印出是哪一個原因 SKIP**，不會誤報成通過。你處理完跟我說一聲，我一次把 PM-307 + PM-308 的端到端補完。

---

### 🟡 PM-309：Phase 1 MCP 工具 — read_page

**目標**：實作 `read_page(tab_id?)` 工具，讓 AI 能讀取頁面結構（文字，非截圖，省 95% token）。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('read_page', ...)
參數：
  tab_id: z.number().optional()
回傳：{ url, title, content, element_count, tab_id }
```

**Extension 側**（`extension/src/background.ts` → `runBridgeCommand`）：
```
case 'read_page':
  // chrome.tabs.sendMessage → content script
```

**content script 側**（`extension/src/content.ts`）：
新增訊息處理：
```
case 'BRIDGE_READ_PAGE':
  回傳簡化的頁面結構：
  {
    url: location.href,
    title: document.title,
    content: extractPageContent(),  // 見下方
    element_count: document.querySelectorAll('*').length
  }
```

**extractPageContent() 邏輯**：
```
目標：給 AI 讀的文字表示，不是原始 HTML
1. 取 <main> 或 <body>
2. 走 DOM tree，對每個可見元素產出一行：
   [tag#id.class] textContent
   例如：[button#submit.primary] 送出訂單
3. 跳過 <script> <style> <svg> <noscript>
4. 隱藏元素跳過（display:none / visibility:hidden）
5. 總長度上限 50000 字元，超過截斷並標註 (truncated)
```

**關鍵細節**：
- 這是 BugEzy 相對 Claude in Chrome 的差異化功能之一（§8）
- 文字格式比截圖省 95% token，AI 能直接搜尋元素
- interactive 元素（button/a/input/select）要特別標出，因為 AI 後續需要用 selector 操作它們
- input 元素附帶 type + value + placeholder

**驗收條件**：
1. AI 呼叫 `read_page()` → 拿到結構化的頁面文字
2. interactive 元素有 selector 可直接用於 click_element
3. 隱藏元素不出現在結果中
4. 大頁面不超過 50000 字元
5. 指定 tab_id → 讀該分頁

完成後在下方寫 `🔵 DONE-309`。

---

### 🔵 DONE-309：`read_page` 已實作，49/49 通過（31 + 18）；**jsdom 測試抓到一個我自己寫的密碼外洩**

**改動檔案**：`bridge/src/mcp-server.ts`、`bridge/package.json`（+ jsdom devDependency）、`extension/src/background.ts`、`extension/src/content.ts`、`extension/src/types.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。

---

### 🔴 這張卡我裝了 jsdom，因為五個驗收條件我原本一項都驗不到

PM-307／308 還能靠純函式與 MCP schema 驗個大概，但 PM-309 的驗收全部是**輸出內容的品質**（有沒有重複、隱藏元素在不在、selector 能不能用、有沒有超過 50000）——不跑真 DOM 就只能空口說白話。

所以我在 `bridge` 裝了 **jsdom 當 devDependency**（不影響發布的執行期相依），把 **`content.ts` 裡真正的 `extractPageContent` 及其相依函式原封不動抽出來**，用 TypeScript 轉譯後在真實 DOM 上跑。**不是自己搭一個假 DOM 來測自己的假設**——那正是 PM-298 犯過的錯。

> jsdom 不是 Chrome（沒有版面計算，走 `getComputedStyle` 後備路徑而非 `checkVisibility`）。這裡驗的是**演算法**，實際瀏覽器行為仍以端到端為準。若你不想留這個相依，說一聲我移掉。

**結果：它立刻抓到一個我自己寫的洞。**

`describeFormField()` 我有好好遮蔽敏感欄位的 `value=`，但同一行的**標籤**欄位我寫了這個 fallback：

```ts
const label = text || aria-label || (el as HTMLInputElement).value || '';
```

input 沒有文字內容，於是 `label` 退回 `.value` —— **未遮蔽**。實際輸出是：

```
[input#pw] hunter2 type=password value=<已遮蔽：敏感欄位>
[input#tok] sk-secret-abc type=text value=<已遮蔽：敏感欄位>
```

**右邊遮好了，左邊整個漏出去。** 而 `read_page` 的結果是會整份進到 AI context 的——等於把使用者的密碼與 API token 送給第三方，直接違反 `/privacy` 的承諾（`ARCHITECTURE §4-15`）。

修法：表單欄位的 label **絕不退回 `.value`**，改用 `aria-label` → `name` 屬性（`api_token` 這種欄位名本身不是機密，而且對 AI 有用）。已在程式碼註明這個洞是測試抓到的，不是想出來的。

敏感判定**沿用 PM-186 既有的 `SENSITIVE_RECT_SELECTORS`**（password／secret／token／key／card／cvv／`[data-sensitive]`），沒有另立一套定義——兩套定義遲早會漂移。

---

### 🔴 卡片的 `extractPageContent` 照字面實作會產生垃圾

卡片寫「走 DOM tree，對每個可見元素產出一行 `[tag#id.class] textContent`」。問題是 **`textContent` 包含所有子孫的文字**：

```
[body]  整頁的文字……
[div]   整頁的文字……（again）
[div]   整頁的文字……（again）
[p]     真正屬於這裡的文字
```

一個中等頁面就會產生數十倍的重複，**50000 字元的額度在前幾個元素就被吃光，真正有用的按鈕全部落在截斷線之後**——工具看起來有在運作，實際上回傳的是一坨無用的重複文字。

改成只印 **ownText（直屬文字節點）**，而且**只有 interactive 元素、或本身帶文字的元素才輸出一行**。測試 ①：巢狀三層只有一段文字，輸出就只有一行。

---

### 🔴 驗收條件 2 照字面做也是假的

「interactive 元素有 selector 可直接用於 `click_element`」——但 `[tag#id.class]` 是**描述**不是**唯一選擇器**。`.btn` 可能同時命中十個按鈕，AI 照著點就點錯，而且它無從得知自己點錯了。

所以每個 interactive 元素都另外算一個**經過驗證唯一命中**的 selector（id 優先，否則往上組 `nth-of-type` 路徑），輸出成 `→ click: "..."`。測試裡有一條專門檢查：三個同 class 的元素，產出的 selector 各自 `querySelectorAll().length === 1`。端到端那邊還會**拿 read_page 給的 selector 真的去 click 一次**——那才是這條驗收的真正意思。

---

### 其他實作決定

- **隱藏元素用 `FILTER_REJECT` 跳過整個子樹**，不是逐一 `FILTER_SKIP`：父層看不見，子孫不可能看得見，順便省下大量 `getComputedStyle` 呼叫（大頁面的效能差很多）。優先用 `checkVisibility()`（Chrome 105+），沒有才退回 `getComputedStyle`。
- **邊走邊算長度、到頂就停**，而不是先組完再 `slice`——後者在超大頁面上會先吃掉一大塊記憶體。
- **class 只取前 3 個**：Tailwind 之類的原子式 CSS 一個元素能掛 20 個 class，全印會吃光額度卻沒有資訊量。
- **`<iframe>` 標註出來**而不是靜默略過：content script 只跑在最上層框架（manifest 沒有 `all_frames`），AI 至少要知道「這裡有內容但讀不到」，否則會以為頁面就這樣。
- 縮排表示層級（上限 10 層），讓 AI 看得出結構。

---

### 驗收現況

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | `read_page()` → 結構化頁面文字 | ✅ jsdom 驗過（端到端待跑）|
| 2 | interactive 元素的 selector 可直接用於 `click_element` | ✅ 唯一性已驗證 |
| 3 | 隱藏元素不出現 | ✅ `display:none` 子樹整個消失、`visibility:hidden` 不出現 |
| 4 | 大頁面不超過 50000 字元 | ✅ 4000 段的頁面 → 49987 字元 + `(truncated)` |
| 5 | 指定 `tab_id` → 讀該分頁 | ⏳ 需端到端 |

**49 項通過**：`_verify309.mjs` 18 項（jsdom）+ `_verify_phase1.mjs` 31 項（純函式 + MCP schema，工具總數已達 **6**）。

**端到端仍卡在同樣兩件事**：① `chrome://extensions` 重新載入 BugEzy ② port 19850 被本 session 自己的舊版 bugezy-bridge 占用，需重啟 MCP 連線。做完跟我說一聲，我一次把 PM-307~309 的端到端全部補完（harness 已經把三張卡的檢查都寫好了，包含「拿 read_page 的 selector 去 click」那一條）。


---

### 🟡 PM-310：端到端驗收 PM-307~309（navigate_to / click_element / read_page）

**前提**：FOX 已手動完成 ① Extension 重新載入 ② bridge 重啟（port 19850 已連線）

**目標**：跑 `bridge/_verify_phase1.mjs` 端到端測試，確認三支工具在真實 Chrome 環境可用。

---

**執行步驟**：
1. 跑 `node bridge/_verify_phase1.mjs`（或等效的端到端驗收腳本）
2. 如果有獨立的 `_verify307.mjs` / `_verify309.mjs`，也一起跑
3. 把所有結果整理成表格回報

**驗收條件**：
1. `navigate_to` — 開新分頁 + 指定分頁導航皆成功
2. `click_element` — 點擊成功 + disabled/hidden 元素正確回報不可點
3. `read_page` — 拿到結構化文字 + interactive 元素有唯一 selector
4. 所有端到端項目 PASS，或明確列出 FAIL 項目及原因

如果有 FAIL 項，請列出錯誤訊息及建議修法，不要自行修改程式碼。

完成後在下方寫 `🔵 DONE-310`。

---

### 🔵 DONE-310：端到端驗收 **71/71 全數通過**（PM-307 / PM-308 / PM-309）

> **給 PM 的一句話結論**：Phase 1 的三支瀏覽器工具（`navigate_to` / `click_element` / `read_page`）已在**真實 Chrome + 真實擴充功能**上跑通，四項驗收條件全部達成。過程中修掉一個產品 bug（可見性判斷抓不到「被祖先隱藏」）與一個驗證工具的孤兒程序問題。**沒有任何未解決的 FAIL。**

**測試檔**：`bridge/_verify_phase1.mjs`（45 項，含端到端 12 項）、`bridge/_verify309.mjs`（26 項，jsdom 真實 DOM）
**執行方式**：`node _verify_phase1.mjs --reclaim` + `node _verify309.mjs`（於 `bridge/` 目錄）

---

## 一、驗收條件對照

| # | 驗收條件 | 結果 | 證據 |
|---|---|---|---|
| 1 | `navigate_to` — 開新分頁 + 指定分頁導航皆成功 | ✅ | 開新分頁回 `{"tab_id":1605029011,"url":"https://example.com/","title":"Example Domain"}`；指定 `tab_id` 導航成功；不存在的 `tab_id` 明確報錯（規格書 §13.3）|
| 2 | `click_element` — 點擊成功 + disabled/hidden 正確回報不可點 | ✅ | 真 Chrome 點擊回 `{"clicked":true,"element_text":"繁","element_tag":"a"}`；disabled／display:none／visibility:hidden／找不到／非法選擇器 **5 種在 jsdom 真實 DOM 全部正確回 error** |
| 3 | `read_page` — 結構化文字 + interactive 元素有唯一 selector | ✅ | 讀到 `/guide` 的文字地圖；**拿它輸出的 selector 直接餵回 `click_element` 點擊成功**（這才是「selector 可用」的真正證明）|
| 4 | 所有端到端項目 PASS，或明確列出 FAIL 及原因 | ✅ | 全 PASS，`45 passed, 0 failed` / `26 passed, 0 failed` |

---

## 二、這次重跑的完整輸出

### `_verify_phase1.mjs --reclaim`

```
=== ① PM-307 URL 驗證（函式抽自 extension/dist/background.js）===
  PASS  https 通過
  PASS  http 通過
  PASS  拒絕 chrome://extensions
  PASS  拒絕 file:///C:/secret.txt
  PASS  拒絕 javascript:alert(1)
  PASS  拒絕 data:text/html,<h1>x
  PASS  拒絕 ftp://x.com
  PASS  拒絕 "not a url"（不 crash）
  PASS  拒絕 ""（不 crash）
  PASS  拒絕 undefined（不 crash）
  PASS  錯誤訊息含 protocol 提示

=== ① PM-308 click 前置檢查存在性（原始碼靜態檢查）===
  PASS  有 disabled 檢查
  PASS  有可見性檢查且共用 isElementVisible
  PASS  isElementVisible 優先用 checkVisibility，並保留 computedStyle 後備
  PASS  有尺寸為 0 檢查
  PASS  有非法選擇器 try/catch
  PASS  textContent 截斷 100
  PASS  先取 text 再點擊（點擊可能導航）
  PASS  找不到元素的錯誤含 selector

=== ② MCP 工具註冊 ===
  PASS  工具總數 6
  PASS  navigate_to 已註冊
  PASS    navigate_to.url 必填
  PASS    navigate_to.tab_id 可選 integer
  PASS  click_element 已註冊
  PASS    click_element.selector 必填
  PASS    click_element.tab_id 可選 integer
  PASS  read_page 已註冊
  PASS    read_page.tab_id 可選 integer
  PASS    read_page 描述提到敏感欄位遮蔽
  PASS    click_element 描述說明「不會假裝點成功」
  PASS  PM-307 無效 URL 在 MCP 層被擋
  PASS  PM-308 空 selector 在 MCP 層被擋

=== ③ 端到端（真 Chrome + 已重新載入的擴充功能）===
    navigate_to → {"tab_id":1605029011,"url":"https://example.com/","title":"Example Domain"}
  PASS  307-1 開新分頁回 tab_id + url + title
  PASS  307-2 指定 tab_id 導航
  PASS  307 §13.3 不存在 tab_id → 明確報錯
  PASS  308-1 前置：read_page 給得出可用的 selector
    click_element( body > div:nth-of-type(1) > a:nth-of-type(1) )
      → {"clicked":true,"element_text":"繁","element_tag":"a","tab_id":1605029011}
  PASS  308-1 點擊成功回 element_text
  PASS  308-2 找不到元素 → error 含 selector
  PASS  308-3/4 分頁不存在 → error
  PASS  308 非法選擇器 → error（不 crash）
    read_page → {"url":"https://bugezy.dev/guide","title":"使用指南 · BugEzy","content":"[a.lang-switch] EN → click: \"body > a\" …
  PASS  309-1 回傳結構化頁面文字
  PASS  309-2 interactive 元素附 click: selector
  PASS  309-4 不超過 50000 字元
  PASS  309-5 回傳 tab_id + element_count
  PASS  309-2 read_page 的 selector 可直接餵給 click_element

45 passed, 0 failed
```

### `_verify309.mjs`（jsdom 真實 DOM）

```
=== ① 不重複輸出父層文字（卡片版本最大的坑）===
  PASS  「唯一的一段文字」只出現 1 次（不是每層各印一次）
  PASS  沒有文字的容器不佔行

=== ② interactive 元素附可直接用的 selector ===
  PASS  button 有 click: selector
  PASS  a 也有 selector
    selectors → ["body > div > button:nth-of-type(1)","body > div > button:nth-of-type(2)"]
  PASS  同 class 多元素 → selector 各自唯一命中 1 個

=== ③ 隱藏元素不出現 ===
  PASS  display:none 的子樹整個不出現
  PASS  visibility:hidden 不出現
  PASS  可見的仍在

=== ④ 50000 字元上限 ===
    長度 = 49987 truncated = true
  PASS  未超過 50000
  PASS  truncated 旗標為 true
  PASS  結尾標註 (truncated)

=== ⑤ 表單欄位資訊 + 敏感值遮蔽 ===
  PASS  一般 input 有 type/placeholder/value
  PASS  🔴 password 的值被遮蔽
  PASS  🔴 name 含 token 的欄位也被遮蔽

=== ⑥ 跳過 script/style/svg + iframe 標註 ===
  PASS  script 內容不出現
  PASS  style 內容不出現
  PASS  iframe 有標註讀不到

=== ⑦ 優先取 <main> ===
  PASS  有 <main> 時只讀 main

=== ⑧ PM-308 bridgeClick：不可點的元素要明確回報（不是靜默成功）===
  PASS  可點的元素 → clicked:true 且真的觸發了 click
  PASS    回傳 tag 與文字
  PASS  🔴 disabled → 回 error 而不是 clicked:true
  PASS  🔴 display:none → 回 error
  PASS  🔴 visibility:hidden → 回 error
  PASS  找不到 → error 含 selector
  PASS  非法選擇器 → error（不 crash）
  PASS  不可點時仍回報找到的 tag/text（讓 AI 知道選對了元素）
  NOTE  「祖先 display:none」與「尺寸為 0」兩條在 jsdom 測不準（無版面、無 checkVisibility），
        真瀏覽器走 el.checkVisibility() 與真實 rect —— 以端到端為準。

26 passed, 0 failed
```

**孤兒檢查**：跑完後 `port 19850 free` —— 無殘留程序。

---

## 三、第一次沒跑成的原因（已解決）

第一次執行時端到端整段 SKIP，原因**不是產品問題，也不是 FOX 沒做前置**：port 19850 被**驗證工具自己先前留下的孤兒 bridge** 占住（`node dist/index.js`，父程序 `cmd.exe`）。連帶效果是 FOX 重啟的 MCP server 一起來就被擋在門外、進入 `disable()` 狀態。

追查方式：查 port 持有者 PID → 比對啟動時間與建置時間 → 用 session 的 MCP `ping` 反證「被擋住的是 MCP server 自己」→ 查父程序命令列確認是測試工具留下的。

**這是同一個坑的第二次**（PM-298 曾有 PID 11964 的同款孤兒），所以這次連根修掉。

### harness 修法

- `proc.kill()` → **`taskkill /PID <pid> /T /F`**（`/T` 連同整棵 process tree），並掛在 `exit` / `SIGINT` / `uncaughtException` 三個出口，不論怎麼結束都收乾淨
- 加**啟動前 port 檢查**，但兩道保險：① 只有**父程序是 `cmd.exe` 且指令是 `dist/index.js`**（＝從 shell 直接跑的測試程序）才判定為洩漏 ② 要**明確帶 `--reclaim`** 才動手。因為占 port 的也可能是 **Claude Code 自己 spawn 的 MCP server**，那個絕不能亂殺；不符條件時只回報、不動它

修完連跑五輪，port 每次都乾淨釋放。

---

## 四、過程中修掉的一個真產品 bug

把 `bridgeClick` 也放進 jsdom 測之後（PM-308 當時只做了靜態檢查），測試立刻打臉：

原本的可見性判斷是手寫的 `getComputedStyle(el).display === 'none' || ...`，**但 `getComputedStyle` 只看元素自己**。父層 `display:none` 時，子元素的 computed display **仍然是 `block`**（Chrome 也一樣）——所以「按鈕被藏在一個收合的 `<div>` 裡」這種最常見的情況，原本的檢查抓不到，只能靠後面「尺寸為 0」那道兜底。

**修法**：改呼叫與 `read_page` **共用的 `isElementVisible()`** —— 優先走 `el.checkVisibility()`（Chrome 105+，正確處理祖先隱藏），沒有才退回 `getComputedStyle`。兩支工具現在對「可見」的定義一致，不會出現 `read_page` 說看不到、`click_element` 卻說可以點的矛盾。

**改動檔案**：`extension/src/content.ts`（`bridgeClick` 的可見性判斷）。

---

## 五、出現過但都是測試自身問題的三個 FAIL

記下來是因為它們**長得很像產品缺陷**，PM 讀報告時容易誤判：

| FAIL | 真正原因 | 處置 |
|---|---|---|
| `click_element` 找不到 `a[href*="faq"]` | 我把 selector 寫死，但分頁那時已被導到 `/faq`，頁面上自然沒有指向 faq 的連結。**工具行為完全正確**（回報找不到 + 提示可用 `read_page` 確認）| 改成**從 `read_page` 的輸出取 selector** —— 這本來就是驗收條件 2 要證明的事 |
| `read_page` 回空的 `content` | 上一步的 click 觸發導航，read_page 在頁面 render 完之前就讀了（`element_count` 只有 31）| 改成先用 `navigate_to` 把分頁帶到確定頁面（它本來就會等 load 完成）|
| 靜態檢查「有 display:none / visibility:hidden 檢查」 | 我把 `bridgeClick` 改用 `isElementVisible` 後，舊正規表示式對不上。**這正是靜態檢查該有的行為** —— 結構一變就失敗，而不是靜默通過 | 更新為斷言新結構 |

---

## 六、留給後續的兩件事（未自行處理）

1. **`read_page` 在頁面載入中會回空字串**，AI 分不出「這頁真的空的」還是「還沒載完」。建議回傳加上 `document.readyState`。**超出本卡範圍，未動手。**
2. **FOX 的 bugezy-bridge MCP server 仍是舊的工具清單**（只有 `ping` / `get_page_url` / `get_live_errors`），且啟動時已 `disable()`。要在對話裡直接叫用 `navigate_to` / `click_element` / `read_page`，需**重連一次 MCP**。驗證工具不受影響（自己 spawn bridge）。

---

### 🟡 PM-311：Phase 1 MCP 工具 — type_text

**目標**：實作 `type_text(selector, text, tab_id?)` 工具，讓 AI 能在表單欄位輸入文字。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('type_text', ...)
參數：
  selector: z.string() — CSS 選擇器，指向 input/textarea/contenteditable
  text: z.string() — 要輸入的文字
  tab_id: z.number().optional()
回傳：{ typed: true, selector, tab_id, previous_value, new_value }
```

**Extension 側**（`extension/src/background.ts` → `runBridgeCommand`）：
```
case 'type_text':
  // 用 resolveTargetTab() 取得 tab_id（沿用 PM-308 的共用函式）
  // chrome.tabs.sendMessage → content script
```

**content script 側**（`extension/src/content.ts`）：
```
case 'BRIDGE_TYPE_TEXT':
  const el = document.querySelector(msg.selector)
  驗證：
  1. 元素存在？（沿用 click_element 的錯誤格式）
  2. 元素是 input/textarea/[contenteditable]？不是 → error
  3. 元素 disabled/readonly？→ error 帶原因
  4. 元素可見？（用共用 isElementVisible）
  
  執行：
  - input/textarea → el.value = text，觸發 input + change 事件
  - contenteditable → el.textContent = text，觸發 input 事件
  - 回傳 previous_value + new_value
```

**關鍵細節**：
- 必須觸發 `input` 和 `change` 事件（React/Vue 等框架靠這些事件同步狀態）
- `previous_value` 回傳讓 AI 知道有沒有覆蓋掉原本的值
- 敏感欄位（password/token）的 `previous_value` 和 `new_value` 要遮蔽（沿用 PM-309 的 SENSITIVE_RECT_SELECTORS）
- readonly 和 disabled 分開報錯（AI 需要知道是哪一種）

**驗收條件**：
1. 對 input 輸入文字 → value 更新 + input/change 事件觸發
2. 對 textarea 輸入 → 同上
3. disabled/readonly → error 帶原因
4. 非表單元素 → error
5. password 欄位的值被遮蔽

完成後在下方寫 `🔵 DONE-311`。

---

### 🔵 DONE-311：`type_text` 已實作，五項驗收全數在真實 DOM 驗過；**端到端差一次擴充功能重新載入**

**改動檔案**：`bridge/src/mcp-server.ts`、`extension/src/background.ts`、`extension/src/content.ts`、`extension/src/types.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 對 input 輸入 → value 更新 + input/change 事件觸發 | ✅ jsdom 實測值更新，且兩個事件都以 `bubbles:true` 觸發各一次 |
| 2 | 對 textarea 輸入 | ✅ |
| 3 | disabled / readonly → error 帶原因 | ✅ **兩者訊息分開**（readonly 的錯誤裡不含 disabled 字樣，反之亦然）|
| 4 | 非表單元素 → error | ✅ 另含 checkbox／radio／file 等不吃文字的 input type |
| 5 | password 欄位的值被遮蔽 | ✅ `previous_value` 與 `new_value` 皆遮蔽，但**值確實有寫進 DOM** |

**測試結果**：`_verify309.mjs` **41/41**（jsdom 真實 DOM，其中 PM-311 佔 14 項）、`_verify_phase1.mjs` **50 PASS / 3 FAIL**，3 個 FAIL 全是同一個原因 —— 見下方。

---

### 🔴 卡片寫的做法在 React 網站上會靜默失敗

卡片的執行步驟是：

```
input/textarea → el.value = text，觸發 input + change 事件
```

「要觸發 input/change」這個判斷是對的，**但只做到這樣，React 網站上不會生效**。

React 會在 input 節點上裝一個 **`_valueTracker`**：它用 own property 的 getter/setter 攔截 `.value`，並快取目前的值。流程是：

- `el.value = text` → 打到 **tracker 的 setter** → 快取同步更新成新值
- 接著 dispatch `input` → React 比對「快取 vs 現值」→ **一樣** → 判定沒有變化 → **整個事件被忽略**

結果就是：**畫面上文字出現了，但 React state 完全沒動**，接著送出表單會送出空值。而工具會回報 `typed: true`，AI 從回傳值上完全看不出來 —— 又一個 PM-308 那種「靜默假成功」。

**正解**是透過 **prototype 上的原生 setter** 寫入，繞過 tracker：

```ts
const proto = isTextarea ? HTMLTextAreaElement.prototype : HTMLInputElement.prototype;
Object.getOwnPropertyDescriptor(proto, 'value')?.set?.call(input, text);
input.dispatchEvent(new Event('input',  { bubbles: true }));
input.dispatchEvent(new Event('change', { bubbles: true }));
```

這樣 tracker 的快取會維持舊值，React 一比對就發現不一致，正常處理事件。

**`bubbles: true` 也是必要的**：React 用的是掛在 root 的委派監聽，事件不冒泡就收不到。

測試裡我模擬了一個 React 風格的 tracker，斷言兩件事：**真實值已更新** ＋ **tracker 快取仍是舊的**。

> 寫這條測試時我第一次把期望寫反了（斷言快取應該更新），結果 FAIL —— 但那個 FAIL 正好證明程式是對的：**快取被更新才是壞掉的路徑**。已改成正確的模型。

---

### 另外三個卡片沒寫的處理

**① 不吃文字的 input type 要擋下來。** `checkbox` / `radio` / `file` / `button` / `submit` / `reset` / `image` / `range` / `color` 硬寫 `value` 只會靜默無效。已個別報錯並給下一步：checkbox／radio 明講「要改變勾選狀態請用 `click_element`」，file 明講「瀏覽器基於安全性禁止用程式設定檔案欄位」。

**② 寫進去之後再讀一次確認。** 欄位可能有 `maxlength` 或輸入遮罩，實際存下的值和要求的不一樣。不一致時回傳會附 `warning`，並要 AI 以 `new_value` 為準 —— 否則 AI 會以為自己填了 20 碼但其實只進去 10 碼。

**③ 敏感欄位的 `previous_value` 也要遮。** 卡片有提到遮蔽，但要注意**兩個**欄位都會外洩：`new_value` 是 AI 自己給的（它本來就知道），**`previous_value` 才是真正的風險** —— 那是使用者原本存在欄位裡的密碼。沿用 PM-309 的 `SENSITIVE_RECT_SELECTORS`，沒有另立定義。

順帶：`previous_value` 的用途正是卡片說的「讓 AI 知道有沒有覆蓋掉原本的值」，所以工具描述裡明講 **`text` 會取代欄位原有的全部內容，不是附加**。

---

### 🟡 端到端還差一步：擴充功能要再重新載入一次

3 個 FAIL 的錯誤訊息都是同一句：

```
{"error":"未知的指令：type_text","extension_connected":true}
```

**Chrome 不會自動重載 `dist/`** —— 你為 PM-307~309 重新載入之後，我又為這張卡改了 `background.js`，所以目前跑的仍是沒有 `type_text` 的版本。

值得注意的是，**前置步驟全部通過了**：`read_page` 在真實的 DuckDuckGo 表單頁上找到了輸入框並給出可用的 selector：

```
[input#search_form_input_homepage.search__input] q type=text → click: "#search_form_input_homepage"
```

（這裡的標籤 `q` 就是 PM-309 那個遮蔽修正的效果 —— 表單欄位的 label 退回 `name` 屬性而不是 `.value`。）

也就是說**整條鏈路只差擴充功能端的那個 `case`**。

> 順帶說明測試為什麼要開 DuckDuckGo：**bugezy.dev 全站沒有任何 `input` 或 `textarea`**（四頁都查過，0 個），沒有欄位可以測。選 `html.duckduckgo.com/html/` 是因為它是**純 HTML 表單、沒有 JS 自動完成**，打字不會送出任何請求；而且欄位是從 `read_page` 的輸出找出來的，沒有寫死 markup。

**請到 `chrome://extensions` 重新載入 BugEzy**，跟我說一聲我立刻重跑補完這 3 項。

---

### 🟡 PM-312：Phase 1 MCP 工具 — take_screenshot

**目標**：實作 `take_screenshot(tab_id?)` 工具，讓 AI 能截取頁面截圖。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('take_screenshot', ...)
參數：
  tab_id: z.number().optional()
回傳：{ image_base64, format: 'png', width, height, tab_id, url }
```

**Extension 側**（`extension/src/background.ts` → `runBridgeCommand`）：
```
case 'take_screenshot':
  // 用 resolveTargetTab()
  // chrome.tabs.captureVisibleTab()（只能截當前可見分頁）
```

**§13.2 背景分頁限制**：
`captureVisibleTab` 只能截 active tab。如果目標 tab 不是 active：
1. 暫時 `chrome.tabs.update(tab_id, { active: true })`
2. 等一小段時間讓頁面 render
3. 截圖
4. 切回原本的 active tab
5. 回傳時附帶 `warning: '已暫時切換分頁以截圖'`

**或者**（更簡單但限制更大）：
- 如果 tab 不是 active，直接回 error：`'take_screenshot 只能截當前可見分頁，請先用 navigate_to 切換或省略 tab_id'`

**→ 請 FOX 決定用哪個方案。我傾向後者（簡單方案），因為搶焦點會干擾用戶。**

**驗收條件**：
1. 截取當前分頁 → 回傳 base64 PNG + 寬高
2. 回傳的 base64 是有效圖片（可解碼）
3. 非 active 分頁 → 依選定方案處理
4. chrome:// 等受限頁面 → error

完成後在下方寫 `🔵 DONE-312`。

---

### 🔵 DONE-312：`take_screenshot` 已實作；**Chrome 權限擋住，驗收 1~3 無法通過 —— 需要決策**

> **給 PM 的一句話結論**：程式碼寫完也 build 過了，但實測發現 `captureVisibleTab` 在 bridge 的呼叫情境下**拿不到 Chrome 的權限**。這不是 bug，是 Chrome 權限模型的硬限制，**三條技術路線都繞不開**。要不要為它送一次 Chrome Web Store 審核，是產品決策，我沒有自行決定。

**改動檔案**：`bridge/src/mcp-server.ts`、`extension/src/background.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。**未新增任何權限。**

---

## 一、驗收條件現況

| # | 驗收條件 | 狀態 |
|---|---|---|
| 1 | 截取當前分頁 → base64 PNG + 寬高 | 🔴 **權限擋住，無法驗證** |
| 2 | base64 是有效圖片（可解碼）| 🔴 同上（測試已寫好，一旦取得圖片就會自動驗）|
| 3 | 非 active 分頁 → 依方案 B 處理 | 🔴 同上 |
| 4 | 受限頁面 → error | ✅ 且錯誤訊息會明講原因與下一步 |

**測試總數**：`_verify_phase1.mjs` **62 passed, 0 failed**；`_verify309.mjs` **41 passed, 0 failed**。

驗收 1~3 在測試中標為 **`LIMIT`**：

```
  LIMIT 312-1~3 未驗：captureVisibleTab 需 activeTab／<all_urls>，bridge 呼叫無使用者手勢
        → 這是 Chrome 權限模型的限制，非實作問題；處置方式待決策
```

**刻意不標 PASS** —— 沒驗到就是沒驗到，不因為「原因合理」就當作通過。
**也刻意不標 FAIL** —— 否則這條已知限制會永遠紅著，把日後真正的迴歸蓋掉。
權限情況一旦改變（加了 `<all_urls>`，或使用者先點過圖示），測試會**自動改走正常驗證路徑**。

---

## 二、實測發現了什麼

Chrome 回的原話：

```
Either the '<all_urls>' or 'activeTab' permission is required.
```

**根因**：`captureVisibleTab` 需要 `activeTab` 或 `<all_urls>`。BugEzy 只有 `activeTab`，而 **`activeTab` 只在使用者「主動叫用擴充功能」之後才會授予該分頁**（點擴充圖示／快捷鍵／右鍵選單）。**bridge 的呼叫永遠沒有使用者手勢。**

現有的截圖功能之所以一直沒問題，是因為它的觸發路徑本來就是使用者操作（popup／快捷鍵 → content overlay → `CAPTURE_SEGMENT`），手勢天然存在。

**而且變通法比原本設想的更受限：導航會撤銷 `activeTab`。**

所以「請使用者先點一下 BugEzy 圖示」只在這個順序下成立：

```
使用者點圖示 → 直接 take_screenshot        ✅ 可行
使用者點圖示 → navigate_to → take_screenshot ❌ 導航已撤銷授權
```

**結論：出任務模式（AI 自己開背景分頁去跑）裡這支工具基本不可用**，只有偵察模式（使用者剛操作過那一頁）能用。

---

## 三、這正好印證了規格書 §13.2 當初標記的疑慮

PM-300 撰寫 §13.2 時我列過出任務模式截圖的三條路，並標明「**Phase 1 動工前需實測確認**」：

| 方案 | 當時的評估 | 實測後 |
|---|---|---|
| ① 暫時切過去截圖 | 會搶焦點 | **焦點不是問題，權限才是**（本次採用的方案 B）|
| ② `chrome.debugger` | 需 `debugger` 權限＝重新審核 ＋ 黃色橫幅 | 不變 |
| ③ 獨立視窗 `windows.create({focused:false})` | 當時認為「最划算，需實測」 | **一樣過不了 `activeTab` 這關** |

**實測後可以確定：三條路都繞不開權限。** `captureVisibleTab` 的門檻不在「分頁可不可見」，而在「**有沒有使用者手勢**」——③ 只解決了可見性，沒解決授權。這比規格書當時寫的更明確，**建議回填進 §13.2**。

---

## 四、需要 FOX 決策的三條路

我已詢問但尚未收到選擇，因此**維持現狀、不加任何權限**（唯一可逆的選項）。

| 方案 | 得到 | 代價 |
|---|---|---|
| **A. 保留現狀 + 文件化限制**（我的建議）| 不動權限、不觸發審核；偵察模式下仍可用 | 出任務模式的截圖不可用 |
| **B. 加 `<all_urls>` host permission** | 截圖在所有情境可用，包含背景分頁 | **必送 Chrome Web Store 重新審核**，而 v1.1.5 已被退件兩次（未使用的 `scripting` 權限、隱私政策資訊不足）；`<all_urls>` 是審核最嚴格的權限之一；`/privacy` 與 Dashboard 都要同步更新；**並打破 bridge 從 PM-297 以來「不新增任何權限」的設計前提**（那也正是當初否決 Native Messaging 的同一個理由）|
| **C. 移出 Phase 1** | Phase 1 專注在 `read_page`（本來就是 §8 主打的差異化：文字讀 DOM 省 95% token）| 少一支工具；等 v1.1.5 過審穩定後再單獨評估 |

**程式碼已經寫好且會保留** —— A 與 C 都不需要再動實作，只是文件與定位的差別；**只有 B 需要改 manifest 並送審**。

---

## 五、實作內容（已完成的部分）

**方案 B 的切換流程**（依 FOX 先前的決策）：目標分頁不是 active 時 → `chrome.tabs.update(tab_id,{active:true})` → 等 400ms render → `captureVisibleTab` → **切回原本的 active tab** → 回傳附 `warning`。

切回那一步放在 **`finally`** 裡：**截圖失敗也一定會切回去**，否則使用者的分頁會被丟在別的地方不還。

**兩個卡片沒寫但影響很大的決定**：

**① 圖片不塞進 JSON 文字，改走 MCP 原生的 image content。** 卡片的回傳規格是 `{ image_base64, ... }`，但一張全螢幕 PNG 的 base64 動輒數十萬字元，**當成文字回傳會直接灌爆 AI 的 context**，而且用戶端也無法把它當圖片顯示。改為 `content: [{type:'image', data, mimeType}, {type:'text', text: metadata}]`。

**② 工具描述主動把 AI 推向 `read_page`。** §8 把「文字讀 DOM 而非截圖，省 95% token」列為 BugEzy 的差異化重點；描述若寫得中性，AI 會習慣性先截圖，等於自己把優勢丟掉。描述裡明寫「想了解頁面結構請優先用 `read_page`」，並載明截圖的前提條件。

**已驗證的部分**：`pngSize()` 用**真實 PNG 檔**驗過（函式從 `dist/background.js` 抽出，非另抄一份）——

```
  PASS  icon-16.png → 16x16
  PASS  icon-48.png → 48x48
  PASS  icon-128.png → 128x128
  PASS  非 PNG 資料 → 回 0x0（不 crash）
  PASS  空字串 → 回 0x0（不 crash）
```

> 尺寸直接讀 PNG 的 IHDR（前 24 bytes），沒有用 `createImageBitmap` 解整張圖 —— service worker 裡沒有 `Image`，而為了拿寬高去解一張全螢幕 PNG 太浪費。

**MCP schema 也已驗**：`take_screenshot` 已註冊、工具總數 **8**、`tab_id` 可選 integer、描述確實包含引導使用 `read_page` 的字樣。

---

### 🟡 PM-313：Phase 1 MCP 工具 — get_browser_errors（即時瀏覽器錯誤）

**目標**：在 bridge 端新增 `get_browser_errors(tab_id?)` 工具，透過 Extension 即時讀取瀏覽器 Console + Network 錯誤。

**背景**（決策 1）：既有 `get_live_errors` 維持不動（讀 R2 快取），bridge 端另建 `get_browser_errors` 專門走即時通道。未來 Cloudflare Workers 端的 `get_live_errors` 可根據 `source` 參數路由。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('get_browser_errors', ...)
參數：
  tab_id: z.number().optional()
回傳：{
  console_errors: [{ level, message, source, line, timestamp }],
  network_errors: [{ url, status, method, timestamp }],
  total_count, tab_id
}
```

**Extension 側**（`extension/src/background.ts` → `runBridgeCommand`）：
```
case 'get_browser_errors':
  // chrome.tabs.sendMessage → content script
```

**content script 側**（`extension/src/content.ts`）：

**關鍵設計**：BugEzy 已有完整的錯誤攔截機制（inject.ts 七代迭代），不要重寫一套。

沿用既有機制的路線：
1. inject.ts 已攔截 console.error / window.onerror / unhandledrejection / network 錯誤
2. 這些資料已經存在 content script 可存取的範圍內（或透過 window.postMessage 傳遞）
3. `BRIDGE_GET_BROWSER_ERRORS` handler 只需**讀取已收集的錯誤**，不需自己掛攔截器

如果既有機制的資料不在 content script 能直接拿到的地方，備案：
- content script 自己掛一組輕量攔截器（onerror + unhandledrejection + PerformanceObserver for network）
- 啟動時開始收集，呼叫時回傳已收集的內容
- 明確標註「這是獨立於 inject.ts 的輕量版」

**驗收條件**：
1. 呼叫 `get_browser_errors()` → 回傳 console + network 錯誤陣列
2. console_errors 每筆含 level / message / source
3. network_errors 每筆含 url / status / method
4. 沒有錯誤時回空陣列（不是 error）
5. 指定 tab_id → 讀該分頁的錯誤

完成後在下方寫 `🔵 DONE-313`。

---

### 🔵 DONE-313：`get_browser_errors` 已實作（沿用 inject.ts 既有機制）；**端到端差一次擴充功能重新載入**

**改動檔案**：`bridge/src/mcp-server.ts`、`extension/src/background.ts`、`extension/src/content.ts`、`extension/src/types.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。

**採用卡片建議的主路線**：不重寫攔截器，直接讀 inject.ts 既有的背景緩存（PM-50/51 的 `GET_LIVE_ERRORS` 通道）。查證確認 **inject 在 `document_start` 就開始收，不需要先按錄製**，所以 content script 這邊只需要轉發，不必掛備案的輕量攔截器。

**測試現況**：`_verify_phase1.mjs` **66 passed, 4 failed** —— 4 個 FAIL 全是 `未知的指令：get_browser_errors`（Chrome 未重載）。MCP schema 全數通過，工具總數 **9**。

---

### 🔴 最重要的一件事：這支工具只看得到最近 30 秒

查 `inject.ts` 才發現，背景緩存是 **`REWIND_WINDOW = 30_000` 的滾動視窗**，每 5 秒裁掉舊資料：

```ts
// 每 5 秒裁掉超過視窗的舊資料（循環 buffer）
bgConsoleLogs = bgConsoleLogs.filter((e) => e.timestamp > cutoff);
```

卡片完全沒提到這件事，但它會直接造成誤判：**AI 打開一個頁面、做了幾件事、過個一分鐘才問「這頁有沒有錯誤？」→ 拿到空陣列 → 判定頁面正常**，而其實載入當下就噴了一堆錯，只是已經被裁掉了。

這跟 PM-304 的 `Unassigned` zone、PM-308 的靜默點擊是**同一類問題**：回傳看起來很正常，但語意跟 AI 的理解不一樣。

處置：回傳固定帶 `window_seconds: 30` 與一句 `note`，工具描述也用**大寫 IMPORTANT** 開頭講明：

> 只涵蓋最近約 30 秒（頁面內的滾動緩存）—— 要抓載入當下的錯誤，請先 `navigate_to`／重新整理再**立刻**呼叫。

---

### 另外三個處理

**① 過濾掉 `level: 'info'`。** 既有 `ConsoleLog` 的 level 有 `warn / error / info`，而 `info` 是 Web Vitals 之類的中性訊息 —— 產品內部本來就把它排除在「即時監控錯誤數」之外（`inject.ts` 的 `consoleProblems` 就是 `filter(level !== 'info')`）。一支叫 `get_browser_errors` 的工具若把它們算成錯誤，會讓「沒有錯誤時回空陣列」這條驗收永遠不成立。

**② 🔴 不回傳 `requestBody` / `responseBody`。** 既有 `NetworkError` 型別帶這兩個欄位，裡面可能有**認證 token 或使用者個資**，而這份結果會整份進到 AI 的 context。卡片要的欄位是 `url / status / method / timestamp`，我只回這些（外加 `duration`），把兩個 body 濾掉。同 PM-309 的考量。

**③ `source` 欄位補預設值。** 既有型別裡 `source` 是**可選**的（來自一般 `console.*` 時會省略），但驗收條件 2 要求每筆都含 `source`。缺的一律補 `'console'`，讓欄位結構穩定。

**沒有 `line` 欄位。** 卡片的回傳規格寫了 `line`，但既有 `ConsoleLog` 型別**沒有這個欄位**，inject 也沒有收。我**沒有硬湊一個出來**（從 message 字串裡剖析行號並不可靠，湊出來的錯誤行號比沒有更糟）。驗收條件 2 只要求 `level / message / source`，這三個都有。若確實需要 `line`，要改的是 inject.ts 的攔截層，範圍比這張卡大。

---

### 🟡 一個重疊，我沒有自行移除

bridge 端**現在有兩支功能重疊的工具**：PM-297 建的 `get_live_errors`，和這張卡新建的 `get_browser_errors`。兩者都是「即時讀當前分頁的錯誤」，而且 bridge 的 `get_live_errors` 本來就**不讀 R2 快取**（它會直接回一句「請改用雲端 MCP」）。

卡片寫「既有 `get_live_errors` 維持不動（讀 R2 快取）」—— 那句描述**符合 Cloudflare Workers 端的那一支，不符合 bridge 這一支**，所以我判斷不該把它當成「保留 bridge 上的重複工具」的指示，但也不敢直接刪。

折衷處置：**保留工具本體不動**（不刪、行為不變），只把它的描述改成標明「已由 `get_browser_errors` 取代，請優先用那一支」。這樣 AI 不會在兩支之間亂猜，而你要保留或移除都還有空間。

> 這一點值得決策：PM-296 決策 2 選 snake_case 不加前綴的理由之一就是「**工具數量翻倍會稀釋 AI 的選擇準確度**」，留兩支做同一件事正好違反這個原則。建議之後把 bridge 的 `get_live_errors` 刪掉 —— 它尚未 `npm publish`，現在移除成本最低。

---

### 已通過的驗證

MCP schema 全數通過：

```
  PASS  工具總數 9
  PASS  get_browser_errors 已註冊
  PASS    get_browser_errors.tab_id 可選 integer
  PASS    描述明講只涵蓋最近 30 秒（避免空陣列被誤讀成沒問題）
  PASS    舊的 get_live_errors 已標示由 get_browser_errors 取代
```

端到端的 5 條驗收測試都已寫好（乾淨頁回空陣列、`tab_id` 對應、`window_seconds`、欄位結構、**斷言不含 `requestBody`/`responseBody`**），並且會導到一個必定 404 的路徑製造真實錯誤再讀。

**請到 `chrome://extensions` 重新載入 BugEzy**，跟我說一聲我立刻補完。

> 順帶一提：Phase 1 每張改到 `background.js` 或 `content.ts` 的卡都需要重新載入一次。若後面還有連續幾張工具卡，可以等全部寫完再一起載入、一次驗收，省掉來回。


---

### 🟡 PM-314：刪除 bridge 端舊 get_live_errors + 端到端驗收 PM-311/313

**目標**：
1. 刪除 `bridge/src/mcp-server.ts` 中舊的 `get_live_errors` 工具（已被 `get_browser_errors` 取代）
2. Extension 已重新載入，跑端到端驗收補完 PM-311（type_text 3 項）+ PM-313（get_browser_errors 5 項）

---

**步驟 1：刪除舊工具**
- 移除 `mcp-server.ts` 中 `server.tool('get_live_errors', ...)` 的整個定義
- 移除 `extension/src/background.ts` 中 `runBridgeCommand` 的 `case 'get_live_errors'`（如果有的話）
- bridge `tsc` 確認通過
- 工具總數應從 9 降為 8

**步驟 2：端到端驗收**
- 跑 `_verify_phase1.mjs --reclaim` + `_verify309.mjs`
- PM-311 type_text 的 3 項端到端應轉為 PASS
- PM-313 get_browser_errors 的 5 項端到端應跑通
- 確認舊 `get_live_errors` 不在工具清單中

**驗收條件**：
1. bridge 工具總數 = 8（無舊 get_live_errors）
2. PM-311 type_text 端到端全 PASS
3. PM-313 get_browser_errors 端到端全 PASS
4. 全部測試 0 failed

完成後在下方寫 `🔵 DONE-314`。

---

### 🔵 DONE-314：舊 `get_live_errors` 已移除，工具總數 **8**；端到端 **72 + 41 = 113 項全數 0 failed**

**改動檔案**：`bridge/src/mcp-server.ts`（移除工具）、`extension/src/background.ts`（移除指令 case）、`bridge/_verify_phase1.mjs`（更新預期 + 修正一處測試盲點）。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | bridge 工具總數 = 8（無舊 `get_live_errors`）| ✅ 並另加一條斷言：`get_live_errors` **不在**工具清單中 |
| 2 | PM-311 `type_text` 端到端全 PASS | ✅ 3/3（PM-311b 已通過，本次重跑仍全綠）|
| 3 | PM-313 `get_browser_errors` 端到端全 PASS | ✅ 5 項通過 + **1 項標 LIMIT**（見下方說明，不是 FAIL 也不是假 PASS）|
| 4 | 全部測試 0 failed | ✅ `_verify_phase1.mjs` **72 passed, 0 failed**、`_verify309.mjs` **41 passed, 0 failed** |

跑完 port 19850 乾淨釋放，無孤兒殘留。

---

### 步驟 1：移除的範圍（比卡片列的少一項，刻意的）

- ✅ `bridge/src/mcp-server.ts` 的 `server.tool('get_live_errors', ...)` 整段移除（1726 字元）
- ✅ `extension/src/background.ts` 的 `case 'get_live_errors'` 移除
- 🔴 **`content.ts` 的 `GET_LIVE_ERRORS` 訊息處理保留不動**

第三項要特別說明：那個訊息**不只 bridge 在用**。查證後確認 `background.ts:344` 的**即時監控功能（PM-51/52）**也在送同一個訊息 —— 一起刪掉會把一個跟本次無關的既有功能弄壞。已在移除處留註解說明為什麼只刪一半。

---

### 🔴 順手修掉一個測試盲點：驗收 3 差點在「其實有資料」的情況下被跳過

第一次重跑時 `_verify_phase1.mjs` 顯示 **70 passed, 0 failed**，看起來全綠。但仔細看輸出，PM-313 的 **313-2（console 欄位結構）與 313-3（network 欄位結構）根本沒執行**，只印了一行 NOTE。

原因是我在 PM-313 寫測試時，把欄位結構的斷言押在「導到 404 頁之後那一次呼叫」，而那次回的是 `total_count: 0`，於是整個 `if` 區塊被跳過。

**但同一次跑的「乾淨頁」呼叫其實有抓到 console 錯誤**：

```json
{"level":"warn","message":"Resource load failed: <script> https://static.cloudflareinsights.com/beacon.min.js/…","source":"resource-error"}
```

也就是說 —— **資料一直都在，只是我押錯地方**。0 failed 讓它看起來通過了，但驗收 3 其實沒被驗到。這正是我一路在防的那類問題（PM-304 的 `Unassigned`、PM-308 的靜默點擊、PM-313 的 30 秒視窗），這次是出現在**我自己的測試**上。

**修法**：欄位結構改成用「兩次呼叫中**任何一次真的抓到東西**」的結果來驗，並補一條斷言 —— **確認 `level:'info'` 真的被濾掉了**。

修完之後 313-2 正常通過，總數從 70 升到 **72**。

---

### 🟡 313-3 標為 LIMIT：需要頁面自己發出失敗的 fetch/XHR

```
  LIMIT 313-3 未驗：需要頁面自己發出 4xx/5xx 的 fetch/XHR 才會有資料
        （導航到 404 只是文件請求，inject 攔的是 fetch/XHR，不會產生 network_errors）
```

我原本以為「導到一個 404 網址」就會產生 network error，實測發現不會 —— **inject 攔截的是 `fetch` / `XHR`，而導航是文件請求，不走那條路**。要驗這一條，需要一個「頁面本身會發出失敗 API 請求」的測試頁，而 bugezy.dev 上沒有這種頁面（全站連 `input` 都沒有）。

一樣的原則：**不標 PASS**（沒驗到就是沒驗到），**也不標 FAIL**（不是程式問題）。若之後有需要，最乾淨的作法是在 bugezy.dev 放一個測試頁，故意 fetch 一個 404 端點。

> `network_errors` 的**欄位形狀本身**是安全的：它直接來自既有的 `NetworkError` 型別（`method`/`url`/`status`/`timestamp`/`duration`），而我在 content script 那層是**逐欄挑出來**的白名單，不是整包轉發 —— 所以 `requestBody`/`responseBody` 不可能漏出去（那段程式碼是靜態可讀的）。

---

### 目前 bridge 的 8 支工具

`ping` · `get_page_url` · `navigate_to` · `click_element` · `read_page` · `type_text` · `take_screenshot` · `get_browser_errors`

其中 `take_screenshot` 仍卡在 PM-312 的 `activeTab` 權限限制（標 LIMIT，待你決策），其餘 7 支端到端全通。


---

### 🟡 PM-315：Phase 1 MCP 工具 — analyze_element

**目標**：實作 `analyze_element(selector, tab_id?)` 工具，對單一元素做深度分析（DOM 屬性 + CSS 計算樣式 + 事件監聽器 + 可及性）。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('analyze_element', ...)
參數：
  selector: z.string()
  tab_id: z.number().optional()
回傳：{ selector, tag, id, classes, attributes, computed_styles, box_model, event_listeners, accessibility, visibility, tab_id }
```

**content script 側**（`extension/src/content.ts`）：
```
case 'BRIDGE_ANALYZE_ELEMENT':
  const el = document.querySelector(msg.selector)
  驗證存在 + 用共用 resolveTargetTab

  回傳：
  {
    selector,
    tag: el.tagName,
    id: el.id,
    classes: [...el.classList],
    attributes: 重要屬性（href/src/alt/role/aria-*/data-*），不要全部倒出來
    computed_styles: {
      display, position, visibility, opacity, overflow,
      width, height, margin, padding, border,
      color, backgroundColor, fontSize, fontFamily, zIndex
    },
    box_model: el.getBoundingClientRect() → { x, y, width, height, top, left },
    event_listeners: getEventListeners 不可用於 content script，改用
      - 列出 on* 屬性（onclick 等）
      - 標註「完整事件監聽器需 chrome.debugger，Phase 1 不支援」
    accessibility: {
      role: el.getAttribute('role') || 隱含角色,
      aria_label, aria_hidden, tabindex, focusable
    },
    visibility: isElementVisible(el) 的詳細結果
  }
```

**關鍵細節**：
- computed_styles 只取 debug 常用的 15~20 個屬性，不要 getComputedStyle 全部倒出（幾百個，灌爆 token）
- attributes 過濾敏感值（沿用 SENSITIVE_RECT_SELECTORS）
- 找不到元素 → 沿用 click_element 的錯誤格式（帶 selector + hint）

**驗收條件**：
1. 回傳含 tag / attributes / computed_styles / box_model
2. computed_styles 只有精選屬性（不超過 25 個）
3. 找不到元素 → error 含 selector
4. 敏感屬性值被遮蔽

完成後在下方寫 `🔵 DONE-315`。

---

### 🔵 DONE-315：`analyze_element` 已實作，**四項驗收全數在真實 DOM 驗過**；端到端差一次擴充功能重新載入

**改動檔案**：`bridge/src/mcp-server.ts`、`extension/src/background.ts`、`extension/src/content.ts`、`extension/src/types.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 回傳含 tag / attributes / computed_styles / box_model | ✅ 另含 classes、accessibility、visibility |
| 2 | computed_styles 只有精選屬性（不超過 25 個）| ✅ **20 個** |
| 3 | 找不到元素 → error 含 selector | ✅ 非法選擇器也回 error 不 crash |
| 4 | 敏感屬性值被遮蔽 | ✅ password 的 `value` 遮蔽，並加 `sensitive_field: true` 標記 |

**測試結果**：`_verify309.mjs` **57 passed, 0 failed**（PM-315 佔 16 項，jsdom 真實 DOM）；`_verify_phase1.mjs` **74 passed, 3 failed** —— 3 個 FAIL 全是 `未知的指令：analyze_element`（Chrome 未重載）。MCP schema 全過，工具總數 **9**。

---

### 🔴 `event_listeners` 這個欄位有誤導風險，我改了它的形狀

卡片已經注意到 `getEventListeners` 在 content script 不可用，要求「列出 `on*` 屬性 + 標註不支援」。但直接叫 `event_listeners` 並回一個陣列，會產生一個很糟的後果：

**現代網站幾乎都用 `addEventListener` 綁事件，行內 `onclick` 早就沒人寫了。** 所以這個欄位在絕大多數真實頁面上**永遠是空陣列** —— 而一個叫 `event_listeners: []` 的回傳，AI 讀起來就是「這個元素沒有事件處理器」。接著它會據此推論「按鈕沒反應是因為沒綁事件」，然後往完全錯誤的方向查下去。

改成物件並把話講死：

```json
"event_listeners": {
  "inline_attributes": ["onclick"],
  "note": "一律只列得出行內 on* 屬性。用 addEventListener 綁的監聽器在 content script 無法列舉（getEventListeners 僅存在於 DevTools），**空陣列不代表沒有事件處理器**。完整列舉需要 chrome.debugger 權限，Phase 1 不支援。"
}
```

工具描述裡也用 **`does NOT mean`／「不代表沒有事件處理器」** 再講一次。測試有一條專門驗「沒有行內 on* 時，那句說明仍然附帶」。

這跟 PM-308 的靜默點擊、PM-313 的 30 秒視窗是同一個模式：**回傳本身沒錯，但 AI 的預設解讀是錯的。**

---

### 其他實作決定

**① `computed_styles` 取 20 個。** 卡片列了 15 個，我另加 `boxSizing`、`fontWeight`、`flexDirection`、`justifyContent`、`alignItems` —— 這五個是查版面 bug 時最常要的（尤其 flex 相關）。仍在 25 的上限內。完整 `getComputedStyle` 有數百個屬性，一次呼叫就能吃掉數千 token，其中絕大多數（`-webkit-*` 之類）對找 bug 毫無幫助。

**② `attributes` 用白名單而非全倒。** 白名單 + `aria-*` + `data-*`，且**排除 `class`/`id`/`style`**（前兩個已有獨立欄位，`style` 可能極長）。上限 30 個，超過標 `attributes_truncated`。

**③ `role` 補隱含角色並標示來源。** 卡片寫「`role` 屬性 || 隱含角色」，但只給一個字串的話，AI 分不出「網站明確寫了 `role`」還是「我推算出來的」—— 這在查可及性問題時差很多（前者是開發者的意圖，後者是瀏覽器的預設）。多回一個 `role_is_implicit` 布林值。

**④ `visibility` 給細項而非單一布林。** 除了 `isElementVisible()` 的結果，另附 `display`/`visibility`/`opacity`/`has_size`/`in_viewport`，這樣 AI 能直接看出「為什麼看不到」，不用再多呼叫一次。

---

### 🟡 順手修掉自己測試的一個假通過

第一次跑端到端時，`315-2 computed_styles 不超過 25 個` **顯示 PASS**，但當時 `analyze_element` 根本還沒生效（回的是 `未知的指令`）。原因是我寫成：

```js
Object.keys(an.computed_styles || {}).length <= 25   // undefined → {} → 0 <= 25 → 永遠 true
```

**工具完全壞掉的情況下，這條斷言照樣綠燈。** 已把整段移進 `!an.error` 之內，並改成要求「1~25 個」（非空），錯誤時另有一條明確的 FAIL。

這是這幾張卡裡**第二次**在自己的測試上抓到同類問題（PM-314 是押錯資料來源）。共同點都是：**斷言在「受測目標不存在」時仍然成立。**

---

### 已驗證的 16 項（jsdom 真實 DOM）

涵蓋：四項驗收、classes 展開、白名單有效、`class`/`id`/`style` 不重複倒出、`role` 明寫 vs 隱含、`focusable` 判定、行內 `on*` 列舉、**空清單仍附說明**、敏感值遮蔽 + `sensitive_field` 標記、找不到元素、非法選擇器。

端到端的 6 條測試也已寫好（含 box_model 在真實版面上要有非零尺寸），**請到 `chrome://extensions` 重新載入 BugEzy**，跟我說一聲我立刻補完。

---

### 🟡 PM-316：Phase 1 MCP 工具 — get_web_vitals

**目標**：實作 `get_web_vitals(tab_id?)` 工具，讀取頁面效能指標（Core Web Vitals + 額外指標）。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('get_web_vitals', ...)
參數：
  tab_id: z.number().optional()
回傳：{ vitals, resource_summary, tab_id }
```

**content script 側**（`extension/src/content.ts`）：
```
case 'BRIDGE_GET_WEB_VITALS':
  回傳：
  {
    vitals: {
      // Core Web Vitals（PerformanceObserver 或 performance.getEntries）
      LCP: { value_ms, rating },  // Largest Contentful Paint
      FID: { value_ms, rating },  // First Input Delay（可能沒有，用戶沒互動過）
      CLS: { value, rating },     // Cumulative Layout Shift
      // 額外指標
      FCP: { value_ms, rating },  // First Contentful Paint
      TTFB: { value_ms, rating }, // Time to First Byte
      domContentLoaded_ms,
      load_ms,
    },
    resource_summary: {
      total_requests,
      total_size_kb,
      by_type: { script: { count, size_kb }, css: {...}, image: {...}, font: {...}, other: {...} }
    }
  }
```

**rating 判定**（跟 Google 標準一致）：
- LCP: good ≤2500ms, needs-improvement ≤4000ms, poor >4000ms
- FID: good ≤100ms, needs-improvement ≤300ms, poor >300ms
- CLS: good ≤0.1, needs-improvement ≤0.25, poor >0.25
- FCP: good ≤1800ms, needs-improvement ≤3000ms, poor >3000ms
- TTFB: good ≤800ms, needs-improvement ≤1800ms, poor >1800ms

**關鍵細節**：
- BugEzy inject.ts 已有 Web Vitals 收集（PM-186 系列），優先沿用
- 如果 inject.ts 的資料不在 content script 可拿到的範圍，用 `performance.getEntriesByType('navigation')` + `PerformanceObserver` 自行取
- FID 可能沒有值（用戶沒互動過），回 `null` 不要回 0
- resource_summary 用 `performance.getEntriesByType('resource')` 彙總

**驗收條件**：
1. 回傳含 LCP / FCP / TTFB（至少這三個有值）
2. 每個指標有 value + rating
3. rating 符合 Google 標準
4. resource_summary 有 total + by_type 分類
5. 沒有效能資料時回空物件（不是 error）

完成後在下方寫 `🔵 DONE-316`。

---

### 🔵 DONE-316：`get_web_vitals` 已實作；**評級與資源彙總已完整驗過**，端到端待擴充功能重新載入

**改動檔案**：`bridge/src/mcp-server.ts`、`extension/src/background.ts`、`extension/src/content.ts`、`extension/src/types.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 回傳含 LCP / FCP / TTFB | ⏳ 端到端待重載（實作完成，測試已寫好）|
| 2 | 每個指標有 value + rating | ⏳ 同上 |
| 3 | rating 符合 Google 標準 | ✅ **20 個邊界值全對**（每個指標的兩道門檻各測上下一格）|
| 4 | resource_summary 有 total + by_type | ✅ 分類、加總、低估揭露皆驗過 |
| 5 | 沒有效能資料時回空物件（不是 error）| ✅ 指標取不到一律回 `null`，不拋錯 |

**測試結果**：`_verify309.mjs` **62 passed, 0 failed**（PM-316 佔 5 項）；`_verify_phase1.mjs` **78 passed, 3 failed** —— 3 個 FAIL 是 `未知的指令`（PM-315 兩項 + PM-316 一項，Chrome 未重載）。MCP schema 全過，工具總數 **10**。

---

### 🔴 沒有 `buffered: true` 的話，這支工具會永遠回 null

LCP / CLS / FID 只能透過 `PerformanceObserver` 取得，而**工具被呼叫時，這些事件早就發生過了**。若照一般寫法註冊 observer，只會收到「註冊之後」才產生的 entry —— LCP 在頁面載入時就定案了，之後不會再有新的 entry。

也就是說：**少了 `buffered: true`，這三個指標在任何「載入完才呼叫」的情境下都回 null，而那正是這支工具唯一的使用情境。**

```ts
o.observe({ type: 'largest-contentful-paint', buffered: true });  // buffered 會補送註冊前的 entry
```

另外 observer 的回呼是在下一個 task 才送達，所以收網前等 300ms 才 disconnect —— 直接同步讀會拿到空的。

---

### 沒有沿用 inject.ts 的 Web Vitals，理由如下

卡片說「BugEzy inject.ts 已有 Web Vitals 收集，優先沿用」。查了 `inject.ts:843~895`，它確實有收 LCP/CLS/FID，**但它把結果丟進 `collectConsoleLog()` 變成人類可讀的 console 訊息**（`source: 'web-vitals'`，超標 warn、良好 info），例如「LCP 慢（超過 2.5 秒）」。

要沿用就得**從格式化字串裡把數字剖析回來** —— 既脆弱（文案一改就壞）又有損（它只在「頁面隱藏或載入 5 秒後」定案回報一次，且 CLS 只留三位小數）。

所以走卡片列的備案：**在 content script 用 Performance API 直接取**。這條路更精準，也不會跟既有的監控顯示互相干擾。（`inject.ts` 那份完全沒動。）

---

### 🔴 資源大小是低估值，而且必須講出來

`resource_summary` 的大小取自 `transferSize`，但它在兩種情況會是 **0**：

1. 快取命中
2. **跨網域且對方沒送 `Timing-Allow-Origin`** —— CDN、字型、分析腳本幾乎都屬於這類

不揭露的話，AI 會拿一個系統性偏低的數字去判斷「頁面很輕」，而真正肥的往往就是那些跨網域資源。所以回傳固定帶 `size_unknown_count`，且只要有 0 就附一句說明：

```json
"size_note": "1 個資源的 transferSize 為 0（快取命中，或跨網域且未送 Timing-Allow-Origin），**總大小為低估值**。"
```

工具描述裡也寫明 `the total is a lower bound`。

---

### 另外兩個處理

**① FID 沒有值時回 `null`，並附一句說明。** 卡片有交代「回 null 不要回 0」—— 這點很重要，因為 `FID: 0` 讀起來是「反應極快」，實際上是「使用者根本還沒點過任何東西」。除了回 null，另加 `FID_note` 講明原因，避免 AI 誤判成「這頁互動延遲完美」。

**② rating 的門檻逐一驗邊界。** 評級寫錯一格，AI 就會把「需要改進」當成「良好」而跳過真正的效能問題。所以測試不只驗典型值，而是對 5 個指標的每道門檻各測**上下一格**（2500 vs 2501、4000 vs 4001…），共 20 個案例全對。

---

### 已驗證

```
  PASS  3. rating 符合 Google 標準（20 個邊界值全對）
  PASS  4. resource_summary 有 total_requests + by_type
  PASS  4. by_type 分類正確（script/css/image/font/other）
  PASS  4. total_size_kb 合計正確
  PASS  🔴 transferSize=0 的資源有被計數並揭露「總大小為低估值」
  PASS  get_web_vitals 已註冊 / tab_id 可選 / 描述講明 FID null 與低估值
```

端到端 6 條也已寫好（含「FID 未互動時必須是 null」與「rating 只會是三種合法值」）。

**請到 `chrome://extensions` 重新載入 BugEzy** —— 這次一併會補完 **PM-315（3 項）+ PM-316（6 項）**，跟我說一聲我立刻跑。

---

### 🟡 PM-317：Phase 1 MCP 工具 — get_page_health

**目標**：實作 `get_page_health(tab_id?)` 工具，產出頁面整體健康報告（整合 errors + vitals + DOM 統計）。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('get_page_health', ...)
參數：
  tab_id: z.number().optional()
回傳：{ score, summary, details, tab_id }
```

**content script 側**（`extension/src/content.ts`）：
```
case 'BRIDGE_GET_PAGE_HEALTH':
  整合三個來源的資料：
  1. get_browser_errors 的結果（console + network 錯誤數）
  2. get_web_vitals 的結果（效能指標）
  3. DOM 統計（元素數、圖片數、無 alt 圖片、broken links、表單無 label）

  回傳：
  {
    score: 0~100,  // 綜合分數
    summary: '3 errors, 1 poor vital, 2 accessibility issues',
    details: {
      errors: { console_count, network_count, critical_count },
      performance: { lcp_rating, fcp_rating, cls_rating },
      accessibility: {
        images_without_alt,
        inputs_without_label,
        missing_lang,
        missing_title
      },
      dom: {
        element_count,
        max_depth,
        inline_styles_count,
        iframes_count
      }
    }
  }
```

**評分規則**（從 100 扣分）：
- console error：-5/個（上限 -30）
- network error：-5/個（上限 -20）
- poor vital：-10/個
- needs-improvement vital：-5/個
- 無 alt 圖片：-2/個（上限 -10）
- 無 label input：-3/個（上限 -10）
- missing lang：-5
- element_count > 3000：-5

**關鍵細節**：
- 這支工具是 AI 的「一鍵健檢」，省去分別呼叫 errors + vitals + DOM 分析
- **內部呼叫 get_browser_errors 和 get_web_vitals 的邏輯，不要透過 bridge 再繞回來**，直接在 content script 內呼叫同一組函式
- summary 用一句話講完最嚴重的問題，AI 可以決定要不要深入

**驗收條件**：
1. 回傳含 score（0~100）+ summary + details
2. 乾淨頁面 score 接近 100
3. details 含 errors / performance / accessibility / dom 四個區塊
4. summary 是人類可讀的一句話

完成後在下方寫 `🔵 DONE-317`。

---

### 🔵 DONE-317：`get_page_health` 已實作，評分規則與可及性判定全數驗過；端到端待重載

**改動檔案**：`bridge/src/mcp-server.ts`、`extension/src/background.ts`、`extension/src/content.ts`、`extension/src/types.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 回傳含 score（0~100）+ summary + details | ⏳ 端到端待重載（score 夾在 0~100 已驗）|
| 2 | 乾淨頁面 score 接近 100 | ⏳ 端到端測試已寫（`bugezy.dev/guide` 應 ≥ 70）|
| 3 | details 含 errors / performance / accessibility / dom | ⏳ 同上 |
| 4 | summary 是人類可讀的一句話 | ⏳ 同上（斷言含分數且非空）|
| — | **評分規則與卡片扣分表逐條一致** | ✅ 8 條全對 |
| — | **可及性判定不誤報** | ✅ 見下 |

**測試結果**：`_verify309.mjs` **72 passed, 0 failed**（PM-317 佔 10 項）；`_verify_phase1.mjs` **81 passed, 4 failed** —— 4 個 FAIL 全是 `未知的指令`（PM-315 兩項、PM-316 一項、PM-317 一項，Chrome 未重載）。工具總數 **11**。

依卡片要求，`bridgeGetPageHealth()` **直接呼叫 `queryInjectLiveErrors()` 與 `bridgeGetWebVitals()`**，不繞回 bridge；兩者用 `Promise.all` 並行。

---

### 🔴 兩個會製造大量誤報的可及性判定

**① `alt=""` 是合法的，不是缺失。**

空的 `alt` 屬性在 HTML 規範裡有明確語意：**這是裝飾性圖片，螢幕閱讀器應該略過它**。把它算成問題，會在**做得越用心的網站上噴越多誤報**（因為只有認真處理過可及性的人才會寫 `alt=""`）。

所以查的是 `img:not([alt])` —— 只抓「連屬性都沒有」的。測試用三張圖驗證：沒有 alt（算）、`alt=""`（不算）、`alt="說明"`（不算）。

**② 「input 沒有 label」有五種合法寫法，只查 `<label for>` 會誤報一片。**

實際會被判定為「有標籤」的情況：`label[for=id]`、**被 `<label>` 包起來**、`aria-label`、`aria-labelledby`、`title`。另外 `hidden`/`submit`/`button`/`reset`/`image` 這幾種 type 本來就不需要標籤。

測試用 7 個欄位涵蓋全部分支，只有最後那個真的沒標籤 —— **實際算出 1 個，正確**。

---

### 🔴 `null` 的指標不能算失分

`get_web_vitals` 在使用者尚未互動時 FID 是 `null`（PM-316 特意這樣設計）。若評分時把 null 當成「不合格」，**每一個還沒被點過的頁面都會平白被扣 10 分** —— 而「剛用 `navigate_to` 開好、還沒互動」正是 AI 呼叫這支工具最典型的時機。

所以扣分前先 `filter(r => r !== null)`，只計真的量到的指標。測試有一條專門檢查原始碼裡這個 filter 存在。

---

### 🔴 高分不等於沒問題 —— 必須講明

分數的錯誤部分沿用 inject 的 **30 秒滾動緩存**（同 PM-313）。也就是說：**一個載入時噴滿錯誤的頁面，只要放著超過 30 秒再檢查，就會拿到接近 100 分。**

一支叫 `get_page_health` 的工具回「100 分」，AI 幾乎一定會據此結案。所以：

- 回傳固定帶 `errors_window_seconds: 30` 與 `note`
- 乾淨時的 summary 明寫「**最近 30 秒內**沒有錯誤」，而不是「沒有錯誤」
- 工具描述也寫明 `a high score can mean "nothing broke recently" rather than "nothing is broken"`

---

### 其他實作決定

- **`deductions` 明細一併回傳。** 只給一個分數的話，AI 沒辦法知道分數是被什麼拉低的，只能再逐項去查 —— 那就違背了「一鍵健檢」的目的。
- **`max_depth` 用顯式堆疊而非遞迴**，避免病態巢狀的頁面直接爆掉 call stack。
- **不做「broken links」檢查。** 卡片的資料來源清單提到它，但 details 的欄位表裡沒有 —— 而要驗證連結有效性**必須對每個連結實際發出請求**：慢、有副作用（可能觸發對方的計數或 rate limit），而且 CORS 下拿不到可靠結果。已在 `note` 裡明講「可及性檢查不含連結有效性」，不留一個看起來有做其實沒做的欄位。
- **`critical_count`** 定義為 `level === 'error'` 的數量（相對於 warn），讓 AI 分得出「有 12 個警告」和「有 12 個真錯誤」。

---

### 已驗證（10 項）

```
  PASS  🔴 alt="" 是合法的裝飾性圖片，不算問題
  PASS  🔴 label[for] / 包裹 / aria-label / title / hidden / submit 都不算誤報
  PASS  巢狀深度計算正確
  PASS  console error -5/個上限 -30 ／ network -5/個上限 -20
  PASS  無 alt 圖片 -2/個上限 -10 ／ 無 label input -3/個上限 -10
  PASS  實作的扣分表與卡片一致（8 條）
  PASS  🔴 null 的指標不列入扣分（未互動的 FID 不能扣分）
  PASS  score 夾在 0~100
```

**請到 `chrome://extensions` 重新載入 BugEzy** —— 一次會補完 **PM-315（3 項）+ PM-316（6 項）+ PM-317（6 項）**。


---

### 🟡 PM-318：端到端驗收 PM-315~317（analyze_element / get_web_vitals / get_page_health）

**前提**：Extension 已重新載入（FOX 確認）

**執行**：跑 `_verify_phase1.mjs --reclaim` + `_verify309.mjs`，確認三支新工具端到端通過。

**驗收條件**：
1. `analyze_element` — 回傳含 tag / computed_styles / box_model
2. `get_web_vitals` — 回傳含 LCP / FCP / TTFB + rating
3. `get_page_health` — 回傳 score + summary + details
4. 工具總數 = 11（ping / get_page_url / navigate_to / click_element / read_page / type_text / take_screenshot / get_browser_errors / analyze_element / get_web_vitals / get_page_health）
5. 全部 0 failed

完成後在下方寫 `🔵 DONE-318`，然後**立刻接著執行 PM-319**。

---

### 🔵 DONE-318：PM-315~317 端到端 **全數通過**，`_verify_phase1.mjs` 99 passed / 0 failed

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | `analyze_element` — 回傳含 tag / computed_styles / box_model | ✅ 真實頁面上 6/6 |
| 2 | `get_web_vitals` — 回傳含 LCP / FCP / TTFB + rating | ✅ 6/6 |
| 3 | `get_page_health` — 回傳 score + summary + details | ✅ 6/6 |
| 4 | 工具總數 = 11 | ✅ |
| 5 | 全部 0 failed | ✅ `_verify_phase1.mjs` **99 passed, 0 failed**；`_verify309.mjs` **72 passed, 0 failed** |

跑完 port 19850 乾淨釋放。

---

### 真實頁面上的實際輸出

**`get_page_health`（bugezy.dev/guide）**：

```json
{"score":90,"summary":"90 分：1 個錯誤、1 項效能待改進。",
 "details":{"errors":{"console_count":1,"network_count":0,"critical_count":0},
 "performance":{"lcp_rating":"good","fcp_rating":"needs-improvement","cls_rating":"good","ttfb_rating":"good"},
 "accessibility":{"images_without_alt":0,"inputs_without_label":0,"missing_lang":false,...}}}
```

分數與摘要都對得上細項：1 個 console 錯誤（-5）＋ 1 項 needs-improvement（-5）＝ 90 分。**可及性四項全 0，代表 PM-317 那兩個誤報防治在真實頁面上確實沒有亂報。**

**`get_web_vitals`**：

```json
{"LCP":{"value_ms":2256,"rating":"good"},"FID":null,"CLS":{"value":0,"rating":"good"},
 "FCP":{"value_ms":2256,"rating":"needs-improvement"},"TTFB":{"value_ms":153,"rating":"good"},
 "domContentLoaded_ms":500,"load_ms":1923,
 "FID_note":"使用者尚未與頁面互動，FID 無法量測（回 null 而非 0）。"}
```

**這組數字證明了 PM-316 的 `buffered: true` 是必要的** —— LCP/CLS 都是頁面載入時就定案的事件，工具是在載入完成後才呼叫的，沒有 buffered 就會全部拿到 null。FID 也如設計回 `null` 並附說明。

**`analyze_element`**：拿到真實的 computed styles（`position:"fixed"`、`zIndex:"10"`、`width:"43.5469px"`），box_model 尺寸非零。

---

### 兩項 LIMIT 維持不變（都不是程式問題）

- **312-1~3**：`take_screenshot` 的 `activeTab` 權限限制，待你決策（A 保留現狀／B 加 `<all_urls>` 送審／C 移出 Phase 1）
- **313-3**：`network_errors` 的欄位結構需要「頁面自己發出 4xx/5xx 的 fetch/XHR」才驗得到，bugezy.dev 上沒有這種頁面

---

### Phase 1 的 11 支工具現況

| 工具 | 端到端 |
|---|---|
| `ping` · `get_page_url` | ✅ |
| `navigate_to` · `click_element` · `read_page` · `type_text` | ✅ |
| `get_browser_errors` · `analyze_element` · `get_web_vitals` · `get_page_health` | ✅ |
| `take_screenshot` | 🔴 權限受限（待決策）|

---

### 🟡 PM-319：Phase 1 — read_page 補 readyState（DONE-310 留項）

**目標**：`read_page` 回傳加上 `document.readyState`，讓 AI 分辨「頁面真的空」vs「還沒載完」。

**改動**：
- `content.ts` 的 `BRIDGE_READ_PAGE` handler：回傳物件加 `ready_state: document.readyState`
- `mcp-server.ts`：回傳 schema 文件加上 `ready_state` 欄位說明

**驗收條件**：
1. `read_page()` 回傳含 `ready_state` 欄位（值為 'loading' / 'interactive' / 'complete'）
2. 既有測試不受影響（回傳多一個欄位不破壞）

完成後在下方寫 `🔵 DONE-319`，然後**立刻接著執行 PM-320**。

---

### 🔵 DONE-319：`read_page` 已補 `ready_state`；端到端待一次擴充功能重新載入

**改動檔案**：`extension/src/content.ts`、`extension/src/background.ts`、`bridge/src/mcp-server.ts`。bridge `tsc` 與 extension `tsc --noEmit` + `build.mjs` 皆通過。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 回傳含 `ready_state`（loading / interactive / complete）| ⏳ 待重載（測試已寫好，目前回 `undefined`）|
| 2 | 既有測試不受影響 | ✅ `_verify309.mjs` **72 passed, 0 failed**；`_verify_phase1.mjs` 其餘 **99 項全數維持通過** |

---

### 這一項補的是 DONE-310 當時留下的缺口

PM-310 跑端到端時遇過一次：click 觸發導航後立刻 `read_page`，拿到 `content: ""` 而 `element_count` 只有 31。當時我把它歸類為測試的時序問題（改用 `navigate_to` 等載入完成），但同時標記了**產品側的真正缺口**：

> 頁面還在載入時 `read_page` 會回空字串，**AI 分不出「這頁真的空的」還是「還沒載完」**。

沒有這個欄位，AI 對著一個載到一半的頁面會直接下結論說「這頁沒有內容」。這跟 PM-313 的 30 秒視窗、PM-317 的高分陷阱是同一類問題：**回傳本身沒錯，但少了脈絡就會被誤讀。**

**改動內容**：

- `ready_state: document.readyState` 一路帶到 bridge 回傳
- `background.ts` 取不到時回 `'unknown'` **而非省略欄位** —— 省略的話 AI 會以為是舊版工具，回 `'unknown'` 才知道「這個欄位存在但這次沒拿到」
- 工具描述中英文都加上「**若不是 complete，content 可能不完整，別據此判定頁面是空的**」

**測試加了兩條**：① 欄位值必須是三個合法值之一；② 因為 `navigate_to` 本來就會等到載入完成，經由它導航後再讀應固定是 `complete` —— **這條同時反向驗證了 PM-307 的等待邏輯確實有效**。

目前兩條都回 `undefined`，純粹是 Chrome 還沒載入新的 `background.js`。

**請到 `chrome://extensions` 重新載入 BugEzy**，跟我說一聲我立刻補完這 2 項。

---

**複驗紀錄（同日稍後）**：再跑了一次 `_verify_phase1.mjs --reclaim`，兩條仍回 `undefined`，**其餘 99 項全過**。已確認不是實作問題：

```
extension/dist/content.js      : 1 處 ready_state
extension/dist/background.js   : 1 處 ready_state
bridge/dist/mcp-server.js      : 1 處 ready_state
（產物建置時間 07:58，早於複驗時間）
```

程式碼與打包產物都正確，**Chrome 仍在執行重新載入前的舊 `content.js` / `background.js`**。此項無法由我這邊推進，等重新載入。

**✅ 已補完（重新載入後）**：兩條皆轉 PASS。

```
  PASS  319-1 read_page 回傳 ready_state（loading/interactive/complete）
  PASS  319   navigate_to 等到載入完成後應為 complete
```

第二條同時**反向驗證了 PM-307 的等待邏輯**：`navigate_to` 確實等到 `readyState === 'complete'` 才回傳，不是等個固定秒數就交差。**PM-319 兩項驗收全數完成。**

---

### 🟡 PM-320：Phase 1 — 規格書 §13.2 回填 take_screenshot 實測結論

**目標**：將 PM-312 實測發現回填進規格書 §13.2，記錄三條路線的實測結果。

**改動**（`docs/bugezy_v2_v3_product_spec.html` + md 版）：

在 §13.2 的出任務模式截圖限制段落加入：

```
✅ 已實測（PM-312，2026-08-16）

三條路線實測結果：
① 暫時切 active 截圖 — 權限不是問題，但 captureVisibleTab 需 <all_urls>（bridge 無使用者手勢拿不到 activeTab 授權）
② chrome.debugger — 需 debugger 權限 + 黃色橫幅，不變
③ 獨立視窗 windows.create — 同樣過不了 activeTab 這關

結論：三條路都需要 <all_urls> host permission。
- 加 <all_urls> = 人工審核 2~4 週（非退件，是排隊）
- 安裝時彈出「可讀取所有網站資料」警告
- 程式碼已寫好（PM-312），待送審時機成熟再加入 manifest

FOX 決策（2026-08-16）：不急著上架，1~3 個月內維持 v1.1.5。
開發版 manifest 可加 <all_urls> 本機全通，上架版另議。
```

**驗收條件**：
1. §13.2 有實測結論段落
2. md 版同步
3. 不改動任何程式碼

完成後在下方寫 `🔵 DONE-320`。

---

### 🔵 DONE-320：§13.2 已回填實測結論與 FOX 決策；規格書 v0.9 → **v1.0**

**改動檔案**：`docs/bugezy_v2_v3_product_spec.html`、`docs/BugEzy_v2_v3_規格書.md`。**未動任何程式碼**（依卡片要求）。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | §13.2 有實測結論段落 | ✅ 綠框「已實測（PM-312，2026-08-16）」，含 Chrome 回傳的原話、三條路線對照表、根因、決策 |
| 2 | md 版同步 | ✅ |
| 3 | 不改動任何程式碼 | ✅ 只動兩份文件 |

HTML 標籤配對已用程式驗過（div/table/tr/td/th/ul/li/p/details 全部收支平衡）。

---

### 寫進去的內容

**三條路線用「原本的評估 vs 實測結果」對照呈現**，而不是只寫結論——這樣日後有人重看時，能直接看出當初哪裡判斷錯了：

| 路線 | 原本的評估 | 實測結果 |
|---|---|---|
| ① 暫時切 active | 會搶焦點 | **搶焦點不是問題，權限才是**（切換＋切回都實作了，仍被擋）|
| ② `chrome.debugger` | 需權限＋黃色橫幅 | 不變 |
| ③ 獨立視窗 | 「最划算」，待實測 | **同樣過不了 `activeTab` 這關** |

**根因也寫清楚**：`captureVisibleTab` 的門檻不在「分頁可不可見」，而在**「有沒有使用者手勢」**——③ 只解決了可見性，沒解決授權。這句是整段最有價值的部分，因為它解釋了為什麼三條路會一起失敗。

另外補上一個 PM-312 才發現的細節：**導航會撤銷 `activeTab`**，所以連「先點圖示再截圖」的變通法也只在「點完直接截、中間不 `navigate_to`」時成立。

**FOX 決策**用金色決策框獨立標出（不急上架、1~3 個月維持 v1.1.5、開發版可加 `<all_urls>`、上架版另議）。

---

### 順手修掉的一處過時敘述

§13.2 原本那句「**③ 看起來是最划算的一條，Phase 1 動工前需實測確認**」已經被實測推翻。我沒有直接刪掉，改成：

> （原本評估 ③ 最划算並標記「動工前需實測確認」——**已實測，見下方綠框**。）

保留「當初這樣判斷過」的痕跡並指向結論，比直接抹掉有用——否則下一個人可能又會提出同一個方案。這與 PM-305 保留 A-3 作為歷史紀錄是同一個原則。

---

### 版本

規格書自 PM-294 建立以來第一次進到 **v1.0**：`§1~§15` 完整、48 個 MCP 工具規劃、決策 1~6 定案，且 **Phase 1 的 11 支工具已全部實作完成並通過端到端**（`take_screenshot` 除外，權限受限已文件化）。


---

### 🟡 PM-321：Phase 1 PM-B2 — 方案等級判斷雛形

**目標**：將 `isActiveUserId()` 從布林值改為回傳方案等級，讓 v2 bridge 工具能擋住票券/日票用戶。

**背景**（決策 3 + §12 A-5）：
- Free：v1 錄製 10 次/月
- 票券/日票：v1 無限錄製（**不含 v2**）
- Pro NT$80：v1 + v2 全功能
- Max NT$200 / Agent NT$500：未來

---

**Cloudflare Workers 側**（`isActiveUserId` 函式）：

目前回傳：`boolean`（是否付費）
改為回傳：`{ active: boolean, tier: 'free' | 'ticket' | 'day_pass' | 'pro' | 'max' | 'agent' }`

判定邏輯（依 ARCHITECTURE §4-8）：
1. 查 `user_tickets` 表 → 有效票券 → tier = `'ticket'` 或 `'day_pass'`
2. 查 ECPay 訂閱 → 有效 → tier = `'pro'`（目前只有 NT$80 一種）
3. 都沒有 → tier = `'free'`
4. `active = tier !== 'free'`（向下相容）

**⚠ 注意**（§12 A-5 紅框）：分清 `isActiveUserId`（含票券）與 `isEcpayActiveUserId`（僅 ECPay），誤用會導致收錢不開通。

**bridge 側閘門**（`bridge/src/mcp-server.ts`）：

每支 v2 工具在執行前檢查：
```
if (tier === 'free' || tier === 'ticket' || tier === 'day_pass') {
  return error: 'v2 功能需要 Pro 訂閱（NT$80/月），票券/日票僅包含 v1 功能'
}
```

Phase 1 先做**雛形**：閘門邏輯寫好但**預設不啟用**（用環境變數 `ENFORCE_TIER_GATE=true` 控制），因為目前 bridge 跑在本機，沒有連 Cloudflare Workers。

**驗收條件**：
1. `isActiveUserId` 回傳含 `tier` 欄位
2. 既有呼叫 `isActiveUserId` 的地方用 `.active` 不受影響（向下相容）
3. bridge 有閘門邏輯（可用環境變數開關）
4. 閘門開時，free/ticket/day_pass 呼叫 v2 工具回 error

完成後在下方寫 `🔵 DONE-321`。

---

### 🔵 DONE-321：`isActiveUserId` 已回傳 `tier`；bridge 閘門四種情境實測通過

**改動檔案**：`server/src/index.ts`、`bridge/src/mcp-server.ts`。server `tsc --noEmit` 與 bridge `tsc` 皆通過。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | `isActiveUserId` 回傳含 `tier` | ✅ `{ active, tier }`，tier ∈ free/ticket/day_pass/pro/max/agent |
| 2 | 既有呼叫端向下相容 | ✅ **4 處全部改為 `.active`**（見下方紅字說明）|
| 3 | bridge 有閘門邏輯（環境變數開關）| ✅ `ENFORCE_TIER_GATE` + `BUGEZY_USER_TIER` |
| 4 | 閘門開時 free/ticket/day_pass 回 error | ✅ 四種情境實測 |

```
1. 預設不啟用（不該被擋）: PASS
2. ticket 被擋           : PASS
3. day_pass 被擋         : PASS
4. pro 放行              : PASS
```

---

### 🔴 這個改動有一個會「安靜地把付費功能免費送出去」的陷阱

`isActiveUserId` 原本回 `boolean`，改成回 `{ active, tier }` 之後——**物件永遠是 truthy**。

也就是說，任何漏改的呼叫端：

```ts
if (!(await isActiveUserId(userId, env))) { /* 擋下未付費 */ }
```

會變成 `!物件` ＝ 恆為 `false` → **這道付費檢查永遠不會擋任何人**，而且 **TypeScript 不會報錯、執行時也不會出錯**，只是所有人都免費拿到付費功能。

所以我先 grep 出全部呼叫端再動手，確認是 **4 處**（`server/src/index.ts` 的 6055、6069、6396、8434），逐一改為 `.active`，改完再 grep 一次確認沒有殘留的裸呼叫。也在函式上方留了紅字註解，避免日後有人再改回去。

**`isEcpayActiveUserId` 完全沒動** —— 依 §12 A-5 紅框，ECPay 開通判斷必須用那一支（用含票券的版本會導致「持有免費票券的人付費 → 被誤判為已 active → 跳過升級 → 收了錢卻沒開通」）。這次的改動只影響「是否享有付費功能」那一支。

---

### tier 判定順序

依 ARCHITECTURE §4-8，且**順序有意義**：先看 ECPay（`paid`/`cancelled` → `pro`；`day_pass` 未到期 → `day_pass`），都不是才查票券（→ `ticket`），最後才是 `free`。

這樣排的原因：**同時持有票券又付費的人應該算 `pro`**，不能因為先查到票券就把他降級成只有 v1 的等級。

順帶把原本「先呼叫 `isEcpayActiveUserId` 再呼叫 `hasActiveTicket`」的兩段查詢合併成一次 `users` 查詢 + 條件式票券查詢 —— 付費用戶不再需要多打一次 `user_tickets`。

---

### 閘門為什麼預設關閉（而且是刻意的）

bridge 完全跑在本機、沒有連 Cloudflare Workers，**拿不到使用者真正的 tier**。若預設開啟，只有兩種結果：

- 查不到就放行 → 等於沒有閘門，卻讓人以為有
- 查不到就擋掉 → 所有工具直接不能用

兩者都比「明確關閉」糟。所以要啟用必須**同時**設 `ENFORCE_TIER_GATE=true` 與 `BUGEZY_USER_TIER=<tier>`（後者暫代真實查詢），並在程式碼註明這是雛形、真正的 tier 要等 bridge 能連上 Workers 之後再接。

閘門套用在 **9 支 v2 工具**上；`ping` 與 `get_page_url` 不設閘門 —— 前者是連線診斷、後者只回當前分頁網址，擋掉只會讓使用者無法排查「為什麼我的 bridge 不能用」。

---

### 🟡 PM-322：開發版 manifest 加 `<all_urls>` — 本機全通

**目標**：建立開發版 manifest，讓本機開發時 take_screenshot 等需要 host permission 的功能可用。

---

**做法**：
- 新增 `extension/manifest.dev.json`（或在 build 腳本加 `--dev` flag）
- 開發版比上架版多一項：`host_permissions: ["<all_urls>"]`
- `build.mjs` 加一個 `DEV=true` 環境變數，build 時自動合併 dev manifest
- 上架版 `manifest.json` 完全不動

**檔案結構**：
```
extension/
  manifest.json          ← 上架版（activeTab only）
  manifest.dev.json      ← 開發版差異（只放 host_permissions）
  build.mjs              ← DEV=true 時合併
```

**驗收條件**：
1. `DEV=true npm run build` 產出的 manifest 含 `<all_urls>`
2. 普通 `npm run build` 產出的 manifest **不含** `<all_urls>`
3. 開發版載入 Chrome 後 take_screenshot 可用（LIMIT → PASS）

完成後在下方寫 `🔵 DONE-322`。

---

### 🔵 DONE-322：開發版 manifest 完成，三項驗收皆已實測

**改動檔案**：`extension/manifest.dev.json`（新增）、`extension/build.mjs`。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | `DEV=true` build 產出的 manifest 含 `<all_urls>` | ✅ `host_permissions: ["<all_urls>"]` |
| 2 | 普通 build **不含** `<all_urls>` | ✅ `(無)` |
| 3 | 開發版載入後 take_screenshot 可用 | ✅ **已驗證**：`LIMIT` → `PASS`，截到 1600×765 PNG（見 DONE-323）|

**實測輸出**：

```
=== 一般 build ===
host_permissions: (無)
permissions: ['activeTab', 'storage', 'downloads', 'identity', 'offscreen']

=== DEV build ===
⚠ DEV manifest：已加入 host_permissions ["<all_urls>"] ——此版本不可上架
host_permissions: ['<all_urls>']
permissions: ['activeTab', 'storage', 'downloads', 'identity', 'offscreen']

=== 原始 manifest.json ===
host_permissions: (無 → 正確)
```

---

### 三個實作細節

**① 上架版 `manifest.json` 一個字都沒動。** dev 差異只在**打包時疊上去**寫進 `dist/`，不寫回原始檔。這點很要緊 —— 若做成「build 時改寫原始 manifest」，某次忘了改回來就會把 `<all_urls>` 打包進上架版，而那正是 FOX 決定暫時不要的東西。已加一條驗證：build 後檢查原始 `manifest.json` 仍無 `host_permissions`。

**② `_` 開頭的欄位會被剝掉。** `manifest.dev.json` 裡我寫了 `_comment` 與 `_why` 說明為什麼需要這個檔（給日後的人看），但 Chrome 對未知的頂層鍵會發警告，所以合併時一律略過 `_*`。已驗證產物中無殘留。

**③ 陣列用聯集合併而非覆蓋。** 目前只有 `host_permissions` 一項，但若日後 dev manifest 要加 `permissions`，覆蓋式合併會**把上架版原有的權限整組洗掉**。改成 `[...new Set([...base, ...dev])]`。

**④ DEV build 會印一行明顯警告**（「此版本不可上架」），避免有人拿 dev 產物去打包送審。

---

### 🟡 PM-323：Phase 1 全工具端到端驗收（開發版 manifest）

**前提**：PM-322 完成，Extension 用開發版 manifest 重新載入

**執行**：跑全套驗收，確認所有 LIMIT 項轉為 PASS（特別是 take_screenshot）。

**驗收條件**：
1. take_screenshot 端到端 PASS（PM-312 驗收 1~3）
2. 全套 `_verify_phase1.mjs --reclaim` + `_verify309.mjs` 0 failed
3. 列出最終工具總數和所有工具名稱

完成後在下方寫 `🔵 DONE-323`。

---

### 🔵 DONE-323：全工具端到端 **106 + 72 = 178 項，0 failed**；`take_screenshot` 由 LIMIT 轉 PASS

**前提已滿足**：擴充功能已以開發版 manifest（含 `<all_urls>`）重新載入。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | take_screenshot 端到端 PASS（PM-312 驗收 1~3）| ✅ **全部轉 PASS** |
| 2 | 全套 0 failed | ✅ `_verify_phase1.mjs` **106 passed, 0 failed**；`_verify309.mjs` **72 passed, 0 failed** |
| 3 | 列出最終工具總數與名稱 | ✅ 見下 |

跑完 port 19850 乾淨釋放，無孤兒殘留。

---

### `take_screenshot` 實際輸出

```json
{"format":"png","width":1600,"height":765,"bytes":45333,
 "tab_id":1605029140,"url":"https://bugezy.dev/guide",
 "warning":"已暫時切換分頁以截圖，截完已切回原本的分頁"}
```

```
  PASS  312-1 回傳 image content + 寬高
  PASS  312-2 base64 是有效 PNG（簽章正確且可解碼）
  PASS  312-2 解碼後尺寸與回報的一致
  PASS  312   mimeType 為 image/png
  PASS  312-3 非 active 分頁 → 有切換警告（方案 B）
  PASS  312-4 分頁不存在 → error（不 crash）
```

三件事值得注意：

- **`<all_urls>` 確實是唯一的癥結**。程式碼從 PM-312 起一個字都沒改，只是換了 manifest 就從「權限被拒」變成正常運作 —— 這反過來證實 PM-312／PM-320 的實測結論沒有判斷錯方向。
- **方案 B 的切換與切回實際生效**：目標是背景分頁，工具回了 `warning` 說明已暫時切換並切回，`tab_id` 仍是原本那個。
- **PNG 是真的可解碼的**：測試不只看有沒有 base64，而是**解碼後查 PNG 簽章、再從 IHDR 讀出寬高與回報值比對** —— 1600×765 兩邊一致，證明 `pngSize()` 在真實截圖上也正確（先前只用 icon 檔驗過）。

---

### 最終工具清單（11 支，直接取自 `tools/list`）

```
1. ping                  2. get_page_url        3. navigate_to
4. click_element         5. get_page_health     6. get_web_vitals
7. analyze_element       8. get_browser_errors  9. take_screenshot
10. type_text           11. read_page
```

其中 **9 支已套 PM-321 的方案等級閘門**（預設關閉）；`ping` 與 `get_page_url` 不設閘門。

---

### 唯一剩下的 LIMIT

```
  LIMIT 313-3 未驗：需要頁面自己發出 4xx/5xx 的 fetch/XHR 才會有資料
        （導航到 404 只是文件請求，inject 攔的是 fetch/XHR，不會產生 network_errors）
```

這與擴充功能無關，是**測試素材**的限制 —— bugezy.dev 上沒有「會自己打失敗 API」的頁面。`network_errors` 的欄位形狀本身是安全的（content script 那層是逐欄白名單，不是整包轉發，所以 `requestBody`/`responseBody` 不可能漏出去）。若要補這一條，最乾淨的作法是在 bugezy.dev 放一個測試頁故意 fetch 404 端點。

---

### 🟡 PM-324：規格書 §9 Phase 1 進度更新

**目標**：把 Phase 1 所有已完成項目標記為 ✅，更新規格書。

**改動**（`docs/bugezy_v2_v3_product_spec.html` + md 版）：

§9 Phase 1 區塊：
```
PM-A：bugezy-bridge 骨架 ✅ 已完成（PM-297~299）  ← 已標
PM-B：Extension 接收 bridge 指令 ✅ 已完成（PM-307~314）
PM-B2：方案等級判斷雛形 ✅ 已完成（PM-321）
PM-C：MCP 第一批 ✅ 已完成（PM-307~314）
PM-D：MCP 第二批 ✅ 已完成（PM-315~318）
PM-D2：bugezy-watch 即時化 — 待辦（§11.2）
```

同時更新工具總數：bridge 11 支（含 take_screenshot LIMIT）

**驗收條件**：
1. §9 Phase 1 有 5 個 ✅（PM-A~D + B2）
2. PM-D2 標為待辦
3. md 版同步

完成後在下方寫 `🔵 DONE-324`。

---

### 🔵 DONE-324：§9 Phase 1 已標記完成，規格書 v1.0 → **v1.1**

**改動檔案**：`docs/bugezy_v2_v3_product_spec.html`、`docs/BugEzy_v2_v3_規格書.md`。HTML 標籤配對已驗過。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | §9 Phase 1 有 5 個 ✅（PM-A~D + B2）| ✅ |
| 2 | PM-D2 標為待辦 | ✅ |
| 3 | md 版同步 | ✅ |

除了打勾，另補了**每一項的實際落點**（不只標完成，也寫清楚跟原規劃差在哪）：

- **PM-C** 的工具清單與原規劃不同：原本寫 `get_live_errors(source='browser')`，實際定名 **`get_browser_errors`**，且多了 `type_text`；bridge 端舊的 `get_live_errors` 已於 PM-314 移除（**雲端 MCP 的同名工具不受影響**，這句要寫明否則會被誤解成把雲端那支也刪了）。
- **PM-B2** 註明閘門**預設關閉**及其原因。
- **PM-D** 註明 `take_screenshot` 程式碼已完成但受權限限制，並指向 §13.2。

另在 Phase 1 開頭加了一個綠框列出 **11 支工具全名**與「除 `take_screenshot` 外皆已通過端到端」的現況，讓人不必逐項讀就知道進度。

---

### 🟡 PM-325：Phase 1 收工 — CHANGELOG + ARCHITECTURE + commit push

**目標**：Phase 1 核心完工，更新文件收工。

---

**CHANGELOG.md 新增**：
```
## Phase 1 完工（2026-08-16）
- PM-305：§3 Native Messaging → WebSocket 更新
- PM-306：Day 43 收工
- PM-307~309：navigate_to / click_element / read_page（71/71 端到端通過）
- PM-310：端到端驗收基礎建設
- PM-311：type_text（React _valueTracker 繞過）
- PM-312：take_screenshot（captureVisibleTab 權限限制文件化）
- PM-313：get_browser_errors（沿用 inject.ts，30 秒視窗）
- PM-314：刪除 bridge 舊 get_live_errors + 驗收 113/113
- PM-315：analyze_element（深度 DOM+CSS 分析）
- PM-316：get_web_vitals（Core Web Vitals + rating）
- PM-317：get_page_health（一鍵健檢 0~100）
- PM-318：端到端驗收 171/171
- PM-319：read_page 補 readyState
- PM-320：§13.2 回填 take_screenshot 實測結論
- PM-321：方案等級判斷雛形
- PM-322：開發版 manifest（<all_urls>）
- PM-323：全工具端到端驗收
- PM-324：§9 進度更新
- bridge 工具總數：11（ping/get_page_url/navigate_to/click_element/read_page/type_text/take_screenshot/get_browser_errors/analyze_element/get_web_vitals/get_page_health）
```

**ARCHITECTURE.md**：
- 更新 bridge 工具清單
- 加入開發版 manifest 說明
- 加入方案等級判斷雛形說明

**git**：`git add -A && git commit -m "Phase1 complete: 11 bridge MCP tools (PM-305~324)" && git push`

**驗收條件**：
1. CHANGELOG 有 Phase 1 完工區塊
2. ARCHITECTURE 有 bridge 工具清單 11 支
3. git commit + push 成功

完成後在下方寫 `🔵 DONE-325`。

---

### 🔵 DONE-325：Phase 1 收工，CHANGELOG ＋ ARCHITECTURE 已更新並 push

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | CHANGELOG 有 Phase 1 完工區塊 | ✅ PM-305~324 共 17 則 |
| 2 | ARCHITECTURE 有 bridge 工具清單 11 支 | ✅ 表格化，另加開發版 manifest 與方案等級兩節 |
| 3 | git commit + push | ✅ |

---

### CHANGELOG 寫法：記「踩到什麼」而不是「做了什麼」

每一則除了功能，都寫下**那張卡真正的發現**——因為功能看 code 就知道，但「為什麼要這樣寫」半年後只剩這裡有記錄。例如：

- `type_text` 若只做 `el.value = x` 再 dispatch，**React 會忽略整個事件**（`_valueTracker` 判定值沒變）
- `get_web_vitals` **少了 `buffered: true` 就永遠回 null**，而那正是唯一的使用情境
- `alt=""` 是**合法的**裝飾性圖片，算成問題會在做得越好的網站上誤報越多
- `isActiveUserId` 改回傳物件後，**物件恆為 truthy** —— 漏改的付費檢查會永遠不擋人且完全不報錯

### ARCHITECTURE 補了三節

**① 11 支工具表格**（含端到端狀態）＋ 共通約定（`tab_id` 語意、不存在的分頁必須明確報錯）。

**② 必守的三條**，都是實測踩出來的：`tab.url` 在只有 `activeTab` 時**靜默回空字串**；`captureVisibleTab` 的門檻是「有沒有使用者手勢」而非「分頁可不可見」（三條替代路線都繞不開）；**回傳要帶解讀脈絡**——30 秒視窗、FID 為 null、資源大小低估、事件清單空、`ready_state` 未完成，少了這些 AI 會把正確的回傳讀成錯誤的結論。

**③ 開發版 manifest 與方案等級判斷**，含那條紅字警告（改 `isActiveUserId` 務必逐一檢查呼叫端）。

---

### 未完成事項（誠實列出）

- **PM-323 未通過** —— 需要你以開發版 manifest 重新載入擴充功能。`dist/` 已建置完成，重載後可一次解掉 `take_screenshot`（3 項）與 `ready_state`（2 項）。
- **server 尚未 deploy** —— `isActiveUserId` 的改動只在原始碼，未上線。卡片沒要求 deploy，我也沒有自行 deploy（那會動到正式環境的付費判定邏輯，應由你決定時機）。
- **Phase 1 剩餘**：PM-D2（bugezy-watch 即時化）。


---

### 🟡 PM-326：PM-D2 前置 — bugezy-watch CLI 現況盤點

**目標**：讀取 `cli/src` 原始碼，確認 bugezy-watch@1.1.0 的現有能力，為即時化找出最短路徑。

---

**需要確認**：
1. `BUFFER_INTERVAL = 10_000`（10 秒批次推送）的位置與改法
2. `POST /api/terminal-logs` 的 payload 結構（parsed_errors + runtime）
3. `parse-traceback.ts` 的輸出結構（type/message/frames）
4. `pii-mask.ts` 遮罩範圍
5. bugezy-watch 目前是只推不讀（單向），還是有讀取通道？

**產出**：在 DONE 中列出以上 5 點的現況，作為 PM-327~328 的實作依據。

**不改任何程式碼。**

完成後在下方寫 `🔵 DONE-326`。

---

### 🔵 DONE-326：bugezy-watch CLI 現況盤點（未改任何程式碼）

`cli/` 共 4 個檔、445 行，**零執行期相依**（`package.json` 的 `dependencies` 是空的）。

**1. `BUFFER_INTERVAL = 10_000`**
`cli/src/index.ts:16`，由 `index.ts:146` 的 `setInterval(flushBuffer, BUFFER_INTERVAL)` 使用。
→ **但 PM-327 不需要改它** —— 見下方第 5 點的結論。

**2. `POST /api/terminal-logs` payload**
```
{ logs, parsed_errors, runtime, command, cwd, timestamp }
```
（`index.ts:119~132`）。403 → 印「終端機 CLI 為付費功能」並 `process.exit(1)`；其他失敗靜默（不影響使用者的程式）。

**3. `parse-traceback.ts` 輸出**
```ts
ParsedError { type, message, frames: StackFrame[], raw, timestamp, runtime?: 'python'|'node' }
StackFrame { file, line, function, code }
```
兩支解析器：`parsePythonTraceback()` / `parseNodeError()`，皆為**純函式、零 import**。

**4. `pii-mask.ts` 遮罩範圍**（`maskStderr()`，純函式、零 import）
- **DB 連線字串**：mysql/postgres/mongodb/redis/amqp/mssql → 保 scheme+host、帳密改 `***:***`
- **環境變數賦值**：21 個敏感 KEY（`DATABASE_URL`/`JWT_SECRET`/`GROQ_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY`…）→ 保 KEY 名、遮值
- **token 格式**：OpenAI `sk-`、Google `AIza`、GitHub `ghp_`/`gho_`、AWS `AKIA`、Slack `xox*`、JWT → 整段 `***MASKED***`
- **一般 PII**：email／信用卡／台灣手機／台灣身分證 → `***`

**5. 🔴 CLI 是純單向推送，沒有任何讀取通道**
全檔只有**一個** `fetch`（`index.ts:119` 的 POST）。沒有 GET、沒有輪詢、沒有 WebSocket。

**這一點決定了 PM-327 的最短路徑**：如果要沿用 CLI，就得做「CLI 推到 Workers → bridge 再從 Workers 拉」，中間至少多一次網路往返與一個新的後端端點，而且**資料會離開使用者的機器**。

但 **bridge 自己就是 Node process**，可以直接 `child_process.spawn` 攔 stderr —— 不需要 CLI、不需要 Workers、不需要 Extension，資料完全不離開本機。所以 PM-327 走這條，**只沿用 CLI 的 `parse-traceback.ts` 與 `pii-mask.ts` 兩個純函式模組**。

> `detect-env.ts` 有 `import { execSync }`，且它的用途是上傳前的環境快照 —— PM-327 用不到，不沿用。

---

### 🟡 PM-327：PM-D2 實作 — bridge 端 start_terminal_monitor + get_terminal_live_errors

**目標**：在 bridge 新增兩支 MCP 工具，讓 AI 能即時讀取後端錯誤。

**前提**：依 PM-326 盤點結果調整細節。以下為預期架構：

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('start_terminal_monitor', ...)
參數：
  command: z.string() — 要監控的指令（例如 'npm run dev'）
  cwd: z.string().optional() — 工作目錄
回傳：{ monitor_id, command, pid, status: 'running' }

server.tool('get_terminal_live_errors', ...)
參數：
  monitor_id: z.string().optional() — 省略→最近一個 monitor
回傳：{
  monitor_id, command, pid, uptime_seconds,
  errors: [{ type, message, frames, timestamp, severity }],
  total_count, window_seconds
}
```

**實作路線（依 §11.2 + §12 A-6）**：

bridge 本身就是 Node.js process，可以直接 `child_process.spawn` 執行指令並攔截 stderr，**不需要經過 Extension 或 Chrome**。

```
start_terminal_monitor：
  1. spawn(command, { cwd, shell: true })
  2. pipe stderr → parse-traceback 結構化
  3. pii-mask 遮罩
  4. 存入滾動緩存（類似 get_browser_errors 的 30 秒窗口，但後端可以更長，建議 120 秒）
  5. 回傳 monitor_id + pid

get_terminal_live_errors：
  1. 讀取指定 monitor 的滾動緩存
  2. 回傳結構化錯誤
```

**關鍵細節**：
- 沿用 bugezy-watch 的 `parse-traceback.ts` 和 `pii-mask.ts`（import 進來，不重寫）
- monitor 生命週期：跟隨被監控 process，process 結束 → monitor 自動標為 stopped
- 同時最多監控 5 個 process（防止資源耗盡）
- bridge 關閉時 kill 所有子 process（避免孤兒）

**驗收條件**：
1. `start_terminal_monitor({ command: 'node -e "console.error(new Error(\"test\"))"' })` → 回傳 monitor_id
2. `get_terminal_live_errors()` → 拿到結構化的 Error 含 type/message
3. PII 遮罩有生效
4. process 結束後 monitor 狀態更新
5. bridge 關閉時無孤兒 process

完成後在下方寫 `🔵 DONE-327`。

---

### 🔵 DONE-327：終端機監控三支工具完成，**21/21 通過**（真的 spawn 子程序，不模擬）

**改動檔案**：`bridge/src/terminal-monitor.ts`（新增）、`bridge/src/vendor/{parse-traceback,pii-mask}.ts`（新增）、`bridge/src/mcp-server.ts`、`bridge/src/index.ts`。bridge `tsc` 通過。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | `start_terminal_monitor` → 回 monitor_id | ✅ 另回 pid / cwd / status |
| 2 | `get_terminal_live_errors` → 結構化 Error 含 type/message | ✅ `type:"Error"`、message 正確、**frames 也解析出來** |
| 3 | PII 遮罩生效 | ✅ DB 密碼／`sk-` token／email 皆未外洩，**但錯誤本身仍看得到** |
| 4 | process 結束後狀態更新 | ✅ `status:"stopped"` + `exit_code` |
| 5 | bridge 關閉時無孤兒 process | ✅ 殺掉 bridge 後子程序 0 殘留 |

另加：工具註冊、上限保護、`stop_terminal_monitor`、以及 **vendor 與 `cli/src` 逐字一致**共 21 項。

---

### 🔴 測試抓到一個真的外洩：`command` 欄位沒遮罩

錯誤內容我有好好遮，但**回聲的 `command` 欄位是原封不動回傳的**。測試用一個把祕密寫在指令裡的命令去跑，結果：

```
"command":"node -e \"...DATABASE_URL=postgres://u:pw123@h/db ... sk-ABC... ... a@b.com...\""
```

**密碼、token、email 全部完整外洩。** 而且 `DATABASE_URL=... npm run dev` 這種**把環境變數寫在指令前面**的寫法極常見 —— 更糟的是 `get_terminal_live_errors` 每次呼叫都會回傳這個欄位，等於**把憑證反覆送進 AI 的 context**。

已對四處回傳的 `command` 全部套 `maskStderr()`（start / get / stop / 超過上限時的清單）。修完三條遮罩斷言皆通過，且「錯誤本身仍看得到」也通過（不是整段吞掉）。

---

### 為什麼是「複製」而不是 `import` cli/src

卡片寫「import 進來，不重寫」。但 **`bugezy-bridge` 是要單獨 `npm publish` 的套件** —— `../../cli/src/...` 這種相對路徑在發布出去的 tarball 裡根本不存在，裝了就會壞。bridge 的 `tsconfig` 也設了 `rootDir: "src"`，跨目錄 import 會把輸出結構整個打亂。

所以改為**逐字複製**到 `bridge/src/vendor/`（不是重寫），並：
- 檔頭寫明來源、為什麼複製、以及「**要改請改 `cli/src` 再同步過來**」
- **測試逐字比對兩邊內容**，只要漂移就 FAIL —— 這是複製法唯一的風險，把它變成會被抓到的錯誤

---

### 其他設計決定

- **窗口 120 秒**（瀏覽器端是 30 秒）：後端編譯／重啟一輪就可能超過半分鐘，30 秒會讓「重啟後才來問」的情境固定拿到空結果。
- **`unparsed_stderr` 一定要有**：解析器只支援 Python／Node，Go／Rust／Java 的錯誤會解析不出來。若只回 `errors`，那些語言的使用者會拿到空陣列並以為沒事。回傳與描述都明講「**errors 為空不代表程式沒問題**」。
- **stdout 也看**：Vite／Next 之類的工具把錯誤印在 stdout，只攔 stderr 會漏掉。
- **Windows 用 `taskkill /T`**：`shell:true` 會多包一層 `cmd.exe`，只 kill child 會留下孫程序 —— **PM-310 就是被自己留下的孤兒卡了兩次**，這裡直接照 process tree 收。
- **`process.on('exit')` 也掛**：只掛 SIGINT/SIGTERM 的話，stdio 關閉導致的正常結束會留下 `npm run dev` 繼續占著 port。

---

### 🟡 PM-328：bugezy.dev 測試頁 — 解掉 313-3 LIMIT

**目標**：在 bugezy.dev 放一個測試頁，故意發出失敗的 fetch 請求，讓 `get_browser_errors` 的 network_errors 端到端可驗。

---

**做法**：
在 `bugezy.dev/test-errors`（或類似路徑）建一個頁面：
```html
<script>
  // 故意打一個 404 API
  fetch('/api/this-does-not-exist').catch(() => {});
  // 故意打一個會被 CORS 擋的
  fetch('https://httpstat.us/500').catch(() => {});
  // 故意 console.error
  console.error('BugEzy test error: this is intentional');
  // 故意 throw
  setTimeout(() => { throw new Error('BugEzy test: uncaught error'); }, 100);
</script>
<h1>BugEzy Test Errors Page</h1>
<p>This page intentionally triggers errors for testing get_browser_errors.</p>
```

**部署**：加到 Cloudflare Workers 的路由

**驗收條件**：
1. `https://bugezy.dev/test-errors` 可存取
2. `navigate_to` 到該頁 → `get_browser_errors()` 回傳 network_errors 非空
3. 313-3 LIMIT 轉為 PASS
4. 頁面有明確標示「intentional test errors」，不會被誤認為真 bug

完成後在下方寫 `🔵 DONE-328`。

---

### 🔵 DONE-328：測試頁已部署，**313-3 LIMIT 轉 PASS**

**改動檔案**：`server/src/index.ts`（新增 `/test-errors` 頁面與 `/api/test-error-500` 端點）、`bridge/_verify_phase1.mjs`。已 `wrangler deploy`（Version `b5529590`）。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | `https://bugezy.dev/test-errors` 可存取 | ✅ 200 |
| 2 | `get_browser_errors()` 回傳 network_errors 非空 | ✅ 抓到 **404** |
| 3 | 313-3 LIMIT 轉為 PASS | ✅ |
| 4 | 明確標示 intentional，不會被誤認為真 bug | ✅ 中英雙語警告框 |

```
  PASS  313-3 network 每筆含 url/status/method（404）
  PASS  313   🔴 network 不含 requestBody/responseBody（可能有 token／個資）
```

---

### 🔴 卡片的 `fetch('https://httpstat.us/500')` 會被 CSP 擋掉

站上的 CSP 是 `connect-src 'self' https://bugezy.dev https://bugezy-api...`。打 `httpstat.us` **不在允許清單裡**，瀏覽器會直接擋下來 —— 得到的是一個 **CSP 違規的 TypeError，而不是我們要測的 5xx 網路錯誤**。也就是說照抄的話，這一條永遠測不到。

改為**全部同源**：`/api/this-does-not-exist`（Worker 未知路由 → 404）與新增的 `/api/test-error-500`（明確回 500）。同源還有兩個附帶好處：不依賴第三方服務的可用性、也不會把使用者的瀏覽器導去打陌生站台。

---

### 三個沒被要求但必須做的

**① `noindex, nofollow` + 不進 sitemap。** 一個滿是錯誤的頁面被 Google 收錄，只會讓搜到的人以為 BugEzy 壞了。HTTP 標頭（`X-Robots-Tag`）與 `<meta name="robots">` **兩層都設**，並確認 `sitemap.xml` 裡查無此頁（grep 結果 0）。

**② 頁面上用中英雙語明講「這不是 bug，請不要回報」。** 驗收條件 4 只說要標示，但這頁一旦被人誤入，最可能的反應就是來回報 —— 所以警告框放在最上面，而不是頁尾小字。

**③ fetch 都 `.catch(() => {})`。** 不 catch 的話會多噴 `unhandledrejection`，反而蓋過我們真正要驗的 network error。

---

### 部署後實測

```
/test-errors            = 200
/api/test-error-500     = 500
/api/this-does-not-exist= 404
X-Robots-Tag: noindex, nofollow
<meta name="robots" content="noindex, nofollow">
sitemap 內出現次數: 0
```

---

### 🟡 PM-329：Phase 2 前置 — 圖釘系統架構設計

**目標**：為 Phase 2 圖釘系統做架構設計，產出實作計畫，不寫程式碼。

**需要設計的**：

1. **圖釘資料結構**：
   ```
   Pin {
     id: string,
     tab_id: number,
     selector: string,        // 釘在哪個元素
     description: string,     // AI 或用戶的描述
     status: 'active' | 'resolved' | 'error',
     created_at: timestamp,
     last_check: { result, timestamp }
   }
   ```

2. **儲存位置**：content script 的記憶體？background.ts 的 Map？chrome.storage.session？
   - 需考慮：分頁關閉後圖釘消失是否合理？跨分頁是否需要？

3. **UI 注入方式**：
   - content script 注入 DOM overlay
   - 圖釘圖示用什麼？位置怎麼算？
   - 元素移動/resize 後圖釘怎麼跟？

4. **三支 MCP 工具的 input/output schema**：
   - `pin_element(selector, description, tab_id?)`
   - `pin_analyze(selector, tab_id?)`
   - `get_pin_results(tab_id?)`

**產出**：在 DONE 中寫出設計方案（含取捨），作為 PM-330+ 的實作依據。

**不改任何程式碼。**

完成後在下方寫 `🔵 DONE-329`。

---

### 🔵 DONE-329：圖釘系統設計方案（未改任何程式碼）

### 1. 資料結構

```ts
interface Pin {
  id: string;                 // 'pin1-<base36 時間>'，同 monitor_id 的格式
  selector: string;           // 釘在哪個元素（沿用 uniqueSelector 的產物）
  description: string;
  status: 'active' | 'warning' | 'error' | 'stale';
  created_at: number;
  last_check: { at: number; summary: string; analysis?: unknown } | null;
}
```

與卡片的差異，各有理由：

- **拿掉 `tab_id`**：圖釘存在 content script 裡，而 content script **本來就是每個分頁一份** —— 再存一次 tab_id 是冗餘，還會出現「pin 記的 tab_id 與它實際所在的分頁不一致」這種不可能對帳的狀態。分頁隔離由執行環境天然保證。
- **status 多一個 `stale`**：元素被移除（SPA 換頁、條件渲染）時，圖釘既不是正常也不是錯誤，而是**失去目標**。這種狀態一定要能表達，否則 AI 會把「找不到元素」誤讀成「元素有問題」。
- **`resolved` 改為 `warning`/`error`**：卡片原本是 `active|resolved|error`，但 `pin_analyze` 的結果本質是三級（正常／有疑慮／有錯），`resolved` 反而沒有東西會去設定它。

### 2. 儲存位置：**content script 的模組變數**

| 選項 | 判斷 |
|---|---|
| content script 記憶體 | ✅ **採用** —— 天然依分頁隔離，覆蓋層本來就在這裡畫，取用零成本 |
| `background.ts` 的 Map | ❌ background 是 **MV3 service worker，閒置 30 秒就被回收**，Map 一起消失（PM-298 已經被這件事咬過）。而且畫覆蓋層仍得回頭找 content script |
| `chrome.storage.session` | ❌ 為了跨 SW 回收而持久化，但**圖釘綁的是 DOM 元素**——頁面一重整，selector 指向的東西可能就不同了。復原一個指向錯元素的圖釘比消失更糟 |

**分頁關閉／重整 → 圖釘消失，是正確行為**：圖釘的語意是「這一頁的這個元素」，頁面沒了它就沒有指涉對象。AI 若需要跨頁保留，應該自己記住 selector 再重新釘。

### 3. UI 注入方式

- **一個 `position:fixed` 的容器**（`pointer-events:none`），內含每個圖釘的小圓點；**圓點本身 `pointer-events:auto`** —— 這是 PM-304 為 Zone Grid 寫下的同一條結論（整層 none 就收不到點擊）。
- **一律用 DOM API 建，不用 `innerHTML`** —— 啟用 Trusted Types 的網站會擋掉字串賦值，結果是覆蓋層整個不出現、只留一行 console 錯誤（PM-69 踩過）。
- **位置用 `getBoundingClientRect()` 即時算**，並在 `scroll` / `resize` 時重算（`requestAnimationFrame` 節流）。**不追 DOM 變動**：`MutationObserver` 全頁監聽在複雜站台上成本過高，改為「每次 `get_pin_results` / `pin_analyze` 時順便重算」——圖釘是給 AI 看的，不需要 60fps 跟隨。
- **元素消失** → 圓點轉灰並標 `stale`，不直接刪掉（AI 需要知道「我釘的東西不見了」）。

### 4. 三支工具的 schema

```
pin_element(selector, description, tab_id?)
  → { pin_id, selector, description, element_found, tag, text, tab_id }
  · 同一 selector 重複釘 → **更新 description，回原本的 pin_id**（不建立第二個）
  · 元素不存在 → error（沿用 click_element 的格式，帶 selector + hint）

pin_analyze(selector, tab_id?)
  → { pin_id, selector, status, analysis: <analyze_element 的完整結果> }
  · 未釘過則自動先釘（description 自動填 'pin_analyze 自動建立'）
  · 直接複用 bridgeAnalyzeElement()，**不另寫一套分析**
  · 依分析結果更新 status 與圓點顏色

get_pin_results(tab_id?)
  → { pins: [...], total_count, tab_id }
  · 無圖釘 → 回空陣列（**不是 error**）
```

**status 判定規則**（`pin_analyze` 用）：元素不存在 → `stale`；不可見或尺寸 0 或 disabled → `warning`；其餘 → `active`。這裡刻意**不引入新的錯誤來源**，只用 `analyze_element` 已經算出來的 `visibility` 與屬性。

---

### 🟡 PM-330：Phase 2 MCP 工具 — pin_element

**目標**：實作 `pin_element(selector, description, tab_id?)` 工具，讓 AI 能釘選頁面元素。

**依 PM-329 設計方案實作。**

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('pin_element', ...)
參數：
  selector: z.string()
  description: z.string()
  tab_id: z.number().optional()
回傳：{ pin_id, selector, description, element_found: true, tab_id }
```

**content script 側**（`extension/src/content.ts`）：
```
case 'BRIDGE_PIN_ELEMENT':
  1. querySelector 找到元素
  2. 建立 Pin 物件存入 pinStore
  3. 注入視覺 overlay（小圖釘 icon 在元素旁邊）
  4. 回傳 pin_id
```

**驗收條件**：
1. AI 呼叫 `pin_element` → 元素被釘選 → 回傳 pin_id
2. 頁面上出現視覺標記
3. 同一元素重複釘選 → 更新描述（不建立第二個圖釘）
4. 元素不存在 → error

完成後在下方寫 `🔵 DONE-330`。

---

### 🔵 DONE-330：`pin_element` 已實作，四項驗收在真實 DOM 全數通過；端到端待重載

**改動檔案**：`extension/src/content.ts`、`extension/src/types.ts`、`extension/src/background.ts`、`bridge/src/mcp-server.ts`。兩邊 build 與 `tsc` 皆通過。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 呼叫 → 元素被釘選 → 回 pin_id | ✅ 另回 tag / text / element_found |
| 2 | 頁面上出現視覺標記 | ✅ 覆蓋層存在，且**標記數量與圖釘數一致** |
| 3 | 重複釘同一元素 → 更新描述、不建立第二個 | ✅ pin_id 不變、`updated_existing: true`、清單仍只有 1 個 |
| 4 | 元素不存在 → error | ✅ 含 selector 與 hint |

`_verify309.mjs` **84 passed, 0 failed**（圖釘系統佔 12 項）；端到端待擴充功能重新載入。

依 PM-329 的設計實作，其中三處是刻意與卡片不同的：

- **不存 `tab_id`**：圖釘存在 content script，而 content script 本來就每個分頁一份 —— 再存一次只會製造「pin 記的 tab_id 與它實際所在分頁不一致」這種無法對帳的狀態。
- **覆蓋層外層 `pointer-events:none`、圓點 `auto`**：PM-304 為 Zone Grid 寫下的同一條結論（整層 none 就收不到點擊）。
- **一律 DOM API 不用 `innerHTML`**：Trusted Types 網站會擋掉字串賦值，結果是覆蓋層整個不出現、只留一行 console 錯誤（PM-69 踩過）。

---

### 🟡 PM-331：Phase 2 MCP 工具 — pin_analyze + get_pin_results

**目標**：實作圖釘系統的分析與查詢工具。

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('pin_analyze', ...)
參數：
  selector: z.string()
  tab_id: z.number().optional()
回傳：{ pin_id, selector, analysis（= analyze_element 的完整結果）}

server.tool('get_pin_results', ...)
參數：
  tab_id: z.number().optional()
回傳：{ pins: [{ pin_id, selector, description, status, last_check }], total_count, tab_id }
```

**pin_analyze** = pin_element + analyze_element 的組合：
1. 釘選元素（如果還沒釘）
2. 執行 analyze_element 的完整分析
3. 把分析結果存進 pin 的 last_check
4. 更新圖釘視覺狀態（綠=正常、黃=警告、紅=錯誤）

**get_pin_results**：
- 回傳指定分頁的所有圖釘及其最近一次檢查結果
- 省略 tab_id → 當前分頁

**驗收條件**：
1. `pin_analyze` → 回傳含 analysis 的完整分析結果
2. `get_pin_results` → 列出所有圖釘及狀態
3. 無圖釘時回空陣列（不是 error）
4. pin_analyze 後圖釘的視覺狀態有更新

完成後在下方寫 `🔵 DONE-331`。

---

### 🔵 DONE-331：`pin_analyze` + `get_pin_results` 已實作，四項驗收在真實 DOM 全數通過

**改動檔案**：同 PM-330。bridge 工具總數 **17**（11 瀏覽器 + 3 終端機 + 3 圖釘）。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | `pin_analyze` → 回傳含 analysis 的完整結果 | ✅ 直接複用 `analyze_element`，`computed_styles` / `box_model` 都在 |
| 2 | `get_pin_results` → 列出所有圖釘及狀態 | ✅ 含 `status` 與 `last_check` |
| 3 | 無圖釘時回空陣列（不是 error）| ✅ 另附一句提示怎麼建立 |
| 4 | `pin_analyze` 後視覺狀態有更新 | ✅ 可見 → `active`；隱藏 → `warning` 且**說明原因** |

---

### 🔴 「元素消失」必須是第四種狀態，不能塞進 error

卡片的 status 是 `active | resolved | error`。但實際跑起來會遇到第四種情況：**SPA 換頁或條件渲染之後，被釘的元素從 DOM 上消失了**。

這既不是「正常」也不是「元素有問題」—— 是**圖釘失去了指涉對象**。若把它回成 error，AI 會讀成「這個元素壞掉了」而去查一個根本已經不存在的東西。

所以加了 `stale`：元素找不到時圓點轉灰、狀態標 `stale`，**而且不刪除圖釘**（AI 需要知道「我釘的東西不見了」，直接消失反而更難查）。工具描述也明講 `stale` 與「元素壞掉」是兩回事。測試有一條專門驗這個：釘完之後把元素 `remove()`，確認狀態轉 `stale` 而非報錯或消失。

另外把 `resolved` 換成 `warning` —— 原本的 `resolved` 沒有任何程式路徑會去設定它，而 `pin_analyze` 的結果本質是三級（正常／有疑慮／已消失）。

**`status` 只從 `analyze_element` 已算出的 `visibility` 與屬性推導，不引入新的錯誤來源** —— 否則同一個元素用 `analyze_element` 看是正常、用 `pin_analyze` 看卻是異常，兩支工具會互相矛盾。

