# BugEzy Job — 2026-06-18

> 接續 job-0617.md（PM-11~26）
> 第 6 代「更好用」繼續打磨

---

### 🟡 PM-27：錄製即時字幕分兩區（底部 interim + 右上堆疊已確認文字）

**問題**：目前即時字幕只有底部一條，確認的文字一閃就消失，使用者看不到已經收錄了哪些內容，不放心。

**目標**：字幕分成兩區——底部顯示「正在講的」，右上角累積顯示「已確認的」。使用者邊講邊看，一目了然。

---

#### §1 inject.ts 改動

在 `showCaptionBar()` 裡，除了底部字幕條，**多建一個右上角面板**：

```ts
function showCaptionBar() {
  // ── 底部即時字幕（已有，不動）──
  // ...existing code...

  // ── 右上角已確認文字面板（新增）──
  const panel = document.createElement('div');
  panel.id = 'bugezy-voice-panel';
  panel.style.cssText = `
    position: fixed; top: 60px; right: 12px;
    z-index: 2147483647; pointer-events: none;
    width: 260px; max-height: 50vh; overflow-y: auto;
    background: rgba(0, 0, 0, 0.8);
    border: 1px solid rgba(124, 58, 237, 0.5);
    border-radius: 12px;
    padding: 10px 14px;
    font-family: system-ui, sans-serif;
    font-size: 14px; color: #eee;
    line-height: 1.6;
    transition: opacity 0.3s;
  `;

  // 標題列（含收合按鈕，pointer-events: auto 讓它可點）
  const header = document.createElement('div');
  header.style.cssText = `
    display: flex; justify-content: space-between; align-items: center;
    margin-bottom: 8px; padding-bottom: 6px;
    border-bottom: 1px solid rgba(255,255,255,0.15);
    pointer-events: auto; cursor: pointer;
  `;
  header.innerHTML = '<span style="font-size:12px;color:#a78bfa;">📝 語音記錄</span><span id="bugezy-panel-toggle" style="font-size:12px;color:#888;">▼ 收合</span>';
  
  const content = document.createElement('div');
  content.id = 'bugezy-voice-content';
  content.style.cssText = 'white-space: pre-wrap; word-break: break-word;';
  
  panel.appendChild(header);
  panel.appendChild(content);
  document.body.appendChild(panel);
  
  // 收合/展開 toggle
  let collapsed = false;
  header.addEventListener('click', () => {
    collapsed = !collapsed;
    content.style.display = collapsed ? 'none' : 'block';
    const toggle = document.getElementById('bugezy-panel-toggle');
    if (toggle) toggle.textContent = collapsed ? '▶ 展開' : '▼ 收合';
  });
}
```

**在 `recognition.onresult` 裡，確認的文字追加到右上面板**：

```ts
// final text → 追加到右上面板
if (finalText) {
  const voiceContent = document.getElementById('bugezy-voice-content');
  if (voiceContent) {
    voiceContent.textContent += (voiceContent.textContent ? '\n' : '') + finalText;
    // 自動捲到最新
    const panel = document.getElementById('bugezy-voice-panel');
    if (panel) panel.scrollTop = panel.scrollHeight;
  }
  // 底部字幕顯示確認（已有的邏輯保留）
}
```

**在 `hideCaptionBar()` 裡移除面板**：

```ts
function hideCaptionBar() {
  captionBar?.remove();
  captionBar = null;
  document.getElementById('bugezy-voice-panel')?.remove();
}
```

---

#### §2 不改的東西

- annotate.ts/html：不動（標注頁已有自己的字幕邏輯）
- edit-report：不動
- background/content/popup：不動
- server/web：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 開網頁 → F5 → 按「🎬 錄製」
2. 頁面右上角出現「📝 語音記錄」面板（半透明黑底）
3. 底部字幕即時閃動（正在講的 interim）
4. 確認的文字**一行一行堆疊在右上面板裡**
5. 點面板標題的「▼ 收合」→ 內容收起，再點「▶ 展開」恢復
6. 停止後面板消失

---

### 🟡 PM-28：錄製編輯頁加時間軸標記（📌 多個時間點 + 文字說明）

**目標**：編輯頁加一個 mini rrweb 播放器 + 時間軸，使用者可以暫停在任意秒數 → 按「📌 標記」→ 輸入文字。可標多個。標記資料存進報告，AI 透過 MCP 可讀。

---

#### §1 edit-report.html 改動

在錄製摘要下方、語音記錄上方，插入「時間軸標記區」：

```html
<!-- 時間軸標記區 -->
<div id="markerSection" class="section">
  <h2>📌 時間軸標記</h2>
  <div class="marker-player">
    <div id="miniPlayer" class="mini-player-container"></div>
    <div class="marker-controls">
      <button id="markerPlayBtn" class="btn btn-sm">▶</button>
      <input type="range" id="markerSeek" min="0" max="100" value="0" style="flex:1;">
      <span id="markerTime" class="time-display">0:00 / 0:00</span>
      <button id="addMarkerBtn" class="btn btn-sm btn-accent">📌 標記此刻</button>
    </div>
  </div>
  <div id="markerList" class="marker-list"></div>
</div>
```

CSS：
```css
.mini-player-container {
  width: 100%; max-width: 640px; height: 360px;
  overflow: hidden; border-radius: 8px;
  border: 1px solid var(--line, #2a2a3e);
  margin-bottom: 8px;
}
.mini-player-container iframe {
  width: 640px; height: 360px; border: none;
  transform-origin: top left;
}
.marker-controls {
  display: flex; align-items: center; gap: 8px;
  padding: 8px 0;
}
.btn-sm {
  padding: 4px 12px; font-size: 13px; border-radius: 6px;
  background: var(--panel, #1a1a2e); color: #fff;
  border: 1px solid var(--line, #2a2a3e); cursor: pointer;
}
.btn-accent { background: #7c3aed; border-color: #7c3aed; font-weight: 600; }
.marker-list { margin-top: 8px; }
.marker-item {
  display: flex; align-items: flex-start; gap: 8px;
  padding: 8px; margin: 4px 0;
  background: var(--panel, #1a1a2e);
  border: 1px solid var(--line, #2a2a3e);
  border-radius: 8px;
}
.marker-time {
  background: #7c3aed; color: #fff; padding: 2px 8px;
  border-radius: 4px; font-size: 12px; font-family: monospace;
  white-space: nowrap; cursor: pointer;
}
.marker-time:hover { background: #6d28d9; }
.marker-note {
  flex: 1; background: transparent; color: #eee;
  border: 1px solid var(--line, #2a2a3e);
  border-radius: 4px; padding: 4px 8px; font-size: 13px;
}
.marker-delete {
  background: none; border: none; color: #888;
  cursor: pointer; font-size: 16px;
}
.marker-delete:hover { color: #ef4444; }
```

---

#### §2 edit-report.ts 時間軸標記邏輯

```ts
import { Replayer } from '@rrweb/replay';
import '@rrweb/replay/dist/style.css';

interface Marker {
  time_sec: number;
  note: string;
}

const markers: Marker[] = [];
let replayer: Replayer | null = null;
let duration = 0;
let playing = false;

// 初始化 mini player（如果有 rrweb 資料）
function initMiniPlayer(events: unknown[]) {
  const container = document.getElementById('miniPlayer');
  if (!container || events.length < 2) {
    document.getElementById('markerSection')!.style.display = 'none';
    return;
  }

  replayer = new Replayer(events as any, {
    root: container,
    skipInactive: true,
    showWarning: false,
  });

  const meta = replayer.getMetaData();
  duration = meta.totalTime || 0;
  const seekBar = document.getElementById('markerSeek') as HTMLInputElement;
  seekBar.max = String(duration);

  // 播放/暫停
  document.getElementById('markerPlayBtn')!.addEventListener('click', () => {
    if (!replayer) return;
    if (playing) {
      replayer.pause();
      playing = false;
      document.getElementById('markerPlayBtn')!.textContent = '▶';
    } else {
      const current = replayer.getCurrentTime();
      if (current >= duration - 100) {
        replayer.play(0);
      } else {
        replayer.resume(current);
      }
      playing = true;
      document.getElementById('markerPlayBtn')!.textContent = '⏸';
      trackProgress();
    }
  });

  // seek
  seekBar.addEventListener('input', (e) => {
    const time = Number((e.target as HTMLInputElement).value);
    replayer!.play(time);
    if (!playing) setTimeout(() => replayer!.pause(time), 50);
    updateTimeDisplay(time);
  });

  // 📌 標記按鈕
  document.getElementById('addMarkerBtn')!.addEventListener('mousedown', (e) => {
    e.preventDefault(); // 不搶焦點
  });
  document.getElementById('addMarkerBtn')!.addEventListener('click', () => {
    if (!replayer) return;
    const currentTime = replayer.getCurrentTime();
    const sec = Math.floor(currentTime / 1000);
    // 暫停在當前位置
    if (playing) {
      replayer.pause();
      playing = false;
      document.getElementById('markerPlayBtn')!.textContent = '▶';
    }
    addMarker(sec);
  });
}

function addMarker(sec: number) {
  const marker: Marker = { time_sec: sec, note: '' };
  markers.push(marker);
  renderMarkers();
  // 自動 focus 到新標記的輸入框
  setTimeout(() => {
    const inputs = document.querySelectorAll('.marker-note');
    const last = inputs[inputs.length - 1] as HTMLInputElement;
    last?.focus();
  }, 50);
}

function renderMarkers() {
  const list = document.getElementById('markerList')!;
  list.innerHTML = '';
  // 按時間排序
  markers.sort((a, b) => a.time_sec - b.time_sec);
  markers.forEach((m, i) => {
    const item = document.createElement('div');
    item.className = 'marker-item';
    
    const timeBtn = document.createElement('span');
    timeBtn.className = 'marker-time';
    timeBtn.textContent = formatSec(m.time_sec);
    timeBtn.title = '點擊跳到此時間';
    timeBtn.addEventListener('click', () => {
      if (replayer) {
        replayer.play(m.time_sec * 1000);
        setTimeout(() => replayer!.pause(m.time_sec * 1000), 50);
        updateTimeDisplay(m.time_sec * 1000);
      }
    });
    
    const noteInput = document.createElement('input');
    noteInput.className = 'marker-note';
    noteInput.type = 'text';
    noteInput.placeholder = '描述這個時間點的問題...';
    noteInput.value = m.note;
    noteInput.addEventListener('input', () => { m.note = noteInput.value; });
    
    const delBtn = document.createElement('button');
    delBtn.className = 'marker-delete';
    delBtn.textContent = '✕';
    delBtn.addEventListener('click', () => {
      markers.splice(i, 1);
      renderMarkers();
    });
    
    item.appendChild(timeBtn);
    item.appendChild(noteInput);
    item.appendChild(delBtn);
    list.appendChild(item);
  });
}

function formatSec(sec: number): string {
  const m = Math.floor(sec / 60);
  const s = sec % 60;
  return `${m}:${String(s).padStart(2, '0')}`;
}

function updateTimeDisplay(ms: number) {
  const el = document.getElementById('markerTime');
  if (el) el.textContent = `${formatSec(Math.floor(ms / 1000))} / ${formatSec(Math.floor(duration / 1000))}`;
  const seekBar = document.getElementById('markerSeek') as HTMLInputElement;
  seekBar.value = String(ms);
}

function trackProgress() {
  if (!playing || !replayer) return;
  const current = replayer.getCurrentTime();
  updateTimeDisplay(current);
  if (current < duration) {
    requestAnimationFrame(trackProgress);
  } else {
    playing = false;
    document.getElementById('markerPlayBtn')!.textContent = '▶';
  }
}
```

---

#### §3 Markers 存進報告

**上傳時把 markers 加進 payload**：

edit-report.ts 的上傳按鈕 handler 裡：
```ts
// 組 payload 時加 markers
payload.markers = markers.filter(m => m.note.trim()); // 只保留有寫文字的標記
```

**types.ts 加**（extension 共用型別）：
```ts
export interface TimeMarker {
  time_sec: number;
  note: string;
}
```

RecordingPayload 加：
```ts
markers?: TimeMarker[];  // 選填，向後相容
```

---

#### §4 Server 改動

**POST /api/reports**：payload 裡的 `markers` 存入 Supabase。

**Supabase schema**（FOX 手動）：
```sql
ALTER TABLE reports ADD COLUMN IF NOT EXISTS markers JSONB DEFAULT '[]';
```

**GET /api/reports/:id**：回傳 markers。

---

#### §5 MCP 改動

`get_report_overview` 的 select 加上 markers 欄位。AI 讀到的就是：
```json
{
  "markers": [
    { "time_sec": 5, "note": "點了搜尋按鈕" },
    { "time_sec": 12, "note": "白畫面，應顯示搜尋結果" }
  ]
}
```

---

#### §6 報告頁（web/）改動

ReportPage.tsx：如果 report.markers 非空，在 rrweb player 下方顯示標記列表。點擊時間戳跳到對應播放位置。

---

#### §7 安裝依賴

edit-report.ts 要用 `@rrweb/replay`，這個已經裝在 web/ 裡但不在 extension/。需要：
```bash
cd extension && npm install @rrweb/replay
```

build.mjs 的 `external` 不要排除 `@rrweb/replay`（讓 esbuild bundle 進去）。

---

#### §8 不改的東西

- inject.ts：不動（PM-27 改的字幕邏輯與此無關）
- annotate.ts/html：不動
- popup：不動

---

#### Build

```
cd extension && npm install @rrweb/replay && npm run build
cd ../server && npx tsc --noEmit
cd ../web && npm run build
```

然後部署 server：`cd server && npx wrangler deploy`

#### 驗收 PM-27（字幕兩區）

1. 開網頁 → F5 → 按「🎬 錄製」
2. 右上角出現「📝 語音記錄」面板
3. 講中文 → 底部閃動 interim → 確認文字堆疊到右上面板
4. 點「▼ 收合」→ 面板收起
5. 停止後面板消失

#### 驗收 PM-28（時間軸標記）

1. 錄製一段（30 秒以上，中間點幾下頁面）→ 停止 → 進編輯頁
2. 看到 mini rrweb 播放器 + 播放按鈕 + 進度條
3. 播放 → 暫停在某秒 → 按「📌 標記此刻」→ 輸入「這裡按鈕沒反應」
4. 繼續播放 → 暫停在另一秒 → 再標記 → 輸入「這個圖片沒載入」
5. 點標記的時間戳 → 播放器跳到那個時間
6. 按「✅ 上傳報告」
7. BugEzy MCP 讀報告 → markers 陣列有兩個時間點 + 說明

---

### 🔵 DONE-27：錄製即時字幕分兩區 ✅

**改動**：`extension/src/inject.ts` 一個檔。

- `showCaptionBar()`：底部字幕條保留不動，**新增右上角 `#bugezy-voice-panel` 面板**（半透明黑底、紫框、`top:60px;right:12px`、`pointer-events:none` 不擋頁面）。標題列 `📝 語音記錄` + 收合/展開 toggle（`pointer-events:auto` 可點）。
- `recognition.onresult`：每段 **final 文字** 除了推進 `voiceSegments` + 底部短暫顯示 `✅`，也 **append 到 `#bugezy-voice-content`**（一行一行堆疊），並自動捲到最新。interim 仍只閃在底部字幕條。
- `hideCaptionBar()`：停止錄製時一併 `#bugezy-voice-panel?.remove()`。

**build**：`npm run build` ✅（inject.js 405.8kb）。

**待 FOX 瀏覽器驗收**：右上面板出現、final 堆疊、收合/展開、停止後消失（headless 無法驗 Chrome 麥克風 UI）。

---

### 🔵 DONE-28：錄製編輯頁時間軸標記 ✅（⚠ 需 FOX 先跑 SQL）

**全鏈改動（extension + server + mcp + web）**：

- **types.ts**：新增 `TimeMarker { time_sec; note }`；`RecordingPayload.markers?`；`UPLOAD_REPORT` 訊息帶 `markers?`。
- **edit-report.html**：摘要下方插入 `#markerSection`（mini player 容器 + 播放/進度/`📌 標記此刻` + 標記列表）＋對應 CSS；`<link rel="stylesheet" href="edit-report.css">`（esbuild 從 `@rrweb/replay/dist/style.css` 產出的 sibling CSS）。
- **edit-report.ts**：`import { Replayer } from '@rrweb/replay'`；`initMiniPlayer(payload.rrwebEvents)`（<2 筆事件自動隱藏 markerSection）；播放/暫停/seek/`getCurrentTime` 標記/點時間戳跳播/刪除/排序；上傳時 `markers: markers.filter(m=>m.note.trim())` 隨 `UPLOAD_REPORT` 送出。
- **background.ts**：`uploadReport(description, markers)` → `payload.markers = markers ?? []`。
- **server/src/index.ts**：`RecordingPayload.markers?`；insert row 加 `markers`；`getReport` 回傳 `markers`；`META_COLS` 加 `markers`（MCP overview/list 可讀）。
- **mcp-server/src/index.ts**（stdio）：`ReportData.markers?`；`get_report_overview` 回傳 `markers`。
- **web**：`types.ts` 加 `TimeMarker` + `Report.markers?`；`RrwebPlayer` 新增 `markers` prop → 控制列下方渲染可點時間戳（`seekTo`）+ 說明；`ReportPage` 傳入 `report.markers`；`index.css` 加 `.marker-list` 樣式。
- **server/schema.sql**：建表加 `markers JSONB` ＋ PM-28 ALTER 區塊。

**安裝**：`extension` 已加 `@rrweb/replay ^2.0.1`（hoist 到 root node_modules）。

**Build / Deploy 全綠**：
- `extension npm run build` ✅（edit-report.js 413.7kb，內含 rrweb replay；edit-report.css 2.3kb 已產出）
- `server npx tsc --noEmit` ✅
- `mcp-server npx tsc --noEmit` ✅
- `web npm run build` ✅
- `npx wrangler deploy` ✅（Version 778da6a3）

**🚨 阻擋項 — FOX 請立刻執行（否則所有上傳會失敗）**：
部署後的 Worker 已在 insert/select 引用 `markers` 欄位，但 Supabase 尚未有此欄。我用 live curl 實測 POST，回：
```
supabase insert failed: Could not find the 'markers' column of 'reports' in the schema cache
```
這代表**目前線上所有錄製上傳都會失敗**（不只帶 markers 的）。請到 Supabase SQL Editor 跑（已寫進 `server/schema.sql`）：
```sql
ALTER TABLE reports ADD COLUMN IF NOT EXISTS markers JSONB DEFAULT '[]';
```
跑完上傳即恢復、markers 全鏈即生效。（此為 spec §4「Supabase schema（FOX 手動）」的預期步驟，但因已部署故須即時補上。）測試 POST 失敗 = 無垃圾資料寫入，DB 乾淨。

**待 FOX 驗收（跑完 SQL 後）**：編輯頁 mini player 回放、標記、點時間戳跳播、上傳後 MCP `get_report_overview` 讀到 markers、報告頁時間戳跳播。

---

### 🟡 PM-29：修時間軸標記 UX — 按 📌 彈對話框 + 保留空標記

**問題**：
1. 按「📌 標記此刻」後，文字輸入框出現在下方但不夠明顯，使用者不知道要打字
2. 沒打字的標記被 `filter(m => m.note.trim())` 過濾掉，上傳後 markers 為空

**修法**：

---

#### edit-report.ts 改動

**改動 1：按 📌 後彈 prompt 對話框**

把 `addMarker()` 改為：
```ts
function addMarker(sec: number) {
  const note = prompt(`📌 ${formatSec(sec)} — 描述這個時間點的問題：`) ?? '';
  const marker: Marker = { time_sec: sec, note: note.trim() };
  markers.push(marker);
  renderMarkers();
}
```

這樣按 📌 會立刻跳出瀏覽器原生對話框，使用者一定看得到。輸入完按確定，標記就加好了。按取消也會加一個空標記（只有時間點）。

**改動 2：移除空標記過濾**

上傳時改為：
```ts
// 保留所有標記（包含沒有文字的，時間點本身就有價值）
payload.markers = markers;
```

不再 `.filter(m => m.note.trim())`。

---

#### 不改的東西

- renderMarkers()：不動（下方的標記列表仍可編輯文字）
- 其他所有檔案：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 錄製 → 停止 → 編輯頁
2. 播放 → 暫停 → 按「📌 標記此刻」→ **應跳出對話框問你描述**
3. 輸入文字 → 確定 → 標記出現在下方列表
4. 再標記一個但按取消（不輸入文字）→ 標記仍出現（只有時間點）
5. 上傳 → MCP 讀到 markers 有內容

---

### 🔵 DONE-29：修時間軸標記 UX ✅

**改動**：`extension/src/edit-report.ts` 兩處（其他檔案皆不動）。

1. **`addMarker(sec)` 改彈原生對話框**：按「📌 標記此刻」立刻 `window.prompt('📌 m:ss — 描述這個時間點的問題：')`，使用者一定看得到。輸入 → `note.trim()`；按取消（回傳 null）→ 空描述，仍 push 標記（保留時間點）。移除原本 push 空標記 + 自動 focus 下方輸入框的邏輯。下方列表的可編輯輸入框（`renderMarkers`）保留不動，仍可事後補/改字。
2. **上傳不再過濾空標記**：`UPLOAD_REPORT` 由 `markers.filter(m => m.note.trim())` 改為直接送 `markers`（全部，含只有時間點的）。

**build**：`npm run build` ✅（edit-report.js 413.7kb，無型別/打包錯誤）。

**待 FOX 瀏覽器驗收**：按 📌 跳對話框、輸入後列表出現、按取消仍留時間點、上傳後 MCP 讀到 markers。（仍需先跑 DONE-28 的 `ALTER TABLE … markers` SQL，上傳才會成功。）

---

### 🟡 PM-30：字幕條加永久「🔄 重啟」按鈕

**問題**：語音中斷重啟按鈕只在 `rec.start()` 拋錯時出現，但大部分中斷是靜默的（auto restart 有空窗），使用者看到字幕停了但沒按鈕可按。

**修法**：字幕條右邊永遠顯示一個小「🔄」按鈕，使用者覺得語音斷了隨時可以按。按了會建新的 SpeechRecognition 實例強制重啟。

---

#### inject.ts 改動

**showCaptionBar() 裡，底部字幕條加一個重啟按鈕**：

```ts
function showCaptionBar() {
  // ...existing code 建 bar...
  
  // 重啟按鈕（永遠顯示在字幕條右邊）
  const restartBtn = document.createElement('button');
  restartBtn.id = 'bugezy-voice-restart';
  restartBtn.textContent = '🔄';
  restartBtn.title = '重新啟動語音辨識';
  restartBtn.style.cssText = `
    pointer-events: auto;
    background: rgba(124, 58, 237, 0.8);
    color: #fff;
    border: none;
    border-radius: 6px;
    padding: 4px 10px;
    font-size: 16px;
    cursor: pointer;
    margin-left: 12px;
    vertical-align: middle;
  `;
  restartBtn.addEventListener('click', () => {
    forceRestartVoice();
  });
  bar.appendChild(restartBtn);
  
  // bar 的 pointer-events 改成 none，但內部按鈕是 auto
  bar.style.pointerEvents = 'none';
  // 讓 bar 內部可以有可點擊元素
  // 做法：bar 用 flex，文字部分 pointer-events:none，按鈕 pointer-events:auto
}
```

**字幕條改成 flex 佈局**：
```ts
bar.style.cssText = `
  position: fixed; bottom: 100px; left: 50%; transform: translateX(-50%);
  z-index: 2147483647;
  background: rgba(0,0,0,0.85); color: #fff;
  padding: 12px 28px; border-radius: 12px;
  font-size: 22px; max-width: 80%; text-align: center;
  font-family: system-ui, sans-serif;
  display: flex; align-items: center; gap: 8px;
  pointer-events: none;
`;

// 文字用 span 包裹
const textSpan = document.createElement('span');
textSpan.id = 'bugezy-caption-text';
textSpan.style.cssText = 'flex: 1; pointer-events: none;';
textSpan.textContent = '🔴 錄製中，可以用中文描述問題...';
bar.appendChild(textSpan);
bar.appendChild(restartBtn); // 按鈕在右邊
```

**更新字幕文字時改為更新 textSpan**：
所有 `captionBar.textContent = ...` 改成 `document.getElementById('bugezy-caption-text')!.textContent = ...`
（因為現在 captionBar 裡有文字 span + 按鈕，直接改 textContent 會把按鈕也清掉）

**新增 forceRestartVoice() 函式**：
```ts
function forceRestartVoice() {
  blog('手動強制重啟語音');
  const SR = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition;
  if (!SR || !voiceActive) return;
  
  // 停掉舊的
  try { recognition?.stop(); } catch {}
  
  // 建新的
  try {
    const newRec = new SR();
    newRec.lang = 'zh-TW';
    newRec.continuous = true;
    newRec.interimResults = false;
    newRec.onresult = recognition!.onresult;
    newRec.onend = recognition!.onend;
    newRec.onerror = recognition!.onerror;
    recognition = newRec;
    newRec.start();
    
    const textEl = document.getElementById('bugezy-caption-text');
    if (textEl) textEl.textContent = '🔴 語音已重啟，繼續說...';
    blog('語音強制重啟成功');
  } catch (err) {
    blog('語音強制重啟失敗', err);
    const textEl = document.getElementById('bugezy-caption-text');
    if (textEl) textEl.textContent = '❌ 語音重啟失敗';
  }
}
```

**同時保留 showRestartButton() 的現有邏輯**（自動重啟失敗時會把整個字幕條變成錯誤狀態）。

---

#### 不改的東西
- 右上面板（PM-27）：不動
- edit-report / annotate / server / web：不動

---

#### Build
```
cd extension && npm run build
```

#### 驗收
1. 錄製 → 字幕條底部應有「🔴 文字... 🔄」，右邊有小紫色 🔄 按鈕
2. 講中文 → 字幕正常
3. 等語音斷掉 → 按 🔄 → 字幕顯示「🔴 語音已重啟，繼續說...」→ 繼續講 → 字幕恢復

---

### 🔵 DONE-30：字幕條加永久「🔄 重啟」按鈕 ✅

**改動**：`extension/src/inject.ts` 一個檔（右上面板 PM-27 / edit-report / annotate / server / web 皆不動）。

- **`showCaptionBar()` 改 flex 佈局**：底部字幕條 `display:flex;align-items:center;gap:8px`，本體 `pointer-events:none`。文字改用 `#bugezy-caption-text` span 包裹（`flex:1;pointer-events:none`），右邊永久放一顆紫色 `#bugezy-voice-restart` 🔄 按鈕（`pointer-events:auto`），點擊呼叫 `forceRestartVoice()`。
- **新增 `setCaptionText(text)` helper**：只更新 `#bugezy-caption-text` 的 textContent，避免像舊的 `captionBar.textContent=` 直接覆寫把 🔄 按鈕一起清掉。onresult 的三處字幕更新（interim `🔴`、final `✅`、回復 `🔴 聆聽中...`）全改用 `setCaptionText()`。
- **新增 `forceRestartVoice()`**：`recognition?.stop()` 停舊的 → `new SR()`（zh-TW / continuous / interimResults=false）沿用既有 `onresult/onend/onerror` handlers → `start()` → 字幕顯示「🔴 語音已重啟，繼續說...」；失敗顯示「❌ 語音重啟失敗」。`!voiceActive` 時直接 return。
- **`showRestartButton()`（自動重啟失敗時的整條錯誤狀態）保留不動**，與永久 🔄 按鈕並存。

**build**：`npm run build` ✅（inject.js 405.8 → 407.6kb，無錯）。

**待 FOX 瀏覽器驗收**：字幕條右邊出現小紫 🔄、靜默中斷後按 🔄 顯示「語音已重啟」並可續錄。（headless 無法驗 Chrome 麥克風 UI。）

---

### 🟡 PM-31：修三個 Bug（面板當機 + player 太小 + 文字插入位置）

**來源**：FOX 用 BugEzy 自己回報的 Bug（report 2de2c098）

---

#### Bug 1：右上語音面板點擊會當機（🔴 嚴重）

**問題**：PM-27 的右上「📝 語音記錄」面板，收合按鈕的 `pointer-events: auto` 區域太大，使用者不小心點到面板其他地方會觸發奇怪的行為導致頁面卡死。

**修法**：

在 inject.ts 的 `showCaptionBar()` 裡，右上面板的修改：

1. 整個面板 `pointer-events: none`（保持不擋頁面操作）
2. 只有收合按鈕本身是 `pointer-events: auto`，且用一個小 `<button>` 而不是整個 header 區域
3. 收合按鈕改小，只放一個「▼」/「▶」圖示

```ts
// header 改為 pointer-events: none
header.style.cssText = `
  display: flex; justify-content: space-between; align-items: center;
  margin-bottom: 8px; padding-bottom: 6px;
  border-bottom: 1px solid rgba(255,255,255,0.15);
  pointer-events: none;
`;

// 收合按鈕獨立，只有它可點
const toggleBtn = document.createElement('button');
toggleBtn.id = 'bugezy-panel-toggle';
toggleBtn.textContent = '▼';
toggleBtn.title = '收合/展開';
toggleBtn.style.cssText = `
  pointer-events: auto;
  background: rgba(124,58,237,0.6);
  border: none; border-radius: 4px;
  color: #fff; font-size: 12px;
  padding: 2px 8px; cursor: pointer;
`;
toggleBtn.addEventListener('click', (e) => {
  e.stopPropagation();
  collapsed = !collapsed;
  content.style.display = collapsed ? 'none' : 'block';
  toggleBtn.textContent = collapsed ? '▶' : '▼';
});

header.innerHTML = '<span style="font-size:12px;color:#a78bfa;">📝 語音記錄</span>';
header.appendChild(toggleBtn);
```

---

#### Bug 3：Mini player 太小（看報紙感）

**問題**：edit-report.html 的 mini rrweb player 只有 640x360，內容看不清楚。

**修法**：

edit-report.html / edit-report.ts 的 player 容器改為更大 + 響應式：

```css
.mini-player-container {
  width: 100%;
  max-width: 960px;  /* 從 640 改成 960 */
  height: auto;
  aspect-ratio: 16/9;  /* 保持比例 */
  overflow: hidden;
  border-radius: 8px;
  border: 1px solid var(--line, #2a2a3e);
  margin-bottom: 8px;
}
```

Replayer 初始化時 width 也要跟著改：
```ts
// 如果有設定 Replayer 的寬高，改為更大或自適應
// 讓 iframe 填滿容器
const iframe = container.querySelector('iframe');
if (iframe) {
  iframe.style.width = '100%';
  iframe.style.height = '100%';
}
```

---

#### Bug 4：文字框插入位置永遠跑到末端

**問題**：補充說明的 textarea，語音辨識 append 文字後 cursor 被移到最後面。使用者想在中間插入文字，但打的字永遠跑到句尾。

**修法**：

在 edit-report.ts 和 annotate.ts 的語音 `onresult` handler 裡，append 文字時**保留原始 cursor 位置**：

```ts
// 語音 final text append 時，保留 cursor 位置
if (finalText) {
  const cursorPos = descInput.selectionStart; // 記住 cursor
  const isAtEnd = cursorPos === descInput.value.length; // cursor 是否在末端
  
  // append 到末端
  descInput.value += finalText;
  
  // 如果 cursor 原本不在末端，恢復 cursor 位置
  if (!isAtEnd) {
    descInput.selectionStart = cursorPos;
    descInput.selectionEnd = cursorPos;
  }
}
```

這樣：
- 使用者在末端 → 語音 append 後 cursor 跟著到新末端（自然）
- 使用者在中間編輯 → 語音 append 到末端但 cursor 留在原位（不干擾）

**同時修 annotate.ts 的同一段邏輯。**

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. **面板不當機**：錄製 → 右上面板出現 → 用滑鼠點面板文字區域 → 不應該卡死
2. **player 更大**：編輯頁的 mini player 明顯變大，內容看得清楚
3. **文字插入**：在補充說明框中間打字 → 同時語音在 append → cursor 位置不被搶走

---

### 🔵 DONE-31：修三個 Bug ✅（來源 report 2de2c098）

**改動**：`inject.ts` + `edit-report.html` + `edit-report.ts` + `annotate.ts`（server/web 不動）。

**Bug 1 — 右上語音面板誤點卡死（inject.ts）**：
- 右上面板 `header` 由 `pointer-events:auto;cursor:pointer`（整列可點）改為 **`pointer-events:none`**（整列不擋、不可點）。
- 收合改成獨立 `<button id="bugezy-panel-toggle">`，**只有按鈕本身 `pointer-events:auto`**，紫底小圖示 `▼`/`▶`，handler 加 `e.stopPropagation()`。面板其他區域不再吃點擊，誤點不會觸發任何行為。

**Bug 3 — mini player 太小（edit-report.html + edit-report.ts）**：
- `.mini-player-container`：`max-width` 640→**960px**，`height:360px` 改 **`height:auto;aspect-ratio:16/9`**（響應式保比例）；`iframe` 改 `width:100%;height:100%`。
- `initMiniPlayer()` 建 Replayer 後，把內部 `iframe.style.width/height = '100%'`，填滿放大後的容器。

**Bug 4 — 文字插入永遠跑末端（edit-report.ts + annotate.ts）**：
- 兩處語音 `onresult` 的 final append 改為：先記 `selectionStart` + 判斷 `isAtEnd`，`value +=` 後若 cursor 原本不在末端則還原 `selectionStart/End`。使用者在中間編輯時語音 append 到末端但不搶 cursor；在末端則自然跟到新末端。

**build**：`npm run build` ✅（edit-report.js 414.1kb / inject.js 407.8kb / annotate.js 8.8kb，無錯）。

**待 FOX 瀏覽器驗收**：① 點右上面板文字區不卡死、只有 ▼ 按鈕可收合；② 編輯頁 player 明顯放大且清楚；③ 補充說明框中間打字時語音 append 不搶 cursor。

---

### 🟡 PM-32：修語音重啟按鈕（按了反而掛掉）

**問題**：錄製中按 🔄 重啟語音後，語音完全死掉無法喚醒。原因是 `forceRestartVoice()` 複製舊 recognition 的 event handlers（`recognition.onresult` 等），但舊 handlers 可能引用了已失效的閉包變數或已銷毀的 recognition 實例。

**修法**：不複製舊 handlers，重新建立完整的 event handlers。把語音初始化邏輯抽成可重複呼叫的函式。

---

#### inject.ts 改動

**核心思路**：把目前散落在 `startRecording()` 裡的 SpeechRecognition 初始化邏輯抽成一個獨立函式 `createRecognition()`，`forceRestartVoice()` 呼叫這個函式而不是複製舊 handlers。

```ts
/** 建立新的 SpeechRecognition 實例（可重複呼叫） */
function createRecognition(): SRInstance | null {
  const SR = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition;
  if (!SR) return null;

  const rec = new SR() as SRInstance;
  rec.lang = 'zh-TW';
  rec.continuous = true;
  rec.interimResults = false;

  rec.onresult = (e: any) => {
    for (let i = e.resultIndex; i < e.results.length; i++) {
      if (e.results[i].isFinal) {
        const text = e.results[i][0].transcript.trim();
        if (text) {
          const seg: VoiceSegment = { text, timestamp: Date.now(), isFinal: true };
          voiceSegments.push(seg);
          blog('voice segment:', text.slice(0, 40));

          // 更新右上面板
          const voiceContent = document.getElementById('bugezy-voice-content');
          if (voiceContent) {
            voiceContent.textContent += (voiceContent.textContent ? '\n' : '') + text;
            const panel = document.getElementById('bugezy-voice-panel');
            if (panel) panel.scrollTop = panel.scrollHeight;
          }

          // 更新底部字幕
          const textEl = document.getElementById('bugezy-caption-text');
          if (textEl) {
            textEl.textContent = '✅ ' + text;
            setTimeout(() => {
              if (voiceActive && textEl) textEl.textContent = '🔴 聆聽中...';
            }, 1500);
          }
        }
      }
    }
  };

  rec.onend = () => {
    if (voiceActive) {
      blog('SpeechRecognition onend → auto restart');
      try {
        rec.start();
      } catch {
        blog('SpeechRecognition auto restart 失敗');
        // 不呼叫 showRestartButton()，因為 🔄 按鈕已永遠在
        const textEl = document.getElementById('bugezy-caption-text');
        if (textEl) textEl.textContent = '⚠ 語音中斷，按 🔄 重啟';
      }
    }
  };

  rec.onerror = (e: any) => {
    blog('SpeechRecognition error:', e.error);
    if (e.error === 'not-allowed' || e.error === 'service-not-allowed') {
      voiceActive = false;
      const textEl = document.getElementById('bugezy-caption-text');
      if (textEl) textEl.textContent = '❌ 麥克風被拒絕';
    }
  };

  return rec;
}
```

**startRecording() 裡的語音初始化改為**：
```ts
voiceSegments = [];
voiceActive = true;
recognition = createRecognition();
if (recognition) {
  try {
    recognition.start();
    blog('SpeechRecognition started (zh-TW)');
  } catch (err) {
    blog('SpeechRecognition start 失敗', err);
    showMicPermissionOverlay(); // 已有的授權橫幅
  }
} else {
  blog('此瀏覽器不支援 SpeechRecognition');
  voiceActive = false;
}
```

**forceRestartVoice() 改為**：
```ts
function forceRestartVoice() {
  blog('手動強制重啟語音');
  if (!voiceActive) return;

  // 停掉舊的
  try { recognition?.stop(); } catch {}
  recognition = null;

  // 建全新的（不複製舊 handlers）
  recognition = createRecognition();
  if (recognition) {
    try {
      recognition.start();
      const textEl = document.getElementById('bugezy-caption-text');
      if (textEl) textEl.textContent = '🔴 語音已重啟，繼續說...';
      blog('語音強制重啟成功');
    } catch (err) {
      blog('語音強制重啟失敗', err);
      const textEl = document.getElementById('bugezy-caption-text');
      if (textEl) textEl.textContent = '❌ 語音重啟失敗';
    }
  }
}
```

**刪除 showRestartButton() 函式**（不再需要，🔄 按鈕已永遠在字幕條上）。

---

#### 不改的東西

- 右上面板、收合按鈕：不動（PM-31 已修）
- edit-report / annotate / server / web：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 錄製 → 講中文 → 字幕正常
2. 等語音斷掉（或故意靜默 15 秒）→ 字幕顯示「⚠ 語音中斷，按 🔄 重啟」
3. 按 🔄 → 字幕顯示「🔴 語音已重啟，繼續說...」
4. **繼續講中文 → 字幕恢復跟著 → 右上面板繼續堆疊新文字**
5. 再斷再按 🔄 → 可以無限次重啟

---

### 🔵 DONE-32：修 🔄 重啟後語音死掉 ✅

**改動**：`extension/src/inject.ts` 一個檔（右上面板/收合鈕、edit-report/annotate/server/web 皆不動）。

**根因**：舊 `forceRestartVoice()` 建新實例時**複製舊 recognition 的 handlers**（`newRec.onresult = recognition!.onresult` 等），這些 handler 的閉包綁在已銷毀的舊實例上，重啟後形同空轉 → 語音死掉。

**修法（照規格 3 點）**：
1. **抽出 `createRecognition(): SRInstance | null`**：每次都建全新實例 + **全新 handlers**（onresult 推 `voiceSegments`＋堆疊右上面板＋更新底部字幕；onend 自動重啟、失敗顯示「⚠ 語音中斷，按 🔄 重啟」；onerror 處理 not-allowed）。不再複製任何舊閉包。
2. **`forceRestartVoice()` 改用新實例**：`recognition?.stop()` → `recognition = null` → `recognition = createRecognition()` → `start()`，成功顯示「🔴 語音已重啟，繼續說...」。可無限次重啟。
3. **刪除 `showRestartButton()`**：永久 🔄 按鈕（PM-30）已在字幕條上，自動重啟失敗只改字幕文字提示即可，整段函式移除。

**順帶整合（不擴大範圍）**：`initSpeechRecognition()` 內部改為走同一個 `createRecognition()` 工廠（錄製啟動與 🔄 重啟同一條路徑），但**保留 `tryStartVoice()` + 授權浮層 `showMicPermissionOverlay()` 的麥克風授權流程不動**（PM-09 的 user-gesture 解法，SKILL.md 核心鐵律 #4），故 `startRecording()` 維持原樣，未照規格範例那樣改成直接 `start()`——避免回歸首次授權體驗。

**⚠ 行為變更（請 PM 知悉）**：`createRecognition()` 依規格設 `interimResults = false`，因此**底部字幕不再閃動「正在講的 interim」**，只顯示 `✅ 已確認` 與 `🔴 聆聽中...`；右上面板的 final 堆疊不受影響。若要保留 interim 即時閃字，需另開 PM 調整。

**build**：`npm run build` ✅（inject.js 407.8 → 405.9kb，刪冗碼後變小，無錯）。

**待 FOX 瀏覽器驗收**：講中文字幕正常 → 靜默 15 秒顯示「⚠ 語音中斷，按 🔄 重啟」→ 按 🔄 顯示「語音已重啟」→ 續講字幕與右上面板恢復堆疊 → 可無限次重啟。

---

### 🟡 PM-33：修語音重啟 — getUserMedia 刷新 + 延遲重建

**問題**：🔄 按了語音還是無法恢復。Chrome 的 SpeechRecognition 多次 onend→restart 後進入壞狀態，即使建新實例也啟動不了。

**根因假設**：Chrome 內部的音訊管線（audio pipeline）卡住了，新的 SpeechRecognition 實例連不上麥克風。

**修法**：🔄 按鈕的 handler 裡，先用 `getUserMedia` 強制刷新瀏覽器的音訊連線，等 500ms 讓 Chrome 清理舊資源，再建新的 SpeechRecognition。🔄 的點擊是有效 user gesture，所以 getUserMedia 保證能過。

---

#### inject.ts 改動

**forceRestartVoice() 改為 async**：

```ts
async function forceRestartVoice() {
  blog('手動強制重啟語音');
  if (!voiceActive) return;

  const textEl = document.getElementById('bugezy-caption-text');
  if (textEl) textEl.textContent = '🔄 重啟中...';

  // Step 1：停掉舊的
  try { recognition?.stop(); } catch {}
  recognition = null;

  // Step 2：用 getUserMedia 刷新麥克風連線
  try {
    const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
    // 拿到就立刻釋放（我們只要刷新 audio pipeline，不要 stream）
    stream.getTracks().forEach(t => t.stop());
    blog('getUserMedia 刷新成功');
  } catch (err) {
    blog('getUserMedia 刷新失敗（麥克風可能被封鎖）', err);
    if (textEl) textEl.textContent = '❌ 麥克風無法存取';
    return;
  }

  // Step 3：等 500ms 讓 Chrome 清理
  await new Promise(r => setTimeout(r, 500));

  // Step 4：建全新實例
  recognition = createRecognition();
  if (recognition) {
    try {
      recognition.start();
      if (textEl) textEl.textContent = '🔴 語音已重啟，繼續說...';
      blog('語音強制重啟成功');
    } catch (err) {
      blog('語音強制重啟失敗', err);
      if (textEl) textEl.textContent = '❌ 語音重啟失敗，請重新整理頁面';
    }
  }
}
```

**🔄 按鈕的 event listener 也要改成 async**：
```ts
restartBtn.addEventListener('click', async () => {
  restartBtn.disabled = true;
  await forceRestartVoice();
  restartBtn.disabled = false;
});
```

**同時改善 onend 自動重啟**：加一個失敗計數器，連續失敗 3 次就不再自動重啟，改為等使用者按 🔄：

在 `createRecognition()` 裡：
```ts
let autoRestartFails = 0;

rec.onend = () => {
  if (voiceActive) {
    blog('SpeechRecognition onend → auto restart');
    try {
      rec.start();
      autoRestartFails = 0; // 成功就歸零
    } catch {
      autoRestartFails++;
      blog(`auto restart 失敗 (第 ${autoRestartFails} 次)`);
      if (autoRestartFails >= 3) {
        // 連續失敗 3 次，停止自動重啟，等使用者按 🔄
        const textEl = document.getElementById('bugezy-caption-text');
        if (textEl) textEl.textContent = '⚠ 語音中斷，按 🔄 重啟';
        blog('auto restart 連續失敗 3 次，等待手動重啟');
      }
    }
  }
};
```

**🔄 成功後重置失敗計數器**：在 `forceRestartVoice()` 成功的地方加 `autoRestartFails = 0;`

⚠ `autoRestartFails` 要放在 `createRecognition()` 外面（函式外的閉包層級），這樣每次建新實例不會重置。

---

#### 不改的東西

- 右上面板：不動
- 底部字幕條結構：不動
- edit-report / annotate / server / web：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 錄製 → 講中文 → 字幕正常
2. 故意靜默 15 秒 → 等字幕顯示「⚠ 語音中斷，按 🔄 重啟」
3. 按 🔄 → 字幕顯示「🔄 重啟中...」→ 短暫等待 → 「🔴 語音已重啟，繼續說...」
4. **繼續講中文 → 字幕恢復 → 右上面板繼續堆疊** ← 這是關鍵
5. 再斷再按 🔄 → 第二次也能成功
6. 試第三次重啟 → 也能成功

---

### 🔵 DONE-33：修語音重啟 — getUserMedia 刷新 + 延遲重建 ✅

**改動**：`extension/src/inject.ts` 一個檔（右上面板/字幕條結構、edit-report/annotate/server/web 皆不動）。

**根因假設**：Chrome 多次 `onend→start()` 後內部音訊管線卡死，即使 `createRecognition()` 建新實例也連不上麥克風。

**修法（照規格 4 點）**：
1. **`forceRestartVoice()` 改 `async`**：先顯示「🔄 重啟中...」→ `recognition?.stop()` 丟棄 → **`await getUserMedia({audio:true})` 強制刷新音訊連線**（拿到 stream 立刻 `t.stop()` 釋放；🔄 點擊是有效 user gesture 故保證能過，失敗顯示「❌ 麥克風無法存取」並 return）→ **`await 500ms`** 讓 Chrome 清理舊資源 → `createRecognition()` + `start()`，成功顯示「🔴 語音已重啟，繼續說...」，失敗顯示「❌ 語音重啟失敗，請重新整理頁面」。
2. **🔄 按鈕 listener 改 `async`**：點擊時 `restartBtn.disabled = true` → `await forceRestartVoice()` → `disabled = false`（重啟過程中防連點）。
3. **`autoRestartFails` 計數器**：宣告在 `createRecognition()` **外層閉包**（建新實例不重置）。`onend` 自動重啟成功歸零、失敗 `++`；**連續失敗 3 次**才顯示「⚠ 語音中斷，按 🔄 重啟」並停手等手動（避免每次失敗都洗字幕）。
4. **手動重啟成功重置計數**：`forceRestartVoice()` 成功 `start()` 後 `autoRestartFails = 0`。

**build**：`npm run build` ✅（inject.js 405.9 → 406.8kb，無錯）。

**待 FOX 瀏覽器驗收**：靜默 15 秒 → 連續失敗 3 次顯示「⚠ 語音中斷，按 🔄 重啟」→ 按 🔄 顯示「🔄 重啟中...」→（getUserMedia 刷新 + 500ms）→「🔴 語音已重啟，繼續說...」→ 續講字幕與右上面板恢復堆疊 → 第二、三次重啟也成功。（getUserMedia / SpeechRecognition headless 無法驗，須 FOX 實機。）

---

### 🟡 PM-34：修頁面跳轉資料全丟（即時 flush 到 background 暫存）

**問題**：錄製中使用者點連結跳到新頁面，inject.ts 被銷毀，所有 rrweb events、voice segments、console logs、network errors 全部消失。voice_count=0, rrweb_count=0。

**根因**：所有資料都只存在 inject.ts 的 JavaScript 記憶體變數裡，直到 STOP 才一次打包。頁面跳轉 = 記憶體清空。

**修法**：inject.ts 每收到資料就**即時透過 postMessage 送給 content.ts → background.ts 暫存到 chrome.storage.local**。STOP 時 background 從 storage 讀回完整資料打包。

---

#### §1 新增暫存 storage keys（types.ts）

```ts
export const BUFFER_VOICE_KEY = 'bugezy:buffer:voice';
export const BUFFER_CONSOLE_KEY = 'bugezy:buffer:console';
export const BUFFER_NETWORK_KEY = 'bugezy:buffer:network';
export const BUFFER_RRWEB_KEY = 'bugezy:buffer:rrweb';
```

#### §2 新增 flush 訊息（types.ts ControlMessage）

inject → content → background 的即時 flush 訊息：

```ts
| { type: 'FLUSH_VOICE'; segment: VoiceSegment }
| { type: 'FLUSH_CONSOLE'; log: ConsoleLog }
| { type: 'FLUSH_NETWORK'; error: NetworkError }
| { type: 'FLUSH_RRWEB'; events: unknown[] }  // 批次，每 5 秒一次
```

#### §3 inject.ts 改動

**語音**：`onresult` 裡每收到 final segment 立即 flush：
```ts
if (text) {
  const seg: VoiceSegment = { text, timestamp: Date.now(), isFinal: true };
  voiceSegments.push(seg);  // 本地也存（同頁 STOP 用）
  // 即時 flush 給 content → background
  window.postMessage({
    source: BUGEZY_SOURCE,
    dir: 'to-content',
    kind: 'FLUSH_VOICE',
    segment: seg,
  }, '*');
}
```

**Console**：console.warn/error 攔截到時立即 flush：
```ts
consoleLogs.push(entry);
window.postMessage({
  source: BUGEZY_SOURCE,
  dir: 'to-content',
  kind: 'FLUSH_CONSOLE',
  log: entry,
}, '*');
```

**Network**：fetch/XHR 4xx/5xx 攔截到時立即 flush：
```ts
networkErrors.push(entry);
window.postMessage({
  source: BUGEZY_SOURCE,
  dir: 'to-content',
  kind: 'FLUSH_NETWORK',
  error: entry,
}, '*');
```

**rrweb**：事件太頻繁不能每筆都 flush。用定時器每 5 秒批次 flush：
```ts
let lastFlushedIndex = 0;
const rrwebFlushInterval = setInterval(() => {
  if (events.length > lastFlushedIndex) {
    const batch = events.slice(lastFlushedIndex);
    lastFlushedIndex = events.length;
    window.postMessage({
      source: BUGEZY_SOURCE,
      dir: 'to-content',
      kind: 'FLUSH_RRWEB',
      events: batch,
    }, '*');
  }
}, 5000);

// stopRecording() 裡清除 interval：
clearInterval(rrwebFlushInterval);
// 同時 flush 最後一批：
const finalBatch = events.slice(lastFlushedIndex);
if (finalBatch.length > 0) {
  window.postMessage({ source: BUGEZY_SOURCE, dir: 'to-content', kind: 'FLUSH_RRWEB', events: finalBatch }, '*');
}
```

#### §4 content.ts 改動

在 `window.addEventListener('message')` handler 裡，收到 FLUSH 訊息轉發給 background：

```ts
if (data.kind === 'FLUSH_VOICE') {
  chrome.runtime.sendMessage({ type: 'FLUSH_VOICE', segment: data.segment });
} else if (data.kind === 'FLUSH_CONSOLE') {
  chrome.runtime.sendMessage({ type: 'FLUSH_CONSOLE', log: data.log });
} else if (data.kind === 'FLUSH_NETWORK') {
  chrome.runtime.sendMessage({ type: 'FLUSH_NETWORK', error: data.error });
} else if (data.kind === 'FLUSH_RRWEB') {
  chrome.runtime.sendMessage({ type: 'FLUSH_RRWEB', events: data.events });
}
```

#### §5 background.ts 改動

**START 時清空所有 buffer**：
```ts
await chrome.storage.local.set({
  [BUFFER_VOICE_KEY]: [],
  [BUFFER_CONSOLE_KEY]: [],
  [BUFFER_NETWORK_KEY]: [],
  [BUFFER_RRWEB_KEY]: [],
});
```

**收到 FLUSH 訊息時追加到 buffer**：
```ts
case 'FLUSH_VOICE': {
  const r = await chrome.storage.local.get(BUFFER_VOICE_KEY);
  const arr = r[BUFFER_VOICE_KEY] ?? [];
  arr.push((msg as any).segment);
  await chrome.storage.local.set({ [BUFFER_VOICE_KEY]: arr });
  break;
}
case 'FLUSH_CONSOLE': {
  const r = await chrome.storage.local.get(BUFFER_CONSOLE_KEY);
  const arr = r[BUFFER_CONSOLE_KEY] ?? [];
  arr.push((msg as any).log);
  await chrome.storage.local.set({ [BUFFER_CONSOLE_KEY]: arr });
  break;
}
case 'FLUSH_NETWORK': {
  const r = await chrome.storage.local.get(BUFFER_NETWORK_KEY);
  const arr = r[BUFFER_NETWORK_KEY] ?? [];
  arr.push((msg as any).error);
  await chrome.storage.local.set({ [BUFFER_NETWORK_KEY]: arr });
  break;
}
case 'FLUSH_RRWEB': {
  const r = await chrome.storage.local.get(BUFFER_RRWEB_KEY);
  const arr = r[BUFFER_RRWEB_KEY] ?? [];
  arr.push(...((msg as any).events));
  await chrome.storage.local.set({ [BUFFER_RRWEB_KEY]: arr });
  break;
}
```

**RECORDING_DONE / uploadPayload() 改動**：

上傳前，從 buffer 讀取累積的資料，合併成完整的 payload（取代 inject.ts 的單次打包）：

```ts
async function buildFullPayload(): Promise<RecordingPayload> {
  const [voiceR, consoleR, networkR, rrwebR, payloadR] = await Promise.all([
    chrome.storage.local.get(BUFFER_VOICE_KEY),
    chrome.storage.local.get(BUFFER_CONSOLE_KEY),
    chrome.storage.local.get(BUFFER_NETWORK_KEY),
    chrome.storage.local.get(BUFFER_RRWEB_KEY),
    chrome.storage.local.get(STORAGE_KEY),
  ]);

  // inject 的最後一次打包（如果有的話，STOP 時 inject 會送一次）
  const injectPayload = payloadR[STORAGE_KEY];
  
  // 合併 buffer + inject 的資料
  return {
    rrwebEvents: [...(rrwebR[BUFFER_RRWEB_KEY] ?? []), ...(injectPayload?.rrwebEvents ?? [])],
    consoleLogs: [...(consoleR[BUFFER_CONSOLE_KEY] ?? []), ...(injectPayload?.consoleLogs ?? [])],
    networkErrors: [...(networkR[BUFFER_NETWORK_KEY] ?? []), ...(injectPayload?.networkErrors ?? [])],
    voiceTranscript: [...(voiceR[BUFFER_VOICE_KEY] ?? []), ...(injectPayload?.voiceTranscript ?? [])],
    screenshots: injectPayload?.screenshots ?? [],
    pageInfo: injectPayload?.pageInfo ?? { url: '', title: '', browser: '', screenSize: '', timestamp: '' },
  };
}
```

⚠ **去重**：buffer 裡的資料和 inject 最後打包的資料可能有重複（同頁 STOP 的情況）。用 timestamp 去重：
```ts
// voice 去重（同 timestamp 的只保留一筆）
const seen = new Set();
payload.voiceTranscript = payload.voiceTranscript.filter(s => {
  const key = `${s.timestamp}-${s.text}`;
  if (seen.has(key)) return false;
  seen.add(key);
  return true;
});
```

**CLEAR 時清除 buffer**：
```ts
chrome.storage.local.remove([BUFFER_VOICE_KEY, BUFFER_CONSOLE_KEY, BUFFER_NETWORK_KEY, BUFFER_RRWEB_KEY]);
```

#### §6 不改的東西

- annotate / popup / edit-report：不動
- server / web：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 開 ruten.com.tw → F5 → 按「🎬 錄製」→ 講幾句中文
2. **點一個連結跳到商品頁**（頁面跳轉！）
3. 在新頁面繼續講幾句
4. 按「停止」
5. 編輯頁應看到：
   - voice_count > 0（包含跳頁前講的 + 跳頁後講的）
   - rrweb_count > 0（包含兩個頁面的 DOM 事件）
6. 上傳 → MCP 讀到完整資料

---

### 🔵 DONE-34：頁面跳轉資料即時 flush 暫存 ✅（架構改動）

**改動**：`types.ts` + `inject.ts` + `content.ts` + `background.ts`（annotate/popup/edit-report/server/web 皆不動）。

**訊息鏈**：`inject.ts（MAIN world）`──postMessage──▶`content.ts（ISOLATED world）`──chrome.runtime.sendMessage──▶`background.ts（Service Worker）`──▶`chrome.storage.local` buffer。

**§1 types.ts**：新增 4 個 buffer key（`bugezy:buffer:{voice,console,network,rrweb}`）；`ControlMessage` 加 `FLUSH_VOICE/CONSOLE/NETWORK/RRWEB`（content→background）；`InjectMessage` 加同名 `to-content` flush 變體（inject→content，走既有 `post()` 型別）。

**§2 inject.ts**：
- voice：onresult 每筆 final → `push` 本地 + `post(FLUSH_VOICE)`。
- console：warn/error 攔到 → `push` + `post(FLUSH_CONSOLE)`。
- network：fetch + XHR 4xx/5xx → `push` + `post(FLUSH_NETWORK)`。
- rrweb：太頻繁不逐筆，`setInterval` **每 5 秒**批次 flush `events.slice(lastFlushedIndex)`；`startRecording` 重置 `lastFlushedIndex=0` 並起 interval；`stopRecording` `clearInterval` + flush 最後一批。

**§3 content.ts**：message handler 收到 4 種 FLUSH → 原樣 `chrome.runtime.sendMessage` 轉發 background（content.js 仍 0 top-level import/export，已驗）。

**§4 background.ts**：
- START：先把 4 個 buffer 設為 `[]`（清上一場殘留）。
- FLUSH_*：各自讀 buffer→`push`→寫回。
- **`buildFullPayload()`**：`Promise.all` 讀 4 buffer + STORAGE_KEY，合併 `[...buffer, ...inject單頁]`。
- **RECORDING_DONE**：呼叫 `buildFullPayload()` → 覆寫 STORAGE_KEY → 摘要計數改用合併後（domEvents/console/network/voice/pageInfo）。
- CLEAR：`remove` 一併清 4 buffer；上傳成功也 `remove` buffer。

**⚠ 我據實做的兩處工程判斷（請 PM 知悉）**：
1. **去重擴及四類，不只 voice**：規格 `buildFullPayload` 是 `[...buffer, ...inject]`，但每筆資料「即時 flush 進 buffer」+「inject 在 STOP 又帶整包單頁」→ **最終頁四類資料都會重複**（規格只示範 voice 去重）。若不補去重，rrweb 會 ×2 直接破壞回放、計數灌水。故四類都做去重：voice=`timestamp-text`、console=`timestamp-level-message`、network=`timestamp-method-url-status`、rrweb=`JSON.stringify(event)`。
2. **跳頁後的「新頁面」目前不會被錄到**：本 PM 只做 flush/buffer/合併，**未含「導航後在新頁重新 START 錄製」**（不在指派範圍）。因此驗收步驟 3「在新頁面繼續講」目前不會進資料——但本 PM 的核心價值已達成：**跳頁前已 flush 的 voice/console/network/rrweb 不再全丟**（之前 voice_count=0/rrweb_count=0 的根因解除）。若要讓導航後的新頁也錄，需另開 PM（background 監聽 tab 導航重送 START，或 content 依持久化 state 自動續錄）。

**build**：`npm run build` ✅（inject.js 408.2kb / content.js 11.7kb / background.js 10.6kb）＋ `tsc --noEmit` ✅。

**待 FOX 瀏覽器驗收**：錄製講話 → flush 進 buffer → 跳頁 → 停止 → 編輯頁 voice_count/rrweb_count 應 **保住跳頁前的量**（不再歸零）。導航後新頁的錄製需待後續 PM。

---

### 🟡 PM-35：修跳頁後新頁面不繼續錄製（content.ts 載入時自動恢復）

**根因**：START_RECORDING 只送一次給第一個頁面。使用者跳頁後，新頁面的 content.ts + inject.ts 不知道在錄製中，所以完全不動。

**修法**：content.ts 載入時主動問 background「現在是否在錄製？」，如果是就自動送 START 給 inject.ts，讓新頁面也開始錄。

---

#### content.ts 改動

在檔案最後面（或 `chrome.runtime.onMessage.addListener` 之前），加**載入時自動恢復錄製**：

```ts
// PM-35：頁面載入時自動恢復錄製
// 使用者跳頁後，新頁面的 content.ts 需要檢查是否正在錄製中
(async () => {
  try {
    const state = await chrome.runtime.sendMessage({ type: 'GET_STATE' }) as StateResponse;
    if (state?.recording) {
      blog('偵測到正在錄製中，自動恢復 inject 錄製');
      // 等 inject 報到後再送 START
      const waitForInject = () => {
        if (injectReady) {
          sendToInject('START');
          blog('已送 START 給 inject（跳頁恢復）');
        } else {
          // inject 還沒 READY，等 100ms 再試
          setTimeout(waitForInject, 100);
        }
      };
      waitForInject();
    }
  } catch {
    // GET_STATE 失敗（background 未就緒），忽略
  }
})();
```

**重點**：
- `GET_STATE` 是 background 已有的 handler，回傳 `{ recording: boolean, ... }`
- 要等 inject 的 `READY` 訊號才能送 START（否則 inject 還沒初始化完畢）
- 用 `setTimeout` poll 等待 `injectReady`，最多等幾秒
- 加一個最大重試次數避免無限 loop：

```ts
const waitForInject = (retries = 0) => {
  if (injectReady) {
    sendToInject('START');
    blog('已送 START 給 inject（跳頁恢復）');
  } else if (retries < 30) {  // 最多等 3 秒
    setTimeout(() => waitForInject(retries + 1), 100);
  } else {
    blog('⚠ inject 未就緒，跳頁恢復失敗');
  }
};
waitForInject();
```

---

#### inject.ts 確認

inject.ts 收到 START 時，如果已經在錄製中（`recording = true`），應該**不要重複啟動**（已有這個防護：`if (recording) { blog('START 重複呼叫'); return; }`）。新頁面的 inject.ts 是全新的（`recording = false`），所以會正常啟動。✅ 不需改。

#### background.ts 確認

`GET_STATE` handler 已存在，回傳 `{ recording, startedAt, ... }`。✅ 不需改。

---

#### 預期行為

```
使用者在頁面 A 開始錄製
  → 頁面 A：inject 錄 rrweb + voice + console + network
  → 資料即時 flush 到 background buffer

使用者跳到頁面 B
  → 頁面 A：inject 銷毀（最後一批資料已 flush）
  → 頁面 B：content.ts 載入 → 問 GET_STATE → recording=true
  → content.ts 等 inject READY → 送 START
  → 頁面 B：inject 開始新一輪錄製 + 顯示字幕
  → 新資料繼續 flush 到 buffer

使用者按停止
  → 頁面 B：inject 打包當前頁的資料
  → background：合併 buffer（頁面 A + B 的所有資料）→ 完整 payload
```

---

#### 不改的東西

- inject.ts：不改（已有 START 防重複保護）
- background.ts：不改（GET_STATE 已有）
- server / web / annotate / edit-report：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 開 ruten.com.tw → F5 → 按「🎬 錄製」→ 講幾句中文
2. **點一個商品連結跳到新頁面**
3. **新頁面應自動恢復**：底部字幕重新出現 + 右上面板重新出現
4. 在新頁面繼續講中文
5. 按「停止」
6. 編輯頁 voice_count 應包含**兩個頁面的語音**
7. rrweb_count 也應包含兩個頁面的 DOM 事件

---

### 🔵 DONE-35：跳頁後新頁面自動恢復錄製 ✅（接 DONE-34 第 2 點）

**改動**：只改 `extension/src/content.ts`（inject.ts / background.ts / 其餘皆不動，與規格一致）。

- content.ts 載入時（檔尾加一個 IIFE）主動 `chrome.runtime.sendMessage({ type: 'GET_STATE' })` 問 background 目前是否在錄製。
- 若 `state.recording === true` → 等 inject 的 `READY`（`injectReady` 旗標）後 `sendToInject('START')`。
- `waitForInject(retries)` 每 100ms poll，**最多 30 次（3 秒）**，逾時印「⚠ inject 未就緒，跳頁恢復失敗」；`GET_STATE` 失敗（background 未就緒）以 try/catch 吞掉。
- 新頁面的 inject 是全新實例（`recording=false`），收到 START 正常啟動；同頁原 inject 的 `if (recording) return` 防重複保護不受影響。

**為何只改 content.ts 就夠**：`GET_STATE` handler、inject 的 START 防重複都已存在（規格已確認），故 inject.ts / background.ts 不動。

**串起 DONE-34 的完整鏈**：頁面 A 錄製→即時 flush 進 buffer→跳到頁面 B→B 的 content 問 GET_STATE=true→等 inject READY→送 START→B 也開始錄+顯示字幕+繼續 flush→停止→background `buildFullPayload()` 合併 A+B 全部資料（去重）。**至此 DONE-34 標註的「新頁不錄」缺口補上**。

**build**：`npm run build` ✅（content.js 11.7 → 12.4kb）＋ `tsc --noEmit` ✅；content.js 仍 0 top-level import/export（已驗）。

**待 FOX 瀏覽器驗收**：A 頁錄製講話 → 點連結跳 B 頁 → **B 頁底部字幕 + 右上面板自動重現** → 續講 → 停止 → 編輯頁 voice_count / rrweb_count 含**兩頁**資料。

---

### 🟡 PM-36：修跳頁後右上面板清空 + 加速語音恢復

**問題 1**：跳頁後右上「📝 語音記錄」面板是空的，看不到之前講的內容。
**修法**：inject.ts 建面板時，從 background buffer 讀取已累積的語音文字填入。

**問題 2**：跳頁後 ~1 秒語音才恢復，不夠滑順。
**修法**：縮短 poll 間隔 + 語音初始化不等 rrweb 完成。

---

#### §1 修右上面板：載入 buffer 歷史文字

**inject.ts 改動**：

在 `showCaptionBar()` 建完右上面板後，透過 postMessage 請 content.ts 幫忙讀 buffer：

```ts
// 建完面板後，請求歷史語音文字
window.postMessage({
  source: BUGEZY_SOURCE,
  dir: 'to-content',
  kind: 'REQUEST_VOICE_HISTORY',
}, '*');
```

在 inject.ts 的 message listener 裡加一個接收 handler：
```ts
// 收到歷史語音文字 → 填入右上面板
if (data.kind === 'VOICE_HISTORY') {
  const voiceContent = document.getElementById('bugezy-voice-content');
  if (voiceContent && data.segments && data.segments.length > 0) {
    voiceContent.textContent = data.segments.map((s: any) => s.text).join('\n');
    const panel = document.getElementById('bugezy-voice-panel');
    if (panel) panel.scrollTop = panel.scrollHeight;
    blog('載入歷史語音', data.segments.length, '段');
  }
}
```

**content.ts 改動**：

收到 REQUEST_VOICE_HISTORY → 從 background 讀 buffer → 回傳給 inject：

```ts
if (data.kind === 'REQUEST_VOICE_HISTORY') {
  chrome.runtime.sendMessage({ type: 'GET_VOICE_BUFFER' }, (response) => {
    if (response?.segments) {
      window.postMessage({
        source: BUGEZY_SOURCE,
        dir: 'to-inject',
        kind: 'VOICE_HISTORY',
        segments: response.segments,
      }, '*');
    }
  });
  return;
}
```

⚠ 注意：這裡 inject 收訊息的 `dir` 是 `'to-inject'`。inject.ts 的 message listener 目前可能只聽 `'to-inject'` 方向的 `START` / `STOP`，要確認能收到 `VOICE_HISTORY`。

**background.ts 改動**：

新增 GET_VOICE_BUFFER handler：
```ts
case 'GET_VOICE_BUFFER': {
  const r = await chrome.storage.local.get(BUFFER_VOICE_KEY);
  sendResponse({ segments: r[BUFFER_VOICE_KEY] ?? [] });
  break;
}
```

**types.ts 改動**：

InjectMessage 加：
```ts
| { source: typeof BUGEZY_SOURCE; dir: 'to-content'; kind: 'REQUEST_VOICE_HISTORY' }
| { source: typeof BUGEZY_SOURCE; dir: 'to-inject'; kind: 'VOICE_HISTORY'; segments: VoiceSegment[] }
```

ControlMessage 加：
```ts
| { type: 'GET_VOICE_BUFFER' }
```

---

#### §2 加速語音恢復

**content.ts 改動**：

PM-35 的自動恢復，縮短 poll 間隔從 100ms → 50ms：
```ts
const waitForInject = (retries = 0) => {
  if (injectReady) {
    sendToInject('START');
    blog('已送 START 給 inject（跳頁恢復）');
  } else if (retries < 40) {  // 40 * 50ms = 2 秒
    setTimeout(() => waitForInject(retries + 1), 50);  // 從 100ms 改 50ms
  } else {
    blog('⚠ inject 未就緒，跳頁恢復失敗');
  }
};
```

**inject.ts 改動**：

`startRecording()` 裡，語音初始化**不要等 rrweb 完成**，同時啟動：

目前如果是順序執行的話：
```ts
// 先 rrweb → 再 voice → 再 caption
```
改成並行：
```ts
// rrweb 和 voice 同時啟動，不互相等待
startRrweb();
startVoice();  // 不需要等 rrweb 完成
showCaptionBar();
```

確認 `startRecording()` 裡 rrweb 和語音初始化沒有順序依賴就好。如果已經是並行的就不用改。

---

#### §3 不改的東西

- annotate / edit-report / popup：不動
- server / web：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 開網頁 → F5 → 按「🎬 錄製」→ 講幾句
2. 右上面板堆疊了文字
3. **跳到新頁面**
4. 右上面板應**自動載入之前的文字**（不是空的）
5. 語音恢復應比之前快（~0.5 秒內）
6. 繼續講 → 新文字接在舊文字後面堆疊

---

### 🔵 DONE-36：跳頁後右上面板回填歷史 + 加速語音恢復 ✅

**改動**：`types.ts` + `inject.ts` + `content.ts` + `background.ts`（annotate/edit-report/popup/server/web 皆不動）。

**§1 右上面板回填歷史語音**（新訊息往返：inject → content → background → content → inject）：
- **types.ts**：`InjectMessage` 加 `REQUEST_VOICE_HISTORY`（to-content）+ `VOICE_HISTORY`（**to-inject**，帶 `segments: VoiceSegment[]`）；`ControlMessage` 加 `GET_VOICE_BUFFER`。
- **inject.ts**：`showCaptionBar()` 建完右上面板後 `post(REQUEST_VOICE_HISTORY)`；message listener（原只認 `to-inject` 的 `cmd`）擴充成也認 `kind === 'VOICE_HISTORY'` → 把 `segments.map(s=>s.text).join('\n')` 填進 `#bugezy-voice-content` 並捲到底。
- **content.ts**：收到 `REQUEST_VOICE_HISTORY` → `sendMessage(GET_VOICE_BUFFER)` 拿 buffer → `postMessage(VOICE_HISTORY, dir:'to-inject')` 回填 inject（有段才回）。
- **background.ts**：新增 `GET_VOICE_BUFFER` → 回 `{ segments: BUFFER_VOICE_KEY }`。

**§2 加速語音恢復**：
- **content.ts**：PM-35 的 `waitForInject` poll 由 **100ms→50ms**、上限 `retries<30`→`<40`（仍約 2 秒封頂），跳頁恢復更滑順。
- **inject.ts**：確認 `startRecording()` 裡 rrweb（`record()` 同步、不 await）與語音初始化**無順序依賴、本來就並行**，故依規格「已並行就不用改」維持不動。

**訊息方向自洽**：`REQUEST_VOICE_HISTORY` 是 `to-content`（content 處理、inject 自己忽略）；`VOICE_HISTORY` 是 `to-inject`（inject 處理、content 自己忽略，dir guard 擋住，不會迴圈）。content.js 仍 0 top-level import/export（已驗）。

**build**：`npm run build` ✅（inject 408.8 / content 12.7 / background 10.8kb）＋ `tsc --noEmit` ✅。

**待 FOX 瀏覽器驗收**：A 頁講話堆疊右上面板 → 跳 B 頁 → **B 頁右上面板自動載入 A 頁的文字（非空）** → 語音 ~0.5 秒內恢復 → 續講新文字接在舊文字後堆疊。

---

### 🟡 PM-37：修跳頁後錄製 UI 不恢復（READY 競爭條件）

**根因**：Chrome 同時載入 inject.js（MAIN）和 content.js（ISOLATED），順序不保證。如果 inject 先載完，READY 訊息送出時 content 還沒聽 → READY 丟失 → content 的 waitForInject 永遠等不到 → 不送 START → 新頁面不錄製。

**修法**：inject.ts 重複發 READY（每 100ms），直到收到 content 的 ACK 才停。

---

#### inject.ts 改動

把最後面的單次 `post(READY)` 改成重複發送：

```ts
// 原本：
// post({ source: BUGEZY_SOURCE, dir: 'to-content', kind: 'READY' });

// 改為：重複發 READY 直到收到 ACK
let readyAcked = false;
const readyInterval = setInterval(() => {
  if (readyAcked) {
    clearInterval(readyInterval);
    return;
  }
  post({ source: BUGEZY_SOURCE, dir: 'to-content', kind: 'READY' });
}, 100);

// 5 秒後停止（避免無限發送）
setTimeout(() => clearInterval(readyInterval), 5000);
```

在 inject.ts 的 message listener 裡加收 ACK：

```ts
if (data.kind === 'READY_ACK') {
  readyAcked = true;
  blog('收到 READY_ACK');
}
```

#### content.ts 改動

收到 READY 後**回 ACK**：

```ts
if (data.kind === 'READY') {
  injectReady = true;
  blog('✓ inject 已報到（READY）');
  // 回 ACK 讓 inject 停止重複發 READY
  window.postMessage({
    source: BUGEZY_SOURCE,
    dir: 'to-inject',
    kind: 'READY_ACK',
  }, '*');
  return;
}
```

#### types.ts 改動

InjectMessage / InjectCommand 加：
```ts
| { source: typeof BUGEZY_SOURCE; dir: 'to-inject'; kind: 'READY_ACK' }
```

---

#### 不改的東西

- background / server / web / annotate / edit-report：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 開網頁 → F5 → 按「🎬 錄製」→ 講幾句 → 右上面板有文字
2. **點連結跳新頁面**
3. **新頁面 1-2 秒內應自動出現字幕條 + 右上面板**
4. 講中文 → 字幕跟著 → 面板堆疊
5. 停止 → 編輯頁開 → voice_count 包含兩頁語音

---

### 🔵 DONE-37：修 READY 競爭條件（重複發 READY + ACK 握手）✅

**改動**：`types.ts` + `inject.ts` + `content.ts`（background/server/web/annotate/edit-report 皆不動，與規格一致）。

**根因**：Chrome 載入 inject.js（MAIN）與 content.js（ISOLATED）順序不保證。若 inject 先載完，單次 `READY` 送出時 content 還沒掛 listener → READY 丟失 → content 的 `waitForInject`（PM-35）永遠等不到 `injectReady` → 不送 START → 跳頁後新頁不錄製、UI 不恢復。

**修法（握手）**：
- **types.ts**：`InjectMessage` 加 `READY_ACK`（to-inject）。
- **inject.ts**：把檔尾單次 `post(READY)` 改成 `setInterval` **每 100ms 重發 READY**，收到 `READY_ACK` 即 `clearInterval`；另 `setTimeout` 5 秒保險一定停（避免無限發送）。message listener 加 `kind === 'READY_ACK'` → `readyAcked = true`。
- **content.ts**：收到 `READY` 時除了 `injectReady = true`，再 `postMessage(READY_ACK, dir:'to-inject')` 回 inject。content 每次收到 READY 都回 ACK（冪等），inject 收到第一個 ACK 就停發。

**方向自洽**：`READY` 是 to-content（content 處理）、`READY_ACK` 是 to-inject（inject 處理、content dir guard 忽略）→ 不迴圈。content.js 仍 0 top-level import/export（已驗）。

**build**：`npm run build` ✅（inject 409.1 / content 12.8kb）＋ `tsc --noEmit` ✅。

**待 FOX 瀏覽器驗收**：跳頁後新頁面 1-2 秒內穩定出現字幕條 + 右上面板（不再因載入順序競爭而偶發不恢復）→ 續講 → 停止 → 編輯頁 voice_count 含兩頁。

---

### 🟡 PM-38：修編輯頁 mini player（放大鏡問題 + 開頭卡頓）

**問題 1**：rrweb player 用原始解析度渲染（例如 1600x900），但容器只有 960px 寬，沒有縮放 → 只看到頁面左上角一小塊，像拿放大鏡看報紙。

**問題 2**：播放開頭極慢/卡住，手動跳轉後才正常。跨頁錄製的事件之間有大段時間差。

---

#### edit-report.ts 改動

**修 1：自動縮放 iframe 到容器大小**

Replayer 建好後，計算縮放比例並套用 CSS transform：

```ts
function initMiniPlayer(events: unknown[]) {
  const container = document.getElementById('miniPlayer');
  if (!container || events.length < 2) {
    document.getElementById('markerSection')!.style.display = 'none';
    return;
  }

  replayer = new Replayer(events as any, {
    root: container,
    skipInactive: true,
    showWarning: false,
  });

  // 取得錄製的原始尺寸
  const meta = replayer.getMetaData();
  duration = meta.totalTime || 0;

  // 等 iframe 渲染後計算縮放
  requestAnimationFrame(() => {
    const iframe = container.querySelector('iframe');
    if (iframe) {
      // 取得原始頁面尺寸（從 rrweb meta event）
      const metaEvent = events.find((e: any) => e.type === 4); // type 4 = Meta
      const pageWidth = (metaEvent as any)?.data?.width || 1920;
      const pageHeight = (metaEvent as any)?.data?.height || 1080;

      // 計算容器寬度和縮放比例
      const containerWidth = container.clientWidth;
      const scale = containerWidth / pageWidth;

      // 設定 iframe 為原始尺寸，用 transform 縮放
      iframe.style.width = pageWidth + 'px';
      iframe.style.height = pageHeight + 'px';
      iframe.style.transform = `scale(${scale})`;
      iframe.style.transformOrigin = 'top left';
      iframe.style.border = 'none';

      // 容器高度配合縮放後的高度
      container.style.height = (pageHeight * scale) + 'px';
      container.style.overflow = 'hidden';
    }
  });

  // ... 播放控制邏輯不變 ...
}
```

**修 2：開頭卡頓 — 播放時跳過前面的空白**

```ts
// 播放按鈕 handler 改動：
document.getElementById('markerPlayBtn')!.addEventListener('click', () => {
  if (!replayer) return;
  if (playing) {
    replayer.pause();
    playing = false;
    document.getElementById('markerPlayBtn')!.textContent = '▶';
  } else {
    const current = replayer.getCurrentTime();
    if (current >= duration - 100) {
      // 從頭播
      replayer.play(0);
    } else {
      replayer.resume(current);
    }
    playing = true;
    document.getElementById('markerPlayBtn')!.textContent = '⏸';
    trackProgress();
  }
});
```

Replayer 的 `skipInactive: true` 應該已經處理空白，但如果還是卡，可以加 `speed` 參數讓回放加速：

```ts
replayer = new Replayer(events as any, {
  root: container,
  skipInactive: true,
  showWarning: false,
  speed: 1,           // 正常速度
  mouseTail: false,   // 關閉滑鼠軌跡（省資源）
});
```

如果開頭的 FullSnapshot 太大導致卡，可以在播放前先 `replayer.play(0)` 然後立即 `replayer.pause(0)` 預載第一幀：

```ts
// 預載第一幀（避免點播放時卡住在 FullSnapshot 解析）
try {
  replayer.play(0);
  replayer.pause(0);
} catch {}
```

---

#### edit-report.html CSS 改動

```css
.mini-player-container {
  width: 100%;
  max-width: 960px;
  overflow: hidden;
  border-radius: 8px;
  border: 1px solid var(--line, #2a2a3e);
  margin-bottom: 8px;
  background: #000;  /* 黑底讓縮放後邊緣不刺眼 */
}
```

移除 `aspect-ratio` 和固定 `height`（改由 JS 動態計算）。

---

#### 不改的東西

- inject.ts / content.ts / background.ts：不動
- server / web / annotate：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 錄製一段（跳幾頁）→ 停止 → 進編輯頁
2. mini player 應顯示**完整的網頁畫面**（不是放大鏡），能看到整個頁面
3. 按播放 → 應流暢播放，不卡在開頭
4. 📌 標記功能照常可用

---

### 🔵 DONE-38：修編輯頁 mini player（放大鏡 + 開頭卡頓）✅

**改動**：`extension/src/edit-report.ts` + `edit-report.html`（inject/content/background/server/web/annotate 皆不動）。

**問題 1 — 放大鏡（只看到左上角）**：rrweb 以錄製原始解析度（如 1600×900）渲染，但容器只有 960px 寬、無縮放。
**修法**：Replayer 建好後 `requestAnimationFrame` 等 iframe 渲染 → 從 rrweb **Meta 事件（type=4）** 取原始 `data.width/height`（fallback 1920×1080）→ `scale = container.clientWidth / pageWidth` → iframe 設原始寬高 + `transform: scale(...)` + `transform-origin: top left`；容器高度動態設為 `pageHeight × scale`、`overflow:hidden`。整頁完整顯示、不再像放大鏡。

**問題 2 — 開頭卡頓**：點播放卡在開頭 FullSnapshot 解析（跨頁錄製事件間時間差大）。
**修法**：
- Replayer 建立時加 `mouseTail: false`（關滑鼠軌跡、省資源）；`skipInactive: true` 維持（跳過空白）。
- 建好後立即 `replayer.play(0); replayer.pause(0);`（try/catch 包住）**預載第一幀**，把 FullSnapshot 解析成本提前到初始化，點播放就不卡。

**edit-report.html CSS**：`.mini-player-container` 移除 `aspect-ratio:16/9` 與固定 `height`（高度改由 JS 依縮放動態設定），保留 `max-width:960px / overflow:hidden / 黑底`；`iframe` 規則只留 `border:none; transform-origin`（尺寸/縮放由 JS inline 設定，避免與 100% 衝突）。

**build**：`npm run build` ✅ ＋ `tsc --noEmit` ✅（`mouseTail` 為 Replayer 合法選項，型別通過）。

**待 FOX 瀏覽器驗收**：編輯頁 mini player 顯示**完整網頁畫面**（非放大鏡）→ 按播放流暢不卡開頭 → 📌 標記照常可用。

---

### 🟡 PM-39：修語音記錄文字可手動編輯

**問題**：edit-report.html 的語音記錄 textarea 有 `readonly` 屬性，使用者無法手動修正錯別字。

**修法**：移除 `readonly`。

#### edit-report.html 改動

第 129 行：
```html
<!-- 移除 readonly -->
<textarea id="voiceText" rows="3" placeholder="（這次錄製沒有語音）"></textarea>
```

#### 不改的東西
- 其他所有檔案：不動

#### Build
```
cd extension && npm run build
```

---

### 🔵 DONE-39：語音記錄可手動編輯 ✅

**改動**：`extension/src/edit-report.html` 一行（移除 `#voiceText` 的 `readonly`，其他檔案皆不動）。

- 移除後使用者可直接在語音記錄 textarea 修正錯別字。
- 附帶效果：原本 `textarea[readonly] { color: var(--muted) }` 不再套用，文字改用正常 `--fg` 色更清楚——符合「可編輯」的意圖，不另調整。
- AI 精簡（PM-25）仍會覆寫此欄內容；手動編輯與 AI 精簡並存，使用者可在精簡後再微調。

**build**：`npm run build` ✅。

**待 FOX 瀏覽器驗收**：編輯頁語音記錄欄可點進去打字修改。

---

### 🟡 PM-40：UI 調整（語音面板下移 + mini player 放大按鈕）

---

#### §1 語音面板位置下移（inject.ts）

右上「📝 語音記錄」面板的 `top: 60px` 太高，會被瀏覽器書籤列或其他擴充遮擋。

改為 `top: 140px`（大約在藍色箭頭的位置）：

```ts
// 找到 panel 的 style.cssText 裡的 top:60px
// 改成 top:140px
```

---

#### §2 Mini player 加放大/縮小按鈕（edit-report.html + edit-report.ts）

在 mini player 控制列（播放按鈕旁邊）加一個放大按鈕：

**edit-report.html**：在 `.marker-controls` 裡加：
```html
<button id="zoomBtn" class="btn btn-sm" title="放大/縮小">🔍 2x</button>
```

**edit-report.ts**：

```ts
let zoomed = false;
document.getElementById('zoomBtn')!.addEventListener('click', () => {
  const container = document.getElementById('miniPlayer')!;
  const iframe = container.querySelector('iframe');
  if (!iframe) return;

  zoomed = !zoomed;

  if (zoomed) {
    // 放大 2 倍：scale 變成原本的 2 倍，容器高度也跟著
    const currentScale = parseFloat(iframe.style.transform.replace('scale(', '').replace(')', '')) || 0.5;
    const newScale = currentScale * 2;
    iframe.style.transform = `scale(${newScale})`;
    container.style.height = (parseInt(iframe.style.height) * newScale) + 'px';
    document.getElementById('zoomBtn')!.textContent = '🔍 1x';
  } else {
    // 縮回原始大小
    const metaEvent = /* 從 events 找 type===4 的 meta */ null;
    // 重新計算原始 scale
    const containerWidth = container.clientWidth;
    const pageWidth = parseInt(iframe.style.width) || 1920;
    const originalScale = containerWidth / pageWidth;
    iframe.style.transform = `scale(${originalScale})`;
    container.style.height = (parseInt(iframe.style.height) * originalScale) + 'px';
    document.getElementById('zoomBtn')!.textContent = '🔍 2x';
  }
});
```

⚠ 放大後容器要加 `overflow: auto`（讓使用者可以捲動看完整頁面），縮小時改回 `overflow: hidden`。

```ts
if (zoomed) {
  container.style.overflow = 'auto';
} else {
  container.style.overflow = 'hidden';
}
```

---

#### 不改的東西
- content.ts / background.ts / server / web：不動

#### Build
```
cd extension && npm run build
```

---

### 🔵 DONE-40：UI 調整（語音面板下移 + mini player 放大鈕）✅

**改動**：`inject.ts` + `edit-report.html` + `edit-report.ts`（content/background/server/web/annotate 皆不動）。

**§1 語音面板下移（inject.ts）**：右上「📝 語音記錄」面板 `top:60px → 140px`，避免被瀏覽器書籤列/其他擴充按鈕遮擋。

**§2 mini player 放大鈕（edit-report.html + edit-report.ts）**：
- html：`.marker-controls` 在進度條與「📌 標記此刻」之間加 `<button id="zoomBtn" class="btn btn-sm">🔍 2x</button>`。
- ts：`initMiniPlayer` 的縮放計算（PM-38）把基準縮放 `baseScale` 與原始高度 `basePageHeight` 回填到模組變數；`zoomBtn` click 在 `baseScale` ↔ `baseScale×2` 間切換 → 改 `iframe.transform: scale()` + 容器高度 `basePageHeight×scale`；**放大時 `overflow:auto`**（可捲動看完整頁面）、縮回 `overflow:hidden`；按鈕文字 `🔍 2x`↔`🔍 1x`。
- 用回填的 `baseScale` 而非每次重算 clientWidth，避免放大後捲軸影響量測 → 切換精準。

**build**：`npm run build` ✅ ＋ `tsc --noEmit` ✅。

**待 FOX 瀏覽器驗收**：① 錄製時右上面板位置下移、不再被書籤列擋；② 編輯頁 mini player 按「🔍 2x」放大 2 倍且可捲動、再按「🔍 1x」縮回，播放/標記照常。

---
