# BugEzy Job — 2026-06-18

> 接續 job-0617.md（PM-11~26）
> 第 6 代「更好用」繼續打磨

---

### 🟡 PM-27：錄製即時字幕分兩區（底部 interim + 右上堆疊已確認文字）

**問題**：目前即時字幕只有底部一條，確認的文字一閃就消失，使用者看不到已經收錄了哪些內容，不放心。

**目標**：字幕分成兩區——底部顯示「正在講的」，右上角累積顯示「已確認的」。使用者邊講邊看，一目了然。

---

#### §1 inject.ts 改動

在 `showCaptionBar()` 裡，除了底部字幕條，**多建一個右上角面板**：

```ts
function showCaptionBar() {
  // ── 底部即時字幕（已有，不動）──
  // ...existing code...

  // ── 右上角已確認文字面板（新增）──
  const panel = document.createElement('div');
  panel.id = 'bugezy-voice-panel';
  panel.style.cssText = `
    position: fixed; top: 60px; right: 12px;
    z-index: 2147483647; pointer-events: none;
    width: 260px; max-height: 50vh; overflow-y: auto;
    background: rgba(0, 0, 0, 0.8);
    border: 1px solid rgba(124, 58, 237, 0.5);
    border-radius: 12px;
    padding: 10px 14px;
    font-family: system-ui, sans-serif;
    font-size: 14px; color: #eee;
    line-height: 1.6;
    transition: opacity 0.3s;
  `;

  // 標題列（含收合按鈕，pointer-events: auto 讓它可點）
  const header = document.createElement('div');
  header.style.cssText = `
    display: flex; justify-content: space-between; align-items: center;
    margin-bottom: 8px; padding-bottom: 6px;
    border-bottom: 1px solid rgba(255,255,255,0.15);
    pointer-events: auto; cursor: pointer;
  `;
  header.innerHTML = '<span style="font-size:12px;color:#a78bfa;">📝 語音記錄</span><span id="bugezy-panel-toggle" style="font-size:12px;color:#888;">▼ 收合</span>';
  
  const content = document.createElement('div');
  content.id = 'bugezy-voice-content';
  content.style.cssText = 'white-space: pre-wrap; word-break: break-word;';
  
  panel.appendChild(header);
  panel.appendChild(content);
  document.body.appendChild(panel);
  
  // 收合/展開 toggle
  let collapsed = false;
  header.addEventListener('click', () => {
    collapsed = !collapsed;
    content.style.display = collapsed ? 'none' : 'block';
    const toggle = document.getElementById('bugezy-panel-toggle');
    if (toggle) toggle.textContent = collapsed ? '▶ 展開' : '▼ 收合';
  });
}
```

**在 `recognition.onresult` 裡，確認的文字追加到右上面板**：

```ts
// final text → 追加到右上面板
if (finalText) {
  const voiceContent = document.getElementById('bugezy-voice-content');
  if (voiceContent) {
    voiceContent.textContent += (voiceContent.textContent ? '\n' : '') + finalText;
    // 自動捲到最新
    const panel = document.getElementById('bugezy-voice-panel');
    if (panel) panel.scrollTop = panel.scrollHeight;
  }
  // 底部字幕顯示確認（已有的邏輯保留）
}
```

**在 `hideCaptionBar()` 裡移除面板**：

```ts
function hideCaptionBar() {
  captionBar?.remove();
  captionBar = null;
  document.getElementById('bugezy-voice-panel')?.remove();
}
```

---

#### §2 不改的東西

- annotate.ts/html：不動（標注頁已有自己的字幕邏輯）
- edit-report：不動
- background/content/popup：不動
- server/web：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 開網頁 → F5 → 按「🎬 錄製」
2. 頁面右上角出現「📝 語音記錄」面板（半透明黑底）
3. 底部字幕即時閃動（正在講的 interim）
4. 確認的文字**一行一行堆疊在右上面板裡**
5. 點面板標題的「▼ 收合」→ 內容收起，再點「▶ 展開」恢復
6. 停止後面板消失

---

### 🟡 PM-28：錄製編輯頁加時間軸標記（📌 多個時間點 + 文字說明）

**目標**：編輯頁加一個 mini rrweb 播放器 + 時間軸，使用者可以暫停在任意秒數 → 按「📌 標記」→ 輸入文字。可標多個。標記資料存進報告，AI 透過 MCP 可讀。

---

#### §1 edit-report.html 改動

在錄製摘要下方、語音記錄上方，插入「時間軸標記區」：

```html
<!-- 時間軸標記區 -->
<div id="markerSection" class="section">
  <h2>📌 時間軸標記</h2>
  <div class="marker-player">
    <div id="miniPlayer" class="mini-player-container"></div>
    <div class="marker-controls">
      <button id="markerPlayBtn" class="btn btn-sm">▶</button>
      <input type="range" id="markerSeek" min="0" max="100" value="0" style="flex:1;">
      <span id="markerTime" class="time-display">0:00 / 0:00</span>
      <button id="addMarkerBtn" class="btn btn-sm btn-accent">📌 標記此刻</button>
    </div>
  </div>
  <div id="markerList" class="marker-list"></div>
</div>
```

CSS：
```css
.mini-player-container {
  width: 100%; max-width: 640px; height: 360px;
  overflow: hidden; border-radius: 8px;
  border: 1px solid var(--line, #2a2a3e);
  margin-bottom: 8px;
}
.mini-player-container iframe {
  width: 640px; height: 360px; border: none;
  transform-origin: top left;
}
.marker-controls {
  display: flex; align-items: center; gap: 8px;
  padding: 8px 0;
}
.btn-sm {
  padding: 4px 12px; font-size: 13px; border-radius: 6px;
  background: var(--panel, #1a1a2e); color: #fff;
  border: 1px solid var(--line, #2a2a3e); cursor: pointer;
}
.btn-accent { background: #7c3aed; border-color: #7c3aed; font-weight: 600; }
.marker-list { margin-top: 8px; }
.marker-item {
  display: flex; align-items: flex-start; gap: 8px;
  padding: 8px; margin: 4px 0;
  background: var(--panel, #1a1a2e);
  border: 1px solid var(--line, #2a2a3e);
  border-radius: 8px;
}
.marker-time {
  background: #7c3aed; color: #fff; padding: 2px 8px;
  border-radius: 4px; font-size: 12px; font-family: monospace;
  white-space: nowrap; cursor: pointer;
}
.marker-time:hover { background: #6d28d9; }
.marker-note {
  flex: 1; background: transparent; color: #eee;
  border: 1px solid var(--line, #2a2a3e);
  border-radius: 4px; padding: 4px 8px; font-size: 13px;
}
.marker-delete {
  background: none; border: none; color: #888;
  cursor: pointer; font-size: 16px;
}
.marker-delete:hover { color: #ef4444; }
```

---

#### §2 edit-report.ts 時間軸標記邏輯

```ts
import { Replayer } from '@rrweb/replay';
import '@rrweb/replay/dist/style.css';

interface Marker {
  time_sec: number;
  note: string;
}

const markers: Marker[] = [];
let replayer: Replayer | null = null;
let duration = 0;
let playing = false;

// 初始化 mini player（如果有 rrweb 資料）
function initMiniPlayer(events: unknown[]) {
  const container = document.getElementById('miniPlayer');
  if (!container || events.length < 2) {
    document.getElementById('markerSection')!.style.display = 'none';
    return;
  }

  replayer = new Replayer(events as any, {
    root: container,
    skipInactive: true,
    showWarning: false,
  });

  const meta = replayer.getMetaData();
  duration = meta.totalTime || 0;
  const seekBar = document.getElementById('markerSeek') as HTMLInputElement;
  seekBar.max = String(duration);

  // 播放/暫停
  document.getElementById('markerPlayBtn')!.addEventListener('click', () => {
    if (!replayer) return;
    if (playing) {
      replayer.pause();
      playing = false;
      document.getElementById('markerPlayBtn')!.textContent = '▶';
    } else {
      const current = replayer.getCurrentTime();
      if (current >= duration - 100) {
        replayer.play(0);
      } else {
        replayer.resume(current);
      }
      playing = true;
      document.getElementById('markerPlayBtn')!.textContent = '⏸';
      trackProgress();
    }
  });

  // seek
  seekBar.addEventListener('input', (e) => {
    const time = Number((e.target as HTMLInputElement).value);
    replayer!.play(time);
    if (!playing) setTimeout(() => replayer!.pause(time), 50);
    updateTimeDisplay(time);
  });

  // 📌 標記按鈕
  document.getElementById('addMarkerBtn')!.addEventListener('mousedown', (e) => {
    e.preventDefault(); // 不搶焦點
  });
  document.getElementById('addMarkerBtn')!.addEventListener('click', () => {
    if (!replayer) return;
    const currentTime = replayer.getCurrentTime();
    const sec = Math.floor(currentTime / 1000);
    // 暫停在當前位置
    if (playing) {
      replayer.pause();
      playing = false;
      document.getElementById('markerPlayBtn')!.textContent = '▶';
    }
    addMarker(sec);
  });
}

function addMarker(sec: number) {
  const marker: Marker = { time_sec: sec, note: '' };
  markers.push(marker);
  renderMarkers();
  // 自動 focus 到新標記的輸入框
  setTimeout(() => {
    const inputs = document.querySelectorAll('.marker-note');
    const last = inputs[inputs.length - 1] as HTMLInputElement;
    last?.focus();
  }, 50);
}

function renderMarkers() {
  const list = document.getElementById('markerList')!;
  list.innerHTML = '';
  // 按時間排序
  markers.sort((a, b) => a.time_sec - b.time_sec);
  markers.forEach((m, i) => {
    const item = document.createElement('div');
    item.className = 'marker-item';
    
    const timeBtn = document.createElement('span');
    timeBtn.className = 'marker-time';
    timeBtn.textContent = formatSec(m.time_sec);
    timeBtn.title = '點擊跳到此時間';
    timeBtn.addEventListener('click', () => {
      if (replayer) {
        replayer.play(m.time_sec * 1000);
        setTimeout(() => replayer!.pause(m.time_sec * 1000), 50);
        updateTimeDisplay(m.time_sec * 1000);
      }
    });
    
    const noteInput = document.createElement('input');
    noteInput.className = 'marker-note';
    noteInput.type = 'text';
    noteInput.placeholder = '描述這個時間點的問題...';
    noteInput.value = m.note;
    noteInput.addEventListener('input', () => { m.note = noteInput.value; });
    
    const delBtn = document.createElement('button');
    delBtn.className = 'marker-delete';
    delBtn.textContent = '✕';
    delBtn.addEventListener('click', () => {
      markers.splice(i, 1);
      renderMarkers();
    });
    
    item.appendChild(timeBtn);
    item.appendChild(noteInput);
    item.appendChild(delBtn);
    list.appendChild(item);
  });
}

function formatSec(sec: number): string {
  const m = Math.floor(sec / 60);
  const s = sec % 60;
  return `${m}:${String(s).padStart(2, '0')}`;
}

function updateTimeDisplay(ms: number) {
  const el = document.getElementById('markerTime');
  if (el) el.textContent = `${formatSec(Math.floor(ms / 1000))} / ${formatSec(Math.floor(duration / 1000))}`;
  const seekBar = document.getElementById('markerSeek') as HTMLInputElement;
  seekBar.value = String(ms);
}

function trackProgress() {
  if (!playing || !replayer) return;
  const current = replayer.getCurrentTime();
  updateTimeDisplay(current);
  if (current < duration) {
    requestAnimationFrame(trackProgress);
  } else {
    playing = false;
    document.getElementById('markerPlayBtn')!.textContent = '▶';
  }
}
```

---

#### §3 Markers 存進報告

**上傳時把 markers 加進 payload**：

edit-report.ts 的上傳按鈕 handler 裡：
```ts
// 組 payload 時加 markers
payload.markers = markers.filter(m => m.note.trim()); // 只保留有寫文字的標記
```

**types.ts 加**（extension 共用型別）：
```ts
export interface TimeMarker {
  time_sec: number;
  note: string;
}
```

RecordingPayload 加：
```ts
markers?: TimeMarker[];  // 選填，向後相容
```

---

#### §4 Server 改動

**POST /api/reports**：payload 裡的 `markers` 存入 Supabase。

**Supabase schema**（FOX 手動）：
```sql
ALTER TABLE reports ADD COLUMN IF NOT EXISTS markers JSONB DEFAULT '[]';
```

**GET /api/reports/:id**：回傳 markers。

---

#### §5 MCP 改動

`get_report_overview` 的 select 加上 markers 欄位。AI 讀到的就是：
```json
{
  "markers": [
    { "time_sec": 5, "note": "點了搜尋按鈕" },
    { "time_sec": 12, "note": "白畫面，應顯示搜尋結果" }
  ]
}
```

---

#### §6 報告頁（web/）改動

ReportPage.tsx：如果 report.markers 非空，在 rrweb player 下方顯示標記列表。點擊時間戳跳到對應播放位置。

---

#### §7 安裝依賴

edit-report.ts 要用 `@rrweb/replay`，這個已經裝在 web/ 裡但不在 extension/。需要：
```bash
cd extension && npm install @rrweb/replay
```

build.mjs 的 `external` 不要排除 `@rrweb/replay`（讓 esbuild bundle 進去）。

---

#### §8 不改的東西

- inject.ts：不動（PM-27 改的字幕邏輯與此無關）
- annotate.ts/html：不動
- popup：不動

---

#### Build

```
cd extension && npm install @rrweb/replay && npm run build
cd ../server && npx tsc --noEmit
cd ../web && npm run build
```

然後部署 server：`cd server && npx wrangler deploy`

#### 驗收 PM-27（字幕兩區）

1. 開網頁 → F5 → 按「🎬 錄製」
2. 右上角出現「📝 語音記錄」面板
3. 講中文 → 底部閃動 interim → 確認文字堆疊到右上面板
4. 點「▼ 收合」→ 面板收起
5. 停止後面板消失

#### 驗收 PM-28（時間軸標記）

1. 錄製一段（30 秒以上，中間點幾下頁面）→ 停止 → 進編輯頁
2. 看到 mini rrweb 播放器 + 播放按鈕 + 進度條
3. 播放 → 暫停在某秒 → 按「📌 標記此刻」→ 輸入「這裡按鈕沒反應」
4. 繼續播放 → 暫停在另一秒 → 再標記 → 輸入「這個圖片沒載入」
5. 點標記的時間戳 → 播放器跳到那個時間
6. 按「✅ 上傳報告」
7. BugEzy MCP 讀報告 → markers 陣列有兩個時間點 + 說明

---

### 🔵 DONE-27：錄製即時字幕分兩區 ✅

**改動**：`extension/src/inject.ts` 一個檔。

- `showCaptionBar()`：底部字幕條保留不動，**新增右上角 `#bugezy-voice-panel` 面板**（半透明黑底、紫框、`top:60px;right:12px`、`pointer-events:none` 不擋頁面）。標題列 `📝 語音記錄` + 收合/展開 toggle（`pointer-events:auto` 可點）。
- `recognition.onresult`：每段 **final 文字** 除了推進 `voiceSegments` + 底部短暫顯示 `✅`，也 **append 到 `#bugezy-voice-content`**（一行一行堆疊），並自動捲到最新。interim 仍只閃在底部字幕條。
- `hideCaptionBar()`：停止錄製時一併 `#bugezy-voice-panel?.remove()`。

**build**：`npm run build` ✅（inject.js 405.8kb）。

**待 FOX 瀏覽器驗收**：右上面板出現、final 堆疊、收合/展開、停止後消失（headless 無法驗 Chrome 麥克風 UI）。

---

### 🔵 DONE-28：錄製編輯頁時間軸標記 ✅（⚠ 需 FOX 先跑 SQL）

**全鏈改動（extension + server + mcp + web）**：

- **types.ts**：新增 `TimeMarker { time_sec; note }`；`RecordingPayload.markers?`；`UPLOAD_REPORT` 訊息帶 `markers?`。
- **edit-report.html**：摘要下方插入 `#markerSection`（mini player 容器 + 播放/進度/`📌 標記此刻` + 標記列表）＋對應 CSS；`<link rel="stylesheet" href="edit-report.css">`（esbuild 從 `@rrweb/replay/dist/style.css` 產出的 sibling CSS）。
- **edit-report.ts**：`import { Replayer } from '@rrweb/replay'`；`initMiniPlayer(payload.rrwebEvents)`（<2 筆事件自動隱藏 markerSection）；播放/暫停/seek/`getCurrentTime` 標記/點時間戳跳播/刪除/排序；上傳時 `markers: markers.filter(m=>m.note.trim())` 隨 `UPLOAD_REPORT` 送出。
- **background.ts**：`uploadReport(description, markers)` → `payload.markers = markers ?? []`。
- **server/src/index.ts**：`RecordingPayload.markers?`；insert row 加 `markers`；`getReport` 回傳 `markers`；`META_COLS` 加 `markers`（MCP overview/list 可讀）。
- **mcp-server/src/index.ts**（stdio）：`ReportData.markers?`；`get_report_overview` 回傳 `markers`。
- **web**：`types.ts` 加 `TimeMarker` + `Report.markers?`；`RrwebPlayer` 新增 `markers` prop → 控制列下方渲染可點時間戳（`seekTo`）+ 說明；`ReportPage` 傳入 `report.markers`；`index.css` 加 `.marker-list` 樣式。
- **server/schema.sql**：建表加 `markers JSONB` ＋ PM-28 ALTER 區塊。

**安裝**：`extension` 已加 `@rrweb/replay ^2.0.1`（hoist 到 root node_modules）。

**Build / Deploy 全綠**：
- `extension npm run build` ✅（edit-report.js 413.7kb，內含 rrweb replay；edit-report.css 2.3kb 已產出）
- `server npx tsc --noEmit` ✅
- `mcp-server npx tsc --noEmit` ✅
- `web npm run build` ✅
- `npx wrangler deploy` ✅（Version 778da6a3）

**🚨 阻擋項 — FOX 請立刻執行（否則所有上傳會失敗）**：
部署後的 Worker 已在 insert/select 引用 `markers` 欄位，但 Supabase 尚未有此欄。我用 live curl 實測 POST，回：
```
supabase insert failed: Could not find the 'markers' column of 'reports' in the schema cache
```
這代表**目前線上所有錄製上傳都會失敗**（不只帶 markers 的）。請到 Supabase SQL Editor 跑（已寫進 `server/schema.sql`）：
```sql
ALTER TABLE reports ADD COLUMN IF NOT EXISTS markers JSONB DEFAULT '[]';
```
跑完上傳即恢復、markers 全鏈即生效。（此為 spec §4「Supabase schema（FOX 手動）」的預期步驟，但因已部署故須即時補上。）測試 POST 失敗 = 無垃圾資料寫入，DB 乾淨。

**待 FOX 驗收（跑完 SQL 後）**：編輯頁 mini player 回放、標記、點時間戳跳播、上傳後 MCP `get_report_overview` 讀到 markers、報告頁時間戳跳播。

---

### 🟡 PM-29：修時間軸標記 UX — 按 📌 彈對話框 + 保留空標記

**問題**：
1. 按「📌 標記此刻」後，文字輸入框出現在下方但不夠明顯，使用者不知道要打字
2. 沒打字的標記被 `filter(m => m.note.trim())` 過濾掉，上傳後 markers 為空

**修法**：

---

#### edit-report.ts 改動

**改動 1：按 📌 後彈 prompt 對話框**

把 `addMarker()` 改為：
```ts
function addMarker(sec: number) {
  const note = prompt(`📌 ${formatSec(sec)} — 描述這個時間點的問題：`) ?? '';
  const marker: Marker = { time_sec: sec, note: note.trim() };
  markers.push(marker);
  renderMarkers();
}
```

這樣按 📌 會立刻跳出瀏覽器原生對話框，使用者一定看得到。輸入完按確定，標記就加好了。按取消也會加一個空標記（只有時間點）。

**改動 2：移除空標記過濾**

上傳時改為：
```ts
// 保留所有標記（包含沒有文字的，時間點本身就有價值）
payload.markers = markers;
```

不再 `.filter(m => m.note.trim())`。

---

#### 不改的東西

- renderMarkers()：不動（下方的標記列表仍可編輯文字）
- 其他所有檔案：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 錄製 → 停止 → 編輯頁
2. 播放 → 暫停 → 按「📌 標記此刻」→ **應跳出對話框問你描述**
3. 輸入文字 → 確定 → 標記出現在下方列表
4. 再標記一個但按取消（不輸入文字）→ 標記仍出現（只有時間點）
5. 上傳 → MCP 讀到 markers 有內容

---

### 🔵 DONE-29：修時間軸標記 UX ✅

**改動**：`extension/src/edit-report.ts` 兩處（其他檔案皆不動）。

1. **`addMarker(sec)` 改彈原生對話框**：按「📌 標記此刻」立刻 `window.prompt('📌 m:ss — 描述這個時間點的問題：')`，使用者一定看得到。輸入 → `note.trim()`；按取消（回傳 null）→ 空描述，仍 push 標記（保留時間點）。移除原本 push 空標記 + 自動 focus 下方輸入框的邏輯。下方列表的可編輯輸入框（`renderMarkers`）保留不動，仍可事後補/改字。
2. **上傳不再過濾空標記**：`UPLOAD_REPORT` 由 `markers.filter(m => m.note.trim())` 改為直接送 `markers`（全部，含只有時間點的）。

**build**：`npm run build` ✅（edit-report.js 413.7kb，無型別/打包錯誤）。

**待 FOX 瀏覽器驗收**：按 📌 跳對話框、輸入後列表出現、按取消仍留時間點、上傳後 MCP 讀到 markers。（仍需先跑 DONE-28 的 `ALTER TABLE … markers` SQL，上傳才會成功。）

---

### 🟡 PM-30：字幕條加永久「🔄 重啟」按鈕

**問題**：語音中斷重啟按鈕只在 `rec.start()` 拋錯時出現，但大部分中斷是靜默的（auto restart 有空窗），使用者看到字幕停了但沒按鈕可按。

**修法**：字幕條右邊永遠顯示一個小「🔄」按鈕，使用者覺得語音斷了隨時可以按。按了會建新的 SpeechRecognition 實例強制重啟。

---

#### inject.ts 改動

**showCaptionBar() 裡，底部字幕條加一個重啟按鈕**：

```ts
function showCaptionBar() {
  // ...existing code 建 bar...
  
  // 重啟按鈕（永遠顯示在字幕條右邊）
  const restartBtn = document.createElement('button');
  restartBtn.id = 'bugezy-voice-restart';
  restartBtn.textContent = '🔄';
  restartBtn.title = '重新啟動語音辨識';
  restartBtn.style.cssText = `
    pointer-events: auto;
    background: rgba(124, 58, 237, 0.8);
    color: #fff;
    border: none;
    border-radius: 6px;
    padding: 4px 10px;
    font-size: 16px;
    cursor: pointer;
    margin-left: 12px;
    vertical-align: middle;
  `;
  restartBtn.addEventListener('click', () => {
    forceRestartVoice();
  });
  bar.appendChild(restartBtn);
  
  // bar 的 pointer-events 改成 none，但內部按鈕是 auto
  bar.style.pointerEvents = 'none';
  // 讓 bar 內部可以有可點擊元素
  // 做法：bar 用 flex，文字部分 pointer-events:none，按鈕 pointer-events:auto
}
```

**字幕條改成 flex 佈局**：
```ts
bar.style.cssText = `
  position: fixed; bottom: 100px; left: 50%; transform: translateX(-50%);
  z-index: 2147483647;
  background: rgba(0,0,0,0.85); color: #fff;
  padding: 12px 28px; border-radius: 12px;
  font-size: 22px; max-width: 80%; text-align: center;
  font-family: system-ui, sans-serif;
  display: flex; align-items: center; gap: 8px;
  pointer-events: none;
`;

// 文字用 span 包裹
const textSpan = document.createElement('span');
textSpan.id = 'bugezy-caption-text';
textSpan.style.cssText = 'flex: 1; pointer-events: none;';
textSpan.textContent = '🔴 錄製中，可以用中文描述問題...';
bar.appendChild(textSpan);
bar.appendChild(restartBtn); // 按鈕在右邊
```

**更新字幕文字時改為更新 textSpan**：
所有 `captionBar.textContent = ...` 改成 `document.getElementById('bugezy-caption-text')!.textContent = ...`
（因為現在 captionBar 裡有文字 span + 按鈕，直接改 textContent 會把按鈕也清掉）

**新增 forceRestartVoice() 函式**：
```ts
function forceRestartVoice() {
  blog('手動強制重啟語音');
  const SR = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition;
  if (!SR || !voiceActive) return;
  
  // 停掉舊的
  try { recognition?.stop(); } catch {}
  
  // 建新的
  try {
    const newRec = new SR();
    newRec.lang = 'zh-TW';
    newRec.continuous = true;
    newRec.interimResults = false;
    newRec.onresult = recognition!.onresult;
    newRec.onend = recognition!.onend;
    newRec.onerror = recognition!.onerror;
    recognition = newRec;
    newRec.start();
    
    const textEl = document.getElementById('bugezy-caption-text');
    if (textEl) textEl.textContent = '🔴 語音已重啟，繼續說...';
    blog('語音強制重啟成功');
  } catch (err) {
    blog('語音強制重啟失敗', err);
    const textEl = document.getElementById('bugezy-caption-text');
    if (textEl) textEl.textContent = '❌ 語音重啟失敗';
  }
}
```

**同時保留 showRestartButton() 的現有邏輯**（自動重啟失敗時會把整個字幕條變成錯誤狀態）。

---

#### 不改的東西
- 右上面板（PM-27）：不動
- edit-report / annotate / server / web：不動

---

#### Build
```
cd extension && npm run build
```

#### 驗收
1. 錄製 → 字幕條底部應有「🔴 文字... 🔄」，右邊有小紫色 🔄 按鈕
2. 講中文 → 字幕正常
3. 等語音斷掉 → 按 🔄 → 字幕顯示「🔴 語音已重啟，繼續說...」→ 繼續講 → 字幕恢復

---

### 🔵 DONE-30：字幕條加永久「🔄 重啟」按鈕 ✅

**改動**：`extension/src/inject.ts` 一個檔（右上面板 PM-27 / edit-report / annotate / server / web 皆不動）。

- **`showCaptionBar()` 改 flex 佈局**：底部字幕條 `display:flex;align-items:center;gap:8px`，本體 `pointer-events:none`。文字改用 `#bugezy-caption-text` span 包裹（`flex:1;pointer-events:none`），右邊永久放一顆紫色 `#bugezy-voice-restart` 🔄 按鈕（`pointer-events:auto`），點擊呼叫 `forceRestartVoice()`。
- **新增 `setCaptionText(text)` helper**：只更新 `#bugezy-caption-text` 的 textContent，避免像舊的 `captionBar.textContent=` 直接覆寫把 🔄 按鈕一起清掉。onresult 的三處字幕更新（interim `🔴`、final `✅`、回復 `🔴 聆聽中...`）全改用 `setCaptionText()`。
- **新增 `forceRestartVoice()`**：`recognition?.stop()` 停舊的 → `new SR()`（zh-TW / continuous / interimResults=false）沿用既有 `onresult/onend/onerror` handlers → `start()` → 字幕顯示「🔴 語音已重啟，繼續說...」；失敗顯示「❌ 語音重啟失敗」。`!voiceActive` 時直接 return。
- **`showRestartButton()`（自動重啟失敗時的整條錯誤狀態）保留不動**，與永久 🔄 按鈕並存。

**build**：`npm run build` ✅（inject.js 405.8 → 407.6kb，無錯）。

**待 FOX 瀏覽器驗收**：字幕條右邊出現小紫 🔄、靜默中斷後按 🔄 顯示「語音已重啟」並可續錄。（headless 無法驗 Chrome 麥克風 UI。）

---

### 🟡 PM-31：修三個 Bug（面板當機 + player 太小 + 文字插入位置）

**來源**：FOX 用 BugEzy 自己回報的 Bug（report 2de2c098）

---

#### Bug 1：右上語音面板點擊會當機（🔴 嚴重）

**問題**：PM-27 的右上「📝 語音記錄」面板，收合按鈕的 `pointer-events: auto` 區域太大，使用者不小心點到面板其他地方會觸發奇怪的行為導致頁面卡死。

**修法**：

在 inject.ts 的 `showCaptionBar()` 裡，右上面板的修改：

1. 整個面板 `pointer-events: none`（保持不擋頁面操作）
2. 只有收合按鈕本身是 `pointer-events: auto`，且用一個小 `<button>` 而不是整個 header 區域
3. 收合按鈕改小，只放一個「▼」/「▶」圖示

```ts
// header 改為 pointer-events: none
header.style.cssText = `
  display: flex; justify-content: space-between; align-items: center;
  margin-bottom: 8px; padding-bottom: 6px;
  border-bottom: 1px solid rgba(255,255,255,0.15);
  pointer-events: none;
`;

// 收合按鈕獨立，只有它可點
const toggleBtn = document.createElement('button');
toggleBtn.id = 'bugezy-panel-toggle';
toggleBtn.textContent = '▼';
toggleBtn.title = '收合/展開';
toggleBtn.style.cssText = `
  pointer-events: auto;
  background: rgba(124,58,237,0.6);
  border: none; border-radius: 4px;
  color: #fff; font-size: 12px;
  padding: 2px 8px; cursor: pointer;
`;
toggleBtn.addEventListener('click', (e) => {
  e.stopPropagation();
  collapsed = !collapsed;
  content.style.display = collapsed ? 'none' : 'block';
  toggleBtn.textContent = collapsed ? '▶' : '▼';
});

header.innerHTML = '<span style="font-size:12px;color:#a78bfa;">📝 語音記錄</span>';
header.appendChild(toggleBtn);
```

---

#### Bug 3：Mini player 太小（看報紙感）

**問題**：edit-report.html 的 mini rrweb player 只有 640x360，內容看不清楚。

**修法**：

edit-report.html / edit-report.ts 的 player 容器改為更大 + 響應式：

```css
.mini-player-container {
  width: 100%;
  max-width: 960px;  /* 從 640 改成 960 */
  height: auto;
  aspect-ratio: 16/9;  /* 保持比例 */
  overflow: hidden;
  border-radius: 8px;
  border: 1px solid var(--line, #2a2a3e);
  margin-bottom: 8px;
}
```

Replayer 初始化時 width 也要跟著改：
```ts
// 如果有設定 Replayer 的寬高，改為更大或自適應
// 讓 iframe 填滿容器
const iframe = container.querySelector('iframe');
if (iframe) {
  iframe.style.width = '100%';
  iframe.style.height = '100%';
}
```

---

#### Bug 4：文字框插入位置永遠跑到末端

**問題**：補充說明的 textarea，語音辨識 append 文字後 cursor 被移到最後面。使用者想在中間插入文字，但打的字永遠跑到句尾。

**修法**：

在 edit-report.ts 和 annotate.ts 的語音 `onresult` handler 裡，append 文字時**保留原始 cursor 位置**：

```ts
// 語音 final text append 時，保留 cursor 位置
if (finalText) {
  const cursorPos = descInput.selectionStart; // 記住 cursor
  const isAtEnd = cursorPos === descInput.value.length; // cursor 是否在末端
  
  // append 到末端
  descInput.value += finalText;
  
  // 如果 cursor 原本不在末端，恢復 cursor 位置
  if (!isAtEnd) {
    descInput.selectionStart = cursorPos;
    descInput.selectionEnd = cursorPos;
  }
}
```

這樣：
- 使用者在末端 → 語音 append 後 cursor 跟著到新末端（自然）
- 使用者在中間編輯 → 語音 append 到末端但 cursor 留在原位（不干擾）

**同時修 annotate.ts 的同一段邏輯。**

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. **面板不當機**：錄製 → 右上面板出現 → 用滑鼠點面板文字區域 → 不應該卡死
2. **player 更大**：編輯頁的 mini player 明顯變大，內容看得清楚
3. **文字插入**：在補充說明框中間打字 → 同時語音在 append → cursor 位置不被搶走

---

### 🔵 DONE-31：修三個 Bug ✅（來源 report 2de2c098）

**改動**：`inject.ts` + `edit-report.html` + `edit-report.ts` + `annotate.ts`（server/web 不動）。

**Bug 1 — 右上語音面板誤點卡死（inject.ts）**：
- 右上面板 `header` 由 `pointer-events:auto;cursor:pointer`（整列可點）改為 **`pointer-events:none`**（整列不擋、不可點）。
- 收合改成獨立 `<button id="bugezy-panel-toggle">`，**只有按鈕本身 `pointer-events:auto`**，紫底小圖示 `▼`/`▶`，handler 加 `e.stopPropagation()`。面板其他區域不再吃點擊，誤點不會觸發任何行為。

**Bug 3 — mini player 太小（edit-report.html + edit-report.ts）**：
- `.mini-player-container`：`max-width` 640→**960px**，`height:360px` 改 **`height:auto;aspect-ratio:16/9`**（響應式保比例）；`iframe` 改 `width:100%;height:100%`。
- `initMiniPlayer()` 建 Replayer 後，把內部 `iframe.style.width/height = '100%'`，填滿放大後的容器。

**Bug 4 — 文字插入永遠跑末端（edit-report.ts + annotate.ts）**：
- 兩處語音 `onresult` 的 final append 改為：先記 `selectionStart` + 判斷 `isAtEnd`，`value +=` 後若 cursor 原本不在末端則還原 `selectionStart/End`。使用者在中間編輯時語音 append 到末端但不搶 cursor；在末端則自然跟到新末端。

**build**：`npm run build` ✅（edit-report.js 414.1kb / inject.js 407.8kb / annotate.js 8.8kb，無錯）。

**待 FOX 瀏覽器驗收**：① 點右上面板文字區不卡死、只有 ▼ 按鈕可收合；② 編輯頁 player 明顯放大且清楚；③ 補充說明框中間打字時語音 append 不搶 cursor。

---

### 🟡 PM-32：修語音重啟按鈕（按了反而掛掉）

**問題**：錄製中按 🔄 重啟語音後，語音完全死掉無法喚醒。原因是 `forceRestartVoice()` 複製舊 recognition 的 event handlers（`recognition.onresult` 等），但舊 handlers 可能引用了已失效的閉包變數或已銷毀的 recognition 實例。

**修法**：不複製舊 handlers，重新建立完整的 event handlers。把語音初始化邏輯抽成可重複呼叫的函式。

---

#### inject.ts 改動

**核心思路**：把目前散落在 `startRecording()` 裡的 SpeechRecognition 初始化邏輯抽成一個獨立函式 `createRecognition()`，`forceRestartVoice()` 呼叫這個函式而不是複製舊 handlers。

```ts
/** 建立新的 SpeechRecognition 實例（可重複呼叫） */
function createRecognition(): SRInstance | null {
  const SR = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition;
  if (!SR) return null;

  const rec = new SR() as SRInstance;
  rec.lang = 'zh-TW';
  rec.continuous = true;
  rec.interimResults = false;

  rec.onresult = (e: any) => {
    for (let i = e.resultIndex; i < e.results.length; i++) {
      if (e.results[i].isFinal) {
        const text = e.results[i][0].transcript.trim();
        if (text) {
          const seg: VoiceSegment = { text, timestamp: Date.now(), isFinal: true };
          voiceSegments.push(seg);
          blog('voice segment:', text.slice(0, 40));

          // 更新右上面板
          const voiceContent = document.getElementById('bugezy-voice-content');
          if (voiceContent) {
            voiceContent.textContent += (voiceContent.textContent ? '\n' : '') + text;
            const panel = document.getElementById('bugezy-voice-panel');
            if (panel) panel.scrollTop = panel.scrollHeight;
          }

          // 更新底部字幕
          const textEl = document.getElementById('bugezy-caption-text');
          if (textEl) {
            textEl.textContent = '✅ ' + text;
            setTimeout(() => {
              if (voiceActive && textEl) textEl.textContent = '🔴 聆聽中...';
            }, 1500);
          }
        }
      }
    }
  };

  rec.onend = () => {
    if (voiceActive) {
      blog('SpeechRecognition onend → auto restart');
      try {
        rec.start();
      } catch {
        blog('SpeechRecognition auto restart 失敗');
        // 不呼叫 showRestartButton()，因為 🔄 按鈕已永遠在
        const textEl = document.getElementById('bugezy-caption-text');
        if (textEl) textEl.textContent = '⚠ 語音中斷，按 🔄 重啟';
      }
    }
  };

  rec.onerror = (e: any) => {
    blog('SpeechRecognition error:', e.error);
    if (e.error === 'not-allowed' || e.error === 'service-not-allowed') {
      voiceActive = false;
      const textEl = document.getElementById('bugezy-caption-text');
      if (textEl) textEl.textContent = '❌ 麥克風被拒絕';
    }
  };

  return rec;
}
```

**startRecording() 裡的語音初始化改為**：
```ts
voiceSegments = [];
voiceActive = true;
recognition = createRecognition();
if (recognition) {
  try {
    recognition.start();
    blog('SpeechRecognition started (zh-TW)');
  } catch (err) {
    blog('SpeechRecognition start 失敗', err);
    showMicPermissionOverlay(); // 已有的授權橫幅
  }
} else {
  blog('此瀏覽器不支援 SpeechRecognition');
  voiceActive = false;
}
```

**forceRestartVoice() 改為**：
```ts
function forceRestartVoice() {
  blog('手動強制重啟語音');
  if (!voiceActive) return;

  // 停掉舊的
  try { recognition?.stop(); } catch {}
  recognition = null;

  // 建全新的（不複製舊 handlers）
  recognition = createRecognition();
  if (recognition) {
    try {
      recognition.start();
      const textEl = document.getElementById('bugezy-caption-text');
      if (textEl) textEl.textContent = '🔴 語音已重啟，繼續說...';
      blog('語音強制重啟成功');
    } catch (err) {
      blog('語音強制重啟失敗', err);
      const textEl = document.getElementById('bugezy-caption-text');
      if (textEl) textEl.textContent = '❌ 語音重啟失敗';
    }
  }
}
```

**刪除 showRestartButton() 函式**（不再需要，🔄 按鈕已永遠在字幕條上）。

---

#### 不改的東西

- 右上面板、收合按鈕：不動（PM-31 已修）
- edit-report / annotate / server / web：不動

---

#### Build

```
cd extension && npm run build
```

#### 驗收

1. 錄製 → 講中文 → 字幕正常
2. 等語音斷掉（或故意靜默 15 秒）→ 字幕顯示「⚠ 語音中斷，按 🔄 重啟」
3. 按 🔄 → 字幕顯示「🔴 語音已重啟，繼續說...」
4. **繼續講中文 → 字幕恢復跟著 → 右上面板繼續堆疊新文字**
5. 再斷再按 🔄 → 可以無限次重啟

---

### 🔵 DONE-32：修 🔄 重啟後語音死掉 ✅

**改動**：`extension/src/inject.ts` 一個檔（右上面板/收合鈕、edit-report/annotate/server/web 皆不動）。

**根因**：舊 `forceRestartVoice()` 建新實例時**複製舊 recognition 的 handlers**（`newRec.onresult = recognition!.onresult` 等），這些 handler 的閉包綁在已銷毀的舊實例上，重啟後形同空轉 → 語音死掉。

**修法（照規格 3 點）**：
1. **抽出 `createRecognition(): SRInstance | null`**：每次都建全新實例 + **全新 handlers**（onresult 推 `voiceSegments`＋堆疊右上面板＋更新底部字幕；onend 自動重啟、失敗顯示「⚠ 語音中斷，按 🔄 重啟」；onerror 處理 not-allowed）。不再複製任何舊閉包。
2. **`forceRestartVoice()` 改用新實例**：`recognition?.stop()` → `recognition = null` → `recognition = createRecognition()` → `start()`，成功顯示「🔴 語音已重啟，繼續說...」。可無限次重啟。
3. **刪除 `showRestartButton()`**：永久 🔄 按鈕（PM-30）已在字幕條上，自動重啟失敗只改字幕文字提示即可，整段函式移除。

**順帶整合（不擴大範圍）**：`initSpeechRecognition()` 內部改為走同一個 `createRecognition()` 工廠（錄製啟動與 🔄 重啟同一條路徑），但**保留 `tryStartVoice()` + 授權浮層 `showMicPermissionOverlay()` 的麥克風授權流程不動**（PM-09 的 user-gesture 解法，SKILL.md 核心鐵律 #4），故 `startRecording()` 維持原樣，未照規格範例那樣改成直接 `start()`——避免回歸首次授權體驗。

**⚠ 行為變更（請 PM 知悉）**：`createRecognition()` 依規格設 `interimResults = false`，因此**底部字幕不再閃動「正在講的 interim」**，只顯示 `✅ 已確認` 與 `🔴 聆聽中...`；右上面板的 final 堆疊不受影響。若要保留 interim 即時閃字，需另開 PM 調整。

**build**：`npm run build` ✅（inject.js 407.8 → 405.9kb，刪冗碼後變小，無錯）。

**待 FOX 瀏覽器驗收**：講中文字幕正常 → 靜默 15 秒顯示「⚠ 語音中斷，按 🔄 重啟」→ 按 🔄 顯示「語音已重啟」→ 續講字幕與右上面板恢復堆疊 → 可無限次重啟。

---
