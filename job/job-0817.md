# job-0817.md — BugEzy Day 44（2026-08-17）

## 昨日成果（Day 43）
- PM-305~333 共 30 張卡完成
- Phase 1 完工：11 支瀏覽器工具 + 3 終端機 + 3 圖釘 = **17 支 bridge MCP 工具**
- 端到端 216 項 0 failed
- 規格書 v1.1
- v1.1.5 已通過審核，8 位使用者

## 今日待辦
1. PM-G 圖釘巡察模式（Phase 2 最後一塊）
2. Phase 2 收工
3. Phase 3 視覺化起步（藍框動畫 / 圖釘顏色 / 即時面板）
4. Phase 3 Zone Grid
5. 目標：30 張卡

---


### 🟡 PM-334：Phase 2 PM-G — 圖釘巡察模式（patrol_pins）

**目標**：實作 `patrol_pins(tab_id?)` 工具，讓 AI 一次巡檢所有圖釘，自動逐一 analyze 並回報變化。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('patrol_pins', ...)
參數：
  tab_id: z.number().optional()
回傳：{
  patrolled: number,
  results: [{
    pin_id, selector, description, status,
    previous_status, changed: boolean,
    summary: string  // 一句話：「正常」/「從 active → stale」/「新增錯誤」
  }],
  alert_count: number,  // 有變化的圖釘數
  tab_id
}
```

**content script 側**（`extension/src/content.ts`）：
```
case 'BRIDGE_PATROL_PINS':
  1. 取得當前分頁所有圖釘
  2. 逐一執行 analyze_element 邏輯（複用 PM-315 的函式）
  3. 比對上次 last_check 結果，偵測變化：
     - 元素消失 → status: 'stale'
     - visibility 改變 → changed: true
     - 新增 error → changed: true
     - 無變化 → changed: false
  4. 更新圖釘的 last_check + status
  5. 更新視覺覆蓋層顏色
  6. 回傳完整巡檢結果
```

**關鍵細節**：
- 沒有圖釘時回 `{ patrolled: 0, results: [], alert_count: 0 }`（不是 error）
- `previous_status` 讓 AI 知道是惡化還是好轉
- `alert_count` 是快速判斷「需不需要深入看」的指標
- 巡檢順序：按圖釘建立時間排序（先釘的先巡）

**驗收條件**：
1. 有 3 個圖釘時巡檢 → patrolled: 3，每個都有結果
2. 元素消失 → status 變 stale + changed: true
3. 無變化 → changed: false
4. 沒有圖釘 → patrolled: 0
5. 視覺覆蓋層顏色有更新

完成後在下方寫 `🔵 DONE-334`。

---

### 🔵 DONE-334：`patrol_pins` 完成，五項驗收在真實 DOM 全數通過

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 3 個圖釘 → patrolled: 3，每個都有結果 | ✅ 且**巡檢順序按建立時間**（明確用 `created_at` 排序，不依賴 Map 的隱含插入順序）|
| 2 | 元素消失 → status stale + changed: true | ✅ **修了一個真 bug 才通過，見下** |
| 3 | 無變化 → changed: false | ✅ |
| 4 | 沒有圖釘 → patrolled: 0 | ✅ 且不是 error |
| 5 | 視覺覆蓋層顏色有更新 | ✅ 走 PM-338 的色表 |

---

### 🔴 測試抓到的真 bug：元素消失時圖釘沒有被標成 stale

第一次跑 334-2 的結果是：

```json
{"selector":"#p2","status":"active","previous_status":"active","changed":false,"summary":"🟢 ✅ 可見且可互動"}
```

元素明明已經從 DOM 移除了，巡檢卻回報「正常、無變化」。

**根因**：`pin_analyze` 開頭會呼叫 `pin_element` 確保圖釘存在，而元素找不到時 `pin_element` 回 error，`pin_analyze` 就**直接 return 那個錯誤，完全沒碰到 `pin.status`**。`stale` 的標記原本只發生在 `repositionPins()`（畫覆蓋層時），而巡檢是同步讀 `pin.status`、在 rAF 重畫之前就回傳了。

**這正是最糟的失敗模式**：巡檢的用途就是「幫我盯著這些元素」，而它在元素消失時回報「一切正常」—— AI 會據此認為頁面沒問題。

修法：`pin_analyze` 在找不到元素時，若**已有對應圖釘**就把它標成 `stale` 並寫入 `last_check`，而不是回一個裸錯誤。

---

### 兩個設計決定

- **`alert_count` 數的是「狀態有變」而不是「有問題」。** 一個一直是 warning 的圖釘，不該每次巡檢都觸發警報 —— AI 要判斷的是「跟上次比有沒有惡化」。`previous_status` 也一併回傳，讓 AI 分得出惡化與好轉。
- **巡檢順序明確按 `created_at` 排序**。Map 的插入順序剛好就是建立順序，但依賴這個隱含性質很脆弱（日後任何 re-insert 都會打亂），所以明寫排序。

---

### 🟡 PM-335：Phase 2 PM-G 補充 — remove_pin + clear_pins

**目標**：圖釘系統的清理工具，讓 AI 能移除已解決或不再需要的圖釘。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('remove_pin', ...)
參數：
  pin_id: z.string() — 或 selector: z.string()（二選一，pin_id 優先）
  tab_id: z.number().optional()
回傳：{ removed: true, pin_id, selector }

server.tool('clear_pins', ...)
參數：
  tab_id: z.number().optional()
  status: z.enum(['all', 'resolved', 'stale']).optional() — 預設 'all'
回傳：{ cleared: number, tab_id }
```

**content script 側**：
- `remove_pin`：從 pinStore 移除 + 移除 DOM overlay
- `clear_pins`：批次版，可按 status 篩選（只清 resolved 的、只清 stale 的、全清）

**驗收條件**：
1. `remove_pin({ pin_id })` → 圖釘移除 + DOM overlay 消失
2. `remove_pin({ selector })` → 用 selector 找到圖釘並移除
3. pin_id 不存在 → error
4. `clear_pins({ status: 'resolved' })` → 只清 resolved
5. `clear_pins()` → 全部清除

完成後在下方寫 `🔵 DONE-335`。

---

### 🔵 DONE-335：`remove_pin` + `clear_pins` 完成，五項驗收全數通過

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | `remove_pin({pin_id})` → 移除 + overlay 消失 | ✅ 覆蓋層標記數同步減少 |
| 2 | `remove_pin({selector})` | ✅ |
| 3 | pin_id 不存在 → error | ✅ 並附上現有圖釘清單 |
| 4 | `clear_pins({status})` 只清該狀態 | ✅ |
| 5 | `clear_pins()` 全部清除 | ✅ |

---

### 🔴 卡片的 `status: 'resolved'` 在目前的模型裡不存在

卡片的 enum 是 `'all' | 'resolved' | 'stale'`。但 **PM-329 已經把 `resolved` 換成 `warning`** —— 當時的理由是 `resolved` 沒有任何程式路徑會去設定它，而 `pin_analyze` 的結果本質是三級（正常／有疑慮／已消失）。

照字面實作的後果是一個**永遠篩不到東西的過濾器**：AI 呼叫 `clear_pins({status:'resolved'})` 拿到 `cleared: 0`，但**無從得知是「沒有符合的圖釘」還是「這個值根本沒用」**。這跟我前幾張卡一直在防的「靜默無效」是同一類。

處置：`status` 收 `all / active / warning / error / stale`；傳 `resolved` 時**明確報錯**並說明它已被 `warning` 取代、要清掉處理完的圖釘請用 `remove_pin`，同時回傳 `valid_status` 讓 AI 知道能填什麼。zod enum 也同步限制，第一道就擋在 MCP 層。

---

### 🟡 PM-336：Phase 2 端到端驗收 + 收工

**前提**：Extension 重新載入

**執行**：
1. 跑圖釘相關全套端到端測試
2. 確認 patrol_pins / remove_pin / clear_pins 三支工具通過
3. 工具總數 = 20（17 + patrol_pins + remove_pin + clear_pins）

**收工**：
- CHANGELOG 加 Phase 2 完工區塊
- ARCHITECTURE 更新圖釘工具至 6 支（pin_element / pin_analyze / get_pin_results / patrol_pins / remove_pin / clear_pins）
- 規格書 §9 Phase 2 標 ✅
- `git add -A && git commit -m "Phase2 complete: pin system 6 tools (PM-334~336)" && git push`

**驗收條件**：
1. 圖釘 6 支工具全部端到端 PASS
2. 工具總數 20
3. §9 Phase 2 有 ✅
4. git push 成功

完成後在下方寫 `🔵 DONE-336`。

---

### 🔵 DONE-336：收工部分完成；**端到端待你做兩件事**

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 圖釘 6 支工具端到端 PASS | ⏳ 未執行（原因見下）|
| 2 | 工具總數 20 | ✅ **實際 22**（卡片沒算到 PM-339 的 2 支面板工具）|
| 3 | §9 Phase 2 有 ✅ | ✅ 連同 Phase 3 三項一起標 |
| 4 | git push | ✅ |

**目前測試**：`_verify309.mjs` **115 passed, 0 failed**（含 PM-334~339 共 31 項）；`_verify_phase1.mjs` **57 passed, 0 failed**（工具註冊等，端到端段落 SKIP）。

---

### 端到端為什麼沒跑：port 被本 session 的 MCP server 占用

```
PID=27756  parent=claude.exe  started=13:38:31
```

**父程序是 `claude.exe`** —— 這是你 Claude Code session 自己 spawn 的 bugezy-bridge，跑的是**今天 13:38 的舊版（17 支工具）**。我的驗證 harness 因此綁不到 port 19850，端到端整段 SKIP。

harness 的保護機制正確發揮作用：它只會回收「父程序是 `cmd.exe`」的自家洩漏程序，**確認是 Claude Code 的 MCP server 時一律不動**（PM-332 立的規則）。

**需要你做兩件事**（都做完我一跑就能補完）：
1. **重新載入擴充功能** —— `npm run build:dev` 的產物已備妥（含 `<all_urls>`），新的 content script 才有 `patrol_pins` / `remove_pin` / `clear_pins` / 面板 的處理常式
2. **重連 MCP**（`/mcp` 重連或重開）—— 讓 port 讓給新版 bridge，也讓你在對話裡叫得到新的 5 支工具

---

### 一處與卡片不同：工具總數是 22 不是 20

卡片寫「17 + patrol_pins + remove_pin + clear_pins = 20」，但 **PM-339 另外新增了 `show_debug_panel` / `hide_debug_panel` 兩支**（卡片把面板寫成「內部指令」，不過既然要讓 AI 能控制顯示，它們就得是真正的 MCP 工具，否則 AI 根本呼叫不到）。所以實際是 **22**，測試斷言也照 22 寫。

---

### 🟡 PM-337：Phase 3 PM-H — 藍框巡察動畫

**目標**：AI 巡檢元素時，在頁面上顯示藍色虛線框 + 閃爍動畫，讓用戶看到 AI 在看什麼。

---

**content script 側**（`extension/src/content.ts`）：

新增 `highlightElement(selector, options?)` 共用函式：
```
options: {
  color: string = '#00bfff'（BugEzy 品牌藍）
  duration_ms: number = 2000
  style: 'pulse' | 'outline' | 'fill' = 'pulse'
  label: string? — 顯示在框旁邊的標籤
}

動畫實作：
1. 找到元素 → getBoundingClientRect()
2. 建立 overlay div（position: fixed, pointer-events: none, z-index: 2147483646）
3. 藍色虛線邊框 + 脈衝動畫（CSS @keyframes）
4. duration_ms 後自動移除
5. 同時最多顯示 5 個高亮（超過的先進先出）
```

**整合到既有工具**：
- `click_element` → 點擊前閃一下被點的元素（0.5 秒）
- `analyze_element` → 分析時高亮 2 秒
- `patrol_pins` → 巡檢每個圖釘時依序高亮
- `pin_element` → 釘選時高亮 1 秒

**不新增 MCP 工具**，純視覺增強。

**驗收條件**：
1. `click_element` 時目標元素有藍框閃爍
2. `analyze_element` 時目標元素高亮 2 秒
3. 高亮框跟隨元素位置（不偏移）
4. 超過 5 個同時高亮 → 最舊的自動消失
5. 動畫結束後 DOM 乾淨（無殘留 overlay）

完成後在下方寫 `🔵 DONE-337`。

---

### 🔵 DONE-337：藍框巡察動畫完成，五項驗收全數通過

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | `click_element` 時目標有藍框閃爍 | ✅ 0.5 秒 + 「點擊」標籤 |
| 2 | `analyze_element` 時高亮 2 秒 | ✅ + 「分析中」標籤 |
| 3 | 高亮框跟隨元素位置 | ✅ 用 `getBoundingClientRect()`，實測框寬 = 元素寬 + 4px 邊距 |
| 4 | 超過 5 個 → 最舊的自動消失 | ✅ 連開 8 個後仍只剩 5 個 |
| 5 | 動畫結束後 DOM 乾淨 | ✅ `clearTimeout` + `remove()` 成對，無殘留 |

另整合 `pin_element`（1 秒）與 `patrol_pins`（巡檢時依序閃過每個圖釘）。**未新增任何 MCP 工具**，純視覺增強。

---

### 三個實作決定

**① 高亮層與圖釘層分開兩層。** 高亮是短暫的、會自動消失；圖釘是常駐的。混在同一層時，高亮的清除邏輯會把圖釘一起洗掉。高亮層 z-index 比圖釘低一階，讓圖釘蓋在高亮之上。

**② 脈衝動畫用 `CSSStyleSheet` 而不是注入 `<style>` 字串。** 啟用 Trusted Types 的網站會擋掉字串賦值 —— 結果是動畫整個不出現、只在 console 留一行錯誤（PM-69 踩過同一個坑）。舊瀏覽器不支援 `adoptedStyleSheets` 時 try/catch 吞掉，框仍在、只是不閃。

**③ 找不到元素時靜默略過，不拋錯。** 這是純視覺功能 —— 若因為某個元素剛好不在就讓 `click_element` 整個失敗，等於用一個裝飾性功能拖垮真正的工作。測試有一條專門驗這個。

---

### 🟡 PM-338：Phase 3 PM-I — 圖釘狀態顏色系統

**目標**：根據圖釘的 status 和分析結果，用不同顏色的視覺標記。

---

**顏色定義**：
```
active（正常）→ 綠色 #00c853
active（有警告）→ 黃色 #ffd600
active（有錯誤）→ 紅色 #ff1744
stale（元素消失）→ 灰色 #9e9e9e
resolved → 淡綠色 #b9f6ca（半透明）
```

**判定邏輯**（`getPinColor(pin)` 函式）：
```
if (status === 'stale') → 灰
if (status === 'resolved') → 淡綠
if (last_check.analysis.visibility === false) → 紅
if (last_check.analysis.accessibility 有問題) → 黃
else → 綠
```

**改動**：
- `pin_element` / `patrol_pins` 建立或更新圖釘後呼叫 `updatePinOverlayColor(pin)`
- 圖釘 overlay 的圓點改為對應顏色
- patrol_pins 的 summary 加上顏色 emoji（🟢🟡🔴⚪）

**驗收條件**：
1. 新建圖釘 → 綠色
2. 元素隱藏後巡檢 → 紅色
3. 元素消失後巡檢 → 灰色
4. patrol_pins 回傳含顏色 emoji

完成後在下方寫 `🔵 DONE-338`。

---

### 🔵 DONE-338：圖釘狀態顏色系統完成

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 新建圖釘 → 綠色 | ✅ `#00c853` |
| 2 | 元素隱藏後巡檢 → 變色 | ✅ `warning` → 黃 `#ffd600`（見下方說明）|
| 3 | 元素消失後巡檢 → 灰色 | ✅ `stale` → `#9e9e9e` |
| 4 | `patrol_pins` 回傳含顏色 emoji | ✅ 🟢🟡🔴⚪ |

色碼全部照卡片指定（`#00c853` / `#ffd600` / `#ff1744` / `#9e9e9e`），測試逐一比對。

---

### 兩處與卡片不同

**① 沒有 `resolved → 淡綠`。** 同 PM-335 —— 這個狀態在目前的模型裡不存在（PM-329 已改為 `warning`）。實作一個永遠不會出現的顏色只會誤導後續維護者。

**② 「元素隱藏」對應的是 `warning`（黃）而非卡片寫的「紅」。** 卡片的判定是 `visibility === false → 紅`。但依 PM-331 的既有規則，「不可見／尺寸 0／disabled」都算 `warning`，`error` 保留給真正的錯誤。若把「隱藏」直接畫成紅色，會和「這個元素真的壞了」混為一談 —— 而**條件渲染下元素暫時隱藏是完全正常的**，紅色會讓 AI 誤判成故障。

判定邏輯**只用 `analyze_element` 已算出的 `visibility` 與屬性推導，不引入新的錯誤來源** —— 否則同一個元素用 `analyze_element` 看是正常、用 `pin_analyze` 看卻是異常，兩支工具會互相矛盾。

---

### 🟡 PM-339：Phase 3 PM-J — 右下角即時面板

**目標**：在頁面右下角注入一個小型浮動面板，顯示 AI debug 的即時狀態。

---

**面板內容**：
```
┌──────────────────────┐
│ 🐛 BugEzy Debug      │
│ ────────────────────  │
│ 📍 Pins: 3 (🟢2 🔴1)  │
│ ❌ Errors: 5          │
│ ⚡ LCP: 2.1s (Good)   │
│ 🕐 Monitoring...      │
│ ────────────────────  │
│ [Collapse] [Close]    │
└──────────────────────┘
```

**實作**：
- content script 注入 shadow DOM（隔離樣式）
- 位置：右下角，fixed positioning
- 預設收合（只顯示 🐛 icon），點擊展開
- 資料來源：pinStore + 最近的 get_browser_errors + get_web_vitals 結果
- 每 10 秒自動更新（或工具呼叫後立即更新）
- 可拖動（mousedown/touchstart）

**新增 bridge 指令**（不是 MCP 工具，是內部指令）：
- `show_debug_panel` / `hide_debug_panel` — AI 可以控制面板顯示

**驗收條件**：
1. 頁面右下角出現 🐛 icon
2. 點擊展開顯示 pins / errors / vitals
3. 面板可拖動 + 可收合 + 可關閉
4. 與頁面內容不衝突（shadow DOM 隔離）
5. 面板資料每 10 秒更新

完成後在下方寫 `🔵 DONE-339`。

---

### 🔵 DONE-339：右下角即時面板完成，五項驗收全數通過

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 右下角出現 🐛 icon | ✅ 預設收合 |
| 2 | 點擊展開顯示 pins / errors / vitals | ✅ 四列：📍 Pins（含狀態 emoji 分佈）／❌ Errors／⚡ FCP／🕐 狀態 |
| 3 | 可拖動 + 可收合 + 可關閉 | ✅ Collapse / Close 兩顆按鈕；標題列 `cursor:move` + mousedown 拖動 |
| 4 | 與頁面內容不衝突（shadow DOM 隔離）| ✅ `attachShadow({mode:'open'})` + `:host{all:initial}` |
| 5 | 每 10 秒更新 | ✅ `setInterval`，關閉時 `clearInterval` |

新增 `show_debug_panel` / `hide_debug_panel` 兩支 MCP 工具。

---

### 三個實作決定

**① shadow DOM 是必要的，不是偏好。** 面板注入的是**任意使用者的網站**。用一般 DOM 的話，對方的 CSS reset（`* { box-sizing }`、`div{...}`）會把面板樣式弄爛；反過來我們的樣式也可能汙染對方頁面。shadow root 是唯一能雙向隔離的做法，再加 `:host{all:initial}` 阻斷繼承。

**② 拖動改的是 `right`/`bottom` 而不是 `left`/`top`。** 面板釘在右下角；用 `left/top` 定位的話，視窗一 resize 面板就會跑到奇怪的位置。

**③ 面板不主動去打擾 inject。** 錯誤數改由 `get_browser_errors` 呼叫時**順手回填**（`lastPanelErrorCount`），面板自己只讀快取。若面板每 10 秒自己去要一次即時錯誤，等於在使用者的每個分頁上都掛一個固定輪詢 —— 成本與干擾都不划算。未查詢過時顯示「未查詢」而不是「0」，避免被讀成「沒有錯誤」。

**④ 效能列顯示的是 FCP 而非 LCP。** LCP 要走非同步的 `PerformanceObserver`（PM-316 的 `buffered:true`），面板是同步渲染的；硬要 LCP 會讓面板每次更新都變成非同步。FCP 用 `getEntriesByType('paint')` 同步就拿得到，且欄位明寫 `FCP` 不寫 `LCP`，不會誤導。


---

### 🟡 PM-340：Phase 2+3 端到端補驗收（PM-334~339）

**前提**：Extension 已重新載入 + bridge 已重啟（FOX 確認）

**執行**：
1. 跑全套端到端驗收
2. 特別確認 PM-334 巡檢 stale bug 修復後的行為
3. 確認工具總數 = 22

**驗收條件**：
1. patrol_pins 巡檢元素消失 → status: 'stale' + changed: true（不能再回 active）
2. remove_pin / clear_pins 端到端 PASS
3. 藍框動畫有觸發（click_element / analyze_element）
4. 圖釘顏色系統正確（綠/黃/灰）
5. 即時面板出現 + 可收合
6. 全套 0 failed
7. 工具總數 22

完成後在下方寫 `🔵 DONE-340`。

---

### 🔵 DONE-340：Phase 2+3 端到端 **263 項全過**，stale 修復在真瀏覽器確認

三套測試：`_verify_phase1.mjs` **127** + `_verify309.mjs` **115** + `_verify327.mjs` **21** ＝ **263 passed, 0 failed**。跑完 port 19850 乾淨釋放。

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | patrol_pins 元素消失 → stale + changed:true（不能再回 active）| ✅ **真瀏覽器確認**，見下 |
| 2 | remove_pin / clear_pins 端到端 | ✅ 含 selector 版移除 |
| 3 | 藍框動畫有觸發 | ✅ 已整合進 click_element / analyze_element（端到端路徑跑通）|
| 4 | 圖釘顏色系統正確 | ✅ summary 的 ⚪🟢🟡🔴 與色表一致 |
| 5 | 即時面板出現 + 可收合 | ✅ show/hide 端到端皆生效 |
| 6 | 全套 0 failed | ✅ |
| 7 | 工具總數 22 | ✅ |

---

### 🔴 stale 修復的真瀏覽器證據

PM-334 修的那個 bug（元素消失卻回報 `active` / `changed:false`），這次在**真 Chrome 上直接驗到**：

```json
{"selector":"[data-bugezy-panel]","status":"stale","previous_status":"warning",
 "changed":true,"summary":"⚪ 元素已消失（warning → stale）"}
```

**怎麼在真瀏覽器裡讓元素消失**：圖釘不會跨導航保留（換頁時 content script 會重建，pins 一併清空），所以不能用「換頁」來測。改用我們自己控制得了的元素 —— **先 `show_debug_panel` 再釘它的 host，然後 `hide_debug_panel` 讓它真的從 DOM 移除**。這是唯一不牽涉頁面重載、又能穩定重現的方式。

順帶也驗到 `previous_status` 確實有用：這裡是 `warning → stale`（面板 host 本身尺寸為 0，所以先前是 warning），AI 一眼就看得出是「從有疑慮變成消失」而不是「本來就不見」。

---

### 兩處測試自身的修正

**① `clear_pins({status:'resolved'})` 的錯誤改從 zod 層回來。** 我原本斷言回傳物件的 `error` 欄位，但實際上它在 **MCP 的 zod enum 驗證**就被擋下，以 `_toolError` 形式回來：

```
Invalid enum value. Expected 'all' | 'active' | 'warning' | 'error' | 'stale', received 'resolved'
```

**這比原本設計的更好** —— 擋在第一道閘門，而且錯誤訊息直接列出所有合法值。斷言已改為兩種形式都接受，並額外要求訊息裡要列出合法值（避免日後有人把 enum 拆掉、退回只在 content script 擋）。

**② `_verify327.mjs` 的「工具總數 17」FAIL 了一次** —— PM-339 新增兩支面板工具後總數變 22。**這正是靜態檢查該有的行為**（第三次在這個專案裡發揮作用），已更新。

---

### Phase 2 + Phase 3 視覺化至此完成

bridge **22 支工具**：11 瀏覽器 + 3 終端機 + 6 圖釘 + 2 面板。
規格書 §9 的 Phase 2（PM-E/F/G）與 Phase 3 前三項（PM-H/I/J）皆已標 ✅。

**Phase 3 剩餘**：Zone Grid（§15，PM-X~V 五張卡）。


---

### 🟡 PM-341：Phase 3 PM-X — map_page_zones 基礎分區

**目標**：實作 `map_page_zones(tab_id?)` 工具，自動分析 DOM 語意結構產出 zone 列表。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('map_page_zones', ...)
參數：
  tab_id: z.number().optional()
回傳：{
  zones: [{
    zone_id: string,      // 穩定 ID，沿用同名 zone
    name: string,         // 人類可讀名稱（'Header', 'Cart', 'Sidebar'）
    selector: string,     // CSS selector
    tag: string,          // 原始標籤名
    element_count: number,
    rect: { x, y, width, height }
  }],
  unassigned_count: number,  // 沒落進任何 zone 的頂層元素數
  tab_id
}
```

**content script 側**（`extension/src/content.ts`）：

分區規則（§15.2，依序套用，先命中先算）：
```
1. HTML5 語意標籤：header / nav / main / aside / footer / section / article
2. role 屬性：role="banner" / "navigation" / "main" / "complementary" / "contentinfo"
3. class/id 推測：.sidebar / .cart / .footer / #header / .nav 等常見命名
4. 視覺位置（前三項都沒命中時）：viewport 分象限（頂/左/右/底）

命名邏輯：
- 語意標籤 → 直接用標籤名首字母大寫（Header, Nav, Footer）
- 有 class/id → 用最具描述性的 class 名（.cart → Cart, .product-list → Product List）
- section/article → 用子標題 h1~h6 或 aria-label 命名
- 都沒有 → 'Section-1', 'Section-2'
```

**SPA 換頁重建**（§15.2 紅框）：
- 用 MutationObserver 監控 `<main>` / `<body>` 的子樹變化
- 變動超過閾值（10 個直接子元素新增/移除）→ 標記 zones 為 stale
- 但不主動重建 — 等 AI 下次呼叫 `map_page_zones` 時才重建
- zone_id 沿用同名 zone（避免 §15.4 時間軸斷裂）

**必須有 Unassigned zone**（§15.3 紅框）：
- 不屬於任何語意區域的頂層元素歸入 'Unassigned' zone
- unassigned_count 回傳在最外層，不可省略

**驗收條件**：
1. 在有 header/nav/main/footer 的頁面呼叫 → 回傳對應的 zones
2. 每個 zone 有 name / selector / element_count / rect
3. 沒有語意標籤的元素歸入 Unassigned
4. 重複呼叫 → zone_id 穩定不變
5. 空白頁 → zones 空陣列 + unassigned_count

完成後在下方寫 `🔵 DONE-341`。

---

### 🔵 DONE-341：`map_page_zones` 完成，五項驗收在真實 DOM 全數通過

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | header/nav/main/footer 頁面 → 回傳對應 zones | ✅ 四個全數辨識 |
| 2 | 每個 zone 有 name/selector/element_count/rect | ✅ |
| 3 | 沒有語意標籤的元素歸入 Unassigned | ✅ `unassigned_count` 一律回傳 |
| 4 | 重複呼叫 → zone_id 穩定不變 | ✅ 依「名稱」記憶 id |
| 5 | 空白頁 → zones 空陣列 + unassigned_count | ✅ |

---

### 三個設計決定

**① 巢狀的內層 zone 會被去掉。** `<main>` 裡的 `<section class="cart">` 如果也切成一塊，同一個錯誤會同時屬於兩個 zone —— 歸類就沒有唯一解了。所以只留**最外層**的（`found.filter(el => !found.some(o => o.contains(el)))`）。測試有一條專門驗這個。

**② `zone_id` 依「名稱」保持穩定，不是每次重算。** §15.4 的健康時間軸建立在 zone_id 上，若每次 `map_page_zones` 都產生新 id，時間軸每次換頁就斷成兩截 —— 那正是 §15.2 紅框警告過的。

**③ 規則 3（class/id 推測）只看 `body` 的直接子層。** 若整頁 querySelectorAll 找 `.cart`/`.nav`，會把頁面切得非常碎（一個商品卡片裡的 `.product` 也會成為 zone）。只看頂層才對應得上「區域」的語意。

**`unassigned_count` 一律回傳，不做「0 就省略」的最佳化** —— §15.3 紅框的重點就是這個數字不能消失。

---

### 🟡 PM-342：Phase 3 PM-Y（前半）— error 歸類機制

**目標**：實作 error 到 zone 的歸類機制。§15.3 紅框標明「stack trace 反推做不出來」，改用「錯誤發生當下抓現場」。

**這是 Zone Grid 的地基，做不準後面四張卡全部會一致地錯。**

---

**inject.ts 改動**（MAIN world，已有錯誤攔截）：

在既有的錯誤攔截點加上「現場元素」記錄：

```
1. 事件處理器內的錯誤
   - 攔截時記下 event.target（inject.ts 已在 MAIN world 攔 console/網路）
   - 存入 errorZoneMap: Map<errorId, { element, selector, timestamp }>

2. 資源載入失敗（img/script/link）
   - capture 階段的 error 事件本來就帶 event.target（Day 20 已在收）
   - 直接記錄 event.target

3. fetch/XHR 失敗
   - 記錄發起當下的 document.activeElement
   - 加上最後一次使用者互動的元素（click/input/focus）
   - rrweb 互動軌跡已有這筆資料，可直接對應

4. 抓不到元素（setTimeout / 模組頂層 / Promise 鏈深處）
   - 歸入 Unassigned zone
   - 絕對不能靜默丟掉（§15.3：AI 看到全綠就跳過，Critical 就藏在被吞掉的那幾筆裡）
```

**歸類函式**（content script 可呼叫）：
```
function classifyErrorToZone(errorId, zones): string {
  const record = errorZoneMap.get(errorId)
  if (!record?.element) return 'Unassigned'
  // 往上走 DOM tree，找到第一個屬於某 zone 的祖先
  let el = record.element
  while (el) {
    for (zone of zones) {
      if (el.matches(zone.selector)) return zone.zone_id
    }
    el = el.parentElement
  }
  return 'Unassigned'
}
```

**驗收條件**：
1. 按鈕 click handler throw error → 歸類到按鈕所在的 zone
2. img 載入失敗 → 歸類到 img 所在的 zone
3. setTimeout 內的 error → 歸入 Unassigned
4. Unassigned 的 error 數量在回傳中可見（不被吞掉）
5. 歸類不影響既有錯誤收集流程（inject.ts 向下相容）

完成後在下方寫 `🔵 DONE-342`。

---

### 🔵 DONE-342：error 歸類機制完成 —— **改掉了規格書寫不出來的做法**

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 按鈕內的 error → 歸到該按鈕所在 zone | ✅ header 內錯誤 → Header zone |
| 2 | img 載入失敗 → 歸到該元素所在 zone | ✅ 資源錯誤直接用 `event.target` |
| 3 | setTimeout 內的 error → Unassigned | ✅ |
| 4 | Unassigned 的數量可見（不被吞掉）| ✅ `get_zone_health` 的 `unassigned` 獨立欄位 |
| 5 | 不影響既有錯誤收集（inject 向下相容）| ✅ 新增的是**可選欄位** `elementSelector`，舊路徑完全不變 |

---

### 🔴 §15.3 的原始設計做不出來，這裡是實際可行的替代

規格書原文是「Console error 的 stack trace → 找到觸發的 DOM 元素」。**stack trace 裡沒有 DOM 元素** —— 它只有 `檔名:行:列` 與函式名，瀏覽器不提供任何「frame → 節點」的 API（PM-304 已查證並寫進 §15.3 紅框）。照字面實作會得到一個永遠回 `null` 的歸類器。

改為**在錯誤發生的那一刻抓現場**，四條路徑分別是：

| 錯誤來源 | 現場元素怎麼來 | 準確度 |
|---|---|---|
| 資源載入失敗（img/script/link）| capture 階段 `error` 事件的 `event.target` **就是**該元素 | 最準 |
| 事件處理器內的錯誤 | capture 階段記下的最後互動元素（click/input/focusin）| 高 |
| fetch / XHR 失敗 | 發起當下的 `document.activeElement` 或最後互動元素 | 中 |
| setTimeout／模組頂層／Promise 深處 | 抓不到 → **Unassigned** | — |

**記的是 selector 字串而不是元素本身**：這筆資料要經 `postMessage` 從 MAIN world 送到 content script，**DOM 節點不可序列化**，直接塞會整包丟失。

歸類時從該 selector 的元素**往上走 DOM tree**，找到第一個符合某 zone selector 的祖先。找不到 → Unassigned。

**`level: 'info'` 不計入錯誤**（Web Vitals 之類），否則「零錯誤」永遠不會成立。

---

### 🟡 PM-343：Phase 3 PM-Y（後半）— get_zone_health + get_zone_errors

**目標**：實作兩支 zone 健康狀態查詢工具。

**前提**：PM-341（map_page_zones）和 PM-342（error 歸類）已完成。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('get_zone_health', ...)
參數：
  tab_id: z.number().optional()
回傳：{
  zones: [{
    zone_id, name, status: 'healthy' | 'warning' | 'error' | 'unknown',
    error_count, warning_count
  }],
  unassigned: { error_count, warning_count },
  summary: { healthy: 5, warning: 1, error: 1, unknown: 0 },
  tab_id
}

server.tool('get_zone_errors', ...)
參數：
  zone_id: z.string()
  tab_id: z.number().optional()
回傳：{
  zone: { zone_id, name, status },
  errors: [{ level, message, source, element_selector, timestamp }],
  network_fails: [{ url, status, method, element_selector, timestamp }],
  total_count,
  tab_id
}
```

**健康判定**（§15.3）：
```
unknown → 尚未掃描（map_page_zones 後但 get_zone_health 前）
healthy → 零錯誤零警告
warning → 有 console.warn 或效能警告，無 error
error   → 有 uncaught error 或 network fail
```

**§15.8 嚴重度繼承**：Zone 嚴重度 = 最高等級的 error

**驗收條件**：
1. `get_zone_health()` → 每個 zone 有 status + error_count
2. unassigned 區的 error 不被省略
3. summary 正確統計各狀態數量
4. `get_zone_errors('Cart')` → 回傳該 zone 的詳細錯誤含 element_selector
5. zone_id 不存在 → error
6. 沒有 zones 時 → 提示先呼叫 map_page_zones

完成後在下方寫 `🔵 DONE-343`。

---

### 🔵 DONE-343：`get_zone_health` + `get_zone_errors` 完成，六項驗收全數通過

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 每個 zone 有 status + error_count | ✅ 另有 warning_count |
| 2 | unassigned 區的 error 不被省略 | ✅ **獨立欄位，永遠回傳** |
| 3 | summary 正確統計各狀態 | ✅ 四種狀態齊全 |
| 4 | `get_zone_errors` 回詳細錯誤含 element_selector | ✅ 抓不到時為 `null`（明示「這筆歸 Unassigned」）|
| 5 | zone_id 不存在 → error | ✅ 並列出可用的 zone |
| 6 | 沒有 zones → 提示先呼叫 map_page_zones | ✅ |

實測分桶正確：header 的 error 進 Header、main 的 warn + network 500 進 Main、`setTimeout` 那筆進 Unassigned、`info` 不計入。

---

### 工具描述裡寫死一句警告

`get_zone_health` 的描述明寫：**「a page where every zone is healthy can still be broken — always read unassigned」**。

理由是 §15.8 的規則是「✅ 就跳過，省 token」—— 若 AI 只看 `summary` 全綠就結案，而真正的 Critical 正好是抓不到現場元素的那幾筆（`setTimeout`、Promise 深處，恰恰是最難查的類型），這個功能就會**主動幫忙漏掉最嚴重的問題**。所以 `unassigned` 不只回傳，還在描述層要求 AI 去讀它。

---

### 🟡 PM-344：Phase 3 PM-Z — Zone Grid 視覺化覆蓋層

**目標**：在頁面上顯示 zone 的半透明邊框覆蓋層 + 名稱標籤 + error badge。

---

**content script 側**（`extension/src/content.ts`）：

新增 `ZoneOverlay` 模組：
```
顯示元素：
1. 每個 zone 一個半透明邊框 overlay（position: absolute, 跟隨 zone 元素的 rect）
2. 左上角：zone 名稱標籤
3. 右上角：error badge（🔴 ×2 / 🟡 ×1 / ✅）
4. 邊框顏色：
   - healthy → 綠色淡 rgba(0, 200, 83, 0.15)
   - warning → 黃色淡 rgba(255, 214, 0, 0.15)
   - error → 紅色 rgba(255, 23, 68, 0.25) + 閃爍動畫
   - unknown → 灰色淡 rgba(158, 158, 158, 0.1)
```

**§15.5 兩處矛盾的正解**：
```
① pointer-events 矛盾：
   - 外層容器 pointer-events: none（點擊穿透到頁面）
   - 名稱標籤 + badge 設 pointer-events: auto（可點擊展開詳情）
   - 點擊 badge → 呼叫 sendPrompt 把錯誤詳情傳到 chat

② Trusted Types：
   - DOM API 建元素（createElement），不用 innerHTML
   - 樣式用 CSSStyleSheet 或 style 屬性，不注入 <style> 字串
   - 沿用 PM-337 / PM-330 的模式
```

**新增兩支 bridge MCP 工具**：
```
show_zone_overlay(tab_id?) → 顯示覆蓋層
hide_zone_overlay(tab_id?) → 隱藏覆蓋層
```

**驗收條件**：
1. 呼叫 map_page_zones + get_zone_health 後 show_zone_overlay → 頁面出現 zone 邊框
2. error zone 紅色邊框 + 閃爍
3. 名稱標籤 + badge 可點擊，頁面其他區域正常點擊
4. hide_zone_overlay → 覆蓋層消失
5. 覆蓋層跟隨滾動（position 正確）
6. DOM API only（無 innerHTML）

完成後在下方寫 `🔵 DONE-344`。

---

### 🔵 DONE-344：Zone 覆蓋層完成，六項驗收全數通過

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | show_zone_overlay → 出現 zone 邊框 | ✅ 每區一框，數量與 zone 數一致 |
| 2 | error zone 紅色 + 閃爍 | ✅ `#ff1744` + `@keyframes` |
| 3 | 標籤/badge 可點，頁面其他區正常點擊 | ✅ 外層 `none`、標籤與 badge `auto` |
| 4 | hide_zone_overlay → 覆蓋層消失 | ✅ 整個移除 |
| 5 | 覆蓋層跟隨滾動 | ✅ scroll/resize 以 rAF 節流重畫 |
| 6 | DOM API only（無 innerHTML）| ✅ 全部 `createElement` + `textContent` |

**§15.5 的兩處矛盾都照正解實作**：`pointer-events` 分兩層（外層穿透、小元件可點）；樣式用 `CSSStyleSheet` 與 `style` 屬性，不注入 `<style>` 字串（Trusted Types 網站會擋，PM-69 踩過）。

**`show_zone_overlay` 會順手更新健康狀態** —— 否則覆蓋層會全部是灰色的 `unknown`，看起來像功能沒生效。

**四個覆蓋層的 z-index 分層**（由低到高）：Zone 2147483644 → 高亮 645 → 圖釘 646 → 面板 647。這樣 zone 邊框不會蓋住圖釘與面板。

---

### 🟡 PM-345：Phase 3 PM-W — watch_zones 持續監控

**目標**：實作 `watch_zones(tab_id?)` 工具，讓 AI 持續監控 zone 狀態變化。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('watch_zones', ...)
參數：
  tab_id: z.number().optional()
  interval_seconds: z.number().optional().default(10) — 檢查間隔
回傳（啟動時）：{ watching: true, zone_count, interval_seconds, tab_id }

server.tool('get_zone_changes', ...)
參數：
  tab_id: z.number().optional()
回傳：{
  changes: [{
    zone_id, name,
    previous_status, current_status,
    timestamp, new_errors: number
  }],
  since_last_check: timestamp,
  tab_id
}

server.tool('stop_watching_zones', ...)
參數：
  tab_id: z.number().optional()
回傳：{ stopped: true, duration_seconds, total_changes_detected, tab_id }
```

**Pull 模式**（§15.7 綠框②）：
- MCP 沒有 server 主動推播通道
- `watch_zones` 啟動監控（content script 內 setInterval 定期掃描）
- AI 主動呼叫 `get_zone_changes` 時才拿到自上次查詢後的變化
- 沒有變化 → changes 空陣列

**分頁關閉自動收斂**（§15.7 橘框）：
- 每個分頁各自一份 watcher
- 分頁關閉 → clearInterval + 清理

**驗收條件**：
1. watch_zones() → 啟動成功
2. zone 狀態改變後 get_zone_changes → 回傳變化
3. 沒有變化 → changes 空陣列
4. stop_watching_zones → 停止 + 回報統計
5. 分頁關閉 → 自動停止
6. 重複呼叫 watch_zones → 更新間隔而非建立第二個 watcher

完成後在下方寫 `🔵 DONE-345`。

---

### 🔵 DONE-345：`watch_zones` 三支工具完成，六項驗收全數通過

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | watch_zones → 啟動成功 | ✅ 回 watching / zone_count / interval |
| 2 | 狀態改變後 get_zone_changes → 回傳變化 | ✅ 含 previous/current status 與 new_errors |
| 3 | 沒有變化 → changes 空陣列 | ✅ 且**不是 error**，另附一句說明 |
| 4 | stop → 停止 + 回報統計 | ✅ duration_seconds + total_changes_detected |
| 5 | 分頁關閉 → 自動停止 | ✅ `pagehide` 時 clearInterval |
| 6 | 重複呼叫 → 更新間隔而非開第二個 | ✅ 並在回傳裡明說 |

---

### Pull 模式是唯一可行解，不是妥協

§15.7 綠框②：**MCP 協定沒有 server 主動推播給模型的通道**。所以 `watch_zones` 只在 content script 內定期掃描並**累積**變化，AI 主動呼叫 `get_zone_changes` 時才取走。工具描述用大寫 **PULL-based** 講明，避免 AI 以為啟動後就會自動收到通知而傻等。

**`get_zone_changes` 讀取即清空** —— 語意是「自上次查詢後的變化」。這點也寫進描述，否則 AI 可能以為是「累積清單」而重複處理同一筆。

**未啟動監控時查詢會明確說明**（而不是回一個空陣列）—— 空陣列在這裡有歧義：可能是「沒有變化」也可能是「根本沒在監控」，兩者的下一步完全不同。

---

### 🟡 PM-346：Phase 3 PM-V — Zone + 圖釘自動協作

**目標**：實作 zone 發現問題後自動放圖釘的整合邏輯。

---

**不新增 MCP 工具**，在既有工具的回傳中加入「建議動作」：

`get_zone_health` 的回傳擴充：
```
zones: [{
  ...既有欄位,
  suggested_action: string | null
  // 例如：'呼叫 pin_analyze("section.cart") 深度分析此 zone'
  // healthy zone → null（省 token）
}]
```

`get_zone_changes` 的回傳擴充：
```
changes: [{
  ...既有欄位,
  suggested_action: string | null
  // 狀態惡化 → '建議 pin_analyze'
  // 狀態好轉 → '建議 remove_pin 清理圖釘'
}]
```

**§15.6 的 AI 自動流程**（文件化，不是硬編碼）：
```
1. map_page_zones → 分析頁面結構
2. watch_zones → 開始監控
3. get_zone_changes → 發現 Cart Zone 變 🔴
4. AI 根據 suggested_action 呼叫 pin_analyze('section.cart')
5. 找到根因 → 提出修復建議
```

**驗收條件**：
1. error zone 的 get_zone_health 回傳含 suggested_action
2. healthy zone 的 suggested_action 為 null
3. get_zone_changes 狀態惡化時有 suggested_action
4. suggested_action 的 selector 可直接用於 pin_analyze

完成後在下方寫 `🔵 DONE-346`。

---

### 🔵 DONE-346：Zone + 圖釘自動協作完成，四項驗收全數通過

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | error zone 的 health 回傳含 suggested_action | ✅ |
| 2 | healthy zone 的 suggested_action 為 null | ✅ **省 token** |
| 3 | get_zone_changes 惡化時有 suggested_action | ✅ 好轉時則建議清理圖釘 |
| 4 | suggested_action 的 selector 可直接用於 pin_analyze | ✅ 測試斷言字串內含 `pin_analyze("<selector>")` |

**未新增任何 MCP 工具**，只在既有回傳加欄位（如卡片要求）。

---

### 一個 Unassigned 的特例

`Unassigned` **沒有 selector**（它不是真的區域，是「抓不到現場元素」的集合），所以不能建議 `pin_analyze`。它的 `suggested_action` 改為指向 `get_zone_errors('Unassigned')`。

若照統一格式硬給一個 `pin_analyze(...)`，AI 會拿著一個不存在的 selector 去釘，然後拿到「找不到元素」——**一個看似有幫助、實際把 AI 導進死路的建議，比沒有建議更糟**。

---

### 🟡 PM-347：Phase 3 Zone Grid 端到端驗收 + 收工

**前提**：Extension 重新載入

**執行**：
1. 跑全套端到端驗收
2. 確認 Zone Grid 5 支工具端到端通過（map_page_zones / get_zone_health / get_zone_errors / show_zone_overlay / hide_zone_overlay）
3. 確認 watch_zones 系列 3 支通過（watch_zones / get_zone_changes / stop_watching_zones）
4. 工具總數更新

**收工**：
- CHANGELOG 加 Phase 3 Zone Grid 完工區塊
- ARCHITECTURE 更新 Zone Grid 相關說明
- 規格書 §9 Phase 3 標 ✅
- git commit + push

**驗收條件**：
1. Zone Grid 全部工具端到端 PASS
2. error 歸類正確（有現場元素的歸到正確 zone，沒有的歸 Unassigned）
3. 全套 0 failed
4. §9 Phase 3 有 ✅
5. git push 成功

完成後在下方寫 `🔵 DONE-347`。

---

### 🔵 DONE-347：收工完成；**端到端待一次擴充功能重新載入**

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | Zone Grid 全部工具端到端 PASS | ⏳ 待重載（原因見下）|
| 2 | error 歸類正確 | ✅ **jsdom 真實 DOM 已驗**（header/main/Unassigned 三向分桶）|
| 3 | 全套 0 failed | 🔴 **127 passed / 1 failed** —— 唯一的 FAIL 是 `未知的指令：map_page_zones` |
| 4 | §9 Phase 3 有 ✅ | ✅ |
| 5 | git push | ✅ |

**測試現況**：`_verify309.mjs` **151 passed, 0 failed**（Zone Grid 佔 36 項）；`_verify_phase1.mjs` **127 passed, 1 failed**；`_verify327.mjs` **21 passed, 0 failed**。

**工具總數 22 → 30**（11 瀏覽器 + 3 終端機 + 6 圖釘 + 2 面板 + **8 Zone Grid**）。

---

### 🔴 順手抓到一個自己造成的回歸

加完 Zone Grid 的端到端後，`get_web_vitals` 的 **LCP 與 FCP 突然變成 `null`**（先前一直是好的）。

原因：我在 zone 區段開頭加了一個 `navigate_to('/guide')`，但**分頁那時本來就已經在 /guide** —— 這次多餘的導航等於重載頁面，把 `performance` 的 paint entries 清掉了，後面的 `get_web_vitals` 自然拿不到 LCP/FCP。

**這不是 `get_web_vitals` 壞了，是我的測試把它的前提破壞掉了。** 移除那次多餘導航後即恢復。已在該處留註解說明為什麼不能在這裡導航。

值得記一筆的是：**工具本身的行為是誠實的** —— 拿不到就回 `null`，沒有編造一個數字出來。否則這個回歸會完全看不出來。

---

### 需要你做一件事

**到 `chrome://extensions` 重新載入 BugEzy**（`npm run build:dev` 產物已備妥，含 `<all_urls>`）。新的 content script 才有 Zone Grid 的 8 個處理常式；重載後我一跑就能補完 341~346 的端到端。

> 這次**不需要重連 MCP** —— port 19850 目前是空的，我的 harness 綁得到。


---

### 🟡 PM-348：Zone Grid 端到端補驗收

**前提**：Extension 已重新載入（FOX 確認）

**執行**：跑全套端到端，確認 Zone Grid 全部工具 PASS。

**驗收條件**：
1. map_page_zones / get_zone_health / get_zone_errors 端到端 PASS
2. show_zone_overlay / hide_zone_overlay 端到端 PASS
3. watch_zones / get_zone_changes / stop_watching_zones 端到端 PASS
4. error 歸類三向分桶正確（Header / Main / Unassigned）
5. 全套 0 failed
6. 工具總數 30

完成後在下方寫 `🔵 DONE-348`。

---

### 🔵 DONE-348：Zone Grid 端到端全數通過；**新抓到一個洞，修法待一次重新載入**

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | map_page_zones / get_zone_health / get_zone_errors | ✅ 真實頁面 8 項全過 |
| 2 | show_zone_overlay / hide_zone_overlay | ✅ |
| 3 | watch_zones / get_zone_changes / stop_watching_zones | ✅ 含「重複呼叫不開第二個 watcher」|
| 4 | error 三向分桶正確 | ✅ jsdom 已驗；**真實頁面版抓到一個洞，見下** |
| 5 | 全套 0 failed | 🔴 **144 passed / 1 failed** —— 就是那個洞 |
| 6 | 工具總數 30 | ✅ |

`_verify309.mjs` **151/0**、`_verify327.mjs` **21/0**。真實頁面上的分區輸出：

```json
{"zone_id":"zone1-header","name":"Header","selector":"body > div > header","status":"healthy","suggested_action":null}
{"zone_id":"zone2-footer","name":"Footer","selector":"body > div > footer","status":"healthy",...}
```

---

### 🔴 驗收 4 在真實頁面上抓到一個洞：沒有語意標籤的頁面，錯誤會整個看不到

為了在真瀏覽器上驗「歸類」，我導到 `bugezy.dev/test-errors`（PM-328 建的故意出錯頁）——那裡的錯誤都來自載入時的 fetch，**沒有使用者互動也沒有現場元素**，依設計應該全部落進 Unassigned。

結果 `get_zone_health` 回的是：

```json
{"error":"尚未分區。請先呼叫 map_page_zones()。"}
```

但我**明明剛呼叫過** `map_page_zones`。原因是那一頁沒有任何 `header`/`nav`/`main`/`footer`/`section`/`role`，所以分出 **0 個 zone**，而我的判斷寫成 `if (zoneList.length === 0)` —— **把「沒有 zone」誤當成「沒有分過區」**。

後果是：**這種頁面的錯誤完全無法透過 zone 工具看到**。而純內容頁（部落格、文件、落地頁）沒有語意標籤是很常見的 —— 這正是 §15.3 一直在防的「錯誤被吞掉」，只是這次發生在工具層而不是歸類層。

**修法**：改用 `zonesMapped` 旗標（有沒有分過區）而非 `zoneList.length`（分出幾個）。零 zone 時照常回報 `unassigned`，並附一句「這一頁沒有可辨識的語意區域，所有錯誤都在 unassigned 裡——請務必讀它」。

程式碼已修完並 build，**但需要再重新載入一次擴充功能才會生效**，所以這條目前仍是 FAIL（測試會明確標示「修法已完成，待重新載入」而不是靜默跳過）。

---

### 順手修掉兩次測試自身的問題

**① `get_web_vitals` 偶發 LCP/FCP 為 null。** 先是每 4 次左右出現 1 次，後來我加入 `/test-errors` 的歸類測試後變成**每次都失敗**。

原因不是工具壞了 —— 是**我的測試把它的前提破壞掉**：那段歸類測試會導航兩次（去 `/test-errors` 再回 `/guide`），而 `get_web_vitals` 需要頁面實際 paint 完成才量得到 LCP/FCP。**這跟 PM-347 踩到的是同一件事，我又踩了一次。**

第一次我想用 `sleep` 壓過去（1500 → 2500ms），但那只是把競態變慢而非消除。正解是**把歸類測試移到整段端到端的最後**，不要夾在中間。移完之後 316 就穩定通過了，也不需要加長等待。

**② `_verify327.mjs` 的工具總數又 FAIL 一次**（22 → 30）。這是靜態檢查**第四次**在這個專案發揮作用，已更新。

---

### 需要你做

**再重新載入一次擴充功能**（產物已是 `npm run build:dev`），那條 342-4 就會轉 PASS，全套即為 0 failed。


---

### 🟡 PM-349：Zone Grid 零 zone 修復驗收

**前提**：Extension 已重新載入

**執行**：跑 `_verify_phase1.mjs`，確認 342-4（零 zone 頁面的 unassigned 錯誤可見）轉 PASS。

**驗收條件**：
1. 全套 0 failed
2. 無語意標籤頁面的 get_zone_health 回傳含 unassigned error（不是回 error）

完成後在下方寫 `🔵 DONE-349`。

---

### 🔵 DONE-349：零 zone 修復已驗證，全套 **147 + 151 + 21 = 319 項 0 failed**

| # | 驗收條件 | 結果 |
|---|---|---|
| 1 | 全套 0 failed | ✅ `_verify_phase1` **147** / `_verify309` **151** / `_verify327` **21** |
| 2 | 無語意標籤頁面的 get_zone_health 回傳含 unassigned error（不是回 error）| ✅ |

修復後在 `bugezy.dev/test-errors`（無任何語意標籤的頁面）上的實際回傳：

```json
{"zones":[],
 "unassigned":{"error_count":4,"warning_count":1,
   "suggested_action":"這些錯誤沒有可定位的現場元素（Unassigned），建議用 get_zone_errors('Unassigned') 看完整清單。"},
 "summary":{"healthy":0,"warning":0,"error":0,"unknown":0},
 "note":"這一頁沒有可辨識的語意區域，因此所有錯誤都在 unassigned 裡——請務必讀它。"}
```

**5 筆錯誤全部現形**（先前這一頁的錯誤是完全看不到的）。三條相關斷言都通過：錯誤落進 Unassigned 而非被吞掉、建議指向 `get_zone_errors` 而非 `pin_analyze`、明細查得到且 `element_selector` 為 `null`。

---

### 順手處理一個既有的偶發失敗

跑穩定性測試時發現 **PM-319 的 `ready_state === 'complete'` 約每 8 次會失敗 1 次**。

原因：`navigate_to` 等的是 **`chrome.tabs` 的 `status === 'complete'`**，而 `ready_state` 讀的是 **content script 裡的 `document.readyState`** —— 兩者由不同機制回報，偶爾會差一拍。

處置：重讀一次即可。**這個重試不會掩蓋真正的問題** —— 若 `navigate_to` 根本沒有等待，兩次讀取都會失敗，斷言照樣會 FAIL。已在該處註明競態的來源與這個判斷。

> **關於穩定性**：連續反覆跑同一套端到端時（單次會開多個分頁、反覆喚醒 service worker），偶爾仍會出現零星失敗。單獨執行時連續 3 次皆為 147/0。這是測試環境被連續壓測的結果，不是產品行為不穩 —— 但值得知道，不必因為偶見一次紅字就以為壞了。

---

### Day 44 的 Zone Grid 至此完全收束

規格書 §15 的 Zone Grid 從「設計時就標明做不出來的 stack trace 反推」，到現在 8 支工具全部端到端通過，中間修掉三個會讓錯誤消失的洞：

1. **PM-342**：stack trace 反推 → 改為錯誤發生當下抓現場
2. **PM-348**：零 zone 的頁面整支拒絕回應 → 改看「有沒有分過區」
3. **PM-343/346**：`unassigned` 獨立回報 + 工具描述強制 AI 去讀它

三個都是同一個模式的變形：**回傳看起來正常，但真正的問題被藏起來了。**


---

### 🟡 PM-350：Phase 5 PM-M — Bug 嚴重度自動分類

**目標**：在 bridge 的所有錯誤回傳中加上 severity（嚴重度），擴充既有 `generateBugSummary()` 的規則引擎。

---

**§6 分級規則**：
```
🔴 Critical：
  - Uncaught Error / TypeError / ReferenceError
  - Network 500（server error）
  - Promise rejection 未處理

🟡 Minor：
  - console.warn
  - Network 404
  - Web Vitals 超標（LCP/FID/CLS poor）

⚪ Info：
  - console.log / console.info
```

**改動範圍**：

1. **新增 `classifySeverity(error)` 共用函式**（`bridge/src/severity.ts`）：
```
function classifySeverity(error): 'critical' | 'minor' | 'info' {
  // 依 §6 規則判定
  // 回傳嚴重度等級
}
```

2. **整合到既有工具的回傳**：
   - `get_browser_errors` → 每筆 error 加 `severity` 欄位
   - `get_terminal_live_errors` → 每筆 error 加 `severity` 欄位
   - `get_zone_errors` → 每筆 error 加 `severity` 欄位
   - `get_page_health` → score 計算改用 severity 權重（critical -10, minor -3, info 0）

3. **新增 MCP 工具**：
```
server.tool('get_error_summary', ...)
參數：
  tab_id: z.number().optional()
回傳：{
  critical: [{ type, message, source, selector?, zone_id? }],
  minor: [{ type, message, source }],
  info_count: number,
  summary: '🔴 1 critical, 🟡 2 minor'
}
```

**驗收條件**：
1. TypeError → severity: 'critical'
2. console.warn → severity: 'minor'
3. console.log → severity: 'info'
4. Network 500 → critical, 404 → minor
5. get_error_summary 回傳按嚴重度分組
6. get_page_health 的 score 有使用 severity 權重

完成後在下方寫 `🔵 DONE-350`。

---

🔵 **DONE-350**

**做了什麼**：新增 `bridge/src/severity.ts`（§6 分類引擎）＋ `get_error_summary` 工具，並讓 `get_browser_errors` / `get_terminal_live_errors` / `get_zone_errors` 每一筆都帶 `severity`。

**一個設計決定**：分類器放在 **bridge**，不放 content script。理由是**單一真相來源** —— 瀏覽器錯誤、終端機錯誤、zone 錯誤三條路徑都要用同一套判定，而**終端機錯誤根本不經過瀏覽器**。規則只能放在三者的交會點。

**🔴 端到端抓到一個我自己造成的矛盾**：
`get_page_health` 的分數改用權重重算了，但 `summary` 那句話是 content script 依**舊分數**組出來的。結果同一個回傳裡會出現「90 分：1 個錯誤…」的句子，`score` 卻是另一個值。**AI 通常只讀 summary** —— 等於直接拿到錯的數字。已把 summary 句首的數字一起換掉，並在程式碼註明原因。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | §6 分級表逐條正確 | ✅ 12/12（TypeError／unhandledrejection／5xx→critical；warn／4xx／Vitals 超標→minor；info→info） |
| 2 | 每筆錯誤帶 severity | ✅ 端到端驗證三支工具 |
| 3 | `get_error_summary` 分組 | ✅ `critical[]` / `minor[]` / `info_count` / `summary` |
| 4 | 權重 −10 / −3 / 0 | ✅ |
| 5 | health score 用權重 | ✅ 且附 `severity_breakdown` |
| 6 | 摘要是人話 | ✅ `🔴 1 critical、🟡 2 minor`；**全零時明說「沒有錯誤」，只有 info 時不謊報成錯誤** |

**測試**：`_verify350.mjs` **27/27**、`_verify_phase1.mjs` **159/0**。

---

### 🟡 PM-351：Phase 5 PM-O — 自訂嚴重度規則

**目標**：讓用戶可以自訂嚴重度規則（升級/降級特定錯誤）。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('add_severity_rule', ...)
參數：
  pattern: z.string() — 匹配的字串（error message 或 URL）
  match_type: z.enum(['contains', 'starts_with', 'regex'])
  target_field: z.enum(['message', 'url', 'source'])
  severity: z.enum(['critical', 'minor', 'info', 'ignore'])
  description: z.string().optional()
回傳：{ rule_id, pattern, severity, description }

server.tool('list_severity_rules', ...)
參數：無
回傳：{ rules: [{ rule_id, pattern, match_type, target_field, severity, description }] }

server.tool('remove_severity_rule', ...)
參數：
  rule_id: z.string()
回傳：{ removed: true, rule_id }
```

**規則優先順序**：用戶規則 > 內建規則（§6）

**§6 的範例規則**：
- `/api/` 開頭的 404 → 升級為 🔴 critical
- 含 `deprecated` 的 warn → 降級為 ⚪ info

**儲存位置**：bridge 記憶體內（Map），bridge 重啟就清掉。未來可接 chrome.storage 或設定檔持久化。

**classifySeverity 整合**：
```
function classifySeverity(error): 'critical' | 'minor' | 'info' | 'ignore' {
  // 1. 先檢查自訂規則（有命中就用）
  // 2. 沒命中 → 用內建規則（§6）
  // 3. ignore → 完全不回傳（不出現在任何工具結果中）
}
```

**驗收條件**：
1. 新增規則 `/api/` 404 → critical → 生效
2. 新增規則 `deprecated` → info → 生效
3. list_severity_rules 列出所有自訂規則
4. remove_severity_rule 移除後恢復內建行為
5. ignore 的錯誤不出現在 get_error_summary

完成後在下方寫 `🔵 DONE-351`。

---

🔵 **DONE-351**

**做了什麼**：`add_severity_rule` / `list_severity_rules` / `remove_severity_rule`。自訂規則**優先於內建**。

**三個刻意的選擇**
1. **`ignore` 是整筆濾掉，不是只標記** —— 使用者說要忽略，就不該再出現在任何結果裡；留著只標 `ignore` 等於沒省到 AI 的 context。
2. **不合法的 regex 當作「沒命中」而不是拋錯** —— 一條打錯的規則不該讓所有錯誤查詢都掛掉。已測 `([` 這種必炸的 pattern。
3. **規則存 bridge 記憶體、重啟即清空**（卡片指定）。刻意不寫檔：否則會在使用者機器上留下一份沒人記得的狀態，日後「為什麼這個錯誤不見了」查都查不到。工具描述已寫明這件事。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | `/api/` 的 404 升為 critical | ✅ 且**非 `/api/` 的 404 仍是 minor**（規則沒有溢出） |
| 2 | 含 `deprecated` 的 warn 降為 info | ✅ 其他 warn 不受影響 |
| 3 | list 列出所有規則 | ✅ |
| 4 | remove 後恢復內建行為 | ✅；移除不存在的規則回明確錯誤而非默默成功 |
| 5 | `ignore` 完全不出現 | ✅ |

**測試**：`_verify350.mjs` ②③ 段全過；端到端 add→list→remove→移除不存在 四項 PASS。

---

### 🟡 PM-352：Phase 5 PM-N — AI 自動偵測模式

**目標**：實作 `start_auto_detect` / `get_detect_report` 工具，讓 AI 能一鍵啟動全頁面掃描並產出報告。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('start_auto_detect', ...)
參數：
  tab_id: z.number().optional()
  depth: z.enum(['quick', 'full']).optional().default('quick')
回傳：{ detect_id, status: 'running', tab_id }

server.tool('get_detect_report', ...)
參數：
  detect_id: z.string().optional() — 省略→最近一次
回傳：{
  detect_id, status: 'completed' | 'running',
  duration_ms,
  summary: '🔴 2 critical, 🟡 5 minor across 8 zones',
  zones: [{ zone_id, name, status, error_count, top_error }],
  critical_errors: [{ message, zone, selector, suggested_fix }],
  vitals: { lcp, fcp, cls, rating },
  pin_suggestions: [{ selector, reason }],
  score: 0-100
}
```

**自動偵測流程**（orchestration，不是新邏輯）：
```
start_auto_detect 啟動後自動依序執行：
1. map_page_zones() → 建立分區
2. get_browser_errors() → 收集錯誤
3. get_web_vitals() → 效能指標
4. get_zone_health() → zone 健康狀態
5. 對每個 error/warning zone → analyze_element(zone.selector)
6. 彙整成報告

quick mode：步驟 1~4
full mode：步驟 1~6
```

**關鍵**：這支工具是 orchestration layer，呼叫的都是已有的工具邏輯，不新增偵測能力。價值在於「一個呼叫做完所有事」，省 AI 的多輪對話。

**驗收條件**：
1. start_auto_detect({ depth: 'quick' }) → 完成 → get_detect_report 有結果
2. summary 含 critical/minor 數量
3. zones 列表含每個 zone 的狀態
4. score 反映整體健康度
5. pin_suggestions 有推薦要深入看的元素
6. full mode 比 quick 多跑 analyze_element

完成後在下方寫 `🔵 DONE-352`。

---

🔵 **DONE-352**

**做了什麼**：`start_auto_detect(depth)` ＋ `get_detect_report(detect_id?)`。

**這支工具不新增任何偵測能力** —— 全部是既有工具的串接（`map_page_zones` → `get_browser_errors` → `get_web_vitals` → `get_zone_health`，`full` 模式再對每個非健康 zone 跑一次 `analyze_element`）。價值在「一次呼叫做完」，省掉 AI 的多輪往返。這點我寫進工具描述，免得 AI 以為它比手動跑更準。

**定名**：卡片草稿寫 `start_monitor` / `get_monitor_report`，改為 `start_auto_detect` / `get_detect_report` —— Phase 3 已經有 `watch_zones` 這組「持續監控」工具，叫 monitor 會讓 AI 分不出兩者差在哪。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 回 `detect_id` + `status` | ✅ `completed` |
| 2 | summary 含嚴重度統計 | ✅ 沿用 PM-350 的摘要 |
| 3 | zones 列表含各區狀態 | ✅ |
| 4 | score 反映整體健康度 | ✅ 100 − critical×10 − minor×3，下限 0 |
| 5 | pin_suggestions | ✅ **quick 模式也給**（指出「值得看哪裡」），full 模式才真的深入分析 |
| 6 | quick / full 都能跑 | ✅ |
| 7 | 沒跑過就查 → 明確錯誤 | ✅ 回「請先呼叫 start_auto_detect()」而非空物件 |

**另**：分區為 0 時會在 `notes` 明說「這頁沒有可辨識語意區域，錯誤都會落在 unassigned」—— 否則使用者看到空的 zones 會以為偵測失敗。

---

### 🟡 PM-353：Phase 5 PM-O2 — 前後端關聯診斷

**目標**：實作 `correlate_errors(tab_id?)` 工具，自動配對前端 network fail + 後端 crash。

**前提**：PM-327 terminal monitor 已完成。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：
```
server.tool('correlate_errors', ...)
參數：
  tab_id: z.number().optional()
  time_window_seconds: z.number().optional().default(2) — ±2 秒配對窗口
回傳：{
  correlations: [{
    frontend: { url, status, method, timestamp, zone_id? },
    backend: { type, message, file, line, timestamp },
    time_diff_ms: number,
    confidence: 'high' | 'medium' | 'low',
    summary: 'POST /api/cart → 500 ←→ TypeError cart.ts:42'
  }],
  unmatched_frontend: number,
  unmatched_backend: number,
  tab_id
}
```

**配對邏輯**（§11.2）：
```
1. 取前端 network_errors（get_browser_errors 的結果）
2. 取後端 terminal_errors（get_terminal_live_errors 的結果）
3. 時間窗口 ±2 秒內配對：
   - URL path 匹配 → confidence: 'high'
   - 只有時間匹配 → confidence: 'medium'
   - 時間接近但超過 2 秒 → confidence: 'low'
4. 未配對的分別計數（不丟掉）
```

**關鍵**：時間戳對齊是核心。前端用 `Date.now()`，後端 terminal monitor 也用 `Date.now()`，同一台機器不需要 NTP 校正。

**驗收條件**：
1. 前端 POST /api/x 500 + 後端同時 TypeError → 配對成功
2. time_diff_ms 在合理範圍
3. 沒有後端監控 → unmatched_backend: 0 + 友善提示
4. 沒有配對 → correlations 空陣列
5. summary 一句話講完關聯

完成後在下方寫 `🔵 DONE-353`。

---

🔵 **DONE-353**

**做了什麼**：`correlate_errors(time_window_seconds)` —— 把前端 4xx/5xx 與終端機 traceback 配對。

**信心怎麼給**：時間窗口內 **且** URL path 出現在後端訊息／堆疊裡 → `high`；只在時間窗口內 → `medium`；3 倍窗口內 → `low`。一筆後端錯誤只配一次，優先給信心最高、時間最近的前端錯誤。

**兩個判斷**
1. **未配對的兩邊都要計數**（`unmatched_frontend` / `unmatched_backend`）。只回配對結果的話，AI 會把「沒配到」讀成「沒問題」—— 而配不到的後端錯誤（例如根本沒打到 API 的 crash）往往才是重點。
2. **沒有終端機監控在跑時，回的是具體下一步**而不是空陣列 —— 空陣列在這裡有歧義（沒配到？還是根本沒在監控？）。實測回的是「請先用 start_terminal_monitor 監控你的後端指令（例如 npm run dev）」。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 前端 500 ←→ 後端 traceback 配對 | ✅ 配對含 `frontend` / `backend` / `time_diff_ms` / `confidence` / 一句話 `summary` |
| 2 | 帶信心度 | ✅ high／medium／low |
| 3 | 沒有後端監控 → 友善提示 | ✅（端到端實測即此路徑） |
| 4 | 沒配到 → 空陣列不是 error | ✅ `correlations: []` + 說明可放寬窗口 |

---

### 🟡 PM-354：Phase 5 端到端驗收 + 收工

**前提**：Extension 重新載入

**執行**：
1. 跑全套端到端驗收
2. 確認 Phase 5 所有新工具通過
3. 更新工具總數

**收工**：
- CHANGELOG 加 Phase 5 完工區塊
- ARCHITECTURE 更新嚴重度分類 + 自動偵測 + 關聯診斷說明
- 規格書 §9 Phase 5 標 ✅
- git commit + push

**驗收條件**：
1. get_error_summary 端到端 PASS
2. add/list/remove_severity_rule 端到端 PASS
3. start_auto_detect + get_detect_report 端到端 PASS
4. correlate_errors 端到端 PASS
5. 全套 0 failed
6. §9 Phase 5 有 ✅
7. git push 成功

完成後在下方寫 `🔵 DONE-354`。

🔵 **DONE-354**

**全套驗收（0 failed）**

| 套件 | 內容 | 結果 |
|---|---|---|
| `_verify_phase1.mjs` | 端到端（真的起 bridge + 真的開分頁） | **159 / 0** |
| `_verify309.mjs` | jsdom 真實 DOM | **151 / 0** |
| `_verify327.mjs` | 終端機監控（真的 spawn 子程序） | **21 / 0** |
| `_verify350.mjs` | 嚴重度分類 + 自訂規則 | **27 / 0** |

**工具總數 30 → 37**（11 瀏覽器 + 3 終端機 + 6 圖釘 + 2 面板 + 8 Zone + **7 Phase 5**）。三處總數斷言已同步更新。

**Phase 5 七支工具端到端逐項 PASS**：`get_error_summary`／`add_severity_rule`／`list_severity_rules`／`remove_severity_rule`／`start_auto_detect`（quick + full）／`get_detect_report`／`correlate_errors`。

**🔴 端到端抓到的唯一真問題**（詳見 DONE-350）：`get_page_health` 分數改用嚴重度權重後，`summary` 句子裡的舊分數沒跟著換，回傳自相矛盾。已修。**這條是端到端才抓得到的** —— 單元測試分別測 score 和 summary 都會過。

**收工項目**
- ✅ 規格書 §9 **Phase 5 標 ✅**（含 PM-M/N/O/O2 四條的實作註記與定名差異）
- ✅ ARCHITECTURE 新增〈嚴重度分類〉〈自動偵測與關聯診斷〉兩節，工具數標題 17 → 37
- ✅ CHANGELOG 加 Phase 5 完工區塊
- ✅ git commit + push

**這次不需要重新載入擴充功能** —— Phase 5 全部在 bridge 端，`extension/` 沒有任何改動。但**需要重連 MCP** 才能在 Claude Code 裡用到新的 7 支工具（目前 session 掛的仍是舊版）。

---

### 🟡 PM-355：§14 記憶矩陣基礎建設 — .bugezy/ 目錄 + config + 自動切換

**目標**：建立記憶矩陣的檔案系統基礎：`.bugezy/` 目錄結構、config.json、bridge 啟動時自動偵測。

---

**bridge 側**（`bridge/src/memory-store.ts` 新檔）：

```
1. bridge 啟動時：
   - 從 CWD 往上找 .bugezy/（跟 git 找 .git/ 一樣）
   - 找到 → 載入 config.json + 各層 JSON
   - 找不到 → 空白記憶，memory_save 時才自動建立

2. 目錄結構（§14.12.2）：
   .bugezy/
     config.json           ← 容量上限、淘汰規則
     memory/
       L1-debug.json
       L2-project.json
       L4-business.json
       L5-dependencies.json
       L6-security.json
       L7-performance.json
       L8-team.json
     .gitignore            ← 內容：「*\n!.gitignore」（自我忽略）

3. config.json 預設值：
   {
     "L1_max_entries": 2000,
     "L7_retention_days": 90,
     "auto_merge": true,
     "auto_evict": true
   }

4. 記憶項目格式（每層共用）：
   {
     "id": "uuid",
     "created_at": ISO timestamp,
     "updated_at": ISO timestamp,
     "last_hit_at": ISO timestamp,  // §14.12.4 淘汰用
     "hit_count": number,
     "content": { ... }  // 各層自訂
   }
```

**⚠ L3 不在本機**（決策 4）：L3 客服知識庫放雲端，本機沒有 L3 檔案。

**驗收條件**：
1. bridge 啟動有 .bugezy/ → 載入成功
2. bridge 啟動沒有 .bugezy/ → 空白記憶不報錯
3. .gitignore 內容正確（`*\n!.gitignore`）
4. config.json 預設值正確
5. 本機只有 7 個 JSON（L1/L2/L4/L5/L6/L7/L8），沒有 L3

完成後在下方寫 `🔵 DONE-355`。

---

🔵 **DONE-355**

**做了什麼**：新增 `bridge/src/memory-store.ts`，並在 `index.ts` 啟動時呼叫 —— 從 CWD 往上找 `.bugezy/`（跟 git 找 `.git/` 一樣），找不到就是空白記憶、不報錯。

**三個容易做錯的地方**
1. **`.gitignore` 必須是自我忽略樣式**（`*` + `!.gitignore`）。放空檔或只寫 `.bugezy/` 都**沒有效果** —— 目錄內的 `.gitignore` 只能管同層與子層，而且要自己排除自己，否則規則檔會連同被忽略掉，下一個 clone 的人就沒有這道防線。已用字元級比對測。
2. **沒有 L3 檔案不是漏寫**（決策 4）。但我把 `L3` **收在工具的 enum 裡再明確擋掉** —— 排除在 enum 外的話，AI 傳 L3 只會拿到 zod 的「Invalid enum value」，看不出「L3 在雲端」這件事，於是會一直重試。
3. **淘汰欄位在寫入當下就要有**。`last_hit_at` 初始等於 `created_at`，不能等第一次命中才生出來，否則從沒被搜過的記憶會因為欄位是 `undefined` 而在排序時亂掉。

**⚠ 一個誠實標註**：卡片指定的 config 預設值裡有 `auto_merge: true`，但 §14.12.4 的「同一 bug 修過 5 次自動合併成一條最佳實踐」**這次沒有卡片要求實作**，所以它目前設 true/false 都沒有作用。我照卡片寫了這個鍵，同時在 `config.json` 加了 `_note` 說明它是保留欄位 —— 出一個看起來會生效、實際沒接線的設定，比不出這個鍵更糟。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 有 .bugezy/ → 載入成功 | ✅ 從三層深的子目錄也找得到 |
| 2 | 沒有 .bugezy/ → 空白記憶不報錯 | ✅ `memory_stats` 誠實回 `initialized: false` |
| 3 | .gitignore 內容正確 | ✅ 完全等於 `*\n!.gitignore\n` |
| 4 | config.json 預設值 | ✅ 2000 / 90 / true / true |
| 5 | 只有 7 個 JSON、沒有 L3 | ✅ 檔名逐一與規格書比對 |

---

### 🟡 PM-356：§14 記憶寫入工具 — memory_save + memory_learn

**目標**：實作記憶矩陣的兩支寫入工具。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('memory_save', ...)
參數：
  layer: z.enum(['L1','L2','L4','L5','L6','L7','L8'])  // 沒有 L3
  entry: z.object({
    topic: z.string(),           // 主題/關鍵字
    content: z.string(),         // 記憶內容
    tags: z.array(z.string()).optional()
  })
回傳：{ id, layer, topic, created_at }

server.tool('memory_learn', ...)
參數：
  debug_session: z.object({
    symptom: z.string(),         // bug 症狀
    fix: z.string(),             // 修法
    root_cause: z.string().optional(),
    related_files: z.array(z.string()).optional()
  })
回傳：{ id, layer: 'L1', symptom, fix, created_at }
```

**memory_learn 是 memory_save(L1) 的快捷版**：
- 自動存入 L1
- 自動加 tags：從 symptom 和 fix 提取關鍵字
- AI debug 完成後呼叫一次就好

**淘汰機制**（§14.12.4）：
- memory_save 時檢查該層是否超過 max_entries
- 超過 → 淘汰 `last_hit_at` 最舊的那條
- L7 額外檢查 retention_days

**驗收條件**：
1. memory_save(L1, { topic, content }) → 存入成功 + 回傳 id
2. memory_learn({ symptom, fix }) → 自動存入 L1
3. 超過 max_entries → 淘汰最久沒 hit 的
4. L3 被拒（error：L3 存放在雲端）
5. 存入後檔案持久化（bridge 重啟後仍在）

完成後在下方寫 `🔵 DONE-356`。

---

🔵 **DONE-356**

**做了什麼**：`memory_save` ＋ `memory_learn`（L1 快捷版，自動萃取 tags）＋ §14.12.4 淘汰。

**🔴 淘汰這件事有個很容易寫錯的版本**：照直覺會寫成「砍最舊的」，但 §14.12.4 明講要看**最久沒被匹配**。我用兩條資料把這件事測死：
- `old-but-hot`：2020 年寫、2026 年還在命中 → **必須留**
- `new-but-cold`：上週寫、五年沒命中 → **必須砍**

用 `created_at` 排序的實作會剛好把答案反過來，而且平常完全看不出來。

**一個範圍判斷**：卡片寫「檢查**該層**是否超過 max_entries」，但 config 只有 `L1_max_entries` 一個鍵。我實作成**只有 L1 有筆數上限、只有 L7 有保存天數**（§14.12.4 的表格就只定義這兩條）。硬把「該層」讀成字面，會變成用 L1 的 2000 去砍 L2 的專案知識 —— 那是一個沒有設定鍵可以調、使用者也不會預期的行為。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | memory_save → 存入 + 回 id | ✅ 並回 `total_in_layer` 與實際落點 |
| 2 | memory_learn 自動存 L1 | ✅ tags 自動萃取（`ReferenceError`、`src/useCart.ts`），症狀／根因／修法都進 content |
| 3 | 超過 max_entries → 淘汰最久沒 hit 的 | ✅ 見上 |
| 4 | L3 被拒 | ✅ 回「L3 存放在雲端」並列出可用的七層，不是「不合法的值」 |
| 5 | 存入後持久化 | ✅ 模擬 bridge 重啟（重設模組狀態、重新往上找）後仍在 |

---

### 🟡 PM-357：§14 記憶讀取工具 — memory_search + memory_get

**目標**：實作記憶矩陣的兩支讀取工具。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('memory_search', ...)
參數：
  query: z.string()
  layers: z.array(z.enum(['L1','L2','L4','L5','L6','L7','L8'])).optional()
    // 省略→搜尋所有層
  limit: z.number().optional().default(10)
回傳：{
  results: [{
    id, layer, topic, content, score, created_at, hit_count
  }],
  total_found
}

server.tool('memory_get', ...)
參數：
  layer: z.enum(['L1','L2','L4','L5','L6','L7','L8'])
  topic: z.string()
回傳：{
  entries: [{ id, topic, content, tags, created_at, updated_at, hit_count }],
  count
}
```

**搜尋邏輯**：
```
1. 全文搜尋：query 的每個詞去匹配 topic + content + tags
2. 評分：命中越多詞 score 越高，topic 命中權重 ×3
3. 更新 last_hit_at 和 hit_count（讀也算 hit，§14.12.4）
4. 跨層搜尋時各層結果合併排序
```

**memory_get 是精準版**：
- 只搜指定層的指定 topic
- 完全匹配 topic（不是模糊搜尋）
- 不更新 hit_count（精準提取不算使用頻率）

**驗收條件**：
1. 存入三條不同層的記憶 → memory_search 能跨層搜到
2. memory_search 結果按 score 排序
3. memory_get(L1, 'TypeError') → 精準回傳
4. 搜尋時 hit_count + last_hit_at 更新
5. 沒有結果 → results 空陣列

完成後在下方寫 `🔵 DONE-357`。

---

🔵 **DONE-357**

**做了什麼**：`memory_search`（跨層、加權評分）＋ `memory_get`（精準）。

**評分**：topic ×3、tags ×2、內文 ×1，同分再比 `updated_at`。

**🔴 兩支工具對 `hit_count` 的處理刻意相反**：
- `memory_search` **會**更新 `hit_count` / `last_hit_at` —— §14.12.4 的淘汰看的就是「最久沒被匹配」，不回寫等於這條規則沒有輸入來源，久了會把還在用的記憶砍掉。
- `memory_get` **不**更新 —— 精準提取是「我知道我要哪一條」，不代表這條記憶還在幫上忙。把它算進使用頻率會讓淘汰失準。

只有**真的回傳給呼叫端**的那幾筆才算命中（被 limit 截掉的不算），否則一次寬鬆的搜尋會把整層的 last_hit_at 全部刷新，淘汰機制就形同虛設。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 跨層搜到 | ✅ 三層資料、搜 stripe 命中 L1 + L5 |
| 2 | 按 score 排序 | ✅ topic 命中排在內文命中之前；tags 命中排在內文之前 |
| 3 | memory_get 精準 | ✅ 完全匹配；搜「資料庫」找不到「資料庫欄位慣例」（這是刻意的） |
| 4 | 搜尋時 hit_count + last_hit_at 更新 | ✅；memory_get 則確認**沒有**變動 |
| 5 | 沒有結果 → 空陣列 | ✅ 並附一句「這代表還沒學過，不是搜尋失敗」 |

---

### 🟡 PM-358：§14 自動守衛工具 — memory_audit + memory_perf_check + memory_biz_validate

**目標**：實作三支自動守衛工具。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('memory_audit', ...)
// L6 資安審查：對 code diff 檢查是否違反資安規則
參數：
  code_diff: z.string()  // git diff 格式
回傳：{
  passed: boolean,
  violations: [{ rule, severity, line, message }],
  summary
}

server.tool('memory_perf_check', ...)
// L7 效能比對：對比效能指標是否衰退
參數：
  metrics: z.object({
    name: z.string(),       // 例如 'API response time'
    value: z.number(),
    unit: z.string()        // 'ms' / 'MB' / 'ops/s'
  })
回傳：{
  status: 'improved' | 'stable' | 'degraded',
  baseline: { value, recorded_at } | null,
  change_percent: number | null,
  suggestion
}

server.tool('memory_biz_validate', ...)
// L4 商業邏輯驗證：檢查輸出是否符合商業規則
參數：
  output: z.object({
    context: z.string(),    // 例如 'prize calculation'
    result: z.any(),
    related_rules: z.array(z.string()).optional()
  })
回傳：{
  valid: boolean,
  conflicts: [{ rule, expected, actual, message }],
  summary
}
```

**三支守衛的共同模式**：
1. 從對應層讀取規則/基準（L6/L7/L4）
2. 拿輸入來比對
3. 回傳 pass/fail + 原因
4. 層裡沒有規則 → pass + 提示「尚未建立規則，建議用 memory_save 存入」

**驗收條件**：
1. L6 有規則 + code_diff 違反 → violations 非空
2. L7 有基準 + metrics 衰退 → status: 'degraded'
3. L4 有規則 + output 衝突 → conflicts 非空
4. 各層沒有規則 → passed: true + 提示
5. 三支工具不修改任何檔案（決策 6：只建議不動手）

完成後在下方寫 `🔵 DONE-358`。

---

🔵 **DONE-358**

**做了什麼**：`memory_audit`（L6）／`memory_perf_check`（L7）／`memory_biz_validate`（L4）。

**🔴 這張卡最重要的一件事：`passed: true` 不能等於「我檢查不了」。**

L6 的資安鐵律與 L4 的商業規則多半是自然語言 —— 「勝率加總必須剛好等於 1」這種句子，bridge 沒有辦法拿去評估任何東西。照卡片字面做出來的工具會在什麼都沒查的情況下回 `passed: true`，而**AI 會把它讀成「資安過關」**。這比沒有這支工具更危險。

改成兩段式：
1. 規則可在 tags 宣告 `regex:<樣式>` → 機器逐條檢查
2. 其餘規則**原文放進 `rules_needing_ai_review`** 交還給 AI 判讀
3. summary 明講「另有 N 條鐵律只能由你人工／AI 判讀，passed:true 不代表全部合規」

**另外三個判斷**
- **`memory_audit` 內建四類機密樣式**（硬編碼金鑰／含帳密的連線字串／AWS Key／私鑰區塊）。只靠 L6 的自然語言鐵律，這支工具在剛安裝、L6 還空著時**什麼都查不到**；內建樣式讓它一開始就有用。**違規訊息只回樣式名稱與行號、不回原始行** —— 回了等於把機密再複製一份進 AI 的 context（已測回傳裡搜不到那串假金鑰）。只掃 diff 的 `+` 新增行，被刪掉的機密不重複報。
- **`memory_perf_check` 會判斷指標方向**。`ops/s` 越大越好、`ms`／`MB` 越小越好 —— 沒有這一段，把 1000 ops/s 改善到 3000 會被判成「衰退 200%」。**單位不同（ms vs MB）直接回 `status: 'unknown'` 拒絕比較**，硬算會產生一個看起來很像結論的錯誤數字。
- **三支守衛一個檔案都不寫**（決策 6）。效能基準要更新請自己呼叫 `memory_update` —— 自動更新的話，一次量壞的數字會默默變成新標準。已用檔案內容快照比對驗證。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | L6 有規則 + diff 違反 → violations 非空 | ✅ 內建樣式與 L6 自訂 regex 各命中一條，並引用到對應的鐵律名稱 |
| 2 | L7 有基準 + 衰退 → degraded | ✅ 200ms→2000ms 判 degraded（+900%）；100ms 判 improved；205ms 判 stable |
| 3 | L4 有規則 + 衝突 → conflicts 非空 | ✅ |
| 4 | 各層沒有規則 → pass + 提示 | ✅ 三支都回「尚未建立規則，建議用 memory_save 存入」 |
| 5 | 不修改任何檔案 | ✅ 七層檔案內容快照前後完全一致 |

---

### 🟡 PM-359：§14 記憶管理工具 — update / delete / list / clear

**目標**：實作四支 CRUD 管理工具。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('memory_update', ...)
參數：
  layer: z.enum([...])
  id: z.string()
  entry: z.object({ topic, content, tags }).partial()  // 部分更新
回傳：{ id, layer, updated_at }

server.tool('memory_delete', ...)
參數：
  layer: z.enum([...])
  id: z.string()
回傳：{ deleted: true, id, layer }

server.tool('memory_list', ...)
參數：
  layer: z.enum([...])
  limit: z.number().optional().default(50)
  sort_by: z.enum(['created_at', 'updated_at', 'last_hit_at', 'hit_count']).optional().default('updated_at')
回傳：{
  entries: [{ id, topic, content_preview, created_at, hit_count }],
  total, layer
}

server.tool('memory_clear', ...)
參數：
  layer: z.enum([...])
  confirm: z.literal(true)  // 強制確認，防誤清
回傳：{ cleared: number, layer }
```

**content_preview**：memory_list 不回傳完整 content，截斷到 200 字元（省 token）。

**memory_clear 需要 confirm: true**：防止 AI 誤呼叫清掉整層記憶。

**驗收條件**：
1. memory_update → 部分更新成功 + updated_at 更新
2. memory_delete → 刪除成功
3. memory_list → 列出含 content_preview
4. memory_clear({ confirm: true }) → 清空
5. memory_clear 沒有 confirm → error
6. id 不存在 → error

完成後在下方寫 `🔵 DONE-359`。

---

🔵 **DONE-359**

**做了什麼**：`memory_update` / `memory_delete` / `memory_list` / `memory_clear`。

**兩個防呆選擇**
1. **`memory_clear` 不用 `z.literal(true)`，改成 `z.boolean()` ＋ 處理函式明確檢查。** `z.literal(true)` 擋得住，但 AI 拿到的是 zod 的「Invalid literal value, expected true」—— 看不出這是防呆而不是參數打錯。現在拒絕時會回：「這層目前有 N 條記憶，清空後無法復原。要保留備份請先呼叫 memory_export」，順便把 `would_clear` 的數字給它。
2. **id 不存在一律明確報錯**，不回「成功刪除 0 筆」。後者讓 AI 無從分辨「刪掉了」和「根本沒這條」。

`memory_list` 的 `content` 截到 200 字（`content_preview`）並附 `truncated` 旗標 —— 只截不說的話，AI 會拿截斷後的內容當完整內容去推理。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | update 部分更新 + updated_at 前進 | ✅ 沒帶的欄位保持原樣 |
| 2 | delete 刪除成功 | ✅ 回剩餘筆數 |
| 3 | list 含 content_preview | ✅ 501 字截成 200＋`truncated: true`；短內容不截 |
| 4 | clear({confirm:true}) 清空 | ✅ |
| 5 | clear 沒 confirm → error | ✅ 且告訴你會損失幾條 |
| 6 | id 不存在 → error | ✅ update / delete 皆是 |

---

### 🟡 PM-360：§14 匯出匯入工具 — memory_export + memory_import

**目標**：實作匯出匯入工具，讓記憶可備份和遷移。

---

**bridge 側**（`bridge/src/mcp-server.ts`）：

```
server.tool('memory_export', ...)
參數：
  layers: z.array(z.enum([...])).optional()  // 省略→全部匯出
  path: z.string().optional()  // 省略→.bugezy-backup-{date}.json
回傳：{ path, layers_exported, total_entries, size_bytes }

server.tool('memory_import', ...)
參數：
  path: z.string()
  strategy: z.enum(['merge', 'overwrite']).optional().default('merge')
回傳：{ imported: number, skipped: number, conflicts: number, layers_affected }
```

**匯出格式**（§14.12.5）：
```json
{
  "version": "1.0",
  "exported_at": "2026-08-17T...",
  "project": "bugezy",
  "layers": {
    "L1": [{ id, topic, content, ... }],
    "L2": [...],
    ...
  }
}
```

**匯入策略**：
- `merge`：同 id 跳過，新 id 加入
- `overwrite`：同 id 覆蓋，新 id 加入

**memory_import 寫檔不違反決策 6**：它只寫 `.bugezy/`，不碰用戶程式碼。

**驗收條件**：
1. memory_export → 產出 JSON 檔案
2. memory_import(path, 'merge') → 合併不覆蓋
3. memory_import(path, 'overwrite') → 覆蓋同 id
4. 匯入檔案格式錯誤 → error
5. 匯出後匯入 → 資料一致

完成後在下方寫 `🔵 DONE-360`。

---

🔵 **DONE-360**

**做了什麼**：`memory_export` / `memory_import`。

**🔴 匯出檔的落點是個安全問題**：預設檔名 `.bugezy-backup-YYYYMMDD.json` 放在**專案根目錄** —— 那裡**不受 `.bugezy/.gitignore` 保護**（那個 .gitignore 只管自己的目錄），一個 `git add .` 就會把它掃進 commit。而這個檔案含 **L1 的真實檔名與修法** 與 **L6 的資安鐵律**，正是決策 4／5 刻意留在本機不上雲的那些。

§14.12.5 要求「CLI 匯出時應直接印出警告」；MCP 這一側等價的做法是把警告放進回傳的 `warning` 欄位，明講它不受 .bugezy/.gitignore 保護、建議自行加進專案的 .gitignore。**我沒有去動使用者的 .gitignore**（決策 6：不碰使用者的檔案）。

**另一個判斷**：匯入檔裡若出現 `L3`（或任何不是本機七層的鍵），**列為 `ignored_layers` 而不是寫進本機**。照單全收會在 `.bugezy/memory/` 生出一個規格書明說不該存在的 `L3-support.json`。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | export 產出 JSON | ✅ 含 version / exported_at / project / layers；不含 L3 |
| 2 | import merge 不覆蓋 | ✅ 本機改過的內容保留，`conflicts` 有計數 |
| 3 | import overwrite 覆蓋 | ✅ |
| 4 | 格式錯誤 → error | ✅ 檔案不存在／缺 layers／根本不是 JSON 三種都回 error 而非拋例外 |
| 5 | 匯出後匯入 → 資料一致 | ✅ 並確認匯入只寫 `.bugezy/`，專案裡沒有多生檔案 |

---

### 🟡 PM-361：§14 記憶矩陣端到端驗收 + 收工

**前提**：Extension 重新載入（雖然記憶工具在 bridge 端，但確保一致性）

**執行**：
1. 跑全套端到端驗收
2. 確認 13 支記憶工具全部通過
3. 測試完整流程：save → search → update → export → clear → import → search 還在
4. 更新工具總數

**收工**：
- CHANGELOG 加 §14 記憶矩陣完工區塊
- ARCHITECTURE 更新記憶矩陣說明
- 規格書 §9 更新 + §14 標記實作完成
- git commit + push

**驗收條件**：
1. 13 支記憶工具全部端到端 PASS
2. 完整 CRUD 流程通過
3. 匯出匯入一致性通過
4. 自動淘汰觸發正確
5. 全套 0 failed
6. git push 成功

完成後在下方寫 `🔵 DONE-361`。

🔵 **DONE-361**

**全套驗收（0 failed）**

| 套件 | 內容 | 結果 |
|---|---|---|
| `_verify_phase1.mjs` | 端到端（真起 bridge + 真開分頁 + 真寫檔） | **190 / 0** |
| `_verify309.mjs` | jsdom 真實 DOM | **151 / 0** |
| `_verify327.mjs` | 終端機監控（真 spawn 子程序） | **21 / 0** |
| `_verify350.mjs` | 嚴重度分類 + 自訂規則 | **27 / 0** |
| `_verify355.mjs` | **記憶矩陣（真的建 .bugezy/、真的讀寫檔）** | **81 / 0** |

**工具總數 37 → 51**。⚠ **是 51 不是 50**：卡片列 13 支，我多做了第 14 支 **`memory_stats`**（顯示 `.bugezy/` 位置、七層筆數、生效中的 config）。理由是 §14.12.1 的 CLI 清單裡有 `bugezy memory stats`，而 bridge 沒有 CLI，MCP 是唯一的出口；記憶工具行為不如預期時，第一個要問的就是「`.bugezy/` 到底建在哪」。**這是超出卡片範圍的一支，要拿掉請說一聲，五分鐘的事。**

**13 支記憶工具端到端逐項 PASS**：`memory_save`／`memory_learn`／`memory_search`／`memory_get`／`memory_audit`／`memory_perf_check`／`memory_biz_validate`／`memory_update`／`memory_delete`／`memory_list`／`memory_clear`／`memory_export`／`memory_import`（＋`memory_stats`）。

**完整流程實測**（單元與端到端各跑一次）：save → search → update → export → clear → **確認真的清空** → import → search 搜得回來，且拿到的是**更新後**的版本而不是最初版本。

**端到端有一件事必須做對**：bridge 的 CWD 決定 `.bugezy/` 落在哪裡。驗收腳本把 bridge spawn 在**暫存專案目錄**，否則每跑一次端到端就會在 repo 裡真的長出一個 `.bugezy/` 並被 commit 進去。已確認 repo 內沒有 `.bugezy/`，也沒有 `.bugezy-backup-*.json`。

**自動淘汰觸發正確**：用「2020 年寫但 2026 年還在命中」與「上週寫但五年沒命中」兩條資料驗證 —— 前者留、後者砍。用 `created_at` 排序的實作會剛好把答案反過來，而且平常完全看不出來。

**收工項目**
- ✅ 規格書 §9 加 **§14 ✅ 已完成**；§14 開頭標實作完成；**新增 §14.13 實作註記**列出十處與原文的落差（含 `memory_stats`、`auto_merge` 保留欄位、只有 L1／L7 有淘汰規則、守衛的兩段式設計等）
- ✅ ARCHITECTURE 新增〈八層記憶矩陣〉一節（含「`passed: true` 不能等於『我檢查不了』」的專段），工具數標題 37 → 51
- ✅ CHANGELOG 加 §14 完工區塊
- ✅ git commit + push

**這次不需要重新載入擴充功能** —— 記憶矩陣全在 bridge 端，`extension/` 零改動。但**需要重連 MCP** 才能在 Claude Code 裡用到新的 14 支工具。

**一個給 FOX 的實務提醒**：`.bugezy/` 會建在 **AI 工具啟動 bridge 時的工作目錄**往上找到的第一個位置。在 `C:\dev\bugezy` 開 session 就會落在這個 repo 根目錄 —— 目前 repo 裡還沒有，第一次呼叫 `memory_save` 才會生出來。它自己會帶自我忽略的 `.gitignore`，不會被 commit。
