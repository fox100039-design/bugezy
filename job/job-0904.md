# job-0904.md — BugEzy Day 51（2026-09-04）

## 今日任務
- 群組 D 實作（分享報告頁 4 畫面）
- 群組 E 實作（官網 11 頁）

## 設計交付包位置
`C:\dev\bugezy\BugEzy 大黃蜂色調項目概覽\design_handoff_hornet_visual_system\`

## 關鍵文件
- DESIGN_SPEC.md — 規範（唯一依據）
- README.md — 實作說明 + 畫面對照表
- BugEzy 分享報告頁.dc.html — 畫面 14~17
- BugEzy 官網改版.dc.html — 畫面 28~30

---

### 🟡 PM-432：群組 D — 分享報告頁大黃蜂改版（reportPageHtml CSS + 共用元件）

**背景**：畫面 14~15（報告頁 Console/Info 分頁），`server/src/index.ts` L4575–6100。目前全部舊紫色系（`#0f0f1a` `#1a1a2e` `#7c3aed` `#a78bfa` `#ef4444` `#238636`）。這是 PM 與工程師唯一會看到的公開介面，改版最重要的公開頁面。

**讀取**：`DESIGN_SPEC.md`、設計稿 `BugEzy 分享報告頁.dc.html` L1–295（畫面 14 + 15）

**目標檔案**：`server/src/index.ts` 的 `reportPageHtml()` 函式

**設計規格**：

#### 1) 全頁底色 + 字型
- `background:#F7BE00` + 蜂巢紋 10%（黃底頁）
- Google Fonts 外連 OK（server 端不是擴充，無 CWS 限制）：Archivo 500/600/800 + Noto Sans TC 400/500/700/900 + JetBrains Mono 400/500/700

#### 2) 黑色 header（.topbar）
- `background:#14110B; padding:13px 24px`
- 六角斜紋標記 28×32px（§5.B，三層六角 + repeating-linear-gradient）
- 字標 `BugEzy` Archivo 800 18px `#F7BE00`
- 「Bug 報告」Noto Sans TC 500 13px `#B99C63`
- 右對齊語言切換：`1.5px solid #7A4E1D; border-radius:8px; font:700 12px JetBrains Mono; color:#F7BE00`

#### 3) 標題區 + 分享連結卡（**移到標題右側**）
- 標題 Noto Sans TC 800 25px `#14110B`
- meta：URL（JetBrains Mono 連結色 `#4A2F12`）、瀏覽器/尺寸/時間 JetBrains Mono 11.5px + `rgba(74,47,18,.5)` 分隔
- 分享連結卡（原本在頁尾，移到標題右側）：`background:#7A4E1D; border-radius:12px; width:330px`，URL 欄 `#14110B` 底、複製按鈕 `#F7BE00`

#### 4) 分頁列（.tab-bar + .tab-btn）
- `border-bottom:2px solid #14110B; gap:2px`
- active 分頁：`background:#14110B; color:#F7BE00; border-radius:11px 11px 0 0`
- 非 active：`background:transparent; color:#4A2F12; border:2px solid #14110B; border-bottom:none`
- 徽章：Console 有錯 → `background:#8A2A0F; color:#FFE9AE`（磚紅），其餘 → `background:rgba(20,17,11,.16); color:#4A2F12`
- Console 有錯時自動成預設分頁（沿用原邏輯 `defaultTab`）

#### 5) Console 分頁（畫面 14）
- log 清單：`border:2px solid #14110B; border-radius:12px; overflow:hidden`
- 每列：`background:#FFF4D6; border-bottom:1px solid rgba(20,17,11,.14)`
- error 圖示：`15×15px 圓形 background:#8A2A0F`，內部橫線 `#FFF4D6`
- warn 圖示：`15×14px 三角 clip-path background:#7A4E1D`
- error 文字 `color:#8A2A0F`，warn 文字 `color:#7A4E1D`
- 時間戳 JetBrains Mono 600 11.5px `#6B5A3D`
- 「另外 N 筆」：`background:rgba(255,244,214,.55); color:#5E3A14`
- Network 錯誤表：同框，status 5xx `#8A2A0F` / 4xx `#8A5A24`（但 §2.4 禁用 → 用 `#7A4E1D`），method `#1E6FD9`（設計稿用藍色）
- Token 估算：`background:#7A4E1D; border-radius:12px`，值 JetBrains Mono `#FFE9AE`，合計 `border-top:1px solid rgba(255,233,174,.28)`，底部膠囊 `#F7BE00`

#### 6) Info 分頁（畫面 15）
- 左欄：描述框 `#FFF4D6 + 2px solid #14110B; border-radius:12px`、時間軸標記（手動=實線 `#14110B`、AUTO=虛線 `dashed rgba(20,17,11,.4)` + 咖啡時間膠囊 + AUTO 標籤）、語音記錄表
- 右欄 398px：摘要 2×3 grid（同 edit-report 那套）、網路環境（六角 + 在線狀態 + 延遲/頻寬 + 結束時變化）、儲存狀態（localStorage/sessionStorage/Cookies + 遮罩註記咖啡底）、高畫質 AI 分析（虛線框 + checkbox `accent-color:#14110B`）
- section 標題統一：JetBrains Mono 700 11px `#4A2F12` letter-spacing .1em

#### 7) 舊色碼全清
- `#0f0f1a` `#1a1a2e` `#2a2a3e` `#7c3aed` `#a78bfa` `#238636` `#ef4444` `#f59e0b` `#3b82f6` → 全部換大黃蜂色系
- emoji：🐛→六角斜紋、❌→磚紅圓、⚠→咖啡三角、其餘全清

#### 8) 不動的部分
- `REPORT_PAGE_JS` 的分頁切換邏輯、複製功能、語言切換不動
- `reportsPage()` 資料查詢邏輯不動
- `reportsShell()` 只改 CSS 色碼

**驗收**：
- 黃底 + 黑 header + 分頁列 + Console/Info 兩個分頁完整改版
- 分享連結卡移到標題右側
- Console 有錯 → 磚紅徽章 + 自動成預設分頁
- 無舊紫色系色碼
- 端到端 0 failed

---

🔵 **DONE-432**

**改動**：`server/src/index.ts`（`reportPageHtml()` 全改寫、`REPORT_PAGE_JS` 渲染字串、`reportsShell()` CSS）、`bridge/_verify403.mjs`（+14 條護欄）

| 驗收 | 結果 |
|---|---|
| 黃底 + 黑 header + 分頁列 + Console/Info 兩分頁 | ✅ |
| 分享連結卡移到標題右側 | ✅ 節點不動，render 後 `appendChild` 進 `#share-slot` |
| Console 有錯 → 磚紅徽章 + 自動成預設分頁 | ✅ 兩條都有斷言 |
| 無舊紫色系色碼 | ✅ 21 個舊色碼掃過為 0 |
| 端到端 0 failed | ✅ **952 passed, 0 failed**（14 套）；`_verify403` 219 → **233** |
| `tsc --noEmit` | ✅ exit 0 |

**反向測試 14/14 全紅、0 漏**（把改動一條條破壞，確認斷言真的會紅）。其中兩條原本是假的、已補強：

- `.tab-badge.error` 的斷言被 `.tab-btn.active .tab-badge.error` 那條規則餵飽了 —— 把 active 態改壞照樣綠。改成錨定行首 + 另外要求 active 態那條。
- 「黃底」原本只查 `background:var(--y)` 有沒有出現，但六角標記、複製鈕都是黃的 —— 把 `body` 背景整個換掉還是綠。改成錨定 `body { background:var(--y); background-image:url("data:image/svg+xml,`。

**三件跟卡片不一致的，照 DESIGN_SPEC 走**（交付包 README：規格書 > 設計稿 > 卡片）：

1. **4xx 用 `#8A5A24`，不是卡片寫的 `#7A4E1D`。** 卡片註「§2.4 禁用 → 用 `#7A4E1D`」是把 §2.4 讀寬了 —— §2.4 的禁用**只針對深底**，原文還明講「這些是給黃底／米白底的褐色」。Network 表在 `#FFF4D6` 米白底上，`#8A5A24` 對比約 6.9:1，正是該用的地方。
2. **header 副標「Bug 報告」用 `--on-dark-2 #C9A15A`，不是卡片寫的 `#B99C63`。** 黑底上的次要文字 §2.4 只給兩階，`#B99C63` 不在其中（PM-423 起同一個處置）。
3. **時間軸沒有手動／AUTO 之分。** `TimeMarker` 沒有來源欄位，區分不出來（PM-428 回報過同一件事）。只做了實線標記，**沒有留虛線那套死 CSS**。要做的話得先在錄製端給 marker 加來源欄位。

**`reportsShell()` 的品牌 header 只改了一份。** `<header><a class="brand" ...>` 這行檔案裡有 9 份（官網頁共用），這張卡只碰 `reportsShell` 那一份，其餘 8 份留給群組 E。

**一件要講的事故**：反向測試腳本把 `server/src/index.ts` 截斷成 0 bytes 過一次 —— Python 的 `open(P,'w').write(x)` 是**先開檔（截斷）才求值 `x`**，而 `x` 那次丟了例外，未 commit 的改動就沒了。已用四支 patch 腳本從 HEAD 完整重建，跑回 233/0、`tsc` exit 0、diff 範圍確認只碰 `reportsShell` / `reportPageHtml` / `REPORT_PAGE_JS` 三處。腳本已改成「先算完字串、算完才開檔」，並加了長度下限保護。

---

### 🟡 PM-433：群組 D — 付費牆 + 找不到報告（reportsPage 錯誤頁）

**背景**：畫面 16（付費牆）+ 17（找不到報告）。這兩個是 `reportsPage()` 裡的分支，非報告主頁。

**讀取**：設計稿 `BugEzy 分享報告頁.dc.html` L297–364（畫面 16 + 17）

**目標檔案**：`server/src/index.ts` 的 `reportsPage()` 函式裡的 paywall / not-found 分支

**設計規格**：

#### 畫面 16 — 付費牆（560px）
- 黃底 `#F7BE00` + 蜂巢紋 + 黑 header（同 PM-432）
- 鎖頭圖示 66×76px：黑六角殼 + 鎖扣（`border:2.5px solid #F7BE00; border-radius:10px 10px 0 0`）+ 鎖身（`#F7BE00; border-radius:4px`）+ 鑰匙孔（`#14110B; border-radius:2px`）
- 標題 Archivo 800 21px「此報告需要會員權限才能閱讀」
- 說明 14px + 13px（兩段）
- 兩個按鈕：「免費安裝 BugEzy」主按鈕（黑底黃字 + 硬投影 `3px 3px 0 #4A2F12`）+ 「了解會員方案」次要（描邊）
- 底部提示 12px `#3A2409`

#### 畫面 17 — 找不到報告（520px）
- 黃底 + 黑 header
- 三個空蜂巢格：26×30px，外殼 `rgba(20,17,11,.45)`，內部交替 `#F7BE00` 和 `rgba(20,17,11,.28)` 形成空/滿
- 標題 Archivo 800 19px「找不到報告」
- 說明 13.5px：「連結可能打錯了，或報告已超過保留期限被自動刪除 — 免費版保留 7 天，付費版 90 天。」
- 「回 BugEzy 首頁」主按鈕（黑底黃字 + 硬投影）

#### 不動
- `reportsPage()` 的權限檢查、查詢邏輯不動
- 只改 HTML 輸出的視覺

**驗收**：
- 付費牆：黃底 + 鎖頭六角圖示 + 雙按鈕
- 找不到報告：黃底 + 三空蜂巢 + 保留期限說明
- 無舊色碼
- 端到端 0 failed
