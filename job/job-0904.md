# job-0904.md — BugEzy Day 51（2026-09-04）

## 今日任務
- 群組 D 實作（分享報告頁 4 畫面）
- 群組 E 實作（官網 11 頁）

## 設計交付包位置
`C:\dev\bugezy\BugEzy 大黃蜂色調項目概覽\design_handoff_hornet_visual_system\`

## 關鍵文件
- DESIGN_SPEC.md — 規範（唯一依據）
- README.md — 實作說明 + 畫面對照表
- BugEzy 分享報告頁.dc.html — 畫面 14~17
- BugEzy 官網改版.dc.html — 畫面 28~30

---

### 🟡 PM-432：群組 D — 分享報告頁大黃蜂改版（reportPageHtml CSS + 共用元件）

**背景**：畫面 14~15（報告頁 Console/Info 分頁），`server/src/index.ts` L4575–6100。目前全部舊紫色系（`#0f0f1a` `#1a1a2e` `#7c3aed` `#a78bfa` `#ef4444` `#238636`）。這是 PM 與工程師唯一會看到的公開介面，改版最重要的公開頁面。

**讀取**：`DESIGN_SPEC.md`、設計稿 `BugEzy 分享報告頁.dc.html` L1–295（畫面 14 + 15）

**目標檔案**：`server/src/index.ts` 的 `reportPageHtml()` 函式

**設計規格**：

#### 1) 全頁底色 + 字型
- `background:#F7BE00` + 蜂巢紋 10%（黃底頁）
- Google Fonts 外連 OK（server 端不是擴充，無 CWS 限制）：Archivo 500/600/800 + Noto Sans TC 400/500/700/900 + JetBrains Mono 400/500/700

#### 2) 黑色 header（.topbar）
- `background:#14110B; padding:13px 24px`
- 六角斜紋標記 28×32px（§5.B，三層六角 + repeating-linear-gradient）
- 字標 `BugEzy` Archivo 800 18px `#F7BE00`
- 「Bug 報告」Noto Sans TC 500 13px `#B99C63`
- 右對齊語言切換：`1.5px solid #7A4E1D; border-radius:8px; font:700 12px JetBrains Mono; color:#F7BE00`

#### 3) 標題區 + 分享連結卡（**移到標題右側**）
- 標題 Noto Sans TC 800 25px `#14110B`
- meta：URL（JetBrains Mono 連結色 `#4A2F12`）、瀏覽器/尺寸/時間 JetBrains Mono 11.5px + `rgba(74,47,18,.5)` 分隔
- 分享連結卡（原本在頁尾，移到標題右側）：`background:#7A4E1D; border-radius:12px; width:330px`，URL 欄 `#14110B` 底、複製按鈕 `#F7BE00`

#### 4) 分頁列（.tab-bar + .tab-btn）
- `border-bottom:2px solid #14110B; gap:2px`
- active 分頁：`background:#14110B; color:#F7BE00; border-radius:11px 11px 0 0`
- 非 active：`background:transparent; color:#4A2F12; border:2px solid #14110B; border-bottom:none`
- 徽章：Console 有錯 → `background:#8A2A0F; color:#FFE9AE`（磚紅），其餘 → `background:rgba(20,17,11,.16); color:#4A2F12`
- Console 有錯時自動成預設分頁（沿用原邏輯 `defaultTab`）

#### 5) Console 分頁（畫面 14）
- log 清單：`border:2px solid #14110B; border-radius:12px; overflow:hidden`
- 每列：`background:#FFF4D6; border-bottom:1px solid rgba(20,17,11,.14)`
- error 圖示：`15×15px 圓形 background:#8A2A0F`，內部橫線 `#FFF4D6`
- warn 圖示：`15×14px 三角 clip-path background:#7A4E1D`
- error 文字 `color:#8A2A0F`，warn 文字 `color:#7A4E1D`
- 時間戳 JetBrains Mono 600 11.5px `#6B5A3D`
- 「另外 N 筆」：`background:rgba(255,244,214,.55); color:#5E3A14`
- Network 錯誤表：同框，status 5xx `#8A2A0F` / 4xx `#8A5A24`（但 §2.4 禁用 → 用 `#7A4E1D`），method `#1E6FD9`（設計稿用藍色）
- Token 估算：`background:#7A4E1D; border-radius:12px`，值 JetBrains Mono `#FFE9AE`，合計 `border-top:1px solid rgba(255,233,174,.28)`，底部膠囊 `#F7BE00`

#### 6) Info 分頁（畫面 15）
- 左欄：描述框 `#FFF4D6 + 2px solid #14110B; border-radius:12px`、時間軸標記（手動=實線 `#14110B`、AUTO=虛線 `dashed rgba(20,17,11,.4)` + 咖啡時間膠囊 + AUTO 標籤）、語音記錄表
- 右欄 398px：摘要 2×3 grid（同 edit-report 那套）、網路環境（六角 + 在線狀態 + 延遲/頻寬 + 結束時變化）、儲存狀態（localStorage/sessionStorage/Cookies + 遮罩註記咖啡底）、高畫質 AI 分析（虛線框 + checkbox `accent-color:#14110B`）
- section 標題統一：JetBrains Mono 700 11px `#4A2F12` letter-spacing .1em

#### 7) 舊色碼全清
- `#0f0f1a` `#1a1a2e` `#2a2a3e` `#7c3aed` `#a78bfa` `#238636` `#ef4444` `#f59e0b` `#3b82f6` → 全部換大黃蜂色系
- emoji：🐛→六角斜紋、❌→磚紅圓、⚠→咖啡三角、其餘全清

#### 8) 不動的部分
- `REPORT_PAGE_JS` 的分頁切換邏輯、複製功能、語言切換不動
- `reportsPage()` 資料查詢邏輯不動
- `reportsShell()` 只改 CSS 色碼

**驗收**：
- 黃底 + 黑 header + 分頁列 + Console/Info 兩個分頁完整改版
- 分享連結卡移到標題右側
- Console 有錯 → 磚紅徽章 + 自動成預設分頁
- 無舊紫色系色碼
- 端到端 0 failed

---

🔵 **DONE-432**

**改動**：`server/src/index.ts`（`reportPageHtml()` 全改寫、`REPORT_PAGE_JS` 渲染字串、`reportsShell()` CSS）、`bridge/_verify403.mjs`（+14 條護欄）

| 驗收 | 結果 |
|---|---|
| 黃底 + 黑 header + 分頁列 + Console/Info 兩分頁 | ✅ |
| 分享連結卡移到標題右側 | ✅ 節點不動，render 後 `appendChild` 進 `#share-slot` |
| Console 有錯 → 磚紅徽章 + 自動成預設分頁 | ✅ 兩條都有斷言 |
| 無舊紫色系色碼 | ✅ 21 個舊色碼掃過為 0 |
| 端到端 0 failed | ✅ **952 passed, 0 failed**（14 套）；`_verify403` 219 → **233** |
| `tsc --noEmit` | ✅ exit 0 |

**反向測試 14/14 全紅、0 漏**（把改動一條條破壞，確認斷言真的會紅）。其中兩條原本是假的、已補強：

- `.tab-badge.error` 的斷言被 `.tab-btn.active .tab-badge.error` 那條規則餵飽了 —— 把 active 態改壞照樣綠。改成錨定行首 + 另外要求 active 態那條。
- 「黃底」原本只查 `background:var(--y)` 有沒有出現，但六角標記、複製鈕都是黃的 —— 把 `body` 背景整個換掉還是綠。改成錨定 `body { background:var(--y); background-image:url("data:image/svg+xml,`。

**三件跟卡片不一致的，照 DESIGN_SPEC 走**（交付包 README：規格書 > 設計稿 > 卡片）：

1. **4xx 用 `#8A5A24`，不是卡片寫的 `#7A4E1D`。** 卡片註「§2.4 禁用 → 用 `#7A4E1D`」是把 §2.4 讀寬了 —— §2.4 的禁用**只針對深底**，原文還明講「這些是給黃底／米白底的褐色」。Network 表在 `#FFF4D6` 米白底上，`#8A5A24` 對比約 6.9:1，正是該用的地方。
2. **header 副標「Bug 報告」用 `--on-dark-2 #C9A15A`，不是卡片寫的 `#B99C63`。** 黑底上的次要文字 §2.4 只給兩階，`#B99C63` 不在其中（PM-423 起同一個處置）。
3. **時間軸沒有手動／AUTO 之分。** `TimeMarker` 沒有來源欄位，區分不出來（PM-428 回報過同一件事）。只做了實線標記，**沒有留虛線那套死 CSS**。要做的話得先在錄製端給 marker 加來源欄位。

**`reportsShell()` 的品牌 header 只改了一份。** `<header><a class="brand" ...>` 這行檔案裡有 9 份（官網頁共用），這張卡只碰 `reportsShell` 那一份，其餘 8 份留給群組 E。

**一件要講的事故**：反向測試腳本把 `server/src/index.ts` 截斷成 0 bytes 過一次 —— Python 的 `open(P,'w').write(x)` 是**先開檔（截斷）才求值 `x`**，而 `x` 那次丟了例外，未 commit 的改動就沒了。已用四支 patch 腳本從 HEAD 完整重建，跑回 233/0、`tsc` exit 0、diff 範圍確認只碰 `reportsShell` / `reportPageHtml` / `REPORT_PAGE_JS` 三處。腳本已改成「先算完字串、算完才開檔」，並加了長度下限保護。

---

### 🟡 PM-433：群組 D — 付費牆 + 找不到報告（reportsPage 錯誤頁）

**背景**：畫面 16（付費牆）+ 17（找不到報告）。這兩個是 `reportsPage()` 裡的分支，非報告主頁。

**讀取**：設計稿 `BugEzy 分享報告頁.dc.html` L297–364（畫面 16 + 17）

**目標檔案**：`server/src/index.ts` 的 `reportsPage()` 函式裡的 paywall / not-found 分支

**設計規格**：

#### 畫面 16 — 付費牆（560px）
- 黃底 `#F7BE00` + 蜂巢紋 + 黑 header（同 PM-432）
- 鎖頭圖示 66×76px：黑六角殼 + 鎖扣（`border:2.5px solid #F7BE00; border-radius:10px 10px 0 0`）+ 鎖身（`#F7BE00; border-radius:4px`）+ 鑰匙孔（`#14110B; border-radius:2px`）
- 標題 Archivo 800 21px「此報告需要會員權限才能閱讀」
- 說明 14px + 13px（兩段）
- 兩個按鈕：「免費安裝 BugEzy」主按鈕（黑底黃字 + 硬投影 `3px 3px 0 #4A2F12`）+ 「了解會員方案」次要（描邊）
- 底部提示 12px `#3A2409`

#### 畫面 17 — 找不到報告（520px）
- 黃底 + 黑 header
- 三個空蜂巢格：26×30px，外殼 `rgba(20,17,11,.45)`，內部交替 `#F7BE00` 和 `rgba(20,17,11,.28)` 形成空/滿
- 標題 Archivo 800 19px「找不到報告」
- 說明 13.5px：「連結可能打錯了，或報告已超過保留期限被自動刪除 — 免費版保留 7 天，付費版 90 天。」
- 「回 BugEzy 首頁」主按鈕（黑底黃字 + 硬投影）

#### 不動
- `reportsPage()` 的權限檢查、查詢邏輯不動
- 只改 HTML 輸出的視覺

**驗收**：
- 付費牆：黃底 + 鎖頭六角圖示 + 雙按鈕
- 找不到報告：黃底 + 三空蜂巢 + 保留期限說明
- 無舊色碼
- 端到端 0 failed

---

🔵 **DONE-433**

**改動**：`server/src/index.ts`（`reportPageHtml()` 的付費牆／找不到報告 CSS、`REPORT_PAGE_JS` 的 `renderPaywall()` + 新增 `renderNotFound()`）、`bridge/_verify403.mjs`（+9 條護欄）

| 驗收 | 結果 |
|---|---|
| 付費牆：黃底 + 鎖頭六角圖示 + 雙按鈕 | ✅ 66×76 四層幾何（六角殼／鎖扣／鎖身／鑰匙孔） |
| 找不到報告：黃底 + 三空蜂巢 + 保留期限說明 | ✅ 滿／空／滿三格 26×30 |
| 無舊色碼 | ✅ |
| 端到端 0 failed | ✅ **961 passed, 0 failed**（14 套）；`_verify403` 233 → **242** |
| `tsc --noEmit` | ✅ exit 0 |

**反向測試 14/14 全紅、0 漏。**

**這兩個畫面不在 `reportsPage()` 裡。** 卡片寫「目標檔案：`reportsPage()` 裡的 paywall / not-found 分支」，實際上 `reportsPage()` 是 `/reports`「我的報告列表」，裡面兩個分支都沒有。報告頁 CSP 是 `script-src 'self'`，HTML 只是個殼、資料靠 `fetch` —— 付費牆是 403 時 `renderPaywall()` 畫的，找不到報告是 `fetch` 失敗時畫的，兩個都在 `REPORT_PAGE_JS`。改在實際的位置。

**付費牆的米白卡拿掉了**（跟 PM-432 我自己的做法不同）。PM-432 先給它一張 `.paywall` 米白卡 + 4px 硬投影；這次照設計稿畫面 16 重看，內容是**直接站在黃底蜂巢紋上**，黑 header 已經界定了頁面，沒有卡片。卡片列的元素（黃底／蜂巢／黑 header／鎖頭／標題／兩段說明／雙按鈕／底部提示）也沒提到卡片。已改成無卡，並加了斷言擋著（`.paywall` 不准有 `background:`）。

**按鈕 class 改名 `.paywall-btn` → `.state-btn`**，付費牆和找不到報告共用同一組硬投影按鈕（§7.1），舊名字沒有殘留。`.error-msg` 那條 CSS 也整條刪掉 —— 唯一用它的地方換成 `renderNotFound()` 了，**不留死 CSS**。

**保留天數有跨檔斷言盯著。** 「免費版 7 天／付費版 90 天」這段文案跟 `RETENTION_FREE_DAYS` / `RETENTION_PAID_DAYS` 綁在同一條斷言上，以後改了清理排程忘了改文案會直接紅。

**一件不在這張卡範圍、但你會想知道的**：`/report/` 後面接 10 個字以內的 id 不會走報告頁，會掉到總 fallback 回 `{"error":"not found"}` 的 JSON，看不到畫面 17。要讓打錯的短連結也看到那頁得動 routing，卡片寫明「查詢／權限邏輯不動」，所以沒碰 —— 要做的話跟我說。

### 🟡 PM-434：群組 E — 官網共用外殼 + 首頁大黃蜂改版（homePage）

**背景**：畫面 28（首頁），1280px。所有官網頁共用同一個 nav + footer，改一次全部套用。長頁面不能整頁黃，改用**黃／米白／黑交替分節**，黃色留給 hero、定價、結尾 CTA 三個決策點。

**讀取**：`DESIGN_SPEC.md`、設計稿 `BugEzy 官網改版.dc.html` L34–270（畫面 28 全頁）

**目標檔案**：`server/src/index.ts` — `homePage()` + 所有 `*Page()` 共用的 nav/footer

**設計規格**：

#### 1) 黑色 nav（所有官網頁共用）
- `background:#14110B; padding:14px 40px`
- 六角斜紋標記 26×30px（兩層，非三層）+ 字標 Archivo 800 19px `#F7BE00`
- 導覽連結：Noto Sans TC 600 13px `#C9A15A`，hover `#F7BE00`
- 語言切換：`1px solid #3A3122; border-radius:8px`，active 語言 `background:#F7BE00; color:#14110B`，其餘 `color:#C9A15A`
- 右側 CTA：`background:#F7BE00; color:#14110B; border-radius:9px; font:700 13px`
- **注意**：nav 在 `server/src/index.ts` 裡有 9 份，全部要改

#### 2) 黃底 hero（決策點 1）
- `background:#F7BE00` + 大尺寸蜂巢紋 56×98（hero 用的比一般頁面大）
- 標題 Noto Sans TC 900 52px `#14110B`
- 說明 600 18px `#3A2409`
- 主 CTA：黑底黃字 `padding:17px 30px; border-radius:13px; box-shadow:4px 4px 0 #7A4E1D`
- 次 CTA：描邊 `2px solid rgba(20,17,11,.45)`
- 底部提示：六角 9×10 + 600 13px `#3A2409`
- 右側大黃蜂：`<img>` 340px + `filter:drop-shadow(0 20px 34px rgba(74,47,18,.4))`
- 實拍蜂用 `hornet-real.png`（官網可用，不是擴充）

#### 3) 米白三步驟（內容區）
- `background:#FFF4D6; padding:64px 40px`
- 標題 800 33px + 黃色底線 52×4px
- 3 欄 grid：白卡 `border:2px solid #14110B; border-radius:16px; box-shadow:4px 4px 0 #F7BE00`
- 六角編號 44×51px 黑底 + Archivo 800 20px `#F7BE00`

#### 4) 黑底展示區
- `background:#14110B; padding:64px 40px`
- 標題 800 33px `#F7BE00`
- 三排圖文交替（奇數左圖右文、偶數反轉）
- 圖片區佔位：`border:2px solid #7A4E1D; border-radius:14px; background:#211C13`（之後放截圖）
- 文字：標題 700 22px `#F7BE00`，說明 500 14.5px `#C9A15A`

#### 5) 米白賣點兩欄
- `background:#FFF4D6`
- 2×3 grid：白卡 `border:2px solid #14110B; border-radius:12px`
- 每格：六角 11×13px `#F7BE00` + 600 14.5px 黑字

#### 6) 咖啡語言帶
- `background:#7A4E1D; padding:64px 40px`
- 標題 800 33px `#FFE9AE` + 副標 600 14px `#F0D9A8`
- 7 語言膠囊：`border-radius:999px; background:#F7BE00; color:#14110B; font:700 13.5px`

#### 7) 黃底定價（決策點 2）
- `background:#F7BE00` + 大蜂巢紋
- 3 欄 grid：免費/月費/日票
- 月費卡反黑 `background:#14110B; box-shadow:5px 5px 0 #7A4E1D`
- 「最划算」標籤：`position:absolute; top:-13px; background:#FFF4D6; border:2px solid #14110B`
- 月費價格 Archivo 800 40px `#F7BE00`，`/月` 600 13px `#8A7550`
- 免費/日票價格 800 40px `#14110B`

#### 8) 黑底結尾 CTA（決策點 3）
- `background:#14110B; padding:72px 40px`
- 標語 800 31px `#F7BE00`
- CTA `background:#F7BE00`
- 三個連結 `color:#C9A15A`

#### 9) 深黑 footer（所有官網頁共用）
- `background:#0E0C08; padding:34px 40px`
- 連結 600 12.5px `#A08B62`
- 聯絡/版權 500 11.5px `#6B5A3D`

#### 10) 舊色碼全清（nav/footer 改完會連帶清掉 10 頁的共用框架色）

**驗收**：
- 首頁：nav → hero(黃) → 三步驟(米白) → 展示(黑) → 賣點(米白) → 語言帶(咖啡) → 定價(黃) → CTA(黑) → footer(深黑) 交替分節
- 所有 11 個官網頁的 nav + footer 同步改版
- 無舊紫色系色碼
- 端到端 0 failed

---

🔵 **DONE-434**

**改動**：`server/src/index.ts`（共用外殼 `SITE_FONTS` / `SITE_CHROME_CSS` / `siteNav()` / `siteFooter()`、11 頁套用、`homePage()` 整頁改寫、`/hornet-real.png` 路由）、新增 `server/src/hornet-png.ts`、`bridge/_verify403.mjs`（+16 條護欄）

| 驗收 | 結果 |
|---|---|
| 首頁交替分節 hero(黃)→三步驟(米白)→展示(黑)→賣點(米白)→語言帶(咖啡)→定價(黃)→CTA(黑)→footer(深黑) | ✅ 有斷言鎖順序 |
| 11 個官網頁的 nav + footer 同步改版 | ✅ `siteNav` / `siteFooter` 各 11 處 |
| 無舊紫色系色碼 | ✅ 首頁 + 共用外殼皆 0 |
| 端到端 0 failed | ✅ **977 passed, 0 failed**（14 套）；`_verify403` 242 → **258** |
| `tsc --noEmit` | ✅ exit 0 |
| `wrangler deploy --dry-run` | ✅ 3729 KiB / gzip **770 KiB** |

**反向測試 17/17 全紅、0 漏。** 其中一條破壞（語言膠囊塞回 🇬🇧）本來**沒被抓到** —— 護欄的 emoji 偵測從 `\u{1F300}` 起算，國旗是區域指示符號 `\u{1F1E6}–\u{1F1FF}`，整個漏掉。已把偵測範圍補上，全 14 套重跑仍 0 failed（代表沒有別的地方偷藏國旗）。

**兩件照 DESIGN_SPEC、不照設計稿的**（§2.4 深底次要文字只有兩階）：footer 的聯絡／版權設計稿寫 `#6B5A3D`（黑底 2.1:1）→ 改 `--on-dark`；月費卡的「/月」設計稿寫 `#8A7550`（3.8:1）→ 改 `--on-dark`。兩條都有斷言擋著。

**順手修掉的兩個既有問題**：

1. **付費牆的「了解會員方案」本來指向不存在的錨點。** 站上從來沒有 `#pricing`，PM-433 那顆按鈕是死連結。這次照卡片 §7 補了定價區並掛上 `id="pricing"`，連結才真的到得了。
2. **privacy／skill／guide／changelog／feedback 的 hreflang 宣告 `LANGS_3`（含简），頁面上卻只有 zh/en 兩向切換。** 現在 nav 的語言列直接吃該頁的 `LANGS_*`，宣告與實際一致；並加了一條**跨函式斷言**，11 頁的 `siteNav(lang, LANGS_x)` 必須等於自己的 `hreflangTags(..., LANGS_x)`，以後改一邊忘了改另一邊會直接紅（PM-289 canonical 鐵則）。

**實拍蜂圖走內嵌 base64。** Worker 沒有靜態資源目錄，沿用 `icon-128.png` 那套：`server/src/hornet-png.ts` 存 base64（來源是擴充那份壓過的 71KB，不是交付包的 1.4MB），`GET /hornet-real.png` 提供。**代價是 bundle gzip 從約 700 KiB 變 770 KiB** —— 付費方案上限 10 MB 沒問題，免費方案上限 1 MB 的話這是 77%。要省下來的話可以改丟 R2（截圖已經走 R2 了），但那需要你手動上傳一次。

**兩處刻意留下設計稿沒有的東西**：結尾 CTA 的 Chrome Web Store 截圖、語言帶的語言選擇截圖。設計稿把它們拿掉了，但那是既有內容，不該在換皮的時候悄悄刪掉 —— 都改成咖啡框配深底。要拿掉跟我說。

**定價三顆按鈕都指向 Chrome Web Store**，並在下面加了一行「升級與日票都在擴充功能裡完成付款，先安裝再選方案」。因為 `/checkout` 是 POST + 需要登入 session，網站上沒辦法直接開結帳；沒有這行字，一顆寫著「升級月費」卻跳到商店的按鈕會騙人。

**沒動的**：10 個內容頁的**內文** CSS 還是舊配色（那是 PM-435／436 的範圍），這次只換了它們的 nav / footer / 字型 / 色票變數。所以現在那幾頁會是「大黃蜂 nav + 深色舊內文 + 大黃蜂 footer」的過渡樣子。

---

### 🟡 PM-435：群組 E — 內容頁大黃蜂改版（FAQ / 更新日誌 / 隱私 / 功能 / 指南 / AI 客服手冊）

**背景**：畫面 29 區塊庫 A。這 6 頁共用 PM-434 改好的 nav + footer，只需改內容區的 CSS 和 HTML。

**讀取**：設計稿 `BugEzy 官網改版.dc.html` L277–339（區塊庫 A）

**目標檔案**：`server/src/index.ts` — `faqPage()` `changelogPage()` `privacyPage()` `featuresPage()` `guidePage()` `skillPage()`

**設計規格**：

#### FAQ 手風琴
- 展開項 header `background:#F7BE00`，標題 700 15px，向下箭頭三角 `border-top:6px solid #14110B`
- 收合項 `border:2px solid rgba(20,17,11,.35); background:#fff`，向右三角
- 內容 500 13.5px `#3A2409`

#### 更新日誌版本條目
- 最新版：實線 `border:2px solid #14110B; background:#fff`
  - 版本號膠囊 `background:#14110B; color:#F7BE00; border-radius:7px`
  - 日期 JetBrains Mono 600 12px `#5E3A14`
  - 「最新」標籤 `background:#F7BE00; border-radius:999px; font:700 10.5px`
  - 每項六角 8×9px `#F7BE00` + 500 13px
- 舊版：虛線 `border:2px dashed rgba(20,17,11,.35); background:rgba(255,255,255,.6)`
  - 版本號膠囊 `background:#7A4E1D; color:#FFE9AE`

#### 隱私長文
- 白卡 `border:2px solid #14110B; border-radius:12px`
- h2 700 17px
- 內文 500 13.5px `#3A2409`
- 重點用黃色螢光筆：`background:#F7BE00; padding:1px 5px; border-radius:3px`
- 補充框：咖啡底 `#7A4E1D; border-radius:10px` + 六角 + 米白字

#### 功能 / 指南 / AI 客服手冊
- 沿用同一套白卡 + 六角 bullet + 黃色重點標記
- section 標題 JetBrains Mono 700 10.5px `#5E3A14` letter-spacing .1em
- 所有頁面底色統一 `#FFF4D6`

**驗收**：
- 6 頁全部米白底 + 白卡 + 黃色重點 + 咖啡補充框
- 無舊色碼
- 端到端 0 failed

---

🔵 **DONE-435**

**改動**：`server/src/index.ts`（新增共用 `SITE_CONTENT_CSS`、六頁的 `<style>` 全換、FAQ／更新日誌／隱私的 HTML 結構、JA/KO/VI 三本字典）、`bridge/_verify403.mjs`（+13 條護欄）

| 驗收 | 結果 |
|---|---|
| 6 頁米白底 + 白卡 + 黃色重點 + 咖啡補充框 | ✅ |
| 無舊色碼 | ✅ 六頁掃描皆 0 |
| 端到端 0 failed | ✅ **990 passed, 0 failed**（14 套）；`_verify403` 258 → **271** |
| `tsc` / `wrangler --dry-run` | ✅ exit 0 / gzip 769 KiB |

**反向測試 17/17 全紅、0 漏。**

**一條我自己寫的假斷言，抓出了真問題。** 「字典 key 有跟著改名」那條原本用 `slice(indexOf('const JA_MAP'), indexOf('function makeT'))` 取字典 —— 但 `makeT` 定義在字典**前面**，切出來是空字串，斷言永遠綠。修好切片之後它立刻紅，順著挖出更大的洞：

**`makeT('ja')` 回傳的是字典的「值」，我只清了 zh/en。** 有 **102 條** ja/ko/vi 的值還帶著 emoji（`'免費安裝' => '🔧 無料インストール'`），日／韓／越訪客看首頁、FAQ、功能頁照樣滿滿 emoji。已全部清掉，並補了一條專門盯「值」的斷言。這三本字典只服務 LANGS_6 的三頁（首頁、FAQ、功能），都已經改版完，所以清得乾淨。

**emoji 總計**：六頁 235 個 → 0；`t()` 字串改名 77 條、字典 key 同步 21 條、字典值 102 條。

**`SKILL_MD` 的 emoji 也清了，這件事要你確認。** 那份「AI 客服手冊」不只是 `/skill` 頁面，還是 `GET /skill.md` 和「複製全文」按鈕的內容 —— 使用者會把它放進自己的專案。我判斷該清：被拿掉的都是裝飾（`⭐ 最推薦 —` → `最推薦 —`、`✅ 繁體中文` → `繁體中文`），語意一個字沒少；而且手冊裡本來寫「按『🎬 錄製』按鈕」，PM-413~421 之後擴充上早就沒有那個 emoji 了，清掉反而讓手冊跟實際 UI 對得上。行數前後都是 279，沒有掉內容。**不同意的話我可以只還原這一份。**

**換 `<style>` 時掉了 13 個頁面專屬 class，已補回。** 我是整段替換 `<style>` 內容，`.mcp-box` `.warn-box` `.tips` `.mode` `.mcp-tool`（指南）和 `.tool` `.plan3 .pc/.pn/.pp/.pd/.hl`（功能）這些寫在區塊下半段的規則跟著沒了。用「body 用到的 class 是否都有對應 CSS」掃了一遍才發現，全部補上大黃蜂版本。剩下 `.mcp-cfg`（JS 選取用）和 `.badge2.active`（吃基底樣式）本來就沒有自己的規則，不是這次弄掉的。

**FAQ 手風琴沒動 JS。** 標題和內容在現有 HTML 裡是兄弟節點、不是包在一起的，所以展開態用 `.faq-q.open + .faq-a` 接，三角形改用 CSS 邊框畫（原本是 `content:'▼'`，那也算 emoji）。

**保留天數的黃色螢光筆綁了斷言**，跟 `RETENTION_FREE_DAYS` / `RETENTION_PAID_DAYS` 對得上，跟 PM-433 那條同一個路數。

**沒動的**：JA/KO/VI 還留著 39 個含 emoji 的孤兒 key（對應的頁面／字串在更早的 PM 就沒了，查不到任何 `t()` 用它們）。它們不會被讀到，這次沒去刪 —— 要清的話是獨立的一張清債卡。

---

### 🟡 PM-436：群組 E — 部落格 / 心得 / 問題回報 / 日票成功頁

**背景**：畫面 30 區塊庫 B。4 個獨立頁面。

**讀取**：設計稿 `BugEzy 官網改版.dc.html` L344–414（區塊庫 B）

**目標檔案**：`server/src/index.ts` — `blogListPage()` `blogPostPage()` `testimonialsPage()` `feedbackPage()` `dayPassSuccessPage()`

**設計規格**：

#### 部落格列表卡
- 白卡 `border:2px solid #14110B; border-radius:14px; box-shadow:4px 4px 0 #F7BE00`
- 日期 JetBrains Mono 600 11.5px `#5E3A14`
- 標籤膠囊 `background:#F7BE00; border-radius:999px; font:700 10.5px`
- 標題 800 20px
- 摘要 500 13.5px `#3A2409`

#### 部落格內文
- h2 左側 `border-left:5px solid #F7BE00; padding-left:13px`
- 內文 400 14.5px/2
- 重點同隱私頁：黃色螢光筆

#### 用戶心得引言卡
- 反黑 `background:#14110B; border-radius:14px`
- 引言 500 15px `#FFE9AE`
- 頭像六角 32×37px `background:#F7BE00` + Archivo 800 14px 首字母
- 姓名 700 13px `#F7BE00`，描述 600 11.5px `#8A7550`（改 `#A08B62`）

#### 問題回報表單
- Email + 問題類型並排：`border:2px solid #14110B; border-radius:10px`
- textarea：虛線框 `border:2px dashed rgba(20,17,11,.4); background:rgba(255,244,214,.55)`
- 送出按鈕：黑底黃字 + 硬投影 `3px 3px 0 #7A4E1D`

#### 日票啟用成功（dayPassSuccessPage）
- 黃底 `#F7BE00` + `border:2px solid #14110B; border-radius:14px`
- 三個黑六角 20×23px
- 標題 800 22px
- 說明 600 13.5px `#3A2409`
- 倒數膠囊 `background:#14110B; border-radius:999px` + 脈衝六角 8×9px + JetBrains Mono 700 12.5px `#F7BE00`

**驗收**：
- 5 個函式全部改完
- 無舊色碼
- 端到端 0 failed
