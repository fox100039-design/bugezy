# job-0821.md — BugEzy Day 48（2026-08-21）

## 今日任務
- popup 兩層架構重構
- bugezy.dev/guide MCP 教學頁

---

### 🟡 PM-403：popup 重構 — 第一層主門面

**目標**：把 popup 從單層改為兩層架構，第一層只放日常功能，乾淨不雜亂。

---

**第一層佈局**（上到下）：

```
┌──────────────────────────┐
│ 🐸 BugEzy    [用戶名]  登出│
│ ─────────────────────────│
│                          │
│  🎬 錄製   ⏪ 回溯   📸 截圖 │
│  ✨無限次   ✨無限次  ✨無限次 │
│                          │
│  📋 我的報告              │
│  📌 偵察模式 →            │  ← 進第二層
│                          │
│ ─────────────────────────│
│  📎 複製 AI 提示詞  ◀ 1/4 ▶ │
│ ─────────────────────────│
│  🎫 票券錢包（展開/收合）   │
│  🔑 安裝碼 BZ-XXXX [複製]  │
│ ─────────────────────────│
│  ⚙️ 設定 ｜ ⭐ 付費會員     │
└──────────────────────────┘
```

**改動重點**：
1. 移除第一層的圖釘清單、巡檢全部、清除全部（搬到第二層）
2. 「📌 偵察模式 →」是第二層的入口，帶箭頭暗示可點進去
3. 票券錢包預設收合（只顯示標題 + 票數），點擊展開
4. AI 提示詞保留在第一層（常用功能）
5. 設定 + 付費會員合併一行放底部

**技術實作**：
- popup.html 內用 CSS class 切換（`.page-1` / `.page-2`），不開新頁
- 用 `display: none` / `display: block` 切換，保持 state
- 切頁動畫：左右滑動（CSS transition transform）

**驗收條件**：
1. popup 打開 → 只看到第一層，乾淨簡潔
2. 沒有圖釘清單（搬到第二層了）
3. 「偵察模式 →」可點擊
4. 票券錢包可展開/收合
5. 所有第一層既有功能正常（錄製/截圖/報告/提示詞/兌換）

完成後在下方寫 `🔵 DONE-403`。

---

🔵 **DONE-403**

**做了什麼**：把圖釘那一整段從第一層搬到新的第二層（`#scoutView`），第一層只留「📌 偵察模式 →」入口。

**🔴 實作方式我選了保守的那條，理由值得說明**

卡片寫「用 CSS class 切換 `.page-1` / `.page-2`」。字面上的做法是把 `idleView` 整個拆成兩個並排的滑動面板——但 popup 的第一層掛了太多既有功能（錄製／回溯／截圖／我的報告／AI 提示詞／票券錢包／兌換／安裝碼／設定手風琴），重排它們的 DOM 結構**風險遠大於收益**，而且那些功能的顯示邏輯（`.hidden`、`showMainView`、`lockSettings`…）散在 popup.ts 各處。

改成：**把圖釘那一段整段搬到同層的新 `<section id="scoutView">`，兩者用既有的 `.hidden` 機制切換**。使用者看到的結果一樣（不開新頁、左右切換、帶動畫），但第一層既有功能的 DOM 一個字都沒動。

**動畫用 `animation` 而不是 `transition`** —— 第二層是從 `display:none` 變出來的，transition 沒有起始狀態可以過渡，只會直接跳出來。

**多做了一個小東西**：入口按鈕上有圖釘數的 badge，不進第二層也知道現在有沒有東西在盯。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 第一層乾淨簡潔 | ✅ 圖釘、巡檢、清除全部都搬走了 |
| 2 | 沒有圖釘清單 | ✅ 測試會實際檢查 `idleView` 區段內不含 `pinSection` |
| 3 | 「偵察模式 →」可點 | ✅ |
| 4 | 票券錢包可展開／收合 | ✅ 既有機制未動 |
| 5 | 第一層既有功能正常 | ✅ 六個關鍵元素逐一檢查仍在 |

---

### 🟡 PM-404：popup 重構 — 第二層偵察模式

**目標**：第二層放圖釘 + AI 監測 + 記憶矩陣，進階用戶的主戰場。

---

**第二層佈局**（上到下）：

```
┌──────────────────────────┐
│  ← 返回    📌 偵察模式     │
│ ─────────────────────────│
│                          │
│  📌 釘選模式（開始釘選）    │
│                          │
│  ── 圖釘清單（N）─────────│
│  🔴 button#submit         │
│     「按了沒反應」          │
│     [分析] [移除]          │
│  🟢 input#email           │
│     「輸入正常」           │
│     [分析] [移除]          │
│                          │
│  [🔍 巡檢全部] [🗑 清除全部] │
│                          │
│  ── 🤖 AI 監測 ──────────│
│  Zone: 5 區（🟢3 🟡1 🔴1）  │
│  Error: 3 critical        │
│  Score: 72/100            │
│  [一鍵全掃]               │
│                          │
│  ── 🧠 記憶矩陣 ─────────│
│  42 筆經驗 ｜ 7 層         │
│  [查看] [清除全部]         │
└──────────────────────────┘
```

**AI 監測區塊**：
- 資料來源：最近一次 `start_auto_detect` / `get_zone_health` 的結果
- 如果從沒跑過 → 顯示「尚未掃描，點一鍵全掃開始」
- [一鍵全掃] → 透過 `chrome.tabs.sendMessage` 呼叫 content script 執行 `start_auto_detect` 邏輯
- 結果即時更新 Zone 數、Error 數、Score

**記憶矩陣區塊**：
- 透過 bridge MCP 的 `memory_stats` 取資料（如果 bridge 沒連 → 顯示「Bridge 未連線」）
- [查看] → 展開各層摘要：
  ```
  L1 Debug 經驗（18 筆）
  L2 專案知識（8 筆）
  L4 商業邏輯（3 筆）
  L5 依賴版本（5 筆）
  L6 安全規則（2 筆）
  L7 效能基準（4 筆）
  L8 團隊備註（2 筆）
  ```
- [清除全部] → 確認對話框 → 呼叫 `memory_clear` 逐層清

**返回按鈕**：
- 「← 返回」點擊 → 滑回第一層
- 保持第二層 state（圖釘清單不重載）

**驗收條件**：
1. 「偵察模式 →」點擊 → 滑入第二層
2. 「← 返回」→ 滑回第一層
3. 圖釘清單功能完整（分析/移除/巡檢/清除）
4. AI 監測顯示 Zone + Error + Score（或「尚未掃描」）
5. 記憶矩陣顯示筆數 + 各層摘要
6. Bridge 未連線 → 記憶矩陣顯示「Bridge 未連線」
7. 所有操作不影響第一層功能

完成後在下方寫 `🔵 DONE-404`。

---

🔵 **DONE-404**

**🔴 這張卡有一個做不到的前提，我補了一條通道才做得下去**

卡片說記憶矩陣「透過 bridge MCP 的 `memory_stats` 取資料」。但 **bridge 與 Extension 之間的 WebSocket 協定是單向的**：bridge 送 `{id, command, params}`、Extension 回 `{id, ok, data}`，**Extension 沒有辦法主動問 bridge 任何事**。popup 想拿 `memory_stats` 完全無路可走。

如果照字面做，這個區塊會**永遠**顯示「Bridge 未連線」——連 bridge 明明連著的時候也一樣。那是在畫面上說謊。

所以補了一條 **Extension → bridge 的查詢通道**，並且刻意做窄：

| 設計 | 為什麼 |
|---|---|
| **硬編碼 switch 白名單**（目前只有 `memory_stats`） | 不是「查表找不到就拒絕」——新增能力**一定要有人動這段程式碼**，不會因為某個設定被改掉就多開一條路 |
| 🔴 **只放行唯讀查詢** | 這條通道打開的是「網頁旁邊的擴充功能可以呼叫 bridge」。破壞性操作（`memory_clear`）**不走這條路** |
| 未連線立刻回覆、不重試 | popup 只是要顯示一個數字，卡住比顯示「未連線」糟 |
| 5 秒逾時 | 同上 |

**因此 [清除全部] 按鈕我沒有做。** 卡片有列，但清空整層記憶是破壞性操作，走這條唯讀通道等於繞過 MCP 那邊的方案閘門與 `confirm: true` 防呆（PM-359 特地做的）。改成在區塊下方寫明：「要清除記憶請用 AI 呼叫 `memory_clear`」。**要我把它加上去的話，請先決定要不要為此放寬通道。**

---

**AI 監測：不是重寫偵測，是重跑同一組查詢**

`start_auto_detect` 本身在 bridge 裡（它是**編排**既有工具的呼叫序列，不含自己的偵測邏輯 —— PM-352 就是這樣定義的）。popup 沒有那條通道，但它編排的每一支底層工具 content script 都有。所以 `runScanAll` 依同樣順序問一遍 `map_page_zones` → `get_page_health` / `get_zone_health` / `get_browser_errors`，自己算出 Zone／Error／Score。

**沒有複製任何判定邏輯**：severity 來自 content script 已標好的欄位、score 直接用 `get_page_health` 算好的值。

結果下方固定附一句「錯誤只涵蓋最近約 30 秒；zone 為 0 時所有錯誤都會落在 Unassigned」—— **空結果的意思是「最近沒出事」，不是「沒問題」**，這句話不能省。

**記憶矩陣的三種狀態分開講**：未連線／連上但出錯／連上但 `.bugezy/` 還沒建立。混成一句的話使用者不知道要去修哪個。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 點擊滑入第二層 | ✅ |
| 2 | 返回滑回第一層 | ✅ |
| 3 | 圖釘清單功能完整 | ✅ 六個元素整段搬過來，行為未動 |
| 4 | AI 監測顯示 Zone + Error + Score | ✅ 沒掃過時顯示「尚未掃描」 |
| 5 | 記憶矩陣顯示筆數 + 各層摘要 | ✅ 透過新的唯讀通道；`memory_stats` 本來就回 `entries_per_layer` |
| 6 | Bridge 未連線 → 明確訊息 | ✅ 且與「連上但出錯」分開 |
| 7 | 不影響第一層 | ✅ |

---

### 🟡 PM-405：popup 重構 — 巡檢結果顯示優化

**目標**：巡檢結果從原始 JSON 改為人類可讀的格式。

---

**改動**：

巡檢全部 / 分析 的結果顯示：

```
原本：{"patrolled":2,"results":[{"pin_id":"pin1-msyms2n0"...

改為：
┌─────────────────────────┐
│ 📋 巡檢結果（2 個圖釘）    │
│                         │
│ 🔴 button#submit         │
│   點擊觸發 TypeError      │
│                         │
│ 🟢 input#email           │
│   輸入測試通過            │
│                         │
│ 問題：1 ｜ 正常：1         │
└─────────────────────────┘
```

分析單一圖釘：
```
┌─────────────────────────┐
│ 🔍 分析結果               │
│                         │
│ 🔴 button.btn-error      │
│ 「觸發 console.error」    │
│                         │
│ 探測：click（2012 ms）    │
│ 結果：TypeError: Cannot   │
│   read property of       │
│   undefined              │
│                         │
│ 可見：✅  可互動：✅        │
│ 位置：(120, 340)          │
└─────────────────────────┘
```

**技術**：
- popup.ts 收到分析/巡檢結果後，不直接 `JSON.stringify`
- 用一個 `renderPatrolResult(data)` / `renderAnalyzeResult(data)` 函式格式化
- 顯示在圖釘清單下方的結果區塊

**驗收條件**：
1. 巡檢全部 → 人類可讀格式（不是 JSON）
2. 單一分析 → 人類可讀格式
3. 有 error 的用紅色標示
4. 正常的用綠色標示
5. 探測類型 + 時間有顯示

完成後在下方寫 `🔵 DONE-405`。

---

🔵 **DONE-405**

**做了什麼**：巡檢與分析結果從純文字升級為結構化區塊，依嚴重度上色。

**🔴 顏色取自 content script 已經算好的 emoji，popup 不重新判定嚴重度。**

`summary` 開頭的 🔴/🟡/🟢/⚪ 是 content script 依「靜態 + 動態探測」合併判定出來的（PM-393）。popup 只負責把它對應成邊框顏色。**兩邊各算一次判定遲早會分岔** —— PM-350 的 score/summary 不同步已經吃過這個虧。

**巡檢**：每個圖釘一列（selector + 摘要 + 狀態變化），底部「問題：N ｜ 正常：M」，時間預算用完的話 note 也會顯示。

**分析**：多攤開探測類型與耗時、還原與否、可見性、尺寸。

**把 PM-393 的純文字版整個移除了**（`formatProbeLine` / `formatPatrolResult`）。它們已經沒有呼叫端，留著會讓下一個人以為還有第二條顯示路徑。全部用 DOM API 建構，沒有 `innerHTML`。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 巡檢是人類可讀（不是 JSON） | ✅ 並驗證沒有殘留的 `JSON.stringify` 顯示路徑 |
| 2 | 單一分析人類可讀 | ✅ |
| 3 | error 紅色 | ✅ `#ff6b6b` |
| 4 | 正常綠色 | ✅ `#4ade80`（黃 `#ffd600`、灰 `#9e9e9e`） |
| 5 | 探測類型 + 時間 | ✅ 另加還原狀態、可見性、尺寸 |

---

### 🟡 PM-406：bugezy.dev/guide — MCP 設定教學頁

**目標**：建立 MCP 設定教學頁，讓 MCP30 被拒的用戶知道怎麼設定。

---

**頁面內容**（`bugezy.dev/guide`）：

```
# 如何設定 MCP，讓 AI 直接讀你的 Bug 報告

## 什麼是 MCP？
MCP 是 AI 讀取外部工具的標準協議。設定好之後，你只要跟 AI 說
「請讀取我最新的 BugEzy 報告」，AI 就能直接看到完整 Bug 現場。

## 設定步驟

### Claude（推薦）
1. 打開 Claude → Settings → Connectors
2. 搜尋「BugEzy」或輸入 URL
3. 點 Connect → 完成

### ChatGPT
1. 打開 ChatGPT → Settings → Connected Apps
2. 新增 MCP 連線
3. URL 輸入你的 MCP 連線網址（在 BugEzy Extension → 進階設定 → MCP URL）

### Gemini / 其他
（待支援，敬請期待）

## 驗證是否成功
1. 跟 AI 說「請讀取我最新的 BugEzy 報告」
2. AI 回傳報告內容 → 成功！
3. 回到 BugEzy Extension → 輸入 MCP30 → 拿 30 天體驗券

## 常見問題
Q: 輸入 MCP30 顯示「請先完成 MCP 對接」？
A: 你的 AI 需要成功呼叫至少一次 BugEzy 的 MCP 工具。
   試試跟 AI 說「請讀取我最新的 BugEzy 報告」。

Q: 設定後 AI 說找不到 BugEzy？
A: 確認 MCP URL 正確，重新連線一次。
```

**部署**：加到 Cloudflare Workers 路由，或在現有官網加一頁。

**多語言**：先做繁中 + 英文，其他語言後續。

**驗收條件**：
1. `bugezy.dev/guide` 可存取
2. 內容含 Claude / ChatGPT / Gemini 設定步驟
3. 有 MCP30 領取說明
4. 有常見問題
5. 手機版排版正常

完成後在下方寫 `🔵 DONE-406`。

---

🔵 **DONE-406**

**🔴 `/guide` 早就存在，這張卡是「補內容」不是「建立」。**

現有的 `/guide`（PM-66~70、PM-280 併入 install）已經有：六種錄製模式、五種 AI 工具的 MCP 設定步驟（Claude.ai／ChatGPT／Cursor／VS Code／Zed）、30 秒快速開始、一鍵複製、繁中英雙語、sitemap 三語。**照卡片的草稿整頁重寫會是嚴重的內容倒退**，所以我只加缺的東西：

**① MCP30 領取章節**（三步驟，特別標明第 2 步「**要真的成功呼叫一次**」）

**② 🔴 `?token=` 卡關警告框** —— 這是把 PM-400 查到的東西接到使用者文件上。

原本的頁面有一段 PM-280 的腳本會在登入後自動幫 MCP 網址補上 `?token=`。問題是：**沒有 seed 過 localStorage 的使用者（沒開過「我的報告」）看到的就是不帶 token 的網址**，照著設定完，AI 讀得到報告、但**呼叫不會算在他頭上**，MCP30 永遠說「請先完成 MCP 對接」。

而且這個失敗模式**完全看不出來**——功能是好的，只有領券會失敗。所以警告框直接寫明：怎麼確認、沒補上要先點「我的報告」、或直接從擴充功能複製（那份一定帶 token）。

**③ 常見問題四題**，其中兩題正好對應 PM-398 刻意分開的兩種錯誤訊息：
- 「請先完成 MCP 對接」→ 九成是少了 token，**是你能自己修的**
- 「目前無法驗證 MCP 使用紀錄」→ **是我們這邊的問題**，不是你的設定

這兩句話在 PM-398 就是為了讓使用者分辨得出來才分開的，文件裡也照樣分開。

**④ Gemini** 照卡片說明「尚未開放通用 MCP 連接器」。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | `/guide` 可存取 | ✅ 繁中 + 英文線上實測 HTTP 200 |
| 2 | 含 Claude／ChatGPT／Gemini 步驟 | ✅（另有 Cursor／VS Code／Zed，既有內容） |
| 3 | 有 MCP30 領取說明 | ✅ |
| 4 | 有常見問題 | ✅ 四題 |
| 5 | 手機版排版正常 | ⚠ 有 `viewport` meta 且沿用既有 responsive 樣式；**實機視覺需你看一眼** |

另驗證**既有內容沒有被覆蓋掉**（六種模式／MCP 設定／快速開始／自動補 token 腳本都還在）。已 deploy `e61740a3`。

---

### 🟡 PM-407：popup + guide 端到端驗收 + 收工

**前提**：Extension 重新載入

**執行**：
1. 跑全套端到端驗收
2. 手動測試 popup 兩層切換
3. 確認 bugezy.dev/guide 可存取
4. deploy Workers（如果 guide 在 Workers 端）

**收工**：
- CHANGELOG 加 Day 48 區塊
- git commit + push

**驗收條件**：
1. 全套端到端 0 failed
2. popup 兩層切換流暢
3. 第一層功能完整
4. 第二層圖釘 + AI 監測 + 記憶矩陣正常
5. bugezy.dev/guide 可存取
6. git push 成功

完成後在下方寫 `🔵 DONE-407`。


---

🔵 **DONE-407**

**全套 766 / 0（14 套）**

| 套件 | 結果 |
|---|---|
| `_verify309` jsdom | 151 / 0 |
| `_verify392` 動態探測 | 78 / 0 |
| `_verify355` 記憶矩陣 | 81 / 0 |
| `_verify383` 手動釘選 | 71 / 0 |
| `_verify_phase1` 端到端 | 57 / 0（**瀏覽器段仍被 SKIP**） |
| `_verify367` PII 遮罩 | 47 / 0 |
| `_verify375` 安全修復 | 47 / 0 |
| **`_verify403`（本次新增）** | **47 / 0** |
| `_verify389` stderr 信號 | 43 / 0 |
| `_verify327` 終端機 | 42 / 0 |
| `server/_verify398` | 33 / 0 |
| `_verify350` 嚴重度 | 27 / 0 |
| `_verify362` 方案分層 | 23 / 0 |
| `_verify366` Origin／路徑 | 19 / 0 |

Workers 已 deploy：`e61740a3`（guide）。extension 與 bridge 都已 build。

---

**⚠ 三件我沒有辦法自己驗、需要你動手的事**

**① popup 兩層切換的實機手感**（驗收 2、3、4）
popup 不是 content script，**沒有辦法用 jsdom 抽出來跑**（它依賴 `chrome.tabs` / `chrome.storage` / `chrome.runtime`，而且整個 UI 綁在真實的 popup 生命週期上）。所以 47 項驗收裡 popup 那部分是**原始碼層面的不變式**：元素存在、事件有掛、訊息有送、既有功能沒被搬走。**動畫順不順、排版會不會擠，要你打開看一眼。**

**② 真 Chrome 端到端的瀏覽器段仍被 SKIP** —— 原因與 Day 45、47 相同且未改變：port 19850 被這個 session 的 bugezy-bridge MCP server 占用，驗收腳本刻意不去殺它。補完方式同前（設 `BUGEZY_SESSION_TOKEN` → 重新載入擴充功能 → 暫時關掉 MCP server 再跑）。

**③ 記憶矩陣區塊要看到數字，bridge 必須是新版的** —— 唯讀查詢通道**兩端都改了**（bridge 的 `extension-link.ts` + extension 的 `background.ts`）。只重新載入擴充功能而沒有重連 MCP 的話，bridge 還是舊的，那個區塊會顯示「Bridge 未連線」或逾時。**兩邊都要更新。**

---

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 全套端到端 0 failed | ✅ **766 / 0**（瀏覽器段 SKIP，見上） |
| 2 | popup 兩層切換流暢 | ⚠ 邏輯已驗，**手感需實機**（見上①） |
| 3 | 第一層功能完整 | ✅ 六個關鍵元素逐一檢查；DOM 未被重排 |
| 4 | 第二層圖釘 + AI 監測 + 記憶矩陣正常 | ✅ 邏輯已驗；記憶矩陣需 bridge 更新（見上③） |
| 5 | `bugezy.dev/guide` 可存取 | ✅ 繁中 + 英文線上實測 |
| 6 | git push 成功 | ✅ |

**收工**：CHANGELOG 加 Day 48 區塊、已 commit + push。

🔴 **extension 與 bridge 都有改動 → 要重新載入擴充功能 ＋ 重連 MCP，兩個都要。**
