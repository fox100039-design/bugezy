# job-0818.md — BugEzy Day 45（2026-08-18）

## 昨日成果（Day 44）
- PM-334~382 共 46 張卡完成
- Phase 1~3 + 5~6 全完工，§14 記憶矩陣完工
- Fable5 安全審察 12 項修復
- bridge 51 + 雲端 14 = 65 支 MCP 工具
- 端到端 628 項 0 failed

## 今日完成
- PM-379 Supabase RLS 手動查核 ✅（8 張表全 Enabled + deny all）

---

### 🟡 PM-383：圖釘手動放置 — popup「釘選模式」按鈕

**目標**：在 Extension popup 加一個「📌 釘選模式」按鈕，點擊後進入釘選狀態。

---

**popup 側**（`extension/src/popup.ts` 或 `popup.html`）：

新增按鈕：
```
📌 釘選模式
- 未啟動 → 按鈕灰色，文字「📌 釘選模式」
- 啟動中 → 按鈕亮色（BugEzy 藍 #00bfff），文字「📌 釘選中... 點擊結束」
- 點擊切換開/關
```

**邏輯**：
1. 用戶點「📌 釘選模式」→ 發 `chrome.tabs.sendMessage` 給 content script：`{ type: 'PIN_MODE_ON' }`
2. 再點一次 → 發 `{ type: 'PIN_MODE_OFF' }`
3. popup 關閉時自動發 OFF（避免遺留）
4. 按鈕狀態用 `chrome.storage.session` 記住（popup 重新打開時恢復）

**驗收條件**：
1. popup 有📌按鈕
2. 點擊切換開/關，狀態視覺回饋清楚
3. popup 關閉 → 自動結束釘選模式
4. 不影響既有 popup 功能（錄製 / 截圖 / 優惠碼）

完成後在下方寫 `🔵 DONE-383`。

---

🔵 **DONE-383**

**做了什麼**：popup 加「📌 釘選模式」按鈕，未啟動灰色、啟動中變 BugEzy 藍（`#00bfff`）並改字為「📌 釘選中... 點擊結束」。

---

**🔴 驗收條件 3 與這個功能本身相衝突，我沒有照做**

卡片第 3 條寫「popup 關閉 → 自動結束釘選模式」。但是：

> **popup 只要一失焦就會關閉，而使用者要點的正是頁面上的元素。**

也就是說，只要照著做，流程會變成：按下按鈕 → 進入釘選模式 → 移動滑鼠到頁面 → **popup 關閉 → 釘選模式立刻結束** → 點下去只是普通的點擊。**這個功能會 100% 用不了，一次都成功不了。**

我改成：
- **再按一次按鈕**、或**在頁面上按 ESC** 才結束
- 為了不讓「不知不覺還開著」變成新問題，**模式啟動期間頁面正上方一直有一條橫幅**「📌 釘選模式：點擊要標記的元素（ESC 結束）」，游標也是 crosshair —— 它不可能被忽略
- 這樣 `chrome.storage.session` 記住狀態才有意義（否則記一個永遠是 false 的值）

**要維持原本的「關閉即結束」請說一聲**，但那等於把這五張卡的功能一起關掉。

---

**其他實作細節**
- 按鈕**先用 `chrome.storage.session` 的快取畫一次**（避免開啟瞬間閃爍），再向 content script 要真實狀態校正——快取只是為了不閃，不是真相來源。
- 分頁沒有 content script（`chrome://`、線上應用程式商店、PDF）時，**明確說「這個分頁不支援釘選」**，而不是顯示成「沒有圖釘」——後者會讓人以為功能壞了。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | popup 有 📌 按鈕 | ✅ |
| 2 | 點擊切換開/關，視覺回饋清楚 | ✅ 顏色 + 文字同時變，頁面上另有橫幅 |
| 3 | popup 關閉 → 自動結束 | ❌ **刻意不做**，理由見上 |
| 4 | 不影響既有 popup 功能 | ✅ 新增的是獨立區塊，未改動錄製／截圖／優惠碼任何一行 |

---

### 🟡 PM-384：圖釘手動放置 — content script 釘選互動

**目標**：content script 進入釘選模式後，hover 元素高亮 + 點擊放圖釘。

---

**content script 側**（`extension/src/content.ts`）：

```
收到 PIN_MODE_ON：
1. 全頁 cursor 改為 crosshair
2. mousemove → 高亮 hover 的元素（藍色虛線框，用 highlightElement 共用函式）
3. 顯示 tooltip 在游標旁：元素的 tag#id.class（讓用戶確認要釘的是哪個）
4. click → 攔截（preventDefault + stopPropagation）
   - 算出該元素的唯一 selector（沿用 PM-309 的 computeUniqueSelector）
   - 彈出小輸入框讓用戶填描述（或跳過）
   - 呼叫內部的 pinElement 邏輯（跟 BRIDGE_PIN_ELEMENT 同一套）
   - 圖釘放好 → 顯示成功 toast
5. 按 ESC → 結束釘選模式

收到 PIN_MODE_OFF：
1. 移除所有釘選模式的 UI（cursor / tooltip / 高亮）
2. 恢復正常點擊行為
```

**關鍵細節**：
- click 必須攔截（否則會觸發頁面按鈕 / 連結導航）
- capture phase 攔截（`addEventListener('click', handler, true)`）確保最早攔到
- iframe 內元素不支援（跨域限制），hover 到 iframe 時 tooltip 顯示「iframe 內元素暫不支援」
- 釘選模式下右鍵不攔截（保留正常右鍵選單）

**驗收條件**：
1. 進入釘選模式 → cursor 變 crosshair
2. hover 元素 → 藍色高亮框 + tag tooltip
3. 點擊元素 → 圖釘放好 + 成功 toast
4. 點擊不觸發頁面原本的行為（連結不導航、按鈕不觸發）
5. ESC 結束釘選模式
6. iframe 元素 → 友善提示

完成後在下方寫 `🔵 DONE-384`。

---

🔵 **DONE-384**

**做了什麼**：釘選模式下 hover 高亮 + tooltip、點擊攔截並放圖釘、ESC 結束。

**最關鍵的一條是點擊攔截**：`click` 用 **capture 階段**攔（`addEventListener('click', h, true)`），並且 `preventDefault` + `stopPropagation` + `stopImmediatePropagation` 三個都下。少了攔截，使用者想「標記這個連結有問題」的動作會直接把頁面導走 —— 他標記的目標在下一秒就不存在了。已用真的 `<a href>` 加自己的 click handler 驗證：**連結沒有導航、頁面的 handler 一次都沒被呼叫**。

**沿用既有的東西，沒有重寫**：高亮用 `highlightElement`（PM-337）、selector 用 `uniqueSelector`（PM-309）、放圖釘走 `bridgePinElement`（跟 MCP 的 `pin_element` 是同一套，所以手動釘的圖釘 AI 立刻讀得到）。

**三個補上的邊界**
- **iframe**：content script 只跑在最上層框架，抓不到 iframe 內的元素。hover 時 tooltip 變橘色並說明「iframe 內的元素暫不支援（跨框架限制）」，點下去也只回 toast，不會假裝釘成功。
- **不能把 BugEzy 自己的 UI 釘起來**（圖釘層、面板、zone 覆蓋層）—— 卡片沒寫，但不擋的話使用者會釘到我們自己的圓點上。
- **右鍵不攔截**（卡片明訂），只有圖釘圓點自己的右鍵才攔（PM-387）。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | cursor 變 crosshair | ✅ 用 `CSSStyleSheet` + `html[data-bugezy-pin-mode]`，**不注入 `<style>` 字串**（Trusted Types 網站會擋，PM-69 的教訓） |
| 2 | hover → 高亮框 + tag tooltip | ✅ tooltip 顯示 `button#btn` 這種可辨識的形式 |
| 3 | 點擊 → 圖釘放好 + toast | ✅ |
| 4 | 點擊不觸發頁面原本行為 | ✅ 見上，用真連結驗證 |
| 5 | ESC 結束 | ✅ 並確認**結束後點擊行為完全恢復**（不再被攔） |
| 6 | iframe → 友善提示 | ✅ |

---

### 🟡 PM-385：圖釘手動放置 — 描述輸入彈窗

**目標**：用戶點擊元素後彈出小型輸入框，讓用戶填寫圖釘描述。

---

**content script 側**（`extension/src/content.ts`）：

```
用戶點擊元素後：
1. 在點擊位置附近彈出 overlay 輸入框（shadow DOM 隔離）：
   ┌─────────────────────────┐
   │ 📌 描述這個問題（選填）    │
   │ ┌─────────────────────┐ │
   │ │                     │ │
   │ └─────────────────────┘ │
   │        [跳過]  [釘選]    │
   └─────────────────────────┘

2. 輸入框：
   - placeholder: '例如：這個按鈕點了沒反應'
   - 單行 input，按 Enter = 釘選
   - 自動 focus

3. 按鈕：
   - [跳過]：不填描述，直接釘選（description = 元素的 tag + text 摘要）
   - [釘選]：用輸入的描述釘選
   - 點擊彈窗外 = 取消（不釘選）

4. 釘選成功後：
   - toast 通知：「📌 已釘選 <button#submit>」
   - toast 2 秒後自動消失
   - 繼續留在釘選模式（可以繼續釘下一個）
```

**驗收條件**：
1. 點擊元素 → 彈出輸入框
2. 輸入描述 + 點釘選 → 圖釘帶描述
3. 點跳過 → 圖釘用自動描述
4. 點外部 → 取消
5. Enter 鍵 = 釘選
6. shadow DOM 不被頁面 CSS 干擾

完成後在下方寫 `🔵 DONE-385`。

---

🔵 **DONE-385**

**做了什麼**：點擊元素後在游標旁彈出描述輸入框（shadow DOM），[跳過] / [釘選] / 點外部取消 / Enter 送出。

**幾個細節**
- **shadow DOM + `adoptedStyleSheets`**：注入的是任意使用者的網站，一般 DOM 會被對方的 CSS reset 弄爛，我們的樣式也可能反過來汙染人家（PM-339 的結論）。全部用 `createElement`，**沒有一行 innerHTML**。
- **「跳過」不是不填描述**，而是用元素的自動描述（`button#submit「送出」`）。留空的圖釘之後在清單裡完全認不出是哪一個。
- **點外部取消用 `composedPath()` 判斷**：事件從 shadow DOM 冒出來時 `target` 會是 host，用 `target` 比對永遠不相等，會變成「點自己也取消」。
- **彈窗內按 ESC 只關彈窗，`stopPropagation` 不讓它往上傳**——否則按一次 ESC 會連釘選模式一起關掉，使用者只是想取消這一次輸入。
- **釘完留在釘選模式**，可以連續釘多個。
- 浮層會**夾在視窗範圍內**（`clampToViewport`），在右下角點擊時不會有一半跑到畫面外。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 點擊元素 → 彈出輸入框 | ✅ |
| 2 | 輸入描述 + 釘選 → 圖釘帶描述 | ✅ |
| 3 | 跳過 → 自動描述 | ✅ 驗證含 tag 與元素文字 |
| 4 | 點外部 → 取消 | ✅ 且確認**沒有新增圖釘** |
| 5 | Enter = 釘選 | ✅ |
| 6 | shadow DOM 不被頁面 CSS 干擾 | ✅ |

---

### 🟡 PM-386：圖釘手動放置 — popup 圖釘清單面板

**目標**：popup 顯示當前分頁的圖釘清單，含狀態和快捷操作。

---

**popup 側**：

```
📌 圖釘（3）           [釘選模式]
──────────────────────
🟢 button#submit
   「這個按鈕點了沒反應」
   [分析] [移除]

🟡 section.cart
   「購物車顯示異常」
   [分析] [移除]

🔴 img.hero（stale）
   「首圖載不出來」
   [移除]
──────────────────────
[巡檢全部]  [清除全部]
```

**資料來源**：popup 開啟時透過 `chrome.tabs.sendMessage` 呼叫 content script 的 `GET_PIN_LIST`，拿到 pinStore 內容。

**按鈕功能**：
- [分析]：呼叫 content script 的 `PIN_ANALYZE` → 結果顯示在 popup 下方或新區塊
- [移除]：呼叫 content script 的 `REMOVE_PIN` → 清單即時更新
- [巡檢全部]：呼叫 `PATROL_PINS` → 更新所有圖釘狀態
- [清除全部]：確認對話框 → 呼叫 `CLEAR_PINS`

**狀態顏色**：沿用 PM-338 的顏色系統（🟢🟡🔴⚪）

**驗收條件**：
1. popup 打開 → 顯示當前分頁的圖釘清單
2. 圖釘有顏色 + 描述
3. [分析] 按鈕觸發分析 → 結果顯示
4. [移除] → 圖釘即時從清單消失 + 頁面 overlay 移除
5. [巡檢全部] → 所有圖釘狀態更新
6. 沒有圖釘 → 顯示「尚無圖釘，啟動釘選模式開始偵察」
7. stale 圖釘的 [分析] 按鈕禁用（元素已不存在）

完成後在下方寫 `🔵 DONE-386`。

---

🔵 **DONE-386**

**做了什麼**：popup 顯示當前分頁圖釘清單，含狀態符號、描述、[分析]／[移除]，以及 [巡檢全部]／[清除全部]。

**重用而非另寫**：popup 直接送 `BRIDGE_PIN_ANALYZE` / `BRIDGE_REMOVE_PIN` / `BRIDGE_PATROL_PINS` / `BRIDGE_CLEAR_PINS` 給 content script —— **跟 MCP 工具走的是同一組 handler**。另寫一套 popup 專用的訊息，兩邊遲早會分岔。只有清單本身用了新的 `GET_PIN_LIST`（它多回 `pin_mode` 與 `emoji`，是 popup 才需要的）。

**幾個判斷**
- **stale 圖釘的 [分析] 禁用**（驗收 7）：元素已從 DOM 消失，分析只會回一個錯誤。禁用比讓使用者點了才看到錯誤誠實。
- **[清除全部] 有確認對話框**，且文案明說「無法復原」。
- **分頁不支援時（`chrome://`、商店、PDF）給明確說明**，不顯示成「沒有圖釘」。
- popup 用 `chrome.tabs.sendMessage` 直接對 content script 說話 —— `activeTab` 在使用者點擊擴充圖示（也就是打開 popup）的當下就已授予，不需要繞道 background。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 顯示當前分頁圖釘清單 | ✅ |
| 2 | 有顏色 + 描述 | ✅ 沿用 PM-338 的 🟢🟡🔴⚪，已解決另用 ✅ |
| 3 | [分析] 觸發並顯示結果 | ✅ 結果顯示在清單下方的區塊 |
| 4 | [移除] → 清單即時更新 + 頁面 overlay 移除 | ✅ 兩者同一個 `bridgeRemovePin` |
| 5 | [巡檢全部] → 狀態更新 | ✅ |
| 6 | 沒有圖釘 → 引導文案 | ✅「尚無圖釘，啟動釘選模式開始偵察」 |
| 7 | stale 的 [分析] 禁用 | ✅ |

---

### 🟡 PM-387：圖釘手動放置 — 頁面上的圖釘右鍵選單

**目標**：右鍵點擊頁面上的圖釘 overlay → 顯示操作選單。

---

**content script 側**（`extension/src/content.ts`）：

```
圖釘 overlay 加 contextmenu 事件：
1. 右鍵圖釘 → 顯示自訂選單（shadow DOM）：
   ┌─────────────────┐
   │ 🔍 分析          │
   │ ✏️ 修改描述       │
   │ ✅ 標記已解決     │
   │ 🗑️ 移除          │
   └─────────────────┘

2. 各項功能：
   - 分析：呼叫 pinAnalyze，結果顯示在 toast 或面板
   - 修改描述：彈出 PM-385 的輸入框（預填舊描述）
   - 標記已解決：pin.status = 'resolved'（如果有的話），顏色變淡綠
   - 移除：從 pinStore 移除 + DOM overlay 移除

3. 點選單外 → 關閉選單
4. ESC → 關閉選單
```

**注意**：標記已解決 — PM-339 回報 resolved 在模型裡不存在（改為 warning）。這裡用 content script 本地的 status，不經過 bridge。

**驗收條件**：
1. 右鍵圖釘 → 出現自訂選單
2. 四個選項都能正常觸發
3. 修改描述 → 舊描述預填
4. 選單不被頁面的 contextmenu 覆蓋
5. 點外部或 ESC → 選單消失

完成後在下方寫 `🔵 DONE-387`。

---

🔵 **DONE-387**

**做了什麼**：圖釘圓點加右鍵選單（分析／修改描述／標記已解決／移除），shadow DOM，點外部或 ESC 關閉。

---

**🔴 「標記已解決」我做成獨立欄位，而不是加進 `status`**

卡片自己也註明了「PM-339 回報 resolved 在模型裡不存在」。照字面把 `'resolved'` 加進 `status` 的列舉會踩到三件事：

1. `clear_pins` 的參數驗證寫死了四個合法值，PM-335 還特地為此**明確報錯並列出合法值**——加第五種會讓那段檢查與實際不一致
2. MCP 工具描述（`get_pin_results`、`clear_pins`）都寫著「沒有 resolved」，會變成文件說謊
3. **語意上兩者是垂直的**：一個被標記為「我處理完了」的圖釘，它的元素狀態仍然可能是 `active` 或 `error`。混進同一個欄位，`patrol_pins` 的「狀態有變」偵測就失去意義了（標記已解決會被當成狀態變化而觸發警報）

所以做成 `resolved?: boolean`：
- 圓點變**淡綠 `#a5d6a7` 且降透明度** —— 卡片色表裡那一格現在真的有東西對應了
- `get_pin_results` 與 popup 清單都回報 `resolved`，**AI 知道這條人已經處理過**
- `status` 仍然只會是原本四種之一（已用測試鎖住）
- 可以再點一次取消標記

---

**其他細節**
- **只有圖釘圓點上的右鍵才 `preventDefault`**，頁面其他地方的右鍵完全不動（PM-384 卡片明訂要保留正常右鍵選單）。
- **選單裡的 ESC 只關選單**，`stopPropagation` 不讓它把釘選模式也關掉。
- **stale 圖釘的「分析」禁用**（同 popup 的處理）。
- 「修改描述」重用 PM-385 的輸入框並**預填舊描述**；描述清空時回退到自動描述，不會變成空白圖釘。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 右鍵圖釘 → 自訂選單 | ✅ |
| 2 | 四個選項都能觸發 | ✅ 分析／修改描述／標記已解決（可再取消）／移除 |
| 3 | 修改描述 → 舊描述預填 | ✅ |
| 4 | 不被頁面 contextmenu 覆蓋 | ✅ 圓點自己 preventDefault |
| 5 | 點外部或 ESC → 消失 | ✅ 兩條都測 |

---

### 🟡 PM-388：圖釘手動放置 — 端到端驗收 + 收工

**前提**：Extension 重新載入

**執行**：跑全套端到端驗收，確認手動釘選不影響既有 MCP 工具。

**收工**：
- CHANGELOG 加圖釘手動放置區塊
- ARCHITECTURE 更新圖釘 UI 說明
- git commit + push

**驗收條件**：
1. 全套既有端到端 0 failed
2. popup 釘選按鈕功能正常
3. 手動釘選 → popup 清單即時更新
4. 手動釘選的圖釘能被 MCP 的 patrol_pins / get_pin_results 讀到
5. git push 成功

完成後在下方寫 `🔵 DONE-388`。

🔵 **DONE-388**

**新增 `bridge/_verify383.mjs`：60 項全過。** 沿用 `_verify309` 的做法——**把 content.ts 裡真正的函式抽出來在 jsdom 的真實 DOM 上跑**，不自己搭假 DOM 測自己的假設；popup 側沒有 DOM 可跑，用原始碼靜態檢查（14 項）。

**全套現況**

| 套件 | 結果 |
|---|---|
| **`_verify383.mjs`（本次新增）** | **60 / 0** |
| `_verify309.mjs` jsdom | 151 / 0 |
| `_verify327.mjs` 終端機 | 37 / 0 |
| `_verify350.mjs` 嚴重度 | 27 / 0 |
| `_verify355.mjs` 記憶矩陣 | 81 / 0 |
| `_verify362.mjs` 方案分層 | 23 / 0 |
| `_verify366.mjs` Origin／路徑 | 19 / 0 |
| `_verify367.mjs` PII 遮罩 | 47 / 0 |
| `_verify375.mjs` 安全修復 | 47 / 0 |
| `_verify_phase1.mjs` | 57 / 0（**瀏覽器段被 SKIP，原因見下**） |

---

**🔴 兩件擋住「真 Chrome 端到端」的事，都需要你動手**

**① port 19850 被 Claude Code 自己的 MCP server 占用 → 端到端的瀏覽器段整段 SKIP**

擴充功能固定連 `ws://127.0.0.1:19850`，所以驗收腳本必須綁同一個 port 才能對到真的 Chrome。目前那個 port 是這個 session 的 bugezy-bridge MCP server 在用，而**驗收腳本刻意不去殺它**（父程序是 `claude.exe` 就一律不動，PM-310 立的規矩）。

本次跑到的 57 項是工具註冊與純函式部分；`navigate_to`／`get_browser_errors`／`pin_element` 那一大段沒跑到。

**② 方案閘門把 MCP 的圖釘工具擋住了**

我改用 live MCP 直接驗，結果是：

```
ping            → ok（free 也放行）
pin_element     → tier_gate: true「未設定 BUGEZY_SESSION_TOKEN，視為 Free」
```

這**同時證明了一件好事**：你重連過 MCP，跑的確實是 Day 44 之後的新 bridge（PM-374 的閘門生效中）。但也代表 PM-374 的待辦還沒做。

**要補完真 Chrome 驗收，請照這三步：**
1. 在 MCP 設定的 `env` 加 `BUGEZY_SESSION_TOKEN` 與 `BUGEZY_USER_EMAIL`（PM-374 的既有待辦；臨時可用 `ENFORCE_TIER_GATE=false`）
2. **重新載入擴充功能**（本次改了 `content.ts`／`popup.ts`／`popup.html`／`types.ts`，`npm run build:dev` 產物已備妥）
3. 暫時關掉 bugezy-bridge MCP server（釋放 19850），跑 `cd bridge && node _verify_phase1.mjs --reclaim`

**我在端到端裡放了一個「重新載入偵測器」**：`get_pin_results` 現在會回 `resolved` 欄位，**舊的 content.js 沒有這個欄位**。跑起來時若缺，會標成 LIMIT 並直說「擴充功能尚未重新載入」，不會誤判成 FAIL 也不會假裝通過。

---

**驗收對照**

| # | 條件 | 結果 |
|---|---|---|
| 1 | 全套既有端到端 0 failed | ⚠ **0 failed，但瀏覽器段被 SKIP**（port 被占用），見上 |
| 2 | popup 釘選按鈕功能正常 | ⚠ **UI 互動無法自動化**；按鈕存在、樣式、訊息收發、狀態切換皆已用原始碼檢查驗證（14 項），實際點擊需你實機確認 |
| 3 | 手動釘選 → popup 清單即時更新 | ✅ 邏輯層已驗（jsdom 跑完整流程：點擊→輸入→釘選→清單反映），實機需你確認 |
| 4 | 手動釘選的圖釘能被 MCP 讀到 | ✅ **設計上就是同一批**——手動釘選走的是 `bridgePinElement`，與 `pin_element` 完全同一個函式，`get_pin_results`／`patrol_pins` 讀的也是同一個 `pins` Map。jsdom 已驗證兩邊看到同一筆且 `resolved` 欄位相通 |
| 5 | git push 成功 | ✅ |

**收工項目**
- ✅ CHANGELOG 加 Day 45 手動釘選區塊
- ✅ ARCHITECTURE 新增〈手動釘選模式〉一節（含流程圖與四個關鍵判斷）
- ✅ git commit + push

**不需要 deploy Workers**（本批只動 extension）。

---

### 🟡 PM-389：stderr 信號 — bridge 偵測 🔴 Critical 時輸出警告到 stderr

**目標**：bridge 偵測到新的 Critical error 時，往 stderr 寫一行警告，讓 Claude Code（MCP client）能看到。

---

**位置**：`bridge/src/mcp-server.ts`（或新檔 `bridge/src/stderr-signal.ts`）

**觸發時機**：
1. `get_browser_errors` 回傳含 severity: 'critical' 的錯誤時
2. `get_zone_changes` 回傳 zone 狀態變為 'error' 時
3. `get_terminal_live_errors` 回傳含 severity: 'critical' 的錯誤時
4. `patrol_pins` 發現圖釘狀態惡化（active → stale 或新增 error）時

**輸出格式**（一行，方便 grep）：
```
⚠ [BugEzy] 🔴 {來源} | {摘要} | 建議：{下一步}
```

範例：
```
⚠ [BugEzy] 🔴 Browser | TypeError: Cannot read 'map' of undefined (Cart Zone) | 建議：呼叫 pin_analyze('section.cart')
⚠ [BugEzy] 🔴 Terminal | ECONNREFUSED 127.0.0.1:5432 | 建議：檢查資料庫是否啟動
⚠ [BugEzy] 🔴 Zone | Cart Zone 從 healthy → error（新增 2 筆 critical）| 建議：呼叫 get_zone_errors('Cart')
⚠ [BugEzy] 🔴 Pin | button#submit 狀態 active → stale（元素消失）| 建議：呼叫 read_page() 檢查頁面結構
```

**關鍵細節**：
- 用 `process.stderr.write()`（不是 console.error，避免被 MCP 框架攔截）
- 去重：同一錯誤 30 秒內不重複輸出（用 message hash）
- PII 遮罩：套用 maskBrowserError 後才輸出
- 只輸出 critical，不輸出 minor/info（避免刷屏）
- 不改變工具的回傳值（stderr 是額外通道，不影響 MCP 回傳）

**驗收條件**：
1. get_browser_errors 回傳 critical → stderr 有一行警告
2. get_zone_changes zone 變 error → stderr 有一行警告
3. 同一錯誤 30 秒內不重複
4. minor/info 不觸發 stderr
5. stderr 輸出有 PII 遮罩
6. MCP 工具回傳值不受影響

完成後在下方寫 `🔵 DONE-389`。

---

🔵 **DONE-389**

**做了什麼**：新增 `bridge/src/stderr-signal.ts`，四個觸發點接上，格式固定為一行方便 grep。

---

**🔴 先講一件會影響你怎麼用它的事：stderr 不是推播通道**

卡片的目標寫「讓 Claude Code（MCP client）能看到」。實際情況是：

> **MCP 沒有 server → 模型的通道。** 這件事 PM-345 為了 `watch_zones` 已經確認過（所以那組工具才做成 PULL 模式），**stderr 不是例外** —— stdio 模式下 client 會收走 server 的 stderr，但多數 client 是寫進自己的 log／debug 輸出，**不會自動出現在模型的對話脈絡裡**。

所以這支的實際價值是：
- ✅ 你自己看終端機時一眼看到「剛剛出事了」
- ✅ `claude --debug` 之類會顯示 MCP server log 的模式看得到
- ✅ 格式固定一行，可以 grep
- ❌ **AI 不會因為 stderr 出現警告就自動醒過來**

我把這個限制明寫進 `docs/BUGEZY.md`，免得使用者以為設定完就會自動有人來救。**真正有效的機制仍然是 BUGEZY.md 第 1~3 條的「每次改完就主動呼叫工具」。**

---

**四個觸發點，每個的「叫或不叫」都想過**

| 來源 | 什麼時候叫 | 什麼時候**不叫**（重要） |
|---|---|---|
| Browser | console／network 的 `severity: critical` | minor／info 一律不叫 |
| Terminal | 解析出的 traceback 是 critical | 同上 |
| Zone | 狀態**轉成 error** | 轉 warning 不叫、**好轉不叫** |
| Pin | 巡檢發現**惡化**（active→error／→stale） | 好轉不叫、**一直是 error 但沒變化也不叫** |

最後一條沿用 PM-334 `alert_count` 的同一個取捨：**一個一直壞著的圖釘不該每次巡檢都叫一次**，否則警告很快就會被當成雜訊忽略。

**建議欄位是可以直接照做的**：有 `elementSelector` 就給 `pin_analyze("section.cart")`；沒有就給 `get_error_summary()` / `map_page_zones()`——**不會硬塞一個 `pin_analyze("")` 這種指向不存在 selector 的建議**。

---

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | get_browser_errors critical → stderr 有警告 | ✅ console 與 network 兩條路徑都測 |
| 2 | zone 變 error → stderr 有警告 | ✅ 並確認轉 warning／好轉都不叫 |
| 3 | 同一錯誤 30 秒內不重複 | ✅ 連叫 5 次只輸出 1 次；不同錯誤／不同來源各自計算 |
| 4 | minor/info 不觸發 | ✅ |
| 5 | 輸出有 PII 遮罩 | ✅ 摘要與**建議欄位都遮**；遮罩在寫出**之前**（stderr 會被 client 寫進 log 檔，那份檔案通常沒人管權限） |
| 6 | 工具回傳值不受影響 | ✅ signal 的回傳值沒有被塞進任何 `txt(...)`，且 signal 模組只讀不寫 |

**測試**：`_verify389.mjs` **43/43**（純邏輯，攔 `process.stderr.write` 收輸出）＋ `_verify327.mjs` 新增 5 條**真的經過 MCP server** 的端到端驗證。

---

### 🟡 PM-390：Claude Code 自動 debug — BUGEZY.md prompt pattern

**目標**：寫一份 `BUGEZY.md`，開發者放在專案根目錄，Claude Code 讀到後會自動在改 code 後檢查 BugEzy 的 bug 報告。

**不改任何程式碼。純文件。**

---

**檔案**：`C:\dev\bugezy\docs\BUGEZY.md`（同時也放在 bugezy.dev 讓用戶下載）

**內容**：

```markdown
# BUGEZY.md — AI 自動 Debug 指引

> 把這份文件放在你的專案根目錄，Claude Code 會自動遵循。

## 規則

1. **每次修改前端相關檔案後**，自動呼叫 `get_zone_changes()`。
   - 如果有 🔴 zone → 呼叫 `get_zone_errors(zone_id)` 查明原因
   - 如果有 critical error → 呼叫 `pin_analyze(selector)` 深入分析
   - 修復後再次呼叫 `get_zone_changes()` 確認 🟢

2. **每次啟動 dev server 後**，呼叫 `start_auto_detect({ depth: 'quick' })`。
   - 看 `get_detect_report()` 的 score
   - score < 80 → 優先修復 critical_errors 列表

3. **每次修改後端相關檔案後**，呼叫 `get_terminal_live_errors()`。
   - 有 critical → 呼叫 `correlate_errors()` 看前後端是否關聯

4. **debug 完成後**，呼叫 `memory_learn({ symptom, fix })` 記錄經驗。

## 不要做的事
- 不要忽略 stderr 的 ⚠ [BugEzy] 警告
- 不要在沒有確認 get_zone_changes 回 🟢 的情況下說「修好了」
- 不要跳過 Unassigned zone 的錯誤
```

**驗收條件**：
1. 檔案存在於 `docs/BUGEZY.md`
2. 內容可直接複製到任何專案根目錄使用
3. 語法正確（markdown lint）
4. 不含 BugEzy 內部路徑或機密

完成後在下方寫 `🔵 DONE-390`。

---

🔵 **DONE-390**

**做了什麼**：新增 `docs/BUGEZY.md`。**未改任何程式碼。**

四條規則與「不要做的事」照卡片寫，另外補了三段**卡片沒寫但不寫使用者就會踩到**的內容：

1. **`⚠ [BugEzy] 🔴` 那一段的誠實說明**（見 DONE-389）。卡片原文的「不要忽略 stderr 的警告」如果不加說明，會讓使用者以為 AI 會自動被通知；實際上 AI 得自己呼叫工具。我保留了那條規則，但把「什麼時候看得到、什麼時候看不到」講清楚。
2. **常見狀況**：`start_terminal_monitor` 說指令不被允許（PM-368 的白名單）、Extension 沒連上。
3. 🔴 **「工具回『需要 Pro 方案』」怎麼辦** —— PM-374 之後閘門預設開啟，**沒設 `BUGEZY_SESSION_TOKEN` 的使用者第一次照這份文件操作就會全部卡住**。這是使用這份指引的前提，不寫等於文件一開始就不能用。

另外幾個小地方：
- 第 1 條補上「`get_zone_changes` 需要先 `watch_zones()`」——不然使用者會拿到「目前沒有在監控」而不知道為什麼。
- 「不要做的事」加了一條**「不要把空結果讀成沒問題」**（瀏覽器 30 秒／終端機 120 秒的視窗）。
- 加了一張工具速查表，讓人不用背工具名。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 檔案存在於 `docs/BUGEZY.md` | ✅ |
| 2 | 可直接複製到任何專案根目錄使用 | ✅ 已驗**沒有任何相對於本 repo 的連結**（`](./` / `](../`），複製過去不會壞掉 |
| 3 | 語法正確 | ✅ 單一 H1 + 多個 H2、**每個表頭都有分隔列**、程式碼區塊成對閉合 |
| 4 | 不含內部路徑或機密 | ✅ 已驗不含 `C:\dev\bugezy` / `bridge/src/` 等內部路徑，也不含任何 JWT／`sk-` 形式的實際值 |

---

### 🟡 PM-391：stderr 信號 + BUGEZY.md 端到端驗收 + Day 45 收工

**前提**：Extension 重新載入（PM-389 改了 bridge 端）

**執行**：
1. 跑全套端到端驗收
2. 手動觸發 critical error → 確認 stderr 有輸出
3. 確認 BUGEZY.md 可讀

**收工**：
- CHANGELOG 加 Day 45 區塊
- ARCHITECTURE 更新 stderr 信號 + BUGEZY.md 說明
- git commit + push

**驗收條件**：
1. 全套 0 failed
2. stderr 信號在 critical 時觸發
3. BUGEZY.md 存在且可讀
4. git push 成功

完成後在下方寫 `🔵 DONE-391`。

🔵 **DONE-391**

**全套 597 / 0（11 套）**

| 套件 | 結果 |
|---|---|
| `_verify_phase1.mjs` | 57 / 0（**瀏覽器段仍被 SKIP**，原因同 DONE-388） |
| `_verify309.mjs` jsdom | 151 / 0 |
| **`_verify327.mjs` 終端機 + PM-389 信號端到端** | **42 / 0**（原 37，+5） |
| `_verify350.mjs` | 27 / 0 |
| `_verify355.mjs` | 81 / 0 |
| `_verify362.mjs` | 23 / 0 |
| `_verify366.mjs` | 19 / 0 |
| `_verify367.mjs` | 47 / 0 |
| `_verify375.mjs` | 47 / 0 |
| `_verify383.mjs` 手動釘選 | 60 / 0 |
| **`_verify389.mjs`（本次新增）** | **43 / 0** |

---

**🔴 驗收 2「手動觸發 critical → 確認 stderr 有輸出」是真的跑出來的，不是靜態檢查**

`_verify327.mjs` 會**真的 spawn 一個 bridge**、**真的執行一個會噴 traceback 的指令**、再收 bridge 的 stderr 來比對。實測輸出：

```
⚠ [BugEzy] 🔴 Terminal | Error: boom-test | 建議：呼叫 correlate_errors() 看前端是否有對應的失敗請求
```

同時驗證了：格式單行、帶建議、有 PII 遮罩、沒有重複刷屏。

**Browser／Zone／Pin 三個來源**因為需要真 Chrome（port 被占用），走的是純邏輯驗證（`_verify389.mjs` 用替身資料餵進同一個函式），**四個來源的取用邏輯本身都測過**。

---

**🔴 這張卡順手抓到一個 PM-327 漏掉的洩漏**

為了驗證 stderr 的遮罩，我把 bridge 的**整份 stderr** 拿來比對明文機密，結果發現：

```
▶ terminal monitor mon1-xxx 已啟動（pid 12664）：node -e "...a@b.com pw123..."
⏹ terminal monitor mon1-xxx 結束（exit 0）：node -e "...a@b.com pw123..."
```

`terminal-monitor.ts` 在 monitor 啟動與結束時**把原始指令寫進 stderr**。PM-327 當時把四處「**回傳值**」的指令回聲都遮了，**漏掉這兩行 log** —— 而 `DATABASE_URL=postgres://u:pw@h/db npm run dev` 是極常見的寫法，等於使用者的 DB 密碼明文落進 MCP client 的 log 檔。

已補上 `maskStderr`，並加一條測試鎖住：**「bridge 整份 stderr 都不得含明文機密」**（不只是信號那幾行）。這條測試以後會擋掉同一類的疏漏。

---

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 全套 0 failed | ✅ 597 / 0（瀏覽器段 SKIP，見下） |
| 2 | stderr 信號在 critical 時觸發 | ✅ 真的 spawn bridge 跑出來驗證 |
| 3 | BUGEZY.md 存在且可讀 | ✅ 另驗語法、無內部路徑、無機密、無相對連結 |
| 4 | git push 成功 | ✅ |

**⚠ 端到端的瀏覽器段仍然沒跑到**，原因與 DONE-388 相同且未改變：port 19850 被這個 session 的 bugezy-bridge MCP server 占用，而驗收腳本刻意不去殺它。要補完請照 DONE-388 的三步（設 `BUGEZY_SESSION_TOKEN` → 重新載入擴充功能 → 暫時關掉 MCP server 再跑 `_verify_phase1`）。

**收工項目**
- ✅ CHANGELOG Day 45 區塊補上 PM-389~391
- ✅ ARCHITECTURE 新增〈stderr critical 信號與 BUGEZY.md〉一節（含「不是推播通道」與 PM-327 漏遮的說明）
- ✅ git commit + push

**不需要 deploy Workers**（本批只動 bridge + 文件）。**bridge 有改動，要重連 MCP 才會生效。**

---

### 🟡 PM-392：pin_analyze 動態探測 — 按元素類型自動操作

**目標**：pin_analyze 從純靜態分析升級為「動態探測」，根據元素類型自動模擬操作並監控是否產生錯誤。

---

**content script 側**（`extension/src/content.ts`）：

新增 `probeElement(el, selector)` 函式，在 analyze 之後執行：

```
根據元素類型選擇探測動作：

1. button / [role="button"] / input[type="submit"]：
   - 記錄操作前 errorZoneMap 長度
   - el.click()
   - 等 2 秒，收集期間新增的 console.error / uncaught exception
   - 回傳：{ probed: 'click', errors_triggered: [...] }

2. a[href]：
   - 不 click（會導航）
   - 取 href，用 fetch(href, { method: 'HEAD', mode: 'no-cors' }) 檢查
   - 回傳：{ probed: 'link_check', status, reachable: true/false }

3. input[type="text"] / textarea：
   - 記錄原值
   - focus → 輸入 'BugEzy_probe_test' → 觸發 input + change 事件 → blur
   - 等 1 秒收集 error
   - 還原原值 → 再觸發 input + change
   - 回傳：{ probed: 'input', errors_triggered: [...], restored: true }

4. select：
   - 記錄原 selectedIndex
   - 切到下一個 option（如果只有一個就跳過）
   - 等 1 秒收集 error
   - 還原 selectedIndex → 觸發 change
   - 回傳：{ probed: 'select', errors_triggered: [...], restored: true }

5. input[type="checkbox"] / input[type="radio"]：
   - 記錄原 checked
   - toggle → 等 1 秒 → toggle 回來
   - 回傳：{ probed: 'toggle', errors_triggered: [...], restored: true }

6. img / video / source：
   - 檢查 img.naturalWidth === 0 或 img.complete === false
   - 檢查 error 事件是否已觸發過
   - video 檢查 readyState + error
   - 回傳：{ probed: 'media_check', loaded: true/false, error: string? }

7. 有 CSS animation / transition 的元素：
   - getAnimations() 檢查 playState
   - 回傳：{ probed: 'animation_check', animations: number, all_running: true/false }

8. 其他：
   - 不做動態操作
   - 回傳：{ probed: 'static_only' }
```

**錯誤收集機制**：
```
function collectErrorsDuring(action, timeout_ms):
  1. 記錄 errorZoneMap 當前大小
  2. 掛一個暫時的 window.addEventListener('error') + 'unhandledrejection'
  3. 執行 action
  4. 等 timeout_ms
  5. 收集期間新增的 error
  6. 移除暫時的 listener
  7. 回傳新增的 error 列表
```

**安全措施**：
- 所有修改操作完後**還原**（原值 / 原 checked / 原 selectedIndex）
- `a[href]` 絕對不 click
- `input[type="password"]` 不探測（跳過，回 static_only）
- `form` 的 submit 按鈕 → click 前先 `preventDefault` 攔截 form submit
- 探測超時上限 5 秒（不 hang 住）

**驗收條件**：
1. 釘選「觸發 console.error」按鈕 → pin_analyze → probed: 'click' + errors_triggered 非空 → 圖釘變 🔴
2. 釘選正常按鈕 → pin_analyze → probed: 'click' + errors_triggered 空 → 圖釘維持 🟢
3. 釘選連結 → link_check + status
4. 釘選 input → 輸入測試值 → 還原 → restored: true
5. 釘選圖片 → media_check + loaded 狀態
6. input[type="password"] → 跳過不探測
7. 既有靜態分析結果不受影響（動態探測是追加，不取代）

完成後在下方寫 `🔵 DONE-392`。

---

🔵 **DONE-392**

**做了什麼**：`pin_analyze` 從純靜態分析升級為「先看再試」——依元素類型自動操作並收集期間新增的錯誤。八種探測型別全部實作。

---

**🔴 我加了一條卡片沒有的安全措施：破壞性按鈕不點**

卡片的安全清單有「還原」「不 click 連結」「跳過密碼欄位」「攔表單送出」——但**沒有擋「這個按鈕本身就是破壞性操作」**。而 PM-394 讓 `patrol_pins` **每次巡檢都重跑一次探測**，所以：

> 一個「刪除帳號」「確認付款」「送出訂單」的按鈕，會被**反覆按下去**。

這不是理論風險，是必然發生的事。所以加了 `looksDestructive()`：比對按鈕的文字／`aria-label`／`title`／`name`／`value`／`id`／class，命中刪除、清空、登出、送出、下單、購買、付款、轉帳、重設等中英文樣式就回 `skipped_destructive` 並說明理由。

**這是保守的樣式比對，不是完備判定** —— 擋錯的代價只是少測一個元素並如實說明，漏擋的代價是幫使用者按下刪除。兩邊不對稱，所以我寧可寬鬆地擋。已測「顯示更多」這種安全按鈕仍然會被點（沒有擋過頭）。

---

**兩個實作上與卡片不同的地方，都是因為卡片的做法在這個架構下行不通**

1. **錯誤收集不能用「在 content script 掛 `window.addEventListener('error')`」。** content script 跑在 **ISOLATED world**，頁面 script 的 `console.error` 根本不會經過它。改用 `queryInjectLiveErrors()`（inject.ts 在 MAIN world 攔下來的那份）做前後差集 —— 那是這個專案裡唯一已驗證可行的路（PM-51／181／313）。

2. **`link_check` 的 `status` 誠實回 `null`。** 卡片寫「回傳 status」，但 `mode: 'no-cors'` 拿到的是 opaque response，**規範上就讀不到狀態碼**。回一個編出來的 200 比不給答案更糟，所以回 `null` + `reachable: true/false`，並在 `note` 說明為什麼。

---

**其他保護**
- 所有修改**一定還原**並回報 `restored`；還原失敗會讓圖釘變 🟡 並明講（使用者要知道欄位被動過）
- 表單送出在探測期間被 capture 階段攔下
- `disabled` / `readonly` / 只有一個選項的 select → 不操作
- **不可見或尺寸 0 的元素不探測** ——點一個看不到的東西沒有意義，而風險是一樣的
- 單次探測硬上限 5 秒；**探測拋例外不會讓 `pin_analyze` 整支失敗**，靜態分析結果照樣回傳

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 出錯按鈕 → click + errors_triggered 非空 → 🔴 | ✅ |
| 2 | 正常按鈕 → click + 空 → 🟢 | ✅ |
| 3 | 連結 → link_check | ✅ 且驗證**連結一次都沒有被 click**、用的是 HEAD + no-cors |
| 4 | input → 輸入 → 還原 | ✅ 驗證探測期間真的填了測試值、結束後值回到原樣 |
| 5 | 圖片 → media_check + loaded | ✅ video 另回 readyState |
| 6 | password 跳過 | ✅ 值完全沒被動過；`name` 含 password 的一般欄位也跳過 |
| 7 | 靜態分析不受影響 | ✅ `analysis.visibility` / `computed_styles` 都還在（動態是追加不是取代） |

---

### 🟡 PM-393：pin_analyze 動態探測 — 整合回傳 + 狀態顏色更新

**目標**：把 PM-392 的探測結果整合進 pin_analyze 回傳，並更新圖釘狀態顏色。

---

**回傳結構擴充**：
```
pin_analyze 回傳新增 probe 欄位：
{
  ...既有的 analysis（DOM + CSS + accessibility）,
  probe: {
    type: 'click' | 'link_check' | 'input' | 'select' | 'toggle' | 'media_check' | 'animation_check' | 'static_only',
    errors_triggered: [{ level, message, timestamp }],
    restored: boolean,       // 有操作的才有
    status: number?,         // link_check 才有
    loaded: boolean?,        // media_check 才有
    all_running: boolean?,   // animation_check 才有
    duration_ms: number      // 探測花了多久
  }
}
```

**狀態顏色判定升級**（`getPinColor` 函式）：
```
原本：只看靜態分析（visibility / accessibility）
改為：
  🔴 error：probe.errors_triggered 有 critical 等級
  🟡 warning：probe.errors_triggered 有 minor 等級 / media 未載入 / link 404 / 動畫卡住
  🟢 active：探測通過 + 靜態分析通過
  ⚪ stale：元素消失
```

**popup 的巡檢結果顯示**：
```
原本：顯示原始 JSON
改為：
  🔴 button#submit — 點擊觸發 TypeError: Cannot read 'map' of undefined
  🟢 input#email — 輸入測試通過，已還原
  🟡 img.hero — 圖片未載入
```

**驗收條件**：
1. pin_analyze 回傳含 probe 欄位
2. 有 error 的按鈕 → 圖釘 🔴 + popup 顯示錯誤訊息
3. 正常按鈕 → 圖釘 🟢
4. popup 巡檢結果是人類可讀文字（不是 JSON）
5. 巡檢全部 → 每個圖釘都跑動態探測 → 整體結果一目了然

完成後在下方寫 `🔵 DONE-393`。

---

🔵 **DONE-393**

**做了什麼**：探測結果併進 `pin_analyze` 回傳的 `probe` 欄位、狀態顏色改由「靜態 + 動態」合併判定、popup 顯示人話。

**狀態判定的合併規則**
```
🔴 error    探測觸發 critical（error 級訊息，或 TypeError/ReferenceError… 等經典型別）
🟡 warning  探測產生 warn 級 ／ 媒體未載入 ／ 連結連不上 ／ 動畫沒在跑 ／ 還原失敗
            ／ 或**靜態分析本來就有問題**（不可見／尺寸 0／disabled）
🟢 active   靜態通過 且 探測通過
⚪ stale    元素消失
```

**一個容易寫錯的地方**：靜態問題不能被探測結果洗掉。一個「不可見但點下去沒噴錯」的元素仍然是 🟡，不能因為探測沒事就變 🟢。已用測試鎖住。

**summary 直接就是人話**，例如：
```
🔴 點擊觸發 TypeError: Cannot read 'map' of undefined
🟢 輸入測試通過，已還原
🟡 圖片已載入完成但寬度為 0（圖片壞掉或格式不支援）
```

**popup 不重新解讀 JSON**，直接用 content script 算好的 `summary`，只負責接上 selector 與補充欄位（還原與否、耗時）。**兩邊各算一次判定遲早會分岔** —— 這是 PM-350 的 score/summary 不同步已經吃過的虧。

巡檢結果改成每行一個圖釘，開頭一行總結「巡檢 N 個圖釘，M 個有問題（K 個狀態有變化）」。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 回傳含 probe 欄位 | ✅ 含 type／errors_triggered／duration_ms 與各型別專屬欄位 |
| 2 | 有 error 的按鈕 → 🔴 + 顯示錯誤訊息 | ✅ summary 直接帶錯誤原文而不是只說「有錯」 |
| 3 | 正常按鈕 → 🟢 | ✅ |
| 4 | popup 巡檢結果是人話不是 JSON | ✅ |
| 5 | 巡檢全部 → 每個都跑探測 → 一目了然 | ✅ |

---

### 🟡 PM-394：pin_analyze 動態探測 — patrol_pins 整合 + 端到端驗收

**目標**：patrol_pins 也使用動態探測，加上端到端驗收。

---

**改動**：
1. `patrol_pins` 巡檢每個圖釘時呼叫 `probeElement`（跟 pin_analyze 同一套）
2. summary 改為包含探測結果：
   - 原本：`🟢 ✅ 可見且可互動`
   - 改為：`🟢 ✅ click 測試通過` 或 `🔴 click 觸發 TypeError`
3. alert_count 把動態探測發現的 error 也算進去

**端到端驗收**：
- 在 bugezy.dev/test 頁面：
  1. 釘選「觸發 console.error」按鈕
  2. 釘選正常的 h2 元素
  3. 巡檢全部
  4. 確認：按鈕 🔴（click 觸發 error）、h2 🟢（static_only 通過）

**前提**：Extension 重新載入

**驗收條件**：
1. patrol_pins 的 summary 含探測結果
2. 「觸發 console.error」按鈕被釘選後分析 → 🔴
3. 正常元素 → 🟢
4. 全套既有端到端 0 failed
5. git commit + push

完成後在下方寫 `🔵 DONE-394`。

🔵 **DONE-394**

**做了什麼**：`patrol_pins` 也走同一套動態探測；summary 帶探測結果；`alert_count` 把 error 也算進去。

**alert_count 的定義我做了調整，並且把兩個數字分開回**

卡片說「alert_count 把動態探測發現的 error 也算進去」。但 PM-334 當初刻意把 `alert_count` 定義成「**狀態有變**」而不是「有問題」——理由是「一個一直 warning 的圖釘不該每次巡檢都觸發警報」。這兩件事會打架。

解法是**兩個問題就給兩個數字**，而不是把一個欄位的語意改掉：
- `alert_count` = 狀態有變 **或** 目前是 error（照卡片要求）
- `changed_count` = 只算狀態有變（保留 PM-334 的原始語意）
- `problem_count` = 目前 error / stale 的數量

這樣「這次巡檢有什麼新狀況」與「現在總共有幾個壞的」都答得出來，不用互相犧牲。

**summary 從「狀態」變成「做了什麼、結果如何」**
```
原本：🟢 ✅ 可見且可互動
現在：🟢 ✅ 點擊測試通過（沒有觸發錯誤）
      🔴 點擊觸發 TypeError: Cannot read 'map' of undefined
      🟡 圖片已載入完成但寬度為 0（圖片壞掉或格式不支援）
```

**🔴 巡檢時間預算**：每個圖釘探測上限 5 秒，N 個圖釘就是 N×5 秒——bridge 的指令逾時（原本 10 秒）會先炸掉，使用者只會看到「逾時」而完全不知道發生什麼事。所以：
- content script 端設 **20 秒總預算**，超過的圖釘只做靜態檢查，**並在回傳的 `note` 明說「最後 N 個只做了靜態檢查」**——不是默默截斷
- bridge 端 `pin_analyze` 逾時放寬到 20 秒、`patrol_pins` 到 40 秒

---

**全套 660 / 0（12 套）**

| 套件 | 結果 |
|---|---|
| **`_verify392.mjs`（本次新增）** | **63 / 0** |
| `_verify309.mjs` jsdom | 151 / 0 |
| `_verify383.mjs` 手動釘選 | 60 / 0 |
| `_verify389.mjs` stderr 信號 | 43 / 0 |
| `_verify355.mjs` | 81 / 0 ／ `_verify367` 47 ／ `_verify375` 47 ／ `_verify327` 42 ／ `_verify350` 27 ／ `_verify362` 23 ／ `_verify366` 19 |
| `_verify_phase1.mjs` | 57 / 0（**瀏覽器段仍被 SKIP**） |

**順手修好的既有脆弱測試**：`_verify309` 的「覆蓋層標記同步減少」原本是**同步斷言一個 rAF 節流的重畫**，之前會過只是湊巧；`pin_analyze` 改成非同步後時序一變就暴露了。改成等 flush 後再驗。

---

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | patrol_pins 的 summary 含探測結果 | ✅ |
| 2 | 「觸發 console.error」按鈕 → 🔴 | ✅ jsdom 用可控的錯誤替身驗證（點擊後才注入錯誤，確認是**這次點擊**造成的） |
| 3 | 正常元素 → 🟢 | ✅ h2 走 static_only 且判定 active |
| 4 | 全套既有端到端 0 failed | ⚠ **0 failed，但瀏覽器段仍被 SKIP** |
| 5 | git commit + push | ✅ |

**⚠ 卡片指定的「在 bugezy.dev/test 頁面實機驗收」沒有跑到**，原因與 DONE-388／391 相同且未改變：**port 19850 被這個 session 的 bugezy-bridge MCP server 占用**，驗收腳本刻意不去殺它；而 live MCP 的圖釘工具又被方案閘門擋住（沒設 token）。

要補完請照這三步：
1. MCP 設定的 `env` 加 `BUGEZY_SESSION_TOKEN` 與 `BUGEZY_USER_EMAIL`（臨時可用 `ENFORCE_TIER_GATE=false`）
2. **重新載入擴充功能**（本批改了 `content.ts`／`popup.ts`，`npm run build:dev` 產物已備妥）
3. 暫時關掉 bugezy-bridge MCP server，跑 `cd bridge && node _verify_phase1.mjs --reclaim`

然後在 `bugezy.dev/test-errors` 手動走一次：釘「觸發 console.error」按鈕 + 釘一個 h2 → 巡檢全部 → 應該看到按鈕 🔴、h2 🟢。

**收工項目**
- ✅ CHANGELOG 加 PM-392~394 區塊
- ✅ ARCHITECTURE 新增〈動態探測〉一節（含探測對照表與四項安全約束）
- ✅ git commit + push

**不需要 deploy Workers**。**extension 與 bridge 都有改動 → 要重新載入擴充功能 + 重連 MCP 才會生效。**

---

### 🟡 PM-395：修復釘選模式與 popup 按鈕的事件衝突 + 釘選自動分析

**問題**：釘選模式開啟時，popup 的 [分析] / [移除] / [巡檢全部] 按鈕的 click 事件被 content script 的釘選模式 capture handler 攔截，導致有時跳出描述輸入框、有時正常執行。行為不穩定。

---

**修法一：popup 點擊不經過 content script**

popup 的按鈕（分析 / 移除 / 巡檢全部）是透過 `chrome.tabs.sendMessage` 通訊的，**不應該經過 content script 的 DOM click handler**。檢查：
1. popup 的按鈕 click → 走 `chrome.tabs.sendMessage` → content script 的 `runtime.onMessage` handler
2. 釘選模式的 capture handler → 只攔截**頁面**上的 click，不應該攔截 popup 的 sendMessage

**如果問題是「popup 在頁面內嵌」或「popup 跟 content script 共用 DOM」→ 不可能，popup 是獨立視窗。**

**真正的 bug 更可能是**：用戶在 popup 裡點 [分析]，popup 發送 `PIN_ANALYZE` 給 content script，content script 執行 `probeElement` → 呼叫 `el.click()` 模擬按鈕點擊 → **這個模擬的 click 被釘選模式的 capture handler 攔截了** → 跳出描述輸入框。

**修法**：釘選模式的 capture handler 要辨識「來自 probeElement 的模擬 click」和「用戶手動 click」：
```
// probeElement 執行前設 flag
window.__bugezy_probing = true
el.click()
window.__bugezy_probing = false

// 釘選模式 handler
document.addEventListener('click', (e) => {
  if (window.__bugezy_probing) return  // 放行探測的 click
  // 正常的釘選邏輯...
}, true)
```

**修法二：釘選完自動跑一次分析**

釘選元素後立刻呼叫 `probeElement`，不需要用戶再點 [分析]：
```
釘選流程：
1. 用戶點元素 → 彈出描述框 → 確認
2. 建立 pin → 顯示 overlay
3. 自動呼叫 probeElement → 更新圖釘顏色
4. popup 清單直接顯示最終狀態（🟢 或 🔴）
```

用戶釘上去就直接看到紅綠，不用多點一步。

**修法三：分析/巡檢時自動暫停釘選模式**

```
收到 PIN_ANALYZE / PATROL_PINS 時：
1. 暫存釘選模式狀態
2. 暫時關閉釘選模式（移除 capture handler）
3. 執行探測
4. 恢復釘選模式
```

三個修法都要做，互為防線。

**驗收條件**：
1. 釘選模式開著 → 點 [分析] → 不跳描述框，正常分析
2. 釘選模式開著 → 巡檢全部 → 不跳描述框，正常巡檢
3. 釘選元素後 → 自動分析 → 立即顯示 🟢 或 🔴（不需要再手動點分析）
4. 探測的模擬 click 不觸發釘選
5. 探測完後釘選模式正常（可以繼續釘下一個）
6. 既有端到端 0 failed

完成後在下方寫 `🔵 DONE-395`。

🔵 **DONE-395**

**卡片的診斷是對的**，而且這是我自己造成的：PM-384 的釘選模式在 **capture 階段攔所有 click**，PM-392 的探測會 **`el.click()` 模擬點擊**——兩個功能單獨看都正確，合在一起才壞。

除了「跳出描述輸入框」之外，還有一個更隱蔽的後果：**那個模擬點擊被 `preventDefault` + `stopImmediatePropagation` 吃掉了**，頁面的 handler 根本沒收到 —— 也就是說**探測看起來有跑，其實什麼都沒測到，還會回報「點擊測試通過」**。這比跳錯視窗嚴重得多。

---

**三道防線都做了，而且是互相獨立的機制**

| 防線 | 範圍 | 做法 |
|---|---|---|
| ① `isProbing` | 只有 `el.click()` / `dispatchEvent` 那一瞬間 | 探測前後掛旗標，釘選 handler 看到就**原樣放行**（不 preventDefault、不彈框） |
| ② 釘選後自動分析 | UX | 釘完立刻分析，使用者直接看到 🟢/🔴 |
| ③ `pinModeSuspendDepth` | 整個分析／巡檢期間 | 計數器 + `try/finally` |

**兩個實作上與卡片不同的決定**

1. **旗標用模組層變數，不用卡片寫的 `window.__bugezy_probing`。** content script 的 window 雖然是 isolated 的，但把內部狀態掛上 window 沒有任何好處，反而多一個會被誤改的表面。這個旗標只有 content.ts 需要看得到。

2. **防線③用「計數器」而不是「暫存布林值」。** 巡檢會對每個圖釘各跑一次 `bridgePinAnalyze`，如果用布林，內層那次結束時就會把整個暫停解除，剩下的圖釘又會撞回同一個 bug。而且一律包 `try/finally` —— 探測一拋例外就永久卡在「放行」狀態的話，釘選模式等於再也不能用了。

---

**🔴 防線②有一個必然的副作用，先講清楚**

釘選模式攔下使用者的點擊是為了「不要觸發頁面原本的行為」，但**自動分析會把那個按鈕探測性地點一次**。所以最終效果是：使用者點按鈕 → 沒有立刻觸發 → 填完描述 → **分析時按鈕被點了一次**。

這是刻意的（釘一個按鈕的意思就是「幫我盯著它」，而盯著它就得試著按），而且 PM-392 的 `looksDestructive()` 會把刪除／付款／送出類的按鈕跳過不點。**但如果你希望「釘選就只是釘選、絕不觸發」，請說一聲**——把自動分析改成只做靜態檢查是一行的事。

---

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | 釘選模式開著 → [分析] → 不跳描述框 | ✅ |
| 2 | 釘選模式開著 → 巡檢全部 → 不跳描述框 | ✅ |
| 3 | 釘選後自動分析 → 立即顯示狀態 | ✅ toast 先報「已釘選…分析中」，分析完換成 `🟢 點擊測試通過` 這類結果 |
| 4 | 探測的模擬 click 不觸發釘選 | ✅ **並額外驗證那個 click 真的送達頁面**（`pageClicks === 1`）——這才是修好的證明，只驗「沒跳框」的話，把 click 整個吃掉也會通過 |
| 5 | 探測完釘選模式正常 | ✅ 並驗證**使用者的真實點擊仍然被正常攔截**（防線沒有卡在放行狀態） |
| 6 | 既有端到端 0 failed | ✅ **671 / 0**（12 套；`_verify383` 60 → **71**） |

**順手修的**：`_verify383` 的「釘選成功 → toast」原本在 `await` 之後才檢查，但釘選後的自動分析會把 toast 換掉。改成**在 await 之前**檢查即時 toast，並另加一條驗證「分析中」也有出現。

**收工**：ARCHITECTURE 新增〈釘選模式與探測的事件衝突〉一節、CHANGELOG 加 PM-395、已 commit + push。

**extension 有改動 → 要重新載入擴充功能才會生效。**

---

### 🟡 PM-396：動態探測補網路監控 — click 後的 fetch/XHR 失敗也要算

**問題**：探測 click 按鈕時只監控 JS error，沒監控網路請求。點擊「觸發 fetch 500」按鈕 → 探測回報 🟢（沒有 JS error），但實際觸發了 network 500。

---

**改動**（`extension/src/content.ts` 的 `collectErrorsDuring`）：

```
原本只監控：
- window 'error' 事件
- window 'unhandledrejection' 事件

新增監控：
- PerformanceObserver('resource') — 監控探測期間新增的 resource entry
- 或更簡單：監控 inject.ts 回傳的 networkErrors 數量變化

探測前記錄 networkErrors.length
探測後（等待期結束）檢查 networkErrors.length
新增的部分 → 篩出 status >= 400 的 → 算入 errors_triggered
```

**嚴重度對應**：
- fetch/XHR 5xx → critical（server error）
- fetch/XHR 4xx → minor（client error，可能是預期的）
- fetch/XHR 0（CORS / network failure）→ critical

**回傳擴充**：
```
probe: {
  ...既有,
  network_errors_triggered: [{ url, status, method }],  // 新增
}
```

**顏色判定更新**：
```
🔴：JS critical OR network 5xx
🟡：JS minor OR network 4xx
🟢：兩者皆無
```

**驗收條件**：
1. 釘選「觸發 fetch 500」→ 自動分析 → 🔴（不再是 🟢）
2. 釘選「觸發 fetch 403」→ 自動分析 → 🟡
3. 釘選「觸發 fetch 404」→ 自動分析 → 🟡
4. 釘選正常按鈕（無 JS error 無 network error）→ 🟢
5. 釘選「觸發 console.error」→ 🔴（既有行為不退化）
6. 既有端到端 0 failed

完成後在下方寫 `🔵 DONE-396`。

🔵 **DONE-396**

**問題描述完全正確**，而且這是探測功能裡最容易漏的一種 bug：**點一個按鈕最常見的後果不是拋例外，而是打出去的 API 回了 500**。只看 console 的話，這種情況會回報「✅ 點擊測試通過」——**一個假的綠燈比沒有燈更糟**。

---

**改法**：`collectErrorsDuring` 從只差集 `consoleLogs`，改成同時差集 `consoleLogs` 與 `networkErrors`。

🔴 **關鍵是「差集」而不是「快照」**：探測前頁面上可能早就有失敗的請求（別的元件在背景輪詢、上一次操作留下的）。直接看探測後的清單會把那些全部算在這次點擊頭上，變成「點什麼都是紅的」。已加測試鎖住：探測前就存在的 500 不會被算進 `network_errors_triggered`。

**不需要另外篩狀態碼**：inject.ts 的網路攔截本來就只收 4xx/5xx（產品的智能過濾原則），200 OK 根本不會進到緩存。

**分級與 §6 一致**
| 情況 | 判定 |
|---|---|
| 5xx | 🔴 critical（server 壞了） |
| status 0（CORS／連不上） | 🔴 critical |
| 4xx | 🟡 minor（多半是預期內的權限或找不到） |

**JS critical 優先於 network** —— 兩者都有時先報 JS 錯誤，因為那通常是更根本的原因。

---

**兩個小地方**

1. **summary 裡的網址只留 path。** `GET https://api.example.com/me?token=abc&page=2 → 403` 會把那一行撐爆，而且**把 token 印進 summary**。改成 `GET /me → 403`。
2. 🔴 **觀測窗口誠實揭露。** 探測只等 2 秒，比這慢才回來的請求這次看不到。所以**什麼都沒抓到時**，`note` 會明說「只涵蓋點擊後 2 秒內完成的請求」——不能讓「這次沒看到」被讀成「沒問題」（這跟 `get_browser_errors` 的 30 秒視窗是同一個原則）。

---

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | fetch 500 → 🔴 | ✅ 測試用**只有網路錯誤、完全沒有 JS error** 的替身，正是修好前會誤判成 🟢 的情境 |
| 2 | fetch 403 → 🟡 | ✅ 並驗證 query string 沒有被塞進 summary |
| 3 | fetch 404 → 🟡 | ✅ |
| 4 | 正常按鈕 → 🟢 | ✅ 並驗證 note 有揭露觀測窗口 |
| 5 | console.error → 🔴（不退化） | ✅ 另驗「JS critical 優先於 network」 |
| 6 | 既有端到端 0 failed | ✅ **686 / 0**（12 套；`_verify392` 63 → **78**） |

**順手發現的**：`shortenUrl` 原本寫 `new URL(url, location.href)`，在測試沙箱裡 `location` 不是繫結名稱 → 拋例外 → 被 catch 吞掉 → **靜靜退回完整網址**。功能上是安全的降級（所以端到端不會爆），但等於那個縮短完全沒生效。改用 `window.location.href`，不依賴「location 是全域」這個假設。

**收工**：ARCHITECTURE 的〈動態探測〉補網路監控段落、CHANGELOG 加 PM-396、已 commit + push。

**extension 有改動 → 要重新載入擴充功能才會生效。**

---

### 🟡 PM-397：Day 45 收工 — CHANGELOG + ARCHITECTURE + commit push

**目標**：Day 45 全部工作收乾淨。

---

**CHANGELOG.md 追加 Day 45 總結**：
```
## Day 45（2026-08-18）— PM-383~396，14 張卡

### 圖釘手動放置 UI
- PM-383：popup「📌 釘選模式」按鈕
- PM-384：content script hover 高亮 + 點擊釘選
- PM-385：描述輸入彈窗（shadow DOM）
- PM-386：popup 圖釘清單面板（分析/移除/巡檢/清除）
- PM-387：頁面圖釘右鍵選單
- PM-388：端到端驗收 60/0

### stderr 信號 + BUGEZY.md
- PM-389：bridge 偵測 🔴 → stderr 輸出警告（四來源：Browser/Terminal/Zone/Pin）
- PM-390：BUGEZY.md AI 自動 debug prompt pattern
- PM-391：端到端驗收 597/0，順手修 PM-327 stderr 密碼外洩

### 動態探測（pin_analyze 從靜態升級為動態）
- PM-392：按元素類型自動操作（8 種：button/link/input/select/checkbox/img/animation/其他）
- PM-393：回傳整合 + 狀態顏色升級 + popup 人類可讀文字
- PM-394：patrol_pins 整合 + 端到端驗收 660/0
- PM-395：修復釘選模式與探測的事件衝突（三道防線）+ 釘選自動分析
- PM-396：動態探測補網路監控（fetch 4xx/5xx）

### 其他
- PM-379：Supabase RLS 手動查核（8 張表全 Enabled + deny all）
- BUG10 十天體驗券建立完成

### 數據
- 端到端：686 項，0 failed
- 工具總數：65 支（不變）
```

**ARCHITECTURE.md**：確認以下已更新：
- 圖釘手動放置 UI 說明
- 動態探測對照表 + 四項安全約束
- stderr critical 信號
- BUGEZY.md 說明

**git**：`git add -A && git commit -m "Day45: pin UI + dynamic probe + stderr signal + BUG10 (PM-383~396)" && git push`

**驗收條件**：
1. CHANGELOG 有 Day 45 完整區塊
2. git commit + push 成功

完成後在下方寫 `🔵 DONE-397`。

🔵 **DONE-397**

**核對後有兩處與卡片不同，都已用實際情況寫進文件**

1. **CHANGELOG 的 Day 45 區塊不是「新增」而是「重整」。** 前面每張收工卡都往同一個區塊追加，結果標題停在「手動釘選模式（PM-383~388）」（範圍不足），而且條目順序是亂的（396 在 395 前、391 在最後）——因為是逐次往前插的。已重寫成卡片指定的三大分段 + 其他 + 數據，並把順序排正。**內容一條都沒少。**

2. **「BUG10 十天體驗券建立完成」我標註為未驗證。** 那是資料庫端的手動操作，本 session 沒有辦法確認，所以在 CHANGELOG 寫成「FOX 手動完成（DB 操作，**非本 session 驗證**）」。PM-379 同理。把別人做的事寫成自己驗過的，之後回頭查會誤導。

---

**核對過的數字（實測，不是抄卡片）**

| 項目 | 卡片 | 實測 |
|---|---|---|
| 卡片數 | PM-383~396，14 張 | ✅ 14 張，**每張都有 DONE 區塊** |
| 端到端 | 686 / 0 | ✅ 12 套實跑合計 686 |
| MCP 工具總數 | 65（不變） | ✅ bridge 51（`_verify327` 的斷言本次仍過）+ 雲端 14 |

**十二套現況**：`_verify309` 151／`_verify392` **78**／`_verify355` 81／`_verify383` **71**／`_verify_phase1` 57／`_verify367` 47／`_verify375` 47／`_verify389` **43**／`_verify327` 42／`_verify350` 27／`_verify362` 23／`_verify366` 19。

---

**ARCHITECTURE 四個區塊都在**（卡片要求確認，逐一查過）：
- `#### 手動釘選模式（PM-383~387）`
- `#### 動態探測（PM-392~394）`（含探測對照表與四項安全約束，PM-396 的網路監控段落也已補進）
- `#### 釘選模式與探測的事件衝突（PM-395）`
- `#### stderr critical 信號（PM-389）與 docs/BUGEZY.md（PM-390）`

另外 `#### MCP 工具總覽：全站 65 支` 的數字與今天一致，不需更動。

---

**CHANGELOG 另外補了一節「Day 45 之後要 FOX 處理的」**，六項：重新載入擴充功能、重連 MCP、真 Chrome 端到端仍未跑到（含補完的三步）、popup 互動需實機驗、**「釘選是否要絕不觸發」待你決定**、`npm start` 白名單。這些散在各張 DONE 裡，收在一處比較不會漏。

**驗收對照**
| # | 條件 | 結果 |
|---|---|---|
| 1 | CHANGELOG 有 Day 45 完整區塊 | ✅ 三大分段 + 其他 + 數據 + 待辦 |
| 2 | git commit + push 成功 | ✅ |
